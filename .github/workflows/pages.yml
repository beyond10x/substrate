# Atlas-classified former Pages workflow: repository-owned validation only; deployment authority is forbidden.
# Builds and audits the self-contained public Docusaurus source; Website owns public delivery.
name: Documentation validation

on:
  push:
    branches: [main]
    paths:
      - .github/workflows/pages.yml
      - website/**
  # This job is a required check, so every pull request must create its context.
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: pages-${{ github.ref }}
  cancel-in-progress: false

jobs:
  build:
    name: Build Docusaurus
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      contents: read

    steps:
      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false

      - name: Set up Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: website/package-lock.json

      - name: Install locked dependencies
        working-directory: website
        run: npm ci --ignore-scripts

      - name: Type-check website
        working-directory: website
        run: npm run typecheck

      - name: Audit website dependencies
        working-directory: website
        run: npm audit --audit-level=moderate

      - name: Build website
        working-directory: website
        run: npm run build
