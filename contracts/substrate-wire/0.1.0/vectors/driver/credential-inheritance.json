{
  "$schema": "../../schemas/vector.json",
  "assertions": [
    "child environment contains only the closed non-secret baseline plus explicitly set values",
    "daemon bearer and control socket variables are absent",
    "all inherited descriptors except declared stdio and substrate-owned handoff descriptors are closed",
    "diagnostics contain neither sentinel value nor its length"
  ],
  "context": {
    "fixture_authority": "driver-harness"
  },
  "expected": {
    "outcome": "success"
  },
  "id": "daemon-credentials-never-reach-child",
  "layer": "driver",
  "phase": 2,
  "preconditions": [
    "daemon environment contains DAEMONLOOM_VECTOR_SECRET with a unique sentinel",
    "daemon holds an extra readable descriptor containing a second sentinel"
  ],
  "request": {
    "argv": [
      "/fixture/inspect-env-and-fds"
    ],
    "environment": {
      "allow": [],
      "set": {
        "VECTOR_VISIBLE": "yes"
      }
    },
    "operation": "exec.start"
  }
}
