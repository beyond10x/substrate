version: aep.project/1

# Written by `protocol reverse init`. It points; it does not duplicate — a rule restated
# here would be a second copy with no way to say which one is in force. Principles and
# profiles of this project's own go under `principles/` and `profiles/` beside this file.
protocol: adp/1
profile: development.standard
protocols: git+ssh://git@github.com/beyond10x/engineering-protocols.git#422966a781ab9091c3c8706ef5b24cc6b6ae4b53

summary: >-
  The b10x execution data plane: confined workspaces, bounded processes, sessions, execution
  capsules, a durable operation ledger and observed state, published as versioned wire-contract
  bundles.
