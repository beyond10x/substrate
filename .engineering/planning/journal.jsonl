{"entity":"epic","version":1,"id":"release-hardening","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"<!-- Starting point for an `epic` artifact, seeded by `protocol artifact new epic <name>`.\n     No frontmatter here on purpose: the `---` block is written by the CLI from the id, kind, status\n     and relations you gave it, and a second copy in this file would be the one that went stale.\n     Delete the italic guidance as you fill each section. -->\n\n# Epic: <name>\n\n## Outcome\n\n*The change in the world this delivers, for whom, and how anybody would know it had happened.*\n\n## Why Now\n\n*What makes this the right thing to start — a commitment, a constraint, a cost that is growing. An\nepic with no answer here is a backlog entry.*\n\n## Scope\n\n*The shape of the work: the surfaces it touches and the capabilities it adds. Not a task list — the\nstories underneath carry that, through `decomposes`.*\n\n## Out of Scope\n\n*What is deliberately left out, and where it goes instead. This is the section that gets read in\nthree months when somebody asks why the thing they wanted is missing.*\n\n## Risks\n\n*What could make this take twice as long or land wrong, and what would show it early.*\n\n## Done When\n\n*The condition under which this epic moves to `implemented` — stated once, here, so it is not\nrenegotiated story by story.*\n","owner":"substrate","summary":"A CI gate, a pinned toolchain, a re-observed STATUS.md, and a signed digest-pinned daemon image and contract-bundle artifact; no route, schema or frozen bundle byte changes.","tags":["ci","release"],"title":"Release hardening"},"args":{"command":"create-entity","data":{"body":"<!-- Starting point for an `epic` artifact, seeded by `protocol artifact new epic <name>`.\n     No frontmatter here on purpose: the `---` block is written by the CLI from the id, kind, status\n     and relations you gave it, and a second copy in this file would be the one that went stale.\n     Delete the italic guidance as you fill each section. -->\n\n# Epic: <name>\n\n## Outcome\n\n*The change in the world this delivers, for whom, and how anybody would know it had happened.*\n\n## Why Now\n\n*What makes this the right thing to start — a commitment, a constraint, a cost that is growing. An\nepic with no answer here is a backlog entry.*\n\n## Scope\n\n*The shape of the work: the surfaces it touches and the capabilities it adds. Not a task list — the\nstories underneath carry that, through `decomposes`.*\n\n## Out of Scope\n\n*What is deliberately left out, and where it goes instead. This is the section that gets read in\nthree months when somebody asks why the thing they wanted is missing.*\n\n## Risks\n\n*What could make this take twice as long or land wrong, and what would show it early.*\n\n## Done When\n\n*The condition under which this epic moves to `implemented` — stated once, here, so it is not\nrenegotiated story by story.*\n","owner":"substrate","status":"draft","summary":"A CI gate, a pinned toolchain, a re-observed STATUS.md, and a signed digest-pinned daemon image and contract-bundle artifact; no route, schema or frozen bundle byte changes.","tags":["ci","release"],"title":"Release hardening"},"entity_type":"aep.epic/v1","locator":"ep://planning/store/epic/release-hardening"},"payload":{"actor":"human:timo","at":1788033700899,"causation":"cmd-new-epic-release-hardening","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"epic:release-hardening@1#0~ea90407a62603bd4","executor":null,"recorded_at":"2026-08-29T20:01:40Z"}}
{"entity":"epic","version":1,"id":"byte-plane-completion","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"<!-- Starting point for an `epic` artifact, seeded by `protocol artifact new epic <name>`.\n     No frontmatter here on purpose: the `---` block is written by the CLI from the id, kind, status\n     and relations you gave it, and a second copy in this file would be the one that went stale.\n     Delete the italic guidance as you fill each section. -->\n\n# Epic: <name>\n\n## Outcome\n\n*The change in the world this delivers, for whom, and how anybody would know it had happened.*\n\n## Why Now\n\n*What makes this the right thing to start — a commitment, a constraint, a cost that is growing. An\nepic with no answer here is a backlog entry.*\n\n## Scope\n\n*The shape of the work: the surfaces it touches and the capabilities it adds. Not a task list — the\nstories underneath carry that, through `decomposes`.*\n\n## Out of Scope\n\n*What is deliberately left out, and where it goes instead. This is the section that gets read in\nthree months when somebody asks why the thing they wanted is missing.*\n\n## Risks\n\n*What could make this take twice as long or land wrong, and what would show it early.*\n\n## Done When\n\n*The condition under which this epic moves to `implemented` — stated once, here, so it is not\nrenegotiated story by story.*\n","owner":"substrate","summary":"Phase 4's remaining exit criteria whose design is already accepted: PTY sessions, sealed memfd secret slots, and network session transport with single-use proof-bound authority.","tags":["phase-4","wire"],"title":"Byte-plane completion"},"args":{"command":"create-entity","data":{"body":"<!-- Starting point for an `epic` artifact, seeded by `protocol artifact new epic <name>`.\n     No frontmatter here on purpose: the `---` block is written by the CLI from the id, kind, status\n     and relations you gave it, and a second copy in this file would be the one that went stale.\n     Delete the italic guidance as you fill each section. -->\n\n# Epic: <name>\n\n## Outcome\n\n*The change in the world this delivers, for whom, and how anybody would know it had happened.*\n\n## Why Now\n\n*What makes this the right thing to start — a commitment, a constraint, a cost that is growing. An\nepic with no answer here is a backlog entry.*\n\n## Scope\n\n*The shape of the work: the surfaces it touches and the capabilities it adds. Not a task list — the\nstories underneath carry that, through `decomposes`.*\n\n## Out of Scope\n\n*What is deliberately left out, and where it goes instead. This is the section that gets read in\nthree months when somebody asks why the thing they wanted is missing.*\n\n## Risks\n\n*What could make this take twice as long or land wrong, and what would show it early.*\n\n## Done When\n\n*The condition under which this epic moves to `implemented` — stated once, here, so it is not\nrenegotiated story by story.*\n","owner":"substrate","status":"draft","summary":"Phase 4's remaining exit criteria whose design is already accepted: PTY sessions, sealed memfd secret slots, and network session transport with single-use proof-bound authority.","tags":["phase-4","wire"],"title":"Byte-plane completion"},"entity_type":"aep.epic/v1","locator":"ep://planning/store/epic/byte-plane-completion"},"payload":{"actor":"human:timo","at":1788033700925,"causation":"cmd-new-epic-byte-plane-completion","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"epic:byte-plane-completion@1#0~be8723a80d84b48d","executor":null,"recorded_at":"2026-08-29T20:01:40Z"}}
{"entity":"epic","version":1,"id":"container-driver-entry","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"<!-- Starting point for an `epic` artifact, seeded by `protocol artifact new epic <name>`.\n     No frontmatter here on purpose: the `---` block is written by the CLI from the id, kind, status\n     and relations you gave it, and a second copy in this file would be the one that went stale.\n     Delete the italic guidance as you fill each section. -->\n\n# Epic: <name>\n\n## Outcome\n\n*The change in the world this delivers, for whom, and how anybody would know it had happened.*\n\n## Why Now\n\n*What makes this the right thing to start — a commitment, a constraint, a cost that is growing. An\nepic with no answer here is a backlog entry.*\n\n## Scope\n\n*The shape of the work: the surfaces it touches and the capabilities it adds. Not a task list — the\nstories underneath carry that, through `decomposes`.*\n\n## Out of Scope\n\n*What is deliberately left out, and where it goes instead. This is the section that gets read in\nthree months when somebody asks why the thing they wanted is missing.*\n\n## Risks\n\n*What could make this take twice as long or land wrong, and what would show it early.*\n\n## Done When\n\n*The condition under which this epic moves to `implemented` — stated once, here, so it is not\nrenegotiated story by story.*\n","owner":"substrate","summary":"Plan 03's entry criteria proven before any Docker code: no host type crosses the driver port, one driver-parameterised conformance journey, named root-equivalent Docker authorities.","tags":["phase-5"],"title":"Container driver entry"},"args":{"command":"create-entity","data":{"body":"<!-- Starting point for an `epic` artifact, seeded by `protocol artifact new epic <name>`.\n     No frontmatter here on purpose: the `---` block is written by the CLI from the id, kind, status\n     and relations you gave it, and a second copy in this file would be the one that went stale.\n     Delete the italic guidance as you fill each section. -->\n\n# Epic: <name>\n\n## Outcome\n\n*The change in the world this delivers, for whom, and how anybody would know it had happened.*\n\n## Why Now\n\n*What makes this the right thing to start — a commitment, a constraint, a cost that is growing. An\nepic with no answer here is a backlog entry.*\n\n## Scope\n\n*The shape of the work: the surfaces it touches and the capabilities it adds. Not a task list — the\nstories underneath carry that, through `decomposes`.*\n\n## Out of Scope\n\n*What is deliberately left out, and where it goes instead. This is the section that gets read in\nthree months when somebody asks why the thing they wanted is missing.*\n\n## Risks\n\n*What could make this take twice as long or land wrong, and what would show it early.*\n\n## Done When\n\n*The condition under which this epic moves to `implemented` — stated once, here, so it is not\nrenegotiated story by story.*\n","owner":"substrate","status":"draft","summary":"Plan 03's entry criteria proven before any Docker code: no host type crosses the driver port, one driver-parameterised conformance journey, named root-equivalent Docker authorities.","tags":["phase-5"],"title":"Container driver entry"},"entity_type":"aep.epic/v1","locator":"ep://planning/store/epic/container-driver-entry"},"payload":{"actor":"human:timo","at":1788033700944,"causation":"cmd-new-epic-container-driver-entry","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"epic:container-driver-entry@1#0~bbfd9618d3c478d8","executor":null,"recorded_at":"2026-08-29T20:01:40Z"}}
{"entity":"epic","version":1,"id":"container-driver-entry","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"depends_on":"epic:byte-plane-completion"}]},"args":{"command":"create-relation","kind":"depends_on","source":"01MEM0000000000000007","target":"01MEM0000000000000001"},"payload":{"actor":"human:timo","at":1788033700947,"causation":"cmd-epic-container-driver-entry-depends_on-epic-byte-plane-completion","change":{"change":"related","relation":"depends_on","target":"epic:byte-plane-completion"},"correlation":"protocol-artifact-relate","event_id":"epic:container-driver-entry@1#0~6f645c4e0c792166","executor":null,"recorded_at":"2026-08-29T20:01:40Z"}}
{"entity":"story","version":1,"id":"agents-md-matches-the-scripts","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"<!-- Starting point for a `story` artifact, seeded by `protocol artifact new story <name>`.\n     No frontmatter here on purpose: the `---` block is written by the CLI from the id, kind, status\n     and relations you gave it, and a second copy in this file would be the one that went stale.\n     Delete the italic guidance as you fill each section. -->\n\n# Story: <name>\n\n## Outcome\n\n*What is true for whom once this has shipped, in one sentence. If it names a component rather than a\nperson, it is a task — say what changes for someone.*\n\n## Context\n\n*Why this is worth doing now, and what it depends on. Link the epic or specification it comes from\nrather than restating it; the `derived_from` relation already carries the edge.*\n\n## Acceptance\n\n*The conditions under which this is done, each one something a person or a check can observe. \"Works\ncorrectly\" is not one of them.*\n\n## Out of Scope\n\n*What a reasonable reader would expect to be included and is not — the boundary that stops this\nstory quietly becoming an epic.*\n\n## Open Questions\n\n*What is still undecided, each with who decides it. A story carrying an unowned question is a story\nthat stalls without anybody noticing.*\n","owner":"substrate","summary":"Two verified drifts: AGENTS.md:112 says gate.sh verifies 0.1.0 only (gate.sh:20-23 runs all four checkers); AGENTS.md:172 says bot-token.sh:8 defaults to a foreign org (it defaults to beyond10x).","tags":["docs"],"title":"AGENTS.md says what the gate and the bot script actually do"},"args":{"command":"create-entity","data":{"body":"<!-- Starting point for a `story` artifact, seeded by `protocol artifact new story <name>`.\n     No frontmatter here on purpose: the `---` block is written by the CLI from the id, kind, status\n     and relations you gave it, and a second copy in this file would be the one that went stale.\n     Delete the italic guidance as you fill each section. -->\n\n# Story: <name>\n\n## Outcome\n\n*What is true for whom once this has shipped, in one sentence. If it names a component rather than a\nperson, it is a task — say what changes for someone.*\n\n## Context\n\n*Why this is worth doing now, and what it depends on. Link the epic or specification it comes from\nrather than restating it; the `derived_from` relation already carries the edge.*\n\n## Acceptance\n\n*The conditions under which this is done, each one something a person or a check can observe. \"Works\ncorrectly\" is not one of them.*\n\n## Out of Scope\n\n*What a reasonable reader would expect to be included and is not — the boundary that stops this\nstory quietly becoming an epic.*\n\n## Open Questions\n\n*What is still undecided, each with who decides it. A story carrying an unowned question is a story\nthat stalls without anybody noticing.*\n","owner":"substrate","status":"draft","summary":"Two verified drifts: AGENTS.md:112 says gate.sh verifies 0.1.0 only (gate.sh:20-23 runs all four checkers); AGENTS.md:172 says bot-token.sh:8 defaults to a foreign org (it defaults to beyond10x).","tags":["docs"],"title":"AGENTS.md says what the gate and the bot script actually do"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/agents-md-matches-the-scripts"},"payload":{"actor":"human:timo","at":1788033700966,"causation":"cmd-new-story-agents-md-matches-the-scripts","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:agents-md-matches-the-scripts@1#0~6ad128d09d516791","executor":null,"recorded_at":"2026-08-29T20:01:40Z"}}
{"entity":"story","version":1,"id":"agents-md-matches-the-scripts","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:release-hardening"}]},"args":{"command":"create-relation","kind":"decomposes","source":"01MEM0000000000000012","target":"01MEM0000000000000007"},"payload":{"actor":"human:timo","at":1788033700970,"causation":"cmd-story-agents-md-matches-the-scripts-decomposes-epic-release-hardening","change":{"change":"related","relation":"decomposes","target":"epic:release-hardening"},"correlation":"protocol-artifact-relate","event_id":"story:agents-md-matches-the-scripts@1#0~736fdbac572a88ad","executor":null,"recorded_at":"2026-08-29T20:01:40Z"}}
{"entity":"story","version":1,"id":"ci-runs-the-full-gate","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"<!-- Starting point for a `story` artifact, seeded by `protocol artifact new story <name>`.\n     No frontmatter here on purpose: the `---` block is written by the CLI from the id, kind, status\n     and relations you gave it, and a second copy in this file would be the one that went stale.\n     Delete the italic guidance as you fill each section. -->\n\n# Story: <name>\n\n## Outcome\n\n*What is true for whom once this has shipped, in one sentence. If it names a component rather than a\nperson, it is a task — say what changes for someone.*\n\n## Context\n\n*Why this is worth doing now, and what it depends on. Link the epic or specification it comes from\nrather than restating it; the `derived_from` relation already carries the edge.*\n\n## Acceptance\n\n*The conditions under which this is done, each one something a person or a check can observe. \"Works\ncorrectly\" is not one of them.*\n\n## Out of Scope\n\n*What a reasonable reader would expect to be included and is not — the boundary that stops this\nstory quietly becoming an epic.*\n\n## Open Questions\n\n*What is still undecided, each with who decides it. A story carrying an unowned question is a story\nthat stalls without anybody noticing.*\n","owner":"substrate","summary":"Only pages.yml exists under .github/workflows/; nothing runs scripts/gate.sh on a push or PR.","tags":["ci"],"title":"CI runs the full gate on every push and pull request"},"args":{"command":"create-entity","data":{"body":"<!-- Starting point for a `story` artifact, seeded by `protocol artifact new story <name>`.\n     No frontmatter here on purpose: the `---` block is written by the CLI from the id, kind, status\n     and relations you gave it, and a second copy in this file would be the one that went stale.\n     Delete the italic guidance as you fill each section. -->\n\n# Story: <name>\n\n## Outcome\n\n*What is true for whom once this has shipped, in one sentence. If it names a component rather than a\nperson, it is a task — say what changes for someone.*\n\n## Context\n\n*Why this is worth doing now, and what it depends on. Link the epic or specification it comes from\nrather than restating it; the `derived_from` relation already carries the edge.*\n\n## Acceptance\n\n*The conditions under which this is done, each one something a person or a check can observe. \"Works\ncorrectly\" is not one of them.*\n\n## Out of Scope\n\n*What a reasonable reader would expect to be included and is not — the boundary that stops this\nstory quietly becoming an epic.*\n\n## Open Questions\n\n*What is still undecided, each with who decides it. A story carrying an unowned question is a story\nthat stalls without anybody noticing.*\n","owner":"substrate","status":"draft","summary":"Only pages.yml exists under .github/workflows/; nothing runs scripts/gate.sh on a push or PR.","tags":["ci"],"title":"CI runs the full gate on every push and pull request"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/ci-runs-the-full-gate"},"payload":{"actor":"human:timo","at":1788033700989,"causation":"cmd-new-story-ci-runs-the-full-gate","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:ci-runs-the-full-gate@1#0~451bfd33c2fdf462","executor":null,"recorded_at":"2026-08-29T20:01:40Z"}}
{"entity":"story","version":1,"id":"ci-runs-the-full-gate","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:release-hardening"}]},"args":{"command":"create-relation","kind":"decomposes","source":"01MEM0000000000000017","target":"01MEM0000000000000007"},"payload":{"actor":"human:timo","at":1788033700992,"causation":"cmd-story-ci-runs-the-full-gate-decomposes-epic-release-hardening","change":{"change":"related","relation":"decomposes","target":"epic:release-hardening"},"correlation":"protocol-artifact-relate","event_id":"story:ci-runs-the-full-gate@1#0~1b4629490dfe13cc","executor":null,"recorded_at":"2026-08-29T20:01:40Z"}}
{"entity":"story","version":1,"id":"pinned-rust-toolchain","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"<!-- Starting point for a `story` artifact, seeded by `protocol artifact new story <name>`.\n     No frontmatter here on purpose: the `---` block is written by the CLI from the id, kind, status\n     and relations you gave it, and a second copy in this file would be the one that went stale.\n     Delete the italic guidance as you fill each section. -->\n\n# Story: <name>\n\n## Outcome\n\n*What is true for whom once this has shipped, in one sentence. If it names a component rather than a\nperson, it is a task — say what changes for someone.*\n\n## Context\n\n*Why this is worth doing now, and what it depends on. Link the epic or specification it comes from\nrather than restating it; the `derived_from` relation already carries the edge.*\n\n## Acceptance\n\n*The conditions under which this is done, each one something a person or a check can observe. \"Works\ncorrectly\" is not one of them.*\n\n## Out of Scope\n\n*What a reasonable reader would expect to be included and is not — the boundary that stops this\nstory quietly becoming an epic.*\n\n## Open Questions\n\n*What is still undecided, each with who decides it. A story carrying an unowned question is a story\nthat stalls without anybody noticing.*\n","owner":"substrate","summary":"rust-toolchain.toml pins 1.97, matching Cargo.toml rust-version and the Dockerfile builder; retires the 'rustup update before pushing' rule.","tags":["build"],"title":"The Rust toolchain is pinned so local and CI clippy agree"},"args":{"command":"create-entity","data":{"body":"<!-- Starting point for a `story` artifact, seeded by `protocol artifact new story <name>`.\n     No frontmatter here on purpose: the `---` block is written by the CLI from the id, kind, status\n     and relations you gave it, and a second copy in this file would be the one that went stale.\n     Delete the italic guidance as you fill each section. -->\n\n# Story: <name>\n\n## Outcome\n\n*What is true for whom once this has shipped, in one sentence. If it names a component rather than a\nperson, it is a task — say what changes for someone.*\n\n## Context\n\n*Why this is worth doing now, and what it depends on. Link the epic or specification it comes from\nrather than restating it; the `derived_from` relation already carries the edge.*\n\n## Acceptance\n\n*The conditions under which this is done, each one something a person or a check can observe. \"Works\ncorrectly\" is not one of them.*\n\n## Out of Scope\n\n*What a reasonable reader would expect to be included and is not — the boundary that stops this\nstory quietly becoming an epic.*\n\n## Open Questions\n\n*What is still undecided, each with who decides it. A story carrying an unowned question is a story\nthat stalls without anybody noticing.*\n","owner":"substrate","status":"draft","summary":"rust-toolchain.toml pins 1.97, matching Cargo.toml rust-version and the Dockerfile builder; retires the 'rustup update before pushing' rule.","tags":["build"],"title":"The Rust toolchain is pinned so local and CI clippy agree"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/pinned-rust-toolchain"},"payload":{"actor":"human:timo","at":1788033701010,"causation":"cmd-new-story-pinned-rust-toolchain","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:pinned-rust-toolchain@1#0~909f2fc7d048292d","executor":null,"recorded_at":"2026-08-29T20:01:41Z"}}
{"entity":"story","version":1,"id":"pinned-rust-toolchain","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:release-hardening"}]},"args":{"command":"create-relation","kind":"decomposes","source":"01MEM0000000000000022","target":"01MEM0000000000000007"},"payload":{"actor":"human:timo","at":1788033701014,"causation":"cmd-story-pinned-rust-toolchain-decomposes-epic-release-hardening","change":{"change":"related","relation":"decomposes","target":"epic:release-hardening"},"correlation":"protocol-artifact-relate","event_id":"story:pinned-rust-toolchain@1#0~dae95fcef2057f68","executor":null,"recorded_at":"2026-08-29T20:01:41Z"}}
{"entity":"story","version":1,"id":"status-md-re-observed","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"<!-- Starting point for a `story` artifact, seeded by `protocol artifact new story <name>`.\n     No frontmatter here on purpose: the `---` block is written by the CLI from the id, kind, status\n     and relations you gave it, and a second copy in this file would be the one that went stale.\n     Delete the italic guidance as you fill each section. -->\n\n# Story: <name>\n\n## Outcome\n\n*What is true for whom once this has shipped, in one sentence. If it names a component rather than a\nperson, it is a task — say what changes for someone.*\n\n## Context\n\n*Why this is worth doing now, and what it depends on. Link the epic or specification it comes from\nrather than restating it; the `derived_from` relation already carries the edge.*\n\n## Acceptance\n\n*The conditions under which this is done, each one something a person or a check can observe. \"Works\ncorrectly\" is not one of them.*\n\n## Out of Scope\n\n*What a reasonable reader would expect to be included and is not — the boundary that stops this\nstory quietly becoming an epic.*\n\n## Open Questions\n\n*What is still undecided, each with who decides it. A story carrying an unowned question is a story\nthat stalls without anybody noticing.*\n","owner":"substrate","summary":"STATUS.md is observed 2026-08-17, predates extraction, and still names the monorepo as the source.","tags":["docs"],"title":"STATUS.md is observed again from the standalone repository"},"args":{"command":"create-entity","data":{"body":"<!-- Starting point for a `story` artifact, seeded by `protocol artifact new story <name>`.\n     No frontmatter here on purpose: the `---` block is written by the CLI from the id, kind, status\n     and relations you gave it, and a second copy in this file would be the one that went stale.\n     Delete the italic guidance as you fill each section. -->\n\n# Story: <name>\n\n## Outcome\n\n*What is true for whom once this has shipped, in one sentence. If it names a component rather than a\nperson, it is a task — say what changes for someone.*\n\n## Context\n\n*Why this is worth doing now, and what it depends on. Link the epic or specification it comes from\nrather than restating it; the `derived_from` relation already carries the edge.*\n\n## Acceptance\n\n*The conditions under which this is done, each one something a person or a check can observe. \"Works\ncorrectly\" is not one of them.*\n\n## Out of Scope\n\n*What a reasonable reader would expect to be included and is not — the boundary that stops this\nstory quietly becoming an epic.*\n\n## Open Questions\n\n*What is still undecided, each with who decides it. A story carrying an unowned question is a story\nthat stalls without anybody noticing.*\n","owner":"substrate","status":"draft","summary":"STATUS.md is observed 2026-08-17, predates extraction, and still names the monorepo as the source.","tags":["docs"],"title":"STATUS.md is observed again from the standalone repository"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/status-md-re-observed"},"payload":{"actor":"human:timo","at":1788033701032,"causation":"cmd-new-story-status-md-re-observed","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:status-md-re-observed@1#0~73cad1fc1dab605c","executor":null,"recorded_at":"2026-08-29T20:01:41Z"}}
{"entity":"story","version":1,"id":"status-md-re-observed","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:release-hardening"}]},"args":{"command":"create-relation","kind":"decomposes","source":"01MEM0000000000000027","target":"01MEM0000000000000007"},"payload":{"actor":"human:timo","at":1788033701035,"causation":"cmd-story-status-md-re-observed-decomposes-epic-release-hardening","change":{"change":"related","relation":"decomposes","target":"epic:release-hardening"},"correlation":"protocol-artifact-relate","event_id":"story:status-md-re-observed@1#0~e31aebc005d961d1","executor":null,"recorded_at":"2026-08-29T20:01:41Z"}}
{"entity":"story","version":1,"id":"status-md-re-observed","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:release-hardening"},{"depends_on":"story:ci-runs-the-full-gate"}]},"args":{"command":"create-relation","kind":"depends_on","source":"01MEM0000000000000027","target":"01MEM0000000000000013"},"payload":{"actor":"human:timo","at":1788033701039,"causation":"cmd-story-status-md-re-observed-depends_on-story-ci-runs-the-full-gate","change":{"change":"related","relation":"depends_on","target":"story:ci-runs-the-full-gate"},"correlation":"protocol-artifact-relate","event_id":"story:status-md-re-observed@1#0~55026169d806dd43","executor":null,"recorded_at":"2026-08-29T20:01:41Z"}}
{"entity":"story","version":1,"id":"status-md-re-observed","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:release-hardening"},{"depends_on":"story:ci-runs-the-full-gate"},{"depends_on":"story:pinned-rust-toolchain"}]},"args":{"command":"create-relation","kind":"depends_on","source":"01MEM0000000000000027","target":"01MEM0000000000000016"},"payload":{"actor":"human:timo","at":1788033701043,"causation":"cmd-story-status-md-re-observed-depends_on-story-pinned-rust-toolchain","change":{"change":"related","relation":"depends_on","target":"story:pinned-rust-toolchain"},"correlation":"protocol-artifact-relate","event_id":"story:status-md-re-observed@1#0~805f2e69f09a0e48","executor":null,"recorded_at":"2026-08-29T20:01:41Z"}}
{"entity":"story","version":1,"id":"signed-daemon-image","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"<!-- Starting point for a `story` artifact, seeded by `protocol artifact new story <name>`.\n     No frontmatter here on purpose: the `---` block is written by the CLI from the id, kind, status\n     and relations you gave it, and a second copy in this file would be the one that went stale.\n     Delete the italic guidance as you fill each section. -->\n\n# Story: <name>\n\n## Outcome\n\n*What is true for whom once this has shipped, in one sentence. If it names a component rather than a\nperson, it is a task — say what changes for someone.*\n\n## Context\n\n*Why this is worth doing now, and what it depends on. Link the epic or specification it comes from\nrather than restating it; the `derived_from` relation already carries the edge.*\n\n## Acceptance\n\n*The conditions under which this is done, each one something a person or a check can observe. \"Works\ncorrectly\" is not one of them.*\n\n## Out of Scope\n\n*What a reasonable reader would expect to be included and is not — the boundary that stops this\nstory quietly becoming an epic.*\n\n## Open Questions\n\n*What is still undecided, each with who decides it. A story carrying an unowned question is a story\nthat stalls without anybody noticing.*\n","owner":"substrate","summary":"The Dockerfile exists; no workflow builds, pushes or signs it.","tags":["ci","release"],"title":"A tagged main publishes a signed, digest-pinned daemon image"},"args":{"command":"create-entity","data":{"body":"<!-- Starting point for a `story` artifact, seeded by `protocol artifact new story <name>`.\n     No frontmatter here on purpose: the `---` block is written by the CLI from the id, kind, status\n     and relations you gave it, and a second copy in this file would be the one that went stale.\n     Delete the italic guidance as you fill each section. -->\n\n# Story: <name>\n\n## Outcome\n\n*What is true for whom once this has shipped, in one sentence. If it names a component rather than a\nperson, it is a task — say what changes for someone.*\n\n## Context\n\n*Why this is worth doing now, and what it depends on. Link the epic or specification it comes from\nrather than restating it; the `derived_from` relation already carries the edge.*\n\n## Acceptance\n\n*The conditions under which this is done, each one something a person or a check can observe. \"Works\ncorrectly\" is not one of them.*\n\n## Out of Scope\n\n*What a reasonable reader would expect to be included and is not — the boundary that stops this\nstory quietly becoming an epic.*\n\n## Open Questions\n\n*What is still undecided, each with who decides it. A story carrying an unowned question is a story\nthat stalls without anybody noticing.*\n","owner":"substrate","status":"draft","summary":"The Dockerfile exists; no workflow builds, pushes or signs it.","tags":["ci","release"],"title":"A tagged main publishes a signed, digest-pinned daemon image"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/signed-daemon-image"},"payload":{"actor":"human:timo","at":1788033701062,"causation":"cmd-new-story-signed-daemon-image","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:signed-daemon-image@1#0~53947d035a2d8e7f","executor":null,"recorded_at":"2026-08-29T20:01:41Z"}}
{"entity":"story","version":1,"id":"signed-daemon-image","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:release-hardening"}]},"args":{"command":"create-relation","kind":"decomposes","source":"01MEM0000000000000036","target":"01MEM0000000000000007"},"payload":{"actor":"human:timo","at":1788033701066,"causation":"cmd-story-signed-daemon-image-decomposes-epic-release-hardening","change":{"change":"related","relation":"decomposes","target":"epic:release-hardening"},"correlation":"protocol-artifact-relate","event_id":"story:signed-daemon-image@1#0~a4586c789e12f897","executor":null,"recorded_at":"2026-08-29T20:01:41Z"}}
{"entity":"story","version":1,"id":"signed-daemon-image","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:release-hardening"},{"depends_on":"story:ci-runs-the-full-gate"}]},"args":{"command":"create-relation","kind":"depends_on","source":"01MEM0000000000000036","target":"01MEM0000000000000013"},"payload":{"actor":"human:timo","at":1788033701070,"causation":"cmd-story-signed-daemon-image-depends_on-story-ci-runs-the-full-gate","change":{"change":"related","relation":"depends_on","target":"story:ci-runs-the-full-gate"},"correlation":"protocol-artifact-relate","event_id":"story:signed-daemon-image@1#0~50b3803ec2860145","executor":null,"recorded_at":"2026-08-29T20:01:41Z"}}
{"entity":"story","version":1,"id":"contract-bundle-oci-artifact","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"<!-- Starting point for a `story` artifact, seeded by `protocol artifact new story <name>`.\n     No frontmatter here on purpose: the `---` block is written by the CLI from the id, kind, status\n     and relations you gave it, and a second copy in this file would be the one that went stale.\n     Delete the italic guidance as you fill each section. -->\n\n# Story: <name>\n\n## Outcome\n\n*What is true for whom once this has shipped, in one sentence. If it names a component rather than a\nperson, it is a task — say what changes for someone.*\n\n## Context\n\n*Why this is worth doing now, and what it depends on. Link the epic or specification it comes from\nrather than restating it; the `derived_from` relation already carries the edge.*\n\n## Acceptance\n\n*The conditions under which this is done, each one something a person or a check can observe. \"Works\ncorrectly\" is not one of them.*\n\n## Out of Scope\n\n*What a reasonable reader would expect to be included and is not — the boundary that stops this\nstory quietly becoming an epic.*\n\n## Open Questions\n\n*What is still undecided, each with who decides it. A story carrying an unowned question is a story\nthat stalls without anybody noticing.*\n","owner":"substrate","summary":"Design 07 fixes the packaging shape; consumers copy bundle bytes by hand today.","tags":["contracts","release"],"title":"The 0.4.0 contract bundle is a signed, digest-pinned OCI artifact"},"args":{"command":"create-entity","data":{"body":"<!-- Starting point for a `story` artifact, seeded by `protocol artifact new story <name>`.\n     No frontmatter here on purpose: the `---` block is written by the CLI from the id, kind, status\n     and relations you gave it, and a second copy in this file would be the one that went stale.\n     Delete the italic guidance as you fill each section. -->\n\n# Story: <name>\n\n## Outcome\n\n*What is true for whom once this has shipped, in one sentence. If it names a component rather than a\nperson, it is a task — say what changes for someone.*\n\n## Context\n\n*Why this is worth doing now, and what it depends on. Link the epic or specification it comes from\nrather than restating it; the `derived_from` relation already carries the edge.*\n\n## Acceptance\n\n*The conditions under which this is done, each one something a person or a check can observe. \"Works\ncorrectly\" is not one of them.*\n\n## Out of Scope\n\n*What a reasonable reader would expect to be included and is not — the boundary that stops this\nstory quietly becoming an epic.*\n\n## Open Questions\n\n*What is still undecided, each with who decides it. A story carrying an unowned question is a story\nthat stalls without anybody noticing.*\n","owner":"substrate","status":"draft","summary":"Design 07 fixes the packaging shape; consumers copy bundle bytes by hand today.","tags":["contracts","release"],"title":"The 0.4.0 contract bundle is a signed, digest-pinned OCI artifact"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/contract-bundle-oci-artifact"},"payload":{"actor":"human:timo","at":1788033701091,"causation":"cmd-new-story-contract-bundle-oci-artifact","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:contract-bundle-oci-artifact@1#0~6d562e6b6f172f52","executor":null,"recorded_at":"2026-08-29T20:01:41Z"}}
{"entity":"story","version":1,"id":"contract-bundle-oci-artifact","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:release-hardening"}]},"args":{"command":"create-relation","kind":"decomposes","source":"01MEM0000000000000043","target":"01MEM0000000000000007"},"payload":{"actor":"human:timo","at":1788033701095,"causation":"cmd-story-contract-bundle-oci-artifact-decomposes-epic-release-hardening","change":{"change":"related","relation":"decomposes","target":"epic:release-hardening"},"correlation":"protocol-artifact-relate","event_id":"story:contract-bundle-oci-artifact@1#0~944a7b625cf3a093","executor":null,"recorded_at":"2026-08-29T20:01:41Z"}}
{"entity":"story","version":1,"id":"contract-bundle-oci-artifact","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:release-hardening"},{"depends_on":"story:signed-daemon-image"}]},"args":{"command":"create-relation","kind":"depends_on","source":"01MEM0000000000000043","target":"01MEM0000000000000019"},"payload":{"actor":"human:timo","at":1788033701098,"causation":"cmd-story-contract-bundle-oci-artifact-depends_on-story-signed-daemon-image","change":{"change":"related","relation":"depends_on","target":"story:signed-daemon-image"},"correlation":"protocol-artifact-relate","event_id":"story:contract-bundle-oci-artifact@1#0~bea0b76d983bc14f","executor":null,"recorded_at":"2026-08-29T20:01:41Z"}}
{"entity":"story","version":1,"id":"pty-sessions","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"<!-- Starting point for a `story` artifact, seeded by `protocol artifact new story <name>`.\n     No frontmatter here on purpose: the `---` block is written by the CLI from the id, kind, status\n     and relations you gave it, and a second copy in this file would be the one that went stale.\n     Delete the italic guidance as you fill each section. -->\n\n# Story: <name>\n\n## Outcome\n\n*What is true for whom once this has shipped, in one sentence. If it names a component rather than a\nperson, it is a task — say what changes for someone.*\n\n## Context\n\n*Why this is worth doing now, and what it depends on. Link the epic or specification it comes from\nrather than restating it; the `derived_from` relation already carries the edge.*\n\n## Acceptance\n\n*The conditions under which this is done, each one something a person or a check can observe. \"Works\ncorrectly\" is not one of them.*\n\n## Out of Scope\n\n*What a reasonable reader would expect to be included and is not — the boundary that stops this\nstory quietly becoming an epic.*\n\n## Open Questions\n\n*What is still undecided, each with who decides it. A story carrying an unowned question is a story\nthat stalls without anybody noticing.*\n","owner":"substrate","summary":"Design 05 section 2 fixes the pty kind and frames; README lists PTY as absent; needs ADR and a successor bundle before code.","tags":["daemon","host","wire"],"title":"PTY sessions are a distinct session kind with resize"},"args":{"command":"create-entity","data":{"body":"<!-- Starting point for a `story` artifact, seeded by `protocol artifact new story <name>`.\n     No frontmatter here on purpose: the `---` block is written by the CLI from the id, kind, status\n     and relations you gave it, and a second copy in this file would be the one that went stale.\n     Delete the italic guidance as you fill each section. -->\n\n# Story: <name>\n\n## Outcome\n\n*What is true for whom once this has shipped, in one sentence. If it names a component rather than a\nperson, it is a task — say what changes for someone.*\n\n## Context\n\n*Why this is worth doing now, and what it depends on. Link the epic or specification it comes from\nrather than restating it; the `derived_from` relation already carries the edge.*\n\n## Acceptance\n\n*The conditions under which this is done, each one something a person or a check can observe. \"Works\ncorrectly\" is not one of them.*\n\n## Out of Scope\n\n*What a reasonable reader would expect to be included and is not — the boundary that stops this\nstory quietly becoming an epic.*\n\n## Open Questions\n\n*What is still undecided, each with who decides it. A story carrying an unowned question is a story\nthat stalls without anybody noticing.*\n","owner":"substrate","status":"draft","summary":"Design 05 section 2 fixes the pty kind and frames; README lists PTY as absent; needs ADR and a successor bundle before code.","tags":["daemon","host","wire"],"title":"PTY sessions are a distinct session kind with resize"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/pty-sessions"},"payload":{"actor":"human:timo","at":1788033701121,"causation":"cmd-new-story-pty-sessions","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:pty-sessions@1#0~389d6785a0af51d4","executor":null,"recorded_at":"2026-08-29T20:01:41Z"}}
{"entity":"story","version":1,"id":"pty-sessions","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:byte-plane-completion"}]},"args":{"command":"create-relation","kind":"decomposes","source":"01MEM0000000000000050","target":"01MEM0000000000000001"},"payload":{"actor":"human:timo","at":1788033701125,"causation":"cmd-story-pty-sessions-decomposes-epic-byte-plane-completion","change":{"change":"related","relation":"decomposes","target":"epic:byte-plane-completion"},"correlation":"protocol-artifact-relate","event_id":"story:pty-sessions@1#0~1e29d2bae9cb8fb5","executor":null,"recorded_at":"2026-08-29T20:01:41Z"}}
{"entity":"story","version":1,"id":"sealed-secret-slots","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"<!-- Starting point for a `story` artifact, seeded by `protocol artifact new story <name>`.\n     No frontmatter here on purpose: the `---` block is written by the CLI from the id, kind, status\n     and relations you gave it, and a second copy in this file would be the one that went stale.\n     Delete the italic guidance as you fill each section. -->\n\n# Story: <name>\n\n## Outcome\n\n*What is true for whom once this has shipped, in one sentence. If it names a component rather than a\nperson, it is a task — say what changes for someone.*\n\n## Context\n\n*Why this is worth doing now, and what it depends on. Link the epic or specification it comes from\nrather than restating it; the `derived_from` relation already carries the edge.*\n\n## Acceptance\n\n*The conditions under which this is done, each one something a person or a check can observe. \"Works\ncorrectly\" is not one of them.*\n\n## Out of Scope\n\n*What a reasonable reader would expect to be included and is not — the boundary that stops this\nstory quietly becoming an epic.*\n\n## Open Questions\n\n*What is still undecided, each with who decides it. A story carrying an unowned question is a story\nthat stalls without anybody noticing.*\n","owner":"substrate","summary":"Design 06 decision 3 fixes the mechanism; a live vendor harness stays refused without it.","tags":["daemon","host","wire"],"title":"Sealed secret slots reach a child only as a memfd descriptor"},"args":{"command":"create-entity","data":{"body":"<!-- Starting point for a `story` artifact, seeded by `protocol artifact new story <name>`.\n     No frontmatter here on purpose: the `---` block is written by the CLI from the id, kind, status\n     and relations you gave it, and a second copy in this file would be the one that went stale.\n     Delete the italic guidance as you fill each section. -->\n\n# Story: <name>\n\n## Outcome\n\n*What is true for whom once this has shipped, in one sentence. If it names a component rather than a\nperson, it is a task — say what changes for someone.*\n\n## Context\n\n*Why this is worth doing now, and what it depends on. Link the epic or specification it comes from\nrather than restating it; the `derived_from` relation already carries the edge.*\n\n## Acceptance\n\n*The conditions under which this is done, each one something a person or a check can observe. \"Works\ncorrectly\" is not one of them.*\n\n## Out of Scope\n\n*What a reasonable reader would expect to be included and is not — the boundary that stops this\nstory quietly becoming an epic.*\n\n## Open Questions\n\n*What is still undecided, each with who decides it. A story carrying an unowned question is a story\nthat stalls without anybody noticing.*\n","owner":"substrate","status":"draft","summary":"Design 06 decision 3 fixes the mechanism; a live vendor harness stays refused without it.","tags":["daemon","host","wire"],"title":"Sealed secret slots reach a child only as a memfd descriptor"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/sealed-secret-slots"},"payload":{"actor":"human:timo","at":1788033701148,"causation":"cmd-new-story-sealed-secret-slots","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:sealed-secret-slots@1#0~9dae60dcc53357fd","executor":null,"recorded_at":"2026-08-29T20:01:41Z"}}
{"entity":"story","version":1,"id":"sealed-secret-slots","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:byte-plane-completion"}]},"args":{"command":"create-relation","kind":"decomposes","source":"01MEM0000000000000055","target":"01MEM0000000000000001"},"payload":{"actor":"human:timo","at":1788033701151,"causation":"cmd-story-sealed-secret-slots-decomposes-epic-byte-plane-completion","change":{"change":"related","relation":"decomposes","target":"epic:byte-plane-completion"},"correlation":"protocol-artifact-relate","event_id":"story:sealed-secret-slots@1#0~0bd36a553bba1fc0","executor":null,"recorded_at":"2026-08-29T20:01:41Z"}}
{"entity":"story","version":1,"id":"network-session-authority","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"<!-- Starting point for a `story` artifact, seeded by `protocol artifact new story <name>`.\n     No frontmatter here on purpose: the `---` block is written by the CLI from the id, kind, status\n     and relations you gave it, and a second copy in this file would be the one that went stale.\n     Delete the italic guidance as you fill each section. -->\n\n# Story: <name>\n\n## Outcome\n\n*What is true for whom once this has shipped, in one sentence. If it names a component rather than a\nperson, it is a task — say what changes for someone.*\n\n## Context\n\n*Why this is worth doing now, and what it depends on. Link the epic or specification it comes from\nrather than restating it; the `derived_from` relation already carries the edge.*\n\n## Acceptance\n\n*The conditions under which this is done, each one something a person or a check can observe. \"Works\ncorrectly\" is not one of them.*\n\n## Out of Scope\n\n*What a reasonable reader would expect to be included and is not — the boundary that stops this\nstory quietly becoming an epic.*\n\n## Open Questions\n\n*What is still undecided, each with who decides it. A story carrying an unowned question is a story\nthat stalls without anybody noticing.*\n","owner":"substrate","summary":"Design 05 decisions 3-4: at most 60 s, redeems once, channel-bound; reconnect is a fresh authority; only the Unix socket serves sessions today.","tags":["daemon","wire"],"title":"Network session transport over TLS with single-use proof-bound authority"},"args":{"command":"create-entity","data":{"body":"<!-- Starting point for a `story` artifact, seeded by `protocol artifact new story <name>`.\n     No frontmatter here on purpose: the `---` block is written by the CLI from the id, kind, status\n     and relations you gave it, and a second copy in this file would be the one that went stale.\n     Delete the italic guidance as you fill each section. -->\n\n# Story: <name>\n\n## Outcome\n\n*What is true for whom once this has shipped, in one sentence. If it names a component rather than a\nperson, it is a task — say what changes for someone.*\n\n## Context\n\n*Why this is worth doing now, and what it depends on. Link the epic or specification it comes from\nrather than restating it; the `derived_from` relation already carries the edge.*\n\n## Acceptance\n\n*The conditions under which this is done, each one something a person or a check can observe. \"Works\ncorrectly\" is not one of them.*\n\n## Out of Scope\n\n*What a reasonable reader would expect to be included and is not — the boundary that stops this\nstory quietly becoming an epic.*\n\n## Open Questions\n\n*What is still undecided, each with who decides it. A story carrying an unowned question is a story\nthat stalls without anybody noticing.*\n","owner":"substrate","status":"draft","summary":"Design 05 decisions 3-4: at most 60 s, redeems once, channel-bound; reconnect is a fresh authority; only the Unix socket serves sessions today.","tags":["daemon","wire"],"title":"Network session transport over TLS with single-use proof-bound authority"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/network-session-authority"},"payload":{"actor":"human:timo","at":1788033701175,"causation":"cmd-new-story-network-session-authority","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:network-session-authority@1#0~1f01da50ab866690","executor":null,"recorded_at":"2026-08-29T20:01:41Z"}}
{"entity":"story","version":1,"id":"network-session-authority","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:byte-plane-completion"}]},"args":{"command":"create-relation","kind":"decomposes","source":"01MEM0000000000000060","target":"01MEM0000000000000001"},"payload":{"actor":"human:timo","at":1788033701178,"causation":"cmd-story-network-session-authority-decomposes-epic-byte-plane-completion","change":{"change":"related","relation":"decomposes","target":"epic:byte-plane-completion"},"correlation":"protocol-artifact-relate","event_id":"story:network-session-authority@1#0~da5609bc49e5d28c","executor":null,"recorded_at":"2026-08-29T20:01:41Z"}}
{"entity":"story","version":1,"id":"docker-driver-entry-gate","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"<!-- Starting point for a `story` artifact, seeded by `protocol artifact new story <name>`.\n     No frontmatter here on purpose: the `---` block is written by the CLI from the id, kind, status\n     and relations you gave it, and a second copy in this file would be the one that went stale.\n     Delete the italic guidance as you fill each section. -->\n\n# Story: <name>\n\n## Outcome\n\n*What is true for whom once this has shipped, in one sentence. If it names a component rather than a\nperson, it is a task — say what changes for someone.*\n\n## Context\n\n*Why this is worth doing now, and what it depends on. Link the epic or specification it comes from\nrather than restating it; the `derived_from` relation already carries the edge.*\n\n## Acceptance\n\n*The conditions under which this is done, each one something a person or a check can observe. \"Works\ncorrectly\" is not one of them.*\n\n## Out of Scope\n\n*What a reasonable reader would expect to be included and is not — the boundary that stops this\nstory quietly becoming an epic.*\n\n## Open Questions\n\n*What is still undecided, each with who decides it. A story carrying an unowned question is a story\nthat stalls without anybody noticing.*\n","owner":"substrate","summary":"Plan 03's three unchecked entry criteria, proven mechanically; held until phase 4 exits by roadmap order.","tags":["daemon","docs","host"],"title":"The Docker driver entry gate is proven before any Docker code"},"args":{"command":"create-entity","data":{"body":"<!-- Starting point for a `story` artifact, seeded by `protocol artifact new story <name>`.\n     No frontmatter here on purpose: the `---` block is written by the CLI from the id, kind, status\n     and relations you gave it, and a second copy in this file would be the one that went stale.\n     Delete the italic guidance as you fill each section. -->\n\n# Story: <name>\n\n## Outcome\n\n*What is true for whom once this has shipped, in one sentence. If it names a component rather than a\nperson, it is a task — say what changes for someone.*\n\n## Context\n\n*Why this is worth doing now, and what it depends on. Link the epic or specification it comes from\nrather than restating it; the `derived_from` relation already carries the edge.*\n\n## Acceptance\n\n*The conditions under which this is done, each one something a person or a check can observe. \"Works\ncorrectly\" is not one of them.*\n\n## Out of Scope\n\n*What a reasonable reader would expect to be included and is not — the boundary that stops this\nstory quietly becoming an epic.*\n\n## Open Questions\n\n*What is still undecided, each with who decides it. A story carrying an unowned question is a story\nthat stalls without anybody noticing.*\n","owner":"substrate","status":"draft","summary":"Plan 03's three unchecked entry criteria, proven mechanically; held until phase 4 exits by roadmap order.","tags":["daemon","docs","host"],"title":"The Docker driver entry gate is proven before any Docker code"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/docker-driver-entry-gate"},"payload":{"actor":"human:timo","at":1788033701199,"causation":"cmd-new-story-docker-driver-entry-gate","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:docker-driver-entry-gate@1#0~abdd4e6d70f76641","executor":null,"recorded_at":"2026-08-29T20:01:41Z"}}
{"entity":"story","version":1,"id":"docker-driver-entry-gate","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:container-driver-entry"}]},"args":{"command":"create-relation","kind":"decomposes","source":"01MEM0000000000000065","target":"01MEM0000000000000004"},"payload":{"actor":"human:timo","at":1788033701204,"causation":"cmd-story-docker-driver-entry-gate-decomposes-epic-container-driver-entry","change":{"change":"related","relation":"decomposes","target":"epic:container-driver-entry"},"correlation":"protocol-artifact-relate","event_id":"story:docker-driver-entry-gate@1#0~e3b60503c3396b46","executor":null,"recorded_at":"2026-08-29T20:01:41Z"}}
{"entity":"story","version":1,"id":"docker-driver-entry-gate","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:container-driver-entry"},{"depends_on":"story:pty-sessions"}]},"args":{"command":"create-relation","kind":"depends_on","source":"01MEM0000000000000065","target":"01MEM0000000000000025"},"payload":{"actor":"human:timo","at":1788033701207,"causation":"cmd-story-docker-driver-entry-gate-depends_on-story-pty-sessions","change":{"change":"related","relation":"depends_on","target":"story:pty-sessions"},"correlation":"protocol-artifact-relate","event_id":"story:docker-driver-entry-gate@1#0~9f73f7ae2ef8ea70","executor":null,"recorded_at":"2026-08-29T20:01:41Z"}}
{"entity":"story","version":1,"id":"docker-driver-entry-gate","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:container-driver-entry"},{"depends_on":"story:pty-sessions"},{"depends_on":"story:sealed-secret-slots"}]},"args":{"command":"create-relation","kind":"depends_on","source":"01MEM0000000000000065","target":"01MEM0000000000000028"},"payload":{"actor":"human:timo","at":1788033701211,"causation":"cmd-story-docker-driver-entry-gate-depends_on-story-sealed-secret-slots","change":{"change":"related","relation":"depends_on","target":"story:sealed-secret-slots"},"correlation":"protocol-artifact-relate","event_id":"story:docker-driver-entry-gate@1#0~2ecae2adef2505c3","executor":null,"recorded_at":"2026-08-29T20:01:41Z"}}
{"entity":"story","version":1,"id":"docker-driver-entry-gate","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:container-driver-entry"},{"depends_on":"story:pty-sessions"},{"depends_on":"story:sealed-secret-slots"},{"depends_on":"story:network-session-authority"}]},"args":{"command":"create-relation","kind":"depends_on","source":"01MEM0000000000000065","target":"01MEM0000000000000019"},"payload":{"actor":"human:timo","at":1788033701214,"causation":"cmd-story-docker-driver-entry-gate-depends_on-story-network-session-authority","change":{"change":"related","relation":"depends_on","target":"story:network-session-authority"},"correlation":"protocol-artifact-relate","event_id":"story:docker-driver-entry-gate@1#0~512653a7ba3ff9ef","executor":null,"recorded_at":"2026-08-29T20:01:41Z"}}
{"entity":"epic","version":1,"id":"release-hardening","revision":2,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"# Epic: Release hardening\n\n## Outcome\n\nWhat the repository says about itself is what a fresh clone, a fresh runner or a consumer can\nverify: a pull request cannot show green without `scripts/gate.sh`; local and CI clippy are the\nsame compiler; `STATUS.md` describes the standalone repository; a tag yields a signed daemon image\nand a signed contract-bundle artifact whose digests are in `CHANGELOG.md`.\n\n## Why Now\n\nSubstrate went public and cut `0.2.1` on 2026-08-29 (`CHANGELOG.md`). Its working agreement calls\nthe full gate \"the bar for `main`\" (`AGENTS.md` § *The gate*), but:\n\n- `.github/workflows/` holds one workflow, `pages.yml`, which builds the website. Nothing runs the\n  gate on a push or a pull request.\n- No `rust-toolchain.toml` exists; `AGENTS.md:116-119` documents the drift (\"a newer clippy can\n  fail a commit that passed locally\") and asks for `rustup update` instead of removing the cause.\n- `Dockerfile` builds a distroless `substrate-daemon`; nothing publishes, signs or digest-pins it.\n  `README.md` § *Status*: \"stable publication: **not done**\".\n- Agent consumes the 0.4.0 bundle as a hand-copied tree (`docs/plan/04-direct-byte-plane.md` §\n  *Slice C*); design 07 § *Bundle* already fixes the OCI shape and nothing implements it.\n- Two `AGENTS.md` facts contradict the scripts they describe (`AGENTS.md:112` vs\n  `scripts/gate.sh:20-23`; `AGENTS.md:172` vs `scripts/bot-token.sh:8`), and `STATUS.md` is\n  observed 2026-08-17, before extraction, still naming the monorepo as the source.\n\n## Scope\n\nSix stories, worked in this order because each makes the next checkable:\n\n1. `story:agents-md-matches-the-scripts` — docs only.\n2. `story:ci-runs-the-full-gate` — `gate.yml`, SHA-pinned actions, `contents: read`.\n3. `story:pinned-rust-toolchain` — `rust-toolchain.toml` = `1.97` = `Cargo.toml` `rust-version` =\n   `Dockerfile` builder tag; one commit per bump.\n4. `story:status-md-re-observed` — from fresh gate output.\n5. `story:signed-daemon-image` — `release.yml` on a bare-version tag, GHCR, cosign keyless.\n6. `story:contract-bundle-oci-artifact` — deterministic OCI layout, published beside the image.\n\n## Out of Scope\n\nAny route, schema, wire identifier, capability or frozen bundle byte. The development bundles stay\ndevelopment bundles; a stable-contract decision is its own ADR under atlas ADR 0019.\nBit-for-bit reproducible binaries: digests are recorded, reproduction is a later milestone.\n\n## Risks\n\n- `packages: write` and `id-token: write` widen a workflow's authority; scope them to the release\n  job only.\n- The delegated lane needs bubblewrap and cgroup delegation and cannot run on a hosted GitHub\n  runner; either a self-hosted runner joins, or CI asserts the portable lane and the delegated\n  lane stays a local pre-release step. The CI story records which.\n- Keyless signing depends on Sigstore availability; a release fails closed, never publishes\n  unsigned.\n\n## Done When\n\nFrom a fresh clone: `AGENTS.md` matches the scripts; a PR with a formatting error goes red; clippy\nruns on the pinned toolchain; `STATUS.md` names the standalone repository and the current tag; a\ntag `0.x.y` yields a signed image and a signed bundle artifact whose digests are in `CHANGELOG.md`.\n"},"args":{"changes":{"body":"# Epic: Release hardening\n\n## Outcome\n\nWhat the repository says about itself is what a fresh clone, a fresh runner or a consumer can\nverify: a pull request cannot show green without `scripts/gate.sh`; local and CI clippy are the\nsame compiler; `STATUS.md` describes the standalone repository; a tag yields a signed daemon image\nand a signed contract-bundle artifact whose digests are in `CHANGELOG.md`.\n\n## Why Now\n\nSubstrate went public and cut `0.2.1` on 2026-08-29 (`CHANGELOG.md`). Its working agreement calls\nthe full gate \"the bar for `main`\" (`AGENTS.md` § *The gate*), but:\n\n- `.github/workflows/` holds one workflow, `pages.yml`, which builds the website. Nothing runs the\n  gate on a push or a pull request.\n- No `rust-toolchain.toml` exists; `AGENTS.md:116-119` documents the drift (\"a newer clippy can\n  fail a commit that passed locally\") and asks for `rustup update` instead of removing the cause.\n- `Dockerfile` builds a distroless `substrate-daemon`; nothing publishes, signs or digest-pins it.\n  `README.md` § *Status*: \"stable publication: **not done**\".\n- Agent consumes the 0.4.0 bundle as a hand-copied tree (`docs/plan/04-direct-byte-plane.md` §\n  *Slice C*); design 07 § *Bundle* already fixes the OCI shape and nothing implements it.\n- Two `AGENTS.md` facts contradict the scripts they describe (`AGENTS.md:112` vs\n  `scripts/gate.sh:20-23`; `AGENTS.md:172` vs `scripts/bot-token.sh:8`), and `STATUS.md` is\n  observed 2026-08-17, before extraction, still naming the monorepo as the source.\n\n## Scope\n\nSix stories, worked in this order because each makes the next checkable:\n\n1. `story:agents-md-matches-the-scripts` — docs only.\n2. `story:ci-runs-the-full-gate` — `gate.yml`, SHA-pinned actions, `contents: read`.\n3. `story:pinned-rust-toolchain` — `rust-toolchain.toml` = `1.97` = `Cargo.toml` `rust-version` =\n   `Dockerfile` builder tag; one commit per bump.\n4. `story:status-md-re-observed` — from fresh gate output.\n5. `story:signed-daemon-image` — `release.yml` on a bare-version tag, GHCR, cosign keyless.\n6. `story:contract-bundle-oci-artifact` — deterministic OCI layout, published beside the image.\n\n## Out of Scope\n\nAny route, schema, wire identifier, capability or frozen bundle byte. The development bundles stay\ndevelopment bundles; a stable-contract decision is its own ADR under atlas ADR 0019.\nBit-for-bit reproducible binaries: digests are recorded, reproduction is a later milestone.\n\n## Risks\n\n- `packages: write` and `id-token: write` widen a workflow's authority; scope them to the release\n  job only.\n- The delegated lane needs bubblewrap and cgroup delegation and cannot run on a hosted GitHub\n  runner; either a self-hosted runner joins, or CI asserts the portable lane and the delegated\n  lane stays a local pre-release step. The CI story records which.\n- Keyless signing depends on Sigstore availability; a release fails closed, never publishes\n  unsigned.\n\n## Done When\n\nFrom a fresh clone: `AGENTS.md` matches the scripts; a PR with a formatting error goes red; clippy\nruns on the pinned toolchain; `STATUS.md` names the standalone repository and the current tag; a\ntag `0.x.y` yields a signed image and a signed bundle artifact whose digests are in `CHANGELOG.md`.\n"},"command":"update-entity","target":"01MEM0000000000000007"},"payload":{"actor":"human:timo","at":1788033811096,"causation":"cmd-protocol-artifact-body-epic-release-hardening-1788033811096","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body","event_id":"epic:release-hardening@2#0~94ac47376188b17a","executor":null,"recorded_at":"2026-08-29T20:03:31Z"}}
{"entity":"epic","version":1,"id":"byte-plane-completion","revision":2,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"# Epic: Byte-plane completion\n\n## Outcome\n\n`ROADMAP.md` phase 4 reads *complete*: `README.md` § *Status* no longer lists PTY or network\nsession authority as absent, and design 05's condition for a live vendor harness — secret and\negress-adjacent capabilities — is met on the secret side.\n\n## Why Now\n\n`ROADMAP.md` orders the phases and forbids starting a later one while an earlier exit criterion is\nopen. Phase 4 is *in progress*: raw-pipe sessions, the distinct session resource, the read-only\nexecution capsule and the delegated model-free Agent lane are green\n(`docs/plan/04-direct-byte-plane.md` § *Slices A–D*). The same plan's § *Later phase-4 slices*\nnames what remains, and three of those items already have accepted designs, so they can proceed\nunder invariant 8 with an ADR each and no new design document:\n\n| capability | accepted design | fixed already |\n|---|---|---|\n| PTY session kind | `docs/design/05-streams-sessions-and-endpoints.md` § 2 | frames: input, output, resize, signal, exit, protocol-error; a PTY never substitutes for pipes; both kinds share the finite frame/byte/queue/idle bounds |\n| sealed secret slots | `docs/design/06-authentication-secrets-and-trust.md` § *V1 decisions* 3 | sealed Linux `memfd` at a declared child descriptor; only the slot→descriptor mapping in the shaped environment; acquired after all admission checks; daemon copy closed after spawn; missing proof reports the capability absent |\n| network session authority | design 05 § *V1 decisions* 3–4 | proof-bound, ≤ 60 s, single redemption, one concurrent attachment; reconnect needs a fresh authority; WebSocket over TLS; non-loopback listeners need TLS/mTLS or a trusted tunnel (`architecture/deployment-postures.md`) |\n\n## Scope\n\nThree stories, each an ADR, a successor bundle, host/daemon work and delegated-lane evidence — in\nthat order, which is the order invariant 8 requires:\n\n1. `story:pty-sessions` — smallest, wholly local, exercises the successor-bundle path again.\n2. `story:sealed-secret-slots` — the capability design 05 § *Progress* names as the reason a live\n   vendor harness is refused.\n3. `story:network-session-authority` — the last exit criterion.\n\nEvery story adds to a successor of `0.4.0`; no frozen byte changes (invariant 6). In sequence: PTY\nis `0.5.0`, slots `0.6.0`, network authority `0.7.0`; two landing together share a bundle and the\nADRs say so. Each successor ships its renderer and checker and joins `scripts/gate.sh`. Each\ncapability is a fact the host verifies at startup; absent verification is a named refusal and the\ncapability document omits the fact (invariants 3, 4). Nothing here opens a second spawn path.\n\n## Out of Scope\n\nReconnect (design 05 defines it as \"a fresh authority\", so it falls out of the network story),\ndestination-bound egress (needs the operator-authority model design 04 § 6 only sketches), and the\nhosted trust-envelope verifier (design 06 § 1, phase 7).\n\n## Risks\n\n- Three successor bundles in quick succession is a lot of frozen directories; batching two into one\n  bundle trades that for a larger review. Decide per story when its predecessor lands.\n- `memfd` sealing is Linux-only; the portable lane must prove the typed refusal, not skip the case.\n- Channel binding for the network authority — TLS exporter (RFC 5705) or a client key — is an ADR\n  decision; the design says only \"proof-bound\".\n\n## Done When\n\nPlan 04 § *Exit evidence* is met, `README.md` § *Status* no longer lists PTY or network session\nauthority as absent, and `ROADMAP.md` phase 4 reads *complete*.\n"},"args":{"changes":{"body":"# Epic: Byte-plane completion\n\n## Outcome\n\n`ROADMAP.md` phase 4 reads *complete*: `README.md` § *Status* no longer lists PTY or network\nsession authority as absent, and design 05's condition for a live vendor harness — secret and\negress-adjacent capabilities — is met on the secret side.\n\n## Why Now\n\n`ROADMAP.md` orders the phases and forbids starting a later one while an earlier exit criterion is\nopen. Phase 4 is *in progress*: raw-pipe sessions, the distinct session resource, the read-only\nexecution capsule and the delegated model-free Agent lane are green\n(`docs/plan/04-direct-byte-plane.md` § *Slices A–D*). The same plan's § *Later phase-4 slices*\nnames what remains, and three of those items already have accepted designs, so they can proceed\nunder invariant 8 with an ADR each and no new design document:\n\n| capability | accepted design | fixed already |\n|---|---|---|\n| PTY session kind | `docs/design/05-streams-sessions-and-endpoints.md` § 2 | frames: input, output, resize, signal, exit, protocol-error; a PTY never substitutes for pipes; both kinds share the finite frame/byte/queue/idle bounds |\n| sealed secret slots | `docs/design/06-authentication-secrets-and-trust.md` § *V1 decisions* 3 | sealed Linux `memfd` at a declared child descriptor; only the slot→descriptor mapping in the shaped environment; acquired after all admission checks; daemon copy closed after spawn; missing proof reports the capability absent |\n| network session authority | design 05 § *V1 decisions* 3–4 | proof-bound, ≤ 60 s, single redemption, one concurrent attachment; reconnect needs a fresh authority; WebSocket over TLS; non-loopback listeners need TLS/mTLS or a trusted tunnel (`architecture/deployment-postures.md`) |\n\n## Scope\n\nThree stories, each an ADR, a successor bundle, host/daemon work and delegated-lane evidence — in\nthat order, which is the order invariant 8 requires:\n\n1. `story:pty-sessions` — smallest, wholly local, exercises the successor-bundle path again.\n2. `story:sealed-secret-slots` — the capability design 05 § *Progress* names as the reason a live\n   vendor harness is refused.\n3. `story:network-session-authority` — the last exit criterion.\n\nEvery story adds to a successor of `0.4.0`; no frozen byte changes (invariant 6). In sequence: PTY\nis `0.5.0`, slots `0.6.0`, network authority `0.7.0`; two landing together share a bundle and the\nADRs say so. Each successor ships its renderer and checker and joins `scripts/gate.sh`. Each\ncapability is a fact the host verifies at startup; absent verification is a named refusal and the\ncapability document omits the fact (invariants 3, 4). Nothing here opens a second spawn path.\n\n## Out of Scope\n\nReconnect (design 05 defines it as \"a fresh authority\", so it falls out of the network story),\ndestination-bound egress (needs the operator-authority model design 04 § 6 only sketches), and the\nhosted trust-envelope verifier (design 06 § 1, phase 7).\n\n## Risks\n\n- Three successor bundles in quick succession is a lot of frozen directories; batching two into one\n  bundle trades that for a larger review. Decide per story when its predecessor lands.\n- `memfd` sealing is Linux-only; the portable lane must prove the typed refusal, not skip the case.\n- Channel binding for the network authority — TLS exporter (RFC 5705) or a client key — is an ADR\n  decision; the design says only \"proof-bound\".\n\n## Done When\n\nPlan 04 § *Exit evidence* is met, `README.md` § *Status* no longer lists PTY or network session\nauthority as absent, and `ROADMAP.md` phase 4 reads *complete*.\n"},"command":"update-entity","target":"01MEM0000000000000001"},"payload":{"actor":"human:timo","at":1788033811109,"causation":"cmd-protocol-artifact-body-epic-byte-plane-completion-1788033811109","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body","event_id":"epic:byte-plane-completion@2#0~86a767d5f977c0f7","executor":null,"recorded_at":"2026-08-29T20:03:31Z"}}
{"entity":"epic","version":1,"id":"container-driver-entry","revision":2,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"# Epic: Container driver entry\n\n## Outcome\n\nPlan 03's entry criteria are proven mechanically before any Docker code exists, so a second driver\ntests the contract instead of the contract bending around it. Plan 03 moves from *deferred* to\n*active* and `ROADMAP.md` phase 5 to *in progress* in the same change.\n\n## Why Now\n\n`docs/plan/03-container-driver.md` is *deferred until host conformance* and names four entry\ncriteria. The first — the minimum host slice passes public-wire and driver conformance — is met\n(`STATUS.md`, phase 3 complete). The other three are claims nobody has checked: driver ports\ncontain no host library types; capability predicates and applied-enforcement observations are\nstable; the security design states which Docker authorities are root-equivalent.\n\n`ROADMAP.md` forbids starting phase 5 while a phase-4 exit criterion is open, so this epic holds\none story and depends on `epic:byte-plane-completion`. It exists now so that the entry gate is\nwritten down before anyone writes a `docker` module.\n\n## Scope\n\n`story:docker-driver-entry-gate`: a structural test that no `substrate-host` type crosses the\ndriver port; the workspace/exec conformance journey as a driver-parameterised harness the host\ndriver passes unchanged; the design 04 section naming root-equivalent Docker authorities.\n\n## Out of Scope\n\nThe Docker driver itself, image pull/inspection, workload lifecycle — plan 03 § *Planned proof*,\nwhich starts once this epic is implemented.\n\n## Risks\n\nThe structural test's shape: a compile-time trait-bound check, a `cargo` dependency-graph\nassertion, or a source scan. Prefer the one that fails on the first offending `use`.\n\n## Done When\n\nThe story is implemented, and plan 03's status and `ROADMAP.md` phase 5 change together.\n"},"args":{"changes":{"body":"# Epic: Container driver entry\n\n## Outcome\n\nPlan 03's entry criteria are proven mechanically before any Docker code exists, so a second driver\ntests the contract instead of the contract bending around it. Plan 03 moves from *deferred* to\n*active* and `ROADMAP.md` phase 5 to *in progress* in the same change.\n\n## Why Now\n\n`docs/plan/03-container-driver.md` is *deferred until host conformance* and names four entry\ncriteria. The first — the minimum host slice passes public-wire and driver conformance — is met\n(`STATUS.md`, phase 3 complete). The other three are claims nobody has checked: driver ports\ncontain no host library types; capability predicates and applied-enforcement observations are\nstable; the security design states which Docker authorities are root-equivalent.\n\n`ROADMAP.md` forbids starting phase 5 while a phase-4 exit criterion is open, so this epic holds\none story and depends on `epic:byte-plane-completion`. It exists now so that the entry gate is\nwritten down before anyone writes a `docker` module.\n\n## Scope\n\n`story:docker-driver-entry-gate`: a structural test that no `substrate-host` type crosses the\ndriver port; the workspace/exec conformance journey as a driver-parameterised harness the host\ndriver passes unchanged; the design 04 section naming root-equivalent Docker authorities.\n\n## Out of Scope\n\nThe Docker driver itself, image pull/inspection, workload lifecycle — plan 03 § *Planned proof*,\nwhich starts once this epic is implemented.\n\n## Risks\n\nThe structural test's shape: a compile-time trait-bound check, a `cargo` dependency-graph\nassertion, or a source scan. Prefer the one that fails on the first offending `use`.\n\n## Done When\n\nThe story is implemented, and plan 03's status and `ROADMAP.md` phase 5 change together.\n"},"command":"update-entity","target":"01MEM0000000000000004"},"payload":{"actor":"human:timo","at":1788033811123,"causation":"cmd-protocol-artifact-body-epic-container-driver-entry-1788033811123","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body","event_id":"epic:container-driver-entry@2#0~485abbae771064b9","executor":null,"recorded_at":"2026-08-29T20:03:31Z"}}
{"entity":"story","version":1,"id":"agents-md-matches-the-scripts","revision":2,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"# Story: AGENTS.md says what the gate and the bot script actually do\n\n## Outcome\n\nA contributor who reads `AGENTS.md` § *The gate* and § *Bot identity* gets facts that match the\nscripts they describe. A false working agreement teaches the next agent to distrust the whole file.\n\n## Context\n\nTwo claims are verified false on 2026-08-29:\n\n| `AGENTS.md` says | the script says |\n|---|---|\n| `AGENTS.md:112-114` — \"`scripts/gate.sh` verifies the 0.1.0 bundle only\"; the 0.2.0/0.3.0/0.4.0 checkers \"are **not** run by the gate\" | `scripts/gate.sh:20-23` runs `check-contract-bundle.py`, `-0.2.0.py`, `-0.3.0.py`, `-0.4.0.py` |\n| `AGENTS.md:172-173` — the bot-org default at `scripts/bot-token.sh:8` \"is not the org this repository lives in\" | `scripts/bot-token.sh:8` — `org=\"${B10X_BOT_ORG:-beyond10x}\"`, this repository's org |\n\n## Acceptance\n\n`AGENTS.md` § *The gate* and § *Bot identity* contain no statement that contradicts\n`scripts/gate.sh` or `scripts/bot-token.sh`, and `README.md` § *Build, test, run* lists the gate's\nsteps in the gate's order.\n\nEvidence that satisfies it:\n\n- the \"verifies the 0.1.0 bundle only\" paragraph is replaced by one that names all four checkers\n  and keeps the rule that a new successor bundle's checker joins `scripts/gate.sh`;\n- the `bot-token.sh` paragraph states the default is `beyond10x`;\n- every `file:line` reference remaining in those sections resolves;\n- `bash scripts/gate.sh` exits 0 (`check-links.py` covers the edited links).\n\n## Out of Scope\n\nThe `rustup update` paragraph (`AGENTS.md:116-119`) is true today and is retired by\n`story:pinned-rust-toolchain`, not here. No behaviour, bundle byte or route changes.\n"},"args":{"changes":{"body":"# Story: AGENTS.md says what the gate and the bot script actually do\n\n## Outcome\n\nA contributor who reads `AGENTS.md` § *The gate* and § *Bot identity* gets facts that match the\nscripts they describe. A false working agreement teaches the next agent to distrust the whole file.\n\n## Context\n\nTwo claims are verified false on 2026-08-29:\n\n| `AGENTS.md` says | the script says |\n|---|---|\n| `AGENTS.md:112-114` — \"`scripts/gate.sh` verifies the 0.1.0 bundle only\"; the 0.2.0/0.3.0/0.4.0 checkers \"are **not** run by the gate\" | `scripts/gate.sh:20-23` runs `check-contract-bundle.py`, `-0.2.0.py`, `-0.3.0.py`, `-0.4.0.py` |\n| `AGENTS.md:172-173` — the bot-org default at `scripts/bot-token.sh:8` \"is not the org this repository lives in\" | `scripts/bot-token.sh:8` — `org=\"${B10X_BOT_ORG:-beyond10x}\"`, this repository's org |\n\n## Acceptance\n\n`AGENTS.md` § *The gate* and § *Bot identity* contain no statement that contradicts\n`scripts/gate.sh` or `scripts/bot-token.sh`, and `README.md` § *Build, test, run* lists the gate's\nsteps in the gate's order.\n\nEvidence that satisfies it:\n\n- the \"verifies the 0.1.0 bundle only\" paragraph is replaced by one that names all four checkers\n  and keeps the rule that a new successor bundle's checker joins `scripts/gate.sh`;\n- the `bot-token.sh` paragraph states the default is `beyond10x`;\n- every `file:line` reference remaining in those sections resolves;\n- `bash scripts/gate.sh` exits 0 (`check-links.py` covers the edited links).\n\n## Out of Scope\n\nThe `rustup update` paragraph (`AGENTS.md:116-119`) is true today and is retired by\n`story:pinned-rust-toolchain`, not here. No behaviour, bundle byte or route changes.\n"},"command":"update-entity","target":"01MEM0000000000000010"},"payload":{"actor":"human:timo","at":1788033829569,"causation":"cmd-protocol-artifact-body-story-agents-md-matches-the-scripts-1788033829569","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body","event_id":"story:agents-md-matches-the-scripts@2#0~43b6e566a302bacb","executor":null,"recorded_at":"2026-08-29T20:03:49Z"}}
{"entity":"story","version":1,"id":"ci-runs-the-full-gate","revision":2,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"# Story: CI runs the full gate on every push and pull request\n\n## Outcome\n\nA pull request against `main` cannot show green unless `bash scripts/gate.sh` passed on a fresh\nrunner.\n\n## Context\n\n`.github/workflows/` holds one workflow, `pages.yml`, which builds the website. The gate\n`AGENTS.md` § *The gate* calls \"the bar for `main`\" runs only on whoever remembers to run it. The\nrepository is public (atlas ADR 0003; `CHANGELOG.md` 0.2.1), so pull requests will arrive from\npeople who have not read `AGENTS.md`.\n\nThe Python checkers are stdlib-only and shell out to the built `substrate-contract-check` crate\n(`scripts/contract_json_gate.py:185`; `jsonschema = \"=0.49.9\"` in `Cargo.toml`), so the workflow\ninstalls no pip packages.\n\n## Acceptance\n\n`.github/workflows/gate.yml` runs `bash scripts/gate.sh` on `push` to `main` and on `pull_request`,\nexits with the gate's own status, and a throwaway branch with one `cargo fmt` violation produces a\nred check that the fix turns green.\n\nEvidence that satisfies it:\n\n- every action SHA-pinned with a version comment; `permissions: contents: read`; `timeout-minutes`\n  — the `pages.yml` conventions;\n- the toolchain is `1.97`, pinned explicitly until `story:pinned-rust-toolchain` makes\n  `rust-toolchain.toml` the source;\n- the job log names which lane ran; on a hosted runner without bubblewrap/cgroup delegation the\n  delegated lane is reported **absent**, never passed (invariant 3);\n- the two run URLs (red, then green) recorded in this story;\n- `bash scripts/gate.sh` exits 0 locally.\n\n## Open Questions\n\nWhether the delegated lane gets a self-hosted runner or stays a local pre-release step. Decides:\noperator. Default if nobody answers: **stays local**; CI asserts the portable lane and prints the\ndelegated lane as absent.\n\nBranch protection (require the `gate` check before merge) is a GitHub setting, not a file; it is a\none-line \"to try\" for the operator after the first green run.\n"},"args":{"changes":{"body":"# Story: CI runs the full gate on every push and pull request\n\n## Outcome\n\nA pull request against `main` cannot show green unless `bash scripts/gate.sh` passed on a fresh\nrunner.\n\n## Context\n\n`.github/workflows/` holds one workflow, `pages.yml`, which builds the website. The gate\n`AGENTS.md` § *The gate* calls \"the bar for `main`\" runs only on whoever remembers to run it. The\nrepository is public (atlas ADR 0003; `CHANGELOG.md` 0.2.1), so pull requests will arrive from\npeople who have not read `AGENTS.md`.\n\nThe Python checkers are stdlib-only and shell out to the built `substrate-contract-check` crate\n(`scripts/contract_json_gate.py:185`; `jsonschema = \"=0.49.9\"` in `Cargo.toml`), so the workflow\ninstalls no pip packages.\n\n## Acceptance\n\n`.github/workflows/gate.yml` runs `bash scripts/gate.sh` on `push` to `main` and on `pull_request`,\nexits with the gate's own status, and a throwaway branch with one `cargo fmt` violation produces a\nred check that the fix turns green.\n\nEvidence that satisfies it:\n\n- every action SHA-pinned with a version comment; `permissions: contents: read`; `timeout-minutes`\n  — the `pages.yml` conventions;\n- the toolchain is `1.97`, pinned explicitly until `story:pinned-rust-toolchain` makes\n  `rust-toolchain.toml` the source;\n- the job log names which lane ran; on a hosted runner without bubblewrap/cgroup delegation the\n  delegated lane is reported **absent**, never passed (invariant 3);\n- the two run URLs (red, then green) recorded in this story;\n- `bash scripts/gate.sh` exits 0 locally.\n\n## Open Questions\n\nWhether the delegated lane gets a self-hosted runner or stays a local pre-release step. Decides:\noperator. Default if nobody answers: **stays local**; CI asserts the portable lane and prints the\ndelegated lane as absent.\n\nBranch protection (require the `gate` check before merge) is a GitHub setting, not a file; it is a\none-line \"to try\" for the operator after the first green run.\n"},"command":"update-entity","target":"01MEM0000000000000013"},"payload":{"actor":"human:timo","at":1788033829588,"causation":"cmd-protocol-artifact-body-story-ci-runs-the-full-gate-1788033829588","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body","event_id":"story:ci-runs-the-full-gate@2#0~63081deab2973a21","executor":null,"recorded_at":"2026-08-29T20:03:49Z"}}
{"entity":"story","version":1,"id":"pinned-rust-toolchain","revision":2,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"# Story: The Rust toolchain is pinned so local and CI clippy agree\n\n## Outcome\n\nA commit that passes `cargo clippy -D warnings` locally passes it in CI, because both use the same\ncompiler. The `rustup update` rule in `AGENTS.md` is retired.\n\n## Context\n\nNo `rust-toolchain.toml` exists. `AGENTS.md:116-119` documents the drift — \"CI installs whatever\n`stable` is that day, and a newer clippy can fail a commit that passed locally\" — and asks people\nto `rustup update` instead of removing the cause. The number is already agreed in two places:\n`rust-version = \"1.97\"` (`Cargo.toml:14`) and `rust:1.97-bookworm` (`Dockerfile:2`).\n\n## Acceptance\n\n`rust-toolchain.toml` pins `1.97` with `rustfmt` and `clippy`, a gate step fails when that number,\n`Cargo.toml` `rust-version` and the `Dockerfile` builder tag disagree, and `AGENTS.md` § *The gate*\nsays a bump is one commit that changes all three.\n\nEvidence that satisfies it:\n\n- `rust-toolchain.toml` at the root: `channel = \"1.97\"`, `components = [\"rustfmt\", \"clippy\"]`;\n- `gate.yml` (`story:ci-runs-the-full-gate`) reads the toolchain from the file, not a channel name;\n- the agreement check in `scripts/`, in `scripts/gate.sh`; verified failing-first by setting one of\n  the three to `1.98`;\n- `bash scripts/gate.sh` exits 0 on the pinned toolchain.\n"},"args":{"changes":{"body":"# Story: The Rust toolchain is pinned so local and CI clippy agree\n\n## Outcome\n\nA commit that passes `cargo clippy -D warnings` locally passes it in CI, because both use the same\ncompiler. The `rustup update` rule in `AGENTS.md` is retired.\n\n## Context\n\nNo `rust-toolchain.toml` exists. `AGENTS.md:116-119` documents the drift — \"CI installs whatever\n`stable` is that day, and a newer clippy can fail a commit that passed locally\" — and asks people\nto `rustup update` instead of removing the cause. The number is already agreed in two places:\n`rust-version = \"1.97\"` (`Cargo.toml:14`) and `rust:1.97-bookworm` (`Dockerfile:2`).\n\n## Acceptance\n\n`rust-toolchain.toml` pins `1.97` with `rustfmt` and `clippy`, a gate step fails when that number,\n`Cargo.toml` `rust-version` and the `Dockerfile` builder tag disagree, and `AGENTS.md` § *The gate*\nsays a bump is one commit that changes all three.\n\nEvidence that satisfies it:\n\n- `rust-toolchain.toml` at the root: `channel = \"1.97\"`, `components = [\"rustfmt\", \"clippy\"]`;\n- `gate.yml` (`story:ci-runs-the-full-gate`) reads the toolchain from the file, not a channel name;\n- the agreement check in `scripts/`, in `scripts/gate.sh`; verified failing-first by setting one of\n  the three to `1.98`;\n- `bash scripts/gate.sh` exits 0 on the pinned toolchain.\n"},"command":"update-entity","target":"01MEM0000000000000025"},"payload":{"actor":"human:timo","at":1788033829600,"causation":"cmd-protocol-artifact-body-story-pinned-rust-toolchain-1788033829600","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body","event_id":"story:pinned-rust-toolchain@2#0~6bb1920f15fe6491","executor":null,"recorded_at":"2026-08-29T20:03:49Z"}}
{"entity":"story","version":1,"id":"status-md-re-observed","revision":2,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"# Story: STATUS.md is observed again from the standalone repository\n\n## Outcome\n\n`STATUS.md` describes the repository a reader is standing in: the standalone public repository at\ntag `0.2.1`, with a *Release* row and a *CI* row, every count taken from that day's gate output.\n\n## Context\n\n`STATUS.md:3` is observed 2026-08-17, before the 2026-08-23 extraction; its *Source* row\n(`STATUS.md:17`) still says `foundation/substrate in the predecessor monorepo`. `AGENTS.md`\n§ *Document placement* makes observed progress `STATUS.md`'s job; a stale one is a placement\nviolation. It depends on `story:ci-runs-the-full-gate` and `story:pinned-rust-toolchain` so the\nCI row is not stale the same week.\n\n## Acceptance\n\n`STATUS.md` names the standalone repository and tag `0.2.1`, carries *Release* and *CI* rows, and\nevery count in it is copied from a command whose output is recorded in this story on the\nobservation date.\n\nEvidence that satisfies it:\n\n- `**Observed:**` is the refresh date;\n- the *Wire contract* counts (operations, vectors, requirements, hash fixtures) come from\n  `scripts/check-contract-bundle-0.4.0.py` output pasted here;\n- the *Release* row: `Dockerfile` present, no publish workflow, no signing, no digest pin — next\n  proof `story:signed-daemon-image`;\n- `## Repository facts` contains no sentence true only of the monorepo era, and each remaining\n  fact names the test or script that proves it;\n- `bash scripts/gate.sh` exits 0.\n"},"args":{"changes":{"body":"# Story: STATUS.md is observed again from the standalone repository\n\n## Outcome\n\n`STATUS.md` describes the repository a reader is standing in: the standalone public repository at\ntag `0.2.1`, with a *Release* row and a *CI* row, every count taken from that day's gate output.\n\n## Context\n\n`STATUS.md:3` is observed 2026-08-17, before the 2026-08-23 extraction; its *Source* row\n(`STATUS.md:17`) still says `foundation/substrate in the predecessor monorepo`. `AGENTS.md`\n§ *Document placement* makes observed progress `STATUS.md`'s job; a stale one is a placement\nviolation. It depends on `story:ci-runs-the-full-gate` and `story:pinned-rust-toolchain` so the\nCI row is not stale the same week.\n\n## Acceptance\n\n`STATUS.md` names the standalone repository and tag `0.2.1`, carries *Release* and *CI* rows, and\nevery count in it is copied from a command whose output is recorded in this story on the\nobservation date.\n\nEvidence that satisfies it:\n\n- `**Observed:**` is the refresh date;\n- the *Wire contract* counts (operations, vectors, requirements, hash fixtures) come from\n  `scripts/check-contract-bundle-0.4.0.py` output pasted here;\n- the *Release* row: `Dockerfile` present, no publish workflow, no signing, no digest pin — next\n  proof `story:signed-daemon-image`;\n- `## Repository facts` contains no sentence true only of the monorepo era, and each remaining\n  fact names the test or script that proves it;\n- `bash scripts/gate.sh` exits 0.\n"},"command":"update-entity","target":"01MEM0000000000000037"},"payload":{"actor":"human:timo","at":1788033829612,"causation":"cmd-protocol-artifact-body-story-status-md-re-observed-1788033829612","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body","event_id":"story:status-md-re-observed@2#0~9c217d9bf60573a9","executor":null,"recorded_at":"2026-08-29T20:03:49Z"}}
{"entity":"story","version":1,"id":"signed-daemon-image","revision":2,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"# Story: A tagged main publishes a signed, digest-pinned daemon image\n\n## Outcome\n\nAn operator can `docker pull ghcr.io/beyond10x/b10x-substrate-daemon@sha256:…` for a tag, verify\nits signature, and know it was built by the bot from that tagged commit.\n\n## Context\n\n`Dockerfile` builds a distroless `substrate-daemon` with an `org.opencontainers.image.revision`\nlabel; no workflow builds, pushes or signs it, and `README.md` § *Status* records \"stable\npublication: **not done**\". `README.md:31` fixes the package prefix `b10x-substrate-*`.\n`AGENTS.md` § *Bot identity* requires automated pushes to use the GitHub App, and § *Releases*\nfixes the tag form: the bare version, annotated, at a fully gated `main` commit.\n\n## Acceptance\n\nAn annotated bare-version tag on `main` produces a keyless-signed image at\n`ghcr.io/beyond10x/b10x-substrate-daemon:<version>` whose digest is written to the GitHub release\nand under the version heading in `CHANGELOG.md`, and a pre-release tag such as `0.2.2-rc.0`\nproduces nothing.\n\nEvidence that satisfies it:\n\n- `.github/workflows/release.yml` triggers only on the bare-version form and refuses to build\n  unless `gate.yml` is green for that commit;\n- `SOURCE_SHA` = the tag's commit; `cosign verify` with the workflow identity succeeds; a signing\n  failure fails the job before anything is announced;\n- release notes state that the wire contract bundles remain development bundles;\n- `permissions` are `contents: read` at workflow level and `packages: write`, `id-token: write` on\n  the release job only; every action SHA-pinned;\n- the pre-release-tag run URL recorded here showing no publish.\n\n## Out of Scope\n\nBit-for-bit reproducible binaries: the sha256 is recorded, reproduction is a later milestone. The\nimage's `VOLUME` and `EXPOSE 8080` describe the development-only TCP posture; release notes must\nnot describe the image as a hosted posture (design 06 § 1).\n"},"args":{"changes":{"body":"# Story: A tagged main publishes a signed, digest-pinned daemon image\n\n## Outcome\n\nAn operator can `docker pull ghcr.io/beyond10x/b10x-substrate-daemon@sha256:…` for a tag, verify\nits signature, and know it was built by the bot from that tagged commit.\n\n## Context\n\n`Dockerfile` builds a distroless `substrate-daemon` with an `org.opencontainers.image.revision`\nlabel; no workflow builds, pushes or signs it, and `README.md` § *Status* records \"stable\npublication: **not done**\". `README.md:31` fixes the package prefix `b10x-substrate-*`.\n`AGENTS.md` § *Bot identity* requires automated pushes to use the GitHub App, and § *Releases*\nfixes the tag form: the bare version, annotated, at a fully gated `main` commit.\n\n## Acceptance\n\nAn annotated bare-version tag on `main` produces a keyless-signed image at\n`ghcr.io/beyond10x/b10x-substrate-daemon:<version>` whose digest is written to the GitHub release\nand under the version heading in `CHANGELOG.md`, and a pre-release tag such as `0.2.2-rc.0`\nproduces nothing.\n\nEvidence that satisfies it:\n\n- `.github/workflows/release.yml` triggers only on the bare-version form and refuses to build\n  unless `gate.yml` is green for that commit;\n- `SOURCE_SHA` = the tag's commit; `cosign verify` with the workflow identity succeeds; a signing\n  failure fails the job before anything is announced;\n- release notes state that the wire contract bundles remain development bundles;\n- `permissions` are `contents: read` at workflow level and `packages: write`, `id-token: write` on\n  the release job only; every action SHA-pinned;\n- the pre-release-tag run URL recorded here showing no publish.\n\n## Out of Scope\n\nBit-for-bit reproducible binaries: the sha256 is recorded, reproduction is a later milestone. The\nimage's `VOLUME` and `EXPOSE 8080` describe the development-only TCP posture; release notes must\nnot describe the image as a hosted posture (design 06 § 1).\n"},"command":"update-entity","target":"01MEM0000000000000034"},"payload":{"actor":"human:timo","at":1788033829624,"causation":"cmd-protocol-artifact-body-story-signed-daemon-image-1788033829624","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body","event_id":"story:signed-daemon-image@2#0~4a380c7c4d10fe7a","executor":null,"recorded_at":"2026-08-29T20:03:49Z"}}
{"entity":"story","version":1,"id":"contract-bundle-oci-artifact","revision":2,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"# Story: The 0.4.0 contract bundle is a signed, digest-pinned OCI artifact\n\n## Outcome\n\nA consumer pins the wire contract by OCI digest instead of copying a directory tree.\n\n## Context\n\n`docs/plan/04-direct-byte-plane.md` § *Slice C* says Agent \"consumes an exact independently\nverified copy\"; the copy is made by hand. `docs/design/07-specification-and-conformance.md`\n§ *Bundle* already fixes the packaging: `bundle.json` lists media type, byte length and digest of\nevery bundle path except itself, and the outer OCI manifest digest pins `bundle.json`. Depends on\n`story:signed-daemon-image` for the release workflow it publishes through.\n\n## Acceptance\n\n`scripts/package-contract-bundle.py <version>` produces a deterministic OCI layout from\n`contracts/substrate-wire/<version>/` without writing into `contracts/`, two runs yield\nbyte-identical manifests, and the release workflow publishes and signs it at\n`ghcr.io/beyond10x/b10x-substrate-wire:<bundle-version>` with the digest in `CHANGELOG.md`.\n\nEvidence that satisfies it:\n\n- `scripts/test_package_contract_bundle.py`: identical manifests on two runs; a one-byte change in\n  any bundle file changes the manifest digest — written failing-first against a stub that embeds\n  a timestamp;\n- `bundle.json` matches design 07's shape and a checker asserts each digest and byte length;\n- `check-contract-bundle-0.4.0.py` and every earlier checker stay green (invariant 6: the packager\n  reads the bundle; the bundle does not learn about the packager);\n- the artifact is annotated `development`; publication does not make the bundle stable (atlas ADR\n  0019 governs contract release);\n- `bash scripts/gate.sh` exits 0.\n"},"args":{"changes":{"body":"# Story: The 0.4.0 contract bundle is a signed, digest-pinned OCI artifact\n\n## Outcome\n\nA consumer pins the wire contract by OCI digest instead of copying a directory tree.\n\n## Context\n\n`docs/plan/04-direct-byte-plane.md` § *Slice C* says Agent \"consumes an exact independently\nverified copy\"; the copy is made by hand. `docs/design/07-specification-and-conformance.md`\n§ *Bundle* already fixes the packaging: `bundle.json` lists media type, byte length and digest of\nevery bundle path except itself, and the outer OCI manifest digest pins `bundle.json`. Depends on\n`story:signed-daemon-image` for the release workflow it publishes through.\n\n## Acceptance\n\n`scripts/package-contract-bundle.py <version>` produces a deterministic OCI layout from\n`contracts/substrate-wire/<version>/` without writing into `contracts/`, two runs yield\nbyte-identical manifests, and the release workflow publishes and signs it at\n`ghcr.io/beyond10x/b10x-substrate-wire:<bundle-version>` with the digest in `CHANGELOG.md`.\n\nEvidence that satisfies it:\n\n- `scripts/test_package_contract_bundle.py`: identical manifests on two runs; a one-byte change in\n  any bundle file changes the manifest digest — written failing-first against a stub that embeds\n  a timestamp;\n- `bundle.json` matches design 07's shape and a checker asserts each digest and byte length;\n- `check-contract-bundle-0.4.0.py` and every earlier checker stay green (invariant 6: the packager\n  reads the bundle; the bundle does not learn about the packager);\n- the artifact is annotated `development`; publication does not make the bundle stable (atlas ADR\n  0019 governs contract release);\n- `bash scripts/gate.sh` exits 0.\n"},"command":"update-entity","target":"01MEM0000000000000016"},"payload":{"actor":"human:timo","at":1788033829636,"causation":"cmd-protocol-artifact-body-story-contract-bundle-oci-artifact-1788033829636","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body","event_id":"story:contract-bundle-oci-artifact@2#0~005018e74258c12e","executor":null,"recorded_at":"2026-08-29T20:03:49Z"}}
{"entity":"story","version":1,"id":"pty-sessions","revision":2,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"# Story: PTY sessions are a distinct session kind with resize\n\n## Outcome\n\nA human can attach a terminal to a confined process, resize it, and get the same lease, single\nattachment, bounded-output and whole-tree cleanup guarantees the raw-pipe session gives.\n\n## Context\n\n`docs/design/05-streams-sessions-and-endpoints.md` § 2 fixes the `pty` kind and its frames\n(input, output, resize, signal, exit, protocol-error) and says a PTY is never substituted for\npipes. `README.md` § *Status* lists PTY as **absent**; `docs/plan/04-direct-byte-plane.md`\n§ *Later phase-4 slices* lists it as remaining. Invariant 8 puts the ADR before code; invariant 6\nputs the wire change in a successor bundle.\n\n## Acceptance\n\nThe delegated lane runs an interactive shell through a `pty` session, echoes bytes, applies a\nresize the child observes, and exits on hangup — while the portable lane proves the typed\n`unserved` and every `0.4.0` byte is unchanged.\n\nEvidence that satisfies it, in order:\n\n1. `adr/0011-pty-sessions-are-a-distinct-session-kind.md`: kind, frame set, resize bounds, never a\n   substitute for pipes, and the named refusal when the host cannot allocate a pty.\n2. `contracts/substrate-wire/0.5.0/` adds the `pty` kind and `resize` frame; compatibility block\n   names `0.4.0` with exact `adds_routes`/`preserves_routes`; `render-contract-bundle-0.5.0.py`\n   and `check-contract-bundle-0.5.0.py` exist and the checker is in `scripts/gate.sh`.\n3. `crates/substrate-host`: pty opened with `nix::pty::openpty` inside the existing\n   bubblewrap/cgroup/cleared-environment path. Failing-first tests:\n   `pty_session_refused_without_confinement`, `pty_resize_is_applied_and_observed` (child reads\n   `TIOCGWINSZ`), `pty_session_kills_tree_on_attachment_loss`.\n4. Daemon: the single-attachment Unix-WebSocket route family serves `pty`; `resize` outside the\n   declared bounds is a protocol error.\n5. Capability fact `sessions.pty` appears only after startup verified pty allocation (invariant 4).\n6. `STATUS.md`, `README.md` § *Status* and plan 04 § *Later phase-4 slices* updated in the same\n   change.\n\n## Open Questions\n\nBundle number: `0.5.0` if this lands first in the epic; the ADR states the actual number.\n"},"args":{"changes":{"body":"# Story: PTY sessions are a distinct session kind with resize\n\n## Outcome\n\nA human can attach a terminal to a confined process, resize it, and get the same lease, single\nattachment, bounded-output and whole-tree cleanup guarantees the raw-pipe session gives.\n\n## Context\n\n`docs/design/05-streams-sessions-and-endpoints.md` § 2 fixes the `pty` kind and its frames\n(input, output, resize, signal, exit, protocol-error) and says a PTY is never substituted for\npipes. `README.md` § *Status* lists PTY as **absent**; `docs/plan/04-direct-byte-plane.md`\n§ *Later phase-4 slices* lists it as remaining. Invariant 8 puts the ADR before code; invariant 6\nputs the wire change in a successor bundle.\n\n## Acceptance\n\nThe delegated lane runs an interactive shell through a `pty` session, echoes bytes, applies a\nresize the child observes, and exits on hangup — while the portable lane proves the typed\n`unserved` and every `0.4.0` byte is unchanged.\n\nEvidence that satisfies it, in order:\n\n1. `adr/0011-pty-sessions-are-a-distinct-session-kind.md`: kind, frame set, resize bounds, never a\n   substitute for pipes, and the named refusal when the host cannot allocate a pty.\n2. `contracts/substrate-wire/0.5.0/` adds the `pty` kind and `resize` frame; compatibility block\n   names `0.4.0` with exact `adds_routes`/`preserves_routes`; `render-contract-bundle-0.5.0.py`\n   and `check-contract-bundle-0.5.0.py` exist and the checker is in `scripts/gate.sh`.\n3. `crates/substrate-host`: pty opened with `nix::pty::openpty` inside the existing\n   bubblewrap/cgroup/cleared-environment path. Failing-first tests:\n   `pty_session_refused_without_confinement`, `pty_resize_is_applied_and_observed` (child reads\n   `TIOCGWINSZ`), `pty_session_kills_tree_on_attachment_loss`.\n4. Daemon: the single-attachment Unix-WebSocket route family serves `pty`; `resize` outside the\n   declared bounds is a protocol error.\n5. Capability fact `sessions.pty` appears only after startup verified pty allocation (invariant 4).\n6. `STATUS.md`, `README.md` § *Status* and plan 04 § *Later phase-4 slices* updated in the same\n   change.\n\n## Open Questions\n\nBundle number: `0.5.0` if this lands first in the epic; the ADR states the actual number.\n"},"command":"update-entity","target":"01MEM0000000000000028"},"payload":{"actor":"human:timo","at":1788033829648,"causation":"cmd-protocol-artifact-body-story-pty-sessions-1788033829648","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body","event_id":"story:pty-sessions@2#0~da9da867328eb5cc","executor":null,"recorded_at":"2026-08-29T20:03:49Z"}}
{"entity":"story","version":1,"id":"sealed-secret-slots","revision":2,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"# Story: Sealed secret slots reach a child only as a memfd descriptor\n\n## Outcome\n\nA run can use a daemon-configured secret without the value ever appearing in argv, environment,\nlogs, events, the ledger request hash or an error body — and design 05's reason for refusing a\nlive vendor harness is removed on the secret side.\n\n## Context\n\n`docs/design/06-authentication-secrets-and-trust.md` § *V1 decisions* 3 fixes the mechanism: a\nsealed Linux `memfd` at a declared child descriptor; only the slot→descriptor mapping in the shaped\nenvironment; acquisition after every admission and dispatch-time check; the daemon closes its copy\nimmediately after spawn; a driver that cannot prove sealing, isolation and cleanup reports the\ncapability absent. Design 04 § 7 (line 79): errors may name a slot, never the value.\n\n## Acceptance\n\nThe delegated lane proves the child reads the value from the declared descriptor and the value\nbytes appear nowhere in captured argv, environment, stdout, stderr, event, ledger or diagnostic\nbytes; the portable lane proves the typed refusal.\n\nEvidence that satisfies it, in order:\n\n1. `adr/0012-secret-slots-are-sealed-memfds.md`: slot naming, descriptor declaration, the seal set\n   (`F_SEAL_WRITE|SHRINK|GROW|SEAL`), refusal classes.\n2. A successor bundle adds `secret_slots` on exec and session start, the capability fact\n   `secrets.slots`, and the refusal class; earlier bundle bytes unchanged.\n3. Failing-first tests: `secret_slot_value_absent_from_argv_env_events_and_ledger`,\n   `secret_slot_memfd_is_sealed` (child `fcntl(F_GET_SEALS)` equals the declared set),\n   `secret_slot_refused_when_sealing_unavailable`, `daemon_closes_its_copy_after_spawn`\n   (`/proc/<daemon>/fd` holds no memfd after spawn).\n4. The ledger request hash covers slot **names** only: two requests differing only in slot value\n   hash identically.\n\n## Out of Scope\n\nBrokered artifact secrets (design 06 decision 4) and destination-bound egress.\n"},"args":{"changes":{"body":"# Story: Sealed secret slots reach a child only as a memfd descriptor\n\n## Outcome\n\nA run can use a daemon-configured secret without the value ever appearing in argv, environment,\nlogs, events, the ledger request hash or an error body — and design 05's reason for refusing a\nlive vendor harness is removed on the secret side.\n\n## Context\n\n`docs/design/06-authentication-secrets-and-trust.md` § *V1 decisions* 3 fixes the mechanism: a\nsealed Linux `memfd` at a declared child descriptor; only the slot→descriptor mapping in the shaped\nenvironment; acquisition after every admission and dispatch-time check; the daemon closes its copy\nimmediately after spawn; a driver that cannot prove sealing, isolation and cleanup reports the\ncapability absent. Design 04 § 7 (line 79): errors may name a slot, never the value.\n\n## Acceptance\n\nThe delegated lane proves the child reads the value from the declared descriptor and the value\nbytes appear nowhere in captured argv, environment, stdout, stderr, event, ledger or diagnostic\nbytes; the portable lane proves the typed refusal.\n\nEvidence that satisfies it, in order:\n\n1. `adr/0012-secret-slots-are-sealed-memfds.md`: slot naming, descriptor declaration, the seal set\n   (`F_SEAL_WRITE|SHRINK|GROW|SEAL`), refusal classes.\n2. A successor bundle adds `secret_slots` on exec and session start, the capability fact\n   `secrets.slots`, and the refusal class; earlier bundle bytes unchanged.\n3. Failing-first tests: `secret_slot_value_absent_from_argv_env_events_and_ledger`,\n   `secret_slot_memfd_is_sealed` (child `fcntl(F_GET_SEALS)` equals the declared set),\n   `secret_slot_refused_when_sealing_unavailable`, `daemon_closes_its_copy_after_spawn`\n   (`/proc/<daemon>/fd` holds no memfd after spawn).\n4. The ledger request hash covers slot **names** only: two requests differing only in slot value\n   hash identically.\n\n## Out of Scope\n\nBrokered artifact secrets (design 06 decision 4) and destination-bound egress.\n"},"command":"update-entity","target":"01MEM0000000000000031"},"payload":{"actor":"human:timo","at":1788033829661,"causation":"cmd-protocol-artifact-body-story-sealed-secret-slots-1788033829661","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body","event_id":"story:sealed-secret-slots@2#0~21090d02bc8b3ab0","executor":null,"recorded_at":"2026-08-29T20:03:49Z"}}
{"entity":"story","version":1,"id":"network-session-authority","revision":2,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"# Story: Network session transport over TLS with single-use proof-bound authority\n\n## Outcome\n\nA client on another machine attaches to a session over WebSocket/TLS with an authority that lives\nat most 60 s, redeems exactly once, and is bound to the redeeming channel; a reconnect always\nneeds a fresh one. This is the last phase-4 exit criterion.\n\n## Context\n\n`docs/design/05-streams-sessions-and-endpoints.md` § *V1 decisions* 3–4 fix the authority and the\ntransport; `architecture/deployment-postures.md` requires TLS/mTLS or a trusted tunnel for any\nnon-loopback control listener. Today only the owner-permissioned Unix socket serves sessions\n(plan 04 § *Slice B*). The static-bearer TCP listener is development-only\n(`crates/substrate-daemon/src/main.rs:63-64`, design 06 § 1) and must not gain this route.\n\n## Acceptance\n\nThe delegated lane drives a raw-pipe session end to end over TLS from a second network namespace,\nand the authority it used cannot be redeemed a second time, after 60 s, or over a different TLS\nsession.\n\nEvidence that satisfies it, in order:\n\n1. An ADR decides the proof binding (TLS exporter per RFC 5705, or a client key) and records that\n   reconnect is a new authority, never a resumed one.\n2. A successor bundle adds the network attachment route and the authority-redemption frame; earlier\n   bundle bytes unchanged.\n3. Failing-first tests: `network_session_listener_refuses_plaintext`,\n   `session_authority_redeems_exactly_once`, `session_authority_expires_after_60s` (tokio paused\n   time), `session_authority_bound_to_channel`, `second_concurrent_attachment_refused`.\n4. The authority value never appears in logs, events or error bodies — a test greps captured\n   diagnostics (design 05 § 3, \"non-loggable\").\n5. The development-only TCP listener does not serve the route — a test proves its absence there.\n\n## Out of Scope\n\nThe hosted trust-envelope verifier (design 06 § 1, atlas ADR 0015; phase 7): this story issues\nthe authority from the local bearer subject.\n"},"args":{"changes":{"body":"# Story: Network session transport over TLS with single-use proof-bound authority\n\n## Outcome\n\nA client on another machine attaches to a session over WebSocket/TLS with an authority that lives\nat most 60 s, redeems exactly once, and is bound to the redeeming channel; a reconnect always\nneeds a fresh one. This is the last phase-4 exit criterion.\n\n## Context\n\n`docs/design/05-streams-sessions-and-endpoints.md` § *V1 decisions* 3–4 fix the authority and the\ntransport; `architecture/deployment-postures.md` requires TLS/mTLS or a trusted tunnel for any\nnon-loopback control listener. Today only the owner-permissioned Unix socket serves sessions\n(plan 04 § *Slice B*). The static-bearer TCP listener is development-only\n(`crates/substrate-daemon/src/main.rs:63-64`, design 06 § 1) and must not gain this route.\n\n## Acceptance\n\nThe delegated lane drives a raw-pipe session end to end over TLS from a second network namespace,\nand the authority it used cannot be redeemed a second time, after 60 s, or over a different TLS\nsession.\n\nEvidence that satisfies it, in order:\n\n1. An ADR decides the proof binding (TLS exporter per RFC 5705, or a client key) and records that\n   reconnect is a new authority, never a resumed one.\n2. A successor bundle adds the network attachment route and the authority-redemption frame; earlier\n   bundle bytes unchanged.\n3. Failing-first tests: `network_session_listener_refuses_plaintext`,\n   `session_authority_redeems_exactly_once`, `session_authority_expires_after_60s` (tokio paused\n   time), `session_authority_bound_to_channel`, `second_concurrent_attachment_refused`.\n4. The authority value never appears in logs, events or error bodies — a test greps captured\n   diagnostics (design 05 § 3, \"non-loggable\").\n5. The development-only TCP listener does not serve the route — a test proves its absence there.\n\n## Out of Scope\n\nThe hosted trust-envelope verifier (design 06 § 1, atlas ADR 0015; phase 7): this story issues\nthe authority from the local bearer subject.\n"},"command":"update-entity","target":"01MEM0000000000000022"},"payload":{"actor":"human:timo","at":1788033829672,"causation":"cmd-protocol-artifact-body-story-network-session-authority-1788033829672","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body","event_id":"story:network-session-authority@2#0~91acbe269f3fb9d7","executor":null,"recorded_at":"2026-08-29T20:03:49Z"}}
{"entity":"story","version":1,"id":"docker-driver-entry-gate","revision":2,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"# Story: The Docker driver entry gate is proven before any Docker code\n\n## Outcome\n\nPlan 03's three unchecked entry criteria are proven mechanically, so a second driver tests the\ncontract instead of the contract bending around it.\n\n## Context\n\n`docs/plan/03-container-driver.md` § *Entry criteria* names four; the first (host conformance)\nis met per `STATUS.md`. The others are unchecked claims: driver ports contain no host library\ntypes; capability predicates and applied-enforcement observations are stable; the security design\nstates which Docker authorities are root-equivalent. `ROADMAP.md` forbids starting phase 5 while\na phase-4 exit criterion is open, so this story depends on the three byte-plane stories and stays\n`draft` until they are implemented.\n\n## Acceptance\n\nA structural test fails on the first `substrate-host` type crossing the driver port, the host\ndriver passes the workspace/exec conformance journey through a driver-parameterised harness, and\ndesign 04 names root-equivalent Docker authorities — landed in the change that moves plan 03 to\n*active* and `ROADMAP.md` phase 5 to *in progress*.\n\nEvidence that satisfies it:\n\n- `driver_port_has_no_host_types` in `crates/substrate-daemon` (only `substrate-wire` types cross);\n- the conformance harness takes the driver as a parameter and the host driver passes unchanged;\n- `docs/design/04-security-and-isolation.md` gains the section: daemon socket, `--privileged`,\n  host network/PID namespaces, arbitrary bind mounts — visible deployment facts, never defaults;\n- plan 03 status and `ROADMAP.md` phase 5 change in the same commit, and not before phase 4\n  reads *complete*.\n"},"args":{"changes":{"body":"# Story: The Docker driver entry gate is proven before any Docker code\n\n## Outcome\n\nPlan 03's three unchecked entry criteria are proven mechanically, so a second driver tests the\ncontract instead of the contract bending around it.\n\n## Context\n\n`docs/plan/03-container-driver.md` § *Entry criteria* names four; the first (host conformance)\nis met per `STATUS.md`. The others are unchecked claims: driver ports contain no host library\ntypes; capability predicates and applied-enforcement observations are stable; the security design\nstates which Docker authorities are root-equivalent. `ROADMAP.md` forbids starting phase 5 while\na phase-4 exit criterion is open, so this story depends on the three byte-plane stories and stays\n`draft` until they are implemented.\n\n## Acceptance\n\nA structural test fails on the first `substrate-host` type crossing the driver port, the host\ndriver passes the workspace/exec conformance journey through a driver-parameterised harness, and\ndesign 04 names root-equivalent Docker authorities — landed in the change that moves plan 03 to\n*active* and `ROADMAP.md` phase 5 to *in progress*.\n\nEvidence that satisfies it:\n\n- `driver_port_has_no_host_types` in `crates/substrate-daemon` (only `substrate-wire` types cross);\n- the conformance harness takes the driver as a parameter and the host driver passes unchanged;\n- `docs/design/04-security-and-isolation.md` gains the section: daemon socket, `--privileged`,\n  host network/PID namespaces, arbitrary bind mounts — visible deployment facts, never defaults;\n- plan 03 status and `ROADMAP.md` phase 5 change in the same commit, and not before phase 4\n  reads *complete*.\n"},"command":"update-entity","target":"01MEM0000000000000019"},"payload":{"actor":"human:timo","at":1788033829686,"causation":"cmd-protocol-artifact-body-story-docker-driver-entry-gate-1788033829686","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body","event_id":"story:docker-driver-entry-gate@2#0~035c56901e24dc9d","executor":null,"recorded_at":"2026-08-29T20:03:49Z"}}
{"entity":"story","version":1,"id":"ledger-rows-carry-the-declared-grant","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"<!-- Starting point for a `story` artifact, seeded by `protocol artifact new story <name>`.\n     No frontmatter here on purpose: the `---` block is written by the CLI from the id, kind, status\n     and relations you gave it, and a second copy in this file would be the one that went stale.\n     Delete the italic guidance as you fill each section. -->\n\n# Story: <name>\n\n## Outcome\n\n*What is true for whom once this has shipped, in one sentence. If it names a component rather than a\nperson, it is a task — say what changes for someone.*\n\n## Context\n\n*Why this is worth doing now, and what it depends on. Link the epic or specification it comes from\nrather than restating it; the `derived_from` relation already carries the edge.*\n\n## Acceptance\n\n*The conditions under which this is done, each one something a person or a check can observe. \"Works\ncorrectly\" is not one of them.*\n\n## Out of Scope\n\n*What a reasonable reader would expect to be included and is not — the boundary that stops this\nstory quietly becoming an epic.*\n\n## Open Questions\n\n*What is still undecided, each with who decides it. A story carrying an unowned question is a story\nthat stalls without anybody noticing.*\n","owner":"substrate","summary":"Atlas O1 exit evidence: every effectful call attributable to a declared grant, verified across the connectors/identity seam from substrate's own record; design 06 section 2 fixes the shape.","tags":["ledger","o1","trust"],"title":"An operation's ledger row carries the declared grant it ran under"},"args":{"command":"create-entity","data":{"body":"<!-- Starting point for a `story` artifact, seeded by `protocol artifact new story <name>`.\n     No frontmatter here on purpose: the `---` block is written by the CLI from the id, kind, status\n     and relations you gave it, and a second copy in this file would be the one that went stale.\n     Delete the italic guidance as you fill each section. -->\n\n# Story: <name>\n\n## Outcome\n\n*What is true for whom once this has shipped, in one sentence. If it names a component rather than a\nperson, it is a task — say what changes for someone.*\n\n## Context\n\n*Why this is worth doing now, and what it depends on. Link the epic or specification it comes from\nrather than restating it; the `derived_from` relation already carries the edge.*\n\n## Acceptance\n\n*The conditions under which this is done, each one something a person or a check can observe. \"Works\ncorrectly\" is not one of them.*\n\n## Out of Scope\n\n*What a reasonable reader would expect to be included and is not — the boundary that stops this\nstory quietly becoming an epic.*\n\n## Open Questions\n\n*What is still undecided, each with who decides it. A story carrying an unowned question is a story\nthat stalls without anybody noticing.*\n","owner":"substrate","status":"draft","summary":"Atlas O1 exit evidence: every effectful call attributable to a declared grant, verified across the connectors/identity seam from substrate's own record; design 06 section 2 fixes the shape.","tags":["ledger","o1","trust"],"title":"An operation's ledger row carries the declared grant it ran under"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/ledger-rows-carry-the-declared-grant"},"payload":{"actor":"human:timo","at":1788036237219,"causation":"cmd-new-story-ledger-rows-carry-the-declared-grant","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:ledger-rows-carry-the-declared-grant@1#0~408110000f34045f","executor":null,"recorded_at":"2026-08-29T20:43:57Z"}}
{"entity":"story","version":1,"id":"destination-bound-egress","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"<!-- Starting point for a `story` artifact, seeded by `protocol artifact new story <name>`.\n     No frontmatter here on purpose: the `---` block is written by the CLI from the id, kind, status\n     and relations you gave it, and a second copy in this file would be the one that went stale.\n     Delete the italic guidance as you fill each section. -->\n\n# Story: <name>\n\n## Outcome\n\n*What is true for whom once this has shipped, in one sentence. If it names a component rather than a\nperson, it is a task — say what changes for someone.*\n\n## Context\n\n*Why this is worth doing now, and what it depends on. Link the epic or specification it comes from\nrather than restating it; the `derived_from` relation already carries the edge.*\n\n## Acceptance\n\n*The conditions under which this is done, each one something a person or a check can observe. \"Works\ncorrectly\" is not one of them.*\n\n## Out of Scope\n\n*What a reasonable reader would expect to be included and is not — the boundary that stops this\nstory quietly becoming an epic.*\n\n## Open Questions\n\n*What is still undecided, each with who decides it. A story carrying an unowned question is a story\nthat stalls without anybody noticing.*\n","owner":"substrate","summary":"Design 04 section 6: ordinary execution has no egress; an aperture is operator authority matched to a destination. With sealed secret slots this is what unlocks a confined vendor harness.","tags":["daemon","host","wire"],"title":"Destination-bound egress apertures are declared, verified and refused by name"},"args":{"command":"create-entity","data":{"body":"<!-- Starting point for a `story` artifact, seeded by `protocol artifact new story <name>`.\n     No frontmatter here on purpose: the `---` block is written by the CLI from the id, kind, status\n     and relations you gave it, and a second copy in this file would be the one that went stale.\n     Delete the italic guidance as you fill each section. -->\n\n# Story: <name>\n\n## Outcome\n\n*What is true for whom once this has shipped, in one sentence. If it names a component rather than a\nperson, it is a task — say what changes for someone.*\n\n## Context\n\n*Why this is worth doing now, and what it depends on. Link the epic or specification it comes from\nrather than restating it; the `derived_from` relation already carries the edge.*\n\n## Acceptance\n\n*The conditions under which this is done, each one something a person or a check can observe. \"Works\ncorrectly\" is not one of them.*\n\n## Out of Scope\n\n*What a reasonable reader would expect to be included and is not — the boundary that stops this\nstory quietly becoming an epic.*\n\n## Open Questions\n\n*What is still undecided, each with who decides it. A story carrying an unowned question is a story\nthat stalls without anybody noticing.*\n","owner":"substrate","status":"draft","summary":"Design 04 section 6: ordinary execution has no egress; an aperture is operator authority matched to a destination. With sealed secret slots this is what unlocks a confined vendor harness.","tags":["daemon","host","wire"],"title":"Destination-bound egress apertures are declared, verified and refused by name"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/destination-bound-egress"},"payload":{"actor":"human:timo","at":1788036237253,"causation":"cmd-new-story-destination-bound-egress","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:destination-bound-egress@1#0~026d975d72a5a331","executor":null,"recorded_at":"2026-08-29T20:43:57Z"}}
{"entity":"story","version":1,"id":"destination-bound-egress","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:byte-plane-completion"}]},"args":{"command":"create-relation","kind":"decomposes","source":"01MEM0000000000000079","target":"01MEM0000000000000001"},"payload":{"actor":"human:timo","at":1788036237257,"causation":"cmd-story-destination-bound-egress-decomposes-epic-byte-plane-completion","change":{"change":"related","relation":"decomposes","target":"epic:byte-plane-completion"},"correlation":"protocol-artifact-relate","event_id":"story:destination-bound-egress@1#0~ea76e6477fa4f382","executor":null,"recorded_at":"2026-08-29T20:43:57Z"}}
{"entity":"story","version":1,"id":"destination-bound-egress","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:byte-plane-completion"},{"depends_on":"story:sealed-secret-slots"}]},"args":{"command":"create-relation","kind":"depends_on","source":"01MEM0000000000000079","target":"01MEM0000000000000034"},"payload":{"actor":"human:timo","at":1788036237260,"causation":"cmd-story-destination-bound-egress-depends_on-story-sealed-secret-slots","change":{"change":"related","relation":"depends_on","target":"story:sealed-secret-slots"},"correlation":"protocol-artifact-relate","event_id":"story:destination-bound-egress@1#0~440a2fe229d98689","executor":null,"recorded_at":"2026-08-29T20:43:57Z"}}
{"entity":"story","version":1,"id":"driver-port-carries-no-host-types","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"<!-- Starting point for a `story` artifact, seeded by `protocol artifact new story <name>`.\n     No frontmatter here on purpose: the `---` block is written by the CLI from the id, kind, status\n     and relations you gave it, and a second copy in this file would be the one that went stale.\n     Delete the italic guidance as you fill each section. -->\n\n# Story: <name>\n\n## Outcome\n\n*What is true for whom once this has shipped, in one sentence. If it names a component rather than a\nperson, it is a task — say what changes for someone.*\n\n## Context\n\n*Why this is worth doing now, and what it depends on. Link the epic or specification it comes from\nrather than restating it; the `derived_from` relation already carries the edge.*\n\n## Acceptance\n\n*The conditions under which this is done, each one something a person or a check can observe. \"Works\ncorrectly\" is not one of them.*\n\n## Out of Scope\n\n*What a reasonable reader would expect to be included and is not — the boundary that stops this\nstory quietly becoming an epic.*\n\n## Open Questions\n\n*What is still undecided, each with who decides it. A story carrying an unowned question is a story\nthat stalls without anybody noticing.*\n","owner":"substrate","summary":"Plan 03 entry criterion 2 and invariant 4, proven by a structural test now; unblocked by phase order because it tests existing code.","tags":["daemon","host"],"title":"No substrate-host type crosses the driver port"},"args":{"command":"create-entity","data":{"body":"<!-- Starting point for a `story` artifact, seeded by `protocol artifact new story <name>`.\n     No frontmatter here on purpose: the `---` block is written by the CLI from the id, kind, status\n     and relations you gave it, and a second copy in this file would be the one that went stale.\n     Delete the italic guidance as you fill each section. -->\n\n# Story: <name>\n\n## Outcome\n\n*What is true for whom once this has shipped, in one sentence. If it names a component rather than a\nperson, it is a task — say what changes for someone.*\n\n## Context\n\n*Why this is worth doing now, and what it depends on. Link the epic or specification it comes from\nrather than restating it; the `derived_from` relation already carries the edge.*\n\n## Acceptance\n\n*The conditions under which this is done, each one something a person or a check can observe. \"Works\ncorrectly\" is not one of them.*\n\n## Out of Scope\n\n*What a reasonable reader would expect to be included and is not — the boundary that stops this\nstory quietly becoming an epic.*\n\n## Open Questions\n\n*What is still undecided, each with who decides it. A story carrying an unowned question is a story\nthat stalls without anybody noticing.*\n","owner":"substrate","status":"draft","summary":"Plan 03 entry criterion 2 and invariant 4, proven by a structural test now; unblocked by phase order because it tests existing code.","tags":["daemon","host"],"title":"No substrate-host type crosses the driver port"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/driver-port-carries-no-host-types"},"payload":{"actor":"human:timo","at":1788036237283,"causation":"cmd-new-story-driver-port-carries-no-host-types","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:driver-port-carries-no-host-types@1#0~272b330ab7c69e05","executor":null,"recorded_at":"2026-08-29T20:43:57Z"}}
{"entity":"story","version":1,"id":"driver-port-carries-no-host-types","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:container-driver-entry"}]},"args":{"command":"create-relation","kind":"decomposes","source":"01MEM0000000000000086","target":"01MEM0000000000000004"},"payload":{"actor":"human:timo","at":1788036237288,"causation":"cmd-story-driver-port-carries-no-host-types-decomposes-epic-container-driver-entry","change":{"change":"related","relation":"decomposes","target":"epic:container-driver-entry"},"correlation":"protocol-artifact-relate","event_id":"story:driver-port-carries-no-host-types@1#0~2946fd0bbef530e7","executor":null,"recorded_at":"2026-08-29T20:43:57Z"}}
{"entity":"story","version":1,"id":"ledger-rows-carry-the-declared-grant","revision":2,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"# Story: An operation's ledger row carries the declared grant it ran under\n\n## Outcome\n\nFrom substrate's own durable record, a reader can answer atlas O1's exit question for every\neffectful operation: *which declared grant authorised this, on behalf of which platform principal,\nthrough which immediate actor* — and an operation presented without a grant reference where the\ndeployment requires one is a named refusal, not a missing column.\n\n## Context\n\n`atlas/ROADMAP.md` O1 — *governed reach* — names substrate an owner and states the exit evidence:\n\"every effectful call in a run's record is attributable to a declared grant; a call outside it is\na named refusal, not a missing row\". Its state column says \"connectors grants and identity\naudiences: not yet verified across a seam\". Substrate's side of that seam is already designed:\n`docs/design/06-authentication-secrets-and-trust.md` § 2 — the initiating platform principal is\nretained separately from the immediate service actor \"through tamper-safe delegated context;\ncaller-written identity strings are not trusted\", and every ledger key is scoped by deployment\nand authenticated subject. Nothing in the backlog implemented that context on the wire or in the\nledger; this is the one item that moves the objective substrate serves (`AGENTS.md` § *Serves*).\n\n## Acceptance\n\nAn exec or session operation started with a delegated-context document records the grant\nreference and platform principal in its ledger row and in its `operation.*` events, the same\nbytes a connectors-side fixture presents, and a test proves a caller-written identity string in\nthe request body is ignored in favour of the verified context.\n\nEvidence that satisfies it, in order:\n\n1. **Before code** (invariant 8): an ADR fixing the delegated-context shape — who signs or seals\n   it, what substrate verifies (audience, expiry, binding to the authenticated subject), and the\n   refusal class when it is required and absent or invalid. Coordinated with `connectors` and\n   `identity` under `atlas/AGENTS.md` § *Cross-repo changes*; the ADR names both relying parties.\n2. A successor bundle adds the optional `delegated_context` request field, the ledger/event\n   fields `grant_ref` and `platform_principal`, and the refusal; earlier bundle bytes unchanged.\n3. Failing-first tests: `ledger_row_records_grant_ref_and_platform_principal`,\n   `caller_written_identity_is_ignored_when_context_is_verified`,\n   `missing_delegated_context_is_refused_by_name_when_required`,\n   `delegated_context_bound_to_another_subject_is_refused`.\n4. A conformance vector pair: substrate's fixture and a connectors-owned copy, byte-identical, so\n   the atlas O1 row can cite both sides.\n\n## Out of Scope\n\nGrant *evaluation* — connectors decides; substrate records and verifies binding only (design 06\n§ 2: \"a higher-layer permit cannot override\" substrate's local checks, and substrate never\nbecomes the authorization engine). Hosted identity material (phase 7).\n\n## Open Questions\n\nWhether the delegated context is a signed JWT-shaped token from identity or a sealed blob from\nconnectors. Decides: the ADR, with both owners. Default if nobody answers: **identity-signed,\naudience `substrate`**, because atlas O1 already pairs \"connectors grants and identity audiences\".\n"},"args":{"changes":{"body":"# Story: An operation's ledger row carries the declared grant it ran under\n\n## Outcome\n\nFrom substrate's own durable record, a reader can answer atlas O1's exit question for every\neffectful operation: *which declared grant authorised this, on behalf of which platform principal,\nthrough which immediate actor* — and an operation presented without a grant reference where the\ndeployment requires one is a named refusal, not a missing column.\n\n## Context\n\n`atlas/ROADMAP.md` O1 — *governed reach* — names substrate an owner and states the exit evidence:\n\"every effectful call in a run's record is attributable to a declared grant; a call outside it is\na named refusal, not a missing row\". Its state column says \"connectors grants and identity\naudiences: not yet verified across a seam\". Substrate's side of that seam is already designed:\n`docs/design/06-authentication-secrets-and-trust.md` § 2 — the initiating platform principal is\nretained separately from the immediate service actor \"through tamper-safe delegated context;\ncaller-written identity strings are not trusted\", and every ledger key is scoped by deployment\nand authenticated subject. Nothing in the backlog implemented that context on the wire or in the\nledger; this is the one item that moves the objective substrate serves (`AGENTS.md` § *Serves*).\n\n## Acceptance\n\nAn exec or session operation started with a delegated-context document records the grant\nreference and platform principal in its ledger row and in its `operation.*` events, the same\nbytes a connectors-side fixture presents, and a test proves a caller-written identity string in\nthe request body is ignored in favour of the verified context.\n\nEvidence that satisfies it, in order:\n\n1. **Before code** (invariant 8): an ADR fixing the delegated-context shape — who signs or seals\n   it, what substrate verifies (audience, expiry, binding to the authenticated subject), and the\n   refusal class when it is required and absent or invalid. Coordinated with `connectors` and\n   `identity` under `atlas/AGENTS.md` § *Cross-repo changes*; the ADR names both relying parties.\n2. A successor bundle adds the optional `delegated_context` request field, the ledger/event\n   fields `grant_ref` and `platform_principal`, and the refusal; earlier bundle bytes unchanged.\n3. Failing-first tests: `ledger_row_records_grant_ref_and_platform_principal`,\n   `caller_written_identity_is_ignored_when_context_is_verified`,\n   `missing_delegated_context_is_refused_by_name_when_required`,\n   `delegated_context_bound_to_another_subject_is_refused`.\n4. A conformance vector pair: substrate's fixture and a connectors-owned copy, byte-identical, so\n   the atlas O1 row can cite both sides.\n\n## Out of Scope\n\nGrant *evaluation* — connectors decides; substrate records and verifies binding only (design 06\n§ 2: \"a higher-layer permit cannot override\" substrate's local checks, and substrate never\nbecomes the authorization engine). Hosted identity material (phase 7).\n\n## Open Questions\n\nWhether the delegated context is a signed JWT-shaped token from identity or a sealed blob from\nconnectors. Decides: the ADR, with both owners. Default if nobody answers: **identity-signed,\naudience `substrate`**, because atlas O1 already pairs \"connectors grants and identity audiences\".\n"},"command":"update-entity","target":"01MEM0000000000000028"},"payload":{"actor":"human:timo","at":1788036237303,"causation":"cmd-protocol-artifact-body-story-ledger-rows-carry-the-declared-grant-1788036237303","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body","event_id":"story:ledger-rows-carry-the-declared-grant@2#0~5b12068330a54ba8","executor":null,"recorded_at":"2026-08-29T20:43:57Z"}}
{"entity":"story","version":1,"id":"destination-bound-egress","revision":2,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"# Story: Destination-bound egress apertures are declared, verified and refused by name\n\n## Outcome\n\nA confined run reaches exactly the network destinations an operator declared for it — and, with\n`story:sealed-secret-slots`, a vendor harness (`codex`, `claude`) can run under substrate\nconfinement with its model credential and its model endpoint, which design 05 § *Progress* says is\nrefused until both capabilities exist.\n\n## Context\n\n`docs/design/04-security-and-isolation.md` § 6: egress, listening sockets and exposed endpoints\nare separate capabilities; ordinary execution defaults to **no egress**; \"an aperture is\ndeployment/operator authority\" matched to a destination. The delegated lane today proves\nnamespace no-egress and nothing else. Atlas O3/O4 want vendor harnesses driven under the same\ngovernor as the native loop; `autodev` dispatches only `codex` and `claude`\n(`atlas/ROADMAP.md`, arrow *autodev → the fleet*). A vendor harness that cannot reach its model\nis not confined, it is dead — so secrets and egress are one capability pair, and the review that\nfiled this story found the backlog held only the first half.\n\n## Acceptance\n\nA run started with a declared egress aperture reaches the declared destination and nothing else,\nthe applied aperture is an observation in the run's record, and a request for an aperture the\ndeployment did not declare is `unserved` with the aperture named — proven by the delegated lane\nwith a real listener inside and outside the aperture.\n\nEvidence that satisfies it, in order:\n\n1. **Before code**: an ADR fixing the aperture shape (host:port or name:port, protocol, whether\n   DNS resolution is inside the aperture), the operator declaration surface, and the refusal.\n2. A successor bundle adds the request field, the capability fact `network.egress.apertures`, the\n   observation, and the refusal; earlier bundle bytes unchanged.\n3. Failing-first tests: `egress_defaults_to_none`, `declared_aperture_is_reachable`,\n   `undeclared_destination_is_unreachable_and_named`,\n   `aperture_outside_operator_declaration_is_unserved`, `applied_aperture_is_observed`.\n4. The delegated lane runs the model-free fake app-server against a loopback endpoint inside the\n   aperture and proves a second endpoint outside it is unreachable.\n\n## Out of Scope\n\nPublic ingress, DNS, TLS termination and tunnels (design 05 § 4). Brokered connector artifacts.\n\n## Open Questions\n\nEnforcement mechanism inside the existing namespace no-egress posture: a veth pair plus nftables\nin the sandbox namespace, or a userspace proxy at a declared descriptor. Decides: the ADR. Default\nif nobody answers: **namespace + nftables**, because it keeps bytes out of the daemon.\n"},"args":{"changes":{"body":"# Story: Destination-bound egress apertures are declared, verified and refused by name\n\n## Outcome\n\nA confined run reaches exactly the network destinations an operator declared for it — and, with\n`story:sealed-secret-slots`, a vendor harness (`codex`, `claude`) can run under substrate\nconfinement with its model credential and its model endpoint, which design 05 § *Progress* says is\nrefused until both capabilities exist.\n\n## Context\n\n`docs/design/04-security-and-isolation.md` § 6: egress, listening sockets and exposed endpoints\nare separate capabilities; ordinary execution defaults to **no egress**; \"an aperture is\ndeployment/operator authority\" matched to a destination. The delegated lane today proves\nnamespace no-egress and nothing else. Atlas O3/O4 want vendor harnesses driven under the same\ngovernor as the native loop; `autodev` dispatches only `codex` and `claude`\n(`atlas/ROADMAP.md`, arrow *autodev → the fleet*). A vendor harness that cannot reach its model\nis not confined, it is dead — so secrets and egress are one capability pair, and the review that\nfiled this story found the backlog held only the first half.\n\n## Acceptance\n\nA run started with a declared egress aperture reaches the declared destination and nothing else,\nthe applied aperture is an observation in the run's record, and a request for an aperture the\ndeployment did not declare is `unserved` with the aperture named — proven by the delegated lane\nwith a real listener inside and outside the aperture.\n\nEvidence that satisfies it, in order:\n\n1. **Before code**: an ADR fixing the aperture shape (host:port or name:port, protocol, whether\n   DNS resolution is inside the aperture), the operator declaration surface, and the refusal.\n2. A successor bundle adds the request field, the capability fact `network.egress.apertures`, the\n   observation, and the refusal; earlier bundle bytes unchanged.\n3. Failing-first tests: `egress_defaults_to_none`, `declared_aperture_is_reachable`,\n   `undeclared_destination_is_unreachable_and_named`,\n   `aperture_outside_operator_declaration_is_unserved`, `applied_aperture_is_observed`.\n4. The delegated lane runs the model-free fake app-server against a loopback endpoint inside the\n   aperture and proves a second endpoint outside it is unreachable.\n\n## Out of Scope\n\nPublic ingress, DNS, TLS termination and tunnels (design 05 § 4). Brokered connector artifacts.\n\n## Open Questions\n\nEnforcement mechanism inside the existing namespace no-egress posture: a veth pair plus nftables\nin the sandbox namespace, or a userspace proxy at a declared descriptor. Decides: the ADR. Default\nif nobody answers: **namespace + nftables**, because it keeps bytes out of the daemon.\n"},"command":"update-entity","target":"01MEM0000000000000019"},"payload":{"actor":"human:timo","at":1788036237318,"causation":"cmd-protocol-artifact-body-story-destination-bound-egress-1788036237318","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body","event_id":"story:destination-bound-egress@2#0~b85cd0ffa0404a6c","executor":null,"recorded_at":"2026-08-29T20:43:57Z"}}
{"entity":"story","version":1,"id":"driver-port-carries-no-host-types","revision":2,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"# Story: No substrate-host type crosses the driver port\n\n## Outcome\n\nPlan 03's second entry criterion — \"driver ports contain no host library types\" — is a test that\nfails on the first offending `use`, not a claim. A second driver can then be written against the\nport without the port having quietly become the host driver's shape.\n\n## Context\n\n`docs/plan/03-container-driver.md` § *Entry criteria*; `AGENTS.md` invariant 4 (\"clients do not\nbranch on driver internals\"). This half of the Docker entry gate tests code that already exists\nand does not wait for phase 4; the review of 2026-08-29 split it out of\n`story:docker-driver-entry-gate`, which keeps the phase-5-bound half.\n\n## Acceptance\n\nA test in `crates/substrate-daemon` fails when any item reachable from the driver port module\nresolves to a `substrate-host` type, and passes at HEAD.\n\nEvidence that satisfies it:\n\n- the test (`driver_port_has_no_host_types`, or a `cargo` dependency-graph assertion if the port\n  is its own crate — the story records which shape was chosen and why);\n- verified failing-first by adding one `substrate_host::` import to the port and watching it fail;\n- `bash scripts/gate.sh` exits 0.\n"},"args":{"changes":{"body":"# Story: No substrate-host type crosses the driver port\n\n## Outcome\n\nPlan 03's second entry criterion — \"driver ports contain no host library types\" — is a test that\nfails on the first offending `use`, not a claim. A second driver can then be written against the\nport without the port having quietly become the host driver's shape.\n\n## Context\n\n`docs/plan/03-container-driver.md` § *Entry criteria*; `AGENTS.md` invariant 4 (\"clients do not\nbranch on driver internals\"). This half of the Docker entry gate tests code that already exists\nand does not wait for phase 4; the review of 2026-08-29 split it out of\n`story:docker-driver-entry-gate`, which keeps the phase-5-bound half.\n\n## Acceptance\n\nA test in `crates/substrate-daemon` fails when any item reachable from the driver port module\nresolves to a `substrate-host` type, and passes at HEAD.\n\nEvidence that satisfies it:\n\n- the test (`driver_port_has_no_host_types`, or a `cargo` dependency-graph assertion if the port\n  is its own crate — the story records which shape was chosen and why);\n- verified failing-first by adding one `substrate_host::` import to the port and watching it fail;\n- `bash scripts/gate.sh` exits 0.\n"},"command":"update-entity","target":"01MEM0000000000000025"},"payload":{"actor":"human:timo","at":1788036237332,"causation":"cmd-protocol-artifact-body-story-driver-port-carries-no-host-types-1788036237332","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body","event_id":"story:driver-port-carries-no-host-types@2#0~f5f86ecbfad214d0","executor":null,"recorded_at":"2026-08-29T20:43:57Z"}}
{"entity":"story","version":1,"id":"docker-driver-entry-gate","revision":3,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"# Story: The Docker driver entry gate is proven before any Docker code\n\n## Outcome\n\nPlan 03's remaining entry criteria are proven mechanically, so a second driver tests the contract\ninstead of the contract bending around it. Plan 03 moves from *deferred* to *active* and\n`ROADMAP.md` phase 5 to *in progress* in the same change.\n\n## Context\n\n`docs/plan/03-container-driver.md` § *Entry criteria* names four. The first (host conformance) is\nmet per `STATUS.md`; the second is `story:driver-port-carries-no-host-types`, split out on\n2026-08-29 because it does not wait for phase 4. This story keeps the two that do: a\ndriver-parameterised conformance journey, and the security-design section naming root-equivalent\nDocker authorities — both of which shape the Docker driver itself, which `ROADMAP.md` holds\nbehind phase 4's exit.\n\n## Acceptance\n\nThe host driver passes the workspace/exec conformance journey through a harness that takes the\ndriver as a parameter, design 04 names root-equivalent Docker authorities, and plan 03 and\n`ROADMAP.md` phase 5 change together — not before phase 4 reads *complete*.\n\nEvidence that satisfies it:\n\n- the conformance harness takes the driver as a parameter and the host driver passes unchanged;\n- `docs/design/04-security-and-isolation.md` gains the section: daemon socket, `--privileged`,\n  host network/PID namespaces, arbitrary bind mounts — visible deployment facts, never defaults;\n- plan 03 status and `ROADMAP.md` phase 5 change in the same commit.\n"},"args":{"changes":{"body":"# Story: The Docker driver entry gate is proven before any Docker code\n\n## Outcome\n\nPlan 03's remaining entry criteria are proven mechanically, so a second driver tests the contract\ninstead of the contract bending around it. Plan 03 moves from *deferred* to *active* and\n`ROADMAP.md` phase 5 to *in progress* in the same change.\n\n## Context\n\n`docs/plan/03-container-driver.md` § *Entry criteria* names four. The first (host conformance) is\nmet per `STATUS.md`; the second is `story:driver-port-carries-no-host-types`, split out on\n2026-08-29 because it does not wait for phase 4. This story keeps the two that do: a\ndriver-parameterised conformance journey, and the security-design section naming root-equivalent\nDocker authorities — both of which shape the Docker driver itself, which `ROADMAP.md` holds\nbehind phase 4's exit.\n\n## Acceptance\n\nThe host driver passes the workspace/exec conformance journey through a harness that takes the\ndriver as a parameter, design 04 names root-equivalent Docker authorities, and plan 03 and\n`ROADMAP.md` phase 5 change together — not before phase 4 reads *complete*.\n\nEvidence that satisfies it:\n\n- the conformance harness takes the driver as a parameter and the host driver passes unchanged;\n- `docs/design/04-security-and-isolation.md` gains the section: daemon socket, `--privileged`,\n  host network/PID namespaces, arbitrary bind mounts — visible deployment facts, never defaults;\n- plan 03 status and `ROADMAP.md` phase 5 change in the same commit.\n"},"command":"update-entity","target":"01MEM0000000000000022"},"payload":{"actor":"human:timo","at":1788036237347,"causation":"cmd-protocol-artifact-body-story-docker-driver-entry-gate-1788036237347","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body","event_id":"story:docker-driver-entry-gate@3#0~d1d6b2b10a5bbc24","executor":null,"recorded_at":"2026-08-29T20:43:57Z"}}
{"entity":"epic","version":1,"id":"byte-plane-completion","revision":3,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"# Epic: Byte-plane completion\n\n## Outcome\n\n`ROADMAP.md` phase 4 reads *complete*: `README.md` § *Status* no longer lists PTY or network\nsession authority as absent, and design 05's condition for a live vendor harness — secret and\negress-adjacent capabilities — is met on the secret side.\n\n## Why Now\n\n`ROADMAP.md` orders the phases and forbids starting a later one while an earlier exit criterion is\nopen. Phase 4 is *in progress*: raw-pipe sessions, the distinct session resource, the read-only\nexecution capsule and the delegated model-free Agent lane are green\n(`docs/plan/04-direct-byte-plane.md` § *Slices A–D*). The same plan's § *Later phase-4 slices*\nnames what remains, and three of those items already have accepted designs, so they can proceed\nunder invariant 8 with an ADR each and no new design document:\n\n| capability | accepted design | fixed already |\n|---|---|---|\n| PTY session kind | `docs/design/05-streams-sessions-and-endpoints.md` § 2 | frames: input, output, resize, signal, exit, protocol-error; a PTY never substitutes for pipes; both kinds share the finite frame/byte/queue/idle bounds |\n| sealed secret slots | `docs/design/06-authentication-secrets-and-trust.md` § *V1 decisions* 3 | sealed Linux `memfd` at a declared child descriptor; only the slot→descriptor mapping in the shaped environment; acquired after all admission checks; daemon copy closed after spawn; missing proof reports the capability absent |\n| network session authority | design 05 § *V1 decisions* 3–4 | proof-bound, ≤ 60 s, single redemption, one concurrent attachment; reconnect needs a fresh authority; WebSocket over TLS; non-loopback listeners need TLS/mTLS or a trusted tunnel (`architecture/deployment-postures.md`) |\n\n## Scope\n\nFour stories, each an ADR, a successor bundle, host/daemon work and delegated-lane evidence — in\nthat order, which is the order invariant 8 requires:\n\n1. `story:pty-sessions` — smallest, wholly local, exercises the successor-bundle path again.\n2. `story:sealed-secret-slots` — the capability design 05 § *Progress* names as the reason a live\n   vendor harness is refused.\n3. `story:destination-bound-egress` — the other half of the vendor-harness pair; secrets without\n   egress unlock nothing (added by the 2026-08-29 review against atlas).\n4. `story:network-session-authority` — the last exit criterion, and the one with no consumer named.\n\nEvery story adds to a successor of `0.4.0`; no frozen byte changes (invariant 6). In sequence: PTY\nis `0.5.0`, slots `0.6.0`, network authority `0.7.0`; two landing together share a bundle and the\nADRs say so. Each successor ships its renderer and checker and joins `scripts/gate.sh`. Each\ncapability is a fact the host verifies at startup; absent verification is a named refusal and the\ncapability document omits the fact (invariants 3, 4). Nothing here opens a second spawn path.\n\n## Out of Scope\n\nReconnect (design 05 defines it as \"a fresh authority\", so it falls out of the network story), and\nthe hosted trust-envelope verifier (design 06 § 1, phase 7).\n\n## Risks\n\n- Three successor bundles in quick succession is a lot of frozen directories; batching two into one\n  bundle trades that for a larger review. Decide per story when its predecessor lands.\n- `memfd` sealing is Linux-only; the portable lane must prove the typed refusal, not skip the case.\n- Channel binding for the network authority — TLS exporter (RFC 5705) or a client key — is an ADR\n  decision; the design says only \"proof-bound\".\n\n## Done When\n\nPlan 04 § *Exit evidence* is met, `README.md` § *Status* no longer lists PTY or network session\nauthority as absent, and `ROADMAP.md` phase 4 reads *complete*.\n"},"args":{"changes":{"body":"# Epic: Byte-plane completion\n\n## Outcome\n\n`ROADMAP.md` phase 4 reads *complete*: `README.md` § *Status* no longer lists PTY or network\nsession authority as absent, and design 05's condition for a live vendor harness — secret and\negress-adjacent capabilities — is met on the secret side.\n\n## Why Now\n\n`ROADMAP.md` orders the phases and forbids starting a later one while an earlier exit criterion is\nopen. Phase 4 is *in progress*: raw-pipe sessions, the distinct session resource, the read-only\nexecution capsule and the delegated model-free Agent lane are green\n(`docs/plan/04-direct-byte-plane.md` § *Slices A–D*). The same plan's § *Later phase-4 slices*\nnames what remains, and three of those items already have accepted designs, so they can proceed\nunder invariant 8 with an ADR each and no new design document:\n\n| capability | accepted design | fixed already |\n|---|---|---|\n| PTY session kind | `docs/design/05-streams-sessions-and-endpoints.md` § 2 | frames: input, output, resize, signal, exit, protocol-error; a PTY never substitutes for pipes; both kinds share the finite frame/byte/queue/idle bounds |\n| sealed secret slots | `docs/design/06-authentication-secrets-and-trust.md` § *V1 decisions* 3 | sealed Linux `memfd` at a declared child descriptor; only the slot→descriptor mapping in the shaped environment; acquired after all admission checks; daemon copy closed after spawn; missing proof reports the capability absent |\n| network session authority | design 05 § *V1 decisions* 3–4 | proof-bound, ≤ 60 s, single redemption, one concurrent attachment; reconnect needs a fresh authority; WebSocket over TLS; non-loopback listeners need TLS/mTLS or a trusted tunnel (`architecture/deployment-postures.md`) |\n\n## Scope\n\nFour stories, each an ADR, a successor bundle, host/daemon work and delegated-lane evidence — in\nthat order, which is the order invariant 8 requires:\n\n1. `story:pty-sessions` — smallest, wholly local, exercises the successor-bundle path again.\n2. `story:sealed-secret-slots` — the capability design 05 § *Progress* names as the reason a live\n   vendor harness is refused.\n3. `story:destination-bound-egress` — the other half of the vendor-harness pair; secrets without\n   egress unlock nothing (added by the 2026-08-29 review against atlas).\n4. `story:network-session-authority` — the last exit criterion, and the one with no consumer named.\n\nEvery story adds to a successor of `0.4.0`; no frozen byte changes (invariant 6). In sequence: PTY\nis `0.5.0`, slots `0.6.0`, network authority `0.7.0`; two landing together share a bundle and the\nADRs say so. Each successor ships its renderer and checker and joins `scripts/gate.sh`. Each\ncapability is a fact the host verifies at startup; absent verification is a named refusal and the\ncapability document omits the fact (invariants 3, 4). Nothing here opens a second spawn path.\n\n## Out of Scope\n\nReconnect (design 05 defines it as \"a fresh authority\", so it falls out of the network story), and\nthe hosted trust-envelope verifier (design 06 § 1, phase 7).\n\n## Risks\n\n- Three successor bundles in quick succession is a lot of frozen directories; batching two into one\n  bundle trades that for a larger review. Decide per story when its predecessor lands.\n- `memfd` sealing is Linux-only; the portable lane must prove the typed refusal, not skip the case.\n- Channel binding for the network authority — TLS exporter (RFC 5705) or a client key — is an ADR\n  decision; the design says only \"proof-bound\".\n\n## Done When\n\nPlan 04 § *Exit evidence* is met, `README.md` § *Status* no longer lists PTY or network session\nauthority as absent, and `ROADMAP.md` phase 4 reads *complete*.\n"},"command":"update-entity","target":"01MEM0000000000000001"},"payload":{"actor":"human:timo","at":1788036237383,"causation":"cmd-protocol-artifact-body-epic-byte-plane-completion-1788036237383","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body","event_id":"epic:byte-plane-completion@3#0~055023cdce7bd4c5","executor":null,"recorded_at":"2026-08-29T20:43:57Z"}}
{"entity":"story","version":1,"id":"ci-runs-the-full-gate","revision":3,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000013","to":"proposed"},"payload":{"actor":"human:timo","at":1788036555587,"causation":"cmd-move-story-ci-runs-the-full-gate-1788036555587","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:ci-runs-the-full-gate@3#0~035cef12cd9405cc","executor":null,"recorded_at":"2026-08-29T20:49:15Z"}}
{"entity":"story","version":1,"id":"ci-runs-the-full-gate","revision":4,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000013","to":"active"},"payload":{"actor":"human:timo","at":1788036555612,"causation":"cmd-move-story-ci-runs-the-full-gate-1788036555612","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:ci-runs-the-full-gate@4#0~489ac3135f862260","executor":null,"recorded_at":"2026-08-29T20:49:15Z"}}
{"entity":"story","version":1,"id":"agents-md-matches-the-scripts","revision":3,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000010","to":"proposed"},"payload":{"actor":"human:timo","at":1788036555632,"causation":"cmd-move-story-agents-md-matches-the-scripts-1788036555632","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:agents-md-matches-the-scripts@3#0~0579e912cb63b379","executor":null,"recorded_at":"2026-08-29T20:49:15Z"}}
{"entity":"story","version":1,"id":"agents-md-matches-the-scripts","revision":4,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000010","to":"active"},"payload":{"actor":"human:timo","at":1788036555652,"causation":"cmd-move-story-agents-md-matches-the-scripts-1788036555652","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:agents-md-matches-the-scripts@4#0~647b0407ee3c1771","executor":null,"recorded_at":"2026-08-29T20:49:15Z"}}
{"entity":"story","version":1,"id":"pinned-rust-toolchain","revision":3,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000034","to":"proposed"},"payload":{"actor":"human:timo","at":1788036555672,"causation":"cmd-move-story-pinned-rust-toolchain-1788036555672","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:pinned-rust-toolchain@3#0~1a89715c0ab4ce77","executor":null,"recorded_at":"2026-08-29T20:49:15Z"}}
{"entity":"story","version":1,"id":"pinned-rust-toolchain","revision":4,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000034","to":"active"},"payload":{"actor":"human:timo","at":1788036555692,"causation":"cmd-move-story-pinned-rust-toolchain-1788036555692","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:pinned-rust-toolchain@4#0~2eb905fb95f71fd7","executor":null,"recorded_at":"2026-08-29T20:49:15Z"}}
{"entity":"story","version":1,"id":"driver-port-carries-no-host-types","revision":3,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000025","to":"proposed"},"payload":{"actor":"human:timo","at":1788036555712,"causation":"cmd-move-story-driver-port-carries-no-host-types-1788036555712","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:driver-port-carries-no-host-types@3#0~b0cdceaaa91e388d","executor":null,"recorded_at":"2026-08-29T20:49:15Z"}}
{"entity":"story","version":1,"id":"driver-port-carries-no-host-types","revision":4,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000025","to":"active"},"payload":{"actor":"human:timo","at":1788036555732,"causation":"cmd-move-story-driver-port-carries-no-host-types-1788036555732","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:driver-port-carries-no-host-types@4#0~8cecb13e8118b9bd","executor":null,"recorded_at":"2026-08-29T20:49:15Z"}}
{"entity":"story","version":1,"id":"contract-bundle-oci-artifact","revision":3,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000016","to":"proposed"},"payload":{"actor":"human:timo","at":1788036555752,"causation":"cmd-move-story-contract-bundle-oci-artifact-1788036555752","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:contract-bundle-oci-artifact@3#0~c531405e856b932f","executor":null,"recorded_at":"2026-08-29T20:49:15Z"}}
{"entity":"story","version":1,"id":"contract-bundle-oci-artifact","revision":4,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000016","to":"active"},"payload":{"actor":"human:timo","at":1788036555772,"causation":"cmd-move-story-contract-bundle-oci-artifact-1788036555772","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:contract-bundle-oci-artifact@4#0~c833b53be4960cdf","executor":null,"recorded_at":"2026-08-29T20:49:15Z"}}
{"entity":"story","version":1,"id":"agents-md-matches-the-scripts","revision":5,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"body":"# Story: AGENTS.md says what the gate and the bot script actually do\n\n## Outcome\n\nA contributor who reads `AGENTS.md` § *The gate* and § *Bot identity* gets facts that match the\nscripts they describe. A false working agreement teaches the next agent to distrust the whole file.\n\n## Context\n\nTwo claims are verified false on 2026-08-29:\n\n| `AGENTS.md` says | the script says |\n|---|---|\n| `AGENTS.md:112-114` — \"`scripts/gate.sh` verifies the 0.1.0 bundle only\"; the 0.2.0/0.3.0/0.4.0 checkers \"are **not** run by the gate\" | `scripts/gate.sh:20-23` runs `check-contract-bundle.py`, `-0.2.0.py`, `-0.3.0.py`, `-0.4.0.py` |\n| `AGENTS.md:172-173` — the bot-org default at `scripts/bot-token.sh:8` \"is not the org this repository lives in\" | `scripts/bot-token.sh:8` — `org=\"${B10X_BOT_ORG:-beyond10x}\"`, this repository's org |\n\n## Acceptance\n\n`AGENTS.md` § *The gate* and § *Bot identity* contain no statement that contradicts\n`scripts/gate.sh` or `scripts/bot-token.sh`, and `README.md` § *Build, test, run* lists the gate's\nsteps in the gate's order.\n\nEvidence that satisfies it:\n\n- the \"verifies the 0.1.0 bundle only\" paragraph is replaced by one that names all four checkers\n  and keeps the rule that a new successor bundle's checker joins `scripts/gate.sh`;\n- the `bot-token.sh` paragraph states the default is `beyond10x`;\n- every `file:line` reference remaining in those sections resolves;\n- `bash scripts/gate.sh` exits 0 (`check-links.py` covers the edited links).\n\n## Out of Scope\n\nThe `rustup update` paragraph (`AGENTS.md:116-119`) is true today and is retired by\n`story:pinned-rust-toolchain`, not here. No behaviour, bundle byte or route changes.\n\n## Implemented — 2026-08-29\n\n- `AGENTS.md` § *The gate*: step list matches `scripts/gate.sh:15-26`; the \"0.1.0 bundle only\"\n  paragraph is gone; the `rustup update` paragraph is replaced by the `rust-toolchain.toml` pin,\n  the one-commit bump rule and `scripts/check-toolchain.py`; the `tail`-exit-status sentence kept.\n- `AGENTS.md` § *Bot identity*: the default at `scripts/bot-token.sh:8` is stated as this\n  repository's org, quoting `org=\"${B10X_BOT_ORG:-beyond10x}\"`.\n- `README.md` § *Build, test, run*: table in gate order, four bundle rows split out, the dead\n  `check-brand.sh` row removed (the script does not exist), `bundle packager` and `toolchain`\n  rows added.\n- `grep -n 'verifies the 0.1.0 bundle only\\|is not the org this repository lives in\\|rustup\n  update' AGENTS.md` → no hits. `python3 scripts/check-links.py` → exit 0.\n- Full gate: see the evidence record on this story.\n"},"args":{"changes":{"body":"# Story: AGENTS.md says what the gate and the bot script actually do\n\n## Outcome\n\nA contributor who reads `AGENTS.md` § *The gate* and § *Bot identity* gets facts that match the\nscripts they describe. A false working agreement teaches the next agent to distrust the whole file.\n\n## Context\n\nTwo claims are verified false on 2026-08-29:\n\n| `AGENTS.md` says | the script says |\n|---|---|\n| `AGENTS.md:112-114` — \"`scripts/gate.sh` verifies the 0.1.0 bundle only\"; the 0.2.0/0.3.0/0.4.0 checkers \"are **not** run by the gate\" | `scripts/gate.sh:20-23` runs `check-contract-bundle.py`, `-0.2.0.py`, `-0.3.0.py`, `-0.4.0.py` |\n| `AGENTS.md:172-173` — the bot-org default at `scripts/bot-token.sh:8` \"is not the org this repository lives in\" | `scripts/bot-token.sh:8` — `org=\"${B10X_BOT_ORG:-beyond10x}\"`, this repository's org |\n\n## Acceptance\n\n`AGENTS.md` § *The gate* and § *Bot identity* contain no statement that contradicts\n`scripts/gate.sh` or `scripts/bot-token.sh`, and `README.md` § *Build, test, run* lists the gate's\nsteps in the gate's order.\n\nEvidence that satisfies it:\n\n- the \"verifies the 0.1.0 bundle only\" paragraph is replaced by one that names all four checkers\n  and keeps the rule that a new successor bundle's checker joins `scripts/gate.sh`;\n- the `bot-token.sh` paragraph states the default is `beyond10x`;\n- every `file:line` reference remaining in those sections resolves;\n- `bash scripts/gate.sh` exits 0 (`check-links.py` covers the edited links).\n\n## Out of Scope\n\nThe `rustup update` paragraph (`AGENTS.md:116-119`) is true today and is retired by\n`story:pinned-rust-toolchain`, not here. No behaviour, bundle byte or route changes.\n\n## Implemented — 2026-08-29\n\n- `AGENTS.md` § *The gate*: step list matches `scripts/gate.sh:15-26`; the \"0.1.0 bundle only\"\n  paragraph is gone; the `rustup update` paragraph is replaced by the `rust-toolchain.toml` pin,\n  the one-commit bump rule and `scripts/check-toolchain.py`; the `tail`-exit-status sentence kept.\n- `AGENTS.md` § *Bot identity*: the default at `scripts/bot-token.sh:8` is stated as this\n  repository's org, quoting `org=\"${B10X_BOT_ORG:-beyond10x}\"`.\n- `README.md` § *Build, test, run*: table in gate order, four bundle rows split out, the dead\n  `check-brand.sh` row removed (the script does not exist), `bundle packager` and `toolchain`\n  rows added.\n- `grep -n 'verifies the 0.1.0 bundle only\\|is not the org this repository lives in\\|rustup\n  update' AGENTS.md` → no hits. `python3 scripts/check-links.py` → exit 0.\n- Full gate: see the evidence record on this story.\n"},"command":"update-entity","target":"01MEM0000000000000010"},"payload":{"actor":"human:timo","at":1788037216255,"causation":"cmd-protocol-artifact-body-story-agents-md-matches-the-scripts-1788037216255","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body","event_id":"story:agents-md-matches-the-scripts@5#0~9917ac9c285f92a1","executor":null,"recorded_at":"2026-08-29T21:00:16Z"}}
{"entity":"story","version":1,"id":"pinned-rust-toolchain","revision":5,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"body":"# Story: The Rust toolchain is pinned so local and CI clippy agree\n\n## Outcome\n\nA commit that passes `cargo clippy -D warnings` locally passes it in CI, because both use the same\ncompiler. The `rustup update` rule in `AGENTS.md` is retired.\n\n## Context\n\nNo `rust-toolchain.toml` exists. `AGENTS.md:116-119` documents the drift — \"CI installs whatever\n`stable` is that day, and a newer clippy can fail a commit that passed locally\" — and asks people\nto `rustup update` instead of removing the cause. The number is already agreed in two places:\n`rust-version = \"1.97\"` (`Cargo.toml:14`) and `rust:1.97-bookworm` (`Dockerfile:2`).\n\n## Acceptance\n\n`rust-toolchain.toml` pins `1.97` with `rustfmt` and `clippy`, a gate step fails when that number,\n`Cargo.toml` `rust-version` and the `Dockerfile` builder tag disagree, and `AGENTS.md` § *The gate*\nsays a bump is one commit that changes all three.\n\nEvidence that satisfies it:\n\n- `rust-toolchain.toml` at the root: `channel = \"1.97\"`, `components = [\"rustfmt\", \"clippy\"]`;\n- `gate.yml` (`story:ci-runs-the-full-gate`) reads the toolchain from the file, not a channel name;\n- the agreement check in `scripts/`, in `scripts/gate.sh`; verified failing-first by setting one of\n  the three to `1.98`;\n- `bash scripts/gate.sh` exits 0 on the pinned toolchain.\n\n## Implemented — 2026-08-29\n\n- `rust-toolchain.toml`: `channel = \"1.97\"`, `components = [\"rustfmt\", \"clippy\"]`.\n  `rustup show active-toolchain` from the root → `1.97-x86_64-unknown-linux-gnu (overridden by\n  …/rust-toolchain.toml)`; `cargo 1.97.1`.\n- `scripts/check-toolchain.py` (stdlib, `--root`): exit 0 at HEAD — `Rust toolchain pinned at\n  1.97 in rust-toolchain.toml, Cargo.toml, Dockerfile`. Failing-first on scratch copies under\n  `~/.cache/claude-tmp/toolchain-check/`: Dockerfile tag 1.98 → exit 1 naming all three\n  `file:line` values; `Cargo.toml` 1.98 → exit 1; missing components → exit 1.\n- `scripts/gate.sh:26`: `run python3 scripts/check-toolchain.py`. `.github/workflows/gate.yml`\n  reads the channel from `rust-toolchain.toml`.\n- `cargo clippy --workspace --all-targets --locked -- -D warnings` on 1.97 → exit 0;\n  `Cargo.lock` unchanged. `AGENTS.md` § *The gate* carries the bump rule\n  (`story:agents-md-matches-the-scripts`).\n"},"args":{"changes":{"body":"# Story: The Rust toolchain is pinned so local and CI clippy agree\n\n## Outcome\n\nA commit that passes `cargo clippy -D warnings` locally passes it in CI, because both use the same\ncompiler. The `rustup update` rule in `AGENTS.md` is retired.\n\n## Context\n\nNo `rust-toolchain.toml` exists. `AGENTS.md:116-119` documents the drift — \"CI installs whatever\n`stable` is that day, and a newer clippy can fail a commit that passed locally\" — and asks people\nto `rustup update` instead of removing the cause. The number is already agreed in two places:\n`rust-version = \"1.97\"` (`Cargo.toml:14`) and `rust:1.97-bookworm` (`Dockerfile:2`).\n\n## Acceptance\n\n`rust-toolchain.toml` pins `1.97` with `rustfmt` and `clippy`, a gate step fails when that number,\n`Cargo.toml` `rust-version` and the `Dockerfile` builder tag disagree, and `AGENTS.md` § *The gate*\nsays a bump is one commit that changes all three.\n\nEvidence that satisfies it:\n\n- `rust-toolchain.toml` at the root: `channel = \"1.97\"`, `components = [\"rustfmt\", \"clippy\"]`;\n- `gate.yml` (`story:ci-runs-the-full-gate`) reads the toolchain from the file, not a channel name;\n- the agreement check in `scripts/`, in `scripts/gate.sh`; verified failing-first by setting one of\n  the three to `1.98`;\n- `bash scripts/gate.sh` exits 0 on the pinned toolchain.\n\n## Implemented — 2026-08-29\n\n- `rust-toolchain.toml`: `channel = \"1.97\"`, `components = [\"rustfmt\", \"clippy\"]`.\n  `rustup show active-toolchain` from the root → `1.97-x86_64-unknown-linux-gnu (overridden by\n  …/rust-toolchain.toml)`; `cargo 1.97.1`.\n- `scripts/check-toolchain.py` (stdlib, `--root`): exit 0 at HEAD — `Rust toolchain pinned at\n  1.97 in rust-toolchain.toml, Cargo.toml, Dockerfile`. Failing-first on scratch copies under\n  `~/.cache/claude-tmp/toolchain-check/`: Dockerfile tag 1.98 → exit 1 naming all three\n  `file:line` values; `Cargo.toml` 1.98 → exit 1; missing components → exit 1.\n- `scripts/gate.sh:26`: `run python3 scripts/check-toolchain.py`. `.github/workflows/gate.yml`\n  reads the channel from `rust-toolchain.toml`.\n- `cargo clippy --workspace --all-targets --locked -- -D warnings` on 1.97 → exit 0;\n  `Cargo.lock` unchanged. `AGENTS.md` § *The gate* carries the bump rule\n  (`story:agents-md-matches-the-scripts`).\n"},"command":"update-entity","target":"01MEM0000000000000034"},"payload":{"actor":"human:timo","at":1788037216269,"causation":"cmd-protocol-artifact-body-story-pinned-rust-toolchain-1788037216269","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body","event_id":"story:pinned-rust-toolchain@5#0~246e1d0ce5c5ad60","executor":null,"recorded_at":"2026-08-29T21:00:16Z"}}
{"entity":"story","version":1,"id":"driver-port-carries-no-host-types","revision":5,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"body":"# Story: No substrate-host type crosses the driver port\n\n## Outcome\n\nPlan 03's second entry criterion — \"driver ports contain no host library types\" — is a test that\nfails on the first offending `use`, not a claim. A second driver can then be written against the\nport without the port having quietly become the host driver's shape.\n\n## Context\n\n`docs/plan/03-container-driver.md` § *Entry criteria*; `AGENTS.md` invariant 4 (\"clients do not\nbranch on driver internals\"). This half of the Docker entry gate tests code that already exists\nand does not wait for phase 4; the review of 2026-08-29 split it out of\n`story:docker-driver-entry-gate`, which keeps the phase-5-bound half.\n\n## Acceptance\n\nA test in `crates/substrate-daemon` fails when any item reachable from the driver port module\nresolves to a `substrate-host` type, and passes at HEAD.\n\nEvidence that satisfies it:\n\n- the test (`driver_port_has_no_host_types`, or a `cargo` dependency-graph assertion if the port\n  is its own crate — the story records which shape was chosen and why);\n- verified failing-first by adding one `substrate_host::` import to the port and watching it fail;\n- `bash scripts/gate.sh` exits 0.\n\n## Re-scoped on the finding — 2026-08-29\n\nThe port trait is a **host** type: `pub trait Driver` is `crates/substrate-host/src/lib.rs:171`,\nand its signature forces six more `substrate-host` types on every caller — `DriverError`\n(`lib.rs:95`), `DriverErrorClass` (`:84`), `DispatchOutcome` (`:103`),\n`WorkspaceDestroyProgress` (`:111`), `ExecObservation` (re-export `:26`), `PipeStream`\n(`process.rs:41`). \"Driver ports contain no host library types\" therefore cannot be read\nliterally at HEAD. The checkable claim, and the one the test enforces:\n\n> Outside the composition root (`src/main.rs`, `src/runtime.rs`) and the in-crate test harness\n> (`src/app/tests.rs`), the daemon names nothing from `substrate_host` except the port trait and\n> the types its signature forces.\n\nConcrete host types (`HostDriver`, `HostConfig`) appear only at `src/runtime.rs:21,355,360` at\nHEAD. Moving `Driver` and its vocabulary into a port crate is follow-up work, not this story.\n\n## Implemented — 2026-08-29\n\n- `crates/substrate-daemon/tests/driver_port.rs`: `driver_port_has_no_host_types` plus two\n  parser self-tests so the walker cannot pass vacuously. Stdlib only; `Cargo.lock` unchanged.\n- `cargo test -p substrate-daemon --test driver_port --locked` → `3 passed; 0 failed`.\n- Failing-first: `use substrate_host::{HostConfig, HostDriver};` injected into\n  `src/app/execs.rs` → exit 101 naming `execs.rs:9: substrate_host::HostConfig` /\n  `…HostDriver`; file restored, absent from `git diff --stat`.\n- `cargo clippy -p substrate-daemon --all-targets --locked -- -D warnings` → exit 0.\n"},"args":{"changes":{"body":"# Story: No substrate-host type crosses the driver port\n\n## Outcome\n\nPlan 03's second entry criterion — \"driver ports contain no host library types\" — is a test that\nfails on the first offending `use`, not a claim. A second driver can then be written against the\nport without the port having quietly become the host driver's shape.\n\n## Context\n\n`docs/plan/03-container-driver.md` § *Entry criteria*; `AGENTS.md` invariant 4 (\"clients do not\nbranch on driver internals\"). This half of the Docker entry gate tests code that already exists\nand does not wait for phase 4; the review of 2026-08-29 split it out of\n`story:docker-driver-entry-gate`, which keeps the phase-5-bound half.\n\n## Acceptance\n\nA test in `crates/substrate-daemon` fails when any item reachable from the driver port module\nresolves to a `substrate-host` type, and passes at HEAD.\n\nEvidence that satisfies it:\n\n- the test (`driver_port_has_no_host_types`, or a `cargo` dependency-graph assertion if the port\n  is its own crate — the story records which shape was chosen and why);\n- verified failing-first by adding one `substrate_host::` import to the port and watching it fail;\n- `bash scripts/gate.sh` exits 0.\n\n## Re-scoped on the finding — 2026-08-29\n\nThe port trait is a **host** type: `pub trait Driver` is `crates/substrate-host/src/lib.rs:171`,\nand its signature forces six more `substrate-host` types on every caller — `DriverError`\n(`lib.rs:95`), `DriverErrorClass` (`:84`), `DispatchOutcome` (`:103`),\n`WorkspaceDestroyProgress` (`:111`), `ExecObservation` (re-export `:26`), `PipeStream`\n(`process.rs:41`). \"Driver ports contain no host library types\" therefore cannot be read\nliterally at HEAD. The checkable claim, and the one the test enforces:\n\n> Outside the composition root (`src/main.rs`, `src/runtime.rs`) and the in-crate test harness\n> (`src/app/tests.rs`), the daemon names nothing from `substrate_host` except the port trait and\n> the types its signature forces.\n\nConcrete host types (`HostDriver`, `HostConfig`) appear only at `src/runtime.rs:21,355,360` at\nHEAD. Moving `Driver` and its vocabulary into a port crate is follow-up work, not this story.\n\n## Implemented — 2026-08-29\n\n- `crates/substrate-daemon/tests/driver_port.rs`: `driver_port_has_no_host_types` plus two\n  parser self-tests so the walker cannot pass vacuously. Stdlib only; `Cargo.lock` unchanged.\n- `cargo test -p substrate-daemon --test driver_port --locked` → `3 passed; 0 failed`.\n- Failing-first: `use substrate_host::{HostConfig, HostDriver};` injected into\n  `src/app/execs.rs` → exit 101 naming `execs.rs:9: substrate_host::HostConfig` /\n  `…HostDriver`; file restored, absent from `git diff --stat`.\n- `cargo clippy -p substrate-daemon --all-targets --locked -- -D warnings` → exit 0.\n"},"command":"update-entity","target":"01MEM0000000000000025"},"payload":{"actor":"human:timo","at":1788037216283,"causation":"cmd-protocol-artifact-body-story-driver-port-carries-no-host-types-1788037216283","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body","event_id":"story:driver-port-carries-no-host-types@5#0~5d75904bce685abb","executor":null,"recorded_at":"2026-08-29T21:00:16Z"}}
{"entity":"story","version":1,"id":"ci-runs-the-full-gate","revision":5,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"body":"# Story: CI runs the full gate on every push and pull request\n\n## Outcome\n\nA pull request against `main` cannot show green unless `bash scripts/gate.sh` passed on a fresh\nrunner.\n\n## Context\n\n`.github/workflows/` holds one workflow, `pages.yml`, which builds the website. The gate\n`AGENTS.md` § *The gate* calls \"the bar for `main`\" runs only on whoever remembers to run it. The\nrepository is public (atlas ADR 0003; `CHANGELOG.md` 0.2.1), so pull requests will arrive from\npeople who have not read `AGENTS.md`.\n\nThe Python checkers are stdlib-only and shell out to the built `substrate-contract-check` crate\n(`scripts/contract_json_gate.py:185`; `jsonschema = \"=0.49.9\"` in `Cargo.toml`), so the workflow\ninstalls no pip packages.\n\n## Acceptance\n\n`.github/workflows/gate.yml` runs `bash scripts/gate.sh` on `push` to `main` and on `pull_request`,\nexits with the gate's own status, and a throwaway branch with one `cargo fmt` violation produces a\nred check that the fix turns green.\n\nEvidence that satisfies it:\n\n- every action SHA-pinned with a version comment; `permissions: contents: read`; `timeout-minutes`\n  — the `pages.yml` conventions;\n- the toolchain is `1.97`, pinned explicitly until `story:pinned-rust-toolchain` makes\n  `rust-toolchain.toml` the source;\n- the job log names which lane ran; on a hosted runner without bubblewrap/cgroup delegation the\n  delegated lane is reported **absent**, never passed (invariant 3);\n- the two run URLs (red, then green) recorded in this story;\n- `bash scripts/gate.sh` exits 0 locally.\n\n## Open Questions\n\nWhether the delegated lane gets a self-hosted runner or stays a local pre-release step. Decides:\noperator. Default if nobody answers: **stays local**; CI asserts the portable lane and prints the\ndelegated lane as absent.\n\nBranch protection (require the `gate` check before merge) is a GitHub setting, not a file; it is a\none-line \"to try\" for the operator after the first green run.\n\n## Progress — 2026-08-29\n\n- `.github/workflows/gate.yml` written: `push` to `main`, `pull_request`, `workflow_dispatch`;\n  `actions/checkout@3d3c42e…` (# v7, from `pages.yml:35`) and `actions/cache@55cc8345…` (# v6,\n  `gh api repos/actions/cache/git/ref/tags/v6`); `permissions: contents: read`;\n  `timeout-minutes`; `cancel-in-progress` only for pull requests.\n- Toolchain read from `rust-toolchain.toml` with `1.97` as fallback; runner `rustup`, no\n  third-party action.\n- Lanes: a probe step (`bwrap`, `/sys/fs/cgroup/cgroup.controllers`) before the gate and a\n  `Lane summary` step to `$GITHUB_STEP_SUMMARY` after; delegated lane stated **absent** on a\n  hosted runner (decision: stays a local pre-release step). The runner-images inventory for\n  ubuntu-24.04 lists no bubblewrap.\n- `actionlint 1.7.12` + `shellcheck 0.11.0` → exit 0; YAML parses.\n- **Open:** the red-then-green run URLs need a pushed branch. To try, as the bot:\n  1. `git switch -c ci-gate-smoke && printf '\\n\\n' >> crates/substrate-wire/src/lib.rs` (fails\n     `cargo fmt --all --check`, triggers no clippy lint) → commit, push, open a PR → expect red.\n  2. `cargo fmt --all` → commit, push → expect green; record both run URLs here.\n  3. After the first green run: branch protection on `main` requiring the `Full gate` check.\n"},"args":{"changes":{"body":"# Story: CI runs the full gate on every push and pull request\n\n## Outcome\n\nA pull request against `main` cannot show green unless `bash scripts/gate.sh` passed on a fresh\nrunner.\n\n## Context\n\n`.github/workflows/` holds one workflow, `pages.yml`, which builds the website. The gate\n`AGENTS.md` § *The gate* calls \"the bar for `main`\" runs only on whoever remembers to run it. The\nrepository is public (atlas ADR 0003; `CHANGELOG.md` 0.2.1), so pull requests will arrive from\npeople who have not read `AGENTS.md`.\n\nThe Python checkers are stdlib-only and shell out to the built `substrate-contract-check` crate\n(`scripts/contract_json_gate.py:185`; `jsonschema = \"=0.49.9\"` in `Cargo.toml`), so the workflow\ninstalls no pip packages.\n\n## Acceptance\n\n`.github/workflows/gate.yml` runs `bash scripts/gate.sh` on `push` to `main` and on `pull_request`,\nexits with the gate's own status, and a throwaway branch with one `cargo fmt` violation produces a\nred check that the fix turns green.\n\nEvidence that satisfies it:\n\n- every action SHA-pinned with a version comment; `permissions: contents: read`; `timeout-minutes`\n  — the `pages.yml` conventions;\n- the toolchain is `1.97`, pinned explicitly until `story:pinned-rust-toolchain` makes\n  `rust-toolchain.toml` the source;\n- the job log names which lane ran; on a hosted runner without bubblewrap/cgroup delegation the\n  delegated lane is reported **absent**, never passed (invariant 3);\n- the two run URLs (red, then green) recorded in this story;\n- `bash scripts/gate.sh` exits 0 locally.\n\n## Open Questions\n\nWhether the delegated lane gets a self-hosted runner or stays a local pre-release step. Decides:\noperator. Default if nobody answers: **stays local**; CI asserts the portable lane and prints the\ndelegated lane as absent.\n\nBranch protection (require the `gate` check before merge) is a GitHub setting, not a file; it is a\none-line \"to try\" for the operator after the first green run.\n\n## Progress — 2026-08-29\n\n- `.github/workflows/gate.yml` written: `push` to `main`, `pull_request`, `workflow_dispatch`;\n  `actions/checkout@3d3c42e…` (# v7, from `pages.yml:35`) and `actions/cache@55cc8345…` (# v6,\n  `gh api repos/actions/cache/git/ref/tags/v6`); `permissions: contents: read`;\n  `timeout-minutes`; `cancel-in-progress` only for pull requests.\n- Toolchain read from `rust-toolchain.toml` with `1.97` as fallback; runner `rustup`, no\n  third-party action.\n- Lanes: a probe step (`bwrap`, `/sys/fs/cgroup/cgroup.controllers`) before the gate and a\n  `Lane summary` step to `$GITHUB_STEP_SUMMARY` after; delegated lane stated **absent** on a\n  hosted runner (decision: stays a local pre-release step). The runner-images inventory for\n  ubuntu-24.04 lists no bubblewrap.\n- `actionlint 1.7.12` + `shellcheck 0.11.0` → exit 0; YAML parses.\n- **Open:** the red-then-green run URLs need a pushed branch. To try, as the bot:\n  1. `git switch -c ci-gate-smoke && printf '\\n\\n' >> crates/substrate-wire/src/lib.rs` (fails\n     `cargo fmt --all --check`, triggers no clippy lint) → commit, push, open a PR → expect red.\n  2. `cargo fmt --all` → commit, push → expect green; record both run URLs here.\n  3. After the first green run: branch protection on `main` requiring the `Full gate` check.\n"},"command":"update-entity","target":"01MEM0000000000000013"},"payload":{"actor":"human:timo","at":1788037216297,"causation":"cmd-protocol-artifact-body-story-ci-runs-the-full-gate-1788037216297","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body","event_id":"story:ci-runs-the-full-gate@5#0~869dc377f4f826b5","executor":null,"recorded_at":"2026-08-29T21:00:16Z"}}
{"entity":"story","version":1,"id":"contract-bundle-oci-artifact","revision":5,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"body":"# Story: The 0.4.0 contract bundle is a signed, digest-pinned OCI artifact\n\n## Outcome\n\nA consumer pins the wire contract by OCI digest instead of copying a directory tree.\n\n## Context\n\n`docs/plan/04-direct-byte-plane.md` § *Slice C* says Agent \"consumes an exact independently\nverified copy\"; the copy is made by hand. `docs/design/07-specification-and-conformance.md`\n§ *Bundle* already fixes the packaging: `bundle.json` lists media type, byte length and digest of\nevery bundle path except itself, and the outer OCI manifest digest pins `bundle.json`. Depends on\n`story:signed-daemon-image` for the release workflow it publishes through.\n\n## Acceptance\n\n`scripts/package-contract-bundle.py <version>` produces a deterministic OCI layout from\n`contracts/substrate-wire/<version>/` without writing into `contracts/`, two runs yield\nbyte-identical manifests, and the release workflow publishes and signs it at\n`ghcr.io/beyond10x/b10x-substrate-wire:<bundle-version>` with the digest in `CHANGELOG.md`.\n\nEvidence that satisfies it:\n\n- `scripts/test_package_contract_bundle.py`: identical manifests on two runs; a one-byte change in\n  any bundle file changes the manifest digest — written failing-first against a stub that embeds\n  a timestamp;\n- `bundle.json` matches design 07's shape and a checker asserts each digest and byte length;\n- `check-contract-bundle-0.4.0.py` and every earlier checker stay green (invariant 6: the packager\n  reads the bundle; the bundle does not learn about the packager);\n- the artifact is annotated `development`; publication does not make the bundle stable (atlas ADR\n  0019 governs contract release);\n- `bash scripts/gate.sh` exits 0.\n\n## Progress — 2026-08-29 (packager half)\n\n- `scripts/package-contract-bundle.py <version> --out <dir>`: OCI Image Layout (`oci-layout`,\n  `index.json`, `blobs/sha256/…`); config **is** `bundle.json` verbatim, media type\n  `application/vnd.b10x.substrate-wire.bundle.v1+json`, so the manifest digest pins\n  `bundle.json` (design 07 § *Bundle*); one layer per bundle file, layer digest = the `sha256`\n  already in `bundle.json`; annotations `org.opencontainers.image.version`,\n  `dev.b10x.contract.status=development`, `ref.name` on the index entry. Refuses `--out` inside\n  `contracts/`, non-empty `--out` without `--force`, symlinks, a file set disagreeing with\n  `bundle.json`.\n- `scripts/test_package_contract_bundle.py`: 14 tests; determinism test written failing-first\n  against a timestamp-embedding stub (`FAILED (failures=1)`), then green. Two runs on `0.4.0` →\n  `sha256:f94d15fc116587d991aab6de5628f6ee5baf872af4e7c79d2a35e2b17a8485c4`, `diff -r` empty.\n  `oras manifest fetch --oci-layout` resolves the same digest; `oras pull` round-trip matches\n  `contracts/substrate-wire/0.4.0` byte for byte.\n- `check-contract-bundle-0.4.0.py` → exit 0; `git status contracts/` → clean (invariant 6).\n- Wired into the gate at `scripts/gate.sh:24`.\n- Noted gap: `packaging.json` declares a `posix-tar` source archive; this layout is\n  file-per-layer. Same bytes, per-file digests exposed; the release story decides whether the\n  tar form is also wanted.\n- **Open:** the publish/sign half — `release.yml`, `ghcr.io/beyond10x/b10x-substrate-wire`,\n  cosign, digest in `CHANGELOG.md` — waits on `story:signed-daemon-image`.\n"},"args":{"changes":{"body":"# Story: The 0.4.0 contract bundle is a signed, digest-pinned OCI artifact\n\n## Outcome\n\nA consumer pins the wire contract by OCI digest instead of copying a directory tree.\n\n## Context\n\n`docs/plan/04-direct-byte-plane.md` § *Slice C* says Agent \"consumes an exact independently\nverified copy\"; the copy is made by hand. `docs/design/07-specification-and-conformance.md`\n§ *Bundle* already fixes the packaging: `bundle.json` lists media type, byte length and digest of\nevery bundle path except itself, and the outer OCI manifest digest pins `bundle.json`. Depends on\n`story:signed-daemon-image` for the release workflow it publishes through.\n\n## Acceptance\n\n`scripts/package-contract-bundle.py <version>` produces a deterministic OCI layout from\n`contracts/substrate-wire/<version>/` without writing into `contracts/`, two runs yield\nbyte-identical manifests, and the release workflow publishes and signs it at\n`ghcr.io/beyond10x/b10x-substrate-wire:<bundle-version>` with the digest in `CHANGELOG.md`.\n\nEvidence that satisfies it:\n\n- `scripts/test_package_contract_bundle.py`: identical manifests on two runs; a one-byte change in\n  any bundle file changes the manifest digest — written failing-first against a stub that embeds\n  a timestamp;\n- `bundle.json` matches design 07's shape and a checker asserts each digest and byte length;\n- `check-contract-bundle-0.4.0.py` and every earlier checker stay green (invariant 6: the packager\n  reads the bundle; the bundle does not learn about the packager);\n- the artifact is annotated `development`; publication does not make the bundle stable (atlas ADR\n  0019 governs contract release);\n- `bash scripts/gate.sh` exits 0.\n\n## Progress — 2026-08-29 (packager half)\n\n- `scripts/package-contract-bundle.py <version> --out <dir>`: OCI Image Layout (`oci-layout`,\n  `index.json`, `blobs/sha256/…`); config **is** `bundle.json` verbatim, media type\n  `application/vnd.b10x.substrate-wire.bundle.v1+json`, so the manifest digest pins\n  `bundle.json` (design 07 § *Bundle*); one layer per bundle file, layer digest = the `sha256`\n  already in `bundle.json`; annotations `org.opencontainers.image.version`,\n  `dev.b10x.contract.status=development`, `ref.name` on the index entry. Refuses `--out` inside\n  `contracts/`, non-empty `--out` without `--force`, symlinks, a file set disagreeing with\n  `bundle.json`.\n- `scripts/test_package_contract_bundle.py`: 14 tests; determinism test written failing-first\n  against a timestamp-embedding stub (`FAILED (failures=1)`), then green. Two runs on `0.4.0` →\n  `sha256:f94d15fc116587d991aab6de5628f6ee5baf872af4e7c79d2a35e2b17a8485c4`, `diff -r` empty.\n  `oras manifest fetch --oci-layout` resolves the same digest; `oras pull` round-trip matches\n  `contracts/substrate-wire/0.4.0` byte for byte.\n- `check-contract-bundle-0.4.0.py` → exit 0; `git status contracts/` → clean (invariant 6).\n- Wired into the gate at `scripts/gate.sh:24`.\n- Noted gap: `packaging.json` declares a `posix-tar` source archive; this layout is\n  file-per-layer. Same bytes, per-file digests exposed; the release story decides whether the\n  tar form is also wanted.\n- **Open:** the publish/sign half — `release.yml`, `ghcr.io/beyond10x/b10x-substrate-wire`,\n  cosign, digest in `CHANGELOG.md` — waits on `story:signed-daemon-image`.\n"},"command":"update-entity","target":"01MEM0000000000000016"},"payload":{"actor":"human:timo","at":1788037216311,"causation":"cmd-protocol-artifact-body-story-contract-bundle-oci-artifact-1788037216311","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body","event_id":"story:contract-bundle-oci-artifact@5#0~3cfbc1c14c025337","executor":null,"recorded_at":"2026-08-29T21:00:16Z"}}
{"entity":"story","version":1,"id":"agents-md-matches-the-scripts","revision":5,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"~/.cache/claude-tmp/substrate-gate-wave1.log","source":"bash scripts/gate.sh","target":"01MEM0000000000000010"},"payload":{"actor":"human:timo","at":1788037260000,"causation":"cmd-evidence-story-agents-md-matches-the-scripts-test_result-1788037260540","change":{"change":"evidence","kind":"test_result","reference":"~/.cache/claude-tmp/substrate-gate-wave1.log","source":"bash scripts/gate.sh"},"correlation":"protocol-artifact-evidence","event_id":"story:agents-md-matches-the-scripts@5#0~60f1c4c328c3bac7","executor":null,"recorded_at":"2026-08-29T21:01:00Z"}}
{"entity":"story","version":1,"id":"pinned-rust-toolchain","revision":5,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"~/.cache/claude-tmp/substrate-gate-wave1.log","source":"bash scripts/gate.sh","target":"01MEM0000000000000034"},"payload":{"actor":"human:timo","at":1788037260000,"causation":"cmd-evidence-story-pinned-rust-toolchain-test_result-1788037260577","change":{"change":"evidence","kind":"test_result","reference":"~/.cache/claude-tmp/substrate-gate-wave1.log","source":"bash scripts/gate.sh"},"correlation":"protocol-artifact-evidence","event_id":"story:pinned-rust-toolchain@5#0~187780b1554b0265","executor":null,"recorded_at":"2026-08-29T21:01:00Z"}}
{"entity":"story","version":1,"id":"driver-port-carries-no-host-types","revision":5,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"~/.cache/claude-tmp/substrate-gate-wave1.log","source":"bash scripts/gate.sh","target":"01MEM0000000000000025"},"payload":{"actor":"human:timo","at":1788037260000,"causation":"cmd-evidence-story-driver-port-carries-no-host-types-test_result-1788037260599","change":{"change":"evidence","kind":"test_result","reference":"~/.cache/claude-tmp/substrate-gate-wave1.log","source":"bash scripts/gate.sh"},"correlation":"protocol-artifact-evidence","event_id":"story:driver-port-carries-no-host-types@5#0~c5c4c9ba444734b7","executor":null,"recorded_at":"2026-08-29T21:01:00Z"}}
{"entity":"story","version":1,"id":"ci-runs-the-full-gate","revision":5,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"~/.cache/claude-tmp/substrate-gate-wave1.log","source":"bash scripts/gate.sh","target":"01MEM0000000000000013"},"payload":{"actor":"human:timo","at":1788037260000,"causation":"cmd-evidence-story-ci-runs-the-full-gate-test_result-1788037260620","change":{"change":"evidence","kind":"test_result","reference":"~/.cache/claude-tmp/substrate-gate-wave1.log","source":"bash scripts/gate.sh"},"correlation":"protocol-artifact-evidence","event_id":"story:ci-runs-the-full-gate@5#0~58489fc323dc4e3e","executor":null,"recorded_at":"2026-08-29T21:01:00Z"}}
{"entity":"story","version":1,"id":"contract-bundle-oci-artifact","revision":5,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"~/.cache/claude-tmp/substrate-gate-wave1.log","source":"bash scripts/gate.sh","target":"01MEM0000000000000016"},"payload":{"actor":"human:timo","at":1788037260000,"causation":"cmd-evidence-story-contract-bundle-oci-artifact-test_result-1788037260650","change":{"change":"evidence","kind":"test_result","reference":"~/.cache/claude-tmp/substrate-gate-wave1.log","source":"bash scripts/gate.sh"},"correlation":"protocol-artifact-evidence","event_id":"story:contract-bundle-oci-artifact@5#0~2d06d2c30b5fe4f1","executor":null,"recorded_at":"2026-08-29T21:01:00Z"}}
{"entity":"story","version":1,"id":"agents-md-matches-the-scripts","revision":6,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1}}","target":"01MEM0000000000000010","to":"implemented"},"payload":{"actor":"human:timo","at":1788037260816,"causation":"cmd-move-story-agents-md-matches-the-scripts-1788037260816","change":{"change":"moved","decided_on":{"recorded":{"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:agents-md-matches-the-scripts@6#0~6d4e4482cba0f992","executor":null,"recorded_at":"2026-08-29T21:01:00Z"}}
{"entity":"story","version":1,"id":"pinned-rust-toolchain","revision":6,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1}}","target":"01MEM0000000000000034","to":"implemented"},"payload":{"actor":"human:timo","at":1788037261075,"causation":"cmd-move-story-pinned-rust-toolchain-1788037261075","change":{"change":"moved","decided_on":{"recorded":{"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:pinned-rust-toolchain@6#0~e8a660d13b5dbd5c","executor":null,"recorded_at":"2026-08-29T21:01:01Z"}}
{"entity":"story","version":1,"id":"driver-port-carries-no-host-types","revision":6,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1}}","target":"01MEM0000000000000025","to":"implemented"},"payload":{"actor":"human:timo","at":1788037261119,"causation":"cmd-move-story-driver-port-carries-no-host-types-1788037261119","change":{"change":"moved","decided_on":{"recorded":{"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:driver-port-carries-no-host-types@6#0~b78db637c661d32e","executor":null,"recorded_at":"2026-08-29T21:01:01Z"}}
{"entity":"story","version":1,"id":"ci-runs-the-full-gate","revision":5,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"https://github.com/beyond10x/substrate/actions/runs/33275398365","source":"GitHub Actions Gate workflow, push to main, ec82213","target":"01MEM0000000000000013"},"payload":{"actor":"human:timo","at":1788038090000,"causation":"cmd-evidence-story-ci-runs-the-full-gate-test_result-1788038090279","change":{"change":"evidence","kind":"test_result","reference":"https://github.com/beyond10x/substrate/actions/runs/33275398365","source":"GitHub Actions Gate workflow, push to main, ec82213"},"correlation":"protocol-artifact-evidence","event_id":"story:ci-runs-the-full-gate@5#0~bef1a4f30f91ce7e","executor":null,"recorded_at":"2026-08-29T21:14:50Z"}}
{"entity":"story","version":1,"id":"ci-runs-the-full-gate","revision":6,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"body":"# Story: CI runs the full gate on every push and pull request\n\n## Outcome\n\nA pull request against `main` cannot show green unless `bash scripts/gate.sh` passed on a fresh\nrunner.\n\n## Context\n\n`.github/workflows/` holds one workflow, `pages.yml`, which builds the website. The gate\n`AGENTS.md` § *The gate* calls \"the bar for `main`\" runs only on whoever remembers to run it. The\nrepository is public (atlas ADR 0003; `CHANGELOG.md` 0.2.1), so pull requests will arrive from\npeople who have not read `AGENTS.md`.\n\nThe Python checkers are stdlib-only and shell out to the built `substrate-contract-check` crate\n(`scripts/contract_json_gate.py:185`; `jsonschema = \"=0.49.9\"` in `Cargo.toml`), so the workflow\ninstalls no pip packages.\n\n## Acceptance\n\n`.github/workflows/gate.yml` runs `bash scripts/gate.sh` on `push` to `main` and on `pull_request`,\nexits with the gate's own status, and a throwaway branch with one `cargo fmt` violation produces a\nred check that the fix turns green.\n\nEvidence that satisfies it:\n\n- every action SHA-pinned with a version comment; `permissions: contents: read`; `timeout-minutes`\n  — the `pages.yml` conventions;\n- the toolchain is `1.97`, pinned explicitly until `story:pinned-rust-toolchain` makes\n  `rust-toolchain.toml` the source;\n- the job log names which lane ran; on a hosted runner without bubblewrap/cgroup delegation the\n  delegated lane is reported **absent**, never passed (invariant 3);\n- the two run URLs (red, then green) recorded in this story;\n- `bash scripts/gate.sh` exits 0 locally.\n\n## Open Questions\n\nWhether the delegated lane gets a self-hosted runner or stays a local pre-release step. Decides:\noperator. Default if nobody answers: **stays local**; CI asserts the portable lane and prints the\ndelegated lane as absent.\n\nBranch protection (require the `gate` check before merge) is a GitHub setting, not a file; it is a\none-line \"to try\" for the operator after the first green run.\n\n## Progress — 2026-08-29\n\n- `.github/workflows/gate.yml` written: `push` to `main`, `pull_request`, `workflow_dispatch`;\n  `actions/checkout@3d3c42e…` (# v7, from `pages.yml:35`) and `actions/cache@55cc8345…` (# v6,\n  `gh api repos/actions/cache/git/ref/tags/v6`); `permissions: contents: read`;\n  `timeout-minutes`; `cancel-in-progress` only for pull requests.\n- Toolchain read from `rust-toolchain.toml` with `1.97` as fallback; runner `rustup`, no\n  third-party action.\n- Lanes: a probe step (`bwrap`, `/sys/fs/cgroup/cgroup.controllers`) before the gate and a\n  `Lane summary` step to `$GITHUB_STEP_SUMMARY` after; delegated lane stated **absent** on a\n  hosted runner (decision: stays a local pre-release step). The runner-images inventory for\n  ubuntu-24.04 lists no bubblewrap.\n- `actionlint 1.7.12` + `shellcheck 0.11.0` → exit 0; YAML parses.\n- **Open:** the red-then-green run URLs need a pushed branch. To try, as the bot:\n  1. `git switch -c ci-gate-smoke && printf '\\n\\n' >> crates/substrate-wire/src/lib.rs` (fails\n     `cargo fmt --all --check`, triggers no clippy lint) → commit, push, open a PR → expect red.\n  2. `cargo fmt --all` → commit, push → expect green; record both run URLs here.\n  3. After the first green run: branch protection on `main` requiring the `Full gate` check.\n\n## Implemented — 2026-08-29\n\n- First `push` run on `main` (`ec82213`): **success** —\n  https://github.com/beyond10x/substrate/actions/runs/33275398365 (`Full gate: success`).\n- Red-then-green on pull request #1 (`ci-gate-smoke`, an isolated worktree):\n  `ef70548` two trailing blank lines in `crates/substrate-wire/src/lib.rs` → **failure** in step\n  \"Run the full gate\" — https://github.com/beyond10x/substrate/actions/runs/33275756585;\n  `3f83afc` `cargo fmt --all` → **success** —\n  https://github.com/beyond10x/substrate/actions/runs/33275839665. PR closed unmerged, branch\n  deleted.\n- Delegated lane on the hosted runner: reported **absent** (decision taken: stays a local\n  pre-release step).\n- Branch protection on `main` (2026-08-29, `gh api PUT …/branches/main/protection`): required\n  check `Full gate`, `strict=false`, `enforce_admins=true`, no review requirement, force-push and\n  deletion off. Classic protection does not restrict tags; `0.2.2` verified still resolvable.\n  Consequence: every landing on `main` needs the check green on the exact commit — push the sha\n  to a branch first, or open a PR.\n"},"args":{"changes":{"body":"# Story: CI runs the full gate on every push and pull request\n\n## Outcome\n\nA pull request against `main` cannot show green unless `bash scripts/gate.sh` passed on a fresh\nrunner.\n\n## Context\n\n`.github/workflows/` holds one workflow, `pages.yml`, which builds the website. The gate\n`AGENTS.md` § *The gate* calls \"the bar for `main`\" runs only on whoever remembers to run it. The\nrepository is public (atlas ADR 0003; `CHANGELOG.md` 0.2.1), so pull requests will arrive from\npeople who have not read `AGENTS.md`.\n\nThe Python checkers are stdlib-only and shell out to the built `substrate-contract-check` crate\n(`scripts/contract_json_gate.py:185`; `jsonschema = \"=0.49.9\"` in `Cargo.toml`), so the workflow\ninstalls no pip packages.\n\n## Acceptance\n\n`.github/workflows/gate.yml` runs `bash scripts/gate.sh` on `push` to `main` and on `pull_request`,\nexits with the gate's own status, and a throwaway branch with one `cargo fmt` violation produces a\nred check that the fix turns green.\n\nEvidence that satisfies it:\n\n- every action SHA-pinned with a version comment; `permissions: contents: read`; `timeout-minutes`\n  — the `pages.yml` conventions;\n- the toolchain is `1.97`, pinned explicitly until `story:pinned-rust-toolchain` makes\n  `rust-toolchain.toml` the source;\n- the job log names which lane ran; on a hosted runner without bubblewrap/cgroup delegation the\n  delegated lane is reported **absent**, never passed (invariant 3);\n- the two run URLs (red, then green) recorded in this story;\n- `bash scripts/gate.sh` exits 0 locally.\n\n## Open Questions\n\nWhether the delegated lane gets a self-hosted runner or stays a local pre-release step. Decides:\noperator. Default if nobody answers: **stays local**; CI asserts the portable lane and prints the\ndelegated lane as absent.\n\nBranch protection (require the `gate` check before merge) is a GitHub setting, not a file; it is a\none-line \"to try\" for the operator after the first green run.\n\n## Progress — 2026-08-29\n\n- `.github/workflows/gate.yml` written: `push` to `main`, `pull_request`, `workflow_dispatch`;\n  `actions/checkout@3d3c42e…` (# v7, from `pages.yml:35`) and `actions/cache@55cc8345…` (# v6,\n  `gh api repos/actions/cache/git/ref/tags/v6`); `permissions: contents: read`;\n  `timeout-minutes`; `cancel-in-progress` only for pull requests.\n- Toolchain read from `rust-toolchain.toml` with `1.97` as fallback; runner `rustup`, no\n  third-party action.\n- Lanes: a probe step (`bwrap`, `/sys/fs/cgroup/cgroup.controllers`) before the gate and a\n  `Lane summary` step to `$GITHUB_STEP_SUMMARY` after; delegated lane stated **absent** on a\n  hosted runner (decision: stays a local pre-release step). The runner-images inventory for\n  ubuntu-24.04 lists no bubblewrap.\n- `actionlint 1.7.12` + `shellcheck 0.11.0` → exit 0; YAML parses.\n- **Open:** the red-then-green run URLs need a pushed branch. To try, as the bot:\n  1. `git switch -c ci-gate-smoke && printf '\\n\\n' >> crates/substrate-wire/src/lib.rs` (fails\n     `cargo fmt --all --check`, triggers no clippy lint) → commit, push, open a PR → expect red.\n  2. `cargo fmt --all` → commit, push → expect green; record both run URLs here.\n  3. After the first green run: branch protection on `main` requiring the `Full gate` check.\n\n## Implemented — 2026-08-29\n\n- First `push` run on `main` (`ec82213`): **success** —\n  https://github.com/beyond10x/substrate/actions/runs/33275398365 (`Full gate: success`).\n- Red-then-green on pull request #1 (`ci-gate-smoke`, an isolated worktree):\n  `ef70548` two trailing blank lines in `crates/substrate-wire/src/lib.rs` → **failure** in step\n  \"Run the full gate\" — https://github.com/beyond10x/substrate/actions/runs/33275756585;\n  `3f83afc` `cargo fmt --all` → **success** —\n  https://github.com/beyond10x/substrate/actions/runs/33275839665. PR closed unmerged, branch\n  deleted.\n- Delegated lane on the hosted runner: reported **absent** (decision taken: stays a local\n  pre-release step).\n- Branch protection on `main` (2026-08-29, `gh api PUT …/branches/main/protection`): required\n  check `Full gate`, `strict=false`, `enforce_admins=true`, no review requirement, force-push and\n  deletion off. Classic protection does not restrict tags; `0.2.2` verified still resolvable.\n  Consequence: every landing on `main` needs the check green on the exact commit — push the sha\n  to a branch first, or open a PR.\n"},"command":"update-entity","target":"01MEM0000000000000013"},"payload":{"actor":"human:timo","at":1788038587708,"causation":"cmd-protocol-artifact-body-story-ci-runs-the-full-gate-1788038587708","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body","event_id":"story:ci-runs-the-full-gate@6#0~1bf0df1ce1288e90","executor":null,"recorded_at":"2026-08-29T21:23:07Z"}}
{"entity":"story","version":1,"id":"ci-runs-the-full-gate","revision":6,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"https://github.com/beyond10x/substrate/actions/runs/33275839665","source":"GitHub Actions Gate workflow, PR #1 red then green","target":"01MEM0000000000000013"},"payload":{"actor":"human:timo","at":1788038587000,"causation":"cmd-evidence-story-ci-runs-the-full-gate-test_result-1788038587726","change":{"change":"evidence","kind":"test_result","reference":"https://github.com/beyond10x/substrate/actions/runs/33275839665","source":"GitHub Actions Gate workflow, PR #1 red then green"},"correlation":"protocol-artifact-evidence","event_id":"story:ci-runs-the-full-gate@6#0~f66f2b5b65761534","executor":null,"recorded_at":"2026-08-29T21:23:07Z"}}
{"entity":"story","version":1,"id":"ci-runs-the-full-gate","revision":7,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":3}}","target":"01MEM0000000000000013","to":"implemented"},"payload":{"actor":"human:timo","at":1788038587746,"causation":"cmd-move-story-ci-runs-the-full-gate-1788038587746","change":{"change":"moved","decided_on":{"recorded":{"test_result":3}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:ci-runs-the-full-gate@7#0~6202d46a55752317","executor":null,"recorded_at":"2026-08-29T21:23:07Z"}}
{"entity":"story","version":1,"id":"status-md-re-observed","revision":3,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000046","to":"proposed"},"payload":{"actor":"human:timo","at":1788038992939,"causation":"cmd-move-story-status-md-re-observed-1788038992939","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:status-md-re-observed@3#0~6454e9f49733dcce","executor":null,"recorded_at":"2026-08-29T21:29:52Z"}}
{"entity":"story","version":1,"id":"status-md-re-observed","revision":4,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000046","to":"active"},"payload":{"actor":"human:timo","at":1788038993194,"causation":"cmd-move-story-status-md-re-observed-1788038993194","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:status-md-re-observed@4#0~875da470df07212a","executor":null,"recorded_at":"2026-08-29T21:29:53Z"}}
{"entity":"story","version":1,"id":"contract-bundle-oci-artifact","revision":6,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"body":"# Story: The 0.4.0 contract bundle is a signed, digest-pinned OCI artifact\n\n## Outcome\n\nA consumer pins the wire contract by OCI digest instead of copying a directory tree.\n\n## Context\n\n`docs/plan/04-direct-byte-plane.md` § *Slice C* says Agent \"consumes an exact independently\nverified copy\"; the copy is made by hand. `docs/design/07-specification-and-conformance.md`\n§ *Bundle* already fixes the packaging: `bundle.json` lists media type, byte length and digest of\nevery bundle path except itself, and the outer OCI manifest digest pins `bundle.json`. Depends on\n`story:signed-daemon-image` for the release workflow it publishes through.\n\n## Acceptance\n\n`scripts/package-contract-bundle.py <version>` produces a deterministic OCI layout from\n`contracts/substrate-wire/<version>/` without writing into `contracts/`, two runs yield\nbyte-identical manifests, and the release workflow publishes and signs it at\n`ghcr.io/beyond10x/b10x-substrate-wire:<bundle-version>` with the digest in `CHANGELOG.md`.\n\nEvidence that satisfies it:\n\n- `scripts/test_package_contract_bundle.py`: identical manifests on two runs; a one-byte change in\n  any bundle file changes the manifest digest — written failing-first against a stub that embeds\n  a timestamp;\n- `bundle.json` matches design 07's shape and a checker asserts each digest and byte length;\n- `check-contract-bundle-0.4.0.py` and every earlier checker stay green (invariant 6: the packager\n  reads the bundle; the bundle does not learn about the packager);\n- the artifact is annotated `development`; publication does not make the bundle stable (atlas ADR\n  0019 governs contract release);\n- `bash scripts/gate.sh` exits 0.\n\n## Progress — 2026-08-29 (packager half)\n\n- `scripts/package-contract-bundle.py <version> --out <dir>`: OCI Image Layout (`oci-layout`,\n  `index.json`, `blobs/sha256/…`); config **is** `bundle.json` verbatim, media type\n  `application/vnd.b10x.substrate-wire.bundle.v1+json`, so the manifest digest pins\n  `bundle.json` (design 07 § *Bundle*); one layer per bundle file, layer digest = the `sha256`\n  already in `bundle.json`; annotations `org.opencontainers.image.version`,\n  `dev.b10x.contract.status=development`, `ref.name` on the index entry. Refuses `--out` inside\n  `contracts/`, non-empty `--out` without `--force`, symlinks, a file set disagreeing with\n  `bundle.json`.\n- `scripts/test_package_contract_bundle.py`: 14 tests; determinism test written failing-first\n  against a timestamp-embedding stub (`FAILED (failures=1)`), then green. Two runs on `0.4.0` →\n  `sha256:f94d15fc116587d991aab6de5628f6ee5baf872af4e7c79d2a35e2b17a8485c4`, `diff -r` empty.\n  `oras manifest fetch --oci-layout` resolves the same digest; `oras pull` round-trip matches\n  `contracts/substrate-wire/0.4.0` byte for byte.\n- `check-contract-bundle-0.4.0.py` → exit 0; `git status contracts/` → clean (invariant 6).\n- Wired into the gate at `scripts/gate.sh:24`.\n- The `posix-tar` archive `packaging.json` declares is now the last layer (media type\n  `application/vnd.b10x.substrate-wire.bundle.tar`, title `0.4.0.tar`): ustar, directory entries\n  0755 ahead of files 0644, uid/gid 0, empty names, bytewise order, every mtime =\n  `SOURCE_DATE_EPOCH` = author seconds of the last commit touching the bundle (`1787605082`,\n  commit `4b5d411`; `--source-date-epoch` overrides, absent both → exit 2). Per-file layers and the\n  config are byte-identical to before; the manifest digest moves by design to\n  `sha256:3758e80bc39f1eb03b15c69410608c9ef1d2ba8095c7e707c6988dbb5894ab00`\n  (archive `sha256:91fb5524…`, 880,640 bytes). 21 tests; `tar -xf` of the blob reproduces\n  `contracts/substrate-wire/0.4.0` byte for byte. On a shallow CI checkout the epoch would be the\n  tip's author time — pin `--source-date-epoch` there.\n- **Open:** the publish/sign half — `release.yml`, `ghcr.io/beyond10x/b10x-substrate-wire`,\n  cosign, digest in `CHANGELOG.md` — waits on `story:signed-daemon-image`.\n"},"args":{"changes":{"body":"# Story: The 0.4.0 contract bundle is a signed, digest-pinned OCI artifact\n\n## Outcome\n\nA consumer pins the wire contract by OCI digest instead of copying a directory tree.\n\n## Context\n\n`docs/plan/04-direct-byte-plane.md` § *Slice C* says Agent \"consumes an exact independently\nverified copy\"; the copy is made by hand. `docs/design/07-specification-and-conformance.md`\n§ *Bundle* already fixes the packaging: `bundle.json` lists media type, byte length and digest of\nevery bundle path except itself, and the outer OCI manifest digest pins `bundle.json`. Depends on\n`story:signed-daemon-image` for the release workflow it publishes through.\n\n## Acceptance\n\n`scripts/package-contract-bundle.py <version>` produces a deterministic OCI layout from\n`contracts/substrate-wire/<version>/` without writing into `contracts/`, two runs yield\nbyte-identical manifests, and the release workflow publishes and signs it at\n`ghcr.io/beyond10x/b10x-substrate-wire:<bundle-version>` with the digest in `CHANGELOG.md`.\n\nEvidence that satisfies it:\n\n- `scripts/test_package_contract_bundle.py`: identical manifests on two runs; a one-byte change in\n  any bundle file changes the manifest digest — written failing-first against a stub that embeds\n  a timestamp;\n- `bundle.json` matches design 07's shape and a checker asserts each digest and byte length;\n- `check-contract-bundle-0.4.0.py` and every earlier checker stay green (invariant 6: the packager\n  reads the bundle; the bundle does not learn about the packager);\n- the artifact is annotated `development`; publication does not make the bundle stable (atlas ADR\n  0019 governs contract release);\n- `bash scripts/gate.sh` exits 0.\n\n## Progress — 2026-08-29 (packager half)\n\n- `scripts/package-contract-bundle.py <version> --out <dir>`: OCI Image Layout (`oci-layout`,\n  `index.json`, `blobs/sha256/…`); config **is** `bundle.json` verbatim, media type\n  `application/vnd.b10x.substrate-wire.bundle.v1+json`, so the manifest digest pins\n  `bundle.json` (design 07 § *Bundle*); one layer per bundle file, layer digest = the `sha256`\n  already in `bundle.json`; annotations `org.opencontainers.image.version`,\n  `dev.b10x.contract.status=development`, `ref.name` on the index entry. Refuses `--out` inside\n  `contracts/`, non-empty `--out` without `--force`, symlinks, a file set disagreeing with\n  `bundle.json`.\n- `scripts/test_package_contract_bundle.py`: 14 tests; determinism test written failing-first\n  against a timestamp-embedding stub (`FAILED (failures=1)`), then green. Two runs on `0.4.0` →\n  `sha256:f94d15fc116587d991aab6de5628f6ee5baf872af4e7c79d2a35e2b17a8485c4`, `diff -r` empty.\n  `oras manifest fetch --oci-layout` resolves the same digest; `oras pull` round-trip matches\n  `contracts/substrate-wire/0.4.0` byte for byte.\n- `check-contract-bundle-0.4.0.py` → exit 0; `git status contracts/` → clean (invariant 6).\n- Wired into the gate at `scripts/gate.sh:24`.\n- The `posix-tar` archive `packaging.json` declares is now the last layer (media type\n  `application/vnd.b10x.substrate-wire.bundle.tar`, title `0.4.0.tar`): ustar, directory entries\n  0755 ahead of files 0644, uid/gid 0, empty names, bytewise order, every mtime =\n  `SOURCE_DATE_EPOCH` = author seconds of the last commit touching the bundle (`1787605082`,\n  commit `4b5d411`; `--source-date-epoch` overrides, absent both → exit 2). Per-file layers and the\n  config are byte-identical to before; the manifest digest moves by design to\n  `sha256:3758e80bc39f1eb03b15c69410608c9ef1d2ba8095c7e707c6988dbb5894ab00`\n  (archive `sha256:91fb5524…`, 880,640 bytes). 21 tests; `tar -xf` of the blob reproduces\n  `contracts/substrate-wire/0.4.0` byte for byte. On a shallow CI checkout the epoch would be the\n  tip's author time — pin `--source-date-epoch` there.\n- **Open:** the publish/sign half — `release.yml`, `ghcr.io/beyond10x/b10x-substrate-wire`,\n  cosign, digest in `CHANGELOG.md` — waits on `story:signed-daemon-image`.\n"},"command":"update-entity","target":"01MEM0000000000000016"},"payload":{"actor":"human:timo","at":1788039686542,"causation":"cmd-protocol-artifact-body-story-contract-bundle-oci-artifact-1788039686542","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body","event_id":"story:contract-bundle-oci-artifact@6#0~c51d430ce2be815b","executor":null,"recorded_at":"2026-08-29T21:41:26Z"}}
{"entity":"story","version":1,"id":"tooling-moves-to-cargo-xtask","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"<!-- Starting point for a `story` artifact, seeded by `protocol artifact new story <name>`.\n     No frontmatter here on purpose: the `---` block is written by the CLI from the id, kind, status\n     and relations you gave it, and a second copy in this file would be the one that went stale.\n     Delete the italic guidance as you fill each section. -->\n\n# Story: <name>\n\n## Outcome\n\n*What is true for whom once this has shipped, in one sentence. If it names a component rather than a\nperson, it is a task — say what changes for someone.*\n\n## Context\n\n*Why this is worth doing now, and what it depends on. Link the epic or specification it comes from\nrather than restating it; the `derived_from` relation already carries the edge.*\n\n## Acceptance\n\n*The conditions under which this is done, each one something a person or a check can observe. \"Works\ncorrectly\" is not one of them.*\n\n## Out of Scope\n\n*What a reasonable reader would expect to be included and is not — the boundary that stops this\nstory quietly becoming an epic.*\n\n## Open Questions\n\n*What is still undecided, each with who decides it. A story carrying an unowned question is a story\nthat stalls without anybody noticing.*\n","owner":"substrate","summary":"atlas AGENTS.md section Language: gates, checkers, renderers and packagers are Rust. 23,916 lines of Python beside 30,768 of Rust; the four frozen bundle renderers stay as reproducibility proofs, everything else moves, and 0.5.0 is rendered from substrate-wire types.","tags":["build","rust"],"title":"Anything that runs is Rust: the gate's Python moves to cargo xtask"},"args":{"command":"create-entity","data":{"body":"<!-- Starting point for a `story` artifact, seeded by `protocol artifact new story <name>`.\n     No frontmatter here on purpose: the `---` block is written by the CLI from the id, kind, status\n     and relations you gave it, and a second copy in this file would be the one that went stale.\n     Delete the italic guidance as you fill each section. -->\n\n# Story: <name>\n\n## Outcome\n\n*What is true for whom once this has shipped, in one sentence. If it names a component rather than a\nperson, it is a task — say what changes for someone.*\n\n## Context\n\n*Why this is worth doing now, and what it depends on. Link the epic or specification it comes from\nrather than restating it; the `derived_from` relation already carries the edge.*\n\n## Acceptance\n\n*The conditions under which this is done, each one something a person or a check can observe. \"Works\ncorrectly\" is not one of them.*\n\n## Out of Scope\n\n*What a reasonable reader would expect to be included and is not — the boundary that stops this\nstory quietly becoming an epic.*\n\n## Open Questions\n\n*What is still undecided, each with who decides it. A story carrying an unowned question is a story\nthat stalls without anybody noticing.*\n","owner":"substrate","status":"draft","summary":"atlas AGENTS.md section Language: gates, checkers, renderers and packagers are Rust. 23,916 lines of Python beside 30,768 of Rust; the four frozen bundle renderers stay as reproducibility proofs, everything else moves, and 0.5.0 is rendered from substrate-wire types.","tags":["build","rust"],"title":"Anything that runs is Rust: the gate's Python moves to cargo xtask"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/tooling-moves-to-cargo-xtask"},"payload":{"actor":"human:timo","at":1788039760595,"causation":"cmd-new-story-tooling-moves-to-cargo-xtask","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:tooling-moves-to-cargo-xtask@1#0~6687056456b80925","executor":null,"recorded_at":"2026-08-29T21:42:40Z"}}
{"entity":"story","version":1,"id":"tooling-moves-to-cargo-xtask","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:release-hardening"}]},"args":{"command":"create-relation","kind":"decomposes","source":"01MEM0000000000000091","target":"01MEM0000000000000007"},"payload":{"actor":"human:timo","at":1788039760605,"causation":"cmd-story-tooling-moves-to-cargo-xtask-decomposes-epic-release-hardening","change":{"change":"related","relation":"decomposes","target":"epic:release-hardening"},"correlation":"protocol-artifact-relate","event_id":"story:tooling-moves-to-cargo-xtask@1#0~5c8ee6bed96d6b22","executor":null,"recorded_at":"2026-08-29T21:42:40Z"}}
{"entity":"story","version":1,"id":"tooling-moves-to-cargo-xtask","revision":2,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"# Story: Anything that runs is Rust — the gate's Python moves to `cargo xtask`\n\n## Outcome\n\n`scripts/gate.sh` runs Rust: one `xtask` crate (clap) owns `check-links`, `check-adrs`,\n`check-toolchain`, `package-bundle`, `check-bundle` and the runtime-vector runner, and the next\nwire bundle (`0.5.0`) is rendered from `substrate-wire`'s types rather than from Python dicts. The\nfour frozen renderers and their checkers stay exactly as they are, as the reproducibility proof of\n`0.1.0`–`0.4.0` (invariant 6), and nothing new is written in Python.\n\n## Context\n\n`atlas/AGENTS.md` § *Language*, 2026-08-29: anything that runs is Rust; a new `.py` in a foundation\nrepository is an operator decision. Measured the same day: `scripts/*.py` is 23,916 lines beside\n30,768 lines of Rust; 18,300 of those are `render-contract-bundle{,-0.2.0,-0.3.0,-0.4.0}.py` and\n`check-contract-bundle{,-0.2.0,-0.3.0,-0.4.0}.py`, near-copies per version. The validator that does\nthe standards work is already Rust (`crates/substrate-contract-check`, `jsonschema = \"=0.49.9\"`;\n`scripts/contract_json_gate.py:195` shells out to it). The wire has two descriptions — the Rust\ntypes in `crates/substrate-wire` and the renderer's dicts — and only the vectors would notice them\ndrifting. Wave 1 of this store added ~1,200 more Python lines (`check-toolchain.py`,\n`package-contract-bundle.py` and its tests) by following the local convention; they are the first\nto move.\n\n## Acceptance\n\n`bash scripts/gate.sh` invokes no Python except the four frozen renderers' checkers, every moved\ncheck refuses the same inputs its Python predecessor refused (proven by the predecessor's own\nnegative cases run against the Rust replacement before the Python is deleted), `0.4.0` still\nverifies byte-clean, and `cargo xtask render-bundle 0.5.0` is the only renderer for the successor.\n\nEvidence that satisfies it, in order:\n\n1. `xtask/` crate in the workspace (`cargo xtask <verb>`, clap derive), `--locked` clean.\n2. `check-toolchain`, `check-links`, `check-adrs` moved first (smallest, no wire contact), each with\n   the Python negative cases ported as tests; the Python files deleted in the same commit.\n3. `package-bundle` moved with `test_package_contract_bundle.py`'s 21 cases ported; the 0.4.0\n   manifest digest `sha256:3758e80b…` reproduced by the Rust packager before the Python goes.\n4. `check-runtime-vectors` moved (the black-box Unix-socket HTTP runner, 1,194 lines) — last of the\n   gate steps, because it is the one with the most behaviour.\n5. `render-bundle 0.5.0` derives schemas from `substrate-wire` types (`schemars` or hand-rolled\n   emitters — decided in the story), with the `0.4.0` compatibility block computed, not typed.\n6. `AGENTS.md` § *The gate* and `README.md` § *Build, test, run* list the xtask verbs; the four\n   frozen renderers are documented as frozen proofs, not tooling.\n\n## Out of Scope\n\nRe-rendering `0.1.0`–`0.4.0` from Rust. They are frozen; their Python renderers are their proof.\n\n## Open Questions\n\nWhether `check-runtime-vectors` becomes an `xtask` verb or an integration test under\n`crates/substrate-daemon/tests/` (it drives a real daemon over a socket, which the daemon's tests\nalready do). Decides: operator. Default if nobody answers: **integration test** — one fewer\nbinary, and the gate already runs `cargo test`.\n"},"args":{"changes":{"body":"# Story: Anything that runs is Rust — the gate's Python moves to `cargo xtask`\n\n## Outcome\n\n`scripts/gate.sh` runs Rust: one `xtask` crate (clap) owns `check-links`, `check-adrs`,\n`check-toolchain`, `package-bundle`, `check-bundle` and the runtime-vector runner, and the next\nwire bundle (`0.5.0`) is rendered from `substrate-wire`'s types rather than from Python dicts. The\nfour frozen renderers and their checkers stay exactly as they are, as the reproducibility proof of\n`0.1.0`–`0.4.0` (invariant 6), and nothing new is written in Python.\n\n## Context\n\n`atlas/AGENTS.md` § *Language*, 2026-08-29: anything that runs is Rust; a new `.py` in a foundation\nrepository is an operator decision. Measured the same day: `scripts/*.py` is 23,916 lines beside\n30,768 lines of Rust; 18,300 of those are `render-contract-bundle{,-0.2.0,-0.3.0,-0.4.0}.py` and\n`check-contract-bundle{,-0.2.0,-0.3.0,-0.4.0}.py`, near-copies per version. The validator that does\nthe standards work is already Rust (`crates/substrate-contract-check`, `jsonschema = \"=0.49.9\"`;\n`scripts/contract_json_gate.py:195` shells out to it). The wire has two descriptions — the Rust\ntypes in `crates/substrate-wire` and the renderer's dicts — and only the vectors would notice them\ndrifting. Wave 1 of this store added ~1,200 more Python lines (`check-toolchain.py`,\n`package-contract-bundle.py` and its tests) by following the local convention; they are the first\nto move.\n\n## Acceptance\n\n`bash scripts/gate.sh` invokes no Python except the four frozen renderers' checkers, every moved\ncheck refuses the same inputs its Python predecessor refused (proven by the predecessor's own\nnegative cases run against the Rust replacement before the Python is deleted), `0.4.0` still\nverifies byte-clean, and `cargo xtask render-bundle 0.5.0` is the only renderer for the successor.\n\nEvidence that satisfies it, in order:\n\n1. `xtask/` crate in the workspace (`cargo xtask <verb>`, clap derive), `--locked` clean.\n2. `check-toolchain`, `check-links`, `check-adrs` moved first (smallest, no wire contact), each with\n   the Python negative cases ported as tests; the Python files deleted in the same commit.\n3. `package-bundle` moved with `test_package_contract_bundle.py`'s 21 cases ported; the 0.4.0\n   manifest digest `sha256:3758e80b…` reproduced by the Rust packager before the Python goes.\n4. `check-runtime-vectors` moved (the black-box Unix-socket HTTP runner, 1,194 lines) — last of the\n   gate steps, because it is the one with the most behaviour.\n5. `render-bundle 0.5.0` derives schemas from `substrate-wire` types (`schemars` or hand-rolled\n   emitters — decided in the story), with the `0.4.0` compatibility block computed, not typed.\n6. `AGENTS.md` § *The gate* and `README.md` § *Build, test, run* list the xtask verbs; the four\n   frozen renderers are documented as frozen proofs, not tooling.\n\n## Out of Scope\n\nRe-rendering `0.1.0`–`0.4.0` from Rust. They are frozen; their Python renderers are their proof.\n\n## Open Questions\n\nWhether `check-runtime-vectors` becomes an `xtask` verb or an integration test under\n`crates/substrate-daemon/tests/` (it drives a real daemon over a socket, which the daemon's tests\nalready do). Decides: operator. Default if nobody answers: **integration test** — one fewer\nbinary, and the gate already runs `cargo test`.\n"},"command":"update-entity","target":"01MEM0000000000000049"},"payload":{"actor":"human:timo","at":1788039760633,"causation":"cmd-protocol-artifact-body-story-tooling-moves-to-cargo-xtask-1788039760633","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body","event_id":"story:tooling-moves-to-cargo-xtask@2#0~6eb8f45e1ee5b1f6","executor":null,"recorded_at":"2026-08-29T21:42:40Z"}}
{"entity":"story","version":1,"id":"status-md-re-observed","revision":4,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"~/.cache/claude-tmp/substrate-gate-wave2.log","source":"bash scripts/gate.sh","target":"01MEM0000000000000046"},"payload":{"actor":"human:timo","at":1788039922000,"causation":"cmd-evidence-story-status-md-re-observed-test_result-1788039922492","change":{"change":"evidence","kind":"test_result","reference":"~/.cache/claude-tmp/substrate-gate-wave2.log","source":"bash scripts/gate.sh"},"correlation":"protocol-artifact-evidence","event_id":"story:status-md-re-observed@4#0~56a37b4c100a4ebb","executor":null,"recorded_at":"2026-08-29T21:45:22Z"}}
{"entity":"story","version":1,"id":"contract-bundle-oci-artifact","revision":6,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"~/.cache/claude-tmp/substrate-gate-wave2.log","source":"bash scripts/gate.sh (21 packager tests, tar layer)","target":"01MEM0000000000000016"},"payload":{"actor":"human:timo","at":1788039923000,"causation":"cmd-evidence-story-contract-bundle-oci-artifact-test_result-1788039923031","change":{"change":"evidence","kind":"test_result","reference":"~/.cache/claude-tmp/substrate-gate-wave2.log","source":"bash scripts/gate.sh (21 packager tests, tar layer)"},"correlation":"protocol-artifact-evidence","event_id":"story:contract-bundle-oci-artifact@6#0~ee503c45f2ac9243","executor":null,"recorded_at":"2026-08-29T21:45:23Z"}}
{"entity":"story","version":1,"id":"status-md-re-observed","revision":5,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1}}","target":"01MEM0000000000000046","to":"implemented"},"payload":{"actor":"human:timo","at":1788039923069,"causation":"cmd-move-story-status-md-re-observed-1788039923069","change":{"change":"moved","decided_on":{"recorded":{"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:status-md-re-observed@5#0~e231abc404327f67","executor":null,"recorded_at":"2026-08-29T21:45:23Z"}}
{"entity":"story","version":1,"id":"tooling-moves-to-cargo-xtask","revision":3,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000049","to":"proposed"},"payload":{"actor":"human:timo","at":1788040060471,"causation":"cmd-move-story-tooling-moves-to-cargo-xtask-1788040060471","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:tooling-moves-to-cargo-xtask@3#0~ad137e1b66055433","executor":null,"recorded_at":"2026-08-29T21:47:40Z"}}
{"entity":"story","version":1,"id":"tooling-moves-to-cargo-xtask","revision":4,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000049","to":"active"},"payload":{"actor":"human:timo","at":1788040060566,"causation":"cmd-move-story-tooling-moves-to-cargo-xtask-1788040060566","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:tooling-moves-to-cargo-xtask@4#0~c64d49778958ac1b","executor":null,"recorded_at":"2026-08-29T21:47:40Z"}}
{"entity":"story","version":1,"id":"tooling-moves-to-cargo-xtask","revision":5,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"body":"# Story: Anything that runs is Rust — the gate's Python moves to `cargo xtask`\n\n## Outcome\n\n`scripts/gate.sh` runs Rust: one `xtask` crate (clap) owns `check-links`, `check-adrs`,\n`check-toolchain`, `package-bundle`, `check-bundle` and the runtime-vector runner, and the next\nwire bundle (`0.5.0`) is rendered from `substrate-wire`'s types rather than from Python dicts. The\nfour frozen renderers and their checkers stay exactly as they are, as the reproducibility proof of\n`0.1.0`–`0.4.0` (invariant 6), and nothing new is written in Python.\n\n## Context\n\n`atlas/AGENTS.md` § *Language*, 2026-08-29: anything that runs is Rust; a new `.py` in a foundation\nrepository is an operator decision. Measured the same day: `scripts/*.py` is 23,916 lines beside\n30,768 lines of Rust; 18,300 of those are `render-contract-bundle{,-0.2.0,-0.3.0,-0.4.0}.py` and\n`check-contract-bundle{,-0.2.0,-0.3.0,-0.4.0}.py`, near-copies per version. The validator that does\nthe standards work is already Rust (`crates/substrate-contract-check`, `jsonschema = \"=0.49.9\"`;\n`scripts/contract_json_gate.py:195` shells out to it). The wire has two descriptions — the Rust\ntypes in `crates/substrate-wire` and the renderer's dicts — and only the vectors would notice them\ndrifting. Wave 1 of this store added ~1,200 more Python lines (`check-toolchain.py`,\n`package-contract-bundle.py` and its tests) by following the local convention; they are the first\nto move.\n\n## Acceptance\n\n`bash scripts/gate.sh` invokes no Python except the four frozen renderers' checkers, every moved\ncheck refuses the same inputs its Python predecessor refused (proven by the predecessor's own\nnegative cases run against the Rust replacement before the Python is deleted), `0.4.0` still\nverifies byte-clean, and `cargo xtask render-bundle 0.5.0` is the only renderer for the successor.\n\nEvidence that satisfies it, in order:\n\n1. `xtask/` crate in the workspace (`cargo xtask <verb>`, clap derive), `--locked` clean.\n2. `check-toolchain`, `check-links`, `check-adrs` moved first (smallest, no wire contact), each with\n   the Python negative cases ported as tests; the Python files deleted in the same commit.\n3. `package-bundle` moved with `test_package_contract_bundle.py`'s 21 cases ported; the 0.4.0\n   manifest digest `sha256:3758e80b…` reproduced by the Rust packager before the Python goes.\n4. `check-runtime-vectors` moved (the black-box Unix-socket HTTP runner, 1,194 lines) — last of the\n   gate steps, because it is the one with the most behaviour.\n5. `render-bundle 0.5.0` derives schemas from `substrate-wire` types (`schemars` or hand-rolled\n   emitters — decided in the story), with the `0.4.0` compatibility block computed, not typed.\n6. `AGENTS.md` § *The gate* and `README.md` § *Build, test, run* list the xtask verbs; the four\n   frozen renderers are documented as frozen proofs, not tooling.\n\n## Out of Scope\n\nRe-rendering `0.1.0`–`0.4.0` from Rust. They are frozen; their Python renderers are their proof.\n\n## Open Questions\n\nWhether `check-runtime-vectors` becomes an `xtask` verb or an integration test under\n`crates/substrate-daemon/tests/` (it drives a real daemon over a socket, which the daemon's tests\nalready do). Decides: operator. Default if nobody answers: **integration test** — one fewer\nbinary, and the gate already runs `cargo test`.\n\n## Progress — 2026-08-30, steps 1–2\n\n- `xtask/` (bin-only, clap derive; `anyhow`, `clap`, dev `tempfile` — no new crate, `Cargo.lock`\n  +9 lines, zero version bumps); `.cargo/config.toml` alias `cargo xtask`.\n- `check-toolchain --root`, `check-links`, `check-adrs` ported; the three Python files deleted in\n  the same change. Differential runs against the Python before deletion: check-links 19 cases +\n  whole repo byte-identical stdout/stderr; check-adrs 11 mutations of `adr/` 11/11 identical;\n  check-toolchain 12 mutations 12/12 identical. Two deliberate differences: the predecessor\n  monorepo escape hatch in check-links is gone (no ancestor has `scripts/check-monorepo.sh`), and\n  `[x](   )` is skipped where the Python raised `IndexError` (`check-links.py:33`).\n- Repo root at runtime is the nearest ancestor whose `Cargo.toml` has `[workspace]`\n  (`xtask/src/repo.rs`), not the binary's compile-time path.\n- Tests written failing-first (`2 passed; 26 failed` on `unimplemented!()` stubs) →\n  `cargo test -p xtask --locked`: 28 passed. `bash scripts/gate.sh`: passed, 13 steps.\n- `gate.sh:18,19,27`, `AGENTS.md` § The gate, `README.md` § Build, `STATUS.md` (three links to the\n  deleted files) updated. Remaining: steps 3–6 (packager, runtime vectors, `render-bundle 0.5.0`).\n"},"args":{"changes":{"body":"# Story: Anything that runs is Rust — the gate's Python moves to `cargo xtask`\n\n## Outcome\n\n`scripts/gate.sh` runs Rust: one `xtask` crate (clap) owns `check-links`, `check-adrs`,\n`check-toolchain`, `package-bundle`, `check-bundle` and the runtime-vector runner, and the next\nwire bundle (`0.5.0`) is rendered from `substrate-wire`'s types rather than from Python dicts. The\nfour frozen renderers and their checkers stay exactly as they are, as the reproducibility proof of\n`0.1.0`–`0.4.0` (invariant 6), and nothing new is written in Python.\n\n## Context\n\n`atlas/AGENTS.md` § *Language*, 2026-08-29: anything that runs is Rust; a new `.py` in a foundation\nrepository is an operator decision. Measured the same day: `scripts/*.py` is 23,916 lines beside\n30,768 lines of Rust; 18,300 of those are `render-contract-bundle{,-0.2.0,-0.3.0,-0.4.0}.py` and\n`check-contract-bundle{,-0.2.0,-0.3.0,-0.4.0}.py`, near-copies per version. The validator that does\nthe standards work is already Rust (`crates/substrate-contract-check`, `jsonschema = \"=0.49.9\"`;\n`scripts/contract_json_gate.py:195` shells out to it). The wire has two descriptions — the Rust\ntypes in `crates/substrate-wire` and the renderer's dicts — and only the vectors would notice them\ndrifting. Wave 1 of this store added ~1,200 more Python lines (`check-toolchain.py`,\n`package-contract-bundle.py` and its tests) by following the local convention; they are the first\nto move.\n\n## Acceptance\n\n`bash scripts/gate.sh` invokes no Python except the four frozen renderers' checkers, every moved\ncheck refuses the same inputs its Python predecessor refused (proven by the predecessor's own\nnegative cases run against the Rust replacement before the Python is deleted), `0.4.0` still\nverifies byte-clean, and `cargo xtask render-bundle 0.5.0` is the only renderer for the successor.\n\nEvidence that satisfies it, in order:\n\n1. `xtask/` crate in the workspace (`cargo xtask <verb>`, clap derive), `--locked` clean.\n2. `check-toolchain`, `check-links`, `check-adrs` moved first (smallest, no wire contact), each with\n   the Python negative cases ported as tests; the Python files deleted in the same commit.\n3. `package-bundle` moved with `test_package_contract_bundle.py`'s 21 cases ported; the 0.4.0\n   manifest digest `sha256:3758e80b…` reproduced by the Rust packager before the Python goes.\n4. `check-runtime-vectors` moved (the black-box Unix-socket HTTP runner, 1,194 lines) — last of the\n   gate steps, because it is the one with the most behaviour.\n5. `render-bundle 0.5.0` derives schemas from `substrate-wire` types (`schemars` or hand-rolled\n   emitters — decided in the story), with the `0.4.0` compatibility block computed, not typed.\n6. `AGENTS.md` § *The gate* and `README.md` § *Build, test, run* list the xtask verbs; the four\n   frozen renderers are documented as frozen proofs, not tooling.\n\n## Out of Scope\n\nRe-rendering `0.1.0`–`0.4.0` from Rust. They are frozen; their Python renderers are their proof.\n\n## Open Questions\n\nWhether `check-runtime-vectors` becomes an `xtask` verb or an integration test under\n`crates/substrate-daemon/tests/` (it drives a real daemon over a socket, which the daemon's tests\nalready do). Decides: operator. Default if nobody answers: **integration test** — one fewer\nbinary, and the gate already runs `cargo test`.\n\n## Progress — 2026-08-30, steps 1–2\n\n- `xtask/` (bin-only, clap derive; `anyhow`, `clap`, dev `tempfile` — no new crate, `Cargo.lock`\n  +9 lines, zero version bumps); `.cargo/config.toml` alias `cargo xtask`.\n- `check-toolchain --root`, `check-links`, `check-adrs` ported; the three Python files deleted in\n  the same change. Differential runs against the Python before deletion: check-links 19 cases +\n  whole repo byte-identical stdout/stderr; check-adrs 11 mutations of `adr/` 11/11 identical;\n  check-toolchain 12 mutations 12/12 identical. Two deliberate differences: the predecessor\n  monorepo escape hatch in check-links is gone (no ancestor has `scripts/check-monorepo.sh`), and\n  `[x](   )` is skipped where the Python raised `IndexError` (`check-links.py:33`).\n- Repo root at runtime is the nearest ancestor whose `Cargo.toml` has `[workspace]`\n  (`xtask/src/repo.rs`), not the binary's compile-time path.\n- Tests written failing-first (`2 passed; 26 failed` on `unimplemented!()` stubs) →\n  `cargo test -p xtask --locked`: 28 passed. `bash scripts/gate.sh`: passed, 13 steps.\n- `gate.sh:18,19,27`, `AGENTS.md` § The gate, `README.md` § Build, `STATUS.md` (three links to the\n  deleted files) updated. Remaining: steps 3–6 (packager, runtime vectors, `render-bundle 0.5.0`).\n"},"command":"update-entity","target":"01MEM0000000000000049"},"payload":{"actor":"human:timo","at":1788041175775,"causation":"cmd-protocol-artifact-body-story-tooling-moves-to-cargo-xtask-1788041175775","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body","event_id":"story:tooling-moves-to-cargo-xtask@5#0~5002fb39527bb91c","executor":null,"recorded_at":"2026-08-29T22:06:15Z"}}
{"entity":"story","version":1,"id":"tooling-moves-to-cargo-xtask","revision":5,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"https://github.com/beyond10x/substrate/actions/runs/33277744392","source":"GitHub Actions Gate workflow, PR #4 (steps 1-2)","target":"01MEM0000000000000049"},"payload":{"actor":"human:timo","at":1788041331000,"causation":"cmd-evidence-story-tooling-moves-to-cargo-xtask-test_result-1788041331940","change":{"change":"evidence","kind":"test_result","reference":"https://github.com/beyond10x/substrate/actions/runs/33277744392","source":"GitHub Actions Gate workflow, PR #4 (steps 1-2)"},"correlation":"protocol-artifact-evidence","event_id":"story:tooling-moves-to-cargo-xtask@5#0~5a2103f30938f47d","executor":null,"recorded_at":"2026-08-29T22:08:51Z"}}
{"entity":"story","version":1,"id":"ledger-rows-carry-the-declared-grant","revision":3,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"# Story: An operation's ledger row carries the declared grant it ran under\n\n## Outcome\n\nFrom substrate's own durable record, a reader can answer atlas O1's exit question for every\neffectful operation: *which declared grant authorised this, on behalf of which platform principal,\nthrough which immediate actor* — and an operation presented without a grant reference where the\ndeployment requires one is a named refusal, not a missing column.\n\n## Context\n\n`atlas/ROADMAP.md` O1 — *governed reach* — names substrate an owner and states the exit evidence:\n\"every effectful call in a run's record is attributable to a declared grant; a call outside it is\na named refusal, not a missing row\". Its state column says \"connectors grants and identity\naudiences: not yet verified across a seam\". Substrate's side of that seam is already designed:\n`docs/design/06-authentication-secrets-and-trust.md` § 2 — the initiating platform principal is\nretained separately from the immediate service actor \"through tamper-safe delegated context;\ncaller-written identity strings are not trusted\", and every ledger key is scoped by deployment\nand authenticated subject. Nothing in the backlog implemented that context on the wire or in the\nledger; this is the one item that moves the objective substrate serves (`AGENTS.md` § *Serves*).\n\n## Acceptance\n\nAn exec or session operation started with a delegated-context document records the grant\nreference and platform principal in its ledger row and in its `operation.*` events, the same\nbytes a connectors-side fixture presents, and a test proves a caller-written identity string in\nthe request body is ignored in favour of the verified context.\n\nEvidence that satisfies it, in order:\n\n1. **Before code** (invariant 8): an ADR fixing the delegated-context shape — who signs or seals\n   it, what substrate verifies (audience, expiry, binding to the authenticated subject), and the\n   refusal class when it is required and absent or invalid. Coordinated with `connectors` and\n   `identity` under `atlas/AGENTS.md` § *Cross-repo changes*; the ADR names both relying parties.\n2. A successor bundle adds the optional `delegated_context` request field, the ledger/event\n   fields `grant_ref` and `platform_principal`, and the refusal; earlier bundle bytes unchanged.\n3. Failing-first tests: `ledger_row_records_grant_ref_and_platform_principal`,\n   `caller_written_identity_is_ignored_when_context_is_verified`,\n   `missing_delegated_context_is_refused_by_name_when_required`,\n   `delegated_context_bound_to_another_subject_is_refused`.\n4. A conformance vector pair: substrate's fixture and a connectors-owned copy, byte-identical, so\n   the atlas O1 row can cite both sides.\n\n## Out of Scope\n\nGrant *evaluation* — connectors decides; substrate records and verifies binding only (design 06\n§ 2: \"a higher-layer permit cannot override\" substrate's local checks, and substrate never\nbecomes the authorization engine). Hosted identity material (phase 7).\n\n## Open Questions\n\nWhether the delegated context is a signed JWT-shaped token from identity or a sealed blob from\nconnectors. Decides: the ADR, with both owners. Default if nobody answers: **identity-signed,\naudience `substrate`**, because atlas O1 already pairs \"connectors grants and identity audiences\".\n\n## Design draft — 2026-08-30\n\n`docs/design/09-delegated-context-and-grant-attribution.md` (proposed) carries the shape and the\nADR text ready to accept. Findings that move this story's default: identity issues **opaque**\ntokens and holds no signing key, `kid` or JWKS (`identity/src/lib.rs:1936-1939`, `:2172`), and\n`urn:b10x:substrate` is documented but not issuable (`identity/README.md:144`,\n`identity/src/lib.rs:1920-1922`); connectors **already** issues Ed25519 compact JWS carrying\n`dl_grant`, `sub`, `act` (`connectors/crates/service/src/authority.rs:52-71`) and names the field\n`grant_ref` (`connectors/crates/integration-catalog/src/lib.rs:99-106`). The draft keeps the\nstory's default (identity-signed) with that cost stated; the cheaper path is a connectors-signed\ncontext. Decides: the ADR, with both owners.\n"},"args":{"changes":{"body":"# Story: An operation's ledger row carries the declared grant it ran under\n\n## Outcome\n\nFrom substrate's own durable record, a reader can answer atlas O1's exit question for every\neffectful operation: *which declared grant authorised this, on behalf of which platform principal,\nthrough which immediate actor* — and an operation presented without a grant reference where the\ndeployment requires one is a named refusal, not a missing column.\n\n## Context\n\n`atlas/ROADMAP.md` O1 — *governed reach* — names substrate an owner and states the exit evidence:\n\"every effectful call in a run's record is attributable to a declared grant; a call outside it is\na named refusal, not a missing row\". Its state column says \"connectors grants and identity\naudiences: not yet verified across a seam\". Substrate's side of that seam is already designed:\n`docs/design/06-authentication-secrets-and-trust.md` § 2 — the initiating platform principal is\nretained separately from the immediate service actor \"through tamper-safe delegated context;\ncaller-written identity strings are not trusted\", and every ledger key is scoped by deployment\nand authenticated subject. Nothing in the backlog implemented that context on the wire or in the\nledger; this is the one item that moves the objective substrate serves (`AGENTS.md` § *Serves*).\n\n## Acceptance\n\nAn exec or session operation started with a delegated-context document records the grant\nreference and platform principal in its ledger row and in its `operation.*` events, the same\nbytes a connectors-side fixture presents, and a test proves a caller-written identity string in\nthe request body is ignored in favour of the verified context.\n\nEvidence that satisfies it, in order:\n\n1. **Before code** (invariant 8): an ADR fixing the delegated-context shape — who signs or seals\n   it, what substrate verifies (audience, expiry, binding to the authenticated subject), and the\n   refusal class when it is required and absent or invalid. Coordinated with `connectors` and\n   `identity` under `atlas/AGENTS.md` § *Cross-repo changes*; the ADR names both relying parties.\n2. A successor bundle adds the optional `delegated_context` request field, the ledger/event\n   fields `grant_ref` and `platform_principal`, and the refusal; earlier bundle bytes unchanged.\n3. Failing-first tests: `ledger_row_records_grant_ref_and_platform_principal`,\n   `caller_written_identity_is_ignored_when_context_is_verified`,\n   `missing_delegated_context_is_refused_by_name_when_required`,\n   `delegated_context_bound_to_another_subject_is_refused`.\n4. A conformance vector pair: substrate's fixture and a connectors-owned copy, byte-identical, so\n   the atlas O1 row can cite both sides.\n\n## Out of Scope\n\nGrant *evaluation* — connectors decides; substrate records and verifies binding only (design 06\n§ 2: \"a higher-layer permit cannot override\" substrate's local checks, and substrate never\nbecomes the authorization engine). Hosted identity material (phase 7).\n\n## Open Questions\n\nWhether the delegated context is a signed JWT-shaped token from identity or a sealed blob from\nconnectors. Decides: the ADR, with both owners. Default if nobody answers: **identity-signed,\naudience `substrate`**, because atlas O1 already pairs \"connectors grants and identity audiences\".\n\n## Design draft — 2026-08-30\n\n`docs/design/09-delegated-context-and-grant-attribution.md` (proposed) carries the shape and the\nADR text ready to accept. Findings that move this story's default: identity issues **opaque**\ntokens and holds no signing key, `kid` or JWKS (`identity/src/lib.rs:1936-1939`, `:2172`), and\n`urn:b10x:substrate` is documented but not issuable (`identity/README.md:144`,\n`identity/src/lib.rs:1920-1922`); connectors **already** issues Ed25519 compact JWS carrying\n`dl_grant`, `sub`, `act` (`connectors/crates/service/src/authority.rs:52-71`) and names the field\n`grant_ref` (`connectors/crates/integration-catalog/src/lib.rs:99-106`). The draft keeps the\nstory's default (identity-signed) with that cost stated; the cheaper path is a connectors-signed\ncontext. Decides: the ADR, with both owners.\n"},"command":"update-entity","target":"01MEM0000000000000028"},"payload":{"actor":"human:timo","at":1788041882030,"causation":"cmd-protocol-artifact-body-story-ledger-rows-carry-the-declared-grant-1788041882030","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body","event_id":"story:ledger-rows-carry-the-declared-grant@3#0~95ccedd91302df83","executor":null,"recorded_at":"2026-08-29T22:18:02Z"}}
{"entity":"story","version":1,"id":"destination-bound-egress","revision":3,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"# Story: Destination-bound egress apertures are declared, verified and refused by name\n\n## Outcome\n\nA confined run reaches exactly the network destinations an operator declared for it — and, with\n`story:sealed-secret-slots`, a vendor harness (`codex`, `claude`) can run under substrate\nconfinement with its model credential and its model endpoint, which design 05 § *Progress* says is\nrefused until both capabilities exist.\n\n## Context\n\n`docs/design/04-security-and-isolation.md` § 6: egress, listening sockets and exposed endpoints\nare separate capabilities; ordinary execution defaults to **no egress**; \"an aperture is\ndeployment/operator authority\" matched to a destination. The delegated lane today proves\nnamespace no-egress and nothing else. Atlas O3/O4 want vendor harnesses driven under the same\ngovernor as the native loop; `autodev` dispatches only `codex` and `claude`\n(`atlas/ROADMAP.md`, arrow *autodev → the fleet*). A vendor harness that cannot reach its model\nis not confined, it is dead — so secrets and egress are one capability pair, and the review that\nfiled this story found the backlog held only the first half.\n\n## Acceptance\n\nA run started with a declared egress aperture reaches the declared destination and nothing else,\nthe applied aperture is an observation in the run's record, and a request for an aperture the\ndeployment did not declare is `unserved` with the aperture named — proven by the delegated lane\nwith a real listener inside and outside the aperture.\n\nEvidence that satisfies it, in order:\n\n1. **Before code**: an ADR fixing the aperture shape (host:port or name:port, protocol, whether\n   DNS resolution is inside the aperture), the operator declaration surface, and the refusal.\n2. A successor bundle adds the request field, the capability fact `network.egress.apertures`, the\n   observation, and the refusal; earlier bundle bytes unchanged.\n3. Failing-first tests: `egress_defaults_to_none`, `declared_aperture_is_reachable`,\n   `undeclared_destination_is_unreachable_and_named`,\n   `aperture_outside_operator_declaration_is_unserved`, `applied_aperture_is_observed`.\n4. The delegated lane runs the model-free fake app-server against a loopback endpoint inside the\n   aperture and proves a second endpoint outside it is unreachable.\n\n## Out of Scope\n\nPublic ingress, DNS, TLS termination and tunnels (design 05 § 4). Brokered connector artifacts.\n\n## Open Questions\n\nEnforcement mechanism inside the existing namespace no-egress posture: a veth pair plus nftables\nin the sandbox namespace, or a userspace proxy at a declared descriptor. Decides: the ADR. Default\nif nobody answers: **namespace + nftables**, because it keeps bytes out of the daemon.\n\n## Design draft — 2026-08-30\n\n`docs/design/10-destination-bound-egress.md` (proposed). Finding that overturns this story's\ndefault: option (a), namespace + nftables, needs root, and the daemon withholds `exec` when it\nruns as root (`crates/substrate-host/src/probe.rs:49-51`); bubblewrap 0.11.2 has no `--netns`, so\na prepared namespace cannot be handed in. The draft recommends (c), a per-run forwarder inside the\nsandbox netns, owing a `setns` spike on a delegated host before the ADR. `NetworkMode::Aperture`\nalready exists and is `unserved` twice (`substrate-wire/src/lib.rs:595-599`,\n`operations.rs:290-300`, `process.rs:810-815`); the only executed no-egress proof today is one\n`connect` to `1.1.1.1:53` (`scripts/check-runtime-vectors.py:348-367`).\n"},"args":{"changes":{"body":"# Story: Destination-bound egress apertures are declared, verified and refused by name\n\n## Outcome\n\nA confined run reaches exactly the network destinations an operator declared for it — and, with\n`story:sealed-secret-slots`, a vendor harness (`codex`, `claude`) can run under substrate\nconfinement with its model credential and its model endpoint, which design 05 § *Progress* says is\nrefused until both capabilities exist.\n\n## Context\n\n`docs/design/04-security-and-isolation.md` § 6: egress, listening sockets and exposed endpoints\nare separate capabilities; ordinary execution defaults to **no egress**; \"an aperture is\ndeployment/operator authority\" matched to a destination. The delegated lane today proves\nnamespace no-egress and nothing else. Atlas O3/O4 want vendor harnesses driven under the same\ngovernor as the native loop; `autodev` dispatches only `codex` and `claude`\n(`atlas/ROADMAP.md`, arrow *autodev → the fleet*). A vendor harness that cannot reach its model\nis not confined, it is dead — so secrets and egress are one capability pair, and the review that\nfiled this story found the backlog held only the first half.\n\n## Acceptance\n\nA run started with a declared egress aperture reaches the declared destination and nothing else,\nthe applied aperture is an observation in the run's record, and a request for an aperture the\ndeployment did not declare is `unserved` with the aperture named — proven by the delegated lane\nwith a real listener inside and outside the aperture.\n\nEvidence that satisfies it, in order:\n\n1. **Before code**: an ADR fixing the aperture shape (host:port or name:port, protocol, whether\n   DNS resolution is inside the aperture), the operator declaration surface, and the refusal.\n2. A successor bundle adds the request field, the capability fact `network.egress.apertures`, the\n   observation, and the refusal; earlier bundle bytes unchanged.\n3. Failing-first tests: `egress_defaults_to_none`, `declared_aperture_is_reachable`,\n   `undeclared_destination_is_unreachable_and_named`,\n   `aperture_outside_operator_declaration_is_unserved`, `applied_aperture_is_observed`.\n4. The delegated lane runs the model-free fake app-server against a loopback endpoint inside the\n   aperture and proves a second endpoint outside it is unreachable.\n\n## Out of Scope\n\nPublic ingress, DNS, TLS termination and tunnels (design 05 § 4). Brokered connector artifacts.\n\n## Open Questions\n\nEnforcement mechanism inside the existing namespace no-egress posture: a veth pair plus nftables\nin the sandbox namespace, or a userspace proxy at a declared descriptor. Decides: the ADR. Default\nif nobody answers: **namespace + nftables**, because it keeps bytes out of the daemon.\n\n## Design draft — 2026-08-30\n\n`docs/design/10-destination-bound-egress.md` (proposed). Finding that overturns this story's\ndefault: option (a), namespace + nftables, needs root, and the daemon withholds `exec` when it\nruns as root (`crates/substrate-host/src/probe.rs:49-51`); bubblewrap 0.11.2 has no `--netns`, so\na prepared namespace cannot be handed in. The draft recommends (c), a per-run forwarder inside the\nsandbox netns, owing a `setns` spike on a delegated host before the ADR. `NetworkMode::Aperture`\nalready exists and is `unserved` twice (`substrate-wire/src/lib.rs:595-599`,\n`operations.rs:290-300`, `process.rs:810-815`); the only executed no-egress proof today is one\n`connect` to `1.1.1.1:53` (`scripts/check-runtime-vectors.py:348-367`).\n"},"command":"update-entity","target":"01MEM0000000000000019"},"payload":{"actor":"human:timo","at":1788041882043,"causation":"cmd-protocol-artifact-body-story-destination-bound-egress-1788041882043","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body","event_id":"story:destination-bound-egress@3#0~89a8125fc5c7438b","executor":null,"recorded_at":"2026-08-29T22:18:02Z"}}
{"entity":"story","version":1,"id":"sealed-secret-slots","revision":3,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"# Story: Sealed secret slots reach a child only as a memfd descriptor\n\n## Outcome\n\nA run can use a daemon-configured secret without the value ever appearing in argv, environment,\nlogs, events, the ledger request hash or an error body — and design 05's reason for refusing a\nlive vendor harness is removed on the secret side.\n\n## Context\n\n`docs/design/06-authentication-secrets-and-trust.md` § *V1 decisions* 3 fixes the mechanism: a\nsealed Linux `memfd` at a declared child descriptor; only the slot→descriptor mapping in the shaped\nenvironment; acquisition after every admission and dispatch-time check; the daemon closes its copy\nimmediately after spawn; a driver that cannot prove sealing, isolation and cleanup reports the\ncapability absent. Design 04 § 7 (line 79): errors may name a slot, never the value.\n\n## Acceptance\n\nThe delegated lane proves the child reads the value from the declared descriptor and the value\nbytes appear nowhere in captured argv, environment, stdout, stderr, event, ledger or diagnostic\nbytes; the portable lane proves the typed refusal.\n\nEvidence that satisfies it, in order:\n\n1. `adr/0012-secret-slots-are-sealed-memfds.md`: slot naming, descriptor declaration, the seal set\n   (`F_SEAL_WRITE|SHRINK|GROW|SEAL`), refusal classes.\n2. A successor bundle adds `secret_slots` on exec and session start, the capability fact\n   `secrets.slots`, and the refusal class; earlier bundle bytes unchanged.\n3. Failing-first tests: `secret_slot_value_absent_from_argv_env_events_and_ledger`,\n   `secret_slot_memfd_is_sealed` (child `fcntl(F_GET_SEALS)` equals the declared set),\n   `secret_slot_refused_when_sealing_unavailable`, `daemon_closes_its_copy_after_spawn`\n   (`/proc/<daemon>/fd` holds no memfd after spawn).\n4. The ledger request hash covers slot **names** only: two requests differing only in slot value\n   hash identically.\n\n## Out of Scope\n\nBrokered artifact secrets (design 06 decision 4) and destination-bound egress.\n\n## Design draft — 2026-08-30\n\n`docs/design/11-sealed-secret-slots.md` (proposed), with `adr/0012` text ready to accept. Verified\nat runtime, not inferred: bubblewrap 0.11.2 passes an inherited descriptor to the child at the\nsame number; a sealed memfd read back inside the sandbox reports `F_GET_SEALS == 0xf` and\n`pwrite → EPERM`. `libc` already exports `memfd_create` and the seal constants — no Cargo change.\nToday's `close_range` (`process.rs:348-377`) would close a slot fd; the draft generalises it to one\nrange per gap. Mapping variable `SUBSTRATE_SECRET_SLOTS=name=fd,…`; `MFD_CLOEXEC` mandatory.\n"},"args":{"changes":{"body":"# Story: Sealed secret slots reach a child only as a memfd descriptor\n\n## Outcome\n\nA run can use a daemon-configured secret without the value ever appearing in argv, environment,\nlogs, events, the ledger request hash or an error body — and design 05's reason for refusing a\nlive vendor harness is removed on the secret side.\n\n## Context\n\n`docs/design/06-authentication-secrets-and-trust.md` § *V1 decisions* 3 fixes the mechanism: a\nsealed Linux `memfd` at a declared child descriptor; only the slot→descriptor mapping in the shaped\nenvironment; acquisition after every admission and dispatch-time check; the daemon closes its copy\nimmediately after spawn; a driver that cannot prove sealing, isolation and cleanup reports the\ncapability absent. Design 04 § 7 (line 79): errors may name a slot, never the value.\n\n## Acceptance\n\nThe delegated lane proves the child reads the value from the declared descriptor and the value\nbytes appear nowhere in captured argv, environment, stdout, stderr, event, ledger or diagnostic\nbytes; the portable lane proves the typed refusal.\n\nEvidence that satisfies it, in order:\n\n1. `adr/0012-secret-slots-are-sealed-memfds.md`: slot naming, descriptor declaration, the seal set\n   (`F_SEAL_WRITE|SHRINK|GROW|SEAL`), refusal classes.\n2. A successor bundle adds `secret_slots` on exec and session start, the capability fact\n   `secrets.slots`, and the refusal class; earlier bundle bytes unchanged.\n3. Failing-first tests: `secret_slot_value_absent_from_argv_env_events_and_ledger`,\n   `secret_slot_memfd_is_sealed` (child `fcntl(F_GET_SEALS)` equals the declared set),\n   `secret_slot_refused_when_sealing_unavailable`, `daemon_closes_its_copy_after_spawn`\n   (`/proc/<daemon>/fd` holds no memfd after spawn).\n4. The ledger request hash covers slot **names** only: two requests differing only in slot value\n   hash identically.\n\n## Out of Scope\n\nBrokered artifact secrets (design 06 decision 4) and destination-bound egress.\n\n## Design draft — 2026-08-30\n\n`docs/design/11-sealed-secret-slots.md` (proposed), with `adr/0012` text ready to accept. Verified\nat runtime, not inferred: bubblewrap 0.11.2 passes an inherited descriptor to the child at the\nsame number; a sealed memfd read back inside the sandbox reports `F_GET_SEALS == 0xf` and\n`pwrite → EPERM`. `libc` already exports `memfd_create` and the seal constants — no Cargo change.\nToday's `close_range` (`process.rs:348-377`) would close a slot fd; the draft generalises it to one\nrange per gap. Mapping variable `SUBSTRATE_SECRET_SLOTS=name=fd,…`; `MFD_CLOEXEC` mandatory.\n"},"command":"update-entity","target":"01MEM0000000000000040"},"payload":{"actor":"human:timo","at":1788041882057,"causation":"cmd-protocol-artifact-body-story-sealed-secret-slots-1788041882057","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body","event_id":"story:sealed-secret-slots@3#0~e9509ed86ba848ec","executor":null,"recorded_at":"2026-08-29T22:18:02Z"}}
{"entity":"story","version":1,"id":"tooling-moves-to-cargo-xtask","revision":6,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"body":"# Story: Anything that runs is Rust — the gate's Python moves to `cargo xtask`\n\n## Outcome\n\n`scripts/gate.sh` runs Rust: one `xtask` crate (clap) owns `check-links`, `check-adrs`,\n`check-toolchain`, `package-bundle`, `check-bundle` and the runtime-vector runner, and the next\nwire bundle (`0.5.0`) is rendered from `substrate-wire`'s types rather than from Python dicts. The\nfour frozen renderers and their checkers stay exactly as they are, as the reproducibility proof of\n`0.1.0`–`0.4.0` (invariant 6), and nothing new is written in Python.\n\n## Context\n\n`atlas/AGENTS.md` § *Language*, 2026-08-29: anything that runs is Rust; a new `.py` in a foundation\nrepository is an operator decision. Measured the same day: `scripts/*.py` is 23,916 lines beside\n30,768 lines of Rust; 18,300 of those are `render-contract-bundle{,-0.2.0,-0.3.0,-0.4.0}.py` and\n`check-contract-bundle{,-0.2.0,-0.3.0,-0.4.0}.py`, near-copies per version. The validator that does\nthe standards work is already Rust (`crates/substrate-contract-check`, `jsonschema = \"=0.49.9\"`;\n`scripts/contract_json_gate.py:195` shells out to it). The wire has two descriptions — the Rust\ntypes in `crates/substrate-wire` and the renderer's dicts — and only the vectors would notice them\ndrifting. Wave 1 of this store added ~1,200 more Python lines (`check-toolchain.py`,\n`package-contract-bundle.py` and its tests) by following the local convention; they are the first\nto move.\n\n## Acceptance\n\n`bash scripts/gate.sh` invokes no Python except the four frozen renderers' checkers, every moved\ncheck refuses the same inputs its Python predecessor refused (proven by the predecessor's own\nnegative cases run against the Rust replacement before the Python is deleted), `0.4.0` still\nverifies byte-clean, and `cargo xtask render-bundle 0.5.0` is the only renderer for the successor.\n\nEvidence that satisfies it, in order:\n\n1. `xtask/` crate in the workspace (`cargo xtask <verb>`, clap derive), `--locked` clean.\n2. `check-toolchain`, `check-links`, `check-adrs` moved first (smallest, no wire contact), each with\n   the Python negative cases ported as tests; the Python files deleted in the same commit.\n3. `package-bundle` moved with `test_package_contract_bundle.py`'s 21 cases ported; the 0.4.0\n   manifest digest `sha256:3758e80b…` reproduced by the Rust packager before the Python goes.\n4. `check-runtime-vectors` moved (the black-box Unix-socket HTTP runner, 1,194 lines) — last of the\n   gate steps, because it is the one with the most behaviour.\n5. `render-bundle 0.5.0` derives schemas from `substrate-wire` types (`schemars` or hand-rolled\n   emitters — decided in the story), with the `0.4.0` compatibility block computed, not typed.\n6. `AGENTS.md` § *The gate* and `README.md` § *Build, test, run* list the xtask verbs; the four\n   frozen renderers are documented as frozen proofs, not tooling.\n\n## Out of Scope\n\nRe-rendering `0.1.0`–`0.4.0` from Rust. They are frozen; their Python renderers are their proof.\n\n## Open Questions\n\nWhether `check-runtime-vectors` becomes an `xtask` verb or an integration test under\n`crates/substrate-daemon/tests/` (it drives a real daemon over a socket, which the daemon's tests\nalready do). Decides: operator. Default if nobody answers: **integration test** — one fewer\nbinary, and the gate already runs `cargo test`.\n\n## Progress — 2026-08-30, steps 1–2\n\n- `xtask/` (bin-only, clap derive; `anyhow`, `clap`, dev `tempfile` — no new crate, `Cargo.lock`\n  +9 lines, zero version bumps); `.cargo/config.toml` alias `cargo xtask`.\n- `check-toolchain --root`, `check-links`, `check-adrs` ported; the three Python files deleted in\n  the same change. Differential runs against the Python before deletion: check-links 19 cases +\n  whole repo byte-identical stdout/stderr; check-adrs 11 mutations of `adr/` 11/11 identical;\n  check-toolchain 12 mutations 12/12 identical. Two deliberate differences: the predecessor\n  monorepo escape hatch in check-links is gone (no ancestor has `scripts/check-monorepo.sh`), and\n  `[x](   )` is skipped where the Python raised `IndexError` (`check-links.py:33`).\n- Repo root at runtime is the nearest ancestor whose `Cargo.toml` has `[workspace]`\n  (`xtask/src/repo.rs`), not the binary's compile-time path.\n- Tests written failing-first (`2 passed; 26 failed` on `unimplemented!()` stubs) →\n  `cargo test -p xtask --locked`: 28 passed. `bash scripts/gate.sh`: passed, 13 steps.\n- `gate.sh:18,19,27`, `AGENTS.md` § The gate, `README.md` § Build, `STATUS.md` (three links to the\n  deleted files) updated. Remaining: steps 3–6 (packager, runtime vectors, `render-bundle 0.5.0`).\n\n## Progress — 2026-08-30, step 3\n\n- `cargo xtask package-bundle <version> --out <dir> [--force] [--source-date-epoch] [--contracts-root]`\n  (`xtask/src/package.rs`): hand-written ustar headers matching `tarfile.USTAR_FORMAT` byte for\n  byte, `serde_json` pretty output post-escaped to `json.dumps(ensure_ascii=True)`, epoch from\n  `git log -1 --format=%at -- .` or the flag. Differential against the Python on 0.4.0 before\n  deletion: identical stdout (`sha256:3758e80b…`, archive `sha256:91fb5524…`, 880,640 bytes),\n  `diff -r` empty, modes identical, eleven refusal cases with identical exit code and text.\n- 21 packager tests ported (49 in the crate); failing-first on a timestamp-embedding stub\n  (`45 passed; 4 failed`) then green. Both Python files deleted; `gate.sh` drops the packager\n  test line (it runs under `cargo test --workspace`); gate 12 steps, passed.\n- Remaining: steps 4–6.\n"},"args":{"changes":{"body":"# Story: Anything that runs is Rust — the gate's Python moves to `cargo xtask`\n\n## Outcome\n\n`scripts/gate.sh` runs Rust: one `xtask` crate (clap) owns `check-links`, `check-adrs`,\n`check-toolchain`, `package-bundle`, `check-bundle` and the runtime-vector runner, and the next\nwire bundle (`0.5.0`) is rendered from `substrate-wire`'s types rather than from Python dicts. The\nfour frozen renderers and their checkers stay exactly as they are, as the reproducibility proof of\n`0.1.0`–`0.4.0` (invariant 6), and nothing new is written in Python.\n\n## Context\n\n`atlas/AGENTS.md` § *Language*, 2026-08-29: anything that runs is Rust; a new `.py` in a foundation\nrepository is an operator decision. Measured the same day: `scripts/*.py` is 23,916 lines beside\n30,768 lines of Rust; 18,300 of those are `render-contract-bundle{,-0.2.0,-0.3.0,-0.4.0}.py` and\n`check-contract-bundle{,-0.2.0,-0.3.0,-0.4.0}.py`, near-copies per version. The validator that does\nthe standards work is already Rust (`crates/substrate-contract-check`, `jsonschema = \"=0.49.9\"`;\n`scripts/contract_json_gate.py:195` shells out to it). The wire has two descriptions — the Rust\ntypes in `crates/substrate-wire` and the renderer's dicts — and only the vectors would notice them\ndrifting. Wave 1 of this store added ~1,200 more Python lines (`check-toolchain.py`,\n`package-contract-bundle.py` and its tests) by following the local convention; they are the first\nto move.\n\n## Acceptance\n\n`bash scripts/gate.sh` invokes no Python except the four frozen renderers' checkers, every moved\ncheck refuses the same inputs its Python predecessor refused (proven by the predecessor's own\nnegative cases run against the Rust replacement before the Python is deleted), `0.4.0` still\nverifies byte-clean, and `cargo xtask render-bundle 0.5.0` is the only renderer for the successor.\n\nEvidence that satisfies it, in order:\n\n1. `xtask/` crate in the workspace (`cargo xtask <verb>`, clap derive), `--locked` clean.\n2. `check-toolchain`, `check-links`, `check-adrs` moved first (smallest, no wire contact), each with\n   the Python negative cases ported as tests; the Python files deleted in the same commit.\n3. `package-bundle` moved with `test_package_contract_bundle.py`'s 21 cases ported; the 0.4.0\n   manifest digest `sha256:3758e80b…` reproduced by the Rust packager before the Python goes.\n4. `check-runtime-vectors` moved (the black-box Unix-socket HTTP runner, 1,194 lines) — last of the\n   gate steps, because it is the one with the most behaviour.\n5. `render-bundle 0.5.0` derives schemas from `substrate-wire` types (`schemars` or hand-rolled\n   emitters — decided in the story), with the `0.4.0` compatibility block computed, not typed.\n6. `AGENTS.md` § *The gate* and `README.md` § *Build, test, run* list the xtask verbs; the four\n   frozen renderers are documented as frozen proofs, not tooling.\n\n## Out of Scope\n\nRe-rendering `0.1.0`–`0.4.0` from Rust. They are frozen; their Python renderers are their proof.\n\n## Open Questions\n\nWhether `check-runtime-vectors` becomes an `xtask` verb or an integration test under\n`crates/substrate-daemon/tests/` (it drives a real daemon over a socket, which the daemon's tests\nalready do). Decides: operator. Default if nobody answers: **integration test** — one fewer\nbinary, and the gate already runs `cargo test`.\n\n## Progress — 2026-08-30, steps 1–2\n\n- `xtask/` (bin-only, clap derive; `anyhow`, `clap`, dev `tempfile` — no new crate, `Cargo.lock`\n  +9 lines, zero version bumps); `.cargo/config.toml` alias `cargo xtask`.\n- `check-toolchain --root`, `check-links`, `check-adrs` ported; the three Python files deleted in\n  the same change. Differential runs against the Python before deletion: check-links 19 cases +\n  whole repo byte-identical stdout/stderr; check-adrs 11 mutations of `adr/` 11/11 identical;\n  check-toolchain 12 mutations 12/12 identical. Two deliberate differences: the predecessor\n  monorepo escape hatch in check-links is gone (no ancestor has `scripts/check-monorepo.sh`), and\n  `[x](   )` is skipped where the Python raised `IndexError` (`check-links.py:33`).\n- Repo root at runtime is the nearest ancestor whose `Cargo.toml` has `[workspace]`\n  (`xtask/src/repo.rs`), not the binary's compile-time path.\n- Tests written failing-first (`2 passed; 26 failed` on `unimplemented!()` stubs) →\n  `cargo test -p xtask --locked`: 28 passed. `bash scripts/gate.sh`: passed, 13 steps.\n- `gate.sh:18,19,27`, `AGENTS.md` § The gate, `README.md` § Build, `STATUS.md` (three links to the\n  deleted files) updated. Remaining: steps 3–6 (packager, runtime vectors, `render-bundle 0.5.0`).\n\n## Progress — 2026-08-30, step 3\n\n- `cargo xtask package-bundle <version> --out <dir> [--force] [--source-date-epoch] [--contracts-root]`\n  (`xtask/src/package.rs`): hand-written ustar headers matching `tarfile.USTAR_FORMAT` byte for\n  byte, `serde_json` pretty output post-escaped to `json.dumps(ensure_ascii=True)`, epoch from\n  `git log -1 --format=%at -- .` or the flag. Differential against the Python on 0.4.0 before\n  deletion: identical stdout (`sha256:3758e80b…`, archive `sha256:91fb5524…`, 880,640 bytes),\n  `diff -r` empty, modes identical, eleven refusal cases with identical exit code and text.\n- 21 packager tests ported (49 in the crate); failing-first on a timestamp-embedding stub\n  (`45 passed; 4 failed`) then green. Both Python files deleted; `gate.sh` drops the packager\n  test line (it runs under `cargo test --workspace`); gate 12 steps, passed.\n- Remaining: steps 4–6.\n"},"command":"update-entity","target":"01MEM0000000000000049"},"payload":{"actor":"human:timo","at":1788042325495,"causation":"cmd-protocol-artifact-body-story-tooling-moves-to-cargo-xtask-1788042325495","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body","event_id":"story:tooling-moves-to-cargo-xtask@6#0~875f62b9293ab5e1","executor":null,"recorded_at":"2026-08-29T22:25:25Z"}}
{"entity":"story","version":1,"id":"tooling-moves-to-cargo-xtask","revision":6,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"https://github.com/beyond10x/substrate/actions/runs/33278561873","source":"GitHub Actions Gate workflow, PR #6 (step 3)","target":"01MEM0000000000000049"},"payload":{"actor":"human:timo","at":1788042525000,"causation":"cmd-evidence-story-tooling-moves-to-cargo-xtask-test_result-1788042525821","change":{"change":"evidence","kind":"test_result","reference":"https://github.com/beyond10x/substrate/actions/runs/33278561873","source":"GitHub Actions Gate workflow, PR #6 (step 3)"},"correlation":"protocol-artifact-evidence","event_id":"story:tooling-moves-to-cargo-xtask@6#0~1d8ff6c563ac624b","executor":null,"recorded_at":"2026-08-29T22:28:45Z"}}
{"entity":"story","version":1,"id":"destination-bound-egress","revision":4,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"# Story: Destination-bound egress apertures are declared, verified and refused by name\n\n## Outcome\n\nA confined run reaches exactly the network destinations an operator declared for it — and, with\n`story:sealed-secret-slots`, a vendor harness (`codex`, `claude`) can run under substrate\nconfinement with its model credential and its model endpoint, which design 05 § *Progress* says is\nrefused until both capabilities exist.\n\n## Context\n\n`docs/design/04-security-and-isolation.md` § 6: egress, listening sockets and exposed endpoints\nare separate capabilities; ordinary execution defaults to **no egress**; \"an aperture is\ndeployment/operator authority\" matched to a destination. The delegated lane today proves\nnamespace no-egress and nothing else. Atlas O3/O4 want vendor harnesses driven under the same\ngovernor as the native loop; `autodev` dispatches only `codex` and `claude`\n(`atlas/ROADMAP.md`, arrow *autodev → the fleet*). A vendor harness that cannot reach its model\nis not confined, it is dead — so secrets and egress are one capability pair, and the review that\nfiled this story found the backlog held only the first half.\n\n## Acceptance\n\nA run started with a declared egress aperture reaches the declared destination and nothing else,\nthe applied aperture is an observation in the run's record, and a request for an aperture the\ndeployment did not declare is `unserved` with the aperture named — proven by the delegated lane\nwith a real listener inside and outside the aperture.\n\nEvidence that satisfies it, in order:\n\n1. **Before code**: an ADR fixing the aperture shape (host:port or name:port, protocol, whether\n   DNS resolution is inside the aperture), the operator declaration surface, and the refusal.\n2. A successor bundle adds the request field, the capability fact `network.egress.apertures`, the\n   observation, and the refusal; earlier bundle bytes unchanged.\n3. Failing-first tests: `egress_defaults_to_none`, `declared_aperture_is_reachable`,\n   `undeclared_destination_is_unreachable_and_named`,\n   `aperture_outside_operator_declaration_is_unserved`, `applied_aperture_is_observed`.\n4. The delegated lane runs the model-free fake app-server against a loopback endpoint inside the\n   aperture and proves a second endpoint outside it is unreachable.\n\n## Out of Scope\n\nPublic ingress, DNS, TLS termination and tunnels (design 05 § 4). Brokered connector artifacts.\n\n## Open Questions\n\nEnforcement mechanism inside the existing namespace no-egress posture: a veth pair plus nftables\nin the sandbox namespace, or a userspace proxy at a declared descriptor. Decides: the ADR. Default\nif nobody answers: **namespace + nftables**, because it keeps bytes out of the daemon.\n\n## Design draft — 2026-08-30\n\n`docs/design/10-destination-bound-egress.md` (proposed). Finding that overturns this story's\ndefault: option (a), namespace + nftables, needs root, and the daemon withholds `exec` when it\nruns as root (`crates/substrate-host/src/probe.rs:49-51`); bubblewrap 0.11.2 has no `--netns`, so\na prepared namespace cannot be handed in. The draft recommends (c), a per-run forwarder inside the\nsandbox netns, owing a `setns` spike on a delegated host before the ADR. `NetworkMode::Aperture`\nalready exists and is `unserved` twice (`substrate-wire/src/lib.rs:595-599`,\n`operations.rs:290-300`, `process.rs:810-815`); the only executed no-egress proof today is one\n`connect` to `1.1.1.1:53` (`crates/substrate-daemon/tests/runtime_vectors.rs:620-640`, ported from the retired\n`scripts/check-runtime-vectors.py:348-367` on 2026-08-30).\n"},"args":{"changes":{"body":"# Story: Destination-bound egress apertures are declared, verified and refused by name\n\n## Outcome\n\nA confined run reaches exactly the network destinations an operator declared for it — and, with\n`story:sealed-secret-slots`, a vendor harness (`codex`, `claude`) can run under substrate\nconfinement with its model credential and its model endpoint, which design 05 § *Progress* says is\nrefused until both capabilities exist.\n\n## Context\n\n`docs/design/04-security-and-isolation.md` § 6: egress, listening sockets and exposed endpoints\nare separate capabilities; ordinary execution defaults to **no egress**; \"an aperture is\ndeployment/operator authority\" matched to a destination. The delegated lane today proves\nnamespace no-egress and nothing else. Atlas O3/O4 want vendor harnesses driven under the same\ngovernor as the native loop; `autodev` dispatches only `codex` and `claude`\n(`atlas/ROADMAP.md`, arrow *autodev → the fleet*). A vendor harness that cannot reach its model\nis not confined, it is dead — so secrets and egress are one capability pair, and the review that\nfiled this story found the backlog held only the first half.\n\n## Acceptance\n\nA run started with a declared egress aperture reaches the declared destination and nothing else,\nthe applied aperture is an observation in the run's record, and a request for an aperture the\ndeployment did not declare is `unserved` with the aperture named — proven by the delegated lane\nwith a real listener inside and outside the aperture.\n\nEvidence that satisfies it, in order:\n\n1. **Before code**: an ADR fixing the aperture shape (host:port or name:port, protocol, whether\n   DNS resolution is inside the aperture), the operator declaration surface, and the refusal.\n2. A successor bundle adds the request field, the capability fact `network.egress.apertures`, the\n   observation, and the refusal; earlier bundle bytes unchanged.\n3. Failing-first tests: `egress_defaults_to_none`, `declared_aperture_is_reachable`,\n   `undeclared_destination_is_unreachable_and_named`,\n   `aperture_outside_operator_declaration_is_unserved`, `applied_aperture_is_observed`.\n4. The delegated lane runs the model-free fake app-server against a loopback endpoint inside the\n   aperture and proves a second endpoint outside it is unreachable.\n\n## Out of Scope\n\nPublic ingress, DNS, TLS termination and tunnels (design 05 § 4). Brokered connector artifacts.\n\n## Open Questions\n\nEnforcement mechanism inside the existing namespace no-egress posture: a veth pair plus nftables\nin the sandbox namespace, or a userspace proxy at a declared descriptor. Decides: the ADR. Default\nif nobody answers: **namespace + nftables**, because it keeps bytes out of the daemon.\n\n## Design draft — 2026-08-30\n\n`docs/design/10-destination-bound-egress.md` (proposed). Finding that overturns this story's\ndefault: option (a), namespace + nftables, needs root, and the daemon withholds `exec` when it\nruns as root (`crates/substrate-host/src/probe.rs:49-51`); bubblewrap 0.11.2 has no `--netns`, so\na prepared namespace cannot be handed in. The draft recommends (c), a per-run forwarder inside the\nsandbox netns, owing a `setns` spike on a delegated host before the ADR. `NetworkMode::Aperture`\nalready exists and is `unserved` twice (`substrate-wire/src/lib.rs:595-599`,\n`operations.rs:290-300`, `process.rs:810-815`); the only executed no-egress proof today is one\n`connect` to `1.1.1.1:53` (`crates/substrate-daemon/tests/runtime_vectors.rs:620-640`, ported from the retired\n`scripts/check-runtime-vectors.py:348-367` on 2026-08-30).\n"},"command":"update-entity","target":"01MEM0000000000000019"},"payload":{"actor":"human:timo","at":1788043632724,"causation":"cmd-protocol-artifact-body-story-destination-bound-egress-1788043632724","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body","event_id":"story:destination-bound-egress@4#0~1fa5bb399557b808","executor":null,"recorded_at":"2026-08-29T22:47:12Z"}}
{"entity":"story","version":1,"id":"tooling-moves-to-cargo-xtask","revision":7,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"body":"# Story: Anything that runs is Rust — the gate's Python moves to `cargo xtask`\n\n## Outcome\n\n`scripts/gate.sh` runs Rust: one `xtask` crate (clap) owns `check-links`, `check-adrs`,\n`check-toolchain`, `package-bundle`, `check-bundle` and the runtime-vector runner, and the next\nwire bundle (`0.5.0`) is rendered from `substrate-wire`'s types rather than from Python dicts. The\nfour frozen renderers and their checkers stay exactly as they are, as the reproducibility proof of\n`0.1.0`–`0.4.0` (invariant 6), and nothing new is written in Python.\n\n## Context\n\n`atlas/AGENTS.md` § *Language*, 2026-08-29: anything that runs is Rust; a new `.py` in a foundation\nrepository is an operator decision. Measured the same day: `scripts/*.py` is 23,916 lines beside\n30,768 lines of Rust; 18,300 of those are `render-contract-bundle{,-0.2.0,-0.3.0,-0.4.0}.py` and\n`check-contract-bundle{,-0.2.0,-0.3.0,-0.4.0}.py`, near-copies per version. The validator that does\nthe standards work is already Rust (`crates/substrate-contract-check`, `jsonschema = \"=0.49.9\"`;\n`scripts/contract_json_gate.py:195` shells out to it). The wire has two descriptions — the Rust\ntypes in `crates/substrate-wire` and the renderer's dicts — and only the vectors would notice them\ndrifting. Wave 1 of this store added ~1,200 more Python lines (`check-toolchain.py`,\n`package-contract-bundle.py` and its tests) by following the local convention; they are the first\nto move.\n\n## Acceptance\n\n`bash scripts/gate.sh` invokes no Python except the four frozen renderers' checkers, every moved\ncheck refuses the same inputs its Python predecessor refused (proven by the predecessor's own\nnegative cases run against the Rust replacement before the Python is deleted), `0.4.0` still\nverifies byte-clean, and `cargo xtask render-bundle 0.5.0` is the only renderer for the successor.\n\nEvidence that satisfies it, in order:\n\n1. `xtask/` crate in the workspace (`cargo xtask <verb>`, clap derive), `--locked` clean.\n2. `check-toolchain`, `check-links`, `check-adrs` moved first (smallest, no wire contact), each with\n   the Python negative cases ported as tests; the Python files deleted in the same commit.\n3. `package-bundle` moved with `test_package_contract_bundle.py`'s 21 cases ported; the 0.4.0\n   manifest digest `sha256:3758e80b…` reproduced by the Rust packager before the Python goes.\n4. `check-runtime-vectors` moved (the black-box Unix-socket HTTP runner, 1,194 lines) — last of the\n   gate steps, because it is the one with the most behaviour.\n5. `render-bundle 0.5.0` derives schemas from `substrate-wire` types (`schemars` or hand-rolled\n   emitters — decided in the story), with the `0.4.0` compatibility block computed, not typed.\n6. `AGENTS.md` § *The gate* and `README.md` § *Build, test, run* list the xtask verbs; the four\n   frozen renderers are documented as frozen proofs, not tooling.\n\n## Out of Scope\n\nRe-rendering `0.1.0`–`0.4.0` from Rust. They are frozen; their Python renderers are their proof.\n\n## Open Questions\n\nWhether `check-runtime-vectors` becomes an `xtask` verb or an integration test under\n`crates/substrate-daemon/tests/` (it drives a real daemon over a socket, which the daemon's tests\nalready do). Decides: operator. Default if nobody answers: **integration test** — one fewer\nbinary, and the gate already runs `cargo test`.\n\n**Resolved 2026-08-30: integration test.** No existing daemon test spawns the binary — `http.rs:29-34`,\n`websocket.rs:36-38`, `pipe_session.rs` and `contract_vectors.rs` all build `App::new(...)` in\nprocess — so there was no harness to reuse, but the black-box property survives:\n`env!(\"CARGO_BIN_EXE_substrate-daemon\")` lets a test in that package spawn the shipped binary over\na Unix socket. Nothing outside the gate invoked the script.\n\n## Progress — 2026-08-30, steps 1–2\n\n- `xtask/` (bin-only, clap derive; `anyhow`, `clap`, dev `tempfile` — no new crate, `Cargo.lock`\n  +9 lines, zero version bumps); `.cargo/config.toml` alias `cargo xtask`.\n- `check-toolchain --root`, `check-links`, `check-adrs` ported; the three Python files deleted in\n  the same change. Differential runs against the Python before deletion: check-links 19 cases +\n  whole repo byte-identical stdout/stderr; check-adrs 11 mutations of `adr/` 11/11 identical;\n  check-toolchain 12 mutations 12/12 identical. Two deliberate differences: the predecessor\n  monorepo escape hatch in check-links is gone (no ancestor has `scripts/check-monorepo.sh`), and\n  `[x](   )` is skipped where the Python raised `IndexError` (`check-links.py:33`).\n- Repo root at runtime is the nearest ancestor whose `Cargo.toml` has `[workspace]`\n  (`xtask/src/repo.rs`), not the binary's compile-time path.\n- Tests written failing-first (`2 passed; 26 failed` on `unimplemented!()` stubs) →\n  `cargo test -p xtask --locked`: 28 passed. `bash scripts/gate.sh`: passed, 13 steps.\n- `gate.sh:18,19,27`, `AGENTS.md` § The gate, `README.md` § Build, `STATUS.md` (three links to the\n  deleted files) updated. Remaining: steps 3–6 (packager, runtime vectors, `render-bundle 0.5.0`).\n\n## Progress — 2026-08-30, step 3\n\n- `cargo xtask package-bundle <version> --out <dir> [--force] [--source-date-epoch] [--contracts-root]`\n  (`xtask/src/package.rs`): hand-written ustar headers matching `tarfile.USTAR_FORMAT` byte for\n  byte, `serde_json` pretty output post-escaped to `json.dumps(ensure_ascii=True)`, epoch from\n  `git log -1 --format=%at -- .` or the flag. Differential against the Python on 0.4.0 before\n  deletion: identical stdout (`sha256:3758e80b…`, archive `sha256:91fb5524…`, 880,640 bytes),\n  `diff -r` empty, modes identical, eleven refusal cases with identical exit code and text.\n- 21 packager tests ported (49 in the crate); failing-first on a timestamp-embedding stub\n  (`45 passed; 4 failed`) then green. Both Python files deleted; `gate.sh` drops the packager\n  test line (it runs under `cargo test --workspace`); gate 12 steps, passed.\n- Remaining: steps 4–6.\n\n\n## Progress — 2026-08-30, step 4\n\n- `crates/substrate-daemon/tests/runtime_vectors.rs` (1,730 lines, one `#[tokio::test]` mirroring\n  the Python's `main()` in order) replaces `scripts/check-runtime-vectors.py`, which is deleted.\n  Hand-written HTTP/1.1 over `tokio::net::UnixStream` and raw WebSocket frames; the lane switch is\n  `SUBSTRATE_VECTORS_CGROUP_ROOT` where the script took `--cgroup-root`, and an unset value keeps\n  the portable branch asserting `exec.sandbox-unavailable` 501 with the delegated cases absent,\n  never counted (invariant 3).\n- Zero new crates; `Cargo.lock` byte-identical. `nix` gains its test-only `signal` feature\n  (`crates/substrate-daemon/Cargo.toml:48-50`).\n- Differential against the Python before deletion: portable lane both `27 HTTP cases, startup\n  refusal, dual-daemon refusal`; delegated lane both `38 HTTP cases` under\n  `systemd-run --user -p Delegate=yes --scope`; two negative cases (`SUBSTRATE_ALLOW_UID` set,\n  non-delegated cgroup root) fail identically on both. Failing-first:\n  `not implemented: cases 2..27 are not ported yet` → `0 passed; 1 failed`.\n- Two deliberate improvements: the temp dir is forced to 0700 (Rust's `TempDir` is 0777 & ~umask\n  and the daemon refuses it), and the case counts are now asserted (`PORTABLE_CASES=27`,\n  `DELEGATED_CASES=38`) rather than printed.\n- The gate keeps no separate step: `cargo test --workspace --locked` runs it. Gate 11 steps,\n  passed; `cargo test --workspace`: 17 suites, 205 passed, 0 failed.\n- **The gate now runs no Python except the frozen bundles' own renderers and checkers.** Remaining\n  for this story: step 5 (`render-bundle 0.5.0` from `substrate-wire` types) and step 6.\n"},"args":{"changes":{"body":"# Story: Anything that runs is Rust — the gate's Python moves to `cargo xtask`\n\n## Outcome\n\n`scripts/gate.sh` runs Rust: one `xtask` crate (clap) owns `check-links`, `check-adrs`,\n`check-toolchain`, `package-bundle`, `check-bundle` and the runtime-vector runner, and the next\nwire bundle (`0.5.0`) is rendered from `substrate-wire`'s types rather than from Python dicts. The\nfour frozen renderers and their checkers stay exactly as they are, as the reproducibility proof of\n`0.1.0`–`0.4.0` (invariant 6), and nothing new is written in Python.\n\n## Context\n\n`atlas/AGENTS.md` § *Language*, 2026-08-29: anything that runs is Rust; a new `.py` in a foundation\nrepository is an operator decision. Measured the same day: `scripts/*.py` is 23,916 lines beside\n30,768 lines of Rust; 18,300 of those are `render-contract-bundle{,-0.2.0,-0.3.0,-0.4.0}.py` and\n`check-contract-bundle{,-0.2.0,-0.3.0,-0.4.0}.py`, near-copies per version. The validator that does\nthe standards work is already Rust (`crates/substrate-contract-check`, `jsonschema = \"=0.49.9\"`;\n`scripts/contract_json_gate.py:195` shells out to it). The wire has two descriptions — the Rust\ntypes in `crates/substrate-wire` and the renderer's dicts — and only the vectors would notice them\ndrifting. Wave 1 of this store added ~1,200 more Python lines (`check-toolchain.py`,\n`package-contract-bundle.py` and its tests) by following the local convention; they are the first\nto move.\n\n## Acceptance\n\n`bash scripts/gate.sh` invokes no Python except the four frozen renderers' checkers, every moved\ncheck refuses the same inputs its Python predecessor refused (proven by the predecessor's own\nnegative cases run against the Rust replacement before the Python is deleted), `0.4.0` still\nverifies byte-clean, and `cargo xtask render-bundle 0.5.0` is the only renderer for the successor.\n\nEvidence that satisfies it, in order:\n\n1. `xtask/` crate in the workspace (`cargo xtask <verb>`, clap derive), `--locked` clean.\n2. `check-toolchain`, `check-links`, `check-adrs` moved first (smallest, no wire contact), each with\n   the Python negative cases ported as tests; the Python files deleted in the same commit.\n3. `package-bundle` moved with `test_package_contract_bundle.py`'s 21 cases ported; the 0.4.0\n   manifest digest `sha256:3758e80b…` reproduced by the Rust packager before the Python goes.\n4. `check-runtime-vectors` moved (the black-box Unix-socket HTTP runner, 1,194 lines) — last of the\n   gate steps, because it is the one with the most behaviour.\n5. `render-bundle 0.5.0` derives schemas from `substrate-wire` types (`schemars` or hand-rolled\n   emitters — decided in the story), with the `0.4.0` compatibility block computed, not typed.\n6. `AGENTS.md` § *The gate* and `README.md` § *Build, test, run* list the xtask verbs; the four\n   frozen renderers are documented as frozen proofs, not tooling.\n\n## Out of Scope\n\nRe-rendering `0.1.0`–`0.4.0` from Rust. They are frozen; their Python renderers are their proof.\n\n## Open Questions\n\nWhether `check-runtime-vectors` becomes an `xtask` verb or an integration test under\n`crates/substrate-daemon/tests/` (it drives a real daemon over a socket, which the daemon's tests\nalready do). Decides: operator. Default if nobody answers: **integration test** — one fewer\nbinary, and the gate already runs `cargo test`.\n\n**Resolved 2026-08-30: integration test.** No existing daemon test spawns the binary — `http.rs:29-34`,\n`websocket.rs:36-38`, `pipe_session.rs` and `contract_vectors.rs` all build `App::new(...)` in\nprocess — so there was no harness to reuse, but the black-box property survives:\n`env!(\"CARGO_BIN_EXE_substrate-daemon\")` lets a test in that package spawn the shipped binary over\na Unix socket. Nothing outside the gate invoked the script.\n\n## Progress — 2026-08-30, steps 1–2\n\n- `xtask/` (bin-only, clap derive; `anyhow`, `clap`, dev `tempfile` — no new crate, `Cargo.lock`\n  +9 lines, zero version bumps); `.cargo/config.toml` alias `cargo xtask`.\n- `check-toolchain --root`, `check-links`, `check-adrs` ported; the three Python files deleted in\n  the same change. Differential runs against the Python before deletion: check-links 19 cases +\n  whole repo byte-identical stdout/stderr; check-adrs 11 mutations of `adr/` 11/11 identical;\n  check-toolchain 12 mutations 12/12 identical. Two deliberate differences: the predecessor\n  monorepo escape hatch in check-links is gone (no ancestor has `scripts/check-monorepo.sh`), and\n  `[x](   )` is skipped where the Python raised `IndexError` (`check-links.py:33`).\n- Repo root at runtime is the nearest ancestor whose `Cargo.toml` has `[workspace]`\n  (`xtask/src/repo.rs`), not the binary's compile-time path.\n- Tests written failing-first (`2 passed; 26 failed` on `unimplemented!()` stubs) →\n  `cargo test -p xtask --locked`: 28 passed. `bash scripts/gate.sh`: passed, 13 steps.\n- `gate.sh:18,19,27`, `AGENTS.md` § The gate, `README.md` § Build, `STATUS.md` (three links to the\n  deleted files) updated. Remaining: steps 3–6 (packager, runtime vectors, `render-bundle 0.5.0`).\n\n## Progress — 2026-08-30, step 3\n\n- `cargo xtask package-bundle <version> --out <dir> [--force] [--source-date-epoch] [--contracts-root]`\n  (`xtask/src/package.rs`): hand-written ustar headers matching `tarfile.USTAR_FORMAT` byte for\n  byte, `serde_json` pretty output post-escaped to `json.dumps(ensure_ascii=True)`, epoch from\n  `git log -1 --format=%at -- .` or the flag. Differential against the Python on 0.4.0 before\n  deletion: identical stdout (`sha256:3758e80b…`, archive `sha256:91fb5524…`, 880,640 bytes),\n  `diff -r` empty, modes identical, eleven refusal cases with identical exit code and text.\n- 21 packager tests ported (49 in the crate); failing-first on a timestamp-embedding stub\n  (`45 passed; 4 failed`) then green. Both Python files deleted; `gate.sh` drops the packager\n  test line (it runs under `cargo test --workspace`); gate 12 steps, passed.\n- Remaining: steps 4–6.\n\n\n## Progress — 2026-08-30, step 4\n\n- `crates/substrate-daemon/tests/runtime_vectors.rs` (1,730 lines, one `#[tokio::test]` mirroring\n  the Python's `main()` in order) replaces `scripts/check-runtime-vectors.py`, which is deleted.\n  Hand-written HTTP/1.1 over `tokio::net::UnixStream` and raw WebSocket frames; the lane switch is\n  `SUBSTRATE_VECTORS_CGROUP_ROOT` where the script took `--cgroup-root`, and an unset value keeps\n  the portable branch asserting `exec.sandbox-unavailable` 501 with the delegated cases absent,\n  never counted (invariant 3).\n- Zero new crates; `Cargo.lock` byte-identical. `nix` gains its test-only `signal` feature\n  (`crates/substrate-daemon/Cargo.toml:48-50`).\n- Differential against the Python before deletion: portable lane both `27 HTTP cases, startup\n  refusal, dual-daemon refusal`; delegated lane both `38 HTTP cases` under\n  `systemd-run --user -p Delegate=yes --scope`; two negative cases (`SUBSTRATE_ALLOW_UID` set,\n  non-delegated cgroup root) fail identically on both. Failing-first:\n  `not implemented: cases 2..27 are not ported yet` → `0 passed; 1 failed`.\n- Two deliberate improvements: the temp dir is forced to 0700 (Rust's `TempDir` is 0777 & ~umask\n  and the daemon refuses it), and the case counts are now asserted (`PORTABLE_CASES=27`,\n  `DELEGATED_CASES=38`) rather than printed.\n- The gate keeps no separate step: `cargo test --workspace --locked` runs it. Gate 11 steps,\n  passed; `cargo test --workspace`: 17 suites, 205 passed, 0 failed.\n- **The gate now runs no Python except the frozen bundles' own renderers and checkers.** Remaining\n  for this story: step 5 (`render-bundle 0.5.0` from `substrate-wire` types) and step 6.\n"},"command":"update-entity","target":"01MEM0000000000000049"},"payload":{"actor":"human:timo","at":1788043632743,"causation":"cmd-protocol-artifact-body-story-tooling-moves-to-cargo-xtask-1788043632743","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body","event_id":"story:tooling-moves-to-cargo-xtask@7#0~64f961251bf546ef","executor":null,"recorded_at":"2026-08-29T22:47:12Z"}}
{"entity":"story","version":1,"id":"tooling-moves-to-cargo-xtask","revision":7,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"https://github.com/beyond10x/substrate/actions/runs/33279445732","source":"GitHub Actions Gate workflow, PR #7 (step 4)","target":"01MEM0000000000000049"},"payload":{"actor":"human:timo","at":1788043793000,"causation":"cmd-evidence-story-tooling-moves-to-cargo-xtask-test_result-1788043793811","change":{"change":"evidence","kind":"test_result","reference":"https://github.com/beyond10x/substrate/actions/runs/33279445732","source":"GitHub Actions Gate workflow, PR #7 (step 4)"},"correlation":"protocol-artifact-evidence","event_id":"story:tooling-moves-to-cargo-xtask@7#0~78c1f85b9902f1a7","executor":null,"recorded_at":"2026-08-29T22:49:53Z"}}
{"entity":"story","version":1,"id":"ledger-rows-carry-the-declared-grant","revision":4,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000028","to":"proposed"},"payload":{"actor":"human:timo","at":1788045718552,"causation":"cmd-move-story-ledger-rows-carry-the-declared-grant-1788045718552","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:ledger-rows-carry-the-declared-grant@4#0~36bd28f03013dbfa","executor":null,"recorded_at":"2026-08-29T23:21:58Z"}}
{"entity":"story","version":1,"id":"sealed-secret-slots","revision":4,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000040","to":"proposed"},"payload":{"actor":"human:timo","at":1788045718596,"causation":"cmd-move-story-sealed-secret-slots-1788045718596","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:sealed-secret-slots@4#0~717b52c4816dc414","executor":null,"recorded_at":"2026-08-29T23:21:58Z"}}
{"entity":"story","version":1,"id":"destination-bound-egress","revision":5,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000019","to":"proposed"},"payload":{"actor":"human:timo","at":1788045718617,"causation":"cmd-move-story-destination-bound-egress-1788045718617","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:destination-bound-egress@5#0~ad09e0b292243592","executor":null,"recorded_at":"2026-08-29T23:21:58Z"}}
{"entity":"story","version":1,"id":"ledger-rows-carry-the-declared-grant","revision":4,"type":"aep.evidence.record/v1","from_state":"proposed","to_state":"proposed","changed":{},"args":{"command":"record-evidence","kind":"artifact","reference":"https://github.com/beyond10x/identity/blob/c1e3a07/.engineering/planning/story/delegated-context-signing-key.md","source":"identity story:delegated-context-signing-key (draft) — the ADR 0011 issuer question raised in the repo that must answer it","target":"01MEM0000000000000028"},"payload":{"actor":"human:timo","at":1788046362000,"causation":"cmd-evidence-story-ledger-rows-carry-the-declared-grant-artifact-1788046362321","change":{"change":"evidence","kind":"artifact","reference":"https://github.com/beyond10x/identity/blob/c1e3a07/.engineering/planning/story/delegated-context-signing-key.md","source":"identity story:delegated-context-signing-key (draft) — the ADR 0011 issuer question raised in the repo that must answer it"},"correlation":"protocol-artifact-evidence","event_id":"story:ledger-rows-carry-the-declared-grant@4#0~c865704d3abbce9d","executor":null,"recorded_at":"2026-08-29T23:32:42Z"}}
{"entity":"story","version":1,"id":"destination-bound-egress","revision":5,"type":"aep.evidence.record/v1","from_state":"proposed","to_state":"proposed","changed":{},"args":{"command":"record-evidence","kind":"verification","reference":"docs/design/10a-egress-mechanism-spike.md","source":"egress mechanism spike, run on 6.6.144-3-MANJARO at uid 1000 — mechanism (c) works, (a) refuted by EPERM","target":"01MEM0000000000000019"},"payload":{"actor":"human:timo","at":1788046728000,"causation":"cmd-evidence-story-destination-bound-egress-verification-1788046728051","change":{"change":"evidence","kind":"verification","reference":"docs/design/10a-egress-mechanism-spike.md","source":"egress mechanism spike, run on 6.6.144-3-MANJARO at uid 1000 — mechanism (c) works, (a) refuted by EPERM"},"correlation":"protocol-artifact-evidence","event_id":"story:destination-bound-egress@5#0~1ef57a729def0376","executor":null,"recorded_at":"2026-08-29T23:38:48Z"}}
{"entity":"story","version":1,"id":"tooling-moves-to-cargo-xtask","revision":7,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"verification","reference":"xtask/src/render.rs","source":"cargo xtask render-bundle 0.4.0 — diff -r against the frozen bundle empty, manifest sha256:3758e80b… matches the checked-in line; cargo test -p xtask 64 passed; scripts/gate.sh passed","target":"01MEM0000000000000049"},"payload":{"actor":"human:timo","at":1788047035000,"causation":"cmd-evidence-story-tooling-moves-to-cargo-xtask-verification-1788047035331","change":{"change":"evidence","kind":"verification","reference":"xtask/src/render.rs","source":"cargo xtask render-bundle 0.4.0 — diff -r against the frozen bundle empty, manifest sha256:3758e80b… matches the checked-in line; cargo test -p xtask 64 passed; scripts/gate.sh passed"},"correlation":"protocol-artifact-evidence","event_id":"story:tooling-moves-to-cargo-xtask@7#0~f49bd338ee5a842b","executor":null,"recorded_at":"2026-08-29T23:43:55Z"}}
{"entity":"story","version":1,"id":"tooling-moves-to-cargo-xtask","revision":7,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"static_analysis","reference":"xtask/src/render.rs:19-25","source":"renderer scope finding: 30 of 200 emitted files are computed; no schema shape is derivable from the wire types — schemars is not a dependency and the types are already ahead of 0.4.0","target":"01MEM0000000000000049"},"payload":{"actor":"human:timo","at":1788047035000,"causation":"cmd-evidence-story-tooling-moves-to-cargo-xtask-static_analysis-1788047035348","change":{"change":"evidence","kind":"static_analysis","reference":"xtask/src/render.rs:19-25","source":"renderer scope finding: 30 of 200 emitted files are computed; no schema shape is derivable from the wire types — schemars is not a dependency and the types are already ahead of 0.4.0"},"correlation":"protocol-artifact-evidence","event_id":"story:tooling-moves-to-cargo-xtask@7#0~63cecdbcfd9b5bbf","executor":null,"recorded_at":"2026-08-29T23:43:55Z"}}
{"entity":"story","version":1,"id":"sealed-secret-slots","revision":5,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000040","to":"active"},"payload":{"actor":"human:timo","at":1788047792151,"causation":"cmd-move-story-sealed-secret-slots-1788047792151","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:sealed-secret-slots@5#0~ee79c1853ef76f38","executor":null,"recorded_at":"2026-08-29T23:56:32Z"}}
{"entity":"story","version":1,"id":"tooling-moves-to-cargo-xtask","revision":8,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"body":"# Story: Anything that runs is Rust — the gate's Python moves to `cargo xtask`\n\n## Outcome\n\n`scripts/gate.sh` runs Rust: one `xtask` crate (clap) owns `check-links`, `check-adrs`,\n`check-toolchain`, `package-bundle`, `check-bundle` and the runtime-vector runner, and the next\nwire bundle (`0.5.0`) is rendered from `substrate-wire`'s types rather than from Python dicts. The\nfour frozen renderers and their checkers stay exactly as they are, as the reproducibility proof of\n`0.1.0`–`0.4.0` (invariant 6), and nothing new is written in Python.\n\n## Context\n\n`atlas/AGENTS.md` § *Language*, 2026-08-29: anything that runs is Rust; a new `.py` in a foundation\nrepository is an operator decision. Measured the same day: `scripts/*.py` is 23,916 lines beside\n30,768 lines of Rust; 18,300 of those are `render-contract-bundle{,-0.2.0,-0.3.0,-0.4.0}.py` and\n`check-contract-bundle{,-0.2.0,-0.3.0,-0.4.0}.py`, near-copies per version. The validator that does\nthe standards work is already Rust (`crates/substrate-contract-check`, `jsonschema = \"=0.49.9\"`;\n`scripts/contract_json_gate.py:195` shells out to it). The wire has two descriptions — the Rust\ntypes in `crates/substrate-wire` and the renderer's dicts — and only the vectors would notice them\ndrifting. Wave 1 of this store added ~1,200 more Python lines (`check-toolchain.py`,\n`package-contract-bundle.py` and its tests) by following the local convention; they are the first\nto move.\n\n## Acceptance\n\n`bash scripts/gate.sh` invokes no Python except the four frozen renderers' checkers, every moved\ncheck refuses the same inputs its Python predecessor refused (proven by the predecessor's own\nnegative cases run against the Rust replacement before the Python is deleted), `0.4.0` still\nverifies byte-clean, and `cargo xtask render-bundle 0.5.0` is the only renderer for the successor.\n\nEvidence that satisfies it, in order:\n\n1. `xtask/` crate in the workspace (`cargo xtask <verb>`, clap derive), `--locked` clean.\n2. `check-toolchain`, `check-links`, `check-adrs` moved first (smallest, no wire contact), each with\n   the Python negative cases ported as tests; the Python files deleted in the same commit.\n3. `package-bundle` moved with `test_package_contract_bundle.py`'s 21 cases ported; the 0.4.0\n   manifest digest `sha256:3758e80b…` reproduced by the Rust packager before the Python goes.\n4. `check-runtime-vectors` moved (the black-box Unix-socket HTTP runner, 1,194 lines) — last of the\n   gate steps, because it is the one with the most behaviour.\n5. `render-bundle 0.5.0` derives schemas from `substrate-wire` types (`schemars` or hand-rolled\n   emitters — decided in the story), with the `0.4.0` compatibility block computed, not typed.\n6. `AGENTS.md` § *The gate* and `README.md` § *Build, test, run* list the xtask verbs; the four\n   frozen renderers are documented as frozen proofs, not tooling.\n\n## Out of Scope\n\nRe-rendering `0.1.0`–`0.4.0` from Rust. They are frozen; their Python renderers are their proof.\n\n## Open Questions\n\nWhether `check-runtime-vectors` becomes an `xtask` verb or an integration test under\n`crates/substrate-daemon/tests/` (it drives a real daemon over a socket, which the daemon's tests\nalready do). Decides: operator. Default if nobody answers: **integration test** — one fewer\nbinary, and the gate already runs `cargo test`.\n\n**Resolved 2026-08-30: integration test.** No existing daemon test spawns the binary — `http.rs:29-34`,\n`websocket.rs:36-38`, `pipe_session.rs` and `contract_vectors.rs` all build `App::new(...)` in\nprocess — so there was no harness to reuse, but the black-box property survives:\n`env!(\"CARGO_BIN_EXE_substrate-daemon\")` lets a test in that package spawn the shipped binary over\na Unix socket. Nothing outside the gate invoked the script.\n\n## Progress — 2026-08-30, steps 1–2\n\n- `xtask/` (bin-only, clap derive; `anyhow`, `clap`, dev `tempfile` — no new crate, `Cargo.lock`\n  +9 lines, zero version bumps); `.cargo/config.toml` alias `cargo xtask`.\n- `check-toolchain --root`, `check-links`, `check-adrs` ported; the three Python files deleted in\n  the same change. Differential runs against the Python before deletion: check-links 19 cases +\n  whole repo byte-identical stdout/stderr; check-adrs 11 mutations of `adr/` 11/11 identical;\n  check-toolchain 12 mutations 12/12 identical. Two deliberate differences: the predecessor\n  monorepo escape hatch in check-links is gone (no ancestor has `scripts/check-monorepo.sh`), and\n  `[x](   )` is skipped where the Python raised `IndexError` (`check-links.py:33`).\n- Repo root at runtime is the nearest ancestor whose `Cargo.toml` has `[workspace]`\n  (`xtask/src/repo.rs`), not the binary's compile-time path.\n- Tests written failing-first (`2 passed; 26 failed` on `unimplemented!()` stubs) →\n  `cargo test -p xtask --locked`: 28 passed. `bash scripts/gate.sh`: passed, 13 steps.\n- `gate.sh:18,19,27`, `AGENTS.md` § The gate, `README.md` § Build, `STATUS.md` (three links to the\n  deleted files) updated. Remaining: steps 3–6 (packager, runtime vectors, `render-bundle 0.5.0`).\n\n## Progress — 2026-08-30, step 3\n\n- `cargo xtask package-bundle <version> --out <dir> [--force] [--source-date-epoch] [--contracts-root]`\n  (`xtask/src/package.rs`): hand-written ustar headers matching `tarfile.USTAR_FORMAT` byte for\n  byte, `serde_json` pretty output post-escaped to `json.dumps(ensure_ascii=True)`, epoch from\n  `git log -1 --format=%at -- .` or the flag. Differential against the Python on 0.4.0 before\n  deletion: identical stdout (`sha256:3758e80b…`, archive `sha256:91fb5524…`, 880,640 bytes),\n  `diff -r` empty, modes identical, eleven refusal cases with identical exit code and text.\n- 21 packager tests ported (49 in the crate); failing-first on a timestamp-embedding stub\n  (`45 passed; 4 failed`) then green. Both Python files deleted; `gate.sh` drops the packager\n  test line (it runs under `cargo test --workspace`); gate 12 steps, passed.\n- Remaining: steps 4–6.\n\n\n## Progress — 2026-08-30, step 4\n\n- `crates/substrate-daemon/tests/runtime_vectors.rs` (1,730 lines, one `#[tokio::test]` mirroring\n  the Python's `main()` in order) replaces `scripts/check-runtime-vectors.py`, which is deleted.\n  Hand-written HTTP/1.1 over `tokio::net::UnixStream` and raw WebSocket frames; the lane switch is\n  `SUBSTRATE_VECTORS_CGROUP_ROOT` where the script took `--cgroup-root`, and an unset value keeps\n  the portable branch asserting `exec.sandbox-unavailable` 501 with the delegated cases absent,\n  never counted (invariant 3).\n- Zero new crates; `Cargo.lock` byte-identical. `nix` gains its test-only `signal` feature\n  (`crates/substrate-daemon/Cargo.toml:48-50`).\n- Differential against the Python before deletion: portable lane both `27 HTTP cases, startup\n  refusal, dual-daemon refusal`; delegated lane both `38 HTTP cases` under\n  `systemd-run --user -p Delegate=yes --scope`; two negative cases (`SUBSTRATE_ALLOW_UID` set,\n  non-delegated cgroup root) fail identically on both. Failing-first:\n  `not implemented: cases 2..27 are not ported yet` → `0 passed; 1 failed`.\n- Two deliberate improvements: the temp dir is forced to 0700 (Rust's `TempDir` is 0777 & ~umask\n  and the daemon refuses it), and the case counts are now asserted (`PORTABLE_CASES=27`,\n  `DELEGATED_CASES=38`) rather than printed.\n- The gate keeps no separate step: `cargo test --workspace --locked` runs it. Gate 11 steps,\n  passed; `cargo test --workspace`: 17 suites, 205 passed, 0 failed.\n- **The gate now runs no Python except the frozen bundles' own renderers and checkers.** Remaining\n  for this story: step 5 (`render-bundle 0.5.0` from `substrate-wire` types) and step 6.\n\n## Progress — 2026-08-30, steps 5 and 6\n\n**Step 5 done, and step 5's own premise was wrong.** `cargo xtask render-bundle 0.4.0` reproduces\nthe frozen bundle byte for byte — `diff -r contracts/substrate-wire/0.4.0 <out>` empty, 200 files,\nand packaging the rendered tree yields the manifest digest already checked in,\n`sha256:3758e80bc39f1eb03b15c69410608c9ef1d2ba8095c7e707c6988dbb5894ab00`. A test holds that fixed\npoint (`the_rendered_tree_packages_to_the_released_manifest_digest`), so the renderer cannot drift\nfrom what shipped.\n\nThe story said \"derives schemas from `substrate-wire` types\". It cannot, and the implementation\nrecords that rather than pretending:\n\n- `schemars` is not a workspace dependency (`Cargo.toml:22-59`) and adding derives would mean\n  editing `crates/`, so nothing can reflect the types.\n- Even with reflection the types are **ahead** of 0.4.0: `ExecStartInput::read_only_roots`\n  (`crates/substrate-wire/src/lib.rs:761`) has no 0.4.0 schema at all —\n  `grep -rl read_only_roots contracts/substrate-wire/0.4.0/` → 0 hits — and\n  `FileEditInput`, `FilePatchInput`, `WorkspaceTree*` and `PathMoveInput` have no 0.4.0 input schema.\n- The constraints the schemas state are not on the types. `argv: Vec<String>`\n  (`crates/substrate-wire/src/lib.rs:757`) against a schema of `minLength 1 / maxLength 4096 /\n  maxItems 256`, whose literals live in `crates/substrate-daemon/src/app/operations.rs:245-248` and\n  `crates/substrate-host/src/process.rs:824-826`.\n\nWhat the wire crate does own is taken from it: canonical hashing for all 11 fixture cases,\ncross-checked against `canonical_request_hash_v2`, and 22 bounds constants bound through a `$wire`\nsplice. Of 200 emitted files (708,672 bytes), 30 are computed whole, 14 are authored with computed\nsplices, and 156 are authored verbatim.\n\nOne derivation is **lost**, and is documented as lost at `xtask/src/render.rs:19-25`: the three\nunions in `schemas/vector.json` encode Python dict insertion order (`exec.output-limit-bytes` before\n`exec.max-current`), which a sorted-key bundle records nowhere.\n\nAuthored source is `xtask/bundle-source/<version>/` — one file per emitted path under `documents/`,\nplus `routes.json`, `coverage.json`, `hash-cases.json`, `vector-order.json` and\n`executable-vectors.json`. Not `contracts/source/`: every directory under `contracts/` is a released\nbundle (invariant 6) and `scripts/contract_json_gate.py:301` classifies JSON beneath one and fails\nclosed (invariant 7). Rendering into `contracts/` is a named refusal, exit 1.\n\nFailing-first: `render`/`hash_case` stubbed to `unimplemented!()` gave `55 passed; 9 failed`.\nThen `cargo test -p xtask --locked` → `64 passed; 0 failed` (49 before). `bash scripts/gate.sh`\npassed, 11 steps. `git status --short contracts/` empty.\n\n**Step 6 done.** `AGENTS.md` § *The gate* and `README.md` § *Build, test, run* both carry the verb\ntable — `check-toolchain`, `check-links`, `check-adrs`, `package-bundle`, `render-bundle` — and both\nsay why the four frozen `render-contract-bundle*.py` / `check-contract-bundle*.py` pairs stay: they\nare the released bundles' reproducibility proof, and 0.4.0's `generator.name` points at one of them,\nso deleting it would break the bundle it generated.\n\n**Decision taken by default:** `render-bundle` gets no `gate.sh` step of its own. A test already\nasserts the 0.4.0 digest, and `cargo test --workspace --locked` is the gate's first step — the same\nreasoning that keeps `package-bundle` and the runtime-vector runner out of it.\n\n**Open, and the only thing left for this story:** the acceptance names\n`cargo xtask render-bundle 0.5.0` as the successor's only renderer. The verb exists and refuses\ncorrectly; 0.5.0 itself is cut by `story:sealed-secret-slots`, whose bundle is in progress. When it\nlands, this story is satisfied. For 0.5.0 the renderer needs `generator.name` to point at something\nhashable — it hashes a file or a tree — which is a further reason 0.4.0's Python renderer must\nsurvive.\n"},"args":{"changes":{"body":"# Story: Anything that runs is Rust — the gate's Python moves to `cargo xtask`\n\n## Outcome\n\n`scripts/gate.sh` runs Rust: one `xtask` crate (clap) owns `check-links`, `check-adrs`,\n`check-toolchain`, `package-bundle`, `check-bundle` and the runtime-vector runner, and the next\nwire bundle (`0.5.0`) is rendered from `substrate-wire`'s types rather than from Python dicts. The\nfour frozen renderers and their checkers stay exactly as they are, as the reproducibility proof of\n`0.1.0`–`0.4.0` (invariant 6), and nothing new is written in Python.\n\n## Context\n\n`atlas/AGENTS.md` § *Language*, 2026-08-29: anything that runs is Rust; a new `.py` in a foundation\nrepository is an operator decision. Measured the same day: `scripts/*.py` is 23,916 lines beside\n30,768 lines of Rust; 18,300 of those are `render-contract-bundle{,-0.2.0,-0.3.0,-0.4.0}.py` and\n`check-contract-bundle{,-0.2.0,-0.3.0,-0.4.0}.py`, near-copies per version. The validator that does\nthe standards work is already Rust (`crates/substrate-contract-check`, `jsonschema = \"=0.49.9\"`;\n`scripts/contract_json_gate.py:195` shells out to it). The wire has two descriptions — the Rust\ntypes in `crates/substrate-wire` and the renderer's dicts — and only the vectors would notice them\ndrifting. Wave 1 of this store added ~1,200 more Python lines (`check-toolchain.py`,\n`package-contract-bundle.py` and its tests) by following the local convention; they are the first\nto move.\n\n## Acceptance\n\n`bash scripts/gate.sh` invokes no Python except the four frozen renderers' checkers, every moved\ncheck refuses the same inputs its Python predecessor refused (proven by the predecessor's own\nnegative cases run against the Rust replacement before the Python is deleted), `0.4.0` still\nverifies byte-clean, and `cargo xtask render-bundle 0.5.0` is the only renderer for the successor.\n\nEvidence that satisfies it, in order:\n\n1. `xtask/` crate in the workspace (`cargo xtask <verb>`, clap derive), `--locked` clean.\n2. `check-toolchain`, `check-links`, `check-adrs` moved first (smallest, no wire contact), each with\n   the Python negative cases ported as tests; the Python files deleted in the same commit.\n3. `package-bundle` moved with `test_package_contract_bundle.py`'s 21 cases ported; the 0.4.0\n   manifest digest `sha256:3758e80b…` reproduced by the Rust packager before the Python goes.\n4. `check-runtime-vectors` moved (the black-box Unix-socket HTTP runner, 1,194 lines) — last of the\n   gate steps, because it is the one with the most behaviour.\n5. `render-bundle 0.5.0` derives schemas from `substrate-wire` types (`schemars` or hand-rolled\n   emitters — decided in the story), with the `0.4.0` compatibility block computed, not typed.\n6. `AGENTS.md` § *The gate* and `README.md` § *Build, test, run* list the xtask verbs; the four\n   frozen renderers are documented as frozen proofs, not tooling.\n\n## Out of Scope\n\nRe-rendering `0.1.0`–`0.4.0` from Rust. They are frozen; their Python renderers are their proof.\n\n## Open Questions\n\nWhether `check-runtime-vectors` becomes an `xtask` verb or an integration test under\n`crates/substrate-daemon/tests/` (it drives a real daemon over a socket, which the daemon's tests\nalready do). Decides: operator. Default if nobody answers: **integration test** — one fewer\nbinary, and the gate already runs `cargo test`.\n\n**Resolved 2026-08-30: integration test.** No existing daemon test spawns the binary — `http.rs:29-34`,\n`websocket.rs:36-38`, `pipe_session.rs` and `contract_vectors.rs` all build `App::new(...)` in\nprocess — so there was no harness to reuse, but the black-box property survives:\n`env!(\"CARGO_BIN_EXE_substrate-daemon\")` lets a test in that package spawn the shipped binary over\na Unix socket. Nothing outside the gate invoked the script.\n\n## Progress — 2026-08-30, steps 1–2\n\n- `xtask/` (bin-only, clap derive; `anyhow`, `clap`, dev `tempfile` — no new crate, `Cargo.lock`\n  +9 lines, zero version bumps); `.cargo/config.toml` alias `cargo xtask`.\n- `check-toolchain --root`, `check-links`, `check-adrs` ported; the three Python files deleted in\n  the same change. Differential runs against the Python before deletion: check-links 19 cases +\n  whole repo byte-identical stdout/stderr; check-adrs 11 mutations of `adr/` 11/11 identical;\n  check-toolchain 12 mutations 12/12 identical. Two deliberate differences: the predecessor\n  monorepo escape hatch in check-links is gone (no ancestor has `scripts/check-monorepo.sh`), and\n  `[x](   )` is skipped where the Python raised `IndexError` (`check-links.py:33`).\n- Repo root at runtime is the nearest ancestor whose `Cargo.toml` has `[workspace]`\n  (`xtask/src/repo.rs`), not the binary's compile-time path.\n- Tests written failing-first (`2 passed; 26 failed` on `unimplemented!()` stubs) →\n  `cargo test -p xtask --locked`: 28 passed. `bash scripts/gate.sh`: passed, 13 steps.\n- `gate.sh:18,19,27`, `AGENTS.md` § The gate, `README.md` § Build, `STATUS.md` (three links to the\n  deleted files) updated. Remaining: steps 3–6 (packager, runtime vectors, `render-bundle 0.5.0`).\n\n## Progress — 2026-08-30, step 3\n\n- `cargo xtask package-bundle <version> --out <dir> [--force] [--source-date-epoch] [--contracts-root]`\n  (`xtask/src/package.rs`): hand-written ustar headers matching `tarfile.USTAR_FORMAT` byte for\n  byte, `serde_json` pretty output post-escaped to `json.dumps(ensure_ascii=True)`, epoch from\n  `git log -1 --format=%at -- .` or the flag. Differential against the Python on 0.4.0 before\n  deletion: identical stdout (`sha256:3758e80b…`, archive `sha256:91fb5524…`, 880,640 bytes),\n  `diff -r` empty, modes identical, eleven refusal cases with identical exit code and text.\n- 21 packager tests ported (49 in the crate); failing-first on a timestamp-embedding stub\n  (`45 passed; 4 failed`) then green. Both Python files deleted; `gate.sh` drops the packager\n  test line (it runs under `cargo test --workspace`); gate 12 steps, passed.\n- Remaining: steps 4–6.\n\n\n## Progress — 2026-08-30, step 4\n\n- `crates/substrate-daemon/tests/runtime_vectors.rs` (1,730 lines, one `#[tokio::test]` mirroring\n  the Python's `main()` in order) replaces `scripts/check-runtime-vectors.py`, which is deleted.\n  Hand-written HTTP/1.1 over `tokio::net::UnixStream` and raw WebSocket frames; the lane switch is\n  `SUBSTRATE_VECTORS_CGROUP_ROOT` where the script took `--cgroup-root`, and an unset value keeps\n  the portable branch asserting `exec.sandbox-unavailable` 501 with the delegated cases absent,\n  never counted (invariant 3).\n- Zero new crates; `Cargo.lock` byte-identical. `nix` gains its test-only `signal` feature\n  (`crates/substrate-daemon/Cargo.toml:48-50`).\n- Differential against the Python before deletion: portable lane both `27 HTTP cases, startup\n  refusal, dual-daemon refusal`; delegated lane both `38 HTTP cases` under\n  `systemd-run --user -p Delegate=yes --scope`; two negative cases (`SUBSTRATE_ALLOW_UID` set,\n  non-delegated cgroup root) fail identically on both. Failing-first:\n  `not implemented: cases 2..27 are not ported yet` → `0 passed; 1 failed`.\n- Two deliberate improvements: the temp dir is forced to 0700 (Rust's `TempDir` is 0777 & ~umask\n  and the daemon refuses it), and the case counts are now asserted (`PORTABLE_CASES=27`,\n  `DELEGATED_CASES=38`) rather than printed.\n- The gate keeps no separate step: `cargo test --workspace --locked` runs it. Gate 11 steps,\n  passed; `cargo test --workspace`: 17 suites, 205 passed, 0 failed.\n- **The gate now runs no Python except the frozen bundles' own renderers and checkers.** Remaining\n  for this story: step 5 (`render-bundle 0.5.0` from `substrate-wire` types) and step 6.\n\n## Progress — 2026-08-30, steps 5 and 6\n\n**Step 5 done, and step 5's own premise was wrong.** `cargo xtask render-bundle 0.4.0` reproduces\nthe frozen bundle byte for byte — `diff -r contracts/substrate-wire/0.4.0 <out>` empty, 200 files,\nand packaging the rendered tree yields the manifest digest already checked in,\n`sha256:3758e80bc39f1eb03b15c69410608c9ef1d2ba8095c7e707c6988dbb5894ab00`. A test holds that fixed\npoint (`the_rendered_tree_packages_to_the_released_manifest_digest`), so the renderer cannot drift\nfrom what shipped.\n\nThe story said \"derives schemas from `substrate-wire` types\". It cannot, and the implementation\nrecords that rather than pretending:\n\n- `schemars` is not a workspace dependency (`Cargo.toml:22-59`) and adding derives would mean\n  editing `crates/`, so nothing can reflect the types.\n- Even with reflection the types are **ahead** of 0.4.0: `ExecStartInput::read_only_roots`\n  (`crates/substrate-wire/src/lib.rs:761`) has no 0.4.0 schema at all —\n  `grep -rl read_only_roots contracts/substrate-wire/0.4.0/` → 0 hits — and\n  `FileEditInput`, `FilePatchInput`, `WorkspaceTree*` and `PathMoveInput` have no 0.4.0 input schema.\n- The constraints the schemas state are not on the types. `argv: Vec<String>`\n  (`crates/substrate-wire/src/lib.rs:757`) against a schema of `minLength 1 / maxLength 4096 /\n  maxItems 256`, whose literals live in `crates/substrate-daemon/src/app/operations.rs:245-248` and\n  `crates/substrate-host/src/process.rs:824-826`.\n\nWhat the wire crate does own is taken from it: canonical hashing for all 11 fixture cases,\ncross-checked against `canonical_request_hash_v2`, and 22 bounds constants bound through a `$wire`\nsplice. Of 200 emitted files (708,672 bytes), 30 are computed whole, 14 are authored with computed\nsplices, and 156 are authored verbatim.\n\nOne derivation is **lost**, and is documented as lost at `xtask/src/render.rs:19-25`: the three\nunions in `schemas/vector.json` encode Python dict insertion order (`exec.output-limit-bytes` before\n`exec.max-current`), which a sorted-key bundle records nowhere.\n\nAuthored source is `xtask/bundle-source/<version>/` — one file per emitted path under `documents/`,\nplus `routes.json`, `coverage.json`, `hash-cases.json`, `vector-order.json` and\n`executable-vectors.json`. Not `contracts/source/`: every directory under `contracts/` is a released\nbundle (invariant 6) and `scripts/contract_json_gate.py:301` classifies JSON beneath one and fails\nclosed (invariant 7). Rendering into `contracts/` is a named refusal, exit 1.\n\nFailing-first: `render`/`hash_case` stubbed to `unimplemented!()` gave `55 passed; 9 failed`.\nThen `cargo test -p xtask --locked` → `64 passed; 0 failed` (49 before). `bash scripts/gate.sh`\npassed, 11 steps. `git status --short contracts/` empty.\n\n**Step 6 done.** `AGENTS.md` § *The gate* and `README.md` § *Build, test, run* both carry the verb\ntable — `check-toolchain`, `check-links`, `check-adrs`, `package-bundle`, `render-bundle` — and both\nsay why the four frozen `render-contract-bundle*.py` / `check-contract-bundle*.py` pairs stay: they\nare the released bundles' reproducibility proof, and 0.4.0's `generator.name` points at one of them,\nso deleting it would break the bundle it generated.\n\n**Decision taken by default:** `render-bundle` gets no `gate.sh` step of its own. A test already\nasserts the 0.4.0 digest, and `cargo test --workspace --locked` is the gate's first step — the same\nreasoning that keeps `package-bundle` and the runtime-vector runner out of it.\n\n**Open, and the only thing left for this story:** the acceptance names\n`cargo xtask render-bundle 0.5.0` as the successor's only renderer. The verb exists and refuses\ncorrectly; 0.5.0 itself is cut by `story:sealed-secret-slots`, whose bundle is in progress. When it\nlands, this story is satisfied. For 0.5.0 the renderer needs `generator.name` to point at something\nhashable — it hashes a file or a tree — which is a further reason 0.4.0's Python renderer must\nsurvive.\n"},"command":"update-entity","target":"01MEM0000000000000049"},"payload":{"actor":"human:timo","at":1788047887920,"causation":"cmd-protocol-artifact-body-story-tooling-moves-to-cargo-xtask-1788047887920","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body","event_id":"story:tooling-moves-to-cargo-xtask@8#0~ce8014f40ce51bbe","executor":null,"recorded_at":"2026-08-29T23:58:07Z"}}
{"entity":"story","version":1,"id":"sealed-secret-slots","revision":6,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"body":"# Story: Sealed secret slots reach a child only as a memfd descriptor\n\n## Outcome\n\nA run can use a daemon-configured secret without the value ever appearing in argv, environment,\nlogs, events, the ledger request hash or an error body — and design 05's reason for refusing a\nlive vendor harness is removed on the secret side.\n\n## Context\n\n`docs/design/06-authentication-secrets-and-trust.md` § *V1 decisions* 3 fixes the mechanism: a\nsealed Linux `memfd` at a declared child descriptor; only the slot→descriptor mapping in the shaped\nenvironment; acquisition after every admission and dispatch-time check; the daemon closes its copy\nimmediately after spawn; a driver that cannot prove sealing, isolation and cleanup reports the\ncapability absent. Design 04 § 7 (line 79): errors may name a slot, never the value.\n\n## Acceptance\n\nThe delegated lane proves the child reads the value from the declared descriptor and the value\nbytes appear nowhere in captured argv, environment, stdout, stderr, event, ledger or diagnostic\nbytes; the portable lane proves the typed refusal.\n\nEvidence that satisfies it, in order:\n\n1. `adr/0012-secret-slots-are-sealed-memfds.md`: slot naming, descriptor declaration, the seal set\n   (`F_SEAL_WRITE|SHRINK|GROW|SEAL`), refusal classes.\n2. A successor bundle adds `secret_slots` on exec and session start, the capability fact\n   `secrets.slots`, and the refusal class; earlier bundle bytes unchanged.\n3. Failing-first tests: `secret_slot_value_absent_from_argv_env_events_and_ledger`,\n   `secret_slot_memfd_is_sealed` (child `fcntl(F_GET_SEALS)` equals the declared set),\n   `secret_slot_refused_when_sealing_unavailable`, `daemon_closes_its_copy_after_spawn`\n   (`/proc/<daemon>/fd` holds no memfd after spawn).\n4. The ledger request hash covers slot **names** only: two requests differing only in slot value\n   hash identically.\n\n## Out of Scope\n\nBrokered artifact secrets (design 06 decision 4) and destination-bound egress.\n\n## Design draft — 2026-08-30\n\n`docs/design/11-sealed-secret-slots.md` (proposed), with `adr/0012` text ready to accept. Verified\nat runtime, not inferred: bubblewrap 0.11.2 passes an inherited descriptor to the child at the\nsame number; a sealed memfd read back inside the sandbox reports `F_GET_SEALS == 0xf` and\n`pwrite → EPERM`. `libc` already exports `memfd_create` and the seal constants — no Cargo change.\nToday's `close_range` (`process.rs:348-377`) would close a slot fd; the draft generalises it to one\nrange per gap. Mapping variable `SUBSTRATE_SECRET_SLOTS=name=fd,…`; `MFD_CLOEXEC` mandatory.\n\n## Progress — 2026-08-30, steps 2-4 done\n\nGate green (`bash scripts/gate.sh`, exit 0), nothing under `contracts/` disturbed:\n`git status --short contracts/` shows only `?? contracts/substrate-wire/0.5.0/`, and\n`cargo xtask render-bundle 0.4.0` still reproduces the frozen tree byte for byte (`diff -r` empty,\nmanifest `sha256:002337bd…`).\n\n**Step 2 — the successor bundle.** `contracts/substrate-wire/0.5.0/`, 206 files, adding\n`secret_slots` on exec and session start, the capability fact `secrets.slots` and the refusal class.\nVerified as a fixed point of its own authored source: `cargo xtask check-bundle 0.5.0` →\n`contract bundle 0.5.0 verified: 206 files, fixed point of xtask/bundle-source/0.5.0`.\n\n**Step 3 — the four named tests, failing first.** Recorded before implementation:\nseals `left: -1 right: 15`; child digest `e3b0c44…` (empty read) against `fffca559…`;\n`/proc/self/fd still holds a slot memfd after spawn`; retained descriptors `[0,1,2,5]` against\n`[0,1,2,5,7]`. All five `secrets::tests::*` pass now, plus six `secret_slot_tests::*` in the wire\ncrate.\n\n**Step 4 — the request hash covers names only.**\n`secrets::tests::ledger_request_hash_covers_slot_names_only` passes: two requests differing only in\na slot's value hash identically.\n\n**A real bug the failing-first probe caught, not a test artefact.** `write_all` left the memfd\noffset at EOF, so a child's `read(fd)` returned 0 bytes — and `/proc/self/fd/N` masked it, because\nopening through `/proc` starts at offset 0. Fixed with `lseek(…, 0, SEEK_SET)` before sealing, with\na second child assertion guarding it. A daemon that shipped without this would have handed every\nchild an empty secret and reported success.\n\n**ADR 0012 is contradicted by the code in one place, and the code is right.** Design § 5 wants\n`secrets.slots` *absent* when orphan reconciliation fails. That state is unreachable: `probe()` runs\nbefore `ProcessRuntime::new` (`crates/substrate-host/src/lib.rs:446`), and a failed reconciliation\naborts `HostDriver::open`, so the daemon never starts to report anything. Recorded in `probe.rs`.\nAbsent-is-weaker, so nothing about this is optimistic.\n\n**Defaults taken.**\n\n- ADR § 9 named `scripts/check-contract-bundle-0.5.0.py`. The org language rule won:\n  `cargo xtask check-bundle <version>` instead, which re-renders from the authored source and\n  compares bytes — strictly stronger than a hand-written checker, because a released tree that has\n  stopped being the fixed point of its own source fails whatever else still looks well-formed.\n  Noted in `docs/design/11-sealed-secret-slots.md` § 9.\n- Invariant 4's `driver_port.rs` guard refused a re-export of `substrate_host::SecretSlot`, so the\n  daemon declares its own type and converts at the composition root.\n- `--secret-slot` validates the file at startup rather than at first use.\n- **`CONTRACT_BUNDLE` stays `substrate-wire/0.4.0`** (`crates/substrate-daemon/src/app.rs:3`). A\n  bundle exists to be pinned by a consumer before the server claims it; moving the\n  `x-b10x-contract` header is its own change with its own clients to notify. Same posture\n  `read_only_roots` already has — in the wire crate, in no bundle.\n\nTwo crate edges added, both already in the workspace: `zeroize` for `substrate-host`, `jsonschema`\nfor `xtask`. No new external dependency.\n\n**Not done.** No end-to-end daemon run with a live slot — there is no delegated cgroup on this host,\nso the four named tests are portable-lane, driving the real acquire/place/close code without a\nsandboxed child. `read_only_roots` is still absent from every bundle including `0.5.0`.\n"},"args":{"changes":{"body":"# Story: Sealed secret slots reach a child only as a memfd descriptor\n\n## Outcome\n\nA run can use a daemon-configured secret without the value ever appearing in argv, environment,\nlogs, events, the ledger request hash or an error body — and design 05's reason for refusing a\nlive vendor harness is removed on the secret side.\n\n## Context\n\n`docs/design/06-authentication-secrets-and-trust.md` § *V1 decisions* 3 fixes the mechanism: a\nsealed Linux `memfd` at a declared child descriptor; only the slot→descriptor mapping in the shaped\nenvironment; acquisition after every admission and dispatch-time check; the daemon closes its copy\nimmediately after spawn; a driver that cannot prove sealing, isolation and cleanup reports the\ncapability absent. Design 04 § 7 (line 79): errors may name a slot, never the value.\n\n## Acceptance\n\nThe delegated lane proves the child reads the value from the declared descriptor and the value\nbytes appear nowhere in captured argv, environment, stdout, stderr, event, ledger or diagnostic\nbytes; the portable lane proves the typed refusal.\n\nEvidence that satisfies it, in order:\n\n1. `adr/0012-secret-slots-are-sealed-memfds.md`: slot naming, descriptor declaration, the seal set\n   (`F_SEAL_WRITE|SHRINK|GROW|SEAL`), refusal classes.\n2. A successor bundle adds `secret_slots` on exec and session start, the capability fact\n   `secrets.slots`, and the refusal class; earlier bundle bytes unchanged.\n3. Failing-first tests: `secret_slot_value_absent_from_argv_env_events_and_ledger`,\n   `secret_slot_memfd_is_sealed` (child `fcntl(F_GET_SEALS)` equals the declared set),\n   `secret_slot_refused_when_sealing_unavailable`, `daemon_closes_its_copy_after_spawn`\n   (`/proc/<daemon>/fd` holds no memfd after spawn).\n4. The ledger request hash covers slot **names** only: two requests differing only in slot value\n   hash identically.\n\n## Out of Scope\n\nBrokered artifact secrets (design 06 decision 4) and destination-bound egress.\n\n## Design draft — 2026-08-30\n\n`docs/design/11-sealed-secret-slots.md` (proposed), with `adr/0012` text ready to accept. Verified\nat runtime, not inferred: bubblewrap 0.11.2 passes an inherited descriptor to the child at the\nsame number; a sealed memfd read back inside the sandbox reports `F_GET_SEALS == 0xf` and\n`pwrite → EPERM`. `libc` already exports `memfd_create` and the seal constants — no Cargo change.\nToday's `close_range` (`process.rs:348-377`) would close a slot fd; the draft generalises it to one\nrange per gap. Mapping variable `SUBSTRATE_SECRET_SLOTS=name=fd,…`; `MFD_CLOEXEC` mandatory.\n\n## Progress — 2026-08-30, steps 2-4 done\n\nGate green (`bash scripts/gate.sh`, exit 0), nothing under `contracts/` disturbed:\n`git status --short contracts/` shows only `?? contracts/substrate-wire/0.5.0/`, and\n`cargo xtask render-bundle 0.4.0` still reproduces the frozen tree byte for byte (`diff -r` empty,\nmanifest `sha256:002337bd…`).\n\n**Step 2 — the successor bundle.** `contracts/substrate-wire/0.5.0/`, 206 files, adding\n`secret_slots` on exec and session start, the capability fact `secrets.slots` and the refusal class.\nVerified as a fixed point of its own authored source: `cargo xtask check-bundle 0.5.0` →\n`contract bundle 0.5.0 verified: 206 files, fixed point of xtask/bundle-source/0.5.0`.\n\n**Step 3 — the four named tests, failing first.** Recorded before implementation:\nseals `left: -1 right: 15`; child digest `e3b0c44…` (empty read) against `fffca559…`;\n`/proc/self/fd still holds a slot memfd after spawn`; retained descriptors `[0,1,2,5]` against\n`[0,1,2,5,7]`. All five `secrets::tests::*` pass now, plus six `secret_slot_tests::*` in the wire\ncrate.\n\n**Step 4 — the request hash covers names only.**\n`secrets::tests::ledger_request_hash_covers_slot_names_only` passes: two requests differing only in\na slot's value hash identically.\n\n**A real bug the failing-first probe caught, not a test artefact.** `write_all` left the memfd\noffset at EOF, so a child's `read(fd)` returned 0 bytes — and `/proc/self/fd/N` masked it, because\nopening through `/proc` starts at offset 0. Fixed with `lseek(…, 0, SEEK_SET)` before sealing, with\na second child assertion guarding it. A daemon that shipped without this would have handed every\nchild an empty secret and reported success.\n\n**ADR 0012 is contradicted by the code in one place, and the code is right.** Design § 5 wants\n`secrets.slots` *absent* when orphan reconciliation fails. That state is unreachable: `probe()` runs\nbefore `ProcessRuntime::new` (`crates/substrate-host/src/lib.rs:446`), and a failed reconciliation\naborts `HostDriver::open`, so the daemon never starts to report anything. Recorded in `probe.rs`.\nAbsent-is-weaker, so nothing about this is optimistic.\n\n**Defaults taken.**\n\n- ADR § 9 named `scripts/check-contract-bundle-0.5.0.py`. The org language rule won:\n  `cargo xtask check-bundle <version>` instead, which re-renders from the authored source and\n  compares bytes — strictly stronger than a hand-written checker, because a released tree that has\n  stopped being the fixed point of its own source fails whatever else still looks well-formed.\n  Noted in `docs/design/11-sealed-secret-slots.md` § 9.\n- Invariant 4's `driver_port.rs` guard refused a re-export of `substrate_host::SecretSlot`, so the\n  daemon declares its own type and converts at the composition root.\n- `--secret-slot` validates the file at startup rather than at first use.\n- **`CONTRACT_BUNDLE` stays `substrate-wire/0.4.0`** (`crates/substrate-daemon/src/app.rs:3`). A\n  bundle exists to be pinned by a consumer before the server claims it; moving the\n  `x-b10x-contract` header is its own change with its own clients to notify. Same posture\n  `read_only_roots` already has — in the wire crate, in no bundle.\n\nTwo crate edges added, both already in the workspace: `zeroize` for `substrate-host`, `jsonschema`\nfor `xtask`. No new external dependency.\n\n**Not done.** No end-to-end daemon run with a live slot — there is no delegated cgroup on this host,\nso the four named tests are portable-lane, driving the real acquire/place/close code without a\nsandboxed child. `read_only_roots` is still absent from every bundle including `0.5.0`.\n"},"command":"update-entity","target":"01MEM0000000000000040"},"payload":{"actor":"human:timo","at":1788050216490,"causation":"cmd-protocol-artifact-body-story-sealed-secret-slots-1788050216490","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body","event_id":"story:sealed-secret-slots@6#0~6fb691134fb9e196","executor":null,"recorded_at":"2026-08-30T00:36:56Z"}}
{"entity":"story","version":1,"id":"sealed-secret-slots","revision":6,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"crates/substrate-host/src/secrets.rs","source":"bash scripts/gate.sh exit 0; cargo xtask check-bundle 0.5.0 verified 206 files as the fixed point of its authored source; 5 secrets::tests + 6 secret_slot_tests pass, all recorded failing first","target":"01MEM0000000000000040"},"payload":{"actor":"human:timo","at":1788050216000,"causation":"cmd-evidence-story-sealed-secret-slots-test_result-1788050216512","change":{"change":"evidence","kind":"test_result","reference":"crates/substrate-host/src/secrets.rs","source":"bash scripts/gate.sh exit 0; cargo xtask check-bundle 0.5.0 verified 206 files as the fixed point of its authored source; 5 secrets::tests + 6 secret_slot_tests pass, all recorded failing first"},"correlation":"protocol-artifact-evidence","event_id":"story:sealed-secret-slots@6#0~7527de508fca165a","executor":null,"recorded_at":"2026-08-30T00:36:56Z"}}
{"entity":"story","version":1,"id":"tooling-moves-to-cargo-xtask","revision":9,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"body":"# Story: Anything that runs is Rust — the gate's Python moves to `cargo xtask`\n\n## Outcome\n\n`scripts/gate.sh` runs Rust: one `xtask` crate (clap) owns `check-links`, `check-adrs`,\n`check-toolchain`, `package-bundle`, `check-bundle` and the runtime-vector runner, and the next\nwire bundle (`0.5.0`) is rendered from `substrate-wire`'s types rather than from Python dicts. The\nfour frozen renderers and their checkers stay exactly as they are, as the reproducibility proof of\n`0.1.0`–`0.4.0` (invariant 6), and nothing new is written in Python.\n\n## Context\n\n`atlas/AGENTS.md` § *Language*, 2026-08-29: anything that runs is Rust; a new `.py` in a foundation\nrepository is an operator decision. Measured the same day: `scripts/*.py` is 23,916 lines beside\n30,768 lines of Rust; 18,300 of those are `render-contract-bundle{,-0.2.0,-0.3.0,-0.4.0}.py` and\n`check-contract-bundle{,-0.2.0,-0.3.0,-0.4.0}.py`, near-copies per version. The validator that does\nthe standards work is already Rust (`crates/substrate-contract-check`, `jsonschema = \"=0.49.9\"`;\n`scripts/contract_json_gate.py:195` shells out to it). The wire has two descriptions — the Rust\ntypes in `crates/substrate-wire` and the renderer's dicts — and only the vectors would notice them\ndrifting. Wave 1 of this store added ~1,200 more Python lines (`check-toolchain.py`,\n`package-contract-bundle.py` and its tests) by following the local convention; they are the first\nto move.\n\n## Acceptance\n\n`bash scripts/gate.sh` invokes no Python except the four frozen renderers' checkers, every moved\ncheck refuses the same inputs its Python predecessor refused (proven by the predecessor's own\nnegative cases run against the Rust replacement before the Python is deleted), `0.4.0` still\nverifies byte-clean, and `cargo xtask render-bundle 0.5.0` is the only renderer for the successor.\n\nEvidence that satisfies it, in order:\n\n1. `xtask/` crate in the workspace (`cargo xtask <verb>`, clap derive), `--locked` clean.\n2. `check-toolchain`, `check-links`, `check-adrs` moved first (smallest, no wire contact), each with\n   the Python negative cases ported as tests; the Python files deleted in the same commit.\n3. `package-bundle` moved with `test_package_contract_bundle.py`'s 21 cases ported; the 0.4.0\n   manifest digest `sha256:3758e80b…` reproduced by the Rust packager before the Python goes.\n4. `check-runtime-vectors` moved (the black-box Unix-socket HTTP runner, 1,194 lines) — last of the\n   gate steps, because it is the one with the most behaviour.\n5. `render-bundle 0.5.0` derives schemas from `substrate-wire` types (`schemars` or hand-rolled\n   emitters — decided in the story), with the `0.4.0` compatibility block computed, not typed.\n6. `AGENTS.md` § *The gate* and `README.md` § *Build, test, run* list the xtask verbs; the four\n   frozen renderers are documented as frozen proofs, not tooling.\n\n## Out of Scope\n\nRe-rendering `0.1.0`–`0.4.0` from Rust. They are frozen; their Python renderers are their proof.\n\n## Open Questions\n\nWhether `check-runtime-vectors` becomes an `xtask` verb or an integration test under\n`crates/substrate-daemon/tests/` (it drives a real daemon over a socket, which the daemon's tests\nalready do). Decides: operator. Default if nobody answers: **integration test** — one fewer\nbinary, and the gate already runs `cargo test`.\n\n**Resolved 2026-08-30: integration test.** No existing daemon test spawns the binary — `http.rs:29-34`,\n`websocket.rs:36-38`, `pipe_session.rs` and `contract_vectors.rs` all build `App::new(...)` in\nprocess — so there was no harness to reuse, but the black-box property survives:\n`env!(\"CARGO_BIN_EXE_substrate-daemon\")` lets a test in that package spawn the shipped binary over\na Unix socket. Nothing outside the gate invoked the script.\n\n## Progress — 2026-08-30, steps 1–2\n\n- `xtask/` (bin-only, clap derive; `anyhow`, `clap`, dev `tempfile` — no new crate, `Cargo.lock`\n  +9 lines, zero version bumps); `.cargo/config.toml` alias `cargo xtask`.\n- `check-toolchain --root`, `check-links`, `check-adrs` ported; the three Python files deleted in\n  the same change. Differential runs against the Python before deletion: check-links 19 cases +\n  whole repo byte-identical stdout/stderr; check-adrs 11 mutations of `adr/` 11/11 identical;\n  check-toolchain 12 mutations 12/12 identical. Two deliberate differences: the predecessor\n  monorepo escape hatch in check-links is gone (no ancestor has `scripts/check-monorepo.sh`), and\n  `[x](   )` is skipped where the Python raised `IndexError` (`check-links.py:33`).\n- Repo root at runtime is the nearest ancestor whose `Cargo.toml` has `[workspace]`\n  (`xtask/src/repo.rs`), not the binary's compile-time path.\n- Tests written failing-first (`2 passed; 26 failed` on `unimplemented!()` stubs) →\n  `cargo test -p xtask --locked`: 28 passed. `bash scripts/gate.sh`: passed, 13 steps.\n- `gate.sh:18,19,27`, `AGENTS.md` § The gate, `README.md` § Build, `STATUS.md` (three links to the\n  deleted files) updated. Remaining: steps 3–6 (packager, runtime vectors, `render-bundle 0.5.0`).\n\n## Progress — 2026-08-30, step 3\n\n- `cargo xtask package-bundle <version> --out <dir> [--force] [--source-date-epoch] [--contracts-root]`\n  (`xtask/src/package.rs`): hand-written ustar headers matching `tarfile.USTAR_FORMAT` byte for\n  byte, `serde_json` pretty output post-escaped to `json.dumps(ensure_ascii=True)`, epoch from\n  `git log -1 --format=%at -- .` or the flag. Differential against the Python on 0.4.0 before\n  deletion: identical stdout (`sha256:3758e80b…`, archive `sha256:91fb5524…`, 880,640 bytes),\n  `diff -r` empty, modes identical, eleven refusal cases with identical exit code and text.\n- 21 packager tests ported (49 in the crate); failing-first on a timestamp-embedding stub\n  (`45 passed; 4 failed`) then green. Both Python files deleted; `gate.sh` drops the packager\n  test line (it runs under `cargo test --workspace`); gate 12 steps, passed.\n- Remaining: steps 4–6.\n\n\n## Progress — 2026-08-30, step 4\n\n- `crates/substrate-daemon/tests/runtime_vectors.rs` (1,730 lines, one `#[tokio::test]` mirroring\n  the Python's `main()` in order) replaces `scripts/check-runtime-vectors.py`, which is deleted.\n  Hand-written HTTP/1.1 over `tokio::net::UnixStream` and raw WebSocket frames; the lane switch is\n  `SUBSTRATE_VECTORS_CGROUP_ROOT` where the script took `--cgroup-root`, and an unset value keeps\n  the portable branch asserting `exec.sandbox-unavailable` 501 with the delegated cases absent,\n  never counted (invariant 3).\n- Zero new crates; `Cargo.lock` byte-identical. `nix` gains its test-only `signal` feature\n  (`crates/substrate-daemon/Cargo.toml:48-50`).\n- Differential against the Python before deletion: portable lane both `27 HTTP cases, startup\n  refusal, dual-daemon refusal`; delegated lane both `38 HTTP cases` under\n  `systemd-run --user -p Delegate=yes --scope`; two negative cases (`SUBSTRATE_ALLOW_UID` set,\n  non-delegated cgroup root) fail identically on both. Failing-first:\n  `not implemented: cases 2..27 are not ported yet` → `0 passed; 1 failed`.\n- Two deliberate improvements: the temp dir is forced to 0700 (Rust's `TempDir` is 0777 & ~umask\n  and the daemon refuses it), and the case counts are now asserted (`PORTABLE_CASES=27`,\n  `DELEGATED_CASES=38`) rather than printed.\n- The gate keeps no separate step: `cargo test --workspace --locked` runs it. Gate 11 steps,\n  passed; `cargo test --workspace`: 17 suites, 205 passed, 0 failed.\n- **The gate now runs no Python except the frozen bundles' own renderers and checkers.** Remaining\n  for this story: step 5 (`render-bundle 0.5.0` from `substrate-wire` types) and step 6.\n\n## Progress — 2026-08-30, steps 5 and 6\n\n**Step 5 done, and step 5's own premise was wrong.** `cargo xtask render-bundle 0.4.0` reproduces\nthe frozen bundle byte for byte — `diff -r contracts/substrate-wire/0.4.0 <out>` empty, 200 files,\nand packaging the rendered tree yields the manifest digest already checked in,\n`sha256:3758e80bc39f1eb03b15c69410608c9ef1d2ba8095c7e707c6988dbb5894ab00`. A test holds that fixed\npoint (`the_rendered_tree_packages_to_the_released_manifest_digest`), so the renderer cannot drift\nfrom what shipped.\n\nThe story said \"derives schemas from `substrate-wire` types\". It cannot, and the implementation\nrecords that rather than pretending:\n\n- `schemars` is not a workspace dependency (`Cargo.toml:22-59`) and adding derives would mean\n  editing `crates/`, so nothing can reflect the types.\n- Even with reflection the types are **ahead** of 0.4.0: `ExecStartInput::read_only_roots`\n  (`crates/substrate-wire/src/lib.rs:761`) has no 0.4.0 schema at all —\n  `grep -rl read_only_roots contracts/substrate-wire/0.4.0/` → 0 hits — and\n  `FileEditInput`, `FilePatchInput`, `WorkspaceTree*` and `PathMoveInput` have no 0.4.0 input schema.\n- The constraints the schemas state are not on the types. `argv: Vec<String>`\n  (`crates/substrate-wire/src/lib.rs:757`) against a schema of `minLength 1 / maxLength 4096 /\n  maxItems 256`, whose literals live in `crates/substrate-daemon/src/app/operations.rs:245-248` and\n  `crates/substrate-host/src/process.rs:824-826`.\n\nWhat the wire crate does own is taken from it: canonical hashing for all 11 fixture cases,\ncross-checked against `canonical_request_hash_v2`, and 22 bounds constants bound through a `$wire`\nsplice. Of 200 emitted files (708,672 bytes), 30 are computed whole, 14 are authored with computed\nsplices, and 156 are authored verbatim.\n\nOne derivation is **lost**, and is documented as lost at `xtask/src/render.rs:19-25`: the three\nunions in `schemas/vector.json` encode Python dict insertion order (`exec.output-limit-bytes` before\n`exec.max-current`), which a sorted-key bundle records nowhere.\n\nAuthored source is `xtask/bundle-source/<version>/` — one file per emitted path under `documents/`,\nplus `routes.json`, `coverage.json`, `hash-cases.json`, `vector-order.json` and\n`executable-vectors.json`. Not `contracts/source/`: every directory under `contracts/` is a released\nbundle (invariant 6) and `scripts/contract_json_gate.py:301` classifies JSON beneath one and fails\nclosed (invariant 7). Rendering into `contracts/` is a named refusal, exit 1.\n\nFailing-first: `render`/`hash_case` stubbed to `unimplemented!()` gave `55 passed; 9 failed`.\nThen `cargo test -p xtask --locked` → `64 passed; 0 failed` (49 before). `bash scripts/gate.sh`\npassed, 11 steps. `git status --short contracts/` empty.\n\n**Step 6 done.** `AGENTS.md` § *The gate* and `README.md` § *Build, test, run* both carry the verb\ntable — `check-toolchain`, `check-links`, `check-adrs`, `package-bundle`, `render-bundle` — and both\nsay why the four frozen `render-contract-bundle*.py` / `check-contract-bundle*.py` pairs stay: they\nare the released bundles' reproducibility proof, and 0.4.0's `generator.name` points at one of them,\nso deleting it would break the bundle it generated.\n\n**Decision taken by default:** `render-bundle` gets no `gate.sh` step of its own. A test already\nasserts the 0.4.0 digest, and `cargo test --workspace --locked` is the gate's first step — the same\nreasoning that keeps `package-bundle` and the runtime-vector runner out of it.\n\n**0.5.0 is cut, so that clause is closed.** `story:sealed-secret-slots` rendered\n`contracts/substrate-wire/0.5.0/` (206 files) with `cargo xtask render-bundle`, and no Python\nrenderer was involved or written. It also replaced the ADR's proposed\n`scripts/check-contract-bundle-0.5.0.py` with a new verb, `cargo xtask check-bundle <version>`,\nwhich re-renders from the authored source and compares bytes — strictly stronger than a\nhand-written checker.\n\n## What is actually left — 2026-08-30\n\n**576 lines, and they are not the frozen renderers.** The acceptance says the gate invokes no Python\nexcept the four frozen renderers' checkers. It still invokes one more:\n\n    scripts/gate.sh:28  run python3 scripts/test_contract_json_gate.py\n\n`scripts/contract_json_gate.py` (377 lines) and its self-test (199) are shared live machinery, not a\nper-version frozen artefact — `:301` classifies JSON under a bundle directory and fails closed, and\n`:195` already shells out to `crates/substrate-contract-check`, which is Rust. Porting it is the\nlast step of this story, and it is the one place a new bundle's JSON is admitted or rejected, so it\nwants the same differential proof every other move got: the predecessor's negative cases run against\nthe Rust replacement before the Python is deleted.\n\n`scripts/check-bot-files.py` (39 lines) is not in `scripts/gate.sh` and is out of this story's\nacceptance; name it separately if it should move.\n\nOf the 21,298 Python lines left, 20,683 are the four frozen renderer/checker pairs, which stay.\n"},"args":{"changes":{"body":"# Story: Anything that runs is Rust — the gate's Python moves to `cargo xtask`\n\n## Outcome\n\n`scripts/gate.sh` runs Rust: one `xtask` crate (clap) owns `check-links`, `check-adrs`,\n`check-toolchain`, `package-bundle`, `check-bundle` and the runtime-vector runner, and the next\nwire bundle (`0.5.0`) is rendered from `substrate-wire`'s types rather than from Python dicts. The\nfour frozen renderers and their checkers stay exactly as they are, as the reproducibility proof of\n`0.1.0`–`0.4.0` (invariant 6), and nothing new is written in Python.\n\n## Context\n\n`atlas/AGENTS.md` § *Language*, 2026-08-29: anything that runs is Rust; a new `.py` in a foundation\nrepository is an operator decision. Measured the same day: `scripts/*.py` is 23,916 lines beside\n30,768 lines of Rust; 18,300 of those are `render-contract-bundle{,-0.2.0,-0.3.0,-0.4.0}.py` and\n`check-contract-bundle{,-0.2.0,-0.3.0,-0.4.0}.py`, near-copies per version. The validator that does\nthe standards work is already Rust (`crates/substrate-contract-check`, `jsonschema = \"=0.49.9\"`;\n`scripts/contract_json_gate.py:195` shells out to it). The wire has two descriptions — the Rust\ntypes in `crates/substrate-wire` and the renderer's dicts — and only the vectors would notice them\ndrifting. Wave 1 of this store added ~1,200 more Python lines (`check-toolchain.py`,\n`package-contract-bundle.py` and its tests) by following the local convention; they are the first\nto move.\n\n## Acceptance\n\n`bash scripts/gate.sh` invokes no Python except the four frozen renderers' checkers, every moved\ncheck refuses the same inputs its Python predecessor refused (proven by the predecessor's own\nnegative cases run against the Rust replacement before the Python is deleted), `0.4.0` still\nverifies byte-clean, and `cargo xtask render-bundle 0.5.0` is the only renderer for the successor.\n\nEvidence that satisfies it, in order:\n\n1. `xtask/` crate in the workspace (`cargo xtask <verb>`, clap derive), `--locked` clean.\n2. `check-toolchain`, `check-links`, `check-adrs` moved first (smallest, no wire contact), each with\n   the Python negative cases ported as tests; the Python files deleted in the same commit.\n3. `package-bundle` moved with `test_package_contract_bundle.py`'s 21 cases ported; the 0.4.0\n   manifest digest `sha256:3758e80b…` reproduced by the Rust packager before the Python goes.\n4. `check-runtime-vectors` moved (the black-box Unix-socket HTTP runner, 1,194 lines) — last of the\n   gate steps, because it is the one with the most behaviour.\n5. `render-bundle 0.5.0` derives schemas from `substrate-wire` types (`schemars` or hand-rolled\n   emitters — decided in the story), with the `0.4.0` compatibility block computed, not typed.\n6. `AGENTS.md` § *The gate* and `README.md` § *Build, test, run* list the xtask verbs; the four\n   frozen renderers are documented as frozen proofs, not tooling.\n\n## Out of Scope\n\nRe-rendering `0.1.0`–`0.4.0` from Rust. They are frozen; their Python renderers are their proof.\n\n## Open Questions\n\nWhether `check-runtime-vectors` becomes an `xtask` verb or an integration test under\n`crates/substrate-daemon/tests/` (it drives a real daemon over a socket, which the daemon's tests\nalready do). Decides: operator. Default if nobody answers: **integration test** — one fewer\nbinary, and the gate already runs `cargo test`.\n\n**Resolved 2026-08-30: integration test.** No existing daemon test spawns the binary — `http.rs:29-34`,\n`websocket.rs:36-38`, `pipe_session.rs` and `contract_vectors.rs` all build `App::new(...)` in\nprocess — so there was no harness to reuse, but the black-box property survives:\n`env!(\"CARGO_BIN_EXE_substrate-daemon\")` lets a test in that package spawn the shipped binary over\na Unix socket. Nothing outside the gate invoked the script.\n\n## Progress — 2026-08-30, steps 1–2\n\n- `xtask/` (bin-only, clap derive; `anyhow`, `clap`, dev `tempfile` — no new crate, `Cargo.lock`\n  +9 lines, zero version bumps); `.cargo/config.toml` alias `cargo xtask`.\n- `check-toolchain --root`, `check-links`, `check-adrs` ported; the three Python files deleted in\n  the same change. Differential runs against the Python before deletion: check-links 19 cases +\n  whole repo byte-identical stdout/stderr; check-adrs 11 mutations of `adr/` 11/11 identical;\n  check-toolchain 12 mutations 12/12 identical. Two deliberate differences: the predecessor\n  monorepo escape hatch in check-links is gone (no ancestor has `scripts/check-monorepo.sh`), and\n  `[x](   )` is skipped where the Python raised `IndexError` (`check-links.py:33`).\n- Repo root at runtime is the nearest ancestor whose `Cargo.toml` has `[workspace]`\n  (`xtask/src/repo.rs`), not the binary's compile-time path.\n- Tests written failing-first (`2 passed; 26 failed` on `unimplemented!()` stubs) →\n  `cargo test -p xtask --locked`: 28 passed. `bash scripts/gate.sh`: passed, 13 steps.\n- `gate.sh:18,19,27`, `AGENTS.md` § The gate, `README.md` § Build, `STATUS.md` (three links to the\n  deleted files) updated. Remaining: steps 3–6 (packager, runtime vectors, `render-bundle 0.5.0`).\n\n## Progress — 2026-08-30, step 3\n\n- `cargo xtask package-bundle <version> --out <dir> [--force] [--source-date-epoch] [--contracts-root]`\n  (`xtask/src/package.rs`): hand-written ustar headers matching `tarfile.USTAR_FORMAT` byte for\n  byte, `serde_json` pretty output post-escaped to `json.dumps(ensure_ascii=True)`, epoch from\n  `git log -1 --format=%at -- .` or the flag. Differential against the Python on 0.4.0 before\n  deletion: identical stdout (`sha256:3758e80b…`, archive `sha256:91fb5524…`, 880,640 bytes),\n  `diff -r` empty, modes identical, eleven refusal cases with identical exit code and text.\n- 21 packager tests ported (49 in the crate); failing-first on a timestamp-embedding stub\n  (`45 passed; 4 failed`) then green. Both Python files deleted; `gate.sh` drops the packager\n  test line (it runs under `cargo test --workspace`); gate 12 steps, passed.\n- Remaining: steps 4–6.\n\n\n## Progress — 2026-08-30, step 4\n\n- `crates/substrate-daemon/tests/runtime_vectors.rs` (1,730 lines, one `#[tokio::test]` mirroring\n  the Python's `main()` in order) replaces `scripts/check-runtime-vectors.py`, which is deleted.\n  Hand-written HTTP/1.1 over `tokio::net::UnixStream` and raw WebSocket frames; the lane switch is\n  `SUBSTRATE_VECTORS_CGROUP_ROOT` where the script took `--cgroup-root`, and an unset value keeps\n  the portable branch asserting `exec.sandbox-unavailable` 501 with the delegated cases absent,\n  never counted (invariant 3).\n- Zero new crates; `Cargo.lock` byte-identical. `nix` gains its test-only `signal` feature\n  (`crates/substrate-daemon/Cargo.toml:48-50`).\n- Differential against the Python before deletion: portable lane both `27 HTTP cases, startup\n  refusal, dual-daemon refusal`; delegated lane both `38 HTTP cases` under\n  `systemd-run --user -p Delegate=yes --scope`; two negative cases (`SUBSTRATE_ALLOW_UID` set,\n  non-delegated cgroup root) fail identically on both. Failing-first:\n  `not implemented: cases 2..27 are not ported yet` → `0 passed; 1 failed`.\n- Two deliberate improvements: the temp dir is forced to 0700 (Rust's `TempDir` is 0777 & ~umask\n  and the daemon refuses it), and the case counts are now asserted (`PORTABLE_CASES=27`,\n  `DELEGATED_CASES=38`) rather than printed.\n- The gate keeps no separate step: `cargo test --workspace --locked` runs it. Gate 11 steps,\n  passed; `cargo test --workspace`: 17 suites, 205 passed, 0 failed.\n- **The gate now runs no Python except the frozen bundles' own renderers and checkers.** Remaining\n  for this story: step 5 (`render-bundle 0.5.0` from `substrate-wire` types) and step 6.\n\n## Progress — 2026-08-30, steps 5 and 6\n\n**Step 5 done, and step 5's own premise was wrong.** `cargo xtask render-bundle 0.4.0` reproduces\nthe frozen bundle byte for byte — `diff -r contracts/substrate-wire/0.4.0 <out>` empty, 200 files,\nand packaging the rendered tree yields the manifest digest already checked in,\n`sha256:3758e80bc39f1eb03b15c69410608c9ef1d2ba8095c7e707c6988dbb5894ab00`. A test holds that fixed\npoint (`the_rendered_tree_packages_to_the_released_manifest_digest`), so the renderer cannot drift\nfrom what shipped.\n\nThe story said \"derives schemas from `substrate-wire` types\". It cannot, and the implementation\nrecords that rather than pretending:\n\n- `schemars` is not a workspace dependency (`Cargo.toml:22-59`) and adding derives would mean\n  editing `crates/`, so nothing can reflect the types.\n- Even with reflection the types are **ahead** of 0.4.0: `ExecStartInput::read_only_roots`\n  (`crates/substrate-wire/src/lib.rs:761`) has no 0.4.0 schema at all —\n  `grep -rl read_only_roots contracts/substrate-wire/0.4.0/` → 0 hits — and\n  `FileEditInput`, `FilePatchInput`, `WorkspaceTree*` and `PathMoveInput` have no 0.4.0 input schema.\n- The constraints the schemas state are not on the types. `argv: Vec<String>`\n  (`crates/substrate-wire/src/lib.rs:757`) against a schema of `minLength 1 / maxLength 4096 /\n  maxItems 256`, whose literals live in `crates/substrate-daemon/src/app/operations.rs:245-248` and\n  `crates/substrate-host/src/process.rs:824-826`.\n\nWhat the wire crate does own is taken from it: canonical hashing for all 11 fixture cases,\ncross-checked against `canonical_request_hash_v2`, and 22 bounds constants bound through a `$wire`\nsplice. Of 200 emitted files (708,672 bytes), 30 are computed whole, 14 are authored with computed\nsplices, and 156 are authored verbatim.\n\nOne derivation is **lost**, and is documented as lost at `xtask/src/render.rs:19-25`: the three\nunions in `schemas/vector.json` encode Python dict insertion order (`exec.output-limit-bytes` before\n`exec.max-current`), which a sorted-key bundle records nowhere.\n\nAuthored source is `xtask/bundle-source/<version>/` — one file per emitted path under `documents/`,\nplus `routes.json`, `coverage.json`, `hash-cases.json`, `vector-order.json` and\n`executable-vectors.json`. Not `contracts/source/`: every directory under `contracts/` is a released\nbundle (invariant 6) and `scripts/contract_json_gate.py:301` classifies JSON beneath one and fails\nclosed (invariant 7). Rendering into `contracts/` is a named refusal, exit 1.\n\nFailing-first: `render`/`hash_case` stubbed to `unimplemented!()` gave `55 passed; 9 failed`.\nThen `cargo test -p xtask --locked` → `64 passed; 0 failed` (49 before). `bash scripts/gate.sh`\npassed, 11 steps. `git status --short contracts/` empty.\n\n**Step 6 done.** `AGENTS.md` § *The gate* and `README.md` § *Build, test, run* both carry the verb\ntable — `check-toolchain`, `check-links`, `check-adrs`, `package-bundle`, `render-bundle` — and both\nsay why the four frozen `render-contract-bundle*.py` / `check-contract-bundle*.py` pairs stay: they\nare the released bundles' reproducibility proof, and 0.4.0's `generator.name` points at one of them,\nso deleting it would break the bundle it generated.\n\n**Decision taken by default:** `render-bundle` gets no `gate.sh` step of its own. A test already\nasserts the 0.4.0 digest, and `cargo test --workspace --locked` is the gate's first step — the same\nreasoning that keeps `package-bundle` and the runtime-vector runner out of it.\n\n**0.5.0 is cut, so that clause is closed.** `story:sealed-secret-slots` rendered\n`contracts/substrate-wire/0.5.0/` (206 files) with `cargo xtask render-bundle`, and no Python\nrenderer was involved or written. It also replaced the ADR's proposed\n`scripts/check-contract-bundle-0.5.0.py` with a new verb, `cargo xtask check-bundle <version>`,\nwhich re-renders from the authored source and compares bytes — strictly stronger than a\nhand-written checker.\n\n## What is actually left — 2026-08-30\n\n**576 lines, and they are not the frozen renderers.** The acceptance says the gate invokes no Python\nexcept the four frozen renderers' checkers. It still invokes one more:\n\n    scripts/gate.sh:28  run python3 scripts/test_contract_json_gate.py\n\n`scripts/contract_json_gate.py` (377 lines) and its self-test (199) are shared live machinery, not a\nper-version frozen artefact — `:301` classifies JSON under a bundle directory and fails closed, and\n`:195` already shells out to `crates/substrate-contract-check`, which is Rust. Porting it is the\nlast step of this story, and it is the one place a new bundle's JSON is admitted or rejected, so it\nwants the same differential proof every other move got: the predecessor's negative cases run against\nthe Rust replacement before the Python is deleted.\n\n`scripts/check-bot-files.py` (39 lines) is not in `scripts/gate.sh` and is out of this story's\nacceptance; name it separately if it should move.\n\nOf the 21,298 Python lines left, 20,683 are the four frozen renderer/checker pairs, which stay.\n"},"command":"update-entity","target":"01MEM0000000000000049"},"payload":{"actor":"human:timo","at":1788050231592,"causation":"cmd-protocol-artifact-body-story-tooling-moves-to-cargo-xtask-1788050231592","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body","event_id":"story:tooling-moves-to-cargo-xtask@9#0~ad593e95f864d2d8","executor":null,"recorded_at":"2026-08-30T00:37:11Z"}}
{"entity":"story","version":1,"id":"destination-bound-egress","revision":6,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"verification\":1}}","target":"01MEM0000000000000019","to":"active"},"payload":{"actor":"human:timo","at":1788050514295,"causation":"cmd-move-story-destination-bound-egress-1788050514295","change":{"change":"moved","decided_on":{"recorded":{"verification":1}},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:destination-bound-egress@6#0~520dbf520b463dda","executor":null,"recorded_at":"2026-08-30T00:41:54Z"}}
{"entity":"story","version":1,"id":"destination-bound-egress","revision":7,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"body":"# Story: Destination-bound egress apertures are declared, verified and refused by name\n\n## Outcome\n\nA confined run reaches exactly the network destinations an operator declared for it — and, with\n`story:sealed-secret-slots`, a vendor harness (`codex`, `claude`) can run under substrate\nconfinement with its model credential and its model endpoint, which design 05 § *Progress* says is\nrefused until both capabilities exist.\n\n## Context\n\n`docs/design/04-security-and-isolation.md` § 6: egress, listening sockets and exposed endpoints\nare separate capabilities; ordinary execution defaults to **no egress**; \"an aperture is\ndeployment/operator authority\" matched to a destination. The delegated lane today proves\nnamespace no-egress and nothing else. Atlas O3/O4 want vendor harnesses driven under the same\ngovernor as the native loop; `autodev` dispatches only `codex` and `claude`\n(`atlas/ROADMAP.md`, arrow *autodev → the fleet*). A vendor harness that cannot reach its model\nis not confined, it is dead — so secrets and egress are one capability pair, and the review that\nfiled this story found the backlog held only the first half.\n\n## Acceptance\n\nA run started with a declared egress aperture reaches the declared destination and nothing else,\nthe applied aperture is an observation in the run's record, and a request for an aperture the\ndeployment did not declare is `unserved` with the aperture named — proven by the delegated lane\nwith a real listener inside and outside the aperture.\n\nEvidence that satisfies it, in order:\n\n1. **Before code**: an ADR fixing the aperture shape (host:port or name:port, protocol, whether\n   DNS resolution is inside the aperture), the operator declaration surface, and the refusal.\n2. A successor bundle adds the request field, the capability fact `network.egress.apertures`, the\n   observation, and the refusal; earlier bundle bytes unchanged.\n3. Failing-first tests: `egress_defaults_to_none`, `declared_aperture_is_reachable`,\n   `undeclared_destination_is_unreachable_and_named`,\n   `aperture_outside_operator_declaration_is_unserved`, `applied_aperture_is_observed`.\n4. The delegated lane runs the model-free fake app-server against a loopback endpoint inside the\n   aperture and proves a second endpoint outside it is unreachable.\n\n## Out of Scope\n\nPublic ingress, DNS, TLS termination and tunnels (design 05 § 4). Brokered connector artifacts.\n\n## Open Questions\n\nEnforcement mechanism inside the existing namespace no-egress posture: a veth pair plus nftables\nin the sandbox namespace, or a userspace proxy at a declared descriptor. Decides: the ADR. Default\nif nobody answers: **namespace + nftables**, because it keeps bytes out of the daemon.\n\n## Design draft — 2026-08-30\n\n`docs/design/10-destination-bound-egress.md` (proposed). Finding that overturns this story's\ndefault: option (a), namespace + nftables, needs root, and the daemon withholds `exec` when it\nruns as root (`crates/substrate-host/src/probe.rs:49-51`); bubblewrap 0.11.2 has no `--netns`, so\na prepared namespace cannot be handed in. The draft recommends (c), a per-run forwarder inside the\nsandbox netns, owing a `setns` spike on a delegated host before the ADR. `NetworkMode::Aperture`\nalready exists and is `unserved` twice (`substrate-wire/src/lib.rs:595-599`,\n`operations.rs:290-300`, `process.rs:810-815`); the only executed no-egress proof today is one\n`connect` to `1.1.1.1:53` (`crates/substrate-daemon/tests/runtime_vectors.rs:620-640`, ported from the retired\n`scripts/check-runtime-vectors.py:348-367` on 2026-08-30).\n\n## Progress — 2026-08-30, steps 2-4\n\nGate green (`bash scripts/gate.sh`, exit 0). `contracts/substrate-wire/0.6.0/` (213 files) is the\nsuccessor; `0.4.0` and `0.5.0` both still reproduce byte for byte (`diff -r` empty each), and\n`git status --short contracts/` shows only the new directory.\n\n**Mechanism (c), and it really ran here.** `strace -f -e trace=execve` counts **5 `execve` of\n`/usr/bin/bwrap`** across the 9 `egress::tests::*` — these are not vacuous passes. 40 bytes were\ncounted through the forwarder. `Sandbox::open` deliberately refuses to skip: a sandbox that reports\nno `child-pid` is `expect`-ed, not returned as absent, because silently skipping is exactly how\nthese cases would pass without running.\n\n**A third silent-failure trap, in the same class as the spike's two but not named by it.**\n`std::io::pipe()` is `O_CLOEXEC`, so the first version of the mechanism tests passed in 0.00 s\nhaving opened no sandbox at all. Fixed with `pipe2(…, 0)` and a *written* barrier byte — closing the\nwrite end only releases bwrap once every forked copy of it is gone. Recorded in\n`docs/design/10a-egress-mechanism-spike.md` § 8.\n\n**Trap 2 bites, and a test now catches it.** Injecting `child.id()` in place of the `--info-fd`\n`child-pid` fails `the_mechanism_is_proven_in_a_throwaway_sandbox`. Trap 1 did **not** reproduce for\nthe throwaway argv — bwrap nested no second user namespace there — so `NS_GET_USERNS` is kept as the\nsuperset, not as a demonstrated fix. Say so rather than claim a fix that was never exercised.\n\n**`xtask/src/render.rs` can never be edited again without breaking `0.5.0`.** A bundle's\n`generator.digest` is the sha256 of that file (`xtask/src/render.rs:308-312`), so a successor cannot\nadd a `{\"$wire\": …}` binding there. `MAX_EGRESS_APERTURES` is bound from `xtask/src/bundle.rs`\ninstead, which no bundle hashes. Recorded in `AGENTS.md` § *The gate*.\n\n**The story and the ADR disagree on the fact name; the ADR won.** This story said\n`network.egress.apertures`; ADR 0013 § *Decision* and design 10 § 6 say `exec.egress-apertures`, and\nno fact name in `contracts/…/schemas/capability.json` carries two dots. Shipped as\n`exec.egress-apertures`.\n\n**Test brittleness found and fixed by the orchestrator, not the implementer.**\n`egress_defaults_to_none` pinned the exact errno — `Refused` for a host listener, `Unreachable` for a\npublic address. Under `strace -f` this host answers `Unreachable` where it otherwise answers\n`Refused`, and the test went red. Both outcomes prove the same guarantee, so the assertions now hold\n*did not reach* and report the observed variant. A test that pins the variant goes red on a\ndifferent kernel — including CI's — for something that is not a regression.\n\n**Defaults taken.**\n\n- Per-run read-only CA snapshot behind `--ca-bundle`. Unset means no anchor, so verification fails\n  inside the sandbox: **absent, not unverified** (invariant 3).\n- The forwarder listens on the *declared* port, so the generated `/etc/hosts` makes the operator's\n  own URL work unchanged.\n- `network: \"aperture\"` with no name keeps the frozen `exec.network-unserved`.\n- `exec.aperture-byte-limit` (design 10 § 5 row 5) **deferred**: bytes are counted and observed, the\n  ceiling is not enforced. That is the one row of design 10 § 5 this change does not deliver.\n\n**Not executed on this host: the delegated lane.**\n`/sys/fs/cgroup/user.slice/user-1000.slice/session-3.scope` is root-owned and `mkdir` gives\n`Permission denied`, so `check_confined_apertures` and `DELEGATED_CASES = 42` are **absent, never\nreported as passed** (invariant 3). Portable lane 29/29. Step 4 of the acceptance — the fake\napp-server inside the aperture with a second endpoint outside it — is written and switched on by\n`SUBSTRATE_VECTORS_CGROUP_ROOT`, and has not run anywhere yet.\n"},"args":{"changes":{"body":"# Story: Destination-bound egress apertures are declared, verified and refused by name\n\n## Outcome\n\nA confined run reaches exactly the network destinations an operator declared for it — and, with\n`story:sealed-secret-slots`, a vendor harness (`codex`, `claude`) can run under substrate\nconfinement with its model credential and its model endpoint, which design 05 § *Progress* says is\nrefused until both capabilities exist.\n\n## Context\n\n`docs/design/04-security-and-isolation.md` § 6: egress, listening sockets and exposed endpoints\nare separate capabilities; ordinary execution defaults to **no egress**; \"an aperture is\ndeployment/operator authority\" matched to a destination. The delegated lane today proves\nnamespace no-egress and nothing else. Atlas O3/O4 want vendor harnesses driven under the same\ngovernor as the native loop; `autodev` dispatches only `codex` and `claude`\n(`atlas/ROADMAP.md`, arrow *autodev → the fleet*). A vendor harness that cannot reach its model\nis not confined, it is dead — so secrets and egress are one capability pair, and the review that\nfiled this story found the backlog held only the first half.\n\n## Acceptance\n\nA run started with a declared egress aperture reaches the declared destination and nothing else,\nthe applied aperture is an observation in the run's record, and a request for an aperture the\ndeployment did not declare is `unserved` with the aperture named — proven by the delegated lane\nwith a real listener inside and outside the aperture.\n\nEvidence that satisfies it, in order:\n\n1. **Before code**: an ADR fixing the aperture shape (host:port or name:port, protocol, whether\n   DNS resolution is inside the aperture), the operator declaration surface, and the refusal.\n2. A successor bundle adds the request field, the capability fact `network.egress.apertures`, the\n   observation, and the refusal; earlier bundle bytes unchanged.\n3. Failing-first tests: `egress_defaults_to_none`, `declared_aperture_is_reachable`,\n   `undeclared_destination_is_unreachable_and_named`,\n   `aperture_outside_operator_declaration_is_unserved`, `applied_aperture_is_observed`.\n4. The delegated lane runs the model-free fake app-server against a loopback endpoint inside the\n   aperture and proves a second endpoint outside it is unreachable.\n\n## Out of Scope\n\nPublic ingress, DNS, TLS termination and tunnels (design 05 § 4). Brokered connector artifacts.\n\n## Open Questions\n\nEnforcement mechanism inside the existing namespace no-egress posture: a veth pair plus nftables\nin the sandbox namespace, or a userspace proxy at a declared descriptor. Decides: the ADR. Default\nif nobody answers: **namespace + nftables**, because it keeps bytes out of the daemon.\n\n## Design draft — 2026-08-30\n\n`docs/design/10-destination-bound-egress.md` (proposed). Finding that overturns this story's\ndefault: option (a), namespace + nftables, needs root, and the daemon withholds `exec` when it\nruns as root (`crates/substrate-host/src/probe.rs:49-51`); bubblewrap 0.11.2 has no `--netns`, so\na prepared namespace cannot be handed in. The draft recommends (c), a per-run forwarder inside the\nsandbox netns, owing a `setns` spike on a delegated host before the ADR. `NetworkMode::Aperture`\nalready exists and is `unserved` twice (`substrate-wire/src/lib.rs:595-599`,\n`operations.rs:290-300`, `process.rs:810-815`); the only executed no-egress proof today is one\n`connect` to `1.1.1.1:53` (`crates/substrate-daemon/tests/runtime_vectors.rs:620-640`, ported from the retired\n`scripts/check-runtime-vectors.py:348-367` on 2026-08-30).\n\n## Progress — 2026-08-30, steps 2-4\n\nGate green (`bash scripts/gate.sh`, exit 0). `contracts/substrate-wire/0.6.0/` (213 files) is the\nsuccessor; `0.4.0` and `0.5.0` both still reproduce byte for byte (`diff -r` empty each), and\n`git status --short contracts/` shows only the new directory.\n\n**Mechanism (c), and it really ran here.** `strace -f -e trace=execve` counts **5 `execve` of\n`/usr/bin/bwrap`** across the 9 `egress::tests::*` — these are not vacuous passes. 40 bytes were\ncounted through the forwarder. `Sandbox::open` deliberately refuses to skip: a sandbox that reports\nno `child-pid` is `expect`-ed, not returned as absent, because silently skipping is exactly how\nthese cases would pass without running.\n\n**A third silent-failure trap, in the same class as the spike's two but not named by it.**\n`std::io::pipe()` is `O_CLOEXEC`, so the first version of the mechanism tests passed in 0.00 s\nhaving opened no sandbox at all. Fixed with `pipe2(…, 0)` and a *written* barrier byte — closing the\nwrite end only releases bwrap once every forked copy of it is gone. Recorded in\n`docs/design/10a-egress-mechanism-spike.md` § 8.\n\n**Trap 2 bites, and a test now catches it.** Injecting `child.id()` in place of the `--info-fd`\n`child-pid` fails `the_mechanism_is_proven_in_a_throwaway_sandbox`. Trap 1 did **not** reproduce for\nthe throwaway argv — bwrap nested no second user namespace there — so `NS_GET_USERNS` is kept as the\nsuperset, not as a demonstrated fix. Say so rather than claim a fix that was never exercised.\n\n**`xtask/src/render.rs` can never be edited again without breaking `0.5.0`.** A bundle's\n`generator.digest` is the sha256 of that file (`xtask/src/render.rs:308-312`), so a successor cannot\nadd a `{\"$wire\": …}` binding there. `MAX_EGRESS_APERTURES` is bound from `xtask/src/bundle.rs`\ninstead, which no bundle hashes. Recorded in `AGENTS.md` § *The gate*.\n\n**The story and the ADR disagree on the fact name; the ADR won.** This story said\n`network.egress.apertures`; ADR 0013 § *Decision* and design 10 § 6 say `exec.egress-apertures`, and\nno fact name in `contracts/…/schemas/capability.json` carries two dots. Shipped as\n`exec.egress-apertures`.\n\n**Test brittleness found and fixed by the orchestrator, not the implementer.**\n`egress_defaults_to_none` pinned the exact errno — `Refused` for a host listener, `Unreachable` for a\npublic address. Under `strace -f` this host answers `Unreachable` where it otherwise answers\n`Refused`, and the test went red. Both outcomes prove the same guarantee, so the assertions now hold\n*did not reach* and report the observed variant. A test that pins the variant goes red on a\ndifferent kernel — including CI's — for something that is not a regression.\n\n**Defaults taken.**\n\n- Per-run read-only CA snapshot behind `--ca-bundle`. Unset means no anchor, so verification fails\n  inside the sandbox: **absent, not unverified** (invariant 3).\n- The forwarder listens on the *declared* port, so the generated `/etc/hosts` makes the operator's\n  own URL work unchanged.\n- `network: \"aperture\"` with no name keeps the frozen `exec.network-unserved`.\n- `exec.aperture-byte-limit` (design 10 § 5 row 5) **deferred**: bytes are counted and observed, the\n  ceiling is not enforced. That is the one row of design 10 § 5 this change does not deliver.\n\n**Not executed on this host: the delegated lane.**\n`/sys/fs/cgroup/user.slice/user-1000.slice/session-3.scope` is root-owned and `mkdir` gives\n`Permission denied`, so `check_confined_apertures` and `DELEGATED_CASES = 42` are **absent, never\nreported as passed** (invariant 3). Portable lane 29/29. Step 4 of the acceptance — the fake\napp-server inside the aperture with a second endpoint outside it — is written and switched on by\n`SUBSTRATE_VECTORS_CGROUP_ROOT`, and has not run anywhere yet.\n"},"command":"update-entity","target":"01MEM0000000000000019"},"payload":{"actor":"human:timo","at":1788054387436,"causation":"cmd-protocol-artifact-body-story-destination-bound-egress-1788054387436","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body","event_id":"story:destination-bound-egress@7#0~1322462832a25eca","executor":null,"recorded_at":"2026-08-30T01:46:27Z"}}
{"entity":"story","version":1,"id":"destination-bound-egress","revision":7,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"crates/substrate-host/src/egress.rs","source":"bash scripts/gate.sh exit 0; check-bundle 0.6.0 verified 213 files; 9 egress tests pass with 5 real bwrap execve counted under strace; delegated lane absent (no delegated cgroup on this host)","target":"01MEM0000000000000019"},"payload":{"actor":"human:timo","at":1788054387000,"causation":"cmd-evidence-story-destination-bound-egress-test_result-1788054387450","change":{"change":"evidence","kind":"test_result","reference":"crates/substrate-host/src/egress.rs","source":"bash scripts/gate.sh exit 0; check-bundle 0.6.0 verified 213 files; 9 egress tests pass with 5 real bwrap execve counted under strace; delegated lane absent (no delegated cgroup on this host)"},"correlation":"protocol-artifact-evidence","event_id":"story:destination-bound-egress@7#0~8939f5e920731bca","executor":null,"recorded_at":"2026-08-30T01:46:27Z"}}
{"entity":"story","version":1,"id":"destination-bound-egress","revision":7,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"scripts/delegated-lane.sh","source":"delegated lane executed via scripts/delegated-lane.sh: \"runtime clean-room: 42 HTTP cases, startup refusal, and dual-daemon refusal passed (delegated lane)\" — includes check_confined_apertures with a listener inside and outside the aperture (runtime_vectors.rs:1656)","target":"01MEM0000000000000019"},"payload":{"actor":"human:timo","at":1788054750000,"causation":"cmd-evidence-story-destination-bound-egress-test_result-1788054750419","change":{"change":"evidence","kind":"test_result","reference":"scripts/delegated-lane.sh","source":"delegated lane executed via scripts/delegated-lane.sh: \"runtime clean-room: 42 HTTP cases, startup refusal, and dual-daemon refusal passed (delegated lane)\" — includes check_confined_apertures with a listener inside and outside the aperture (runtime_vectors.rs:1656)"},"correlation":"protocol-artifact-evidence","event_id":"story:destination-bound-egress@7#0~6077b3671f2c905e","executor":null,"recorded_at":"2026-08-30T01:52:30Z"}}
{"entity":"story","version":1,"id":"destination-bound-egress","revision":8,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":2,\"verification\":1}}","target":"01MEM0000000000000019","to":"implemented"},"payload":{"actor":"human:timo","at":1788054750443,"causation":"cmd-move-story-destination-bound-egress-1788054750443","change":{"change":"moved","decided_on":{"recorded":{"test_result":2,"verification":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:destination-bound-egress@8#0~05e216533af14c4e","executor":null,"recorded_at":"2026-08-30T01:52:30Z"}}
{"entity":"story","version":1,"id":"destination-bound-egress","revision":9,"type":"aep.entity.update/v1","from_state":"implemented","to_state":"implemented","changed":{"body":"# Story: Destination-bound egress apertures are declared, verified and refused by name\n\n## Outcome\n\nA confined run reaches exactly the network destinations an operator declared for it — and, with\n`story:sealed-secret-slots`, a vendor harness (`codex`, `claude`) can run under substrate\nconfinement with its model credential and its model endpoint, which design 05 § *Progress* says is\nrefused until both capabilities exist.\n\n## Context\n\n`docs/design/04-security-and-isolation.md` § 6: egress, listening sockets and exposed endpoints\nare separate capabilities; ordinary execution defaults to **no egress**; \"an aperture is\ndeployment/operator authority\" matched to a destination. The delegated lane today proves\nnamespace no-egress and nothing else. Atlas O3/O4 want vendor harnesses driven under the same\ngovernor as the native loop; `autodev` dispatches only `codex` and `claude`\n(`atlas/ROADMAP.md`, arrow *autodev → the fleet*). A vendor harness that cannot reach its model\nis not confined, it is dead — so secrets and egress are one capability pair, and the review that\nfiled this story found the backlog held only the first half.\n\n## Acceptance\n\nA run started with a declared egress aperture reaches the declared destination and nothing else,\nthe applied aperture is an observation in the run's record, and a request for an aperture the\ndeployment did not declare is `unserved` with the aperture named — proven by the delegated lane\nwith a real listener inside and outside the aperture.\n\nEvidence that satisfies it, in order:\n\n1. **Before code**: an ADR fixing the aperture shape (host:port or name:port, protocol, whether\n   DNS resolution is inside the aperture), the operator declaration surface, and the refusal.\n2. A successor bundle adds the request field, the capability fact `network.egress.apertures`, the\n   observation, and the refusal; earlier bundle bytes unchanged.\n3. Failing-first tests: `egress_defaults_to_none`, `declared_aperture_is_reachable`,\n   `undeclared_destination_is_unreachable_and_named`,\n   `aperture_outside_operator_declaration_is_unserved`, `applied_aperture_is_observed`.\n4. The delegated lane runs the model-free fake app-server against a loopback endpoint inside the\n   aperture and proves a second endpoint outside it is unreachable.\n\n## Out of Scope\n\nPublic ingress, DNS, TLS termination and tunnels (design 05 § 4). Brokered connector artifacts.\n\n## Open Questions\n\nEnforcement mechanism inside the existing namespace no-egress posture: a veth pair plus nftables\nin the sandbox namespace, or a userspace proxy at a declared descriptor. Decides: the ADR. Default\nif nobody answers: **namespace + nftables**, because it keeps bytes out of the daemon.\n\n## Design draft — 2026-08-30\n\n`docs/design/10-destination-bound-egress.md` (proposed). Finding that overturns this story's\ndefault: option (a), namespace + nftables, needs root, and the daemon withholds `exec` when it\nruns as root (`crates/substrate-host/src/probe.rs:49-51`); bubblewrap 0.11.2 has no `--netns`, so\na prepared namespace cannot be handed in. The draft recommends (c), a per-run forwarder inside the\nsandbox netns, owing a `setns` spike on a delegated host before the ADR. `NetworkMode::Aperture`\nalready exists and is `unserved` twice (`substrate-wire/src/lib.rs:595-599`,\n`operations.rs:290-300`, `process.rs:810-815`); the only executed no-egress proof today is one\n`connect` to `1.1.1.1:53` (`crates/substrate-daemon/tests/runtime_vectors.rs:620-640`, ported from the retired\n`scripts/check-runtime-vectors.py:348-367` on 2026-08-30).\n\n## Progress — 2026-08-30, steps 2-4\n\nGate green (`bash scripts/gate.sh`, exit 0). `contracts/substrate-wire/0.6.0/` (213 files) is the\nsuccessor; `0.4.0` and `0.5.0` both still reproduce byte for byte (`diff -r` empty each), and\n`git status --short contracts/` shows only the new directory.\n\n**Mechanism (c), and it really ran here.** `strace -f -e trace=execve` counts **5 `execve` of\n`/usr/bin/bwrap`** across the 9 `egress::tests::*` — these are not vacuous passes. 40 bytes were\ncounted through the forwarder. `Sandbox::open` deliberately refuses to skip: a sandbox that reports\nno `child-pid` is `expect`-ed, not returned as absent, because silently skipping is exactly how\nthese cases would pass without running.\n\n**A third silent-failure trap, in the same class as the spike's two but not named by it.**\n`std::io::pipe()` is `O_CLOEXEC`, so the first version of the mechanism tests passed in 0.00 s\nhaving opened no sandbox at all. Fixed with `pipe2(…, 0)` and a *written* barrier byte — closing the\nwrite end only releases bwrap once every forked copy of it is gone. Recorded in\n`docs/design/10a-egress-mechanism-spike.md` § 8.\n\n**Trap 2 bites, and a test now catches it.** Injecting `child.id()` in place of the `--info-fd`\n`child-pid` fails `the_mechanism_is_proven_in_a_throwaway_sandbox`. Trap 1 did **not** reproduce for\nthe throwaway argv — bwrap nested no second user namespace there — so `NS_GET_USERNS` is kept as the\nsuperset, not as a demonstrated fix. Say so rather than claim a fix that was never exercised.\n\n**`xtask/src/render.rs` can never be edited again without breaking `0.5.0`.** A bundle's\n`generator.digest` is the sha256 of that file (`xtask/src/render.rs:308-312`), so a successor cannot\nadd a `{\"$wire\": …}` binding there. `MAX_EGRESS_APERTURES` is bound from `xtask/src/bundle.rs`\ninstead, which no bundle hashes. Recorded in `AGENTS.md` § *The gate*.\n\n**The story and the ADR disagree on the fact name; the ADR won.** This story said\n`network.egress.apertures`; ADR 0013 § *Decision* and design 10 § 6 say `exec.egress-apertures`, and\nno fact name in `contracts/…/schemas/capability.json` carries two dots. Shipped as\n`exec.egress-apertures`.\n\n**Test brittleness found and fixed by the orchestrator, not the implementer.**\n`egress_defaults_to_none` pinned the exact errno — `Refused` for a host listener, `Unreachable` for a\npublic address. Under `strace -f` this host answers `Unreachable` where it otherwise answers\n`Refused`, and the test went red. Both outcomes prove the same guarantee, so the assertions now hold\n*did not reach* and report the observed variant. A test that pins the variant goes red on a\ndifferent kernel — including CI's — for something that is not a regression.\n\n**Defaults taken.**\n\n- Per-run read-only CA snapshot behind `--ca-bundle`. Unset means no anchor, so verification fails\n  inside the sandbox: **absent, not unverified** (invariant 3).\n- The forwarder listens on the *declared* port, so the generated `/etc/hosts` makes the operator's\n  own URL work unchanged.\n- `network: \"aperture\"` with no name keeps the frozen `exec.network-unserved`.\n- `exec.aperture-byte-limit` (design 10 § 5 row 5) **deferred**: bytes are counted and observed, the\n  ceiling is not enforced. That is the one row of design 10 § 5 this change does not deliver.\n\n## The delegated lane does run here — 2026-08-30\n\nThe implementer concluded it could not: `/sys/fs/cgroup/user.slice/user-1000.slice/session-3.scope`\nis root-owned and `mkdir` gives `Permission denied`, so `check_confined_apertures` and\n`DELEGATED_CASES = 42` reported themselves **absent** (invariant 3). That conclusion was about one\ncgroup path, not about the host. Asking systemd for a delegated scope needs no privilege:\n\n    $ bash scripts/delegated-lane.sh\n    delegated-lane: root /sys/fs/cgroup/…/app.slice/run-….scope, controllers cpu memory pids\n    runtime clean-room: 42 HTTP cases, startup refusal, and dual-daemon refusal passed (delegated lane)\n    test result: ok. 1 passed; 0 failed\n\nThe script wraps `systemd-run --user -p Delegate=yes --scope`, moves itself into a child group so\nthe delegation root stays process-free, and sets `SUBSTRATE_VECTORS_CGROUP_ROOT`. It is checked in\nprecisely so nobody reaches that conclusion again: **an absent lane and a green one read the same\nway if you only look at `cargo test`.**\n\n**Acceptance step 4 is therefore executed, not written-but-unrun.** `check_confined_apertures`\n(`crates/substrate-daemon/tests/runtime_vectors.rs:1656`) drives the model-free app-server on a\nloopback endpoint inside the aperture and a second endpoint outside it, and it is one of the 42.\nPortable lane 29/29 separately.\n\n**Deferred and not covered by this story's acceptance:** `exec.aperture-byte-limit`\n(design 10 § 5 row 5). Bytes are counted and observed; the ceiling is not enforced. That is the one\nrow of design 10 § 5 this change does not deliver, and it wants its own story.\n"},"args":{"changes":{"body":"# Story: Destination-bound egress apertures are declared, verified and refused by name\n\n## Outcome\n\nA confined run reaches exactly the network destinations an operator declared for it — and, with\n`story:sealed-secret-slots`, a vendor harness (`codex`, `claude`) can run under substrate\nconfinement with its model credential and its model endpoint, which design 05 § *Progress* says is\nrefused until both capabilities exist.\n\n## Context\n\n`docs/design/04-security-and-isolation.md` § 6: egress, listening sockets and exposed endpoints\nare separate capabilities; ordinary execution defaults to **no egress**; \"an aperture is\ndeployment/operator authority\" matched to a destination. The delegated lane today proves\nnamespace no-egress and nothing else. Atlas O3/O4 want vendor harnesses driven under the same\ngovernor as the native loop; `autodev` dispatches only `codex` and `claude`\n(`atlas/ROADMAP.md`, arrow *autodev → the fleet*). A vendor harness that cannot reach its model\nis not confined, it is dead — so secrets and egress are one capability pair, and the review that\nfiled this story found the backlog held only the first half.\n\n## Acceptance\n\nA run started with a declared egress aperture reaches the declared destination and nothing else,\nthe applied aperture is an observation in the run's record, and a request for an aperture the\ndeployment did not declare is `unserved` with the aperture named — proven by the delegated lane\nwith a real listener inside and outside the aperture.\n\nEvidence that satisfies it, in order:\n\n1. **Before code**: an ADR fixing the aperture shape (host:port or name:port, protocol, whether\n   DNS resolution is inside the aperture), the operator declaration surface, and the refusal.\n2. A successor bundle adds the request field, the capability fact `network.egress.apertures`, the\n   observation, and the refusal; earlier bundle bytes unchanged.\n3. Failing-first tests: `egress_defaults_to_none`, `declared_aperture_is_reachable`,\n   `undeclared_destination_is_unreachable_and_named`,\n   `aperture_outside_operator_declaration_is_unserved`, `applied_aperture_is_observed`.\n4. The delegated lane runs the model-free fake app-server against a loopback endpoint inside the\n   aperture and proves a second endpoint outside it is unreachable.\n\n## Out of Scope\n\nPublic ingress, DNS, TLS termination and tunnels (design 05 § 4). Brokered connector artifacts.\n\n## Open Questions\n\nEnforcement mechanism inside the existing namespace no-egress posture: a veth pair plus nftables\nin the sandbox namespace, or a userspace proxy at a declared descriptor. Decides: the ADR. Default\nif nobody answers: **namespace + nftables**, because it keeps bytes out of the daemon.\n\n## Design draft — 2026-08-30\n\n`docs/design/10-destination-bound-egress.md` (proposed). Finding that overturns this story's\ndefault: option (a), namespace + nftables, needs root, and the daemon withholds `exec` when it\nruns as root (`crates/substrate-host/src/probe.rs:49-51`); bubblewrap 0.11.2 has no `--netns`, so\na prepared namespace cannot be handed in. The draft recommends (c), a per-run forwarder inside the\nsandbox netns, owing a `setns` spike on a delegated host before the ADR. `NetworkMode::Aperture`\nalready exists and is `unserved` twice (`substrate-wire/src/lib.rs:595-599`,\n`operations.rs:290-300`, `process.rs:810-815`); the only executed no-egress proof today is one\n`connect` to `1.1.1.1:53` (`crates/substrate-daemon/tests/runtime_vectors.rs:620-640`, ported from the retired\n`scripts/check-runtime-vectors.py:348-367` on 2026-08-30).\n\n## Progress — 2026-08-30, steps 2-4\n\nGate green (`bash scripts/gate.sh`, exit 0). `contracts/substrate-wire/0.6.0/` (213 files) is the\nsuccessor; `0.4.0` and `0.5.0` both still reproduce byte for byte (`diff -r` empty each), and\n`git status --short contracts/` shows only the new directory.\n\n**Mechanism (c), and it really ran here.** `strace -f -e trace=execve` counts **5 `execve` of\n`/usr/bin/bwrap`** across the 9 `egress::tests::*` — these are not vacuous passes. 40 bytes were\ncounted through the forwarder. `Sandbox::open` deliberately refuses to skip: a sandbox that reports\nno `child-pid` is `expect`-ed, not returned as absent, because silently skipping is exactly how\nthese cases would pass without running.\n\n**A third silent-failure trap, in the same class as the spike's two but not named by it.**\n`std::io::pipe()` is `O_CLOEXEC`, so the first version of the mechanism tests passed in 0.00 s\nhaving opened no sandbox at all. Fixed with `pipe2(…, 0)` and a *written* barrier byte — closing the\nwrite end only releases bwrap once every forked copy of it is gone. Recorded in\n`docs/design/10a-egress-mechanism-spike.md` § 8.\n\n**Trap 2 bites, and a test now catches it.** Injecting `child.id()` in place of the `--info-fd`\n`child-pid` fails `the_mechanism_is_proven_in_a_throwaway_sandbox`. Trap 1 did **not** reproduce for\nthe throwaway argv — bwrap nested no second user namespace there — so `NS_GET_USERNS` is kept as the\nsuperset, not as a demonstrated fix. Say so rather than claim a fix that was never exercised.\n\n**`xtask/src/render.rs` can never be edited again without breaking `0.5.0`.** A bundle's\n`generator.digest` is the sha256 of that file (`xtask/src/render.rs:308-312`), so a successor cannot\nadd a `{\"$wire\": …}` binding there. `MAX_EGRESS_APERTURES` is bound from `xtask/src/bundle.rs`\ninstead, which no bundle hashes. Recorded in `AGENTS.md` § *The gate*.\n\n**The story and the ADR disagree on the fact name; the ADR won.** This story said\n`network.egress.apertures`; ADR 0013 § *Decision* and design 10 § 6 say `exec.egress-apertures`, and\nno fact name in `contracts/…/schemas/capability.json` carries two dots. Shipped as\n`exec.egress-apertures`.\n\n**Test brittleness found and fixed by the orchestrator, not the implementer.**\n`egress_defaults_to_none` pinned the exact errno — `Refused` for a host listener, `Unreachable` for a\npublic address. Under `strace -f` this host answers `Unreachable` where it otherwise answers\n`Refused`, and the test went red. Both outcomes prove the same guarantee, so the assertions now hold\n*did not reach* and report the observed variant. A test that pins the variant goes red on a\ndifferent kernel — including CI's — for something that is not a regression.\n\n**Defaults taken.**\n\n- Per-run read-only CA snapshot behind `--ca-bundle`. Unset means no anchor, so verification fails\n  inside the sandbox: **absent, not unverified** (invariant 3).\n- The forwarder listens on the *declared* port, so the generated `/etc/hosts` makes the operator's\n  own URL work unchanged.\n- `network: \"aperture\"` with no name keeps the frozen `exec.network-unserved`.\n- `exec.aperture-byte-limit` (design 10 § 5 row 5) **deferred**: bytes are counted and observed, the\n  ceiling is not enforced. That is the one row of design 10 § 5 this change does not deliver.\n\n## The delegated lane does run here — 2026-08-30\n\nThe implementer concluded it could not: `/sys/fs/cgroup/user.slice/user-1000.slice/session-3.scope`\nis root-owned and `mkdir` gives `Permission denied`, so `check_confined_apertures` and\n`DELEGATED_CASES = 42` reported themselves **absent** (invariant 3). That conclusion was about one\ncgroup path, not about the host. Asking systemd for a delegated scope needs no privilege:\n\n    $ bash scripts/delegated-lane.sh\n    delegated-lane: root /sys/fs/cgroup/…/app.slice/run-….scope, controllers cpu memory pids\n    runtime clean-room: 42 HTTP cases, startup refusal, and dual-daemon refusal passed (delegated lane)\n    test result: ok. 1 passed; 0 failed\n\nThe script wraps `systemd-run --user -p Delegate=yes --scope`, moves itself into a child group so\nthe delegation root stays process-free, and sets `SUBSTRATE_VECTORS_CGROUP_ROOT`. It is checked in\nprecisely so nobody reaches that conclusion again: **an absent lane and a green one read the same\nway if you only look at `cargo test`.**\n\n**Acceptance step 4 is therefore executed, not written-but-unrun.** `check_confined_apertures`\n(`crates/substrate-daemon/tests/runtime_vectors.rs:1656`) drives the model-free app-server on a\nloopback endpoint inside the aperture and a second endpoint outside it, and it is one of the 42.\nPortable lane 29/29 separately.\n\n**Deferred and not covered by this story's acceptance:** `exec.aperture-byte-limit`\n(design 10 § 5 row 5). Bytes are counted and observed; the ceiling is not enforced. That is the one\nrow of design 10 § 5 this change does not deliver, and it wants its own story.\n"},"command":"update-entity","target":"01MEM0000000000000019"},"payload":{"actor":"human:timo","at":1788054766226,"causation":"cmd-protocol-artifact-body-story-destination-bound-egress-1788054766226","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body","event_id":"story:destination-bound-egress@9#0~469998e3b5136660","executor":null,"recorded_at":"2026-08-30T01:52:46Z"}}
{"entity":"story","version":1,"id":"sealed-secret-slots","revision":6,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"artifact","reference":"scripts/delegated-lane.sh","source":"PR #16, squash 95e6188","target":"01MEM0000000000000040"},"payload":{"actor":"human:timo","at":1788055140000,"causation":"cmd-evidence-story-sealed-secret-slots-artifact-1788055140597","change":{"change":"evidence","kind":"artifact","reference":"scripts/delegated-lane.sh","source":"PR #16, squash 95e6188"},"correlation":"protocol-artifact-evidence","event_id":"story:sealed-secret-slots@6#0~4f3d508c17bd31a3","executor":null,"recorded_at":"2026-08-30T01:59:00Z"}}
{"entity":"story","version":1,"id":"aperture-byte-ceiling","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"<!-- Starting point for a `story` artifact, seeded by `protocol artifact new story <name>`.\n     No frontmatter here on purpose: the `---` block is written by the CLI from the id, kind, status\n     and relations you gave it, and a second copy in this file would be the one that went stale.\n     Delete the italic guidance as you fill each section. -->\n\n# Story: <name>\n\n## Outcome\n\n*What is true for whom once this has shipped, in one sentence. If it names a component rather than a\nperson, it is a task — say what changes for someone.*\n\n## Context\n\n*Why this is worth doing now, and what it depends on. Link the epic or specification it comes from\nrather than restating it; the `derived_from` relation already carries the edge.*\n\n## Acceptance\n\n*The conditions under which this is done, each one something a person or a check can observe. \"Works\ncorrectly\" is not one of them.*\n\n## Out of Scope\n\n*What a reasonable reader would expect to be included and is not — the boundary that stops this\nstory quietly becoming an epic.*\n\n## Open Questions\n\n*What is still undecided, each with who decides it. A story carrying an unowned question is a story\nthat stalls without anybody noticing.*\n","summary":"Design 10 § 5 row 5 names exec.aperture-byte-limit; the counter exists, the ceiling does not.","title":"A declared aperture carries a byte ceiling that refuses mid-run"},"args":{"command":"create-entity","data":{"body":"<!-- Starting point for a `story` artifact, seeded by `protocol artifact new story <name>`.\n     No frontmatter here on purpose: the `---` block is written by the CLI from the id, kind, status\n     and relations you gave it, and a second copy in this file would be the one that went stale.\n     Delete the italic guidance as you fill each section. -->\n\n# Story: <name>\n\n## Outcome\n\n*What is true for whom once this has shipped, in one sentence. If it names a component rather than a\nperson, it is a task — say what changes for someone.*\n\n## Context\n\n*Why this is worth doing now, and what it depends on. Link the epic or specification it comes from\nrather than restating it; the `derived_from` relation already carries the edge.*\n\n## Acceptance\n\n*The conditions under which this is done, each one something a person or a check can observe. \"Works\ncorrectly\" is not one of them.*\n\n## Out of Scope\n\n*What a reasonable reader would expect to be included and is not — the boundary that stops this\nstory quietly becoming an epic.*\n\n## Open Questions\n\n*What is still undecided, each with who decides it. A story carrying an unowned question is a story\nthat stalls without anybody noticing.*\n","status":"draft","summary":"Design 10 § 5 row 5 names exec.aperture-byte-limit; the counter exists, the ceiling does not.","title":"A declared aperture carries a byte ceiling that refuses mid-run"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/aperture-byte-ceiling"},"payload":{"actor":"human:timo","at":1788055167203,"causation":"cmd-new-story-aperture-byte-ceiling","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:aperture-byte-ceiling@1#0~6f24edd42212cfaa","executor":null,"recorded_at":"2026-08-30T01:59:27Z"}}
{"entity":"story","version":1,"id":"aperture-byte-ceiling","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:byte-plane-completion"}]},"args":{"command":"create-relation","kind":"decomposes","source":"01MEM0000000000000096","target":"01MEM0000000000000001"},"payload":{"actor":"human:timo","at":1788055167207,"causation":"cmd-story-aperture-byte-ceiling-decomposes-epic-byte-plane-completion","change":{"change":"related","relation":"decomposes","target":"epic:byte-plane-completion"},"correlation":"protocol-artifact-relate","event_id":"story:aperture-byte-ceiling@1#0~874533c4d93eeef7","executor":null,"recorded_at":"2026-08-30T01:59:27Z"}}
{"entity":"story","version":1,"id":"aperture-byte-ceiling","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:byte-plane-completion"},{"depends_on":"story:destination-bound-egress"}]},"args":{"command":"create-relation","kind":"depends_on","source":"01MEM0000000000000096","target":"01MEM0000000000000019"},"payload":{"actor":"human:timo","at":1788055167210,"causation":"cmd-story-aperture-byte-ceiling-depends_on-story-destination-bound-egress","change":{"change":"related","relation":"depends_on","target":"story:destination-bound-egress"},"correlation":"protocol-artifact-relate","event_id":"story:aperture-byte-ceiling@1#0~45bad6ea93e1201d","executor":null,"recorded_at":"2026-08-30T01:59:27Z"}}
{"entity":"story","version":1,"id":"aperture-byte-ceiling","revision":2,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"# Story: A declared aperture carries a byte ceiling that refuses mid-run\n\n## Outcome\n\nAn operator who declares an aperture can bound what crosses it, and a run that exceeds the bound is\nstopped with a named refusal rather than left to drain the link. Today the bytes are counted and\nreported but nothing reads them, so a confined run's egress volume is observable after the fact and\nunbounded during it.\n\n## Context\n\n`docs/design/10-destination-bound-egress.md:126` names the condition and its answer:\n*Declared byte ceiling exceeded mid-run* → class `exhausted`, code `exec.aperture-byte-limit`, no\naddress. `:284-287` records why it did not ship with ADR 0013: *\"There is no declared byte ceiling\nin the configuration surface, so there is nothing to exceed.\"*\n\nVerified in the tree, not inferred — the half that exists:\n\n- `ApertureBytes { to_destination, from_destination }`, `crates/substrate-wire/src/lib.rs:790-795`.\n- The forwarder holds both counters as `AtomicU64` and the parent reads them without synchronising,\n  `crates/substrate-host/src/egress.rs:621-622` and `:252-256`.\n- Every applied observation already carries them, `crates/substrate-host/src/egress.rs:288-297`.\n\nThe half that does not:\n\n- `EgressAperture { name, host, port, pinned }`, `crates/substrate-host/src/egress.rs:83-88` — four\n  fields, no ceiling.\n- `--egress-aperture name=host:port/tcp`, `crates/substrate-daemon/src/main.rs:108`, parsed at\n  `:34-56` — the grammar has nowhere to put one.\n- Nothing reads `counters` for a comparison; the only readers are `applied()` and the tests.\n\nThe refusal class already exists: `Exhausted`, `crates/substrate-wire/src/lib.rs:132`.\n\n## Acceptance\n\nAn aperture declared with a ceiling refuses the run by name when the ceiling is crossed, and the\nsame aperture declared without one behaves exactly as it does today.\n\nEvidence that satisfies it, in order:\n\n1. An ADR fixing the declaration grammar, which direction(s) the ceiling counts, whether it is\n   per-run or per-aperture-lifetime, and what the child observes at the moment of refusal\n   (invariant 8: design before code).\n2. A successor bundle carrying the ceiling on the aperture capability fact and the\n   `exec.aperture-byte-limit` refusal class; earlier bundle bytes unchanged (invariant 6).\n3. A delegated-lane vector: a child that reads more than the declared ceiling from the pinned\n   destination ends `exhausted` with code `exec.aperture-byte-limit`, and the applied observation\n   reports at least the ceiling in `bytes`.\n4. A delegated-lane vector proving the negative: an aperture declared with no ceiling passes the\n   same traffic to completion, so the existing `declared-aperture-is-reachable` case\n   (`docs/design/10-destination-bound-egress.md:161`) is unchanged.\n5. A vector proving the ceiling is a *deployment* declaration and not request data — a request that\n   carries a ceiling is refused, the same way a raw destination is\n   (`exec.aperture-destination-in-request`, `docs/design/10-destination-bound-egress.md:124`).\n\n## Out of Scope\n\nRate limits, time ceilings, and any ceiling on the `none` network mode — there is no forwarder there\nto count in.\n\n## Open\n\nWhether the ceiling is enforced in the forwarder (it holds the counters, and can stop reading) or by\nthe parent watching the atomics (it can end the operation with the right class but cannot stop the\nbytes already in flight). The ADR decides; both are consistent with the counters as they stand.\n"},"args":{"changes":{"body":"# Story: A declared aperture carries a byte ceiling that refuses mid-run\n\n## Outcome\n\nAn operator who declares an aperture can bound what crosses it, and a run that exceeds the bound is\nstopped with a named refusal rather than left to drain the link. Today the bytes are counted and\nreported but nothing reads them, so a confined run's egress volume is observable after the fact and\nunbounded during it.\n\n## Context\n\n`docs/design/10-destination-bound-egress.md:126` names the condition and its answer:\n*Declared byte ceiling exceeded mid-run* → class `exhausted`, code `exec.aperture-byte-limit`, no\naddress. `:284-287` records why it did not ship with ADR 0013: *\"There is no declared byte ceiling\nin the configuration surface, so there is nothing to exceed.\"*\n\nVerified in the tree, not inferred — the half that exists:\n\n- `ApertureBytes { to_destination, from_destination }`, `crates/substrate-wire/src/lib.rs:790-795`.\n- The forwarder holds both counters as `AtomicU64` and the parent reads them without synchronising,\n  `crates/substrate-host/src/egress.rs:621-622` and `:252-256`.\n- Every applied observation already carries them, `crates/substrate-host/src/egress.rs:288-297`.\n\nThe half that does not:\n\n- `EgressAperture { name, host, port, pinned }`, `crates/substrate-host/src/egress.rs:83-88` — four\n  fields, no ceiling.\n- `--egress-aperture name=host:port/tcp`, `crates/substrate-daemon/src/main.rs:108`, parsed at\n  `:34-56` — the grammar has nowhere to put one.\n- Nothing reads `counters` for a comparison; the only readers are `applied()` and the tests.\n\nThe refusal class already exists: `Exhausted`, `crates/substrate-wire/src/lib.rs:132`.\n\n## Acceptance\n\nAn aperture declared with a ceiling refuses the run by name when the ceiling is crossed, and the\nsame aperture declared without one behaves exactly as it does today.\n\nEvidence that satisfies it, in order:\n\n1. An ADR fixing the declaration grammar, which direction(s) the ceiling counts, whether it is\n   per-run or per-aperture-lifetime, and what the child observes at the moment of refusal\n   (invariant 8: design before code).\n2. A successor bundle carrying the ceiling on the aperture capability fact and the\n   `exec.aperture-byte-limit` refusal class; earlier bundle bytes unchanged (invariant 6).\n3. A delegated-lane vector: a child that reads more than the declared ceiling from the pinned\n   destination ends `exhausted` with code `exec.aperture-byte-limit`, and the applied observation\n   reports at least the ceiling in `bytes`.\n4. A delegated-lane vector proving the negative: an aperture declared with no ceiling passes the\n   same traffic to completion, so the existing `declared-aperture-is-reachable` case\n   (`docs/design/10-destination-bound-egress.md:161`) is unchanged.\n5. A vector proving the ceiling is a *deployment* declaration and not request data — a request that\n   carries a ceiling is refused, the same way a raw destination is\n   (`exec.aperture-destination-in-request`, `docs/design/10-destination-bound-egress.md:124`).\n\n## Out of Scope\n\nRate limits, time ceilings, and any ceiling on the `none` network mode — there is no forwarder there\nto count in.\n\n## Open\n\nWhether the ceiling is enforced in the forwarder (it holds the counters, and can stop reading) or by\nthe parent watching the atomics (it can end the operation with the right class but cannot stop the\nbytes already in flight). The ADR decides; both are consistent with the counters as they stand.\n"},"command":"update-entity","target":"01MEM0000000000000013"},"payload":{"actor":"human:timo","at":1788055215406,"causation":"cmd-protocol-artifact-body-story-aperture-byte-ceiling-1788055215406","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body","event_id":"story:aperture-byte-ceiling@2#0~953335b3cc1aa516","executor":null,"recorded_at":"2026-08-30T02:00:15Z"}}
{"entity":"story","version":1,"id":"aperture-byte-ceiling","revision":3,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"# Story: A declared aperture carries a byte ceiling that refuses mid-run\n\n## Outcome\n\nAn operator who declares an aperture can bound what crosses it, and a run that exceeds the bound is\nstopped with a named refusal rather than left to drain the link. Today the bytes are counted and\nreported but nothing reads them, so a confined run's egress volume is observable after the fact and\nunbounded during it.\n\n## Context\n\n`docs/design/10-destination-bound-egress.md:126` names the condition and its answer:\n*Declared byte ceiling exceeded mid-run* → class `exhausted`, code `exec.aperture-byte-limit`, no\naddress. `:284-287` records why it did not ship with ADR 0013: *\"There is no declared byte ceiling\nin the configuration surface, so there is nothing to exceed.\"*\n\nVerified in the tree, not inferred — the half that exists:\n\n- `ApertureBytes { to_destination, from_destination }`, `crates/substrate-wire/src/lib.rs:790-795`.\n- The forwarder holds both counters as `AtomicU64` and the parent reads them without synchronising,\n  `crates/substrate-host/src/egress.rs:621-622` and `:252-256`.\n- Every applied observation already carries them, `crates/substrate-host/src/egress.rs:288-297`.\n\nThe half that does not:\n\n- `EgressAperture { name, host, port, pinned }`, `crates/substrate-host/src/egress.rs:83-88` — four\n  fields, no ceiling.\n- `--egress-aperture name=host:port/tcp`, `crates/substrate-daemon/src/main.rs:108`, parsed at\n  `:34-56` — the grammar has nowhere to put one.\n- Nothing reads `counters` for a comparison; the only readers are `applied()` and the tests.\n\nThe refusal class already exists: `Exhausted`, `crates/substrate-wire/src/lib.rs:132`.\n\n## Acceptance\n\nAn aperture declared with a ceiling refuses the run by name when the ceiling is crossed, and the\nsame aperture declared without one behaves exactly as it does today.\n\nEvidence that satisfies it, in order:\n\n1. An ADR fixing the declaration grammar, which direction(s) the ceiling counts, whether it is\n   per-run or per-aperture-lifetime, and what the child observes at the moment of refusal\n   (invariant 8: design before code).\n2. A successor bundle carrying the ceiling on the aperture capability fact and the\n   `exec.aperture-byte-limit` refusal class; earlier bundle bytes unchanged (invariant 6).\n3. A delegated-lane vector: a child that reads more than the declared ceiling from the pinned\n   destination ends `exhausted` with code `exec.aperture-byte-limit`, and the applied observation\n   reports at least the ceiling in `bytes`.\n4. A delegated-lane vector proving the negative: an aperture declared with no ceiling passes the\n   same traffic to completion, so the existing `declared-aperture-is-reachable` case\n   (`docs/design/10-destination-bound-egress.md:161`) is unchanged.\n5. A vector proving the ceiling is a *deployment* declaration and not request data — a request that\n   carries a ceiling is refused, the same way a raw destination is\n   (`exec.aperture-destination-in-request`, `docs/design/10-destination-bound-egress.md:124`).\n\n## Out of Scope\n\nRate limits, time ceilings, and any ceiling on the `none` network mode — there is no forwarder there\nto count in.\n\n## Open\n\nWhether the ceiling is enforced in the forwarder (it holds the counters, and can stop reading) or by\nthe parent watching the atomics (it can end the operation with the right class but cannot stop the\nbytes already in flight). The ADR decides; both are consistent with the counters as they stand.\n\n## Design draft — 2026-08-30\n\n`docs/design/12-aperture-byte-ceiling.md`, **proposed**. No ADR number is claimed: `adr/` admits\n`accepted` and `superseded` only (`xtask/src/adrs.rs:12`), so the number is assigned at acceptance.\nDesign 10's deferred note now points at it instead of describing an open gap.\n\nDecisions it fixes, each with what the alternative would have cost:\n\n| Decision | Chosen | Alternative's cost |\n|---|---|---|\n| Grammar | optional `/max=<size>` term on `--egress-aperture name=host:port/tcp`; absent = today byte for byte | a separate flag lets a ceiling name an aperture that does not exist; comma is unavailable either way (`crates/substrate-daemon/src/main.rs:116`, `value_delimiter = ','`) |\n| Direction | one ceiling over `to_destination + from_destination` summed | a per-direction pair is two numbers to get wrong and a bound a child evades by picking a direction |\n| Scope | per run | per-lifetime needs durable cross-run accounting and makes a refusal unreproducible from its own request |\n| Enforcement | in the relay, which stops at the ceiling; overshoot bounded by `RELAY_BUFFER = 16_384` (`crates/substrate-host/src/egress.rs:68`) | parent-only stops no byte: overshoot is bounded by the destination's throughput, not by substrate |\n\n**Gap the draft found, verified not inferred.** At HEAD a mid-run bound has no code to report. Both\nexisting mid-run bounds fold into one state: `forced_cancellation = timed_out || cpu_exhausted ||\ncancellation_requested` (`crates/substrate-host/src/process.rs:1318-1319`) sets\n`ExecState::Cancelled` (`:1364`). The supervision loop that would notice a ceiling already exists\nand already polls at 1 ms (`:1392`, `tokio::time::interval(Duration::from_millis(1))`), so the\nmechanism is there and the vocabulary is not — `exhausted` / `exec.aperture-byte-limit`\n(`docs/design/10-destination-bound-egress.md:126`) has nowhere to live in today's observation. The\ndraft therefore also proposes an optional class/code/message field on the exec observation, which is\nwhat makes this a contract change rather than a host-local one.\n\n## Blocked on\n\nOperator acceptance of design 12. Invariant 8: an ADR before code. Nothing here is implementable\nuntil that decision is made.\n"},"args":{"changes":{"body":"# Story: A declared aperture carries a byte ceiling that refuses mid-run\n\n## Outcome\n\nAn operator who declares an aperture can bound what crosses it, and a run that exceeds the bound is\nstopped with a named refusal rather than left to drain the link. Today the bytes are counted and\nreported but nothing reads them, so a confined run's egress volume is observable after the fact and\nunbounded during it.\n\n## Context\n\n`docs/design/10-destination-bound-egress.md:126` names the condition and its answer:\n*Declared byte ceiling exceeded mid-run* → class `exhausted`, code `exec.aperture-byte-limit`, no\naddress. `:284-287` records why it did not ship with ADR 0013: *\"There is no declared byte ceiling\nin the configuration surface, so there is nothing to exceed.\"*\n\nVerified in the tree, not inferred — the half that exists:\n\n- `ApertureBytes { to_destination, from_destination }`, `crates/substrate-wire/src/lib.rs:790-795`.\n- The forwarder holds both counters as `AtomicU64` and the parent reads them without synchronising,\n  `crates/substrate-host/src/egress.rs:621-622` and `:252-256`.\n- Every applied observation already carries them, `crates/substrate-host/src/egress.rs:288-297`.\n\nThe half that does not:\n\n- `EgressAperture { name, host, port, pinned }`, `crates/substrate-host/src/egress.rs:83-88` — four\n  fields, no ceiling.\n- `--egress-aperture name=host:port/tcp`, `crates/substrate-daemon/src/main.rs:108`, parsed at\n  `:34-56` — the grammar has nowhere to put one.\n- Nothing reads `counters` for a comparison; the only readers are `applied()` and the tests.\n\nThe refusal class already exists: `Exhausted`, `crates/substrate-wire/src/lib.rs:132`.\n\n## Acceptance\n\nAn aperture declared with a ceiling refuses the run by name when the ceiling is crossed, and the\nsame aperture declared without one behaves exactly as it does today.\n\nEvidence that satisfies it, in order:\n\n1. An ADR fixing the declaration grammar, which direction(s) the ceiling counts, whether it is\n   per-run or per-aperture-lifetime, and what the child observes at the moment of refusal\n   (invariant 8: design before code).\n2. A successor bundle carrying the ceiling on the aperture capability fact and the\n   `exec.aperture-byte-limit` refusal class; earlier bundle bytes unchanged (invariant 6).\n3. A delegated-lane vector: a child that reads more than the declared ceiling from the pinned\n   destination ends `exhausted` with code `exec.aperture-byte-limit`, and the applied observation\n   reports at least the ceiling in `bytes`.\n4. A delegated-lane vector proving the negative: an aperture declared with no ceiling passes the\n   same traffic to completion, so the existing `declared-aperture-is-reachable` case\n   (`docs/design/10-destination-bound-egress.md:161`) is unchanged.\n5. A vector proving the ceiling is a *deployment* declaration and not request data — a request that\n   carries a ceiling is refused, the same way a raw destination is\n   (`exec.aperture-destination-in-request`, `docs/design/10-destination-bound-egress.md:124`).\n\n## Out of Scope\n\nRate limits, time ceilings, and any ceiling on the `none` network mode — there is no forwarder there\nto count in.\n\n## Open\n\nWhether the ceiling is enforced in the forwarder (it holds the counters, and can stop reading) or by\nthe parent watching the atomics (it can end the operation with the right class but cannot stop the\nbytes already in flight). The ADR decides; both are consistent with the counters as they stand.\n\n## Design draft — 2026-08-30\n\n`docs/design/12-aperture-byte-ceiling.md`, **proposed**. No ADR number is claimed: `adr/` admits\n`accepted` and `superseded` only (`xtask/src/adrs.rs:12`), so the number is assigned at acceptance.\nDesign 10's deferred note now points at it instead of describing an open gap.\n\nDecisions it fixes, each with what the alternative would have cost:\n\n| Decision | Chosen | Alternative's cost |\n|---|---|---|\n| Grammar | optional `/max=<size>` term on `--egress-aperture name=host:port/tcp`; absent = today byte for byte | a separate flag lets a ceiling name an aperture that does not exist; comma is unavailable either way (`crates/substrate-daemon/src/main.rs:116`, `value_delimiter = ','`) |\n| Direction | one ceiling over `to_destination + from_destination` summed | a per-direction pair is two numbers to get wrong and a bound a child evades by picking a direction |\n| Scope | per run | per-lifetime needs durable cross-run accounting and makes a refusal unreproducible from its own request |\n| Enforcement | in the relay, which stops at the ceiling; overshoot bounded by `RELAY_BUFFER = 16_384` (`crates/substrate-host/src/egress.rs:68`) | parent-only stops no byte: overshoot is bounded by the destination's throughput, not by substrate |\n\n**Gap the draft found, verified not inferred.** At HEAD a mid-run bound has no code to report. Both\nexisting mid-run bounds fold into one state: `forced_cancellation = timed_out || cpu_exhausted ||\ncancellation_requested` (`crates/substrate-host/src/process.rs:1318-1319`) sets\n`ExecState::Cancelled` (`:1364`). The supervision loop that would notice a ceiling already exists\nand already polls at 1 ms (`:1392`, `tokio::time::interval(Duration::from_millis(1))`), so the\nmechanism is there and the vocabulary is not — `exhausted` / `exec.aperture-byte-limit`\n(`docs/design/10-destination-bound-egress.md:126`) has nowhere to live in today's observation. The\ndraft therefore also proposes an optional class/code/message field on the exec observation, which is\nwhat makes this a contract change rather than a host-local one.\n\n## Blocked on\n\nOperator acceptance of design 12. Invariant 8: an ADR before code. Nothing here is implementable\nuntil that decision is made.\n"},"command":"update-entity","target":"01MEM0000000000000013"},"payload":{"actor":"human:timo","at":1788055840029,"causation":"cmd-protocol-artifact-body-story-aperture-byte-ceiling-1788055840029","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body","event_id":"story:aperture-byte-ceiling@3#0~c57e640f9a938c4d","executor":null,"recorded_at":"2026-08-30T02:10:40Z"}}
{"entity":"story","version":1,"id":"aperture-byte-ceiling","revision":3,"type":"aep.evidence.record/v1","from_state":"draft","to_state":"draft","changed":{},"args":{"command":"record-evidence","kind":"specification","reference":"docs/design/12-aperture-byte-ceiling.md","source":"design 12, proposed","target":"01MEM0000000000000013"},"payload":{"actor":"human:timo","at":1788055840000,"causation":"cmd-evidence-story-aperture-byte-ceiling-specification-1788055840047","change":{"change":"evidence","kind":"specification","reference":"docs/design/12-aperture-byte-ceiling.md","source":"design 12, proposed"},"correlation":"protocol-artifact-evidence","event_id":"story:aperture-byte-ceiling@3#0~5c28920e79d4a68b","executor":null,"recorded_at":"2026-08-30T02:10:40Z"}}
{"entity":"story","version":1,"id":"contract-bundle-oci-artifact","revision":7,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"body":"# Story: The 0.4.0 contract bundle is a signed, digest-pinned OCI artifact\n\n## Outcome\n\nA consumer pins the wire contract by OCI digest instead of copying a directory tree.\n\n## Context\n\n`docs/plan/04-direct-byte-plane.md` § *Slice C* says Agent \"consumes an exact independently\nverified copy\"; the copy is made by hand. `docs/design/07-specification-and-conformance.md`\n§ *Bundle* already fixes the packaging: `bundle.json` lists media type, byte length and digest of\nevery bundle path except itself, and the outer OCI manifest digest pins `bundle.json`. Depends on\n`story:signed-daemon-image` for the release workflow it publishes through.\n\n## Acceptance\n\n`cargo xtask package-bundle <version>` produces a deterministic OCI layout from\n`contracts/substrate-wire/<version>/` without writing into `contracts/`, two runs yield\nbyte-identical manifests, and the release workflow publishes and signs it at\n`ghcr.io/beyond10x/b10x-substrate-wire:<bundle-version>` with the digest in `CHANGELOG.md`.\n\nEvidence that satisfies it:\n\n- `xtask/src/package.rs` tests: identical manifests on two runs; a one-byte change in\n  any bundle file changes the manifest digest — written failing-first against a stub that embeds\n  a timestamp;\n- `bundle.json` matches design 07's shape and a checker asserts each digest and byte length;\n- `check-contract-bundle-0.4.0.py` and every earlier checker stay green (invariant 6: the packager\n  reads the bundle; the bundle does not learn about the packager);\n- the artifact is annotated `development`; publication does not make the bundle stable (atlas ADR\n  0019 governs contract release);\n- `bash scripts/gate.sh` exits 0.\n\n## Progress — 2026-08-29 (packager half)\n\n- `cargo xtask package-bundle <version> --out <dir>`: OCI Image Layout (`oci-layout`,\n  `index.json`, `blobs/sha256/…`); config **is** `bundle.json` verbatim, media type\n  `application/vnd.b10x.substrate-wire.bundle.v1+json`, so the manifest digest pins\n  `bundle.json` (design 07 § *Bundle*); one layer per bundle file, layer digest = the `sha256`\n  already in `bundle.json`; annotations `org.opencontainers.image.version`,\n  `dev.b10x.contract.status=development`, `ref.name` on the index entry. Refuses `--out` inside\n  `contracts/`, non-empty `--out` without `--force`, symlinks, a file set disagreeing with\n  `bundle.json`.\n- 14 tests; determinism test written failing-first\n  against a timestamp-embedding stub (`FAILED (failures=1)`), then green. Two runs on `0.4.0` →\n  `sha256:f94d15fc116587d991aab6de5628f6ee5baf872af4e7c79d2a35e2b17a8485c4`, `diff -r` empty.\n  `oras manifest fetch --oci-layout` resolves the same digest; `oras pull` round-trip matches\n  `contracts/substrate-wire/0.4.0` byte for byte.\n- `check-contract-bundle-0.4.0.py` → exit 0; `git status contracts/` → clean (invariant 6).\n- Gated by `cargo test --workspace` (`scripts/gate.sh:15`), not by a gate line of its own.\n- The `posix-tar` archive `packaging.json` declares is now the last layer (media type\n  `application/vnd.b10x.substrate-wire.bundle.tar`, title `0.4.0.tar`): ustar, directory entries\n  0755 ahead of files 0644, uid/gid 0, empty names, bytewise order, every mtime =\n  `SOURCE_DATE_EPOCH` = author seconds of the last commit touching the bundle (`1787605082`,\n  commit `4b5d411`; `--source-date-epoch` overrides, absent both → exit 2). Per-file layers and the\n  config are byte-identical to before; the manifest digest moves by design to\n  `sha256:3758e80bc39f1eb03b15c69410608c9ef1d2ba8095c7e707c6988dbb5894ab00`\n  (archive `sha256:91fb5524…`, 880,640 bytes). 21 tests; `tar -xf` of the blob reproduces\n  `contracts/substrate-wire/0.4.0` byte for byte. On a shallow CI checkout the epoch would be the\n  tip's author time — pin `--source-date-epoch` there.\n- **Open:** the publish/sign half — `release.yml`, `ghcr.io/beyond10x/b10x-substrate-wire`,\n  cosign, digest in `CHANGELOG.md` — waits on `story:signed-daemon-image`.\n\n## Correction — 2026-08-30\n\nThe two Python names this story was written against no longer exist. `ef22de0` (\"the bundle\npackager is cargo xtask package-bundle, and its Python is gone\") ported the packager and deleted\n`scripts/package-contract-bundle.py` and `scripts/test_package_contract_bundle.py`. Acceptance and\nthe progress notes above now name the Rust verb; the behaviour they assert is unchanged, and the\n`0.4.0` manifest digests recorded above were observed against the Python and have not been\nre-observed against the verb.\n\nCoverage is not lost in the move: `xtask/src/package.rs` carries 21 `#[test]`s (was 21 Python\ntests), and `cargo test --workspace` at `scripts/gate.sh:15` runs them. There is no\n`package-bundle` line in `scripts/gate.sh` and none is needed.\n\n**Still open, unchanged:** the publish/sign half — `release.yml`,\n`ghcr.io/beyond10x/b10x-substrate-wire`, cosign, digest in `CHANGELOG.md`. It waits on\n`story:signed-daemon-image`, which is a draft. This story cannot reach `implemented` until that\none is designed and built.\n"},"args":{"changes":{"body":"# Story: The 0.4.0 contract bundle is a signed, digest-pinned OCI artifact\n\n## Outcome\n\nA consumer pins the wire contract by OCI digest instead of copying a directory tree.\n\n## Context\n\n`docs/plan/04-direct-byte-plane.md` § *Slice C* says Agent \"consumes an exact independently\nverified copy\"; the copy is made by hand. `docs/design/07-specification-and-conformance.md`\n§ *Bundle* already fixes the packaging: `bundle.json` lists media type, byte length and digest of\nevery bundle path except itself, and the outer OCI manifest digest pins `bundle.json`. Depends on\n`story:signed-daemon-image` for the release workflow it publishes through.\n\n## Acceptance\n\n`cargo xtask package-bundle <version>` produces a deterministic OCI layout from\n`contracts/substrate-wire/<version>/` without writing into `contracts/`, two runs yield\nbyte-identical manifests, and the release workflow publishes and signs it at\n`ghcr.io/beyond10x/b10x-substrate-wire:<bundle-version>` with the digest in `CHANGELOG.md`.\n\nEvidence that satisfies it:\n\n- `xtask/src/package.rs` tests: identical manifests on two runs; a one-byte change in\n  any bundle file changes the manifest digest — written failing-first against a stub that embeds\n  a timestamp;\n- `bundle.json` matches design 07's shape and a checker asserts each digest and byte length;\n- `check-contract-bundle-0.4.0.py` and every earlier checker stay green (invariant 6: the packager\n  reads the bundle; the bundle does not learn about the packager);\n- the artifact is annotated `development`; publication does not make the bundle stable (atlas ADR\n  0019 governs contract release);\n- `bash scripts/gate.sh` exits 0.\n\n## Progress — 2026-08-29 (packager half)\n\n- `cargo xtask package-bundle <version> --out <dir>`: OCI Image Layout (`oci-layout`,\n  `index.json`, `blobs/sha256/…`); config **is** `bundle.json` verbatim, media type\n  `application/vnd.b10x.substrate-wire.bundle.v1+json`, so the manifest digest pins\n  `bundle.json` (design 07 § *Bundle*); one layer per bundle file, layer digest = the `sha256`\n  already in `bundle.json`; annotations `org.opencontainers.image.version`,\n  `dev.b10x.contract.status=development`, `ref.name` on the index entry. Refuses `--out` inside\n  `contracts/`, non-empty `--out` without `--force`, symlinks, a file set disagreeing with\n  `bundle.json`.\n- 14 tests; determinism test written failing-first\n  against a timestamp-embedding stub (`FAILED (failures=1)`), then green. Two runs on `0.4.0` →\n  `sha256:f94d15fc116587d991aab6de5628f6ee5baf872af4e7c79d2a35e2b17a8485c4`, `diff -r` empty.\n  `oras manifest fetch --oci-layout` resolves the same digest; `oras pull` round-trip matches\n  `contracts/substrate-wire/0.4.0` byte for byte.\n- `check-contract-bundle-0.4.0.py` → exit 0; `git status contracts/` → clean (invariant 6).\n- Gated by `cargo test --workspace` (`scripts/gate.sh:15`), not by a gate line of its own.\n- The `posix-tar` archive `packaging.json` declares is now the last layer (media type\n  `application/vnd.b10x.substrate-wire.bundle.tar`, title `0.4.0.tar`): ustar, directory entries\n  0755 ahead of files 0644, uid/gid 0, empty names, bytewise order, every mtime =\n  `SOURCE_DATE_EPOCH` = author seconds of the last commit touching the bundle (`1787605082`,\n  commit `4b5d411`; `--source-date-epoch` overrides, absent both → exit 2). Per-file layers and the\n  config are byte-identical to before; the manifest digest moves by design to\n  `sha256:3758e80bc39f1eb03b15c69410608c9ef1d2ba8095c7e707c6988dbb5894ab00`\n  (archive `sha256:91fb5524…`, 880,640 bytes). 21 tests; `tar -xf` of the blob reproduces\n  `contracts/substrate-wire/0.4.0` byte for byte. On a shallow CI checkout the epoch would be the\n  tip's author time — pin `--source-date-epoch` there.\n- **Open:** the publish/sign half — `release.yml`, `ghcr.io/beyond10x/b10x-substrate-wire`,\n  cosign, digest in `CHANGELOG.md` — waits on `story:signed-daemon-image`.\n\n## Correction — 2026-08-30\n\nThe two Python names this story was written against no longer exist. `ef22de0` (\"the bundle\npackager is cargo xtask package-bundle, and its Python is gone\") ported the packager and deleted\n`scripts/package-contract-bundle.py` and `scripts/test_package_contract_bundle.py`. Acceptance and\nthe progress notes above now name the Rust verb; the behaviour they assert is unchanged, and the\n`0.4.0` manifest digests recorded above were observed against the Python and have not been\nre-observed against the verb.\n\nCoverage is not lost in the move: `xtask/src/package.rs` carries 21 `#[test]`s (was 21 Python\ntests), and `cargo test --workspace` at `scripts/gate.sh:15` runs them. There is no\n`package-bundle` line in `scripts/gate.sh` and none is needed.\n\n**Still open, unchanged:** the publish/sign half — `release.yml`,\n`ghcr.io/beyond10x/b10x-substrate-wire`, cosign, digest in `CHANGELOG.md`. It waits on\n`story:signed-daemon-image`, which is a draft. This story cannot reach `implemented` until that\none is designed and built.\n"},"command":"update-entity","target":"01MEM0000000000000019"},"payload":{"actor":"human:timo","at":1788056143055,"causation":"cmd-protocol-artifact-body-story-contract-bundle-oci-artifact-1788056143055","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body","event_id":"story:contract-bundle-oci-artifact@7#0~428ea88dab73ca9e","executor":null,"recorded_at":"2026-08-30T02:15:43Z"}}
{"entity":"story","version":1,"id":"pty-sessions","revision":3,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"# Story: PTY sessions are a distinct session kind with resize\n\n## Outcome\n\nA human can attach a terminal to a confined process, resize it, and get the same lease, single\nattachment, bounded-output and whole-tree cleanup guarantees the raw-pipe session gives.\n\n## Context\n\n`docs/design/05-streams-sessions-and-endpoints.md` § 2 fixes the `pty` kind and its frames\n(input, output, resize, signal, exit, protocol-error) and says a PTY is never substituted for\npipes. `README.md` § *Status* lists PTY as **absent**; `docs/plan/04-direct-byte-plane.md`\n§ *Later phase-4 slices* lists it as remaining. Invariant 8 puts the ADR before code; invariant 6\nputs the wire change in a successor bundle.\n\n## Acceptance\n\nThe delegated lane runs an interactive shell through a `pty` session, echoes bytes, applies a\nresize the child observes, and exits on hangup — while the portable lane proves the typed\n`unserved` and every `0.4.0` byte is unchanged.\n\nEvidence that satisfies it, in order:\n\n1. A design document in `docs/design/`, proposed, fixing kind, frame set, resize bounds, never a\n   substitute for pipes, and the named refusal when the host cannot allocate a pty. It claims no\n   ADR number: `adr/` admits `accepted` and `superseded` only (`xtask/src/adrs.rs:12`), so the\n   number is assigned at acceptance.\n2. The next free successor bundle under `contracts/substrate-wire/` adds the `pty` kind and\n   `resize` frame; compatibility block names its predecessor with exact\n   `adds_routes`/`preserves_routes`; authored source under `xtask/bundle-source/<version>/` and\n   `cargo xtask check-bundle <version>` in `scripts/gate.sh`.\n3. `crates/substrate-host`: pty opened with `nix::pty::openpty` inside the existing\n   bubblewrap/cgroup/cleared-environment path. Failing-first tests:\n   `pty_session_refused_without_confinement`, `pty_resize_is_applied_and_observed` (child reads\n   `TIOCGWINSZ`), `pty_session_kills_tree_on_attachment_loss`.\n4. Daemon: the single-attachment Unix-WebSocket route family serves `pty`; `resize` outside the\n   declared bounds is a protocol error.\n5. Capability fact `sessions.pty` appears only after startup verified pty allocation (invariant 4).\n6. `STATUS.md`, `README.md` § *Status* and plan 04 § *Later phase-4 slices* updated in the same\n   change.\n\n## Open Questions\n\nBundle number: `0.5.0` if this lands first in the epic; the ADR states the actual number.\n\n## Correction — 2026-08-30\n\nThree claims in the evidence list above had gone stale and are rewritten, not deleted, so the\nchange is visible:\n\n| Claimed | Actually |\n|---|---|\n| `adr/0011-pty-sessions-are-a-distinct-session-kind.md` | `0011` is `adr/0011-delegated-context-and-grant-attribution.md`; the number was taken while this story sat in draft |\n| `contracts/substrate-wire/0.5.0/` | `0.5.0` is sealed secret slots and `0.6.0` is destination-bound egress; both are frozen (invariant 6) |\n| `render-contract-bundle-0.5.0.py`, `check-contract-bundle-0.5.0.py` | the Python renderer/checker pairs stop at `0.4.0`; from `0.5.0` on the check is `cargo xtask check-bundle <version>` (`scripts/gate.sh:24-28`) |\n\nThe story pins no numbers now, so it cannot go stale the same way again.\n"},"args":{"changes":{"body":"# Story: PTY sessions are a distinct session kind with resize\n\n## Outcome\n\nA human can attach a terminal to a confined process, resize it, and get the same lease, single\nattachment, bounded-output and whole-tree cleanup guarantees the raw-pipe session gives.\n\n## Context\n\n`docs/design/05-streams-sessions-and-endpoints.md` § 2 fixes the `pty` kind and its frames\n(input, output, resize, signal, exit, protocol-error) and says a PTY is never substituted for\npipes. `README.md` § *Status* lists PTY as **absent**; `docs/plan/04-direct-byte-plane.md`\n§ *Later phase-4 slices* lists it as remaining. Invariant 8 puts the ADR before code; invariant 6\nputs the wire change in a successor bundle.\n\n## Acceptance\n\nThe delegated lane runs an interactive shell through a `pty` session, echoes bytes, applies a\nresize the child observes, and exits on hangup — while the portable lane proves the typed\n`unserved` and every `0.4.0` byte is unchanged.\n\nEvidence that satisfies it, in order:\n\n1. A design document in `docs/design/`, proposed, fixing kind, frame set, resize bounds, never a\n   substitute for pipes, and the named refusal when the host cannot allocate a pty. It claims no\n   ADR number: `adr/` admits `accepted` and `superseded` only (`xtask/src/adrs.rs:12`), so the\n   number is assigned at acceptance.\n2. The next free successor bundle under `contracts/substrate-wire/` adds the `pty` kind and\n   `resize` frame; compatibility block names its predecessor with exact\n   `adds_routes`/`preserves_routes`; authored source under `xtask/bundle-source/<version>/` and\n   `cargo xtask check-bundle <version>` in `scripts/gate.sh`.\n3. `crates/substrate-host`: pty opened with `nix::pty::openpty` inside the existing\n   bubblewrap/cgroup/cleared-environment path. Failing-first tests:\n   `pty_session_refused_without_confinement`, `pty_resize_is_applied_and_observed` (child reads\n   `TIOCGWINSZ`), `pty_session_kills_tree_on_attachment_loss`.\n4. Daemon: the single-attachment Unix-WebSocket route family serves `pty`; `resize` outside the\n   declared bounds is a protocol error.\n5. Capability fact `sessions.pty` appears only after startup verified pty allocation (invariant 4).\n6. `STATUS.md`, `README.md` § *Status* and plan 04 § *Later phase-4 slices* updated in the same\n   change.\n\n## Open Questions\n\nBundle number: `0.5.0` if this lands first in the epic; the ADR states the actual number.\n\n## Correction — 2026-08-30\n\nThree claims in the evidence list above had gone stale and are rewritten, not deleted, so the\nchange is visible:\n\n| Claimed | Actually |\n|---|---|\n| `adr/0011-pty-sessions-are-a-distinct-session-kind.md` | `0011` is `adr/0011-delegated-context-and-grant-attribution.md`; the number was taken while this story sat in draft |\n| `contracts/substrate-wire/0.5.0/` | `0.5.0` is sealed secret slots and `0.6.0` is destination-bound egress; both are frozen (invariant 6) |\n| `render-contract-bundle-0.5.0.py`, `check-contract-bundle-0.5.0.py` | the Python renderer/checker pairs stop at `0.4.0`; from `0.5.0` on the check is `cargo xtask check-bundle <version>` (`scripts/gate.sh:24-28`) |\n\nThe story pins no numbers now, so it cannot go stale the same way again.\n"},"command":"update-entity","target":"01MEM0000000000000040"},"payload":{"actor":"human:timo","at":1788056197162,"causation":"cmd-protocol-artifact-body-story-pty-sessions-1788056197162","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body","event_id":"story:pty-sessions@3#0~5739817a7c991531","executor":null,"recorded_at":"2026-08-30T02:16:37Z"}}
{"entity":"story","version":1,"id":"secret-slot-probe-observes-what-it-claims","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"<!-- Starting point for a `story` artifact, seeded by `protocol artifact new story <name>`.\n     No frontmatter here on purpose: the `---` block is written by the CLI from the id, kind, status\n     and relations you gave it, and a second copy in this file would be the one that went stale.\n     Delete the italic guidance as you fill each section. -->\n\n# Story: <name>\n\n## Outcome\n\n*What is true for whom once this has shipped, in one sentence. If it names a component rather than a\nperson, it is a task — say what changes for someone.*\n\n## Context\n\n*Why this is worth doing now, and what it depends on. Link the epic or specification it comes from\nrather than restating it; the `derived_from` relation already carries the edge.*\n\n## Acceptance\n\n*The conditions under which this is done, each one something a person or a check can observe. \"Works\ncorrectly\" is not one of them.*\n\n## Out of Scope\n\n*What a reasonable reader would expect to be included and is not — the boundary that stops this\nstory quietly becoming an epic.*\n\n## Open Questions\n\n*What is still undecided, each with who decides it. A story carrying an unowned question is a story\nthat stalls without anybody noticing.*\n","summary":"Design 11 § 5 requires the probe child report the seals and nothing else above 2; the probe checks neither.","title":"The secret-slot probe observes the seals and descriptor set it publishes a fact about"},"args":{"command":"create-entity","data":{"body":"<!-- Starting point for a `story` artifact, seeded by `protocol artifact new story <name>`.\n     No frontmatter here on purpose: the `---` block is written by the CLI from the id, kind, status\n     and relations you gave it, and a second copy in this file would be the one that went stale.\n     Delete the italic guidance as you fill each section. -->\n\n# Story: <name>\n\n## Outcome\n\n*What is true for whom once this has shipped, in one sentence. If it names a component rather than a\nperson, it is a task — say what changes for someone.*\n\n## Context\n\n*Why this is worth doing now, and what it depends on. Link the epic or specification it comes from\nrather than restating it; the `derived_from` relation already carries the edge.*\n\n## Acceptance\n\n*The conditions under which this is done, each one something a person or a check can observe. \"Works\ncorrectly\" is not one of them.*\n\n## Out of Scope\n\n*What a reasonable reader would expect to be included and is not — the boundary that stops this\nstory quietly becoming an epic.*\n\n## Open Questions\n\n*What is still undecided, each with who decides it. A story carrying an unowned question is a story\nthat stalls without anybody noticing.*\n","status":"draft","summary":"Design 11 § 5 requires the probe child report the seals and nothing else above 2; the probe checks neither.","title":"The secret-slot probe observes the seals and descriptor set it publishes a fact about"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/secret-slot-probe-observes-what-it-claims"},"payload":{"actor":"human:timo","at":1788056472723,"causation":"cmd-new-story-secret-slot-probe-observes-what-it-claims","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:secret-slot-probe-observes-what-it-claims@1#0~848cb1da3128867c","executor":null,"recorded_at":"2026-08-30T02:21:12Z"}}
{"entity":"story","version":1,"id":"secret-slot-probe-observes-what-it-claims","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:byte-plane-completion"}]},"args":{"command":"create-relation","kind":"decomposes","source":"01MEM0000000000000103","target":"01MEM0000000000000001"},"payload":{"actor":"human:timo","at":1788056472754,"causation":"cmd-story-secret-slot-probe-observes-what-it-claims-decomposes-epic-byte-plane-completion","change":{"change":"related","relation":"decomposes","target":"epic:byte-plane-completion"},"correlation":"protocol-artifact-relate","event_id":"story:secret-slot-probe-observes-what-it-claims@1#0~a972aa942c985930","executor":null,"recorded_at":"2026-08-30T02:21:12Z"}}
{"entity":"story","version":1,"id":"secret-slot-probe-observes-what-it-claims","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:byte-plane-completion"},{"depends_on":"story:sealed-secret-slots"}]},"args":{"command":"create-relation","kind":"depends_on","source":"01MEM0000000000000103","target":"01MEM0000000000000043"},"payload":{"actor":"human:timo","at":1788056472761,"causation":"cmd-story-secret-slot-probe-observes-what-it-claims-depends_on-story-sealed-secret-slots","change":{"change":"related","relation":"depends_on","target":"story:sealed-secret-slots"},"correlation":"protocol-artifact-relate","event_id":"story:secret-slot-probe-observes-what-it-claims@1#0~6539f8b4f5365a4d","executor":null,"recorded_at":"2026-08-30T02:21:12Z"}}
{"entity":"story","version":1,"id":"secret-slot-probe-observes-what-it-claims","revision":2,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"# Story: The secret-slot probe observes the seals and descriptor set it publishes a fact about\n\n## Outcome\n\n`secrets.slots` means what design 11 says it means. Today the capability fact is published on a\nweaker observation than the one its design names, so a host whose bubblewrap passed a descriptor\nwith the wrong seals — or with extra descriptors leaked in — would still advertise the capability.\n\n## Context\n\n`docs/design/11-sealed-secret-slots.md:108-110` states the condition for publishing the fact:\n\n> a bubblewrap child reporting the probe descriptor at its declared number with **the same seals and\n> nothing else above 2**\n\nThe probe child does neither of those two things. Its whole command is\n`crates/substrate-host/src/probe.rs:296-298`:\n\n```\ncat <&{target}; if echo x >&{target} 2>/dev/null; then printf writable; else printf sealed; fi\n```\n\nand the acceptance is `output.stdout == format!(\"{sentinel}sealed\")`\n(`crates/substrate-host/src/probe.rs:313`). So the child proves the descriptor arrived at its number\nand is not writable. It never calls `fcntl(F_GET_SEALS)`, so \"the same seals\" is unobserved — a\ndescriptor sealed `F_SEAL_WRITE` alone would pass. It never enumerates `/proc/self/fd`, so \"nothing\nelse above 2\" is unobserved.\n\nThe in-process half of the condition *is* observed — `memfd_create` + `F_ADD_SEALS` with\n`F_GET_SEALS` reading back `0xf` and a refused write — and the parent enforces the rest by\nconstruction (`crates/substrate-host/src/secrets.rs`, `place_and_close`). Enforced by construction\nis not the same as observed, and invariant 3 is that a guarantee substrate cannot verify is a named\nrefusal, not a fact published anyway.\n\nThis is not a live exposure: `crates/substrate-daemon/tests/runtime_vectors.rs` now observes both\nproperties at run time on the delegated lane — the child reports `fds:[0,1,2,7]`, `memfds:[7]`,\n`seals:15`. The gap is that the *gating probe* does less than the test does, so the fact can be\npublished on a host where the test never ran.\n\nFound while proving `story:sealed-secret-slots` on the delegated lane, 2026-08-30.\n\n## Acceptance\n\nThe probe child reports its seal word and its descriptor set, and `secrets.slots` is withheld when\neither disagrees with what design 11 § 5 requires.\n\nEvidence that satisfies it:\n\n1. The probe child reads `F_GET_SEALS` on the target descriptor and enumerates `/proc/self/fd`, and\n   the parent compares both against the declared values — not against a substring of `cat` output.\n2. A failing-first test per property: a probe whose child reports a short seal word withholds the\n   fact; a probe whose child reports an extra descriptor withholds the fact. Both must fail before\n   the change and pass after.\n3. `secrets.slots` is still published on this host after the change (the probe did not become\n   unsatisfiable), proven by `bash scripts/delegated-lane.sh` still reporting its full case count.\n4. Whichever way it lands, `docs/design/11-sealed-secret-slots.md:108-110` and the code agree\n   afterwards. If the decision is instead that the design overstates what a probe can cheaply\n   observe, that is an amendment to an accepted ADR (0012) and needs the operator, not a quiet\n   edit.\n\n## Out of Scope\n\nChanging the seal set, the slot naming rule, or anything about how a slot reaches a child. This\nstory is only about what the probe observes before it publishes the fact.\n"},"args":{"changes":{"body":"# Story: The secret-slot probe observes the seals and descriptor set it publishes a fact about\n\n## Outcome\n\n`secrets.slots` means what design 11 says it means. Today the capability fact is published on a\nweaker observation than the one its design names, so a host whose bubblewrap passed a descriptor\nwith the wrong seals — or with extra descriptors leaked in — would still advertise the capability.\n\n## Context\n\n`docs/design/11-sealed-secret-slots.md:108-110` states the condition for publishing the fact:\n\n> a bubblewrap child reporting the probe descriptor at its declared number with **the same seals and\n> nothing else above 2**\n\nThe probe child does neither of those two things. Its whole command is\n`crates/substrate-host/src/probe.rs:296-298`:\n\n```\ncat <&{target}; if echo x >&{target} 2>/dev/null; then printf writable; else printf sealed; fi\n```\n\nand the acceptance is `output.stdout == format!(\"{sentinel}sealed\")`\n(`crates/substrate-host/src/probe.rs:313`). So the child proves the descriptor arrived at its number\nand is not writable. It never calls `fcntl(F_GET_SEALS)`, so \"the same seals\" is unobserved — a\ndescriptor sealed `F_SEAL_WRITE` alone would pass. It never enumerates `/proc/self/fd`, so \"nothing\nelse above 2\" is unobserved.\n\nThe in-process half of the condition *is* observed — `memfd_create` + `F_ADD_SEALS` with\n`F_GET_SEALS` reading back `0xf` and a refused write — and the parent enforces the rest by\nconstruction (`crates/substrate-host/src/secrets.rs`, `place_and_close`). Enforced by construction\nis not the same as observed, and invariant 3 is that a guarantee substrate cannot verify is a named\nrefusal, not a fact published anyway.\n\nThis is not a live exposure: `crates/substrate-daemon/tests/runtime_vectors.rs` now observes both\nproperties at run time on the delegated lane — the child reports `fds:[0,1,2,7]`, `memfds:[7]`,\n`seals:15`. The gap is that the *gating probe* does less than the test does, so the fact can be\npublished on a host where the test never ran.\n\nFound while proving `story:sealed-secret-slots` on the delegated lane, 2026-08-30.\n\n## Acceptance\n\nThe probe child reports its seal word and its descriptor set, and `secrets.slots` is withheld when\neither disagrees with what design 11 § 5 requires.\n\nEvidence that satisfies it:\n\n1. The probe child reads `F_GET_SEALS` on the target descriptor and enumerates `/proc/self/fd`, and\n   the parent compares both against the declared values — not against a substring of `cat` output.\n2. A failing-first test per property: a probe whose child reports a short seal word withholds the\n   fact; a probe whose child reports an extra descriptor withholds the fact. Both must fail before\n   the change and pass after.\n3. `secrets.slots` is still published on this host after the change (the probe did not become\n   unsatisfiable), proven by `bash scripts/delegated-lane.sh` still reporting its full case count.\n4. Whichever way it lands, `docs/design/11-sealed-secret-slots.md:108-110` and the code agree\n   afterwards. If the decision is instead that the design overstates what a probe can cheaply\n   observe, that is an amendment to an accepted ADR (0012) and needs the operator, not a quiet\n   edit.\n\n## Out of Scope\n\nChanging the seal set, the slot naming rule, or anything about how a slot reaches a child. This\nstory is only about what the probe observes before it publishes the fact.\n"},"command":"update-entity","target":"01MEM0000000000000046"},"payload":{"actor":"human:timo","at":1788056472776,"causation":"cmd-protocol-artifact-body-story-secret-slot-probe-observes-what-it-claims-1788056472776","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body","event_id":"story:secret-slot-probe-observes-what-it-claims@2#0~a1fba92ec8e5f902","executor":null,"recorded_at":"2026-08-30T02:21:12Z"}}
{"entity":"story","version":1,"id":"sealed-secret-slots","revision":7,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"body":"# Story: Sealed secret slots reach a child only as a memfd descriptor\n\n## Outcome\n\nA run can use a daemon-configured secret without the value ever appearing in argv, environment,\nlogs, events, the ledger request hash or an error body — and design 05's reason for refusing a\nlive vendor harness is removed on the secret side.\n\n## Context\n\n`docs/design/06-authentication-secrets-and-trust.md` § *V1 decisions* 3 fixes the mechanism: a\nsealed Linux `memfd` at a declared child descriptor; only the slot→descriptor mapping in the shaped\nenvironment; acquisition after every admission and dispatch-time check; the daemon closes its copy\nimmediately after spawn; a driver that cannot prove sealing, isolation and cleanup reports the\ncapability absent. Design 04 § 7 (line 79): errors may name a slot, never the value.\n\n## Acceptance\n\nThe delegated lane proves the child reads the value from the declared descriptor and the value\nbytes appear nowhere in captured argv, environment, stdout, stderr, event, ledger or diagnostic\nbytes; the portable lane proves the typed refusal.\n\nEvidence that satisfies it, in order:\n\n1. `adr/0012-secret-slots-are-sealed-memfds.md`: slot naming, descriptor declaration, the seal set\n   (`F_SEAL_WRITE|SHRINK|GROW|SEAL`), refusal classes.\n2. A successor bundle adds `secret_slots` on exec and session start, the capability fact\n   `secrets.slots`, and the refusal class; earlier bundle bytes unchanged.\n3. Failing-first tests: `secret_slot_value_absent_from_argv_env_events_and_ledger`,\n   `secret_slot_memfd_is_sealed` (child `fcntl(F_GET_SEALS)` equals the declared set),\n   `secret_slot_refused_when_sealing_unavailable`, `daemon_closes_its_copy_after_spawn`\n   (`/proc/<daemon>/fd` holds no memfd after spawn).\n4. The ledger request hash covers slot **names** only: two requests differing only in slot value\n   hash identically.\n\n## Out of Scope\n\nBrokered artifact secrets (design 06 decision 4) and destination-bound egress.\n\n## Design draft — 2026-08-30\n\n`docs/design/11-sealed-secret-slots.md` (proposed), with `adr/0012` text ready to accept. Verified\nat runtime, not inferred: bubblewrap 0.11.2 passes an inherited descriptor to the child at the\nsame number; a sealed memfd read back inside the sandbox reports `F_GET_SEALS == 0xf` and\n`pwrite → EPERM`. `libc` already exports `memfd_create` and the seal constants — no Cargo change.\nToday's `close_range` (`process.rs:348-377`) would close a slot fd; the draft generalises it to one\nrange per gap. Mapping variable `SUBSTRATE_SECRET_SLOTS=name=fd,…`; `MFD_CLOEXEC` mandatory.\n\n## Progress — 2026-08-30, steps 2-4 done\n\nGate green (`bash scripts/gate.sh`, exit 0), nothing under `contracts/` disturbed:\n`git status --short contracts/` shows only `?? contracts/substrate-wire/0.5.0/`, and\n`cargo xtask render-bundle 0.4.0` still reproduces the frozen tree byte for byte (`diff -r` empty,\nmanifest `sha256:002337bd…`).\n\n**Step 2 — the successor bundle.** `contracts/substrate-wire/0.5.0/`, 206 files, adding\n`secret_slots` on exec and session start, the capability fact `secrets.slots` and the refusal class.\nVerified as a fixed point of its own authored source: `cargo xtask check-bundle 0.5.0` →\n`contract bundle 0.5.0 verified: 206 files, fixed point of xtask/bundle-source/0.5.0`.\n\n**Step 3 — the four named tests, failing first.** Recorded before implementation:\nseals `left: -1 right: 15`; child digest `e3b0c44…` (empty read) against `fffca559…`;\n`/proc/self/fd still holds a slot memfd after spawn`; retained descriptors `[0,1,2,5]` against\n`[0,1,2,5,7]`. All five `secrets::tests::*` pass now, plus six `secret_slot_tests::*` in the wire\ncrate.\n\n**Step 4 — the request hash covers names only.**\n`secrets::tests::ledger_request_hash_covers_slot_names_only` passes: two requests differing only in\na slot's value hash identically.\n\n**A real bug the failing-first probe caught, not a test artefact.** `write_all` left the memfd\noffset at EOF, so a child's `read(fd)` returned 0 bytes — and `/proc/self/fd/N` masked it, because\nopening through `/proc` starts at offset 0. Fixed with `lseek(…, 0, SEEK_SET)` before sealing, with\na second child assertion guarding it. A daemon that shipped without this would have handed every\nchild an empty secret and reported success.\n\n**ADR 0012 is contradicted by the code in one place, and the code is right.** Design § 5 wants\n`secrets.slots` *absent* when orphan reconciliation fails. That state is unreachable: `probe()` runs\nbefore `ProcessRuntime::new` (`crates/substrate-host/src/lib.rs:446`), and a failed reconciliation\naborts `HostDriver::open`, so the daemon never starts to report anything. Recorded in `probe.rs`.\nAbsent-is-weaker, so nothing about this is optimistic.\n\n**Defaults taken.**\n\n- ADR § 9 named `scripts/check-contract-bundle-0.5.0.py`. The org language rule won:\n  `cargo xtask check-bundle <version>` instead, which re-renders from the authored source and\n  compares bytes — strictly stronger than a hand-written checker, because a released tree that has\n  stopped being the fixed point of its own source fails whatever else still looks well-formed.\n  Noted in `docs/design/11-sealed-secret-slots.md` § 9.\n- Invariant 4's `driver_port.rs` guard refused a re-export of `substrate_host::SecretSlot`, so the\n  daemon declares its own type and converts at the composition root.\n- `--secret-slot` validates the file at startup rather than at first use.\n- **`CONTRACT_BUNDLE` stays `substrate-wire/0.4.0`** (`crates/substrate-daemon/src/app.rs:3`). A\n  bundle exists to be pinned by a consumer before the server claims it; moving the\n  `x-b10x-contract` header is its own change with its own clients to notify. Same posture\n  `read_only_roots` already has — in the wire crate, in no bundle.\n\nTwo crate edges added, both already in the workspace: `zeroize` for `substrate-host`, `jsonschema`\nfor `xtask`. No new external dependency.\n\n**Not done.** No end-to-end daemon run with a live slot — there is no delegated cgroup on this host,\nso the four named tests are portable-lane, driving the real acquire/place/close code without a\nsandboxed child. `read_only_roots` is still absent from every bundle including `0.5.0`.\n\n## Progress — 2026-08-30, the delegated lane closes acceptance\n\n`PORTABLE_CASES` 29 → 31 and `DELEGATED_CASES` 42 → 48\n(`crates/substrate-daemon/tests/runtime_vectors.rs:2353-2354`). Both lanes re-run here, not taken\non report:\n\n- `bash scripts/delegated-lane.sh` → `runtime clean-room: 48 HTTP cases, startup refusal, and\n  dual-daemon refusal passed (delegated lane)`, exit 0, 16.47s.\n- `cargo test -p substrate-daemon --test runtime_vectors` → `31 HTTP cases … (portable lane)`,\n  exit 0, 2.59s.\n\nProven against the shipped binary over its socket, so nothing links the implementation: the\nconfined child reads its slot from the declared descriptor and returns a SHA-256 of the bytes; it\nreports `F_GET_SEALS` as the declared set and a write refused with `EPERM`; its descriptor table is\nexactly `{0,1,2}` plus its slot; the value appears in no captured argv, shaped environment, stdout,\nstderr, event page, ledger row, applied record, refusal body or daemon diagnostic; and\n`/proc/<daemon>/fd` holds no slot `memfd` while the child is still running and has not yet read.\nThe portable lane asserts `exec.secret-slots-unserved` and `exec.secret-slot-descriptor-invalid`.\n\nFailing-first, each perturbation reverted:\n\n| perturbation | failure |\n|---|---|\n| daemon started without `--secret-slot` | `left: 501 right: 200`, `exec.secret-slots-unserved` |\n| `SEAL_SET` drops `F_SEAL_GROW` (`crates/substrate-host/src/secrets.rs:33`) | `left: Number(11) right: 15`, read back by the child through the wire |\n| child echoes its slot value | `captured stdout carries the declared value` |\n| `drop(slots)` → `mem::forget` (`crates/substrate-host/src/process.rs:433`) | `left: [\"/memfd:substrate-slot-vendor_api_key (deleted)\", …×2] right: []` |\n\nThe slot value in the test is a synthetic literal, `not-a-credential-…`\n(`crates/substrate-daemon/tests/runtime_vectors.rs:2014`) — no real material in a public\nrepository. `bash scripts/check-secrets.sh` → `no leaks found`, 68 commits scanned.\n\n**Gap found while proving this, filed separately, not fixed here:**\n`story:secret-slot-probe-observes-what-it-claims`. The gating probe verifies less than design 11\n§ 5 says it does — it never reads `F_GET_SEALS` on the far side and never enumerates descriptors\n(`crates/substrate-host/src/probe.rs:296-298`, `:313`). It does not weaken what this story ships;\nit means the fact can be published on a host where these cases never ran.\n"},"args":{"changes":{"body":"# Story: Sealed secret slots reach a child only as a memfd descriptor\n\n## Outcome\n\nA run can use a daemon-configured secret without the value ever appearing in argv, environment,\nlogs, events, the ledger request hash or an error body — and design 05's reason for refusing a\nlive vendor harness is removed on the secret side.\n\n## Context\n\n`docs/design/06-authentication-secrets-and-trust.md` § *V1 decisions* 3 fixes the mechanism: a\nsealed Linux `memfd` at a declared child descriptor; only the slot→descriptor mapping in the shaped\nenvironment; acquisition after every admission and dispatch-time check; the daemon closes its copy\nimmediately after spawn; a driver that cannot prove sealing, isolation and cleanup reports the\ncapability absent. Design 04 § 7 (line 79): errors may name a slot, never the value.\n\n## Acceptance\n\nThe delegated lane proves the child reads the value from the declared descriptor and the value\nbytes appear nowhere in captured argv, environment, stdout, stderr, event, ledger or diagnostic\nbytes; the portable lane proves the typed refusal.\n\nEvidence that satisfies it, in order:\n\n1. `adr/0012-secret-slots-are-sealed-memfds.md`: slot naming, descriptor declaration, the seal set\n   (`F_SEAL_WRITE|SHRINK|GROW|SEAL`), refusal classes.\n2. A successor bundle adds `secret_slots` on exec and session start, the capability fact\n   `secrets.slots`, and the refusal class; earlier bundle bytes unchanged.\n3. Failing-first tests: `secret_slot_value_absent_from_argv_env_events_and_ledger`,\n   `secret_slot_memfd_is_sealed` (child `fcntl(F_GET_SEALS)` equals the declared set),\n   `secret_slot_refused_when_sealing_unavailable`, `daemon_closes_its_copy_after_spawn`\n   (`/proc/<daemon>/fd` holds no memfd after spawn).\n4. The ledger request hash covers slot **names** only: two requests differing only in slot value\n   hash identically.\n\n## Out of Scope\n\nBrokered artifact secrets (design 06 decision 4) and destination-bound egress.\n\n## Design draft — 2026-08-30\n\n`docs/design/11-sealed-secret-slots.md` (proposed), with `adr/0012` text ready to accept. Verified\nat runtime, not inferred: bubblewrap 0.11.2 passes an inherited descriptor to the child at the\nsame number; a sealed memfd read back inside the sandbox reports `F_GET_SEALS == 0xf` and\n`pwrite → EPERM`. `libc` already exports `memfd_create` and the seal constants — no Cargo change.\nToday's `close_range` (`process.rs:348-377`) would close a slot fd; the draft generalises it to one\nrange per gap. Mapping variable `SUBSTRATE_SECRET_SLOTS=name=fd,…`; `MFD_CLOEXEC` mandatory.\n\n## Progress — 2026-08-30, steps 2-4 done\n\nGate green (`bash scripts/gate.sh`, exit 0), nothing under `contracts/` disturbed:\n`git status --short contracts/` shows only `?? contracts/substrate-wire/0.5.0/`, and\n`cargo xtask render-bundle 0.4.0` still reproduces the frozen tree byte for byte (`diff -r` empty,\nmanifest `sha256:002337bd…`).\n\n**Step 2 — the successor bundle.** `contracts/substrate-wire/0.5.0/`, 206 files, adding\n`secret_slots` on exec and session start, the capability fact `secrets.slots` and the refusal class.\nVerified as a fixed point of its own authored source: `cargo xtask check-bundle 0.5.0` →\n`contract bundle 0.5.0 verified: 206 files, fixed point of xtask/bundle-source/0.5.0`.\n\n**Step 3 — the four named tests, failing first.** Recorded before implementation:\nseals `left: -1 right: 15`; child digest `e3b0c44…` (empty read) against `fffca559…`;\n`/proc/self/fd still holds a slot memfd after spawn`; retained descriptors `[0,1,2,5]` against\n`[0,1,2,5,7]`. All five `secrets::tests::*` pass now, plus six `secret_slot_tests::*` in the wire\ncrate.\n\n**Step 4 — the request hash covers names only.**\n`secrets::tests::ledger_request_hash_covers_slot_names_only` passes: two requests differing only in\na slot's value hash identically.\n\n**A real bug the failing-first probe caught, not a test artefact.** `write_all` left the memfd\noffset at EOF, so a child's `read(fd)` returned 0 bytes — and `/proc/self/fd/N` masked it, because\nopening through `/proc` starts at offset 0. Fixed with `lseek(…, 0, SEEK_SET)` before sealing, with\na second child assertion guarding it. A daemon that shipped without this would have handed every\nchild an empty secret and reported success.\n\n**ADR 0012 is contradicted by the code in one place, and the code is right.** Design § 5 wants\n`secrets.slots` *absent* when orphan reconciliation fails. That state is unreachable: `probe()` runs\nbefore `ProcessRuntime::new` (`crates/substrate-host/src/lib.rs:446`), and a failed reconciliation\naborts `HostDriver::open`, so the daemon never starts to report anything. Recorded in `probe.rs`.\nAbsent-is-weaker, so nothing about this is optimistic.\n\n**Defaults taken.**\n\n- ADR § 9 named `scripts/check-contract-bundle-0.5.0.py`. The org language rule won:\n  `cargo xtask check-bundle <version>` instead, which re-renders from the authored source and\n  compares bytes — strictly stronger than a hand-written checker, because a released tree that has\n  stopped being the fixed point of its own source fails whatever else still looks well-formed.\n  Noted in `docs/design/11-sealed-secret-slots.md` § 9.\n- Invariant 4's `driver_port.rs` guard refused a re-export of `substrate_host::SecretSlot`, so the\n  daemon declares its own type and converts at the composition root.\n- `--secret-slot` validates the file at startup rather than at first use.\n- **`CONTRACT_BUNDLE` stays `substrate-wire/0.4.0`** (`crates/substrate-daemon/src/app.rs:3`). A\n  bundle exists to be pinned by a consumer before the server claims it; moving the\n  `x-b10x-contract` header is its own change with its own clients to notify. Same posture\n  `read_only_roots` already has — in the wire crate, in no bundle.\n\nTwo crate edges added, both already in the workspace: `zeroize` for `substrate-host`, `jsonschema`\nfor `xtask`. No new external dependency.\n\n**Not done.** No end-to-end daemon run with a live slot — there is no delegated cgroup on this host,\nso the four named tests are portable-lane, driving the real acquire/place/close code without a\nsandboxed child. `read_only_roots` is still absent from every bundle including `0.5.0`.\n\n## Progress — 2026-08-30, the delegated lane closes acceptance\n\n`PORTABLE_CASES` 29 → 31 and `DELEGATED_CASES` 42 → 48\n(`crates/substrate-daemon/tests/runtime_vectors.rs:2353-2354`). Both lanes re-run here, not taken\non report:\n\n- `bash scripts/delegated-lane.sh` → `runtime clean-room: 48 HTTP cases, startup refusal, and\n  dual-daemon refusal passed (delegated lane)`, exit 0, 16.47s.\n- `cargo test -p substrate-daemon --test runtime_vectors` → `31 HTTP cases … (portable lane)`,\n  exit 0, 2.59s.\n\nProven against the shipped binary over its socket, so nothing links the implementation: the\nconfined child reads its slot from the declared descriptor and returns a SHA-256 of the bytes; it\nreports `F_GET_SEALS` as the declared set and a write refused with `EPERM`; its descriptor table is\nexactly `{0,1,2}` plus its slot; the value appears in no captured argv, shaped environment, stdout,\nstderr, event page, ledger row, applied record, refusal body or daemon diagnostic; and\n`/proc/<daemon>/fd` holds no slot `memfd` while the child is still running and has not yet read.\nThe portable lane asserts `exec.secret-slots-unserved` and `exec.secret-slot-descriptor-invalid`.\n\nFailing-first, each perturbation reverted:\n\n| perturbation | failure |\n|---|---|\n| daemon started without `--secret-slot` | `left: 501 right: 200`, `exec.secret-slots-unserved` |\n| `SEAL_SET` drops `F_SEAL_GROW` (`crates/substrate-host/src/secrets.rs:33`) | `left: Number(11) right: 15`, read back by the child through the wire |\n| child echoes its slot value | `captured stdout carries the declared value` |\n| `drop(slots)` → `mem::forget` (`crates/substrate-host/src/process.rs:433`) | `left: [\"/memfd:substrate-slot-vendor_api_key (deleted)\", …×2] right: []` |\n\nThe slot value in the test is a synthetic literal, `not-a-credential-…`\n(`crates/substrate-daemon/tests/runtime_vectors.rs:2014`) — no real material in a public\nrepository. `bash scripts/check-secrets.sh` → `no leaks found`, 68 commits scanned.\n\n**Gap found while proving this, filed separately, not fixed here:**\n`story:secret-slot-probe-observes-what-it-claims`. The gating probe verifies less than design 11\n§ 5 says it does — it never reads `F_GET_SEALS` on the far side and never enumerates descriptors\n(`crates/substrate-host/src/probe.rs:296-298`, `:313`). It does not weaken what this story ships;\nit means the fact can be published on a host where these cases never ran.\n"},"command":"update-entity","target":"01MEM0000000000000043"},"payload":{"actor":"human:timo","at":1788056489989,"causation":"cmd-protocol-artifact-body-story-sealed-secret-slots-1788056489989","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body","event_id":"story:sealed-secret-slots@7#0~45cdb64d064d6b3b","executor":null,"recorded_at":"2026-08-30T02:21:29Z"}}
{"entity":"story","version":1,"id":"sealed-secret-slots","revision":7,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"48 HTTP cases (delegated lane), exit 0","source":"bash scripts/delegated-lane.sh, re-run by the orchestrator","target":"01MEM0000000000000043"},"payload":{"actor":"human:timo","at":1788056489000,"causation":"cmd-evidence-story-sealed-secret-slots-test_result-1788056490005","change":{"change":"evidence","kind":"test_result","reference":"48 HTTP cases (delegated lane), exit 0","source":"bash scripts/delegated-lane.sh, re-run by the orchestrator"},"correlation":"protocol-artifact-evidence","event_id":"story:sealed-secret-slots@7#0~2ad913fa20712223","executor":null,"recorded_at":"2026-08-30T02:21:29Z"}}
{"entity":"story","version":1,"id":"sealed-secret-slots","revision":8,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":2,\"artifact\":1}}","target":"01MEM0000000000000043","to":"implemented"},"payload":{"actor":"human:timo","at":1788056490027,"causation":"cmd-move-story-sealed-secret-slots-1788056490027","change":{"change":"moved","decided_on":{"recorded":{"artifact":1,"test_result":2}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:sealed-secret-slots@8#0~2937ff02b5d31146","executor":null,"recorded_at":"2026-08-30T02:21:30Z"}}
{"entity":"story","version":1,"id":"ledger-rows-carry-the-declared-grant","revision":5,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"artifact\":1}}","target":"01MEM0000000000000031","to":"active"},"payload":{"actor":"human:timo","at":1788056791149,"causation":"cmd-move-story-ledger-rows-carry-the-declared-grant-1788056791149","change":{"change":"moved","decided_on":{"recorded":{"artifact":1}},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:ledger-rows-carry-the-declared-grant@5#0~b8d965e8a63780c7","executor":null,"recorded_at":"2026-08-30T02:26:31Z"}}
{"entity":"story","version":1,"id":"tooling-moves-to-cargo-xtask","revision":10,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"body":"# Story: Anything that runs is Rust — the gate's Python moves to `cargo xtask`\n\n## Outcome\n\n`scripts/gate.sh` runs Rust: one `xtask` crate (clap) owns `check-links`, `check-adrs`,\n`check-toolchain`, `package-bundle`, `check-bundle` and the runtime-vector runner, and the next\nwire bundle (`0.5.0`) is rendered from `substrate-wire`'s types rather than from Python dicts. The\nfour frozen renderers and their checkers stay exactly as they are, as the reproducibility proof of\n`0.1.0`–`0.4.0` (invariant 6), and nothing new is written in Python.\n\n## Context\n\n`atlas/AGENTS.md` § *Language*, 2026-08-29: anything that runs is Rust; a new `.py` in a foundation\nrepository is an operator decision. Measured the same day: `scripts/*.py` is 23,916 lines beside\n30,768 lines of Rust; 18,300 of those are `render-contract-bundle{,-0.2.0,-0.3.0,-0.4.0}.py` and\n`check-contract-bundle{,-0.2.0,-0.3.0,-0.4.0}.py`, near-copies per version. The validator that does\nthe standards work is already Rust (`crates/substrate-contract-check`, `jsonschema = \"=0.49.9\"`;\n`scripts/contract_json_gate.py:195` shells out to it). The wire has two descriptions — the Rust\ntypes in `crates/substrate-wire` and the renderer's dicts — and only the vectors would notice them\ndrifting. Wave 1 of this store added ~1,200 more Python lines (`check-toolchain.py`,\n`package-contract-bundle.py` and its tests) by following the local convention; they are the first\nto move.\n\n## Acceptance\n\n`bash scripts/gate.sh` invokes no Python except the four frozen renderers' checkers, every moved\ncheck refuses the same inputs its Python predecessor refused (proven by the predecessor's own\nnegative cases run against the Rust replacement before the Python is deleted), `0.4.0` still\nverifies byte-clean, and `cargo xtask render-bundle 0.5.0` is the only renderer for the successor.\n\nEvidence that satisfies it, in order:\n\n1. `xtask/` crate in the workspace (`cargo xtask <verb>`, clap derive), `--locked` clean.\n2. `check-toolchain`, `check-links`, `check-adrs` moved first (smallest, no wire contact), each with\n   the Python negative cases ported as tests; the Python files deleted in the same commit.\n3. `package-bundle` moved with `test_package_contract_bundle.py`'s 21 cases ported; the 0.4.0\n   manifest digest `sha256:3758e80b…` reproduced by the Rust packager before the Python goes.\n4. `check-runtime-vectors` moved (the black-box Unix-socket HTTP runner, 1,194 lines) — last of the\n   gate steps, because it is the one with the most behaviour.\n5. `render-bundle 0.5.0` derives schemas from `substrate-wire` types (`schemars` or hand-rolled\n   emitters — decided in the story), with the `0.4.0` compatibility block computed, not typed.\n6. `AGENTS.md` § *The gate* and `README.md` § *Build, test, run* list the xtask verbs; the four\n   frozen renderers are documented as frozen proofs, not tooling.\n\n## Out of Scope\n\nRe-rendering `0.1.0`–`0.4.0` from Rust. They are frozen; their Python renderers are their proof.\n\n## Open Questions\n\nWhether `check-runtime-vectors` becomes an `xtask` verb or an integration test under\n`crates/substrate-daemon/tests/` (it drives a real daemon over a socket, which the daemon's tests\nalready do). Decides: operator. Default if nobody answers: **integration test** — one fewer\nbinary, and the gate already runs `cargo test`.\n\n**Resolved 2026-08-30: integration test.** No existing daemon test spawns the binary — `http.rs:29-34`,\n`websocket.rs:36-38`, `pipe_session.rs` and `contract_vectors.rs` all build `App::new(...)` in\nprocess — so there was no harness to reuse, but the black-box property survives:\n`env!(\"CARGO_BIN_EXE_substrate-daemon\")` lets a test in that package spawn the shipped binary over\na Unix socket. Nothing outside the gate invoked the script.\n\n## Progress — 2026-08-30, steps 1–2\n\n- `xtask/` (bin-only, clap derive; `anyhow`, `clap`, dev `tempfile` — no new crate, `Cargo.lock`\n  +9 lines, zero version bumps); `.cargo/config.toml` alias `cargo xtask`.\n- `check-toolchain --root`, `check-links`, `check-adrs` ported; the three Python files deleted in\n  the same change. Differential runs against the Python before deletion: check-links 19 cases +\n  whole repo byte-identical stdout/stderr; check-adrs 11 mutations of `adr/` 11/11 identical;\n  check-toolchain 12 mutations 12/12 identical. Two deliberate differences: the predecessor\n  monorepo escape hatch in check-links is gone (no ancestor has `scripts/check-monorepo.sh`), and\n  `[x](   )` is skipped where the Python raised `IndexError` (`check-links.py:33`).\n- Repo root at runtime is the nearest ancestor whose `Cargo.toml` has `[workspace]`\n  (`xtask/src/repo.rs`), not the binary's compile-time path.\n- Tests written failing-first (`2 passed; 26 failed` on `unimplemented!()` stubs) →\n  `cargo test -p xtask --locked`: 28 passed. `bash scripts/gate.sh`: passed, 13 steps.\n- `gate.sh:18,19,27`, `AGENTS.md` § The gate, `README.md` § Build, `STATUS.md` (three links to the\n  deleted files) updated. Remaining: steps 3–6 (packager, runtime vectors, `render-bundle 0.5.0`).\n\n## Progress — 2026-08-30, step 3\n\n- `cargo xtask package-bundle <version> --out <dir> [--force] [--source-date-epoch] [--contracts-root]`\n  (`xtask/src/package.rs`): hand-written ustar headers matching `tarfile.USTAR_FORMAT` byte for\n  byte, `serde_json` pretty output post-escaped to `json.dumps(ensure_ascii=True)`, epoch from\n  `git log -1 --format=%at -- .` or the flag. Differential against the Python on 0.4.0 before\n  deletion: identical stdout (`sha256:3758e80b…`, archive `sha256:91fb5524…`, 880,640 bytes),\n  `diff -r` empty, modes identical, eleven refusal cases with identical exit code and text.\n- 21 packager tests ported (49 in the crate); failing-first on a timestamp-embedding stub\n  (`45 passed; 4 failed`) then green. Both Python files deleted; `gate.sh` drops the packager\n  test line (it runs under `cargo test --workspace`); gate 12 steps, passed.\n- Remaining: steps 4–6.\n\n\n## Progress — 2026-08-30, step 4\n\n- `crates/substrate-daemon/tests/runtime_vectors.rs` (1,730 lines, one `#[tokio::test]` mirroring\n  the Python's `main()` in order) replaces `scripts/check-runtime-vectors.py`, which is deleted.\n  Hand-written HTTP/1.1 over `tokio::net::UnixStream` and raw WebSocket frames; the lane switch is\n  `SUBSTRATE_VECTORS_CGROUP_ROOT` where the script took `--cgroup-root`, and an unset value keeps\n  the portable branch asserting `exec.sandbox-unavailable` 501 with the delegated cases absent,\n  never counted (invariant 3).\n- Zero new crates; `Cargo.lock` byte-identical. `nix` gains its test-only `signal` feature\n  (`crates/substrate-daemon/Cargo.toml:48-50`).\n- Differential against the Python before deletion: portable lane both `27 HTTP cases, startup\n  refusal, dual-daemon refusal`; delegated lane both `38 HTTP cases` under\n  `systemd-run --user -p Delegate=yes --scope`; two negative cases (`SUBSTRATE_ALLOW_UID` set,\n  non-delegated cgroup root) fail identically on both. Failing-first:\n  `not implemented: cases 2..27 are not ported yet` → `0 passed; 1 failed`.\n- Two deliberate improvements: the temp dir is forced to 0700 (Rust's `TempDir` is 0777 & ~umask\n  and the daemon refuses it), and the case counts are now asserted (`PORTABLE_CASES=27`,\n  `DELEGATED_CASES=38`) rather than printed.\n- The gate keeps no separate step: `cargo test --workspace --locked` runs it. Gate 11 steps,\n  passed; `cargo test --workspace`: 17 suites, 205 passed, 0 failed.\n- **The gate now runs no Python except the frozen bundles' own renderers and checkers.** Remaining\n  for this story: step 5 (`render-bundle 0.5.0` from `substrate-wire` types) and step 6.\n\n## Progress — 2026-08-30, steps 5 and 6\n\n**Step 5 done, and step 5's own premise was wrong.** `cargo xtask render-bundle 0.4.0` reproduces\nthe frozen bundle byte for byte — `diff -r contracts/substrate-wire/0.4.0 <out>` empty, 200 files,\nand packaging the rendered tree yields the manifest digest already checked in,\n`sha256:3758e80bc39f1eb03b15c69410608c9ef1d2ba8095c7e707c6988dbb5894ab00`. A test holds that fixed\npoint (`the_rendered_tree_packages_to_the_released_manifest_digest`), so the renderer cannot drift\nfrom what shipped.\n\nThe story said \"derives schemas from `substrate-wire` types\". It cannot, and the implementation\nrecords that rather than pretending:\n\n- `schemars` is not a workspace dependency (`Cargo.toml:22-59`) and adding derives would mean\n  editing `crates/`, so nothing can reflect the types.\n- Even with reflection the types are **ahead** of 0.4.0: `ExecStartInput::read_only_roots`\n  (`crates/substrate-wire/src/lib.rs:761`) has no 0.4.0 schema at all —\n  `grep -rl read_only_roots contracts/substrate-wire/0.4.0/` → 0 hits — and\n  `FileEditInput`, `FilePatchInput`, `WorkspaceTree*` and `PathMoveInput` have no 0.4.0 input schema.\n- The constraints the schemas state are not on the types. `argv: Vec<String>`\n  (`crates/substrate-wire/src/lib.rs:757`) against a schema of `minLength 1 / maxLength 4096 /\n  maxItems 256`, whose literals live in `crates/substrate-daemon/src/app/operations.rs:245-248` and\n  `crates/substrate-host/src/process.rs:824-826`.\n\nWhat the wire crate does own is taken from it: canonical hashing for all 11 fixture cases,\ncross-checked against `canonical_request_hash_v2`, and 22 bounds constants bound through a `$wire`\nsplice. Of 200 emitted files (708,672 bytes), 30 are computed whole, 14 are authored with computed\nsplices, and 156 are authored verbatim.\n\nOne derivation is **lost**, and is documented as lost at `xtask/src/render.rs:19-25`: the three\nunions in `schemas/vector.json` encode Python dict insertion order (`exec.output-limit-bytes` before\n`exec.max-current`), which a sorted-key bundle records nowhere.\n\nAuthored source is `xtask/bundle-source/<version>/` — one file per emitted path under `documents/`,\nplus `routes.json`, `coverage.json`, `hash-cases.json`, `vector-order.json` and\n`executable-vectors.json`. Not `contracts/source/`: every directory under `contracts/` is a released\nbundle (invariant 6) and `scripts/contract_json_gate.py:301` classifies JSON beneath one and fails\nclosed (invariant 7). Rendering into `contracts/` is a named refusal, exit 1.\n\nFailing-first: `render`/`hash_case` stubbed to `unimplemented!()` gave `55 passed; 9 failed`.\nThen `cargo test -p xtask --locked` → `64 passed; 0 failed` (49 before). `bash scripts/gate.sh`\npassed, 11 steps. `git status --short contracts/` empty.\n\n**Step 6 done.** `AGENTS.md` § *The gate* and `README.md` § *Build, test, run* both carry the verb\ntable — `check-toolchain`, `check-links`, `check-adrs`, `package-bundle`, `render-bundle` — and both\nsay why the four frozen `render-contract-bundle*.py` / `check-contract-bundle*.py` pairs stay: they\nare the released bundles' reproducibility proof, and 0.4.0's `generator.name` points at one of them,\nso deleting it would break the bundle it generated.\n\n**Decision taken by default:** `render-bundle` gets no `gate.sh` step of its own. A test already\nasserts the 0.4.0 digest, and `cargo test --workspace --locked` is the gate's first step — the same\nreasoning that keeps `package-bundle` and the runtime-vector runner out of it.\n\n**0.5.0 is cut, so that clause is closed.** `story:sealed-secret-slots` rendered\n`contracts/substrate-wire/0.5.0/` (206 files) with `cargo xtask render-bundle`, and no Python\nrenderer was involved or written. It also replaced the ADR's proposed\n`scripts/check-contract-bundle-0.5.0.py` with a new verb, `cargo xtask check-bundle <version>`,\nwhich re-renders from the authored source and compares bytes — strictly stronger than a\nhand-written checker.\n\n## What is actually left — 2026-08-30\n\n**576 lines, and they are not the frozen renderers.** The acceptance says the gate invokes no Python\nexcept the four frozen renderers' checkers. It still invokes one more:\n\n    scripts/gate.sh:28  run python3 scripts/test_contract_json_gate.py\n\n`scripts/contract_json_gate.py` (377 lines) and its self-test (199) are shared live machinery, not a\nper-version frozen artefact — `:301` classifies JSON under a bundle directory and fails closed, and\n`:195` already shells out to `crates/substrate-contract-check`, which is Rust. Porting it is the\nlast step of this story, and it is the one place a new bundle's JSON is admitted or rejected, so it\nwants the same differential proof every other move got: the predecessor's negative cases run against\nthe Rust replacement before the Python is deleted.\n\n`scripts/check-bot-files.py` (39 lines) is not in `scripts/gate.sh` and is out of this story's\nacceptance; name it separately if it should move.\n\nOf the 21,298 Python lines left, 20,683 are the four frozen renderer/checker pairs, which stay.\n\n## Progress — 2026-08-30, the last Python leaves the gate\n\n`scripts/contract_json_gate.py` (377 lines) and `scripts/test_contract_json_gate.py` (199) are\ndeleted; `cargo xtask check-json` replaces them (`xtask/src/json.rs`, 2,292 lines with tests).\n\nAcceptance, checked line by line:\n\n- **No Python in the gate except the four frozen checkers.** `grep -n python scripts/gate.sh` →\n  lines 20-23 only, the four `check-contract-bundle*.py`.\n- **`bash scripts/gate.sh` → `gate: passed`, exit 0**, 47.4s, re-run by the orchestrator rather\n  than taken on report. Its last three steps: `check-bundle 0.6.0` → 213 files;\n  `check-json` → `contract JSON classified: 0.1.0 114, 0.2.0 165, 0.3.0 194, 0.4.0 200, 0.5.0 206,\n  0.6.0 213 (1092 documents in 6 bundles)`; `check-toolchain` → `Rust toolchain pinned at 1.97`.\n- **`0.4.0` still verifies byte-clean** — `substrate-wire 0.4.0: 199 files, 26 closed operations,\n  21 executable vectors, 71 design vectors, 112 requirements, and 11 exact hash fixtures verified`.\n  `git status --short contracts/` is empty.\n- Failing-first: 7 `unimplemented!()` stubs failed (`69 passed; 7 failed`), then **87 passed**.\n\n**Differential before deletion, and again afterwards from an archived copy: 33 of 36 cases\nbyte-identical** in stdout, stderr and exit code — six untouched bundles plus 30 mutations. The\nthree that differ, each decided rather than absorbed:\n\n| case | Python | Rust | why |\n|---|---|---|---|\n| malformed document | `Expecting property name enclosed in double quotes: line 3 column 1 (char 12)` | `EOF while parsing a value at line 3 column 0` | different parsers; same path, prefix, position and exit code |\n| `additionalProperties` order | `'b'` then `'a'` | `'a'` then `'b'` | reachable only for a document that already failed the deterministic-source-form check; one that passes it has file order == sorted order |\n| cyclic `$ref` | `RecursionError` and a 1,000-frame traceback | named refusal, `MAX_REFERENCE_DEPTH = 64` | **a behaviour change, on purpose**: Python guarded cycles in `dereference_schema` but not on the `validate` path, and a crash is not a refusal (invariant 3) |\n\n**Coverage widened, not narrowed.** The Python classified four bundles — `check_json_authority` was\nonly ever called by the four checkers, each closing over its own `BUNDLE`, and `0.5.0`/`0.6.0` had\nno Python at all. `check-json` covers all six.\n\n**The four frozen checkers changed**, minimally and only where they imported the deleted module:\n7 lines each — the `from contract_json_gate import …` line, the call, and the\n`N classified JSON documents` field of the summary string. `check_renderer_reproducibility` and\nevery bundle assertion are untouched, so their role as the frozen bundles' reproducibility proof\n(invariant 6) is intact; verified by diff and by the gate.\n\n**Two new direct dependencies, zero new packages** (`Cargo.lock` +2 lines), both already in the\nlock under `jsonschema`: `fancy-regex`, because three bundle patterns use negative lookahead that\n`regex` cannot compile and one is reached on a released tree; `serde`, because rejecting duplicate\nobject keys needs a `Deserialize` visitor. `fancy-regex`'s `delegate_size_limit` is raised to\n256 MiB so `common.json`'s `[A-Za-z0-9+/]{1398102}==` compiles.\n\n**Dropped on purpose:** the subprocess-protocol refusals at the old `contract_json_gate.py:206-218`\n— a 16 MiB pipe ceiling and label/URI uniqueness. The standards pass runs in process now; the pipe\nthey guarded does not exist and uniqueness holds by construction.\n\n`git grep contract_json_gate` finds one hit, `docs/reviews/archived/2026-08-13-…md:80`, which is\nimmutable review input and correctly left alone.\n"},"args":{"changes":{"body":"# Story: Anything that runs is Rust — the gate's Python moves to `cargo xtask`\n\n## Outcome\n\n`scripts/gate.sh` runs Rust: one `xtask` crate (clap) owns `check-links`, `check-adrs`,\n`check-toolchain`, `package-bundle`, `check-bundle` and the runtime-vector runner, and the next\nwire bundle (`0.5.0`) is rendered from `substrate-wire`'s types rather than from Python dicts. The\nfour frozen renderers and their checkers stay exactly as they are, as the reproducibility proof of\n`0.1.0`–`0.4.0` (invariant 6), and nothing new is written in Python.\n\n## Context\n\n`atlas/AGENTS.md` § *Language*, 2026-08-29: anything that runs is Rust; a new `.py` in a foundation\nrepository is an operator decision. Measured the same day: `scripts/*.py` is 23,916 lines beside\n30,768 lines of Rust; 18,300 of those are `render-contract-bundle{,-0.2.0,-0.3.0,-0.4.0}.py` and\n`check-contract-bundle{,-0.2.0,-0.3.0,-0.4.0}.py`, near-copies per version. The validator that does\nthe standards work is already Rust (`crates/substrate-contract-check`, `jsonschema = \"=0.49.9\"`;\n`scripts/contract_json_gate.py:195` shells out to it). The wire has two descriptions — the Rust\ntypes in `crates/substrate-wire` and the renderer's dicts — and only the vectors would notice them\ndrifting. Wave 1 of this store added ~1,200 more Python lines (`check-toolchain.py`,\n`package-contract-bundle.py` and its tests) by following the local convention; they are the first\nto move.\n\n## Acceptance\n\n`bash scripts/gate.sh` invokes no Python except the four frozen renderers' checkers, every moved\ncheck refuses the same inputs its Python predecessor refused (proven by the predecessor's own\nnegative cases run against the Rust replacement before the Python is deleted), `0.4.0` still\nverifies byte-clean, and `cargo xtask render-bundle 0.5.0` is the only renderer for the successor.\n\nEvidence that satisfies it, in order:\n\n1. `xtask/` crate in the workspace (`cargo xtask <verb>`, clap derive), `--locked` clean.\n2. `check-toolchain`, `check-links`, `check-adrs` moved first (smallest, no wire contact), each with\n   the Python negative cases ported as tests; the Python files deleted in the same commit.\n3. `package-bundle` moved with `test_package_contract_bundle.py`'s 21 cases ported; the 0.4.0\n   manifest digest `sha256:3758e80b…` reproduced by the Rust packager before the Python goes.\n4. `check-runtime-vectors` moved (the black-box Unix-socket HTTP runner, 1,194 lines) — last of the\n   gate steps, because it is the one with the most behaviour.\n5. `render-bundle 0.5.0` derives schemas from `substrate-wire` types (`schemars` or hand-rolled\n   emitters — decided in the story), with the `0.4.0` compatibility block computed, not typed.\n6. `AGENTS.md` § *The gate* and `README.md` § *Build, test, run* list the xtask verbs; the four\n   frozen renderers are documented as frozen proofs, not tooling.\n\n## Out of Scope\n\nRe-rendering `0.1.0`–`0.4.0` from Rust. They are frozen; their Python renderers are their proof.\n\n## Open Questions\n\nWhether `check-runtime-vectors` becomes an `xtask` verb or an integration test under\n`crates/substrate-daemon/tests/` (it drives a real daemon over a socket, which the daemon's tests\nalready do). Decides: operator. Default if nobody answers: **integration test** — one fewer\nbinary, and the gate already runs `cargo test`.\n\n**Resolved 2026-08-30: integration test.** No existing daemon test spawns the binary — `http.rs:29-34`,\n`websocket.rs:36-38`, `pipe_session.rs` and `contract_vectors.rs` all build `App::new(...)` in\nprocess — so there was no harness to reuse, but the black-box property survives:\n`env!(\"CARGO_BIN_EXE_substrate-daemon\")` lets a test in that package spawn the shipped binary over\na Unix socket. Nothing outside the gate invoked the script.\n\n## Progress — 2026-08-30, steps 1–2\n\n- `xtask/` (bin-only, clap derive; `anyhow`, `clap`, dev `tempfile` — no new crate, `Cargo.lock`\n  +9 lines, zero version bumps); `.cargo/config.toml` alias `cargo xtask`.\n- `check-toolchain --root`, `check-links`, `check-adrs` ported; the three Python files deleted in\n  the same change. Differential runs against the Python before deletion: check-links 19 cases +\n  whole repo byte-identical stdout/stderr; check-adrs 11 mutations of `adr/` 11/11 identical;\n  check-toolchain 12 mutations 12/12 identical. Two deliberate differences: the predecessor\n  monorepo escape hatch in check-links is gone (no ancestor has `scripts/check-monorepo.sh`), and\n  `[x](   )` is skipped where the Python raised `IndexError` (`check-links.py:33`).\n- Repo root at runtime is the nearest ancestor whose `Cargo.toml` has `[workspace]`\n  (`xtask/src/repo.rs`), not the binary's compile-time path.\n- Tests written failing-first (`2 passed; 26 failed` on `unimplemented!()` stubs) →\n  `cargo test -p xtask --locked`: 28 passed. `bash scripts/gate.sh`: passed, 13 steps.\n- `gate.sh:18,19,27`, `AGENTS.md` § The gate, `README.md` § Build, `STATUS.md` (three links to the\n  deleted files) updated. Remaining: steps 3–6 (packager, runtime vectors, `render-bundle 0.5.0`).\n\n## Progress — 2026-08-30, step 3\n\n- `cargo xtask package-bundle <version> --out <dir> [--force] [--source-date-epoch] [--contracts-root]`\n  (`xtask/src/package.rs`): hand-written ustar headers matching `tarfile.USTAR_FORMAT` byte for\n  byte, `serde_json` pretty output post-escaped to `json.dumps(ensure_ascii=True)`, epoch from\n  `git log -1 --format=%at -- .` or the flag. Differential against the Python on 0.4.0 before\n  deletion: identical stdout (`sha256:3758e80b…`, archive `sha256:91fb5524…`, 880,640 bytes),\n  `diff -r` empty, modes identical, eleven refusal cases with identical exit code and text.\n- 21 packager tests ported (49 in the crate); failing-first on a timestamp-embedding stub\n  (`45 passed; 4 failed`) then green. Both Python files deleted; `gate.sh` drops the packager\n  test line (it runs under `cargo test --workspace`); gate 12 steps, passed.\n- Remaining: steps 4–6.\n\n\n## Progress — 2026-08-30, step 4\n\n- `crates/substrate-daemon/tests/runtime_vectors.rs` (1,730 lines, one `#[tokio::test]` mirroring\n  the Python's `main()` in order) replaces `scripts/check-runtime-vectors.py`, which is deleted.\n  Hand-written HTTP/1.1 over `tokio::net::UnixStream` and raw WebSocket frames; the lane switch is\n  `SUBSTRATE_VECTORS_CGROUP_ROOT` where the script took `--cgroup-root`, and an unset value keeps\n  the portable branch asserting `exec.sandbox-unavailable` 501 with the delegated cases absent,\n  never counted (invariant 3).\n- Zero new crates; `Cargo.lock` byte-identical. `nix` gains its test-only `signal` feature\n  (`crates/substrate-daemon/Cargo.toml:48-50`).\n- Differential against the Python before deletion: portable lane both `27 HTTP cases, startup\n  refusal, dual-daemon refusal`; delegated lane both `38 HTTP cases` under\n  `systemd-run --user -p Delegate=yes --scope`; two negative cases (`SUBSTRATE_ALLOW_UID` set,\n  non-delegated cgroup root) fail identically on both. Failing-first:\n  `not implemented: cases 2..27 are not ported yet` → `0 passed; 1 failed`.\n- Two deliberate improvements: the temp dir is forced to 0700 (Rust's `TempDir` is 0777 & ~umask\n  and the daemon refuses it), and the case counts are now asserted (`PORTABLE_CASES=27`,\n  `DELEGATED_CASES=38`) rather than printed.\n- The gate keeps no separate step: `cargo test --workspace --locked` runs it. Gate 11 steps,\n  passed; `cargo test --workspace`: 17 suites, 205 passed, 0 failed.\n- **The gate now runs no Python except the frozen bundles' own renderers and checkers.** Remaining\n  for this story: step 5 (`render-bundle 0.5.0` from `substrate-wire` types) and step 6.\n\n## Progress — 2026-08-30, steps 5 and 6\n\n**Step 5 done, and step 5's own premise was wrong.** `cargo xtask render-bundle 0.4.0` reproduces\nthe frozen bundle byte for byte — `diff -r contracts/substrate-wire/0.4.0 <out>` empty, 200 files,\nand packaging the rendered tree yields the manifest digest already checked in,\n`sha256:3758e80bc39f1eb03b15c69410608c9ef1d2ba8095c7e707c6988dbb5894ab00`. A test holds that fixed\npoint (`the_rendered_tree_packages_to_the_released_manifest_digest`), so the renderer cannot drift\nfrom what shipped.\n\nThe story said \"derives schemas from `substrate-wire` types\". It cannot, and the implementation\nrecords that rather than pretending:\n\n- `schemars` is not a workspace dependency (`Cargo.toml:22-59`) and adding derives would mean\n  editing `crates/`, so nothing can reflect the types.\n- Even with reflection the types are **ahead** of 0.4.0: `ExecStartInput::read_only_roots`\n  (`crates/substrate-wire/src/lib.rs:761`) has no 0.4.0 schema at all —\n  `grep -rl read_only_roots contracts/substrate-wire/0.4.0/` → 0 hits — and\n  `FileEditInput`, `FilePatchInput`, `WorkspaceTree*` and `PathMoveInput` have no 0.4.0 input schema.\n- The constraints the schemas state are not on the types. `argv: Vec<String>`\n  (`crates/substrate-wire/src/lib.rs:757`) against a schema of `minLength 1 / maxLength 4096 /\n  maxItems 256`, whose literals live in `crates/substrate-daemon/src/app/operations.rs:245-248` and\n  `crates/substrate-host/src/process.rs:824-826`.\n\nWhat the wire crate does own is taken from it: canonical hashing for all 11 fixture cases,\ncross-checked against `canonical_request_hash_v2`, and 22 bounds constants bound through a `$wire`\nsplice. Of 200 emitted files (708,672 bytes), 30 are computed whole, 14 are authored with computed\nsplices, and 156 are authored verbatim.\n\nOne derivation is **lost**, and is documented as lost at `xtask/src/render.rs:19-25`: the three\nunions in `schemas/vector.json` encode Python dict insertion order (`exec.output-limit-bytes` before\n`exec.max-current`), which a sorted-key bundle records nowhere.\n\nAuthored source is `xtask/bundle-source/<version>/` — one file per emitted path under `documents/`,\nplus `routes.json`, `coverage.json`, `hash-cases.json`, `vector-order.json` and\n`executable-vectors.json`. Not `contracts/source/`: every directory under `contracts/` is a released\nbundle (invariant 6) and `scripts/contract_json_gate.py:301` classifies JSON beneath one and fails\nclosed (invariant 7). Rendering into `contracts/` is a named refusal, exit 1.\n\nFailing-first: `render`/`hash_case` stubbed to `unimplemented!()` gave `55 passed; 9 failed`.\nThen `cargo test -p xtask --locked` → `64 passed; 0 failed` (49 before). `bash scripts/gate.sh`\npassed, 11 steps. `git status --short contracts/` empty.\n\n**Step 6 done.** `AGENTS.md` § *The gate* and `README.md` § *Build, test, run* both carry the verb\ntable — `check-toolchain`, `check-links`, `check-adrs`, `package-bundle`, `render-bundle` — and both\nsay why the four frozen `render-contract-bundle*.py` / `check-contract-bundle*.py` pairs stay: they\nare the released bundles' reproducibility proof, and 0.4.0's `generator.name` points at one of them,\nso deleting it would break the bundle it generated.\n\n**Decision taken by default:** `render-bundle` gets no `gate.sh` step of its own. A test already\nasserts the 0.4.0 digest, and `cargo test --workspace --locked` is the gate's first step — the same\nreasoning that keeps `package-bundle` and the runtime-vector runner out of it.\n\n**0.5.0 is cut, so that clause is closed.** `story:sealed-secret-slots` rendered\n`contracts/substrate-wire/0.5.0/` (206 files) with `cargo xtask render-bundle`, and no Python\nrenderer was involved or written. It also replaced the ADR's proposed\n`scripts/check-contract-bundle-0.5.0.py` with a new verb, `cargo xtask check-bundle <version>`,\nwhich re-renders from the authored source and compares bytes — strictly stronger than a\nhand-written checker.\n\n## What is actually left — 2026-08-30\n\n**576 lines, and they are not the frozen renderers.** The acceptance says the gate invokes no Python\nexcept the four frozen renderers' checkers. It still invokes one more:\n\n    scripts/gate.sh:28  run python3 scripts/test_contract_json_gate.py\n\n`scripts/contract_json_gate.py` (377 lines) and its self-test (199) are shared live machinery, not a\nper-version frozen artefact — `:301` classifies JSON under a bundle directory and fails closed, and\n`:195` already shells out to `crates/substrate-contract-check`, which is Rust. Porting it is the\nlast step of this story, and it is the one place a new bundle's JSON is admitted or rejected, so it\nwants the same differential proof every other move got: the predecessor's negative cases run against\nthe Rust replacement before the Python is deleted.\n\n`scripts/check-bot-files.py` (39 lines) is not in `scripts/gate.sh` and is out of this story's\nacceptance; name it separately if it should move.\n\nOf the 21,298 Python lines left, 20,683 are the four frozen renderer/checker pairs, which stay.\n\n## Progress — 2026-08-30, the last Python leaves the gate\n\n`scripts/contract_json_gate.py` (377 lines) and `scripts/test_contract_json_gate.py` (199) are\ndeleted; `cargo xtask check-json` replaces them (`xtask/src/json.rs`, 2,292 lines with tests).\n\nAcceptance, checked line by line:\n\n- **No Python in the gate except the four frozen checkers.** `grep -n python scripts/gate.sh` →\n  lines 20-23 only, the four `check-contract-bundle*.py`.\n- **`bash scripts/gate.sh` → `gate: passed`, exit 0**, 47.4s, re-run by the orchestrator rather\n  than taken on report. Its last three steps: `check-bundle 0.6.0` → 213 files;\n  `check-json` → `contract JSON classified: 0.1.0 114, 0.2.0 165, 0.3.0 194, 0.4.0 200, 0.5.0 206,\n  0.6.0 213 (1092 documents in 6 bundles)`; `check-toolchain` → `Rust toolchain pinned at 1.97`.\n- **`0.4.0` still verifies byte-clean** — `substrate-wire 0.4.0: 199 files, 26 closed operations,\n  21 executable vectors, 71 design vectors, 112 requirements, and 11 exact hash fixtures verified`.\n  `git status --short contracts/` is empty.\n- Failing-first: 7 `unimplemented!()` stubs failed (`69 passed; 7 failed`), then **87 passed**.\n\n**Differential before deletion, and again afterwards from an archived copy: 33 of 36 cases\nbyte-identical** in stdout, stderr and exit code — six untouched bundles plus 30 mutations. The\nthree that differ, each decided rather than absorbed:\n\n| case | Python | Rust | why |\n|---|---|---|---|\n| malformed document | `Expecting property name enclosed in double quotes: line 3 column 1 (char 12)` | `EOF while parsing a value at line 3 column 0` | different parsers; same path, prefix, position and exit code |\n| `additionalProperties` order | `'b'` then `'a'` | `'a'` then `'b'` | reachable only for a document that already failed the deterministic-source-form check; one that passes it has file order == sorted order |\n| cyclic `$ref` | `RecursionError` and a 1,000-frame traceback | named refusal, `MAX_REFERENCE_DEPTH = 64` | **a behaviour change, on purpose**: Python guarded cycles in `dereference_schema` but not on the `validate` path, and a crash is not a refusal (invariant 3) |\n\n**Coverage widened, not narrowed.** The Python classified four bundles — `check_json_authority` was\nonly ever called by the four checkers, each closing over its own `BUNDLE`, and `0.5.0`/`0.6.0` had\nno Python at all. `check-json` covers all six.\n\n**The four frozen checkers changed**, minimally and only where they imported the deleted module:\n7 lines each — the `from contract_json_gate import …` line, the call, and the\n`N classified JSON documents` field of the summary string. `check_renderer_reproducibility` and\nevery bundle assertion are untouched, so their role as the frozen bundles' reproducibility proof\n(invariant 6) is intact; verified by diff and by the gate.\n\n**Two new direct dependencies, zero new packages** (`Cargo.lock` +2 lines), both already in the\nlock under `jsonschema`: `fancy-regex`, because three bundle patterns use negative lookahead that\n`regex` cannot compile and one is reached on a released tree; `serde`, because rejecting duplicate\nobject keys needs a `Deserialize` visitor. `fancy-regex`'s `delegate_size_limit` is raised to\n256 MiB so `common.json`'s `[A-Za-z0-9+/]{1398102}==` compiles.\n\n**Dropped on purpose:** the subprocess-protocol refusals at the old `contract_json_gate.py:206-218`\n— a 16 MiB pipe ceiling and label/URI uniqueness. The standards pass runs in process now; the pipe\nthey guarded does not exist and uniqueness holds by construction.\n\n`git grep contract_json_gate` finds one hit, `docs/reviews/archived/2026-08-13-…md:80`, which is\nimmutable review input and correctly left alone.\n"},"command":"update-entity","target":"01MEM0000000000000055"},"payload":{"actor":"human:timo","at":1788057768099,"causation":"cmd-protocol-artifact-body-story-tooling-moves-to-cargo-xtask-1788057768099","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body","event_id":"story:tooling-moves-to-cargo-xtask@10#0~096f7cd03be42e57","executor":null,"recorded_at":"2026-08-30T02:42:48Z"}}
{"entity":"story","version":1,"id":"tooling-moves-to-cargo-xtask","revision":10,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"gate: passed, exit 0, 47.4s","source":"bash scripts/gate.sh, re-run by the orchestrator","target":"01MEM0000000000000055"},"payload":{"actor":"human:timo","at":1788057768000,"causation":"cmd-evidence-story-tooling-moves-to-cargo-xtask-test_result-1788057768120","change":{"change":"evidence","kind":"test_result","reference":"gate: passed, exit 0, 47.4s","source":"bash scripts/gate.sh, re-run by the orchestrator"},"correlation":"protocol-artifact-evidence","event_id":"story:tooling-moves-to-cargo-xtask@10#0~354694396b58334f","executor":null,"recorded_at":"2026-08-30T02:42:48Z"}}
{"entity":"story","version":1,"id":"tooling-moves-to-cargo-xtask","revision":11,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":4,\"static_analysis\":1,\"verification\":1}}","target":"01MEM0000000000000055","to":"implemented"},"payload":{"actor":"human:timo","at":1788057768141,"causation":"cmd-move-story-tooling-moves-to-cargo-xtask-1788057768141","change":{"change":"moved","decided_on":{"recorded":{"static_analysis":1,"test_result":4,"verification":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:tooling-moves-to-cargo-xtask@11#0~584ae15beeb80448","executor":null,"recorded_at":"2026-08-30T02:42:48Z"}}
{"entity":"story","version":1,"id":"ledger-rows-carry-the-declared-grant","revision":6,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"body":"# Story: An operation's ledger row carries the declared grant it ran under\n\n## Outcome\n\nFrom substrate's own durable record, a reader can answer atlas O1's exit question for every\neffectful operation: *which declared grant authorised this, on behalf of which platform principal,\nthrough which immediate actor* — and an operation presented without a grant reference where the\ndeployment requires one is a named refusal, not a missing column.\n\n## Context\n\n`atlas/ROADMAP.md` O1 — *governed reach* — names substrate an owner and states the exit evidence:\n\"every effectful call in a run's record is attributable to a declared grant; a call outside it is\na named refusal, not a missing row\". Its state column says \"connectors grants and identity\naudiences: not yet verified across a seam\". Substrate's side of that seam is already designed:\n`docs/design/06-authentication-secrets-and-trust.md` § 2 — the initiating platform principal is\nretained separately from the immediate service actor \"through tamper-safe delegated context;\ncaller-written identity strings are not trusted\", and every ledger key is scoped by deployment\nand authenticated subject. Nothing in the backlog implemented that context on the wire or in the\nledger; this is the one item that moves the objective substrate serves (`AGENTS.md` § *Serves*).\n\n## Acceptance\n\nAn exec or session operation started with a delegated-context document records the grant\nreference and platform principal in its ledger row and in its `operation.*` events, the same\nbytes a connectors-side fixture presents, and a test proves a caller-written identity string in\nthe request body is ignored in favour of the verified context.\n\nEvidence that satisfies it, in order:\n\n1. **Before code** (invariant 8): an ADR fixing the delegated-context shape — who signs or seals\n   it, what substrate verifies (audience, expiry, binding to the authenticated subject), and the\n   refusal class when it is required and absent or invalid. Coordinated with `connectors` and\n   `identity` under `atlas/AGENTS.md` § *Cross-repo changes*; the ADR names both relying parties.\n2. A successor bundle adds the optional `delegated_context` request field, the ledger/event\n   fields `grant_ref` and `platform_principal`, and the refusal; earlier bundle bytes unchanged.\n3. Failing-first tests: `ledger_row_records_grant_ref_and_platform_principal`,\n   `caller_written_identity_is_ignored_when_context_is_verified`,\n   `missing_delegated_context_is_refused_by_name_when_required`,\n   `delegated_context_bound_to_another_subject_is_refused`.\n4. A conformance vector pair: substrate's fixture and a connectors-owned copy, byte-identical, so\n   the atlas O1 row can cite both sides.\n\n## Out of Scope\n\nGrant *evaluation* — connectors decides; substrate records and verifies binding only (design 06\n§ 2: \"a higher-layer permit cannot override\" substrate's local checks, and substrate never\nbecomes the authorization engine). Hosted identity material (phase 7).\n\n## Open Questions\n\nWhether the delegated context is a signed JWT-shaped token from identity or a sealed blob from\nconnectors. Decides: the ADR, with both owners. Default if nobody answers: **identity-signed,\naudience `substrate`**, because atlas O1 already pairs \"connectors grants and identity audiences\".\n\n## Design draft — 2026-08-30\n\n`docs/design/09-delegated-context-and-grant-attribution.md` (proposed) carries the shape and the\nADR text ready to accept. Findings that move this story's default: identity issues **opaque**\ntokens and holds no signing key, `kid` or JWKS (`identity/src/lib.rs:1936-1939`, `:2172`), and\n`urn:b10x:substrate` is documented but not issuable (`identity/README.md:144`,\n`identity/src/lib.rs:1920-1922`); connectors **already** issues Ed25519 compact JWS carrying\n`dl_grant`, `sub`, `act` (`connectors/crates/service/src/authority.rs:52-71`) and names the field\n`grant_ref` (`connectors/crates/integration-catalog/src/lib.rs:99-106`). The draft keeps the\nstory's default (identity-signed) with that cost stated; the cheaper path is a connectors-signed\ncontext. Decides: the ADR, with both owners.\n\n## Progress — 2026-08-30, steps 2-4 done, story complete\n\nMerged as `16b2b3b` (PR #20). Every command below was re-run by the orchestrator in the agent's\nworktree, not taken from a report.\n\n**Step 2 — the successor bundle.** `contracts/substrate-wire/0.7.0/`, 224 files, additive successor\nto `0.6.0`, carrying the optional `delegated_context` request member, the `grant_ref` and\n`platform_principal` ledger/event fields and the named refusals.\n\n- `cargo xtask check-bundle 0.7.0` → `contract bundle 0.7.0 verified: 224 files, fixed point of\n  xtask/bundle-source/0.7.0`; `0.5.0` (206) and `0.6.0` (213) still fixed points.\n- `git status --short contracts/` showed only the new directory (invariant 6).\n- `cargo xtask check-json` → `1316 documents in 7 bundles`. This matters: `check-json` landed the\n  same night (`5f332b8`) and classifies every document under every bundle, where the retired Python\n  only ever ran on `0.1.0`–`0.4.0`. `0.7.0` is classified, not skipped.\n\n**Step 3 — the four named tests**, on the wire against the shipped binary over its socket.\n`PORTABLE_CASES` 31 → 33, `DELEGATED_CASES` 48 → 50\n(`crates/substrate-daemon/tests/runtime_vectors.rs:2768-2769`).\n`bash scripts/delegated-lane.sh` → `runtime clean-room: 50 HTTP cases, startup refusal, and\ndual-daemon refusal passed (delegated lane)`, plus the four named tests, 5 passed, exit 0.\n\n**Failing-first is partly missing, and this records that rather than papering over it.** The\nimplementing agent hit a session limit before reporting, so its own failing-first output is lost.\nOne perturbation was reproduced to prove the tests are not vacuous: replacing the subject-binding\ncheck at `crates/substrate-daemon/src/delegation.rs:174` with `if false` makes\n`delegated_context_bound_to_another_subject_is_refused` fail `left: 201, right: 422` — a context\nbound to another subject is accepted where it must be refused. Reverted; the file is byte-identical\nto its backup. **The other three tests were not proven to fail first.** If that matters later, the\nperturbations are cheap to repeat.\n\n**Step 4 — key material.** The test signer derives its seed from a sentence\n(`crates/substrate-daemon/tests/runtime_vectors.rs:2405-2409`) rather than carrying a key blob, so\nthe public repository has nothing to leak or rotate, and the cases mint documents bound to *this*\nmachine's subject and *this* instant — which a committed fixture never can.\n`bash scripts/check-secrets.sh` → `no leaks found`.\n\n**What it does not do**, each a deliberate boundary: it never evaluates the grant (connectors\ndecides); it never signs — `--delegated-context-key <kid>=<issuer>=<base64url>` declares a\n*verifying* key, so which service signs is configuration and changes no substrate code; and the\ngrant-set revision, tenant, actor chain and `jti` are verified as part of the closed claim set and\nthen dropped, keeping the query surface to the two named columns.\n\nEight named refusals: `delegated-context.absent`, `.malformed`, `.unknown-key`,\n`.signature-invalid`, `.audience-mismatch`, `.subject-mismatch`, `.expired`, `.grant-conflict`\n(`crates/substrate-daemon/src/delegation.rs:209-256`).\n\n`delegated_context` is a sibling of `op` and `input`, never a member of `input`\n(`crates/substrate-wire/src/lib.rs:100-113`), so it sits outside the canonical request hash:\nreplaying the same `op` with a fresh context is the same operation and returns the original\noutcome, and a request without one serializes exactly as a `0.6.0` client's did — which is what\nkeeps every frozen bundle's vectors true.\n\n**Still open on the seam, not on substrate:** step 4's conformance vector *pair* needs the\nconnectors-side copy. Substrate's side is byte-reproducible; the atlas O1 row cannot cite both\nsides until connectors holds its copy.\n"},"args":{"changes":{"body":"# Story: An operation's ledger row carries the declared grant it ran under\n\n## Outcome\n\nFrom substrate's own durable record, a reader can answer atlas O1's exit question for every\neffectful operation: *which declared grant authorised this, on behalf of which platform principal,\nthrough which immediate actor* — and an operation presented without a grant reference where the\ndeployment requires one is a named refusal, not a missing column.\n\n## Context\n\n`atlas/ROADMAP.md` O1 — *governed reach* — names substrate an owner and states the exit evidence:\n\"every effectful call in a run's record is attributable to a declared grant; a call outside it is\na named refusal, not a missing row\". Its state column says \"connectors grants and identity\naudiences: not yet verified across a seam\". Substrate's side of that seam is already designed:\n`docs/design/06-authentication-secrets-and-trust.md` § 2 — the initiating platform principal is\nretained separately from the immediate service actor \"through tamper-safe delegated context;\ncaller-written identity strings are not trusted\", and every ledger key is scoped by deployment\nand authenticated subject. Nothing in the backlog implemented that context on the wire or in the\nledger; this is the one item that moves the objective substrate serves (`AGENTS.md` § *Serves*).\n\n## Acceptance\n\nAn exec or session operation started with a delegated-context document records the grant\nreference and platform principal in its ledger row and in its `operation.*` events, the same\nbytes a connectors-side fixture presents, and a test proves a caller-written identity string in\nthe request body is ignored in favour of the verified context.\n\nEvidence that satisfies it, in order:\n\n1. **Before code** (invariant 8): an ADR fixing the delegated-context shape — who signs or seals\n   it, what substrate verifies (audience, expiry, binding to the authenticated subject), and the\n   refusal class when it is required and absent or invalid. Coordinated with `connectors` and\n   `identity` under `atlas/AGENTS.md` § *Cross-repo changes*; the ADR names both relying parties.\n2. A successor bundle adds the optional `delegated_context` request field, the ledger/event\n   fields `grant_ref` and `platform_principal`, and the refusal; earlier bundle bytes unchanged.\n3. Failing-first tests: `ledger_row_records_grant_ref_and_platform_principal`,\n   `caller_written_identity_is_ignored_when_context_is_verified`,\n   `missing_delegated_context_is_refused_by_name_when_required`,\n   `delegated_context_bound_to_another_subject_is_refused`.\n4. A conformance vector pair: substrate's fixture and a connectors-owned copy, byte-identical, so\n   the atlas O1 row can cite both sides.\n\n## Out of Scope\n\nGrant *evaluation* — connectors decides; substrate records and verifies binding only (design 06\n§ 2: \"a higher-layer permit cannot override\" substrate's local checks, and substrate never\nbecomes the authorization engine). Hosted identity material (phase 7).\n\n## Open Questions\n\nWhether the delegated context is a signed JWT-shaped token from identity or a sealed blob from\nconnectors. Decides: the ADR, with both owners. Default if nobody answers: **identity-signed,\naudience `substrate`**, because atlas O1 already pairs \"connectors grants and identity audiences\".\n\n## Design draft — 2026-08-30\n\n`docs/design/09-delegated-context-and-grant-attribution.md` (proposed) carries the shape and the\nADR text ready to accept. Findings that move this story's default: identity issues **opaque**\ntokens and holds no signing key, `kid` or JWKS (`identity/src/lib.rs:1936-1939`, `:2172`), and\n`urn:b10x:substrate` is documented but not issuable (`identity/README.md:144`,\n`identity/src/lib.rs:1920-1922`); connectors **already** issues Ed25519 compact JWS carrying\n`dl_grant`, `sub`, `act` (`connectors/crates/service/src/authority.rs:52-71`) and names the field\n`grant_ref` (`connectors/crates/integration-catalog/src/lib.rs:99-106`). The draft keeps the\nstory's default (identity-signed) with that cost stated; the cheaper path is a connectors-signed\ncontext. Decides: the ADR, with both owners.\n\n## Progress — 2026-08-30, steps 2-4 done, story complete\n\nMerged as `16b2b3b` (PR #20). Every command below was re-run by the orchestrator in the agent's\nworktree, not taken from a report.\n\n**Step 2 — the successor bundle.** `contracts/substrate-wire/0.7.0/`, 224 files, additive successor\nto `0.6.0`, carrying the optional `delegated_context` request member, the `grant_ref` and\n`platform_principal` ledger/event fields and the named refusals.\n\n- `cargo xtask check-bundle 0.7.0` → `contract bundle 0.7.0 verified: 224 files, fixed point of\n  xtask/bundle-source/0.7.0`; `0.5.0` (206) and `0.6.0` (213) still fixed points.\n- `git status --short contracts/` showed only the new directory (invariant 6).\n- `cargo xtask check-json` → `1316 documents in 7 bundles`. This matters: `check-json` landed the\n  same night (`5f332b8`) and classifies every document under every bundle, where the retired Python\n  only ever ran on `0.1.0`–`0.4.0`. `0.7.0` is classified, not skipped.\n\n**Step 3 — the four named tests**, on the wire against the shipped binary over its socket.\n`PORTABLE_CASES` 31 → 33, `DELEGATED_CASES` 48 → 50\n(`crates/substrate-daemon/tests/runtime_vectors.rs:2768-2769`).\n`bash scripts/delegated-lane.sh` → `runtime clean-room: 50 HTTP cases, startup refusal, and\ndual-daemon refusal passed (delegated lane)`, plus the four named tests, 5 passed, exit 0.\n\n**Failing-first is partly missing, and this records that rather than papering over it.** The\nimplementing agent hit a session limit before reporting, so its own failing-first output is lost.\nOne perturbation was reproduced to prove the tests are not vacuous: replacing the subject-binding\ncheck at `crates/substrate-daemon/src/delegation.rs:174` with `if false` makes\n`delegated_context_bound_to_another_subject_is_refused` fail `left: 201, right: 422` — a context\nbound to another subject is accepted where it must be refused. Reverted; the file is byte-identical\nto its backup. **The other three tests were not proven to fail first.** If that matters later, the\nperturbations are cheap to repeat.\n\n**Step 4 — key material.** The test signer derives its seed from a sentence\n(`crates/substrate-daemon/tests/runtime_vectors.rs:2405-2409`) rather than carrying a key blob, so\nthe public repository has nothing to leak or rotate, and the cases mint documents bound to *this*\nmachine's subject and *this* instant — which a committed fixture never can.\n`bash scripts/check-secrets.sh` → `no leaks found`.\n\n**What it does not do**, each a deliberate boundary: it never evaluates the grant (connectors\ndecides); it never signs — `--delegated-context-key <kid>=<issuer>=<base64url>` declares a\n*verifying* key, so which service signs is configuration and changes no substrate code; and the\ngrant-set revision, tenant, actor chain and `jti` are verified as part of the closed claim set and\nthen dropped, keeping the query surface to the two named columns.\n\nEight named refusals: `delegated-context.absent`, `.malformed`, `.unknown-key`,\n`.signature-invalid`, `.audience-mismatch`, `.subject-mismatch`, `.expired`, `.grant-conflict`\n(`crates/substrate-daemon/src/delegation.rs:209-256`).\n\n`delegated_context` is a sibling of `op` and `input`, never a member of `input`\n(`crates/substrate-wire/src/lib.rs:100-113`), so it sits outside the canonical request hash:\nreplaying the same `op` with a fresh context is the same operation and returns the original\noutcome, and a request without one serializes exactly as a `0.6.0` client's did — which is what\nkeeps every frozen bundle's vectors true.\n\n**Still open on the seam, not on substrate:** step 4's conformance vector *pair* needs the\nconnectors-side copy. Substrate's side is byte-reproducible; the atlas O1 row cannot cite both\nsides until connectors holds its copy.\n"},"command":"update-entity","target":"01MEM0000000000000031"},"payload":{"actor":"human:timo","at":1788070411451,"causation":"cmd-protocol-artifact-body-story-ledger-rows-carry-the-declared-grant-1788070411451","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body","event_id":"story:ledger-rows-carry-the-declared-grant@6#0~94a45374a1880da7","executor":null,"recorded_at":"2026-08-30T06:13:31Z"}}
{"entity":"story","version":1,"id":"ledger-rows-carry-the-declared-grant","revision":6,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"gate: passed; 50 HTTP cases (delegated lane); 1316 documents in 7 bundles","source":"bash scripts/gate.sh + delegated-lane.sh, re-run by the orchestrator","target":"01MEM0000000000000031"},"payload":{"actor":"human:timo","at":1788070411000,"causation":"cmd-evidence-story-ledger-rows-carry-the-declared-grant-test_result-1788070411471","change":{"change":"evidence","kind":"test_result","reference":"gate: passed; 50 HTTP cases (delegated lane); 1316 documents in 7 bundles","source":"bash scripts/gate.sh + delegated-lane.sh, re-run by the orchestrator"},"correlation":"protocol-artifact-evidence","event_id":"story:ledger-rows-carry-the-declared-grant@6#0~cd633a1dcc99f801","executor":null,"recorded_at":"2026-08-30T06:13:31Z"}}
{"entity":"story","version":1,"id":"ledger-rows-carry-the-declared-grant","revision":7,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1,\"artifact\":1}}","target":"01MEM0000000000000031","to":"implemented"},"payload":{"actor":"human:timo","at":1788070411494,"causation":"cmd-move-story-ledger-rows-carry-the-declared-grant-1788070411494","change":{"change":"moved","decided_on":{"recorded":{"artifact":1,"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:ledger-rows-carry-the-declared-grant@7#0~ca48530faad7e75a","executor":null,"recorded_at":"2026-08-30T06:13:31Z"}}
{"entity":"story","version":1,"id":"signed-daemon-image","revision":3,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"# Story: A tagged main publishes a signed, digest-pinned daemon image\n\n## Outcome\n\nAn operator can `docker pull ghcr.io/beyond10x/b10x-substrate-daemon@sha256:…` for a tag, verify\nits signature, and know it was built by the bot from that tagged commit.\n\n## Context\n\n`Dockerfile` builds a distroless `substrate-daemon` with an `org.opencontainers.image.revision`\nlabel; no workflow builds, pushes or signs it, and `README.md` § *Status* records \"stable\npublication: **not done**\". `README.md:31` fixes the package prefix `b10x-substrate-*`.\n`AGENTS.md` § *Bot identity* requires automated pushes to use the GitHub App, and § *Releases*\nfixes the tag form: the bare version, annotated, at a fully gated `main` commit.\n\n## Acceptance\n\nAn annotated bare-version tag on `main` produces a keyless-signed image at\n`ghcr.io/beyond10x/b10x-substrate-daemon:<version>` whose digest is written to the GitHub release\nand under the version heading in `CHANGELOG.md`, and a pre-release tag such as `0.2.2-rc.0`\nproduces nothing.\n\nEvidence that satisfies it:\n\n- `.github/workflows/release.yml` triggers only on the bare-version form and refuses to build\n  unless `gate.yml` is green for that commit;\n- `SOURCE_SHA` = the tag's commit; `cosign verify` with the workflow identity succeeds; a signing\n  failure fails the job before anything is announced;\n- release notes state that the wire contract bundles remain development bundles;\n- `permissions` are `contents: read` at workflow level and `packages: write`, `id-token: write` on\n  the release job only; every action SHA-pinned;\n- the pre-release-tag run URL recorded here showing no publish.\n\n## Out of Scope\n\nBit-for-bit reproducible binaries: the sha256 is recorded, reproduction is a later milestone. The\nimage's `VOLUME` and `EXPOSE 8080` describe the development-only TCP posture; release notes must\nnot describe the image as a hosted posture (design 06 § 1).\n\n## Progress — 2026-08-30, the workflow exists and has never run\n\nMerged as `fced5fb` (PR #22). `.github/workflows/release.yml` builds the `Dockerfile`, publishes\n`ghcr.io/beyond10x/b10x-substrate-daemon:<version>`, keyless-signs and digest-pins it.\n\n**No image is published.** The workflow existing and an image existing are different claims;\n`README.md`, `STATUS.md` and `AGENTS.md` § *Releases* each keep them apart.\n\nAcceptance, item by item:\n\n| evidence | state |\n|---|---|\n| triggers only on the bare-version form | **met** — coarse ref glob plus an anchored regex in preflight; `0.2.2-rc.0`, `v0.2.2`, `0.2` and `0.2.2.1` all refuse |\n| refuses to build unless `gate.yml` is green for that commit | **met** — preflight reads that workflow's own recorded conclusion for the tagged SHA rather than re-running a lookalike |\n| `SOURCE_SHA` = the tag's commit | **met** — `git rev-parse refs/tags/<tag>^{commit}`, cross-checked against `github.sha` |\n| a signing failure fails the job before anything is announced | **met** — build → push → sign → `cosign verify` → *then* token, notes, release, changelog |\n| notes state the bundles remain development bundles | **met** — § *What this release does not claim*, citing atlas ADR 0019 |\n| `permissions` scoped; every action SHA-pinned | **met** — `packages: write`/`id-token: write` on the release job alone; preflight holds `actions: read` because no smaller scope reads another workflow's conclusion for a SHA |\n| `cosign verify` with the workflow identity succeeds | **open** — needs a real tag push |\n| a pre-release-tag run URL showing no publish | **open** — proving the negative needs `0.2.2-rc.0` pushed |\n\nAll four action pins were resolved against their upstream repositories rather than taken on report:\n`actions/checkout@3d3c42e5` is `refs/tags/v7` and `v7.0.1`; `sigstore/cosign-installer@6f9f1778` is\n`refs/tags/v4.1.2`; `actions/create-github-app-token@bcd2ba49` is `refs/tags/v3` and `v3.2.0`.\n\n## Blocked on the operator\n\n1. Repository secrets `B10X_BOT_APP_ID` and `B10X_BOT_PRIVATE_KEY`, and a b10x-bot installation\n   with `contents: write`. Absent them the job fails **after** the image is pushed and signed and\n   **before** anything is announced — a signed image with no release.\n2. A real tag push, which is the only thing that can close the two open evidence items. Publishing\n   is outward-facing and was not authorised, so no tag was created.\n\n## Related: the credential scan went red, and why it is green again\n\nCommitting bundle `0.7.0` for `story:ledger-rows-carry-the-declared-grant` turned\n`scripts/check-secrets.sh` red — 32 findings, every one the `jwt` rule on a delegated-context\nconformance vector. Those vectors are JWTs by definition (ADR 0011 fixes the document as a compact\nJWS) and carry no credential. `.gitleaks.toml` keeps the default rule set and allows the `jwt` rule\nfor the delegated-context vector paths alone. Proven narrow rather than asserted: the same token is\nallowed in `contracts/substrate-wire/0.7.0/vectors/http/delegated-context-records-grant.json` and\nstill caught in `crates/substrate-daemon/src/`. `bash scripts/check-secrets.sh` → `no leaks found`,\n76 commits.\n"},"args":{"changes":{"body":"# Story: A tagged main publishes a signed, digest-pinned daemon image\n\n## Outcome\n\nAn operator can `docker pull ghcr.io/beyond10x/b10x-substrate-daemon@sha256:…` for a tag, verify\nits signature, and know it was built by the bot from that tagged commit.\n\n## Context\n\n`Dockerfile` builds a distroless `substrate-daemon` with an `org.opencontainers.image.revision`\nlabel; no workflow builds, pushes or signs it, and `README.md` § *Status* records \"stable\npublication: **not done**\". `README.md:31` fixes the package prefix `b10x-substrate-*`.\n`AGENTS.md` § *Bot identity* requires automated pushes to use the GitHub App, and § *Releases*\nfixes the tag form: the bare version, annotated, at a fully gated `main` commit.\n\n## Acceptance\n\nAn annotated bare-version tag on `main` produces a keyless-signed image at\n`ghcr.io/beyond10x/b10x-substrate-daemon:<version>` whose digest is written to the GitHub release\nand under the version heading in `CHANGELOG.md`, and a pre-release tag such as `0.2.2-rc.0`\nproduces nothing.\n\nEvidence that satisfies it:\n\n- `.github/workflows/release.yml` triggers only on the bare-version form and refuses to build\n  unless `gate.yml` is green for that commit;\n- `SOURCE_SHA` = the tag's commit; `cosign verify` with the workflow identity succeeds; a signing\n  failure fails the job before anything is announced;\n- release notes state that the wire contract bundles remain development bundles;\n- `permissions` are `contents: read` at workflow level and `packages: write`, `id-token: write` on\n  the release job only; every action SHA-pinned;\n- the pre-release-tag run URL recorded here showing no publish.\n\n## Out of Scope\n\nBit-for-bit reproducible binaries: the sha256 is recorded, reproduction is a later milestone. The\nimage's `VOLUME` and `EXPOSE 8080` describe the development-only TCP posture; release notes must\nnot describe the image as a hosted posture (design 06 § 1).\n\n## Progress — 2026-08-30, the workflow exists and has never run\n\nMerged as `fced5fb` (PR #22). `.github/workflows/release.yml` builds the `Dockerfile`, publishes\n`ghcr.io/beyond10x/b10x-substrate-daemon:<version>`, keyless-signs and digest-pins it.\n\n**No image is published.** The workflow existing and an image existing are different claims;\n`README.md`, `STATUS.md` and `AGENTS.md` § *Releases* each keep them apart.\n\nAcceptance, item by item:\n\n| evidence | state |\n|---|---|\n| triggers only on the bare-version form | **met** — coarse ref glob plus an anchored regex in preflight; `0.2.2-rc.0`, `v0.2.2`, `0.2` and `0.2.2.1` all refuse |\n| refuses to build unless `gate.yml` is green for that commit | **met** — preflight reads that workflow's own recorded conclusion for the tagged SHA rather than re-running a lookalike |\n| `SOURCE_SHA` = the tag's commit | **met** — `git rev-parse refs/tags/<tag>^{commit}`, cross-checked against `github.sha` |\n| a signing failure fails the job before anything is announced | **met** — build → push → sign → `cosign verify` → *then* token, notes, release, changelog |\n| notes state the bundles remain development bundles | **met** — § *What this release does not claim*, citing atlas ADR 0019 |\n| `permissions` scoped; every action SHA-pinned | **met** — `packages: write`/`id-token: write` on the release job alone; preflight holds `actions: read` because no smaller scope reads another workflow's conclusion for a SHA |\n| `cosign verify` with the workflow identity succeeds | **open** — needs a real tag push |\n| a pre-release-tag run URL showing no publish | **open** — proving the negative needs `0.2.2-rc.0` pushed |\n\nAll four action pins were resolved against their upstream repositories rather than taken on report:\n`actions/checkout@3d3c42e5` is `refs/tags/v7` and `v7.0.1`; `sigstore/cosign-installer@6f9f1778` is\n`refs/tags/v4.1.2`; `actions/create-github-app-token@bcd2ba49` is `refs/tags/v3` and `v3.2.0`.\n\n## Blocked on the operator\n\n1. Repository secrets `B10X_BOT_APP_ID` and `B10X_BOT_PRIVATE_KEY`, and a b10x-bot installation\n   with `contents: write`. Absent them the job fails **after** the image is pushed and signed and\n   **before** anything is announced — a signed image with no release.\n2. A real tag push, which is the only thing that can close the two open evidence items. Publishing\n   is outward-facing and was not authorised, so no tag was created.\n\n## Related: the credential scan went red, and why it is green again\n\nCommitting bundle `0.7.0` for `story:ledger-rows-carry-the-declared-grant` turned\n`scripts/check-secrets.sh` red — 32 findings, every one the `jwt` rule on a delegated-context\nconformance vector. Those vectors are JWTs by definition (ADR 0011 fixes the document as a compact\nJWS) and carry no credential. `.gitleaks.toml` keeps the default rule set and allows the `jwt` rule\nfor the delegated-context vector paths alone. Proven narrow rather than asserted: the same token is\nallowed in `contracts/substrate-wire/0.7.0/vectors/http/delegated-context-records-grant.json` and\nstill caught in `crates/substrate-daemon/src/`. `bash scripts/check-secrets.sh` → `no leaks found`,\n76 commits.\n"},"command":"update-entity","target":"01MEM0000000000000049"},"payload":{"actor":"human:timo","at":1788072036518,"causation":"cmd-protocol-artifact-body-story-signed-daemon-image-1788072036518","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body","event_id":"story:signed-daemon-image@3#0~9a1494ec741e313b","executor":null,"recorded_at":"2026-08-30T06:40:36Z"}}
{"entity":"story","version":1,"id":"signed-daemon-image","revision":3,"type":"aep.evidence.record/v1","from_state":"draft","to_state":"draft","changed":{},"args":{"command":"record-evidence","kind":"artifact","reference":".github/workflows/release.yml","source":"PR #22, squash fced5fb","target":"01MEM0000000000000049"},"payload":{"actor":"human:timo","at":1788072036000,"causation":"cmd-evidence-story-signed-daemon-image-artifact-1788072036541","change":{"change":"evidence","kind":"artifact","reference":".github/workflows/release.yml","source":"PR #22, squash fced5fb"},"correlation":"protocol-artifact-evidence","event_id":"story:signed-daemon-image@3#0~d30d307af1a620b4","executor":null,"recorded_at":"2026-08-30T06:40:36Z"}}
{"entity":"story","version":1,"id":"signed-daemon-image","revision":4,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"artifact\":1}}","target":"01MEM0000000000000049","to":"proposed"},"payload":{"actor":"human:timo","at":1788072036567,"causation":"cmd-move-story-signed-daemon-image-1788072036567","change":{"change":"moved","decided_on":{"recorded":{"artifact":1}},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:signed-daemon-image@4#0~83b77f4594a45d00","executor":null,"recorded_at":"2026-08-30T06:40:36Z"}}
{"entity":"story","version":1,"id":"signed-daemon-image","revision":5,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"artifact\":1}}","target":"01MEM0000000000000049","to":"active"},"payload":{"actor":"human:timo","at":1788072036589,"causation":"cmd-move-story-signed-daemon-image-1788072036589","change":{"change":"moved","decided_on":{"recorded":{"artifact":1}},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:signed-daemon-image@5#0~efc9cf25c9371a6c","executor":null,"recorded_at":"2026-08-30T06:40:36Z"}}
{"entity":"story","version":1,"id":"aperture-byte-ceiling","revision":3,"type":"aep.evidence.record/v1","from_state":"draft","to_state":"draft","changed":{},"args":{"command":"record-evidence","kind":"approval","reference":"adr/0014-apertures-carry-a-declared-byte-ceiling.md accepted","source":"operator, 2026-08-30","target":"01MEM0000000000000013"},"payload":{"actor":"human:timo","at":1788077362000,"causation":"cmd-evidence-story-aperture-byte-ceiling-approval-1788077362203","change":{"change":"evidence","kind":"approval","reference":"adr/0014-apertures-carry-a-declared-byte-ceiling.md accepted","source":"operator, 2026-08-30"},"correlation":"protocol-artifact-evidence","event_id":"story:aperture-byte-ceiling@3#0~c000dd4705c79fdf","executor":null,"recorded_at":"2026-08-30T08:09:22Z"}}
{"entity":"story","version":1,"id":"aperture-byte-ceiling","revision":4,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"# Story: A declared aperture carries a byte ceiling that refuses mid-run\n\n## Outcome\n\nAn operator who declares an aperture can bound what crosses it, and a run that exceeds the bound is\nstopped with a named refusal rather than left to drain the link. Today the bytes are counted and\nreported but nothing reads them, so a confined run's egress volume is observable after the fact and\nunbounded during it.\n\n## Context\n\n`docs/design/10-destination-bound-egress.md:126` names the condition and its answer:\n*Declared byte ceiling exceeded mid-run* → class `exhausted`, code `exec.aperture-byte-limit`, no\naddress. `:284-287` records why it did not ship with ADR 0013: *\"There is no declared byte ceiling\nin the configuration surface, so there is nothing to exceed.\"*\n\nVerified in the tree, not inferred — the half that exists:\n\n- `ApertureBytes { to_destination, from_destination }`, `crates/substrate-wire/src/lib.rs:790-795`.\n- The forwarder holds both counters as `AtomicU64` and the parent reads them without synchronising,\n  `crates/substrate-host/src/egress.rs:621-622` and `:252-256`.\n- Every applied observation already carries them, `crates/substrate-host/src/egress.rs:288-297`.\n\nThe half that does not:\n\n- `EgressAperture { name, host, port, pinned }`, `crates/substrate-host/src/egress.rs:83-88` — four\n  fields, no ceiling.\n- `--egress-aperture name=host:port/tcp`, `crates/substrate-daemon/src/main.rs:108`, parsed at\n  `:34-56` — the grammar has nowhere to put one.\n- Nothing reads `counters` for a comparison; the only readers are `applied()` and the tests.\n\nThe refusal class already exists: `Exhausted`, `crates/substrate-wire/src/lib.rs:132`.\n\n## Acceptance\n\nAn aperture declared with a ceiling refuses the run by name when the ceiling is crossed, and the\nsame aperture declared without one behaves exactly as it does today.\n\nEvidence that satisfies it, in order:\n\n1. An ADR fixing the declaration grammar, which direction(s) the ceiling counts, whether it is\n   per-run or per-aperture-lifetime, and what the child observes at the moment of refusal\n   (invariant 8: design before code).\n2. A successor bundle carrying the ceiling on the aperture capability fact and the\n   `exec.aperture-byte-limit` refusal class; earlier bundle bytes unchanged (invariant 6).\n3. A delegated-lane vector: a child that reads more than the declared ceiling from the pinned\n   destination ends `exhausted` with code `exec.aperture-byte-limit`, and the applied observation\n   reports at least the ceiling in `bytes`.\n4. A delegated-lane vector proving the negative: an aperture declared with no ceiling passes the\n   same traffic to completion, so the existing `declared-aperture-is-reachable` case\n   (`docs/design/10-destination-bound-egress.md:161`) is unchanged.\n5. A vector proving the ceiling is a *deployment* declaration and not request data — a request that\n   carries a ceiling is refused, the same way a raw destination is\n   (`exec.aperture-destination-in-request`, `docs/design/10-destination-bound-egress.md:124`).\n\n## Out of Scope\n\nRate limits, time ceilings, and any ceiling on the `none` network mode — there is no forwarder there\nto count in.\n\n## Open\n\nWhether the ceiling is enforced in the forwarder (it holds the counters, and can stop reading) or by\nthe parent watching the atomics (it can end the operation with the right class but cannot stop the\nbytes already in flight). The ADR decides; both are consistent with the counters as they stand.\n\n## Design draft — 2026-08-30\n\n`docs/design/12-aperture-byte-ceiling.md`, **proposed**. No ADR number is claimed: `adr/` admits\n`accepted` and `superseded` only (`xtask/src/adrs.rs:12`), so the number is assigned at acceptance.\nDesign 10's deferred note now points at it instead of describing an open gap.\n\nDecisions it fixes, each with what the alternative would have cost:\n\n| Decision | Chosen | Alternative's cost |\n|---|---|---|\n| Grammar | optional `/max=<size>` term on `--egress-aperture name=host:port/tcp`; absent = today byte for byte | a separate flag lets a ceiling name an aperture that does not exist; comma is unavailable either way (`crates/substrate-daemon/src/main.rs:116`, `value_delimiter = ','`) |\n| Direction | one ceiling over `to_destination + from_destination` summed | a per-direction pair is two numbers to get wrong and a bound a child evades by picking a direction |\n| Scope | per run | per-lifetime needs durable cross-run accounting and makes a refusal unreproducible from its own request |\n| Enforcement | in the relay, which stops at the ceiling; overshoot bounded by `RELAY_BUFFER = 16_384` (`crates/substrate-host/src/egress.rs:68`) | parent-only stops no byte: overshoot is bounded by the destination's throughput, not by substrate |\n\n**Gap the draft found, verified not inferred.** At HEAD a mid-run bound has no code to report. Both\nexisting mid-run bounds fold into one state: `forced_cancellation = timed_out || cpu_exhausted ||\ncancellation_requested` (`crates/substrate-host/src/process.rs:1318-1319`) sets\n`ExecState::Cancelled` (`:1364`). The supervision loop that would notice a ceiling already exists\nand already polls at 1 ms (`:1392`, `tokio::time::interval(Duration::from_millis(1))`), so the\nmechanism is there and the vocabulary is not — `exhausted` / `exec.aperture-byte-limit`\n(`docs/design/10-destination-bound-egress.md:126`) has nowhere to live in today's observation. The\ndraft therefore also proposes an optional class/code/message field on the exec observation, which is\nwhat makes this a contract change rather than a host-local one.\n\n## Blocked on\n\nOperator acceptance of design 12. Invariant 8: an ADR before code. Nothing here is implementable\nuntil that decision is made.\n\n## Accepted — 2026-08-30\n\nThe operator accepted design 12 as\n[`adr/0014-apertures-carry-a-declared-byte-ceiling.md`](../../../adr/0014-apertures-carry-a-declared-byte-ceiling.md).\nEvidence item 1 is satisfied; invariant 8 no longer blocks implementation.\n\nOne number moved while the design waited: it was drafted against successor bundle `0.7.0`, and that\nnumber went to ADR 0011's grant attribution. The successor is **`0.8.0`**, predecessor `0.7.0`,\n`adds_routes: 0`, `preserves_routes: 26` (`contracts/substrate-wire/0.7.0/bundle.json:5-10`), with\n`cargo xtask check-bundle 0.8.0` added to `scripts/gate.sh`.\n\nRemaining evidence: items 2-5 — the successor bundle, the two delegated-lane vectors (the ceiling\nrefuses; an aperture without one is unchanged) and the request-side refusal\n`exec.aperture-ceiling-in-request`.\n"},"args":{"changes":{"body":"# Story: A declared aperture carries a byte ceiling that refuses mid-run\n\n## Outcome\n\nAn operator who declares an aperture can bound what crosses it, and a run that exceeds the bound is\nstopped with a named refusal rather than left to drain the link. Today the bytes are counted and\nreported but nothing reads them, so a confined run's egress volume is observable after the fact and\nunbounded during it.\n\n## Context\n\n`docs/design/10-destination-bound-egress.md:126` names the condition and its answer:\n*Declared byte ceiling exceeded mid-run* → class `exhausted`, code `exec.aperture-byte-limit`, no\naddress. `:284-287` records why it did not ship with ADR 0013: *\"There is no declared byte ceiling\nin the configuration surface, so there is nothing to exceed.\"*\n\nVerified in the tree, not inferred — the half that exists:\n\n- `ApertureBytes { to_destination, from_destination }`, `crates/substrate-wire/src/lib.rs:790-795`.\n- The forwarder holds both counters as `AtomicU64` and the parent reads them without synchronising,\n  `crates/substrate-host/src/egress.rs:621-622` and `:252-256`.\n- Every applied observation already carries them, `crates/substrate-host/src/egress.rs:288-297`.\n\nThe half that does not:\n\n- `EgressAperture { name, host, port, pinned }`, `crates/substrate-host/src/egress.rs:83-88` — four\n  fields, no ceiling.\n- `--egress-aperture name=host:port/tcp`, `crates/substrate-daemon/src/main.rs:108`, parsed at\n  `:34-56` — the grammar has nowhere to put one.\n- Nothing reads `counters` for a comparison; the only readers are `applied()` and the tests.\n\nThe refusal class already exists: `Exhausted`, `crates/substrate-wire/src/lib.rs:132`.\n\n## Acceptance\n\nAn aperture declared with a ceiling refuses the run by name when the ceiling is crossed, and the\nsame aperture declared without one behaves exactly as it does today.\n\nEvidence that satisfies it, in order:\n\n1. An ADR fixing the declaration grammar, which direction(s) the ceiling counts, whether it is\n   per-run or per-aperture-lifetime, and what the child observes at the moment of refusal\n   (invariant 8: design before code).\n2. A successor bundle carrying the ceiling on the aperture capability fact and the\n   `exec.aperture-byte-limit` refusal class; earlier bundle bytes unchanged (invariant 6).\n3. A delegated-lane vector: a child that reads more than the declared ceiling from the pinned\n   destination ends `exhausted` with code `exec.aperture-byte-limit`, and the applied observation\n   reports at least the ceiling in `bytes`.\n4. A delegated-lane vector proving the negative: an aperture declared with no ceiling passes the\n   same traffic to completion, so the existing `declared-aperture-is-reachable` case\n   (`docs/design/10-destination-bound-egress.md:161`) is unchanged.\n5. A vector proving the ceiling is a *deployment* declaration and not request data — a request that\n   carries a ceiling is refused, the same way a raw destination is\n   (`exec.aperture-destination-in-request`, `docs/design/10-destination-bound-egress.md:124`).\n\n## Out of Scope\n\nRate limits, time ceilings, and any ceiling on the `none` network mode — there is no forwarder there\nto count in.\n\n## Open\n\nWhether the ceiling is enforced in the forwarder (it holds the counters, and can stop reading) or by\nthe parent watching the atomics (it can end the operation with the right class but cannot stop the\nbytes already in flight). The ADR decides; both are consistent with the counters as they stand.\n\n## Design draft — 2026-08-30\n\n`docs/design/12-aperture-byte-ceiling.md`, **proposed**. No ADR number is claimed: `adr/` admits\n`accepted` and `superseded` only (`xtask/src/adrs.rs:12`), so the number is assigned at acceptance.\nDesign 10's deferred note now points at it instead of describing an open gap.\n\nDecisions it fixes, each with what the alternative would have cost:\n\n| Decision | Chosen | Alternative's cost |\n|---|---|---|\n| Grammar | optional `/max=<size>` term on `--egress-aperture name=host:port/tcp`; absent = today byte for byte | a separate flag lets a ceiling name an aperture that does not exist; comma is unavailable either way (`crates/substrate-daemon/src/main.rs:116`, `value_delimiter = ','`) |\n| Direction | one ceiling over `to_destination + from_destination` summed | a per-direction pair is two numbers to get wrong and a bound a child evades by picking a direction |\n| Scope | per run | per-lifetime needs durable cross-run accounting and makes a refusal unreproducible from its own request |\n| Enforcement | in the relay, which stops at the ceiling; overshoot bounded by `RELAY_BUFFER = 16_384` (`crates/substrate-host/src/egress.rs:68`) | parent-only stops no byte: overshoot is bounded by the destination's throughput, not by substrate |\n\n**Gap the draft found, verified not inferred.** At HEAD a mid-run bound has no code to report. Both\nexisting mid-run bounds fold into one state: `forced_cancellation = timed_out || cpu_exhausted ||\ncancellation_requested` (`crates/substrate-host/src/process.rs:1318-1319`) sets\n`ExecState::Cancelled` (`:1364`). The supervision loop that would notice a ceiling already exists\nand already polls at 1 ms (`:1392`, `tokio::time::interval(Duration::from_millis(1))`), so the\nmechanism is there and the vocabulary is not — `exhausted` / `exec.aperture-byte-limit`\n(`docs/design/10-destination-bound-egress.md:126`) has nowhere to live in today's observation. The\ndraft therefore also proposes an optional class/code/message field on the exec observation, which is\nwhat makes this a contract change rather than a host-local one.\n\n## Blocked on\n\nOperator acceptance of design 12. Invariant 8: an ADR before code. Nothing here is implementable\nuntil that decision is made.\n\n## Accepted — 2026-08-30\n\nThe operator accepted design 12 as\n[`adr/0014-apertures-carry-a-declared-byte-ceiling.md`](../../../adr/0014-apertures-carry-a-declared-byte-ceiling.md).\nEvidence item 1 is satisfied; invariant 8 no longer blocks implementation.\n\nOne number moved while the design waited: it was drafted against successor bundle `0.7.0`, and that\nnumber went to ADR 0011's grant attribution. The successor is **`0.8.0`**, predecessor `0.7.0`,\n`adds_routes: 0`, `preserves_routes: 26` (`contracts/substrate-wire/0.7.0/bundle.json:5-10`), with\n`cargo xtask check-bundle 0.8.0` added to `scripts/gate.sh`.\n\nRemaining evidence: items 2-5 — the successor bundle, the two delegated-lane vectors (the ceiling\nrefuses; an aperture without one is unchanged) and the request-side refusal\n`exec.aperture-ceiling-in-request`.\n"},"command":"update-entity","target":"01MEM0000000000000013"},"payload":{"actor":"human:timo","at":1788077362248,"causation":"cmd-protocol-artifact-body-story-aperture-byte-ceiling-1788077362248","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body","event_id":"story:aperture-byte-ceiling@4#0~77e19bfe41ad9758","executor":null,"recorded_at":"2026-08-30T08:09:22Z"}}
{"entity":"story","version":1,"id":"secret-slot-probe-observes-what-it-claims","revision":3,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"# Story: The secret-slot probe observes the seals and descriptor set it publishes a fact about\n\n## Outcome\n\n`secrets.slots` means what design 11 says it means. Today the capability fact is published on a\nweaker observation than the one its design names, so a host whose bubblewrap passed a descriptor\nwith the wrong seals — or with extra descriptors leaked in — would still advertise the capability.\n\n## Context\n\n`docs/design/11-sealed-secret-slots.md:108-110` states the condition for publishing the fact:\n\n> a bubblewrap child reporting the probe descriptor at its declared number with **the same seals and\n> nothing else above 2**\n\nThe probe child does neither of those two things. Its whole command is\n`crates/substrate-host/src/probe.rs:296-298`:\n\n```\ncat <&{target}; if echo x >&{target} 2>/dev/null; then printf writable; else printf sealed; fi\n```\n\nand the acceptance is `output.stdout == format!(\"{sentinel}sealed\")`\n(`crates/substrate-host/src/probe.rs:313`). So the child proves the descriptor arrived at its number\nand is not writable. It never calls `fcntl(F_GET_SEALS)`, so \"the same seals\" is unobserved — a\ndescriptor sealed `F_SEAL_WRITE` alone would pass. It never enumerates `/proc/self/fd`, so \"nothing\nelse above 2\" is unobserved.\n\nThe in-process half of the condition *is* observed — `memfd_create` + `F_ADD_SEALS` with\n`F_GET_SEALS` reading back `0xf` and a refused write — and the parent enforces the rest by\nconstruction (`crates/substrate-host/src/secrets.rs`, `place_and_close`). Enforced by construction\nis not the same as observed, and invariant 3 is that a guarantee substrate cannot verify is a named\nrefusal, not a fact published anyway.\n\nThis is not a live exposure: `crates/substrate-daemon/tests/runtime_vectors.rs` now observes both\nproperties at run time on the delegated lane — the child reports `fds:[0,1,2,7]`, `memfds:[7]`,\n`seals:15`. The gap is that the *gating probe* does less than the test does, so the fact can be\npublished on a host where the test never ran.\n\nFound while proving `story:sealed-secret-slots` on the delegated lane, 2026-08-30.\n\n## Acceptance\n\nThe probe child reports its seal word and its descriptor set, and `secrets.slots` is withheld when\neither disagrees with what design 11 § 5 requires.\n\nEvidence that satisfies it:\n\n1. The probe child reads `F_GET_SEALS` on the target descriptor and enumerates `/proc/self/fd`, and\n   the parent compares both against the declared values — not against a substring of `cat` output.\n2. A failing-first test per property: a probe whose child reports a short seal word withholds the\n   fact; a probe whose child reports an extra descriptor withholds the fact. Both must fail before\n   the change and pass after.\n3. `secrets.slots` is still published on this host after the change (the probe did not become\n   unsatisfiable), proven by `bash scripts/delegated-lane.sh` still reporting its full case count.\n4. Whichever way it lands, `docs/design/11-sealed-secret-slots.md:108-110` and the code agree\n   afterwards. If the decision is instead that the design overstates what a probe can cheaply\n   observe, that is an amendment to an accepted ADR (0012) and needs the operator, not a quiet\n   edit.\n\n## Out of Scope\n\nChanging the seal set, the slot naming rule, or anything about how a slot reaches a child. This\nstory is only about what the probe observes before it publishes the fact.\n\n## Implemented — 2026-08-30\n\nThe probe now observes both properties it publishes a fact about.\n\n| property | how the child reports it | perturbation | failure |\n|---|---|---|---|\n| the seal word | reports `inode=` from `/proc/$$/fdinfo/<fd>` plus its `/memfd:substrate-slot-probe` link, the sentinel bytes and a refused write; the parent reads `F_GET_SEALS` off that inode (`crates/substrate-host/src/probe.rs:419-421`) | drop the `slot.seals == SEAL_SET` term | `a_short_seal_word_withholds_the_fact ... FAILED` |\n| the descriptor set | `fds= 0 1 2 7` from a `/proc/$$/fd/*` glob filtered by `[ -L ]`; the parent compares to `{0,1,2,target}` (`crates/substrate-host/src/probe.rs:422`) | drop the `observed.descriptors == retained` term | `a_child_holding_an_extra_descriptor_withholds_the_fact ... FAILED` |\n\nBoth perturbations were applied by the orchestrator and reverted; `crates/substrate-host/src/probe.rs`\nis byte-identical to its backup and `cargo test -p substrate-host --lib` is `46 passed`.\n\n**The child does not call `fcntl(F_GET_SEALS)` itself, and cannot.** No shell has the call, and\n`/proc/<pid>/fdinfo` carries only `pos`, `flags`, `mnt_id` and `ino` for a memfd. Measured on this\nhost rather than assumed: a sealed memfd's fdinfo on 6.6.144-3-MANJARO prints exactly those four\nlines and no `seals:`. The only child that could issue it is an interpreter, and the shipped image\nis `gcr.io/distroless/cc-debian12` (`Dockerfile:12`), so requiring one would withhold\n`secrets.slots` on every shell-only host — trading a real capability for a literal reading.\n\n**Why the inode form is equivalent and not weaker.** Seals are state of the *inode*, not of a\ndescriptor. The child proves it holds that inode — inode number, the memfd link name, the sentinel\nbytes and a refused write — and the parent reads the seal word off the same inode. `SEAL_SET`\ncontains `F_SEAL_SEAL`, so the word cannot move afterwards. What was unobserved before is observed\nnow: a descriptor sealed `F_SEAL_WRITE` alone withholds the fact, and so does a child holding a\ndescriptor above the declared set.\n\n`docs/design/11-sealed-secret-slots.md` § 5 is rewritten to describe this, so acceptance item 4 —\ndesign and code agree — holds. ADR 0012 needed no amendment: it says the probe \"proves sealing,\ndescriptor isolation across bubblewrap, and orphan reconciliation\"\n(`adr/0012-secret-slots-are-sealed-memfds.md:30`) and never fixed how the child reports.\n\n`secrets.slots` is still published here: `bash scripts/delegated-lane.sh` → `runtime clean-room:\n50 HTTP cases, startup refusal, and dual-daemon refusal passed (delegated lane)`, exit 0.\n`bash scripts/gate.sh` → `gate: passed`, exit 0.\n"},"args":{"changes":{"body":"# Story: The secret-slot probe observes the seals and descriptor set it publishes a fact about\n\n## Outcome\n\n`secrets.slots` means what design 11 says it means. Today the capability fact is published on a\nweaker observation than the one its design names, so a host whose bubblewrap passed a descriptor\nwith the wrong seals — or with extra descriptors leaked in — would still advertise the capability.\n\n## Context\n\n`docs/design/11-sealed-secret-slots.md:108-110` states the condition for publishing the fact:\n\n> a bubblewrap child reporting the probe descriptor at its declared number with **the same seals and\n> nothing else above 2**\n\nThe probe child does neither of those two things. Its whole command is\n`crates/substrate-host/src/probe.rs:296-298`:\n\n```\ncat <&{target}; if echo x >&{target} 2>/dev/null; then printf writable; else printf sealed; fi\n```\n\nand the acceptance is `output.stdout == format!(\"{sentinel}sealed\")`\n(`crates/substrate-host/src/probe.rs:313`). So the child proves the descriptor arrived at its number\nand is not writable. It never calls `fcntl(F_GET_SEALS)`, so \"the same seals\" is unobserved — a\ndescriptor sealed `F_SEAL_WRITE` alone would pass. It never enumerates `/proc/self/fd`, so \"nothing\nelse above 2\" is unobserved.\n\nThe in-process half of the condition *is* observed — `memfd_create` + `F_ADD_SEALS` with\n`F_GET_SEALS` reading back `0xf` and a refused write — and the parent enforces the rest by\nconstruction (`crates/substrate-host/src/secrets.rs`, `place_and_close`). Enforced by construction\nis not the same as observed, and invariant 3 is that a guarantee substrate cannot verify is a named\nrefusal, not a fact published anyway.\n\nThis is not a live exposure: `crates/substrate-daemon/tests/runtime_vectors.rs` now observes both\nproperties at run time on the delegated lane — the child reports `fds:[0,1,2,7]`, `memfds:[7]`,\n`seals:15`. The gap is that the *gating probe* does less than the test does, so the fact can be\npublished on a host where the test never ran.\n\nFound while proving `story:sealed-secret-slots` on the delegated lane, 2026-08-30.\n\n## Acceptance\n\nThe probe child reports its seal word and its descriptor set, and `secrets.slots` is withheld when\neither disagrees with what design 11 § 5 requires.\n\nEvidence that satisfies it:\n\n1. The probe child reads `F_GET_SEALS` on the target descriptor and enumerates `/proc/self/fd`, and\n   the parent compares both against the declared values — not against a substring of `cat` output.\n2. A failing-first test per property: a probe whose child reports a short seal word withholds the\n   fact; a probe whose child reports an extra descriptor withholds the fact. Both must fail before\n   the change and pass after.\n3. `secrets.slots` is still published on this host after the change (the probe did not become\n   unsatisfiable), proven by `bash scripts/delegated-lane.sh` still reporting its full case count.\n4. Whichever way it lands, `docs/design/11-sealed-secret-slots.md:108-110` and the code agree\n   afterwards. If the decision is instead that the design overstates what a probe can cheaply\n   observe, that is an amendment to an accepted ADR (0012) and needs the operator, not a quiet\n   edit.\n\n## Out of Scope\n\nChanging the seal set, the slot naming rule, or anything about how a slot reaches a child. This\nstory is only about what the probe observes before it publishes the fact.\n\n## Implemented — 2026-08-30\n\nThe probe now observes both properties it publishes a fact about.\n\n| property | how the child reports it | perturbation | failure |\n|---|---|---|---|\n| the seal word | reports `inode=` from `/proc/$$/fdinfo/<fd>` plus its `/memfd:substrate-slot-probe` link, the sentinel bytes and a refused write; the parent reads `F_GET_SEALS` off that inode (`crates/substrate-host/src/probe.rs:419-421`) | drop the `slot.seals == SEAL_SET` term | `a_short_seal_word_withholds_the_fact ... FAILED` |\n| the descriptor set | `fds= 0 1 2 7` from a `/proc/$$/fd/*` glob filtered by `[ -L ]`; the parent compares to `{0,1,2,target}` (`crates/substrate-host/src/probe.rs:422`) | drop the `observed.descriptors == retained` term | `a_child_holding_an_extra_descriptor_withholds_the_fact ... FAILED` |\n\nBoth perturbations were applied by the orchestrator and reverted; `crates/substrate-host/src/probe.rs`\nis byte-identical to its backup and `cargo test -p substrate-host --lib` is `46 passed`.\n\n**The child does not call `fcntl(F_GET_SEALS)` itself, and cannot.** No shell has the call, and\n`/proc/<pid>/fdinfo` carries only `pos`, `flags`, `mnt_id` and `ino` for a memfd. Measured on this\nhost rather than assumed: a sealed memfd's fdinfo on 6.6.144-3-MANJARO prints exactly those four\nlines and no `seals:`. The only child that could issue it is an interpreter, and the shipped image\nis `gcr.io/distroless/cc-debian12` (`Dockerfile:12`), so requiring one would withhold\n`secrets.slots` on every shell-only host — trading a real capability for a literal reading.\n\n**Why the inode form is equivalent and not weaker.** Seals are state of the *inode*, not of a\ndescriptor. The child proves it holds that inode — inode number, the memfd link name, the sentinel\nbytes and a refused write — and the parent reads the seal word off the same inode. `SEAL_SET`\ncontains `F_SEAL_SEAL`, so the word cannot move afterwards. What was unobserved before is observed\nnow: a descriptor sealed `F_SEAL_WRITE` alone withholds the fact, and so does a child holding a\ndescriptor above the declared set.\n\n`docs/design/11-sealed-secret-slots.md` § 5 is rewritten to describe this, so acceptance item 4 —\ndesign and code agree — holds. ADR 0012 needed no amendment: it says the probe \"proves sealing,\ndescriptor isolation across bubblewrap, and orphan reconciliation\"\n(`adr/0012-secret-slots-are-sealed-memfds.md:30`) and never fixed how the child reports.\n\n`secrets.slots` is still published here: `bash scripts/delegated-lane.sh` → `runtime clean-room:\n50 HTTP cases, startup refusal, and dual-daemon refusal passed (delegated lane)`, exit 0.\n`bash scripts/gate.sh` → `gate: passed`, exit 0.\n"},"command":"update-entity","target":"01MEM0000000000000046"},"payload":{"actor":"human:timo","at":1788078835145,"causation":"cmd-protocol-artifact-body-story-secret-slot-probe-observes-what-it-claims-1788078835145","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body","event_id":"story:secret-slot-probe-observes-what-it-claims@3#0~9fc5521c209c0690","executor":null,"recorded_at":"2026-08-30T08:33:55Z"}}
{"entity":"story","version":1,"id":"secret-slot-probe-observes-what-it-claims","revision":3,"type":"aep.evidence.record/v1","from_state":"draft","to_state":"draft","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"both tests fail when their term is dropped; 50 HTTP cases, exit 0","source":"orchestrator perturbation + delegated lane","target":"01MEM0000000000000046"},"payload":{"actor":"human:timo","at":1788078835000,"causation":"cmd-evidence-story-secret-slot-probe-observes-what-it-claims-test_result-1788078835166","change":{"change":"evidence","kind":"test_result","reference":"both tests fail when their term is dropped; 50 HTTP cases, exit 0","source":"orchestrator perturbation + delegated lane"},"correlation":"protocol-artifact-evidence","event_id":"story:secret-slot-probe-observes-what-it-claims@3#0~b43f3915100a81f0","executor":null,"recorded_at":"2026-08-30T08:33:55Z"}}
{"entity":"story","version":1,"id":"secret-slot-probe-observes-what-it-claims","revision":4,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1}}","target":"01MEM0000000000000046","to":"proposed"},"payload":{"actor":"human:timo","at":1788078835190,"causation":"cmd-move-story-secret-slot-probe-observes-what-it-claims-1788078835190","change":{"change":"moved","decided_on":{"recorded":{"test_result":1}},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:secret-slot-probe-observes-what-it-claims@4#0~e14e491dc3801ced","executor":null,"recorded_at":"2026-08-30T08:33:55Z"}}
{"entity":"story","version":1,"id":"secret-slot-probe-observes-what-it-claims","revision":5,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1}}","target":"01MEM0000000000000046","to":"active"},"payload":{"actor":"human:timo","at":1788078835215,"causation":"cmd-move-story-secret-slot-probe-observes-what-it-claims-1788078835215","change":{"change":"moved","decided_on":{"recorded":{"test_result":1}},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:secret-slot-probe-observes-what-it-claims@5#0~399883ae04667a9d","executor":null,"recorded_at":"2026-08-30T08:33:55Z"}}
{"entity":"story","version":1,"id":"secret-slot-probe-observes-what-it-claims","revision":6,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1}}","target":"01MEM0000000000000046","to":"implemented"},"payload":{"actor":"human:timo","at":1788078835239,"causation":"cmd-move-story-secret-slot-probe-observes-what-it-claims-1788078835239","change":{"change":"moved","decided_on":{"recorded":{"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:secret-slot-probe-observes-what-it-claims@6#0~e231abc404327f67","executor":null,"recorded_at":"2026-08-30T08:33:55Z"}}
{"entity":"story","version":1,"id":"aperture-byte-ceiling","revision":5,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"# Story: A declared aperture carries a byte ceiling that refuses mid-run\n\n## Outcome\n\nAn operator who declares an aperture can bound what crosses it, and a run that exceeds the bound is\nstopped with a named refusal rather than left to drain the link. Today the bytes are counted and\nreported but nothing reads them, so a confined run's egress volume is observable after the fact and\nunbounded during it.\n\n## Context\n\n`docs/design/10-destination-bound-egress.md:126` names the condition and its answer:\n*Declared byte ceiling exceeded mid-run* → class `exhausted`, code `exec.aperture-byte-limit`, no\naddress. `:284-287` records why it did not ship with ADR 0013: *\"There is no declared byte ceiling\nin the configuration surface, so there is nothing to exceed.\"*\n\nVerified in the tree, not inferred — the half that exists:\n\n- `ApertureBytes { to_destination, from_destination }`, `crates/substrate-wire/src/lib.rs:790-795`.\n- The forwarder holds both counters as `AtomicU64` and the parent reads them without synchronising,\n  `crates/substrate-host/src/egress.rs:621-622` and `:252-256`.\n- Every applied observation already carries them, `crates/substrate-host/src/egress.rs:288-297`.\n\nThe half that does not:\n\n- `EgressAperture { name, host, port, pinned }`, `crates/substrate-host/src/egress.rs:83-88` — four\n  fields, no ceiling.\n- `--egress-aperture name=host:port/tcp`, `crates/substrate-daemon/src/main.rs:108`, parsed at\n  `:34-56` — the grammar has nowhere to put one.\n- Nothing reads `counters` for a comparison; the only readers are `applied()` and the tests.\n\nThe refusal class already exists: `Exhausted`, `crates/substrate-wire/src/lib.rs:132`.\n\n## Acceptance\n\nAn aperture declared with a ceiling refuses the run by name when the ceiling is crossed, and the\nsame aperture declared without one behaves exactly as it does today.\n\nEvidence that satisfies it, in order:\n\n1. An ADR fixing the declaration grammar, which direction(s) the ceiling counts, whether it is\n   per-run or per-aperture-lifetime, and what the child observes at the moment of refusal\n   (invariant 8: design before code).\n2. A successor bundle carrying the ceiling on the aperture capability fact and the\n   `exec.aperture-byte-limit` refusal class; earlier bundle bytes unchanged (invariant 6).\n3. A delegated-lane vector: a child that reads more than the declared ceiling from the pinned\n   destination ends `exhausted` with code `exec.aperture-byte-limit`, and the applied observation\n   reports at least the ceiling in `bytes`.\n4. A delegated-lane vector proving the negative: an aperture declared with no ceiling passes the\n   same traffic to completion, so the existing `declared-aperture-is-reachable` case\n   (`docs/design/10-destination-bound-egress.md:161`) is unchanged.\n5. A vector proving the ceiling is a *deployment* declaration and not request data — a request that\n   carries a ceiling is refused, the same way a raw destination is\n   (`exec.aperture-destination-in-request`, `docs/design/10-destination-bound-egress.md:124`).\n\n## Out of Scope\n\nRate limits, time ceilings, and any ceiling on the `none` network mode — there is no forwarder there\nto count in.\n\n## Open\n\nWhether the ceiling is enforced in the forwarder (it holds the counters, and can stop reading) or by\nthe parent watching the atomics (it can end the operation with the right class but cannot stop the\nbytes already in flight). The ADR decides; both are consistent with the counters as they stand.\n\n## Design draft — 2026-08-30\n\n`docs/design/12-aperture-byte-ceiling.md`, **proposed**. No ADR number is claimed: `adr/` admits\n`accepted` and `superseded` only (`xtask/src/adrs.rs:12`), so the number is assigned at acceptance.\nDesign 10's deferred note now points at it instead of describing an open gap.\n\nDecisions it fixes, each with what the alternative would have cost:\n\n| Decision | Chosen | Alternative's cost |\n|---|---|---|\n| Grammar | optional `/max=<size>` term on `--egress-aperture name=host:port/tcp`; absent = today byte for byte | a separate flag lets a ceiling name an aperture that does not exist; comma is unavailable either way (`crates/substrate-daemon/src/main.rs:116`, `value_delimiter = ','`) |\n| Direction | one ceiling over `to_destination + from_destination` summed | a per-direction pair is two numbers to get wrong and a bound a child evades by picking a direction |\n| Scope | per run | per-lifetime needs durable cross-run accounting and makes a refusal unreproducible from its own request |\n| Enforcement | in the relay, which stops at the ceiling; overshoot bounded by `RELAY_BUFFER = 16_384` (`crates/substrate-host/src/egress.rs:68`) | parent-only stops no byte: overshoot is bounded by the destination's throughput, not by substrate |\n\n**Gap the draft found, verified not inferred.** At HEAD a mid-run bound has no code to report. Both\nexisting mid-run bounds fold into one state: `forced_cancellation = timed_out || cpu_exhausted ||\ncancellation_requested` (`crates/substrate-host/src/process.rs:1318-1319`) sets\n`ExecState::Cancelled` (`:1364`). The supervision loop that would notice a ceiling already exists\nand already polls at 1 ms (`:1392`, `tokio::time::interval(Duration::from_millis(1))`), so the\nmechanism is there and the vocabulary is not — `exhausted` / `exec.aperture-byte-limit`\n(`docs/design/10-destination-bound-egress.md:126`) has nowhere to live in today's observation. The\ndraft therefore also proposes an optional class/code/message field on the exec observation, which is\nwhat makes this a contract change rather than a host-local one.\n\n## Blocked on\n\nOperator acceptance of design 12. Invariant 8: an ADR before code. Nothing here is implementable\nuntil that decision is made.\n\n## Accepted — 2026-08-30\n\nThe operator accepted design 12 as\n[`adr/0014-apertures-carry-a-declared-byte-ceiling.md`](../../../adr/0014-apertures-carry-a-declared-byte-ceiling.md).\nEvidence item 1 is satisfied; invariant 8 no longer blocks implementation.\n\nOne number moved while the design waited: it was drafted against successor bundle `0.7.0`, and that\nnumber went to ADR 0011's grant attribution. The successor is **`0.8.0`**, predecessor `0.7.0`,\n`adds_routes: 0`, `preserves_routes: 26` (`contracts/substrate-wire/0.7.0/bundle.json:5-10`), with\n`cargo xtask check-bundle 0.8.0` added to `scripts/gate.sh`.\n\nRemaining evidence: items 2-5 — the successor bundle, the two delegated-lane vectors (the ceiling\nrefuses; an aperture without one is unchanged) and the request-side refusal\n`exec.aperture-ceiling-in-request`.\n\n## Scope\n\nDerived 2026-08-30 by `story-scoper`, verified against HEAD `34b219a`. Every line is **cited** or\n**inferred**.\n\n- **Primary surface:** `crates/substrate-host` — the aperture byte path — cited (ADR 0014\n  § *Decision*, \"Enforced in the relay, classified by the parent\").\n- **Files, cited and exact at HEAD:** `crates/substrate-host/src/egress.rs:83-88`\n  (`EgressAperture`, gains `Option<u64>`), `:68` (`RELAY_BUFFER`, the stated overshoot bound),\n  `:713-818` (`relay_body`/`forward_once`, where the comparison lands), `:252-256`\n  (`SharedCounters::read`), `:322` (`install`, per-run counter lifetime), `:92-93`\n  (`EgressAperture::fact`).\n- **Files, cited:** `crates/substrate-host/src/process.rs:1318-1319` (`forced_cancellation`),\n  `:1366` (`ExecState::Cancelled`), `:1392-1414` (1 ms supervision poll and `cgroup.kill_all`),\n  `:919` (driver-side aperture refusal mapping).\n- **Files, cited with positions corrected:** `crates/substrate-wire/src/lib.rs:783`\n  (`AppliedAperture`), `:813` (`ApertureBytes`), `:1874` (`EgressApertureFact`), `:983-990`\n  (`Exec`, gains the optional class/code/message field), `:1577` and `:1627`\n  (`WireValidationError`), `:1909` (`validate_aperture_request`), `:155` (`Exhausted`).\n- **Files, cited with positions corrected:** `crates/substrate-daemon/src/main.rs:34-60`\n  (`parse_egress_aperture`, gains `/max=<size>`), `:144-155` (the clap arg; `value_delimiter = ','`\n  at `:152` is why the separator cannot be a comma).\n- **Contract surface, cited:** new `contracts/substrate-wire/0.8.0/` (predecessor `0.7.0`,\n  `adds_routes: 0`, `preserves_routes: 26`, mirroring\n  `contracts/substrate-wire/0.7.0/bundle.json:5-10`), new `xtask/bundle-source/0.8.0/`, and a\n  `check-bundle 0.8.0` line after `scripts/gate.sh:29`.\n- **Also likely:** `xtask/src/bundle.rs:384` (per-version branch) and its census functions at\n  `:681-682`, `:704`, `:865` — inferred; no document names the file.\n- **Also likely:** `crates/substrate-daemon/tests/runtime_vectors.rs` and `tests/contract_vectors.rs`\n  for evidence items 3-5 — inferred by analogy with `vectors/http/aperture-destination-in-request-refused.json`,\n  the only aperture vector that exists.\n- **Confidence:** **high** — ADR 0014 and design 12 name the declaration, enforcement, contract and\n  bundle sites with `file:line`, and every host-side citation reproduces exactly at HEAD.\n- **Would collide with:** any unit touching `crates/substrate-wire/src/lib.rs` (this story edits it\n  in six places), the egress byte path, the supervision loop, the daemon CLI flag surface, or the\n  bundle gate. **Hard exclusion:** any other unit cutting a successor bundle — `0.8.0` is one\n  directory and one gate line, and two stories cannot both own it.\n\n### Gap found while scoping, not present in either document\n\n`crates/substrate-daemon/src/runtime.rs:335-339` declares a **second** `EgressAperture` — the\ndaemon's own three-field config vocabulary (`name`, `host`, `port`), deliberately not re-exported\nfrom `substrate_host` (invariant 4), converted at the composition root (`:424`, `:451`). ADR 0014 and\n`docs/design/12-aperture-byte-ceiling.md` mention `runtime.rs` **zero times**. The ceiling has to\nland there too, or the declared value cannot reach the host type the ADR names. Verified:\n`grep -c runtime.rs` over both documents returns `0`.\n"},"args":{"changes":{"body":"# Story: A declared aperture carries a byte ceiling that refuses mid-run\n\n## Outcome\n\nAn operator who declares an aperture can bound what crosses it, and a run that exceeds the bound is\nstopped with a named refusal rather than left to drain the link. Today the bytes are counted and\nreported but nothing reads them, so a confined run's egress volume is observable after the fact and\nunbounded during it.\n\n## Context\n\n`docs/design/10-destination-bound-egress.md:126` names the condition and its answer:\n*Declared byte ceiling exceeded mid-run* → class `exhausted`, code `exec.aperture-byte-limit`, no\naddress. `:284-287` records why it did not ship with ADR 0013: *\"There is no declared byte ceiling\nin the configuration surface, so there is nothing to exceed.\"*\n\nVerified in the tree, not inferred — the half that exists:\n\n- `ApertureBytes { to_destination, from_destination }`, `crates/substrate-wire/src/lib.rs:790-795`.\n- The forwarder holds both counters as `AtomicU64` and the parent reads them without synchronising,\n  `crates/substrate-host/src/egress.rs:621-622` and `:252-256`.\n- Every applied observation already carries them, `crates/substrate-host/src/egress.rs:288-297`.\n\nThe half that does not:\n\n- `EgressAperture { name, host, port, pinned }`, `crates/substrate-host/src/egress.rs:83-88` — four\n  fields, no ceiling.\n- `--egress-aperture name=host:port/tcp`, `crates/substrate-daemon/src/main.rs:108`, parsed at\n  `:34-56` — the grammar has nowhere to put one.\n- Nothing reads `counters` for a comparison; the only readers are `applied()` and the tests.\n\nThe refusal class already exists: `Exhausted`, `crates/substrate-wire/src/lib.rs:132`.\n\n## Acceptance\n\nAn aperture declared with a ceiling refuses the run by name when the ceiling is crossed, and the\nsame aperture declared without one behaves exactly as it does today.\n\nEvidence that satisfies it, in order:\n\n1. An ADR fixing the declaration grammar, which direction(s) the ceiling counts, whether it is\n   per-run or per-aperture-lifetime, and what the child observes at the moment of refusal\n   (invariant 8: design before code).\n2. A successor bundle carrying the ceiling on the aperture capability fact and the\n   `exec.aperture-byte-limit` refusal class; earlier bundle bytes unchanged (invariant 6).\n3. A delegated-lane vector: a child that reads more than the declared ceiling from the pinned\n   destination ends `exhausted` with code `exec.aperture-byte-limit`, and the applied observation\n   reports at least the ceiling in `bytes`.\n4. A delegated-lane vector proving the negative: an aperture declared with no ceiling passes the\n   same traffic to completion, so the existing `declared-aperture-is-reachable` case\n   (`docs/design/10-destination-bound-egress.md:161`) is unchanged.\n5. A vector proving the ceiling is a *deployment* declaration and not request data — a request that\n   carries a ceiling is refused, the same way a raw destination is\n   (`exec.aperture-destination-in-request`, `docs/design/10-destination-bound-egress.md:124`).\n\n## Out of Scope\n\nRate limits, time ceilings, and any ceiling on the `none` network mode — there is no forwarder there\nto count in.\n\n## Open\n\nWhether the ceiling is enforced in the forwarder (it holds the counters, and can stop reading) or by\nthe parent watching the atomics (it can end the operation with the right class but cannot stop the\nbytes already in flight). The ADR decides; both are consistent with the counters as they stand.\n\n## Design draft — 2026-08-30\n\n`docs/design/12-aperture-byte-ceiling.md`, **proposed**. No ADR number is claimed: `adr/` admits\n`accepted` and `superseded` only (`xtask/src/adrs.rs:12`), so the number is assigned at acceptance.\nDesign 10's deferred note now points at it instead of describing an open gap.\n\nDecisions it fixes, each with what the alternative would have cost:\n\n| Decision | Chosen | Alternative's cost |\n|---|---|---|\n| Grammar | optional `/max=<size>` term on `--egress-aperture name=host:port/tcp`; absent = today byte for byte | a separate flag lets a ceiling name an aperture that does not exist; comma is unavailable either way (`crates/substrate-daemon/src/main.rs:116`, `value_delimiter = ','`) |\n| Direction | one ceiling over `to_destination + from_destination` summed | a per-direction pair is two numbers to get wrong and a bound a child evades by picking a direction |\n| Scope | per run | per-lifetime needs durable cross-run accounting and makes a refusal unreproducible from its own request |\n| Enforcement | in the relay, which stops at the ceiling; overshoot bounded by `RELAY_BUFFER = 16_384` (`crates/substrate-host/src/egress.rs:68`) | parent-only stops no byte: overshoot is bounded by the destination's throughput, not by substrate |\n\n**Gap the draft found, verified not inferred.** At HEAD a mid-run bound has no code to report. Both\nexisting mid-run bounds fold into one state: `forced_cancellation = timed_out || cpu_exhausted ||\ncancellation_requested` (`crates/substrate-host/src/process.rs:1318-1319`) sets\n`ExecState::Cancelled` (`:1364`). The supervision loop that would notice a ceiling already exists\nand already polls at 1 ms (`:1392`, `tokio::time::interval(Duration::from_millis(1))`), so the\nmechanism is there and the vocabulary is not — `exhausted` / `exec.aperture-byte-limit`\n(`docs/design/10-destination-bound-egress.md:126`) has nowhere to live in today's observation. The\ndraft therefore also proposes an optional class/code/message field on the exec observation, which is\nwhat makes this a contract change rather than a host-local one.\n\n## Blocked on\n\nOperator acceptance of design 12. Invariant 8: an ADR before code. Nothing here is implementable\nuntil that decision is made.\n\n## Accepted — 2026-08-30\n\nThe operator accepted design 12 as\n[`adr/0014-apertures-carry-a-declared-byte-ceiling.md`](../../../adr/0014-apertures-carry-a-declared-byte-ceiling.md).\nEvidence item 1 is satisfied; invariant 8 no longer blocks implementation.\n\nOne number moved while the design waited: it was drafted against successor bundle `0.7.0`, and that\nnumber went to ADR 0011's grant attribution. The successor is **`0.8.0`**, predecessor `0.7.0`,\n`adds_routes: 0`, `preserves_routes: 26` (`contracts/substrate-wire/0.7.0/bundle.json:5-10`), with\n`cargo xtask check-bundle 0.8.0` added to `scripts/gate.sh`.\n\nRemaining evidence: items 2-5 — the successor bundle, the two delegated-lane vectors (the ceiling\nrefuses; an aperture without one is unchanged) and the request-side refusal\n`exec.aperture-ceiling-in-request`.\n\n## Scope\n\nDerived 2026-08-30 by `story-scoper`, verified against HEAD `34b219a`. Every line is **cited** or\n**inferred**.\n\n- **Primary surface:** `crates/substrate-host` — the aperture byte path — cited (ADR 0014\n  § *Decision*, \"Enforced in the relay, classified by the parent\").\n- **Files, cited and exact at HEAD:** `crates/substrate-host/src/egress.rs:83-88`\n  (`EgressAperture`, gains `Option<u64>`), `:68` (`RELAY_BUFFER`, the stated overshoot bound),\n  `:713-818` (`relay_body`/`forward_once`, where the comparison lands), `:252-256`\n  (`SharedCounters::read`), `:322` (`install`, per-run counter lifetime), `:92-93`\n  (`EgressAperture::fact`).\n- **Files, cited:** `crates/substrate-host/src/process.rs:1318-1319` (`forced_cancellation`),\n  `:1366` (`ExecState::Cancelled`), `:1392-1414` (1 ms supervision poll and `cgroup.kill_all`),\n  `:919` (driver-side aperture refusal mapping).\n- **Files, cited with positions corrected:** `crates/substrate-wire/src/lib.rs:783`\n  (`AppliedAperture`), `:813` (`ApertureBytes`), `:1874` (`EgressApertureFact`), `:983-990`\n  (`Exec`, gains the optional class/code/message field), `:1577` and `:1627`\n  (`WireValidationError`), `:1909` (`validate_aperture_request`), `:155` (`Exhausted`).\n- **Files, cited with positions corrected:** `crates/substrate-daemon/src/main.rs:34-60`\n  (`parse_egress_aperture`, gains `/max=<size>`), `:144-155` (the clap arg; `value_delimiter = ','`\n  at `:152` is why the separator cannot be a comma).\n- **Contract surface, cited:** new `contracts/substrate-wire/0.8.0/` (predecessor `0.7.0`,\n  `adds_routes: 0`, `preserves_routes: 26`, mirroring\n  `contracts/substrate-wire/0.7.0/bundle.json:5-10`), new `xtask/bundle-source/0.8.0/`, and a\n  `check-bundle 0.8.0` line after `scripts/gate.sh:29`.\n- **Also likely:** `xtask/src/bundle.rs:384` (per-version branch) and its census functions at\n  `:681-682`, `:704`, `:865` — inferred; no document names the file.\n- **Also likely:** `crates/substrate-daemon/tests/runtime_vectors.rs` and `tests/contract_vectors.rs`\n  for evidence items 3-5 — inferred by analogy with `vectors/http/aperture-destination-in-request-refused.json`,\n  the only aperture vector that exists.\n- **Confidence:** **high** — ADR 0014 and design 12 name the declaration, enforcement, contract and\n  bundle sites with `file:line`, and every host-side citation reproduces exactly at HEAD.\n- **Would collide with:** any unit touching `crates/substrate-wire/src/lib.rs` (this story edits it\n  in six places), the egress byte path, the supervision loop, the daemon CLI flag surface, or the\n  bundle gate. **Hard exclusion:** any other unit cutting a successor bundle — `0.8.0` is one\n  directory and one gate line, and two stories cannot both own it.\n\n### Gap found while scoping, not present in either document\n\n`crates/substrate-daemon/src/runtime.rs:335-339` declares a **second** `EgressAperture` — the\ndaemon's own three-field config vocabulary (`name`, `host`, `port`), deliberately not re-exported\nfrom `substrate_host` (invariant 4), converted at the composition root (`:424`, `:451`). ADR 0014 and\n`docs/design/12-aperture-byte-ceiling.md` mention `runtime.rs` **zero times**. The ceiling has to\nland there too, or the declared value cannot reach the host type the ADR names. Verified:\n`grep -c runtime.rs` over both documents returns `0`.\n"},"command":"update-entity","target":"01MEM0000000000000013"},"payload":{"actor":"human:timo","at":1788082748078,"causation":"cmd-protocol-artifact-body-story-aperture-byte-ceiling-1788082748078","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body","event_id":"story:aperture-byte-ceiling@5#0~8fa9a869078ea7c5","executor":null,"recorded_at":"2026-08-30T09:39:08Z"}}
{"entity":"story","version":1,"id":"pty-sessions","revision":4,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"# Story: PTY sessions are a distinct session kind with resize\n\n## Outcome\n\nA human can attach a terminal to a confined process, resize it, and get the same lease, single\nattachment, bounded-output and whole-tree cleanup guarantees the raw-pipe session gives.\n\n## Context\n\n`docs/design/05-streams-sessions-and-endpoints.md` § 2 fixes the `pty` kind and its frames\n(input, output, resize, signal, exit, protocol-error) and says a PTY is never substituted for\npipes. `README.md` § *Status* lists PTY as **absent**; `docs/plan/04-direct-byte-plane.md`\n§ *Later phase-4 slices* lists it as remaining. Invariant 8 puts the ADR before code; invariant 6\nputs the wire change in a successor bundle.\n\n## Acceptance\n\nThe delegated lane runs an interactive shell through a `pty` session, echoes bytes, applies a\nresize the child observes, and exits on hangup — while the portable lane proves the typed\n`unserved` and every `0.4.0` byte is unchanged.\n\nEvidence that satisfies it, in order:\n\n1. A design document in `docs/design/`, proposed, fixing kind, frame set, resize bounds, never a\n   substitute for pipes, and the named refusal when the host cannot allocate a pty. It claims no\n   ADR number: `adr/` admits `accepted` and `superseded` only (`xtask/src/adrs.rs:12`), so the\n   number is assigned at acceptance.\n2. The next free successor bundle under `contracts/substrate-wire/` adds the `pty` kind and\n   `resize` frame; compatibility block names its predecessor with exact\n   `adds_routes`/`preserves_routes`; authored source under `xtask/bundle-source/<version>/` and\n   `cargo xtask check-bundle <version>` in `scripts/gate.sh`.\n3. `crates/substrate-host`: pty opened with `nix::pty::openpty` inside the existing\n   bubblewrap/cgroup/cleared-environment path. Failing-first tests:\n   `pty_session_refused_without_confinement`, `pty_resize_is_applied_and_observed` (child reads\n   `TIOCGWINSZ`), `pty_session_kills_tree_on_attachment_loss`.\n4. Daemon: the single-attachment Unix-WebSocket route family serves `pty`; `resize` outside the\n   declared bounds is a protocol error.\n5. Capability fact `sessions.pty` appears only after startup verified pty allocation (invariant 4).\n6. `STATUS.md`, `README.md` § *Status* and plan 04 § *Later phase-4 slices* updated in the same\n   change.\n\n## Open Questions\n\nBundle number: `0.5.0` if this lands first in the epic; the ADR states the actual number.\n\n## Correction — 2026-08-30\n\nThree claims in the evidence list above had gone stale and are rewritten, not deleted, so the\nchange is visible:\n\n| Claimed | Actually |\n|---|---|\n| `adr/0011-pty-sessions-are-a-distinct-session-kind.md` | `0011` is `adr/0011-delegated-context-and-grant-attribution.md`; the number was taken while this story sat in draft |\n| `contracts/substrate-wire/0.5.0/` | `0.5.0` is sealed secret slots and `0.6.0` is destination-bound egress; both are frozen (invariant 6) |\n| `render-contract-bundle-0.5.0.py`, `check-contract-bundle-0.5.0.py` | the Python renderer/checker pairs stop at `0.4.0`; from `0.5.0` on the check is `cargo xtask check-bundle <version>` (`scripts/gate.sh:24-28`) |\n\nThe story pins no numbers now, so it cannot go stale the same way again.\n\n## Scope\n\nDerived 2026-08-30 by `story-scoper`. Every line is **cited** or **inferred**.\n\n- **Primary surface:** `crates/substrate-host` and the `crates/substrate-daemon` session surface —\n  cited, evidence items 3 and 4 assign work to each by name.\n- **Files, inferred:** `crates/substrate-host/src/process.rs:260` (`start_pipe`, the analogue a pty\n  allocation sits beside); `crates/substrate-wire/src/lib.rs:1196` (`SessionMode`);\n  `crates/substrate-daemon/src/app/sessions.rs` and `app/routes.rs:69-85`;\n  `crates/substrate-store/src/sessions.rs` (ADR 0008 durable identity).\n- **Files, cited:** `scripts/gate.sh:24-28`, `xtask/src/adrs.rs:12`.\n- **Files, inferred:** `Cargo.toml:37` — `nix` is a workspace dependency at features\n  `[\"fs\", \"user\"]`; `nix::pty::openpty` needs `term`, and `substrate-host/Cargo.toml` does not\n  depend on `nix` at all.\n- **Symbols, cited:** `SessionMode`, `PipeSessionStartInput`, `PipeClientFrame`, `PipeServerFrame`,\n  `PipeSessionCapabilities`, `PipeSessionLimits` — all declared `crates/substrate-wire/src/lib.rs:1181-1310`.\n- **Also likely:** `contracts/substrate-wire/0.8.0/` and `xtask/bundle-source/0.8.0/` — inferred;\n  `0.7.0` is the frontier, one past what this story's own correction records.\n- **Confidence:** **high for where the work lands** — every cited path resolved in the tree — but no\n  design document and no ADR exist, so invariant 8 (`AGENTS.md:62-64`) blocks the code half.\n- **Would collide with:** any unit touching the `substrate-wire` session types, the daemon\n  `pipe-sessions` route family, `substrate-host` process spawning, `substrate-store` session\n  persistence, or the next contract bundle number. Broad by construction, not by widening.\n\n### Two contradictions found while scoping\n\n**1. `kind` or `mode`?** This story says a `pty` **kind**; design 05 § 2 and `adr/0007:22` say\n**mode**. The code has both, on different axes: `SessionKind` is the *resource* axis and holds one\nvariant `Session` (`crates/substrate-wire/src/lib.rs:1189-1192`), while `SessionMode` holds one\nvariant `Pipes` (`:1194-1198`). Which enum grows is unresolved, and it decides the wire shape.\n\n**2. This story asks for more than invariant 8 requires, and its parent says so.** Evidence item 1\ndemands a new design document. `epic:byte-plane-completion:29` says these proceed \"under invariant 8\nwith an ADR each and **no new design document**\", because design 05 § 2 already fixes the frames.\n`AGENTS.md:62-64` reads \"a design document **or** an ADR **before code**\" — so the epic's reading is\nthe admissible one and this story is over-specifying its own gate. Resolve before dispatching work.\n"},"args":{"changes":{"body":"# Story: PTY sessions are a distinct session kind with resize\n\n## Outcome\n\nA human can attach a terminal to a confined process, resize it, and get the same lease, single\nattachment, bounded-output and whole-tree cleanup guarantees the raw-pipe session gives.\n\n## Context\n\n`docs/design/05-streams-sessions-and-endpoints.md` § 2 fixes the `pty` kind and its frames\n(input, output, resize, signal, exit, protocol-error) and says a PTY is never substituted for\npipes. `README.md` § *Status* lists PTY as **absent**; `docs/plan/04-direct-byte-plane.md`\n§ *Later phase-4 slices* lists it as remaining. Invariant 8 puts the ADR before code; invariant 6\nputs the wire change in a successor bundle.\n\n## Acceptance\n\nThe delegated lane runs an interactive shell through a `pty` session, echoes bytes, applies a\nresize the child observes, and exits on hangup — while the portable lane proves the typed\n`unserved` and every `0.4.0` byte is unchanged.\n\nEvidence that satisfies it, in order:\n\n1. A design document in `docs/design/`, proposed, fixing kind, frame set, resize bounds, never a\n   substitute for pipes, and the named refusal when the host cannot allocate a pty. It claims no\n   ADR number: `adr/` admits `accepted` and `superseded` only (`xtask/src/adrs.rs:12`), so the\n   number is assigned at acceptance.\n2. The next free successor bundle under `contracts/substrate-wire/` adds the `pty` kind and\n   `resize` frame; compatibility block names its predecessor with exact\n   `adds_routes`/`preserves_routes`; authored source under `xtask/bundle-source/<version>/` and\n   `cargo xtask check-bundle <version>` in `scripts/gate.sh`.\n3. `crates/substrate-host`: pty opened with `nix::pty::openpty` inside the existing\n   bubblewrap/cgroup/cleared-environment path. Failing-first tests:\n   `pty_session_refused_without_confinement`, `pty_resize_is_applied_and_observed` (child reads\n   `TIOCGWINSZ`), `pty_session_kills_tree_on_attachment_loss`.\n4. Daemon: the single-attachment Unix-WebSocket route family serves `pty`; `resize` outside the\n   declared bounds is a protocol error.\n5. Capability fact `sessions.pty` appears only after startup verified pty allocation (invariant 4).\n6. `STATUS.md`, `README.md` § *Status* and plan 04 § *Later phase-4 slices* updated in the same\n   change.\n\n## Open Questions\n\nBundle number: `0.5.0` if this lands first in the epic; the ADR states the actual number.\n\n## Correction — 2026-08-30\n\nThree claims in the evidence list above had gone stale and are rewritten, not deleted, so the\nchange is visible:\n\n| Claimed | Actually |\n|---|---|\n| `adr/0011-pty-sessions-are-a-distinct-session-kind.md` | `0011` is `adr/0011-delegated-context-and-grant-attribution.md`; the number was taken while this story sat in draft |\n| `contracts/substrate-wire/0.5.0/` | `0.5.0` is sealed secret slots and `0.6.0` is destination-bound egress; both are frozen (invariant 6) |\n| `render-contract-bundle-0.5.0.py`, `check-contract-bundle-0.5.0.py` | the Python renderer/checker pairs stop at `0.4.0`; from `0.5.0` on the check is `cargo xtask check-bundle <version>` (`scripts/gate.sh:24-28`) |\n\nThe story pins no numbers now, so it cannot go stale the same way again.\n\n## Scope\n\nDerived 2026-08-30 by `story-scoper`. Every line is **cited** or **inferred**.\n\n- **Primary surface:** `crates/substrate-host` and the `crates/substrate-daemon` session surface —\n  cited, evidence items 3 and 4 assign work to each by name.\n- **Files, inferred:** `crates/substrate-host/src/process.rs:260` (`start_pipe`, the analogue a pty\n  allocation sits beside); `crates/substrate-wire/src/lib.rs:1196` (`SessionMode`);\n  `crates/substrate-daemon/src/app/sessions.rs` and `app/routes.rs:69-85`;\n  `crates/substrate-store/src/sessions.rs` (ADR 0008 durable identity).\n- **Files, cited:** `scripts/gate.sh:24-28`, `xtask/src/adrs.rs:12`.\n- **Files, inferred:** `Cargo.toml:37` — `nix` is a workspace dependency at features\n  `[\"fs\", \"user\"]`; `nix::pty::openpty` needs `term`, and `substrate-host/Cargo.toml` does not\n  depend on `nix` at all.\n- **Symbols, cited:** `SessionMode`, `PipeSessionStartInput`, `PipeClientFrame`, `PipeServerFrame`,\n  `PipeSessionCapabilities`, `PipeSessionLimits` — all declared `crates/substrate-wire/src/lib.rs:1181-1310`.\n- **Also likely:** `contracts/substrate-wire/0.8.0/` and `xtask/bundle-source/0.8.0/` — inferred;\n  `0.7.0` is the frontier, one past what this story's own correction records.\n- **Confidence:** **high for where the work lands** — every cited path resolved in the tree — but no\n  design document and no ADR exist, so invariant 8 (`AGENTS.md:62-64`) blocks the code half.\n- **Would collide with:** any unit touching the `substrate-wire` session types, the daemon\n  `pipe-sessions` route family, `substrate-host` process spawning, `substrate-store` session\n  persistence, or the next contract bundle number. Broad by construction, not by widening.\n\n### Two contradictions found while scoping\n\n**1. `kind` or `mode`?** This story says a `pty` **kind**; design 05 § 2 and `adr/0007:22` say\n**mode**. The code has both, on different axes: `SessionKind` is the *resource* axis and holds one\nvariant `Session` (`crates/substrate-wire/src/lib.rs:1189-1192`), while `SessionMode` holds one\nvariant `Pipes` (`:1194-1198`). Which enum grows is unresolved, and it decides the wire shape.\n\n**2. This story asks for more than invariant 8 requires, and its parent says so.** Evidence item 1\ndemands a new design document. `epic:byte-plane-completion:29` says these proceed \"under invariant 8\nwith an ADR each and **no new design document**\", because design 05 § 2 already fixes the frames.\n`AGENTS.md:62-64` reads \"a design document **or** an ADR **before code**\" — so the epic's reading is\nthe admissible one and this story is over-specifying its own gate. Resolve before dispatching work.\n"},"command":"update-entity","target":"01MEM0000000000000040"},"payload":{"actor":"human:timo","at":1788082764247,"causation":"cmd-protocol-artifact-body-story-pty-sessions-1788082764247","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body","event_id":"story:pty-sessions@4#0~86a32f981327667c","executor":null,"recorded_at":"2026-08-30T09:39:24Z"}}
{"entity":"story","version":1,"id":"network-session-authority","revision":3,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"# Story: Network session transport over TLS with single-use proof-bound authority\n\n## Outcome\n\nA client on another machine attaches to a session over WebSocket/TLS with an authority that lives\nat most 60 s, redeems exactly once, and is bound to the redeeming channel; a reconnect always\nneeds a fresh one. This is the last phase-4 exit criterion.\n\n## Context\n\n`docs/design/05-streams-sessions-and-endpoints.md` § *V1 decisions* 3–4 fix the authority and the\ntransport; `architecture/deployment-postures.md` requires TLS/mTLS or a trusted tunnel for any\nnon-loopback control listener. Today only the owner-permissioned Unix socket serves sessions\n(plan 04 § *Slice B*). The static-bearer TCP listener is development-only\n(`crates/substrate-daemon/src/main.rs:63-64`, design 06 § 1) and must not gain this route.\n\n## Acceptance\n\nThe delegated lane drives a raw-pipe session end to end over TLS from a second network namespace,\nand the authority it used cannot be redeemed a second time, after 60 s, or over a different TLS\nsession.\n\nEvidence that satisfies it, in order:\n\n1. An ADR decides the proof binding (TLS exporter per RFC 5705, or a client key) and records that\n   reconnect is a new authority, never a resumed one.\n2. A successor bundle adds the network attachment route and the authority-redemption frame; earlier\n   bundle bytes unchanged.\n3. Failing-first tests: `network_session_listener_refuses_plaintext`,\n   `session_authority_redeems_exactly_once`, `session_authority_expires_after_60s` (tokio paused\n   time), `session_authority_bound_to_channel`, `second_concurrent_attachment_refused`.\n4. The authority value never appears in logs, events or error bodies — a test greps captured\n   diagnostics (design 05 § 3, \"non-loggable\").\n5. The development-only TCP listener does not serve the route — a test proves its absence there.\n\n## Out of Scope\n\nThe hosted trust-envelope verifier (design 06 § 1, atlas ADR 0015; phase 7): this story issues\nthe authority from the local bearer subject.\n\n## Scope\n\nDerived 2026-08-30 by `story-scoper`. Every line is **cited** or **inferred**.\n\n- **Primary surface:** `crates/substrate-daemon` — cited; both the listener and the attach route\n  live there.\n- **Files, cited:** `crates/substrate-daemon/src/runtime.rs:736` (`serve_tcp`), `:856`\n  (`require_tcp_bearer`), `src/app/routes.rs:33` (`router`), `src/app/sessions.rs:735`\n  (`pipe_session_attach`).\n- **Symbols, cited:** `serve_tcp`, `TcpAuthState`, `require_tcp_bearer`, `router`,\n  `pipe_session_attach`, `PipeAttachmentPermit`, `PipeAttachmentRefusal`.\n- **Also likely, inferred:** `crates/substrate-wire/src/lib.rs:1274`/`:1291` (`PipeClientFrame`,\n  `PipeServerFrame`), where an authority-redemption frame would be added;\n  `crates/substrate-store/src/schema.rs:154` and `src/sessions.rs:526`\n  (`claim_pipe_session_attachment`), where redeem-exactly-once becomes durable;\n  `crates/substrate-daemon/src/main.rs:192-222`, the clap flags beside the `--tcp-*` group.\n- **Dependency, cited:** the workspace has **no** TLS crate — `rustls`/`tokio-rustls` grep to\n  nothing — so the dependency and its configuration surface are unmade.\n- **Confidence:** **medium.** The crate and the two code sites are cited and certain, but there is\n  no accepted ADR (`adr/` holds 0001–0014) and `docs/design/05-streams-sessions-and-endpoints.md:65`\n  points at `architecture/adr/0016-operation-scoped-session-authority.md`, which does not exist in\n  this repository. The proof binding — TLS exporter or client key — is undecided, so every TLS-side\n  file above is a guess at an unmade decision.\n- **Would collide with:** any unit touching the listener surface (`runtime.rs` `serve`/`serve_tcp`)\n  or the shared `app/routes.rs` `router`; and any unit cutting a successor\n  `contracts/substrate-wire/` version — that bump is a single serialising surface across the epic.\n\n### Two corrections found while scoping\n\n**1. A cited line is stale.** This story cites `crates/substrate-daemon/src/main.rs:63-64` as the\ndevelopment-only TCP listener. Those two lines are `port,` and `})` — the tail of\n`parse_egress_aperture`. The listener is `crates/substrate-daemon/src/runtime.rs:736`.\n\n**2. Acceptance 5 is already false at HEAD.** This story says the development-only TCP transport\n\"must not gain this route\". It has it: `serve_tcp` builds its service with the same `router(app)`\nas the Unix path (`crates/substrate-daemon/src/runtime.rs:757` against `:508`), and that router\nregisters `/v1/pipe-sessions/{session_id}/attach` (`crates/substrate-daemon/src/app/routes.rs:76-77`).\n\nNot an open exposure: `serve_tcp` bails unless both `--tcp-development-only` and\n`--tcp-private-overlay` are set (`crates/substrate-daemon/src/runtime.rs:737-743`), so the route is\nreachable only in an explicitly acknowledged development profile on a private overlay. But the story\nasserts a property the code does not have, so either the acceptance must change or the router must\nsplit — and nothing in the tree specifies a split. Decide it in the ADR this story still owes.\n"},"args":{"changes":{"body":"# Story: Network session transport over TLS with single-use proof-bound authority\n\n## Outcome\n\nA client on another machine attaches to a session over WebSocket/TLS with an authority that lives\nat most 60 s, redeems exactly once, and is bound to the redeeming channel; a reconnect always\nneeds a fresh one. This is the last phase-4 exit criterion.\n\n## Context\n\n`docs/design/05-streams-sessions-and-endpoints.md` § *V1 decisions* 3–4 fix the authority and the\ntransport; `architecture/deployment-postures.md` requires TLS/mTLS or a trusted tunnel for any\nnon-loopback control listener. Today only the owner-permissioned Unix socket serves sessions\n(plan 04 § *Slice B*). The static-bearer TCP listener is development-only\n(`crates/substrate-daemon/src/main.rs:63-64`, design 06 § 1) and must not gain this route.\n\n## Acceptance\n\nThe delegated lane drives a raw-pipe session end to end over TLS from a second network namespace,\nand the authority it used cannot be redeemed a second time, after 60 s, or over a different TLS\nsession.\n\nEvidence that satisfies it, in order:\n\n1. An ADR decides the proof binding (TLS exporter per RFC 5705, or a client key) and records that\n   reconnect is a new authority, never a resumed one.\n2. A successor bundle adds the network attachment route and the authority-redemption frame; earlier\n   bundle bytes unchanged.\n3. Failing-first tests: `network_session_listener_refuses_plaintext`,\n   `session_authority_redeems_exactly_once`, `session_authority_expires_after_60s` (tokio paused\n   time), `session_authority_bound_to_channel`, `second_concurrent_attachment_refused`.\n4. The authority value never appears in logs, events or error bodies — a test greps captured\n   diagnostics (design 05 § 3, \"non-loggable\").\n5. The development-only TCP listener does not serve the route — a test proves its absence there.\n\n## Out of Scope\n\nThe hosted trust-envelope verifier (design 06 § 1, atlas ADR 0015; phase 7): this story issues\nthe authority from the local bearer subject.\n\n## Scope\n\nDerived 2026-08-30 by `story-scoper`. Every line is **cited** or **inferred**.\n\n- **Primary surface:** `crates/substrate-daemon` — cited; both the listener and the attach route\n  live there.\n- **Files, cited:** `crates/substrate-daemon/src/runtime.rs:736` (`serve_tcp`), `:856`\n  (`require_tcp_bearer`), `src/app/routes.rs:33` (`router`), `src/app/sessions.rs:735`\n  (`pipe_session_attach`).\n- **Symbols, cited:** `serve_tcp`, `TcpAuthState`, `require_tcp_bearer`, `router`,\n  `pipe_session_attach`, `PipeAttachmentPermit`, `PipeAttachmentRefusal`.\n- **Also likely, inferred:** `crates/substrate-wire/src/lib.rs:1274`/`:1291` (`PipeClientFrame`,\n  `PipeServerFrame`), where an authority-redemption frame would be added;\n  `crates/substrate-store/src/schema.rs:154` and `src/sessions.rs:526`\n  (`claim_pipe_session_attachment`), where redeem-exactly-once becomes durable;\n  `crates/substrate-daemon/src/main.rs:192-222`, the clap flags beside the `--tcp-*` group.\n- **Dependency, cited:** the workspace has **no** TLS crate — `rustls`/`tokio-rustls` grep to\n  nothing — so the dependency and its configuration surface are unmade.\n- **Confidence:** **medium.** The crate and the two code sites are cited and certain, but there is\n  no accepted ADR (`adr/` holds 0001–0014) and `docs/design/05-streams-sessions-and-endpoints.md:65`\n  points at `architecture/adr/0016-operation-scoped-session-authority.md`, which does not exist in\n  this repository. The proof binding — TLS exporter or client key — is undecided, so every TLS-side\n  file above is a guess at an unmade decision.\n- **Would collide with:** any unit touching the listener surface (`runtime.rs` `serve`/`serve_tcp`)\n  or the shared `app/routes.rs` `router`; and any unit cutting a successor\n  `contracts/substrate-wire/` version — that bump is a single serialising surface across the epic.\n\n### Two corrections found while scoping\n\n**1. A cited line is stale.** This story cites `crates/substrate-daemon/src/main.rs:63-64` as the\ndevelopment-only TCP listener. Those two lines are `port,` and `})` — the tail of\n`parse_egress_aperture`. The listener is `crates/substrate-daemon/src/runtime.rs:736`.\n\n**2. Acceptance 5 is already false at HEAD.** This story says the development-only TCP transport\n\"must not gain this route\". It has it: `serve_tcp` builds its service with the same `router(app)`\nas the Unix path (`crates/substrate-daemon/src/runtime.rs:757` against `:508`), and that router\nregisters `/v1/pipe-sessions/{session_id}/attach` (`crates/substrate-daemon/src/app/routes.rs:76-77`).\n\nNot an open exposure: `serve_tcp` bails unless both `--tcp-development-only` and\n`--tcp-private-overlay` are set (`crates/substrate-daemon/src/runtime.rs:737-743`), so the route is\nreachable only in an explicitly acknowledged development profile on a private overlay. But the story\nasserts a property the code does not have, so either the acceptance must change or the router must\nsplit — and nothing in the tree specifies a split. Decide it in the ADR this story still owes.\n"},"command":"update-entity","target":"01MEM0000000000000034"},"payload":{"actor":"human:timo","at":1788082781136,"causation":"cmd-protocol-artifact-body-story-network-session-authority-1788082781136","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body","event_id":"story:network-session-authority@3#0~16af3c1c42c7ae2a","executor":null,"recorded_at":"2026-08-30T09:39:41Z"}}
{"entity":"story","version":1,"id":"aperture-byte-ceiling","revision":6,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"approval\":1,\"specification\":1}}","target":"01MEM0000000000000013","to":"proposed"},"payload":{"actor":"human:timo","at":1788084819417,"causation":"cmd-move-story-aperture-byte-ceiling-1788084819417","change":{"change":"moved","decided_on":{"recorded":{"approval":1,"specification":1}},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:aperture-byte-ceiling@6#0~32199f6e70a43c50","executor":null,"recorded_at":"2026-08-30T10:13:39Z"}}
{"entity":"story","version":1,"id":"aperture-byte-ceiling","revision":7,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"approval\":1,\"specification\":1}}","target":"01MEM0000000000000013","to":"active"},"payload":{"actor":"human:timo","at":1788084819447,"causation":"cmd-move-story-aperture-byte-ceiling-1788084819447","change":{"change":"moved","decided_on":{"recorded":{"approval":1,"specification":1}},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:aperture-byte-ceiling@7#0~c87931d2b1ad4b3c","executor":null,"recorded_at":"2026-08-30T10:13:39Z"}}
{"entity":"story","version":1,"id":"aperture-byte-ceiling","revision":8,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"body":"# Story: A declared aperture carries a byte ceiling that refuses mid-run\n\n## Outcome\n\nAn operator who declares an aperture can bound what crosses it, and a run that exceeds the bound is\nstopped with a named refusal rather than left to drain the link. Today the bytes are counted and\nreported but nothing reads them, so a confined run's egress volume is observable after the fact and\nunbounded during it.\n\n## Context\n\n`docs/design/10-destination-bound-egress.md:126` names the condition and its answer:\n*Declared byte ceiling exceeded mid-run* → class `exhausted`, code `exec.aperture-byte-limit`, no\naddress. `:284-287` records why it did not ship with ADR 0013: *\"There is no declared byte ceiling\nin the configuration surface, so there is nothing to exceed.\"*\n\nVerified in the tree, not inferred — the half that exists:\n\n- `ApertureBytes { to_destination, from_destination }`, `crates/substrate-wire/src/lib.rs:790-795`.\n- The forwarder holds both counters as `AtomicU64` and the parent reads them without synchronising,\n  `crates/substrate-host/src/egress.rs:621-622` and `:252-256`.\n- Every applied observation already carries them, `crates/substrate-host/src/egress.rs:288-297`.\n\nThe half that does not:\n\n- `EgressAperture { name, host, port, pinned }`, `crates/substrate-host/src/egress.rs:83-88` — four\n  fields, no ceiling.\n- `--egress-aperture name=host:port/tcp`, `crates/substrate-daemon/src/main.rs:108`, parsed at\n  `:34-56` — the grammar has nowhere to put one.\n- Nothing reads `counters` for a comparison; the only readers are `applied()` and the tests.\n\nThe refusal class already exists: `Exhausted`, `crates/substrate-wire/src/lib.rs:132`.\n\n## Acceptance\n\nAn aperture declared with a ceiling refuses the run by name when the ceiling is crossed, and the\nsame aperture declared without one behaves exactly as it does today.\n\nEvidence that satisfies it, in order:\n\n1. An ADR fixing the declaration grammar, which direction(s) the ceiling counts, whether it is\n   per-run or per-aperture-lifetime, and what the child observes at the moment of refusal\n   (invariant 8: design before code).\n2. A successor bundle carrying the ceiling on the aperture capability fact and the\n   `exec.aperture-byte-limit` refusal class; earlier bundle bytes unchanged (invariant 6).\n3. A delegated-lane vector: a child that reads more than the declared ceiling from the pinned\n   destination ends `exhausted` with code `exec.aperture-byte-limit`, and the applied observation\n   reports at least the ceiling in `bytes`.\n4. A delegated-lane vector proving the negative: an aperture declared with no ceiling passes the\n   same traffic to completion, so the existing `declared-aperture-is-reachable` case\n   (`docs/design/10-destination-bound-egress.md:161`) is unchanged.\n5. A vector proving the ceiling is a *deployment* declaration and not request data — a request that\n   carries a ceiling is refused, the same way a raw destination is\n   (`exec.aperture-destination-in-request`, `docs/design/10-destination-bound-egress.md:124`).\n\n## Out of Scope\n\nRate limits, time ceilings, and any ceiling on the `none` network mode — there is no forwarder there\nto count in.\n\n## Open\n\nWhether the ceiling is enforced in the forwarder (it holds the counters, and can stop reading) or by\nthe parent watching the atomics (it can end the operation with the right class but cannot stop the\nbytes already in flight). The ADR decides; both are consistent with the counters as they stand.\n\n## Design draft — 2026-08-30\n\n`docs/design/12-aperture-byte-ceiling.md`, **proposed**. No ADR number is claimed: `adr/` admits\n`accepted` and `superseded` only (`xtask/src/adrs.rs:12`), so the number is assigned at acceptance.\nDesign 10's deferred note now points at it instead of describing an open gap.\n\nDecisions it fixes, each with what the alternative would have cost:\n\n| Decision | Chosen | Alternative's cost |\n|---|---|---|\n| Grammar | optional `/max=<size>` term on `--egress-aperture name=host:port/tcp`; absent = today byte for byte | a separate flag lets a ceiling name an aperture that does not exist; comma is unavailable either way (`crates/substrate-daemon/src/main.rs:116`, `value_delimiter = ','`) |\n| Direction | one ceiling over `to_destination + from_destination` summed | a per-direction pair is two numbers to get wrong and a bound a child evades by picking a direction |\n| Scope | per run | per-lifetime needs durable cross-run accounting and makes a refusal unreproducible from its own request |\n| Enforcement | in the relay, which stops at the ceiling; overshoot bounded by `RELAY_BUFFER = 16_384` (`crates/substrate-host/src/egress.rs:68`) | parent-only stops no byte: overshoot is bounded by the destination's throughput, not by substrate |\n\n**Gap the draft found, verified not inferred.** At HEAD a mid-run bound has no code to report. Both\nexisting mid-run bounds fold into one state: `forced_cancellation = timed_out || cpu_exhausted ||\ncancellation_requested` (`crates/substrate-host/src/process.rs:1318-1319`) sets\n`ExecState::Cancelled` (`:1364`). The supervision loop that would notice a ceiling already exists\nand already polls at 1 ms (`:1392`, `tokio::time::interval(Duration::from_millis(1))`), so the\nmechanism is there and the vocabulary is not — `exhausted` / `exec.aperture-byte-limit`\n(`docs/design/10-destination-bound-egress.md:126`) has nowhere to live in today's observation. The\ndraft therefore also proposes an optional class/code/message field on the exec observation, which is\nwhat makes this a contract change rather than a host-local one.\n\n## Blocked on\n\nOperator acceptance of design 12. Invariant 8: an ADR before code. Nothing here is implementable\nuntil that decision is made.\n\n## Accepted — 2026-08-30\n\nThe operator accepted design 12 as\n[`adr/0014-apertures-carry-a-declared-byte-ceiling.md`](../../../adr/0014-apertures-carry-a-declared-byte-ceiling.md).\nEvidence item 1 is satisfied; invariant 8 no longer blocks implementation.\n\nOne number moved while the design waited: it was drafted against successor bundle `0.7.0`, and that\nnumber went to ADR 0011's grant attribution. The successor is **`0.8.0`**, predecessor `0.7.0`,\n`adds_routes: 0`, `preserves_routes: 26` (`contracts/substrate-wire/0.7.0/bundle.json:5-10`), with\n`cargo xtask check-bundle 0.8.0` added to `scripts/gate.sh`.\n\nRemaining evidence: items 2-5 — the successor bundle, the two delegated-lane vectors (the ceiling\nrefuses; an aperture without one is unchanged) and the request-side refusal\n`exec.aperture-ceiling-in-request`.\n\n## Scope\n\nDerived 2026-08-30 by `story-scoper`, verified against HEAD `34b219a`. Every line is **cited** or\n**inferred**.\n\n- **Primary surface:** `crates/substrate-host` — the aperture byte path — cited (ADR 0014\n  § *Decision*, \"Enforced in the relay, classified by the parent\").\n- **Files, cited and exact at HEAD:** `crates/substrate-host/src/egress.rs:83-88`\n  (`EgressAperture`, gains `Option<u64>`), `:68` (`RELAY_BUFFER`, the stated overshoot bound),\n  `:713-818` (`relay_body`/`forward_once`, where the comparison lands), `:252-256`\n  (`SharedCounters::read`), `:322` (`install`, per-run counter lifetime), `:92-93`\n  (`EgressAperture::fact`).\n- **Files, cited:** `crates/substrate-host/src/process.rs:1318-1319` (`forced_cancellation`),\n  `:1366` (`ExecState::Cancelled`), `:1392-1414` (1 ms supervision poll and `cgroup.kill_all`),\n  `:919` (driver-side aperture refusal mapping).\n- **Files, cited with positions corrected:** `crates/substrate-wire/src/lib.rs:783`\n  (`AppliedAperture`), `:813` (`ApertureBytes`), `:1874` (`EgressApertureFact`), `:983-990`\n  (`Exec`, gains the optional class/code/message field), `:1577` and `:1627`\n  (`WireValidationError`), `:1909` (`validate_aperture_request`), `:155` (`Exhausted`).\n- **Files, cited with positions corrected:** `crates/substrate-daemon/src/main.rs:34-60`\n  (`parse_egress_aperture`, gains `/max=<size>`), `:144-155` (the clap arg; `value_delimiter = ','`\n  at `:152` is why the separator cannot be a comma).\n- **Contract surface, cited:** new `contracts/substrate-wire/0.8.0/` (predecessor `0.7.0`,\n  `adds_routes: 0`, `preserves_routes: 26`, mirroring\n  `contracts/substrate-wire/0.7.0/bundle.json:5-10`), new `xtask/bundle-source/0.8.0/`, and a\n  `check-bundle 0.8.0` line after `scripts/gate.sh:29`.\n- **Also likely:** `xtask/src/bundle.rs:384` (per-version branch) and its census functions at\n  `:681-682`, `:704`, `:865` — inferred; no document names the file.\n- **Also likely:** `crates/substrate-daemon/tests/runtime_vectors.rs` and `tests/contract_vectors.rs`\n  for evidence items 3-5 — inferred by analogy with `vectors/http/aperture-destination-in-request-refused.json`,\n  the only aperture vector that exists.\n- **Confidence:** **high** — ADR 0014 and design 12 name the declaration, enforcement, contract and\n  bundle sites with `file:line`, and every host-side citation reproduces exactly at HEAD.\n- **Would collide with:** any unit touching `crates/substrate-wire/src/lib.rs` (this story edits it\n  in six places), the egress byte path, the supervision loop, the daemon CLI flag surface, or the\n  bundle gate. **Hard exclusion:** any other unit cutting a successor bundle — `0.8.0` is one\n  directory and one gate line, and two stories cannot both own it.\n\n### Gap found while scoping, not present in either document\n\n`crates/substrate-daemon/src/runtime.rs:335-339` declares a **second** `EgressAperture` — the\ndaemon's own three-field config vocabulary (`name`, `host`, `port`), deliberately not re-exported\nfrom `substrate_host` (invariant 4), converted at the composition root (`:424`, `:451`). ADR 0014 and\n`docs/design/12-aperture-byte-ceiling.md` mention `runtime.rs` **zero times**. The ceiling has to\nland there too, or the declared value cannot reach the host type the ADR names. Verified:\n`grep -c runtime.rs` over both documents returns `0`.\n\n## Implemented — 2026-08-30, one-unit wave\n\nMerged on `wave/aperture-byte-ceiling` at `433ec68`, unit commit `5802e8d`.\n\nEvidence items 2-5 are met. `contracts/substrate-wire/0.8.0/`, 228 files, predecessor `0.7.0`,\n`adds_routes: 0`, `preserves_routes: 26`; every `0.7.0` vector, fixture and schema byte-identical in\n`0.8.0` except `$id` and one `contract` string. `PORTABLE_CASES` 33 → 34, `DELEGATED_CASES` 50 → 54.\n\nVerified by the orchestrator rather than taken on report: `bash scripts/gate.sh` → `gate: passed`,\nexit 0, run on the integration branch after the merge. `bash scripts/delegated-lane.sh` → exit 0,\n`54 HTTP cases … (delegated lane)`. `cargo xtask check-json` → `1544 documents in 8 bundles`.\n`git status --short contracts/` showed only the new `0.8.0`; `xtask/src/render.rs` untouched.\n\n**The surface ADR 0014 never named.** The ADR and design 12 mention `runtime.rs` zero times, but\n`crates/substrate-daemon/src/runtime.rs` declares the daemon's own three-field `EgressAperture`,\ndeliberately not re-exported from `substrate_host` (invariant 4). The ceiling landed in both types\nand the conversion, and `configuration_generation` now formats `name=host:port/tcp[/max=N]` so a\nchanged ceiling moves the snapshot digest — a published fact that does not move the snapshot is a\nstale fact. Found by `story-scoper` before dispatch, not at merge time.\n\n### Two defects the adversary found, both verified by the orchestrator before being routed back\n\n**A client-supplied aperture name could panic the handler.** `reads_as_ceiling` sliced `term[..4]`\non the raw request string and ran before `valid_aperture_name`, so any name whose multi-byte\ncharacter straddled byte index 4 panicked — `ab€cd`, `mo€del`, `ma€x`. Reproduced independently in\nisolation. At `0.7.0` that request was `422 exec.aperture-name-invalid`; with the slice it became a\ndropped connection carrying zero bytes, no status, no error class. Invariant 3 inverted: a refusal\nturned into a crash. The comparison is now on bytes via `get(..4)`, total for every input.\n\n**A run stopped at the ceiling could report no refusal at all.** `aperture_exhausted` was set only\ninside the supervision tick, which shares a `select!` with `child.wait()`. The relay closes the\nsocket, the child exits cleanly on that EOF, `child.wait()` wins, and the flag was never read again\n— the run recorded `state: exited, exit 0, refusal: null`, byte-identical to a run that finished on\nits own. Measured at 4-6 of 20. The ceiling is now asked once more after the wait, while the\nforwarder is still alive. The implementor reports 200 runs across 10 executions with `refusal: null`\nzero times and the race window still hit 74 of 200, every one now `cancelled` with\n`exec.aperture-byte-limit`; the orchestrator re-ran the case 15 consecutive times, 0 failures.\n\nA third disagreement was fixed with them: the relay and the parent disagreed at\n`max_bytes: Some(0)` — unreachable through the daemon, reachable through the public\n`substrate_host::EgressAperture`.\n\n### Recorded, deliberately not fixed\n\nThe same `select!` race can drop `cpu_exhausted`. It loses one state bit and has no refusal code\ntoday (ADR 0014 defers naming timeout and CPU exhaustion), where the ceiling lost the only field\ndistinguishing a bounded run from an unbounded one. Widening the change was refused rather than\nforgotten.\n\nThere is no bundle *vector* for the startup grammar refusal: the `vector.json` `startup` action\nshape has no configuration member to carry a malformed aperture declaration. The shipped binary is\nasserted instead, in `check_aperture_term_refusal`.\n"},"args":{"changes":{"body":"# Story: A declared aperture carries a byte ceiling that refuses mid-run\n\n## Outcome\n\nAn operator who declares an aperture can bound what crosses it, and a run that exceeds the bound is\nstopped with a named refusal rather than left to drain the link. Today the bytes are counted and\nreported but nothing reads them, so a confined run's egress volume is observable after the fact and\nunbounded during it.\n\n## Context\n\n`docs/design/10-destination-bound-egress.md:126` names the condition and its answer:\n*Declared byte ceiling exceeded mid-run* → class `exhausted`, code `exec.aperture-byte-limit`, no\naddress. `:284-287` records why it did not ship with ADR 0013: *\"There is no declared byte ceiling\nin the configuration surface, so there is nothing to exceed.\"*\n\nVerified in the tree, not inferred — the half that exists:\n\n- `ApertureBytes { to_destination, from_destination }`, `crates/substrate-wire/src/lib.rs:790-795`.\n- The forwarder holds both counters as `AtomicU64` and the parent reads them without synchronising,\n  `crates/substrate-host/src/egress.rs:621-622` and `:252-256`.\n- Every applied observation already carries them, `crates/substrate-host/src/egress.rs:288-297`.\n\nThe half that does not:\n\n- `EgressAperture { name, host, port, pinned }`, `crates/substrate-host/src/egress.rs:83-88` — four\n  fields, no ceiling.\n- `--egress-aperture name=host:port/tcp`, `crates/substrate-daemon/src/main.rs:108`, parsed at\n  `:34-56` — the grammar has nowhere to put one.\n- Nothing reads `counters` for a comparison; the only readers are `applied()` and the tests.\n\nThe refusal class already exists: `Exhausted`, `crates/substrate-wire/src/lib.rs:132`.\n\n## Acceptance\n\nAn aperture declared with a ceiling refuses the run by name when the ceiling is crossed, and the\nsame aperture declared without one behaves exactly as it does today.\n\nEvidence that satisfies it, in order:\n\n1. An ADR fixing the declaration grammar, which direction(s) the ceiling counts, whether it is\n   per-run or per-aperture-lifetime, and what the child observes at the moment of refusal\n   (invariant 8: design before code).\n2. A successor bundle carrying the ceiling on the aperture capability fact and the\n   `exec.aperture-byte-limit` refusal class; earlier bundle bytes unchanged (invariant 6).\n3. A delegated-lane vector: a child that reads more than the declared ceiling from the pinned\n   destination ends `exhausted` with code `exec.aperture-byte-limit`, and the applied observation\n   reports at least the ceiling in `bytes`.\n4. A delegated-lane vector proving the negative: an aperture declared with no ceiling passes the\n   same traffic to completion, so the existing `declared-aperture-is-reachable` case\n   (`docs/design/10-destination-bound-egress.md:161`) is unchanged.\n5. A vector proving the ceiling is a *deployment* declaration and not request data — a request that\n   carries a ceiling is refused, the same way a raw destination is\n   (`exec.aperture-destination-in-request`, `docs/design/10-destination-bound-egress.md:124`).\n\n## Out of Scope\n\nRate limits, time ceilings, and any ceiling on the `none` network mode — there is no forwarder there\nto count in.\n\n## Open\n\nWhether the ceiling is enforced in the forwarder (it holds the counters, and can stop reading) or by\nthe parent watching the atomics (it can end the operation with the right class but cannot stop the\nbytes already in flight). The ADR decides; both are consistent with the counters as they stand.\n\n## Design draft — 2026-08-30\n\n`docs/design/12-aperture-byte-ceiling.md`, **proposed**. No ADR number is claimed: `adr/` admits\n`accepted` and `superseded` only (`xtask/src/adrs.rs:12`), so the number is assigned at acceptance.\nDesign 10's deferred note now points at it instead of describing an open gap.\n\nDecisions it fixes, each with what the alternative would have cost:\n\n| Decision | Chosen | Alternative's cost |\n|---|---|---|\n| Grammar | optional `/max=<size>` term on `--egress-aperture name=host:port/tcp`; absent = today byte for byte | a separate flag lets a ceiling name an aperture that does not exist; comma is unavailable either way (`crates/substrate-daemon/src/main.rs:116`, `value_delimiter = ','`) |\n| Direction | one ceiling over `to_destination + from_destination` summed | a per-direction pair is two numbers to get wrong and a bound a child evades by picking a direction |\n| Scope | per run | per-lifetime needs durable cross-run accounting and makes a refusal unreproducible from its own request |\n| Enforcement | in the relay, which stops at the ceiling; overshoot bounded by `RELAY_BUFFER = 16_384` (`crates/substrate-host/src/egress.rs:68`) | parent-only stops no byte: overshoot is bounded by the destination's throughput, not by substrate |\n\n**Gap the draft found, verified not inferred.** At HEAD a mid-run bound has no code to report. Both\nexisting mid-run bounds fold into one state: `forced_cancellation = timed_out || cpu_exhausted ||\ncancellation_requested` (`crates/substrate-host/src/process.rs:1318-1319`) sets\n`ExecState::Cancelled` (`:1364`). The supervision loop that would notice a ceiling already exists\nand already polls at 1 ms (`:1392`, `tokio::time::interval(Duration::from_millis(1))`), so the\nmechanism is there and the vocabulary is not — `exhausted` / `exec.aperture-byte-limit`\n(`docs/design/10-destination-bound-egress.md:126`) has nowhere to live in today's observation. The\ndraft therefore also proposes an optional class/code/message field on the exec observation, which is\nwhat makes this a contract change rather than a host-local one.\n\n## Blocked on\n\nOperator acceptance of design 12. Invariant 8: an ADR before code. Nothing here is implementable\nuntil that decision is made.\n\n## Accepted — 2026-08-30\n\nThe operator accepted design 12 as\n[`adr/0014-apertures-carry-a-declared-byte-ceiling.md`](../../../adr/0014-apertures-carry-a-declared-byte-ceiling.md).\nEvidence item 1 is satisfied; invariant 8 no longer blocks implementation.\n\nOne number moved while the design waited: it was drafted against successor bundle `0.7.0`, and that\nnumber went to ADR 0011's grant attribution. The successor is **`0.8.0`**, predecessor `0.7.0`,\n`adds_routes: 0`, `preserves_routes: 26` (`contracts/substrate-wire/0.7.0/bundle.json:5-10`), with\n`cargo xtask check-bundle 0.8.0` added to `scripts/gate.sh`.\n\nRemaining evidence: items 2-5 — the successor bundle, the two delegated-lane vectors (the ceiling\nrefuses; an aperture without one is unchanged) and the request-side refusal\n`exec.aperture-ceiling-in-request`.\n\n## Scope\n\nDerived 2026-08-30 by `story-scoper`, verified against HEAD `34b219a`. Every line is **cited** or\n**inferred**.\n\n- **Primary surface:** `crates/substrate-host` — the aperture byte path — cited (ADR 0014\n  § *Decision*, \"Enforced in the relay, classified by the parent\").\n- **Files, cited and exact at HEAD:** `crates/substrate-host/src/egress.rs:83-88`\n  (`EgressAperture`, gains `Option<u64>`), `:68` (`RELAY_BUFFER`, the stated overshoot bound),\n  `:713-818` (`relay_body`/`forward_once`, where the comparison lands), `:252-256`\n  (`SharedCounters::read`), `:322` (`install`, per-run counter lifetime), `:92-93`\n  (`EgressAperture::fact`).\n- **Files, cited:** `crates/substrate-host/src/process.rs:1318-1319` (`forced_cancellation`),\n  `:1366` (`ExecState::Cancelled`), `:1392-1414` (1 ms supervision poll and `cgroup.kill_all`),\n  `:919` (driver-side aperture refusal mapping).\n- **Files, cited with positions corrected:** `crates/substrate-wire/src/lib.rs:783`\n  (`AppliedAperture`), `:813` (`ApertureBytes`), `:1874` (`EgressApertureFact`), `:983-990`\n  (`Exec`, gains the optional class/code/message field), `:1577` and `:1627`\n  (`WireValidationError`), `:1909` (`validate_aperture_request`), `:155` (`Exhausted`).\n- **Files, cited with positions corrected:** `crates/substrate-daemon/src/main.rs:34-60`\n  (`parse_egress_aperture`, gains `/max=<size>`), `:144-155` (the clap arg; `value_delimiter = ','`\n  at `:152` is why the separator cannot be a comma).\n- **Contract surface, cited:** new `contracts/substrate-wire/0.8.0/` (predecessor `0.7.0`,\n  `adds_routes: 0`, `preserves_routes: 26`, mirroring\n  `contracts/substrate-wire/0.7.0/bundle.json:5-10`), new `xtask/bundle-source/0.8.0/`, and a\n  `check-bundle 0.8.0` line after `scripts/gate.sh:29`.\n- **Also likely:** `xtask/src/bundle.rs:384` (per-version branch) and its census functions at\n  `:681-682`, `:704`, `:865` — inferred; no document names the file.\n- **Also likely:** `crates/substrate-daemon/tests/runtime_vectors.rs` and `tests/contract_vectors.rs`\n  for evidence items 3-5 — inferred by analogy with `vectors/http/aperture-destination-in-request-refused.json`,\n  the only aperture vector that exists.\n- **Confidence:** **high** — ADR 0014 and design 12 name the declaration, enforcement, contract and\n  bundle sites with `file:line`, and every host-side citation reproduces exactly at HEAD.\n- **Would collide with:** any unit touching `crates/substrate-wire/src/lib.rs` (this story edits it\n  in six places), the egress byte path, the supervision loop, the daemon CLI flag surface, or the\n  bundle gate. **Hard exclusion:** any other unit cutting a successor bundle — `0.8.0` is one\n  directory and one gate line, and two stories cannot both own it.\n\n### Gap found while scoping, not present in either document\n\n`crates/substrate-daemon/src/runtime.rs:335-339` declares a **second** `EgressAperture` — the\ndaemon's own three-field config vocabulary (`name`, `host`, `port`), deliberately not re-exported\nfrom `substrate_host` (invariant 4), converted at the composition root (`:424`, `:451`). ADR 0014 and\n`docs/design/12-aperture-byte-ceiling.md` mention `runtime.rs` **zero times**. The ceiling has to\nland there too, or the declared value cannot reach the host type the ADR names. Verified:\n`grep -c runtime.rs` over both documents returns `0`.\n\n## Implemented — 2026-08-30, one-unit wave\n\nMerged on `wave/aperture-byte-ceiling` at `433ec68`, unit commit `5802e8d`.\n\nEvidence items 2-5 are met. `contracts/substrate-wire/0.8.0/`, 228 files, predecessor `0.7.0`,\n`adds_routes: 0`, `preserves_routes: 26`; every `0.7.0` vector, fixture and schema byte-identical in\n`0.8.0` except `$id` and one `contract` string. `PORTABLE_CASES` 33 → 34, `DELEGATED_CASES` 50 → 54.\n\nVerified by the orchestrator rather than taken on report: `bash scripts/gate.sh` → `gate: passed`,\nexit 0, run on the integration branch after the merge. `bash scripts/delegated-lane.sh` → exit 0,\n`54 HTTP cases … (delegated lane)`. `cargo xtask check-json` → `1544 documents in 8 bundles`.\n`git status --short contracts/` showed only the new `0.8.0`; `xtask/src/render.rs` untouched.\n\n**The surface ADR 0014 never named.** The ADR and design 12 mention `runtime.rs` zero times, but\n`crates/substrate-daemon/src/runtime.rs` declares the daemon's own three-field `EgressAperture`,\ndeliberately not re-exported from `substrate_host` (invariant 4). The ceiling landed in both types\nand the conversion, and `configuration_generation` now formats `name=host:port/tcp[/max=N]` so a\nchanged ceiling moves the snapshot digest — a published fact that does not move the snapshot is a\nstale fact. Found by `story-scoper` before dispatch, not at merge time.\n\n### Two defects the adversary found, both verified by the orchestrator before being routed back\n\n**A client-supplied aperture name could panic the handler.** `reads_as_ceiling` sliced `term[..4]`\non the raw request string and ran before `valid_aperture_name`, so any name whose multi-byte\ncharacter straddled byte index 4 panicked — `ab€cd`, `mo€del`, `ma€x`. Reproduced independently in\nisolation. At `0.7.0` that request was `422 exec.aperture-name-invalid`; with the slice it became a\ndropped connection carrying zero bytes, no status, no error class. Invariant 3 inverted: a refusal\nturned into a crash. The comparison is now on bytes via `get(..4)`, total for every input.\n\n**A run stopped at the ceiling could report no refusal at all.** `aperture_exhausted` was set only\ninside the supervision tick, which shares a `select!` with `child.wait()`. The relay closes the\nsocket, the child exits cleanly on that EOF, `child.wait()` wins, and the flag was never read again\n— the run recorded `state: exited, exit 0, refusal: null`, byte-identical to a run that finished on\nits own. Measured at 4-6 of 20. The ceiling is now asked once more after the wait, while the\nforwarder is still alive. The implementor reports 200 runs across 10 executions with `refusal: null`\nzero times and the race window still hit 74 of 200, every one now `cancelled` with\n`exec.aperture-byte-limit`; the orchestrator re-ran the case 15 consecutive times, 0 failures.\n\nA third disagreement was fixed with them: the relay and the parent disagreed at\n`max_bytes: Some(0)` — unreachable through the daemon, reachable through the public\n`substrate_host::EgressAperture`.\n\n### Recorded, deliberately not fixed\n\nThe same `select!` race can drop `cpu_exhausted`. It loses one state bit and has no refusal code\ntoday (ADR 0014 defers naming timeout and CPU exhaustion), where the ceiling lost the only field\ndistinguishing a bounded run from an unbounded one. Widening the change was refused rather than\nforgotten.\n\nThere is no bundle *vector* for the startup grammar refusal: the `vector.json` `startup` action\nshape has no configuration member to carry a malformed aperture declaration. The shipped binary is\nasserted instead, in `check_aperture_term_refusal`.\n"},"command":"update-entity","target":"01MEM0000000000000013"},"payload":{"actor":"human:timo","at":1788088397605,"causation":"cmd-protocol-artifact-body-story-aperture-byte-ceiling-1788088397605","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body","event_id":"story:aperture-byte-ceiling@8#0~d7aa7d734f957a9b","executor":null,"recorded_at":"2026-08-30T11:13:17Z"}}
{"entity":"story","version":1,"id":"aperture-byte-ceiling","revision":8,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"gate: passed, exit 0; 54 HTTP cases (delegated lane); 1544 documents in 8 bundles","source":"bash scripts/gate.sh on wave/aperture-byte-ceiling, run by the orchestrator","target":"01MEM0000000000000013"},"payload":{"actor":"human:timo","at":1788088397000,"causation":"cmd-evidence-story-aperture-byte-ceiling-test_result-1788088397619","change":{"change":"evidence","kind":"test_result","reference":"gate: passed, exit 0; 54 HTTP cases (delegated lane); 1544 documents in 8 bundles","source":"bash scripts/gate.sh on wave/aperture-byte-ceiling, run by the orchestrator"},"correlation":"protocol-artifact-evidence","event_id":"story:aperture-byte-ceiling@8#0~43d7081a5f23271b","executor":null,"recorded_at":"2026-08-30T11:13:17Z"}}
{"entity":"story","version":1,"id":"aperture-byte-ceiling","revision":8,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"review","reference":"2 defects found, both verified and fixed: UTF-8 panic in reads_as_ceiling; ceiling refusal lost to a select! race","source":"engineering-protocols:adversary","target":"01MEM0000000000000013"},"payload":{"actor":"human:timo","at":1788088397000,"causation":"cmd-evidence-story-aperture-byte-ceiling-review-1788088397633","change":{"change":"evidence","kind":"review","reference":"2 defects found, both verified and fixed: UTF-8 panic in reads_as_ceiling; ceiling refusal lost to a select! race","source":"engineering-protocols:adversary"},"correlation":"protocol-artifact-evidence","event_id":"story:aperture-byte-ceiling@8#0~330c75e01fd3a38a","executor":null,"recorded_at":"2026-08-30T11:13:17Z"}}
{"entity":"story","version":1,"id":"aperture-byte-ceiling","revision":9,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1,\"approval\":1,\"review\":1,\"specification\":1}}","target":"01MEM0000000000000013","to":"implemented"},"payload":{"actor":"human:timo","at":1788088397656,"causation":"cmd-move-story-aperture-byte-ceiling-1788088397656","change":{"change":"moved","decided_on":{"recorded":{"approval":1,"review":1,"specification":1,"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:aperture-byte-ceiling@9#0~1fb58c45427523da","executor":null,"recorded_at":"2026-08-30T11:13:17Z"}}
{"entity":"story","version":1,"id":"signed-daemon-image","revision":6,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"body":"# Story: A tagged main publishes a signed, digest-pinned daemon image\n\n## Outcome\n\nAn operator can `docker pull ghcr.io/beyond10x/b10x-substrate-daemon@sha256:…` for a tag, verify\nits signature, and know it was built by the bot from that tagged commit.\n\n## Context\n\n`Dockerfile` builds a distroless `substrate-daemon` with an `org.opencontainers.image.revision`\nlabel; no workflow builds, pushes or signs it, and `README.md` § *Status* records \"stable\npublication: **not done**\". `README.md:31` fixes the package prefix `b10x-substrate-*`.\n`AGENTS.md` § *Bot identity* requires automated pushes to use the GitHub App, and § *Releases*\nfixes the tag form: the bare version, annotated, at a fully gated `main` commit.\n\n## Acceptance\n\nAn annotated bare-version tag on `main` produces a keyless-signed image at\n`ghcr.io/beyond10x/b10x-substrate-daemon:<version>` whose digest is written to the GitHub release\nand under the version heading in `CHANGELOG.md`, and a pre-release tag such as `0.2.2-rc.0`\nproduces nothing.\n\nEvidence that satisfies it:\n\n- `.github/workflows/release.yml` triggers only on the bare-version form and refuses to build\n  unless `gate.yml` is green for that commit;\n- `SOURCE_SHA` = the tag's commit; `cosign verify` with the workflow identity succeeds; a signing\n  failure fails the job before anything is announced;\n- release notes state that the wire contract bundles remain development bundles;\n- `permissions` are `contents: read` at workflow level and `packages: write`, `id-token: write` on\n  the release job only; every action SHA-pinned;\n- the pre-release-tag run URL recorded here showing no publish.\n\n## Out of Scope\n\nBit-for-bit reproducible binaries: the sha256 is recorded, reproduction is a later milestone. The\nimage's `VOLUME` and `EXPOSE 8080` describe the development-only TCP posture; release notes must\nnot describe the image as a hosted posture (design 06 § 1).\n\n## Progress — 2026-08-30, the workflow exists and has never run\n\nMerged as `fced5fb` (PR #22). `.github/workflows/release.yml` builds the `Dockerfile`, publishes\n`ghcr.io/beyond10x/b10x-substrate-daemon:<version>`, keyless-signs and digest-pins it.\n\n**No image is published.** The workflow existing and an image existing are different claims;\n`README.md`, `STATUS.md` and `AGENTS.md` § *Releases* each keep them apart.\n\nAcceptance, item by item:\n\n| evidence | state |\n|---|---|\n| triggers only on the bare-version form | **met** — coarse ref glob plus an anchored regex in preflight; `0.2.2-rc.0`, `v0.2.2`, `0.2` and `0.2.2.1` all refuse |\n| refuses to build unless `gate.yml` is green for that commit | **met** — preflight reads that workflow's own recorded conclusion for the tagged SHA rather than re-running a lookalike |\n| `SOURCE_SHA` = the tag's commit | **met** — `git rev-parse refs/tags/<tag>^{commit}`, cross-checked against `github.sha` |\n| a signing failure fails the job before anything is announced | **met** — build → push → sign → `cosign verify` → *then* token, notes, release, changelog |\n| notes state the bundles remain development bundles | **met** — § *What this release does not claim*, citing atlas ADR 0019 |\n| `permissions` scoped; every action SHA-pinned | **met** — `packages: write`/`id-token: write` on the release job alone; preflight holds `actions: read` because no smaller scope reads another workflow's conclusion for a SHA |\n| `cosign verify` with the workflow identity succeeds | **open** — needs a real tag push |\n| a pre-release-tag run URL showing no publish | **open** — proving the negative needs `0.2.2-rc.0` pushed |\n\nAll four action pins were resolved against their upstream repositories rather than taken on report:\n`actions/checkout@3d3c42e5` is `refs/tags/v7` and `v7.0.1`; `sigstore/cosign-installer@6f9f1778` is\n`refs/tags/v4.1.2`; `actions/create-github-app-token@bcd2ba49` is `refs/tags/v3` and `v3.2.0`.\n\n## Blocked on the operator\n\n1. Repository secrets `B10X_BOT_APP_ID` and `B10X_BOT_PRIVATE_KEY`, and a b10x-bot installation\n   with `contents: write`. Absent them the job fails **after** the image is pushed and signed and\n   **before** anything is announced — a signed image with no release.\n2. A real tag push, which is the only thing that can close the two open evidence items. Publishing\n   is outward-facing and was not authorised, so no tag was created.\n\n## Related: the credential scan went red, and why it is green again\n\nCommitting bundle `0.7.0` for `story:ledger-rows-carry-the-declared-grant` turned\n`scripts/check-secrets.sh` red — 32 findings, every one the `jwt` rule on a delegated-context\nconformance vector. Those vectors are JWTs by definition (ADR 0011 fixes the document as a compact\nJWS) and carry no credential. `.gitleaks.toml` keeps the default rule set and allows the `jwt` rule\nfor the delegated-context vector paths alone. Proven narrow rather than asserted: the same token is\nallowed in `contracts/substrate-wire/0.7.0/vectors/http/delegated-context-records-grant.json` and\nstill caught in `crates/substrate-daemon/src/`. `bash scripts/check-secrets.sh` → `no leaks found`,\n76 commits.\n\n## Both open evidence items closed — 2026-08-30\n\n**`cosign verify` with the workflow identity succeeds.** Run\n<https://github.com/beyond10x/substrate/actions/runs/33304493276>, step 8 *Verify the signature with\nthe workflow identity*: `success`. Steps 5-10 — build, push, install cosign, sign, verify, publish\nthe release — all `success`, in that order. `ghcr.io/beyond10x/b10x-substrate-daemon:0.2.3` at\n`sha256:ab10158266b579d705ce8422c7d2a6e783cde950d30e100f61ca6befc4d0beda`, built from `34b219a`.\n\n**A pre-release tag publishes nothing.** Tag `0.2.3-rc.0`, annotated, pushed to the remote at\n`062bc89` — a commit whose `gate.yml` run concluded `success`, so any refusal is about the version\nform and not incidentally about a missing gate conclusion. Confirmed on the remote:\n`git ls-remote --tags origin` → `refs/tags/0.2.3-rc.0` and `refs/tags/0.2.3-rc.0^{}` at `062bc89`.\n\nThe result is stronger than this story asked for. It asked for \"the pre-release-tag run URL showing\nno publish\"; **there is no URL, because no run was created at all**. The coarse ref filter\n`[0-9]+.[0-9]+.[0-9]+` rejected the ref before any workflow existed to refuse anything:\n\n- `gh run list --workflow release.yml` → only `33304493276`, the `0.2.3` run;\n- no run of any workflow for `headBranch=0.2.3-rc.0`;\n- newest run of any kind predates the push;\n- `gh release view 0.2.3-rc.0` → `release not found`.\n\n**What this does not prove.** The workflow's header claims a pre-release tag \"fails both\" the coarse\nglob and the anchored regex in the preflight job. Only the glob is verified: nothing reached the\nregex, so the preflight's version check remains asserted rather than observed. Exercising it needs a\nref that passes the glob and fails the regex.\n\n## Known, and not a defect\n\nThe release run reports `failure`. The only failing step is the last one, *Record the digest in\nCHANGELOG.md on main*: `main` is a protected branch requiring the `Full gate` status check, and a\ndirect push can never satisfy it because no gate run exists for a commit that was never pushed. The\nordering guarantee held — build, push, sign and verify all succeeded before anything was announced.\nThe digest was recorded by pull request (`68d226f`), which is the route branch protection leaves\nopen, and the workflow now says so by name instead of retrying three times and blaming a race.\n"},"args":{"changes":{"body":"# Story: A tagged main publishes a signed, digest-pinned daemon image\n\n## Outcome\n\nAn operator can `docker pull ghcr.io/beyond10x/b10x-substrate-daemon@sha256:…` for a tag, verify\nits signature, and know it was built by the bot from that tagged commit.\n\n## Context\n\n`Dockerfile` builds a distroless `substrate-daemon` with an `org.opencontainers.image.revision`\nlabel; no workflow builds, pushes or signs it, and `README.md` § *Status* records \"stable\npublication: **not done**\". `README.md:31` fixes the package prefix `b10x-substrate-*`.\n`AGENTS.md` § *Bot identity* requires automated pushes to use the GitHub App, and § *Releases*\nfixes the tag form: the bare version, annotated, at a fully gated `main` commit.\n\n## Acceptance\n\nAn annotated bare-version tag on `main` produces a keyless-signed image at\n`ghcr.io/beyond10x/b10x-substrate-daemon:<version>` whose digest is written to the GitHub release\nand under the version heading in `CHANGELOG.md`, and a pre-release tag such as `0.2.2-rc.0`\nproduces nothing.\n\nEvidence that satisfies it:\n\n- `.github/workflows/release.yml` triggers only on the bare-version form and refuses to build\n  unless `gate.yml` is green for that commit;\n- `SOURCE_SHA` = the tag's commit; `cosign verify` with the workflow identity succeeds; a signing\n  failure fails the job before anything is announced;\n- release notes state that the wire contract bundles remain development bundles;\n- `permissions` are `contents: read` at workflow level and `packages: write`, `id-token: write` on\n  the release job only; every action SHA-pinned;\n- the pre-release-tag run URL recorded here showing no publish.\n\n## Out of Scope\n\nBit-for-bit reproducible binaries: the sha256 is recorded, reproduction is a later milestone. The\nimage's `VOLUME` and `EXPOSE 8080` describe the development-only TCP posture; release notes must\nnot describe the image as a hosted posture (design 06 § 1).\n\n## Progress — 2026-08-30, the workflow exists and has never run\n\nMerged as `fced5fb` (PR #22). `.github/workflows/release.yml` builds the `Dockerfile`, publishes\n`ghcr.io/beyond10x/b10x-substrate-daemon:<version>`, keyless-signs and digest-pins it.\n\n**No image is published.** The workflow existing and an image existing are different claims;\n`README.md`, `STATUS.md` and `AGENTS.md` § *Releases* each keep them apart.\n\nAcceptance, item by item:\n\n| evidence | state |\n|---|---|\n| triggers only on the bare-version form | **met** — coarse ref glob plus an anchored regex in preflight; `0.2.2-rc.0`, `v0.2.2`, `0.2` and `0.2.2.1` all refuse |\n| refuses to build unless `gate.yml` is green for that commit | **met** — preflight reads that workflow's own recorded conclusion for the tagged SHA rather than re-running a lookalike |\n| `SOURCE_SHA` = the tag's commit | **met** — `git rev-parse refs/tags/<tag>^{commit}`, cross-checked against `github.sha` |\n| a signing failure fails the job before anything is announced | **met** — build → push → sign → `cosign verify` → *then* token, notes, release, changelog |\n| notes state the bundles remain development bundles | **met** — § *What this release does not claim*, citing atlas ADR 0019 |\n| `permissions` scoped; every action SHA-pinned | **met** — `packages: write`/`id-token: write` on the release job alone; preflight holds `actions: read` because no smaller scope reads another workflow's conclusion for a SHA |\n| `cosign verify` with the workflow identity succeeds | **open** — needs a real tag push |\n| a pre-release-tag run URL showing no publish | **open** — proving the negative needs `0.2.2-rc.0` pushed |\n\nAll four action pins were resolved against their upstream repositories rather than taken on report:\n`actions/checkout@3d3c42e5` is `refs/tags/v7` and `v7.0.1`; `sigstore/cosign-installer@6f9f1778` is\n`refs/tags/v4.1.2`; `actions/create-github-app-token@bcd2ba49` is `refs/tags/v3` and `v3.2.0`.\n\n## Blocked on the operator\n\n1. Repository secrets `B10X_BOT_APP_ID` and `B10X_BOT_PRIVATE_KEY`, and a b10x-bot installation\n   with `contents: write`. Absent them the job fails **after** the image is pushed and signed and\n   **before** anything is announced — a signed image with no release.\n2. A real tag push, which is the only thing that can close the two open evidence items. Publishing\n   is outward-facing and was not authorised, so no tag was created.\n\n## Related: the credential scan went red, and why it is green again\n\nCommitting bundle `0.7.0` for `story:ledger-rows-carry-the-declared-grant` turned\n`scripts/check-secrets.sh` red — 32 findings, every one the `jwt` rule on a delegated-context\nconformance vector. Those vectors are JWTs by definition (ADR 0011 fixes the document as a compact\nJWS) and carry no credential. `.gitleaks.toml` keeps the default rule set and allows the `jwt` rule\nfor the delegated-context vector paths alone. Proven narrow rather than asserted: the same token is\nallowed in `contracts/substrate-wire/0.7.0/vectors/http/delegated-context-records-grant.json` and\nstill caught in `crates/substrate-daemon/src/`. `bash scripts/check-secrets.sh` → `no leaks found`,\n76 commits.\n\n## Both open evidence items closed — 2026-08-30\n\n**`cosign verify` with the workflow identity succeeds.** Run\n<https://github.com/beyond10x/substrate/actions/runs/33304493276>, step 8 *Verify the signature with\nthe workflow identity*: `success`. Steps 5-10 — build, push, install cosign, sign, verify, publish\nthe release — all `success`, in that order. `ghcr.io/beyond10x/b10x-substrate-daemon:0.2.3` at\n`sha256:ab10158266b579d705ce8422c7d2a6e783cde950d30e100f61ca6befc4d0beda`, built from `34b219a`.\n\n**A pre-release tag publishes nothing.** Tag `0.2.3-rc.0`, annotated, pushed to the remote at\n`062bc89` — a commit whose `gate.yml` run concluded `success`, so any refusal is about the version\nform and not incidentally about a missing gate conclusion. Confirmed on the remote:\n`git ls-remote --tags origin` → `refs/tags/0.2.3-rc.0` and `refs/tags/0.2.3-rc.0^{}` at `062bc89`.\n\nThe result is stronger than this story asked for. It asked for \"the pre-release-tag run URL showing\nno publish\"; **there is no URL, because no run was created at all**. The coarse ref filter\n`[0-9]+.[0-9]+.[0-9]+` rejected the ref before any workflow existed to refuse anything:\n\n- `gh run list --workflow release.yml` → only `33304493276`, the `0.2.3` run;\n- no run of any workflow for `headBranch=0.2.3-rc.0`;\n- newest run of any kind predates the push;\n- `gh release view 0.2.3-rc.0` → `release not found`.\n\n**What this does not prove.** The workflow's header claims a pre-release tag \"fails both\" the coarse\nglob and the anchored regex in the preflight job. Only the glob is verified: nothing reached the\nregex, so the preflight's version check remains asserted rather than observed. Exercising it needs a\nref that passes the glob and fails the regex.\n\n## Known, and not a defect\n\nThe release run reports `failure`. The only failing step is the last one, *Record the digest in\nCHANGELOG.md on main*: `main` is a protected branch requiring the `Full gate` status check, and a\ndirect push can never satisfy it because no gate run exists for a commit that was never pushed. The\nordering guarantee held — build, push, sign and verify all succeeded before anything was announced.\nThe digest was recorded by pull request (`68d226f`), which is the route branch protection leaves\nopen, and the workflow now says so by name instead of retrying three times and blaming a race.\n"},"command":"update-entity","target":"01MEM0000000000000049"},"payload":{"actor":"human:timo","at":1788088934332,"causation":"cmd-protocol-artifact-body-story-signed-daemon-image-1788088934332","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body","event_id":"story:signed-daemon-image@6#0~9200dc42e302224e","executor":null,"recorded_at":"2026-08-30T11:22:14Z"}}
{"entity":"story","version":1,"id":"signed-daemon-image","revision":6,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"deployment_result","reference":"ghcr.io/beyond10x/b10x-substrate-daemon:0.2.3 at sha256:ab101582, cosign verify success","source":"release.yml run 33304493276","target":"01MEM0000000000000049"},"payload":{"actor":"human:timo","at":1788088934000,"causation":"cmd-evidence-story-signed-daemon-image-deployment_result-1788088934346","change":{"change":"evidence","kind":"deployment_result","reference":"ghcr.io/beyond10x/b10x-substrate-daemon:0.2.3 at sha256:ab101582, cosign verify success","source":"release.yml run 33304493276"},"correlation":"protocol-artifact-evidence","event_id":"story:signed-daemon-image@6#0~0786dd537a0cc05a","executor":null,"recorded_at":"2026-08-30T11:22:14Z"}}
{"entity":"story","version":1,"id":"signed-daemon-image","revision":6,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"verification","reference":"no workflow run created; gh release view 0.2.3-rc.0 -> release not found","source":"tag 0.2.3-rc.0 pushed to origin at 062bc89","target":"01MEM0000000000000049"},"payload":{"actor":"human:timo","at":1788088934000,"causation":"cmd-evidence-story-signed-daemon-image-verification-1788088934361","change":{"change":"evidence","kind":"verification","reference":"no workflow run created; gh release view 0.2.3-rc.0 -> release not found","source":"tag 0.2.3-rc.0 pushed to origin at 062bc89"},"correlation":"protocol-artifact-evidence","event_id":"story:signed-daemon-image@6#0~3e125f0a9307fe1f","executor":null,"recorded_at":"2026-08-30T11:22:14Z"}}
{"entity":"story","version":1,"id":"signed-daemon-image","revision":6,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"gate: passed, exit 0","source":"bash scripts/gate.sh on main a7c040a, run by the orchestrator","target":"01MEM0000000000000049"},"payload":{"actor":"human:timo","at":1788089012000,"causation":"cmd-evidence-story-signed-daemon-image-test_result-1788089012595","change":{"change":"evidence","kind":"test_result","reference":"gate: passed, exit 0","source":"bash scripts/gate.sh on main a7c040a, run by the orchestrator"},"correlation":"protocol-artifact-evidence","event_id":"story:signed-daemon-image@6#0~79a1297f9cf149c6","executor":null,"recorded_at":"2026-08-30T11:23:32Z"}}
{"entity":"story","version":1,"id":"signed-daemon-image","revision":7,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1,\"deployment_result\":1,\"artifact\":1,\"verification\":1}}","target":"01MEM0000000000000049","to":"implemented"},"payload":{"actor":"human:timo","at":1788089012624,"causation":"cmd-move-story-signed-daemon-image-1788089012624","change":{"change":"moved","decided_on":{"recorded":{"artifact":1,"deployment_result":1,"test_result":1,"verification":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:signed-daemon-image@7#0~704b2bde5c9d51a7","executor":null,"recorded_at":"2026-08-30T11:23:32Z"}}
{"entity":"story","version":1,"id":"pty-sessions","revision":5,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"# Story: PTY sessions are a distinct session kind with resize\n\n## Outcome\n\nA human can attach a terminal to a confined process, resize it, and get the same lease, single\nattachment, bounded-output and whole-tree cleanup guarantees the raw-pipe session gives.\n\n## Context\n\n`docs/design/05-streams-sessions-and-endpoints.md` § 2 fixes the `pty` kind and its frames\n(input, output, resize, signal, exit, protocol-error) and says a PTY is never substituted for\npipes. `README.md` § *Status* lists PTY as **absent**; `docs/plan/04-direct-byte-plane.md`\n§ *Later phase-4 slices* lists it as remaining. Invariant 8 puts the ADR before code; invariant 6\nputs the wire change in a successor bundle.\n\n## Acceptance\n\nThe delegated lane runs an interactive shell through a `pty` session, echoes bytes, applies a\nresize the child observes, and exits on hangup — while the portable lane proves the typed\n`unserved` and every `0.4.0` byte is unchanged.\n\nEvidence that satisfies it, in order:\n\n1. A design document in `docs/design/`, proposed, fixing kind, frame set, resize bounds, never a\n   substitute for pipes, and the named refusal when the host cannot allocate a pty. It claims no\n   ADR number: `adr/` admits `accepted` and `superseded` only (`xtask/src/adrs.rs:12`), so the\n   number is assigned at acceptance.\n2. The next free successor bundle under `contracts/substrate-wire/` adds the `pty` kind and\n   `resize` frame; compatibility block names its predecessor with exact\n   `adds_routes`/`preserves_routes`; authored source under `xtask/bundle-source/<version>/` and\n   `cargo xtask check-bundle <version>` in `scripts/gate.sh`.\n3. `crates/substrate-host`: pty opened with `nix::pty::openpty` inside the existing\n   bubblewrap/cgroup/cleared-environment path. Failing-first tests:\n   `pty_session_refused_without_confinement`, `pty_resize_is_applied_and_observed` (child reads\n   `TIOCGWINSZ`), `pty_session_kills_tree_on_attachment_loss`.\n4. Daemon: the single-attachment Unix-WebSocket route family serves `pty`; `resize` outside the\n   declared bounds is a protocol error.\n5. Capability fact `sessions.pty` appears only after startup verified pty allocation (invariant 4).\n6. `STATUS.md`, `README.md` § *Status* and plan 04 § *Later phase-4 slices* updated in the same\n   change.\n\n## Open Questions\n\nBundle number: `0.5.0` if this lands first in the epic; the ADR states the actual number.\n\n## Correction — 2026-08-30\n\nThree claims in the evidence list above had gone stale and are rewritten, not deleted, so the\nchange is visible:\n\n| Claimed | Actually |\n|---|---|\n| `adr/0011-pty-sessions-are-a-distinct-session-kind.md` | `0011` is `adr/0011-delegated-context-and-grant-attribution.md`; the number was taken while this story sat in draft |\n| `contracts/substrate-wire/0.5.0/` | `0.5.0` is sealed secret slots and `0.6.0` is destination-bound egress; both are frozen (invariant 6) |\n| `render-contract-bundle-0.5.0.py`, `check-contract-bundle-0.5.0.py` | the Python renderer/checker pairs stop at `0.4.0`; from `0.5.0` on the check is `cargo xtask check-bundle <version>` (`scripts/gate.sh:24-28`) |\n\nThe story pins no numbers now, so it cannot go stale the same way again.\n\n## Scope\n\nDerived 2026-08-30 by `story-scoper`. Every line is **cited** or **inferred**.\n\n- **Primary surface:** `crates/substrate-host` and the `crates/substrate-daemon` session surface —\n  cited, evidence items 3 and 4 assign work to each by name.\n- **Files, inferred:** `crates/substrate-host/src/process.rs:260` (`start_pipe`, the analogue a pty\n  allocation sits beside); `crates/substrate-wire/src/lib.rs:1196` (`SessionMode`);\n  `crates/substrate-daemon/src/app/sessions.rs` and `app/routes.rs:69-85`;\n  `crates/substrate-store/src/sessions.rs` (ADR 0008 durable identity).\n- **Files, cited:** `scripts/gate.sh:24-28`, `xtask/src/adrs.rs:12`.\n- **Files, inferred:** `Cargo.toml:37` — `nix` is a workspace dependency at features\n  `[\"fs\", \"user\"]`; `nix::pty::openpty` needs `term`, and `substrate-host/Cargo.toml` does not\n  depend on `nix` at all.\n- **Symbols, cited:** `SessionMode`, `PipeSessionStartInput`, `PipeClientFrame`, `PipeServerFrame`,\n  `PipeSessionCapabilities`, `PipeSessionLimits` — all declared `crates/substrate-wire/src/lib.rs:1181-1310`.\n- **Also likely:** `contracts/substrate-wire/0.8.0/` and `xtask/bundle-source/0.8.0/` — inferred;\n  `0.7.0` is the frontier, one past what this story's own correction records.\n- **Confidence:** **high for where the work lands** — every cited path resolved in the tree — but no\n  design document and no ADR exist, so invariant 8 (`AGENTS.md:62-64`) blocks the code half.\n- **Would collide with:** any unit touching the `substrate-wire` session types, the daemon\n  `pipe-sessions` route family, `substrate-host` process spawning, `substrate-store` session\n  persistence, or the next contract bundle number. Broad by construction, not by widening.\n\n### Two contradictions found while scoping\n\n**1. `kind` or `mode`?** This story says a `pty` **kind**; design 05 § 2 and `adr/0007:22` say\n**mode**. The code has both, on different axes: `SessionKind` is the *resource* axis and holds one\nvariant `Session` (`crates/substrate-wire/src/lib.rs:1189-1192`), while `SessionMode` holds one\nvariant `Pipes` (`:1194-1198`). Which enum grows is unresolved, and it decides the wire shape.\n\n**2. This story asks for more than invariant 8 requires, and its parent says so.** Evidence item 1\ndemands a new design document. `epic:byte-plane-completion:29` says these proceed \"under invariant 8\nwith an ADR each and **no new design document**\", because design 05 § 2 already fixes the frames.\n`AGENTS.md:62-64` reads \"a design document **or** an ADR **before code**\" — so the epic's reading is\nthe admissible one and this story is over-specifying its own gate. Resolve before dispatching work.\n\n## Design draft — 2026-08-30\n\n`docs/design/13-pty-sessions.md`, **proposed**. Claims no ADR number: `adr/` admits `accepted` and\n`superseded` only (`xtask/src/adrs.rs:12`), so the number is assigned at acceptance.\n\nBoth contradictions this story carried are now decided:\n\n- **`SessionMode` grows a `Pty` variant**, not `SessionKind`. `SessionKind` is the *resource* axis\n  and mirrors `ExecKind`; growing it would fork the single `ses_…`/`ex_…` resource ADR 0008 built.\n- **The route family is reused**, `/v1/pipe-sessions/*` with a `mode` field defaulting to `pipes`,\n  so `adds_routes: 0`. A new family means seven duplicate mode-neutral operation ids and a `GET`\n  that would have to claim `absent: true` for a live session; renaming the path is a coordinated\n  migration with an ADR in atlas and removes seven routes, which is not `additive-v1`.\n\nAlso fixed: `sessions.pty` becomes a probe-verified `CapabilityFacts` member; pty unavailable is\n`unserved`/`session.pty-unserved` (501) and allocation failure `exhausted`/`session.pty-exhausted`\n(429, retriable) — **never a fall back to pipes**; resize is 1–1000 cols/rows in cells, rated on the\nexisting control window, with an initial window required and no 80×24 default; an output bound ends\nthe session through ADR 0014's refusal field, because design 05 gave the pty no `truncated` frame\nand a terminal has no offset to resume from.\n\n**Finding the draft turned up, verified in the tree.** The shared confinement path passes\n`--new-session` (`crates/substrate-host/src/process.rs:1113`), which `bwrap(1)` documents as calling\n`setsid()` and disconnecting the sandbox from the controlling terminal. Inheriting the pty slave as\nfd 0/1/2 therefore gives no job control and no hangup — which is what this story's acceptance needs.\nThe design takes the controlling terminal *inside* the sandbox after bwrap's setsid, and forbids\ndropping the flag.\n\nBundle `0.9.0` is **provisional**: design 14 names it too, and it belongs to whichever is accepted\nfirst.\n\nNot established, and left to the implementing work: which interposition acquires the controlling\nterminal. Kernel `SIGWINCH`/`TIOCSCTTY`/hangup semantics are labelled as kernel behaviour rather\nthan verified in this tree.\n"},"args":{"changes":{"body":"# Story: PTY sessions are a distinct session kind with resize\n\n## Outcome\n\nA human can attach a terminal to a confined process, resize it, and get the same lease, single\nattachment, bounded-output and whole-tree cleanup guarantees the raw-pipe session gives.\n\n## Context\n\n`docs/design/05-streams-sessions-and-endpoints.md` § 2 fixes the `pty` kind and its frames\n(input, output, resize, signal, exit, protocol-error) and says a PTY is never substituted for\npipes. `README.md` § *Status* lists PTY as **absent**; `docs/plan/04-direct-byte-plane.md`\n§ *Later phase-4 slices* lists it as remaining. Invariant 8 puts the ADR before code; invariant 6\nputs the wire change in a successor bundle.\n\n## Acceptance\n\nThe delegated lane runs an interactive shell through a `pty` session, echoes bytes, applies a\nresize the child observes, and exits on hangup — while the portable lane proves the typed\n`unserved` and every `0.4.0` byte is unchanged.\n\nEvidence that satisfies it, in order:\n\n1. A design document in `docs/design/`, proposed, fixing kind, frame set, resize bounds, never a\n   substitute for pipes, and the named refusal when the host cannot allocate a pty. It claims no\n   ADR number: `adr/` admits `accepted` and `superseded` only (`xtask/src/adrs.rs:12`), so the\n   number is assigned at acceptance.\n2. The next free successor bundle under `contracts/substrate-wire/` adds the `pty` kind and\n   `resize` frame; compatibility block names its predecessor with exact\n   `adds_routes`/`preserves_routes`; authored source under `xtask/bundle-source/<version>/` and\n   `cargo xtask check-bundle <version>` in `scripts/gate.sh`.\n3. `crates/substrate-host`: pty opened with `nix::pty::openpty` inside the existing\n   bubblewrap/cgroup/cleared-environment path. Failing-first tests:\n   `pty_session_refused_without_confinement`, `pty_resize_is_applied_and_observed` (child reads\n   `TIOCGWINSZ`), `pty_session_kills_tree_on_attachment_loss`.\n4. Daemon: the single-attachment Unix-WebSocket route family serves `pty`; `resize` outside the\n   declared bounds is a protocol error.\n5. Capability fact `sessions.pty` appears only after startup verified pty allocation (invariant 4).\n6. `STATUS.md`, `README.md` § *Status* and plan 04 § *Later phase-4 slices* updated in the same\n   change.\n\n## Open Questions\n\nBundle number: `0.5.0` if this lands first in the epic; the ADR states the actual number.\n\n## Correction — 2026-08-30\n\nThree claims in the evidence list above had gone stale and are rewritten, not deleted, so the\nchange is visible:\n\n| Claimed | Actually |\n|---|---|\n| `adr/0011-pty-sessions-are-a-distinct-session-kind.md` | `0011` is `adr/0011-delegated-context-and-grant-attribution.md`; the number was taken while this story sat in draft |\n| `contracts/substrate-wire/0.5.0/` | `0.5.0` is sealed secret slots and `0.6.0` is destination-bound egress; both are frozen (invariant 6) |\n| `render-contract-bundle-0.5.0.py`, `check-contract-bundle-0.5.0.py` | the Python renderer/checker pairs stop at `0.4.0`; from `0.5.0` on the check is `cargo xtask check-bundle <version>` (`scripts/gate.sh:24-28`) |\n\nThe story pins no numbers now, so it cannot go stale the same way again.\n\n## Scope\n\nDerived 2026-08-30 by `story-scoper`. Every line is **cited** or **inferred**.\n\n- **Primary surface:** `crates/substrate-host` and the `crates/substrate-daemon` session surface —\n  cited, evidence items 3 and 4 assign work to each by name.\n- **Files, inferred:** `crates/substrate-host/src/process.rs:260` (`start_pipe`, the analogue a pty\n  allocation sits beside); `crates/substrate-wire/src/lib.rs:1196` (`SessionMode`);\n  `crates/substrate-daemon/src/app/sessions.rs` and `app/routes.rs:69-85`;\n  `crates/substrate-store/src/sessions.rs` (ADR 0008 durable identity).\n- **Files, cited:** `scripts/gate.sh:24-28`, `xtask/src/adrs.rs:12`.\n- **Files, inferred:** `Cargo.toml:37` — `nix` is a workspace dependency at features\n  `[\"fs\", \"user\"]`; `nix::pty::openpty` needs `term`, and `substrate-host/Cargo.toml` does not\n  depend on `nix` at all.\n- **Symbols, cited:** `SessionMode`, `PipeSessionStartInput`, `PipeClientFrame`, `PipeServerFrame`,\n  `PipeSessionCapabilities`, `PipeSessionLimits` — all declared `crates/substrate-wire/src/lib.rs:1181-1310`.\n- **Also likely:** `contracts/substrate-wire/0.8.0/` and `xtask/bundle-source/0.8.0/` — inferred;\n  `0.7.0` is the frontier, one past what this story's own correction records.\n- **Confidence:** **high for where the work lands** — every cited path resolved in the tree — but no\n  design document and no ADR exist, so invariant 8 (`AGENTS.md:62-64`) blocks the code half.\n- **Would collide with:** any unit touching the `substrate-wire` session types, the daemon\n  `pipe-sessions` route family, `substrate-host` process spawning, `substrate-store` session\n  persistence, or the next contract bundle number. Broad by construction, not by widening.\n\n### Two contradictions found while scoping\n\n**1. `kind` or `mode`?** This story says a `pty` **kind**; design 05 § 2 and `adr/0007:22` say\n**mode**. The code has both, on different axes: `SessionKind` is the *resource* axis and holds one\nvariant `Session` (`crates/substrate-wire/src/lib.rs:1189-1192`), while `SessionMode` holds one\nvariant `Pipes` (`:1194-1198`). Which enum grows is unresolved, and it decides the wire shape.\n\n**2. This story asks for more than invariant 8 requires, and its parent says so.** Evidence item 1\ndemands a new design document. `epic:byte-plane-completion:29` says these proceed \"under invariant 8\nwith an ADR each and **no new design document**\", because design 05 § 2 already fixes the frames.\n`AGENTS.md:62-64` reads \"a design document **or** an ADR **before code**\" — so the epic's reading is\nthe admissible one and this story is over-specifying its own gate. Resolve before dispatching work.\n\n## Design draft — 2026-08-30\n\n`docs/design/13-pty-sessions.md`, **proposed**. Claims no ADR number: `adr/` admits `accepted` and\n`superseded` only (`xtask/src/adrs.rs:12`), so the number is assigned at acceptance.\n\nBoth contradictions this story carried are now decided:\n\n- **`SessionMode` grows a `Pty` variant**, not `SessionKind`. `SessionKind` is the *resource* axis\n  and mirrors `ExecKind`; growing it would fork the single `ses_…`/`ex_…` resource ADR 0008 built.\n- **The route family is reused**, `/v1/pipe-sessions/*` with a `mode` field defaulting to `pipes`,\n  so `adds_routes: 0`. A new family means seven duplicate mode-neutral operation ids and a `GET`\n  that would have to claim `absent: true` for a live session; renaming the path is a coordinated\n  migration with an ADR in atlas and removes seven routes, which is not `additive-v1`.\n\nAlso fixed: `sessions.pty` becomes a probe-verified `CapabilityFacts` member; pty unavailable is\n`unserved`/`session.pty-unserved` (501) and allocation failure `exhausted`/`session.pty-exhausted`\n(429, retriable) — **never a fall back to pipes**; resize is 1–1000 cols/rows in cells, rated on the\nexisting control window, with an initial window required and no 80×24 default; an output bound ends\nthe session through ADR 0014's refusal field, because design 05 gave the pty no `truncated` frame\nand a terminal has no offset to resume from.\n\n**Finding the draft turned up, verified in the tree.** The shared confinement path passes\n`--new-session` (`crates/substrate-host/src/process.rs:1113`), which `bwrap(1)` documents as calling\n`setsid()` and disconnecting the sandbox from the controlling terminal. Inheriting the pty slave as\nfd 0/1/2 therefore gives no job control and no hangup — which is what this story's acceptance needs.\nThe design takes the controlling terminal *inside* the sandbox after bwrap's setsid, and forbids\ndropping the flag.\n\nBundle `0.9.0` is **provisional**: design 14 names it too, and it belongs to whichever is accepted\nfirst.\n\nNot established, and left to the implementing work: which interposition acquires the controlling\nterminal. Kernel `SIGWINCH`/`TIOCSCTTY`/hangup semantics are labelled as kernel behaviour rather\nthan verified in this tree.\n"},"command":"update-entity","target":"01MEM0000000000000040"},"payload":{"actor":"human:timo","at":1788090373609,"causation":"cmd-protocol-artifact-body-story-pty-sessions-1788090373609","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body","event_id":"story:pty-sessions@5#0~f20385258baacf4d","executor":null,"recorded_at":"2026-08-30T11:46:13Z"}}
{"entity":"story","version":1,"id":"pty-sessions","revision":5,"type":"aep.evidence.record/v1","from_state":"draft","to_state":"draft","changed":{},"args":{"command":"record-evidence","kind":"specification","reference":"docs/design/13-pty-sessions.md","source":"design 13, proposed","target":"01MEM0000000000000040"},"payload":{"actor":"human:timo","at":1788090373000,"causation":"cmd-evidence-story-pty-sessions-specification-1788090373629","change":{"change":"evidence","kind":"specification","reference":"docs/design/13-pty-sessions.md","source":"design 13, proposed"},"correlation":"protocol-artifact-evidence","event_id":"story:pty-sessions@5#0~e429fa17cab8c8cb","executor":null,"recorded_at":"2026-08-30T11:46:13Z"}}
{"entity":"story","version":1,"id":"network-session-authority","revision":4,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"# Story: Network session transport over TLS with single-use proof-bound authority\n\n## Outcome\n\nA client on another machine attaches to a session over WebSocket/TLS with an authority that lives\nat most 60 s, redeems exactly once, and is bound to the redeeming channel; a reconnect always\nneeds a fresh one. This is the last phase-4 exit criterion.\n\n## Context\n\n`docs/design/05-streams-sessions-and-endpoints.md` § *V1 decisions* 3–4 fix the authority and the\ntransport; `architecture/deployment-postures.md` requires TLS/mTLS or a trusted tunnel for any\nnon-loopback control listener. Today only the owner-permissioned Unix socket serves sessions\n(plan 04 § *Slice B*). The static-bearer TCP listener is development-only\n(`crates/substrate-daemon/src/main.rs:63-64`, design 06 § 1) and must not gain this route.\n\n## Acceptance\n\nThe delegated lane drives a raw-pipe session end to end over TLS from a second network namespace,\nand the authority it used cannot be redeemed a second time, after 60 s, or over a different TLS\nsession.\n\nEvidence that satisfies it, in order:\n\n1. An ADR decides the proof binding (TLS exporter per RFC 5705, or a client key) and records that\n   reconnect is a new authority, never a resumed one.\n2. A successor bundle adds the network attachment route and the authority-redemption frame; earlier\n   bundle bytes unchanged.\n3. Failing-first tests: `network_session_listener_refuses_plaintext`,\n   `session_authority_redeems_exactly_once`, `session_authority_expires_after_60s` (tokio paused\n   time), `session_authority_bound_to_channel`, `second_concurrent_attachment_refused`.\n4. The authority value never appears in logs, events or error bodies — a test greps captured\n   diagnostics (design 05 § 3, \"non-loggable\").\n5. The development-only TCP listener does not serve the route — a test proves its absence there.\n\n## Out of Scope\n\nThe hosted trust-envelope verifier (design 06 § 1, atlas ADR 0015; phase 7): this story issues\nthe authority from the local bearer subject.\n\n## Scope\n\nDerived 2026-08-30 by `story-scoper`. Every line is **cited** or **inferred**.\n\n- **Primary surface:** `crates/substrate-daemon` — cited; both the listener and the attach route\n  live there.\n- **Files, cited:** `crates/substrate-daemon/src/runtime.rs:736` (`serve_tcp`), `:856`\n  (`require_tcp_bearer`), `src/app/routes.rs:33` (`router`), `src/app/sessions.rs:735`\n  (`pipe_session_attach`).\n- **Symbols, cited:** `serve_tcp`, `TcpAuthState`, `require_tcp_bearer`, `router`,\n  `pipe_session_attach`, `PipeAttachmentPermit`, `PipeAttachmentRefusal`.\n- **Also likely, inferred:** `crates/substrate-wire/src/lib.rs:1274`/`:1291` (`PipeClientFrame`,\n  `PipeServerFrame`), where an authority-redemption frame would be added;\n  `crates/substrate-store/src/schema.rs:154` and `src/sessions.rs:526`\n  (`claim_pipe_session_attachment`), where redeem-exactly-once becomes durable;\n  `crates/substrate-daemon/src/main.rs:192-222`, the clap flags beside the `--tcp-*` group.\n- **Dependency, cited:** the workspace has **no** TLS crate — `rustls`/`tokio-rustls` grep to\n  nothing — so the dependency and its configuration surface are unmade.\n- **Confidence:** **medium.** The crate and the two code sites are cited and certain, but there is\n  no accepted ADR (`adr/` holds 0001–0014) and `docs/design/05-streams-sessions-and-endpoints.md:65`\n  points at `architecture/adr/0016-operation-scoped-session-authority.md`, which does not exist in\n  this repository. The proof binding — TLS exporter or client key — is undecided, so every TLS-side\n  file above is a guess at an unmade decision.\n- **Would collide with:** any unit touching the listener surface (`runtime.rs` `serve`/`serve_tcp`)\n  or the shared `app/routes.rs` `router`; and any unit cutting a successor\n  `contracts/substrate-wire/` version — that bump is a single serialising surface across the epic.\n\n### Two corrections found while scoping\n\n**1. A cited line is stale.** This story cites `crates/substrate-daemon/src/main.rs:63-64` as the\ndevelopment-only TCP listener. Those two lines are `port,` and `})` — the tail of\n`parse_egress_aperture`. The listener is `crates/substrate-daemon/src/runtime.rs:736`.\n\n**2. Acceptance 5 is already false at HEAD.** This story says the development-only TCP transport\n\"must not gain this route\". It has it: `serve_tcp` builds its service with the same `router(app)`\nas the Unix path (`crates/substrate-daemon/src/runtime.rs:757` against `:508`), and that router\nregisters `/v1/pipe-sessions/{session_id}/attach` (`crates/substrate-daemon/src/app/routes.rs:76-77`).\n\nNot an open exposure: `serve_tcp` bails unless both `--tcp-development-only` and\n`--tcp-private-overlay` are set (`crates/substrate-daemon/src/runtime.rs:737-743`), so the route is\nreachable only in an explicitly acknowledged development profile on a private overlay. But the story\nasserts a property the code does not have, so either the acceptance must change or the router must\nsplit — and nothing in the tree specifies a split. Decide it in the ADR this story still owes.\n\n## Design draft — 2026-08-30\n\n`docs/design/14-network-session-authority.md`, **proposed**. Claims no ADR number.\n\n- **Proof binding: a client Ed25519 key, signing the RFC 5705 exporter.** The exporter alone proves\n  nothing — both ends of any TLS session derive a matching value, so a thief redeems on their own\n  connection; it stops a re-encrypting proxy and nothing else, and calling that \"proof-bound\" is\n  invariant 3's silent degradation. The key costs no new crate.\n- **TLS is `rustls` + `tokio-rustls`.** `native-tls` does not portably expose\n  `export_keying_material`, so choosing it would decide the binding by omission.\n- **Single redemption is consumed inside `claim_pipe_session_attachment`'s existing `Immediate`\n  transaction** (`crates/substrate-store/src/sessions.rs:526-546`), in new columns beside\n  `resource_json` and never in it — a second transaction leaves a window where the authority is\n  spent and the attachment is not, and `resource_json` is returned by `GET /v1/pipe-sessions/{id}`.\n\n**The acceptance this story carried was false, and it stands — the code changes.** The\ndevelopment-only TCP transport already serves the attach route: `serve_tcp` builds its service with\nthe same `router(app)` as the Unix path (`crates/substrate-daemon/src/runtime.rs:785` against\n`:514`), and that router registers `/v1/pipe-sessions/{session_id}/attach`\n(`crates/substrate-daemon/src/app/routes.rs:75-78`). The design splits the router by property: no\nlistener that cannot carry an authority confidentially serves the mint or the attach.\n\n**Why the split rather than restating the acceptance, verified independently by the orchestrator.**\n`--tcp-private-overlay` is a bare boolean an operator asserts; there is **no `is_loopback` call\nanywhere** in `crates/substrate-daemon/src/runtime.rs` or `src/main.rs`, so a non-loopback plaintext\nbind is accepted, and the bearer guarding it has no expiry or rotation (`read_bearer_digest`). On a\nwrongly-declared \"private\" address, one observer owns every confined process's stdin and stdout,\nindefinitely. A fifth startup `bail!` refusing non-loopback plaintext lands with the split.\n\nThe static-bearer TCP path is **kept and narrowed**, not superseded: removing `--tcp-listen` breaks\na documented posture and buys nothing here.\n\nBundle `0.9.0` is **provisional**: design 13 names it too.\n\nNot established: the exporter label string is a new wire-visible identifier, and whether it may\ncarry the former brand is atlas ADR 0001's call — deferred to the bundle cut.\n"},"args":{"changes":{"body":"# Story: Network session transport over TLS with single-use proof-bound authority\n\n## Outcome\n\nA client on another machine attaches to a session over WebSocket/TLS with an authority that lives\nat most 60 s, redeems exactly once, and is bound to the redeeming channel; a reconnect always\nneeds a fresh one. This is the last phase-4 exit criterion.\n\n## Context\n\n`docs/design/05-streams-sessions-and-endpoints.md` § *V1 decisions* 3–4 fix the authority and the\ntransport; `architecture/deployment-postures.md` requires TLS/mTLS or a trusted tunnel for any\nnon-loopback control listener. Today only the owner-permissioned Unix socket serves sessions\n(plan 04 § *Slice B*). The static-bearer TCP listener is development-only\n(`crates/substrate-daemon/src/main.rs:63-64`, design 06 § 1) and must not gain this route.\n\n## Acceptance\n\nThe delegated lane drives a raw-pipe session end to end over TLS from a second network namespace,\nand the authority it used cannot be redeemed a second time, after 60 s, or over a different TLS\nsession.\n\nEvidence that satisfies it, in order:\n\n1. An ADR decides the proof binding (TLS exporter per RFC 5705, or a client key) and records that\n   reconnect is a new authority, never a resumed one.\n2. A successor bundle adds the network attachment route and the authority-redemption frame; earlier\n   bundle bytes unchanged.\n3. Failing-first tests: `network_session_listener_refuses_plaintext`,\n   `session_authority_redeems_exactly_once`, `session_authority_expires_after_60s` (tokio paused\n   time), `session_authority_bound_to_channel`, `second_concurrent_attachment_refused`.\n4. The authority value never appears in logs, events or error bodies — a test greps captured\n   diagnostics (design 05 § 3, \"non-loggable\").\n5. The development-only TCP listener does not serve the route — a test proves its absence there.\n\n## Out of Scope\n\nThe hosted trust-envelope verifier (design 06 § 1, atlas ADR 0015; phase 7): this story issues\nthe authority from the local bearer subject.\n\n## Scope\n\nDerived 2026-08-30 by `story-scoper`. Every line is **cited** or **inferred**.\n\n- **Primary surface:** `crates/substrate-daemon` — cited; both the listener and the attach route\n  live there.\n- **Files, cited:** `crates/substrate-daemon/src/runtime.rs:736` (`serve_tcp`), `:856`\n  (`require_tcp_bearer`), `src/app/routes.rs:33` (`router`), `src/app/sessions.rs:735`\n  (`pipe_session_attach`).\n- **Symbols, cited:** `serve_tcp`, `TcpAuthState`, `require_tcp_bearer`, `router`,\n  `pipe_session_attach`, `PipeAttachmentPermit`, `PipeAttachmentRefusal`.\n- **Also likely, inferred:** `crates/substrate-wire/src/lib.rs:1274`/`:1291` (`PipeClientFrame`,\n  `PipeServerFrame`), where an authority-redemption frame would be added;\n  `crates/substrate-store/src/schema.rs:154` and `src/sessions.rs:526`\n  (`claim_pipe_session_attachment`), where redeem-exactly-once becomes durable;\n  `crates/substrate-daemon/src/main.rs:192-222`, the clap flags beside the `--tcp-*` group.\n- **Dependency, cited:** the workspace has **no** TLS crate — `rustls`/`tokio-rustls` grep to\n  nothing — so the dependency and its configuration surface are unmade.\n- **Confidence:** **medium.** The crate and the two code sites are cited and certain, but there is\n  no accepted ADR (`adr/` holds 0001–0014) and `docs/design/05-streams-sessions-and-endpoints.md:65`\n  points at `architecture/adr/0016-operation-scoped-session-authority.md`, which does not exist in\n  this repository. The proof binding — TLS exporter or client key — is undecided, so every TLS-side\n  file above is a guess at an unmade decision.\n- **Would collide with:** any unit touching the listener surface (`runtime.rs` `serve`/`serve_tcp`)\n  or the shared `app/routes.rs` `router`; and any unit cutting a successor\n  `contracts/substrate-wire/` version — that bump is a single serialising surface across the epic.\n\n### Two corrections found while scoping\n\n**1. A cited line is stale.** This story cites `crates/substrate-daemon/src/main.rs:63-64` as the\ndevelopment-only TCP listener. Those two lines are `port,` and `})` — the tail of\n`parse_egress_aperture`. The listener is `crates/substrate-daemon/src/runtime.rs:736`.\n\n**2. Acceptance 5 is already false at HEAD.** This story says the development-only TCP transport\n\"must not gain this route\". It has it: `serve_tcp` builds its service with the same `router(app)`\nas the Unix path (`crates/substrate-daemon/src/runtime.rs:757` against `:508`), and that router\nregisters `/v1/pipe-sessions/{session_id}/attach` (`crates/substrate-daemon/src/app/routes.rs:76-77`).\n\nNot an open exposure: `serve_tcp` bails unless both `--tcp-development-only` and\n`--tcp-private-overlay` are set (`crates/substrate-daemon/src/runtime.rs:737-743`), so the route is\nreachable only in an explicitly acknowledged development profile on a private overlay. But the story\nasserts a property the code does not have, so either the acceptance must change or the router must\nsplit — and nothing in the tree specifies a split. Decide it in the ADR this story still owes.\n\n## Design draft — 2026-08-30\n\n`docs/design/14-network-session-authority.md`, **proposed**. Claims no ADR number.\n\n- **Proof binding: a client Ed25519 key, signing the RFC 5705 exporter.** The exporter alone proves\n  nothing — both ends of any TLS session derive a matching value, so a thief redeems on their own\n  connection; it stops a re-encrypting proxy and nothing else, and calling that \"proof-bound\" is\n  invariant 3's silent degradation. The key costs no new crate.\n- **TLS is `rustls` + `tokio-rustls`.** `native-tls` does not portably expose\n  `export_keying_material`, so choosing it would decide the binding by omission.\n- **Single redemption is consumed inside `claim_pipe_session_attachment`'s existing `Immediate`\n  transaction** (`crates/substrate-store/src/sessions.rs:526-546`), in new columns beside\n  `resource_json` and never in it — a second transaction leaves a window where the authority is\n  spent and the attachment is not, and `resource_json` is returned by `GET /v1/pipe-sessions/{id}`.\n\n**The acceptance this story carried was false, and it stands — the code changes.** The\ndevelopment-only TCP transport already serves the attach route: `serve_tcp` builds its service with\nthe same `router(app)` as the Unix path (`crates/substrate-daemon/src/runtime.rs:785` against\n`:514`), and that router registers `/v1/pipe-sessions/{session_id}/attach`\n(`crates/substrate-daemon/src/app/routes.rs:75-78`). The design splits the router by property: no\nlistener that cannot carry an authority confidentially serves the mint or the attach.\n\n**Why the split rather than restating the acceptance, verified independently by the orchestrator.**\n`--tcp-private-overlay` is a bare boolean an operator asserts; there is **no `is_loopback` call\nanywhere** in `crates/substrate-daemon/src/runtime.rs` or `src/main.rs`, so a non-loopback plaintext\nbind is accepted, and the bearer guarding it has no expiry or rotation (`read_bearer_digest`). On a\nwrongly-declared \"private\" address, one observer owns every confined process's stdin and stdout,\nindefinitely. A fifth startup `bail!` refusing non-loopback plaintext lands with the split.\n\nThe static-bearer TCP path is **kept and narrowed**, not superseded: removing `--tcp-listen` breaks\na documented posture and buys nothing here.\n\nBundle `0.9.0` is **provisional**: design 13 names it too.\n\nNot established: the exporter label string is a new wire-visible identifier, and whether it may\ncarry the former brand is atlas ADR 0001's call — deferred to the bundle cut.\n"},"command":"update-entity","target":"01MEM0000000000000034"},"payload":{"actor":"human:timo","at":1788090373648,"causation":"cmd-protocol-artifact-body-story-network-session-authority-1788090373648","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body","event_id":"story:network-session-authority@4#0~1e1b6cc9d0438777","executor":null,"recorded_at":"2026-08-30T11:46:13Z"}}
{"entity":"story","version":1,"id":"network-session-authority","revision":4,"type":"aep.evidence.record/v1","from_state":"draft","to_state":"draft","changed":{},"args":{"command":"record-evidence","kind":"specification","reference":"docs/design/14-network-session-authority.md","source":"design 14, proposed","target":"01MEM0000000000000034"},"payload":{"actor":"human:timo","at":1788090373000,"causation":"cmd-evidence-story-network-session-authority-specification-1788090373661","change":{"change":"evidence","kind":"specification","reference":"docs/design/14-network-session-authority.md","source":"design 14, proposed"},"correlation":"protocol-artifact-evidence","event_id":"story:network-session-authority@4#0~cfef57642a2a87c8","executor":null,"recorded_at":"2026-08-30T11:46:13Z"}}
{"entity":"story","version":1,"id":"contract-gate-sees-route-paths","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"<!-- Starting point for a `story` artifact, seeded by `protocol artifact new story <name>`.\n     No frontmatter here on purpose: the `---` block is written by the CLI from the id, kind, status\n     and relations you gave it, and a second copy in this file would be the one that went stale.\n     Delete the italic guidance as you fill each section. -->\n\n# Story: <name>\n\n## Outcome\n\n*What is true for whom once this has shipped, in one sentence. If it names a component rather than a\nperson, it is a task — say what changes for someone.*\n\n## Context\n\n*Why this is worth doing now, and what it depends on. Link the epic or specification it comes from\nrather than restating it; the `derived_from` relation already carries the edge.*\n\n## Acceptance\n\n*The conditions under which this is done, each one something a person or a check can observe. \"Works\ncorrectly\" is not one of them.*\n\n## Out of Scope\n\n*What a reasonable reader would expect to be included and is not — the boundary that stops this\nstory quietly becoming an epic.*\n\n## Open Questions\n\n*What is still undecided, each with who decides it. A story carrying an unowned question is a story\nthat stalls without anybody noticing.*\n","summary":"route_ids collects operation ids, never paths, so moving a route passes check-bundle as adds_routes: 0.","title":"The contract gate refuses a renamed route path"},"args":{"command":"create-entity","data":{"body":"<!-- Starting point for a `story` artifact, seeded by `protocol artifact new story <name>`.\n     No frontmatter here on purpose: the `---` block is written by the CLI from the id, kind, status\n     and relations you gave it, and a second copy in this file would be the one that went stale.\n     Delete the italic guidance as you fill each section. -->\n\n# Story: <name>\n\n## Outcome\n\n*What is true for whom once this has shipped, in one sentence. If it names a component rather than a\nperson, it is a task — say what changes for someone.*\n\n## Context\n\n*Why this is worth doing now, and what it depends on. Link the epic or specification it comes from\nrather than restating it; the `derived_from` relation already carries the edge.*\n\n## Acceptance\n\n*The conditions under which this is done, each one something a person or a check can observe. \"Works\ncorrectly\" is not one of them.*\n\n## Out of Scope\n\n*What a reasonable reader would expect to be included and is not — the boundary that stops this\nstory quietly becoming an epic.*\n\n## Open Questions\n\n*What is still undecided, each with who decides it. A story carrying an unowned question is a story\nthat stalls without anybody noticing.*\n","status":"draft","summary":"route_ids collects operation ids, never paths, so moving a route passes check-bundle as adds_routes: 0.","title":"The contract gate refuses a renamed route path"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/contract-gate-sees-route-paths"},"payload":{"actor":"human:timo","at":1788091881704,"causation":"cmd-new-story-contract-gate-sees-route-paths","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:contract-gate-sees-route-paths@1#0~a62367731801fee6","executor":null,"recorded_at":"2026-08-30T12:11:21Z"}}
{"entity":"story","version":1,"id":"contract-gate-sees-route-paths","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:release-hardening"}]},"args":{"command":"create-relation","kind":"decomposes","source":"01MEM0000000000000110","target":"01MEM0000000000000007"},"payload":{"actor":"human:timo","at":1788091881707,"causation":"cmd-story-contract-gate-sees-route-paths-decomposes-epic-release-hardening","change":{"change":"related","relation":"decomposes","target":"epic:release-hardening"},"correlation":"protocol-artifact-relate","event_id":"story:contract-gate-sees-route-paths@1#0~4bf1c0a8456ba6f6","executor":null,"recorded_at":"2026-08-30T12:11:21Z"}}
{"entity":"story","version":1,"id":"contract-gate-sees-route-paths","revision":2,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"# Story: The contract gate refuses a renamed route path\n\n## Outcome\n\nA consumer that pinned a bundle can trust that the paths in it did not move. Today they can move\nsilently: the successor check reads operation **ids** and never the paths they are served at, so a\nreleased route can be relocated and every gate step still passes.\n\n## Context\n\nInvariant 6 says every released contract bundle directory is immutable and a wire change adds a\nsuccessor bundle. `cargo xtask check-bundle` enforces that for the bundle's bytes, and\n`check_additions` enforces that a successor drops no route:\n\n```rust\nfor dropped in previous.difference(&current) {\n    failures.push(format!(\n        \"operations.json: route {dropped} served by {predecessor} is absent; an additive \\\n         successor never drops one\"\n    ));\n}\n```\n\n(`xtask/src/bundle.rs:263-268`.) The sets it differences come from `route_ids`, which collects\nexactly one field:\n\n```rust\n.filter_map(|entry| entry.get(\"id\").and_then(Value::as_str))\n```\n\n(`xtask/src/bundle.rs:534-536`.) So the property enforced is *no operation id disappears*. A\nsuccessor that keeps every id and moves `/v1/pipe-sessions/{session_id}/attach` to\n`/v1/anything-else/{session_id}/attach` has an empty difference, reports `adds_routes: 0`, and\npasses. **The gate cannot tell a path rename from a no-op.**\n\nThis is not hypothetical: `docs/design/16-sessions-are-not-pipe-sessions.md` proposes moving that\nexact family, and had to give its aliases new ids precisely so the checker could see the change at\nall. A design that wanted to move a path quietly would simply not do that, and nothing would\nobject.\n\nFound while designing the `/v1/sessions` rename, 2026-08-30.\n\n## Acceptance\n\nA successor bundle that serves an existing operation id at a different path fails\n`cargo xtask check-bundle`, by name, and the failure says which id moved and between which paths.\n\nEvidence that satisfies it:\n\n1. A failing-first test: take `0.8.0`'s authored source, move one route's path, leave every id\n   intact, and assert `check-bundle` fails. It must pass before the change and fail after.\n2. The existing seven bundles still verify unchanged — the check is added, no frozen byte moves\n   (invariant 6).\n3. A deliberate, declared path change is still expressible, because\n   `docs/design/16-sessions-are-not-pipe-sessions.md` needs one. Whatever shape that takes — an\n   `alias_of` field, an explicit moved-from record — the check reads it rather than being switched\n   off for that version.\n\n## Out of Scope\n\nRemoving routes. That needs a non-additive compatibility kind, which\n`contracts/substrate-wire/0.8.0/schemas/bundle.json:21-22` does not have (`\"const\": \"additive-v1\"`)\nand which is its own decision.\n\n## Open\n\nWhether the same blind spot exists for other route-adjacent fields — method, status codes,\n`response_branches`. This story covers paths, because that is what was found; a sweep of what else\n`route_ids` does not read would be worth doing while the code is open.\n"},"args":{"changes":{"body":"# Story: The contract gate refuses a renamed route path\n\n## Outcome\n\nA consumer that pinned a bundle can trust that the paths in it did not move. Today they can move\nsilently: the successor check reads operation **ids** and never the paths they are served at, so a\nreleased route can be relocated and every gate step still passes.\n\n## Context\n\nInvariant 6 says every released contract bundle directory is immutable and a wire change adds a\nsuccessor bundle. `cargo xtask check-bundle` enforces that for the bundle's bytes, and\n`check_additions` enforces that a successor drops no route:\n\n```rust\nfor dropped in previous.difference(&current) {\n    failures.push(format!(\n        \"operations.json: route {dropped} served by {predecessor} is absent; an additive \\\n         successor never drops one\"\n    ));\n}\n```\n\n(`xtask/src/bundle.rs:263-268`.) The sets it differences come from `route_ids`, which collects\nexactly one field:\n\n```rust\n.filter_map(|entry| entry.get(\"id\").and_then(Value::as_str))\n```\n\n(`xtask/src/bundle.rs:534-536`.) So the property enforced is *no operation id disappears*. A\nsuccessor that keeps every id and moves `/v1/pipe-sessions/{session_id}/attach` to\n`/v1/anything-else/{session_id}/attach` has an empty difference, reports `adds_routes: 0`, and\npasses. **The gate cannot tell a path rename from a no-op.**\n\nThis is not hypothetical: `docs/design/16-sessions-are-not-pipe-sessions.md` proposes moving that\nexact family, and had to give its aliases new ids precisely so the checker could see the change at\nall. A design that wanted to move a path quietly would simply not do that, and nothing would\nobject.\n\nFound while designing the `/v1/sessions` rename, 2026-08-30.\n\n## Acceptance\n\nA successor bundle that serves an existing operation id at a different path fails\n`cargo xtask check-bundle`, by name, and the failure says which id moved and between which paths.\n\nEvidence that satisfies it:\n\n1. A failing-first test: take `0.8.0`'s authored source, move one route's path, leave every id\n   intact, and assert `check-bundle` fails. It must pass before the change and fail after.\n2. The existing seven bundles still verify unchanged — the check is added, no frozen byte moves\n   (invariant 6).\n3. A deliberate, declared path change is still expressible, because\n   `docs/design/16-sessions-are-not-pipe-sessions.md` needs one. Whatever shape that takes — an\n   `alias_of` field, an explicit moved-from record — the check reads it rather than being switched\n   off for that version.\n\n## Out of Scope\n\nRemoving routes. That needs a non-additive compatibility kind, which\n`contracts/substrate-wire/0.8.0/schemas/bundle.json:21-22` does not have (`\"const\": \"additive-v1\"`)\nand which is its own decision.\n\n## Open\n\nWhether the same blind spot exists for other route-adjacent fields — method, status codes,\n`response_branches`. This story covers paths, because that is what was found; a sweep of what else\n`route_ids` does not read would be worth doing while the code is open.\n"},"command":"update-entity","target":"01MEM0000000000000022"},"payload":{"actor":"human:timo","at":1788091881726,"causation":"cmd-protocol-artifact-body-story-contract-gate-sees-route-paths-1788091881726","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body","event_id":"story:contract-gate-sees-route-paths@2#0~593ea1281ca235bb","executor":null,"recorded_at":"2026-08-30T12:11:21Z"}}
{"entity":"story","version":1,"id":"docker-driver-entry-gate","revision":4,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"# Story: The Docker driver entry gate is proven before any Docker code\n\n## Outcome\n\nPlan 03's remaining entry criteria are proven mechanically, so a second driver tests the contract\ninstead of the contract bending around it. Plan 03 moves from *deferred* to *active* and\n`ROADMAP.md` phase 5 to *in progress* in the same change.\n\n## Context\n\n`docs/plan/03-container-driver.md` § *Entry criteria* names four. The first (host conformance) is\nmet per `STATUS.md`; the second is `story:driver-port-carries-no-host-types`, split out on\n2026-08-29 because it does not wait for phase 4. This story keeps the two that do: a\ndriver-parameterised conformance journey, and the security-design section naming root-equivalent\nDocker authorities — both of which shape the Docker driver itself, which `ROADMAP.md` holds\nbehind phase 4's exit.\n\n## Acceptance\n\nThe host driver passes the workspace/exec conformance journey through a harness that takes the\ndriver as a parameter, design 04 names root-equivalent Docker authorities, and plan 03 and\n`ROADMAP.md` phase 5 change together — not before phase 4 reads *complete*.\n\nEvidence that satisfies it:\n\n- the conformance harness takes the driver as a parameter and the host driver passes unchanged;\n- `docs/design/04-security-and-isolation.md` gains the section: daemon socket, `--privileged`,\n  host network/PID namespaces, arbitrary bind mounts — visible deployment facts, never defaults;\n- plan 03 status and `ROADMAP.md` phase 5 change in the same commit.\n\n## Design draft — 2026-08-30\n\n`docs/design/15-docker-driver-entry-gate.md`, **proposed**. Claims no ADR number.\n\n| decision | chosen | alternative's cost |\n|---|---|---|\n| socket acquisition | an explicit flag only; no autodetect, no `DOCKER_HOST` | discovery turns a `chmod` elsewhere on the host into a silent capability here |\n| root-equivalence | published as the fact `exec.host-equivalent-authority` | a driver-name check makes the driver part of the contract (invariant 4) |\n| refused options | the driver builds a closed spec and the probe must pass **with the option applied** | a denylist grows by accident and is never the enforcement |\n| sealed slots | **absent** on containers | there is no `pre_exec` — the runtime forks the child, so passing the value through the runtime API is the leak ADR 0012 closes |\n| apertures | **absent** until a spike observes the `setns` handback | asserting it repeats what design 10 refused |\n| new refusal codes | one, `exec.container-option-in-request` | more would duplicate `exec.sandbox-unavailable`, which reads facts and never a driver kind |\n\nBundle `0.9.0`, provisional — designs 13, 14 and 16 name it too.\n\nTwo findings from the draft: the story's acceptance maps plan 03 criterion 3 onto a conformance\nharness, but that is criterion 1; and `crates/substrate-daemon/tests/driver_port.rs:7` cites\n`substrate-host/src/lib.rs:171` for `pub trait Driver`, which is `:201` at `d65db79` — a\ntext-scanning test, so nothing is broken, but the citation is stale.\n"},"args":{"changes":{"body":"# Story: The Docker driver entry gate is proven before any Docker code\n\n## Outcome\n\nPlan 03's remaining entry criteria are proven mechanically, so a second driver tests the contract\ninstead of the contract bending around it. Plan 03 moves from *deferred* to *active* and\n`ROADMAP.md` phase 5 to *in progress* in the same change.\n\n## Context\n\n`docs/plan/03-container-driver.md` § *Entry criteria* names four. The first (host conformance) is\nmet per `STATUS.md`; the second is `story:driver-port-carries-no-host-types`, split out on\n2026-08-29 because it does not wait for phase 4. This story keeps the two that do: a\ndriver-parameterised conformance journey, and the security-design section naming root-equivalent\nDocker authorities — both of which shape the Docker driver itself, which `ROADMAP.md` holds\nbehind phase 4's exit.\n\n## Acceptance\n\nThe host driver passes the workspace/exec conformance journey through a harness that takes the\ndriver as a parameter, design 04 names root-equivalent Docker authorities, and plan 03 and\n`ROADMAP.md` phase 5 change together — not before phase 4 reads *complete*.\n\nEvidence that satisfies it:\n\n- the conformance harness takes the driver as a parameter and the host driver passes unchanged;\n- `docs/design/04-security-and-isolation.md` gains the section: daemon socket, `--privileged`,\n  host network/PID namespaces, arbitrary bind mounts — visible deployment facts, never defaults;\n- plan 03 status and `ROADMAP.md` phase 5 change in the same commit.\n\n## Design draft — 2026-08-30\n\n`docs/design/15-docker-driver-entry-gate.md`, **proposed**. Claims no ADR number.\n\n| decision | chosen | alternative's cost |\n|---|---|---|\n| socket acquisition | an explicit flag only; no autodetect, no `DOCKER_HOST` | discovery turns a `chmod` elsewhere on the host into a silent capability here |\n| root-equivalence | published as the fact `exec.host-equivalent-authority` | a driver-name check makes the driver part of the contract (invariant 4) |\n| refused options | the driver builds a closed spec and the probe must pass **with the option applied** | a denylist grows by accident and is never the enforcement |\n| sealed slots | **absent** on containers | there is no `pre_exec` — the runtime forks the child, so passing the value through the runtime API is the leak ADR 0012 closes |\n| apertures | **absent** until a spike observes the `setns` handback | asserting it repeats what design 10 refused |\n| new refusal codes | one, `exec.container-option-in-request` | more would duplicate `exec.sandbox-unavailable`, which reads facts and never a driver kind |\n\nBundle `0.9.0`, provisional — designs 13, 14 and 16 name it too.\n\nTwo findings from the draft: the story's acceptance maps plan 03 criterion 3 onto a conformance\nharness, but that is criterion 1; and `crates/substrate-daemon/tests/driver_port.rs:7` cites\n`substrate-host/src/lib.rs:171` for `pub trait Driver`, which is `:201` at `d65db79` — a\ntext-scanning test, so nothing is broken, but the citation is stale.\n"},"command":"update-entity","target":"01MEM0000000000000028"},"payload":{"actor":"human:timo","at":1788091894206,"causation":"cmd-protocol-artifact-body-story-docker-driver-entry-gate-1788091894206","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body","event_id":"story:docker-driver-entry-gate@4#0~5fe175c28006768d","executor":null,"recorded_at":"2026-08-30T12:11:34Z"}}
{"entity":"story","version":1,"id":"docker-driver-entry-gate","revision":4,"type":"aep.evidence.record/v1","from_state":"draft","to_state":"draft","changed":{},"args":{"command":"record-evidence","kind":"specification","reference":"docs/design/15-docker-driver-entry-gate.md","source":"design 15, proposed","target":"01MEM0000000000000028"},"payload":{"actor":"human:timo","at":1788091894000,"causation":"cmd-evidence-story-docker-driver-entry-gate-specification-1788091894221","change":{"change":"evidence","kind":"specification","reference":"docs/design/15-docker-driver-entry-gate.md","source":"design 15, proposed"},"correlation":"protocol-artifact-evidence","event_id":"story:docker-driver-entry-gate@4#0~6b880f709212e770","executor":null,"recorded_at":"2026-08-30T12:11:34Z"}}
{"entity":"epic","version":1,"id":"byte-plane-completion","revision":4,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"# Epic: Byte-plane completion\n\n## Outcome\n\n`ROADMAP.md` phase 4 reads *complete*: `README.md` § *Status* no longer lists PTY or network\nsession authority as absent, and design 05's condition for a live vendor harness — secret and\negress-adjacent capabilities — is met on the secret side.\n\n## Why Now\n\n`ROADMAP.md` orders the phases and forbids starting a later one while an earlier exit criterion is\nopen. Phase 4 is *in progress*: raw-pipe sessions, the distinct session resource, the read-only\nexecution capsule and the delegated model-free Agent lane are green\n(`docs/plan/04-direct-byte-plane.md` § *Slices A–D*). The same plan's § *Later phase-4 slices*\nnames what remains, and three of those items already have accepted designs, so they can proceed\nunder invariant 8 with an ADR each and no new design document:\n\n| capability | accepted design | fixed already |\n|---|---|---|\n| PTY session kind | `docs/design/05-streams-sessions-and-endpoints.md` § 2 | frames: input, output, resize, signal, exit, protocol-error; a PTY never substitutes for pipes; both kinds share the finite frame/byte/queue/idle bounds |\n| sealed secret slots | `docs/design/06-authentication-secrets-and-trust.md` § *V1 decisions* 3 | sealed Linux `memfd` at a declared child descriptor; only the slot→descriptor mapping in the shaped environment; acquired after all admission checks; daemon copy closed after spawn; missing proof reports the capability absent |\n| network session authority | design 05 § *V1 decisions* 3–4 | proof-bound, ≤ 60 s, single redemption, one concurrent attachment; reconnect needs a fresh authority; WebSocket over TLS; non-loopback listeners need TLS/mTLS or a trusted tunnel (`architecture/deployment-postures.md`) |\n\n## Scope\n\nSix stories now decompose this epic, each an ADR, a successor bundle, host/daemon work and delegated-lane evidence — in\nthat order, which is the order invariant 8 requires:\n\n1. `story:pty-sessions` — smallest, wholly local, exercises the successor-bundle path again.\n2. `story:sealed-secret-slots` — the capability design 05 § *Progress* names as the reason a live\n   vendor harness is refused.\n3. `story:destination-bound-egress` — the other half of the vendor-harness pair; secrets without\n   egress unlock nothing (added by the 2026-08-29 review against atlas).\n4. `story:network-session-authority` — the last exit criterion, and the one with no consumer named.\n\nEvery story adds to **the next free successor** at the time it lands; no frozen byte changes\n(invariant 6). Two landing together share a bundle and the ADRs say so. This paragraph once named\nthe numbers — PTY `0.5.0`, slots `0.6.0`, network authority `0.7.0` — and every one of them was\nwrong within a day, because the order stories land in is not the order they were listed in. What\nactually happened: `0.5.0` sealed secret slots, `0.6.0` destination-bound egress, `0.7.0` delegated\ncontext and grant attribution, `0.8.0` the aperture byte ceiling (`CHANGELOG.md:30`, `:83`, `:119`,\n`:155`). A plan that pins a successor number is a plan that is wrong as soon as anything reorders. Each successor ships its renderer and checker and joins `scripts/gate.sh`. Each\ncapability is a fact the host verifies at startup; absent verification is a named refusal and the\ncapability document omits the fact (invariants 3, 4). Nothing here opens a second spawn path.\n\n## Out of Scope\n\nReconnect (design 05 defines it as \"a fresh authority\", so it falls out of the network story), and\nthe hosted trust-envelope verifier (design 06 § 1, phase 7).\n\n## Risks\n\n- Three successor bundles in quick succession is a lot of frozen directories; batching two into one\n  bundle trades that for a larger review. Decide per story when its predecessor lands.\n- `memfd` sealing is Linux-only; the portable lane must prove the typed refusal, not skip the case.\n- Channel binding for the network authority — TLS exporter (RFC 5705) or a client key — is an ADR\n  decision; the design says only \"proof-bound\".\n\n## Done When\n\nPlan 04 § *Exit evidence* is met, `README.md` § *Status* no longer lists PTY or network session\nauthority as absent, and `ROADMAP.md` phase 4 reads *complete*.\n\n## Correction — 2026-08-30\n\nTwo claims above were false and are rewritten rather than deleted, so the drift is visible.\n\nThe bundle sequence named three numbers and got all three wrong; the paragraph now says *next free\nsuccessor* and records what actually landed.\n\nThe scope said four stories. Six decompose this epic: the original four plus\n`story:aperture-byte-ceiling` (ADR 0014, implemented) and\n`story:secret-slot-probe-observes-what-it-claims` (implemented), both filed after the original\nscope was written. Four of the six are implemented; `story:pty-sessions` and\n`story:network-session-authority` remain drafts, each now carrying a proposed design\n(`docs/design/13-pty-sessions.md`, `docs/design/14-network-session-authority.md`) and each awaiting\noperator acceptance before code.\n"},"args":{"changes":{"body":"# Epic: Byte-plane completion\n\n## Outcome\n\n`ROADMAP.md` phase 4 reads *complete*: `README.md` § *Status* no longer lists PTY or network\nsession authority as absent, and design 05's condition for a live vendor harness — secret and\negress-adjacent capabilities — is met on the secret side.\n\n## Why Now\n\n`ROADMAP.md` orders the phases and forbids starting a later one while an earlier exit criterion is\nopen. Phase 4 is *in progress*: raw-pipe sessions, the distinct session resource, the read-only\nexecution capsule and the delegated model-free Agent lane are green\n(`docs/plan/04-direct-byte-plane.md` § *Slices A–D*). The same plan's § *Later phase-4 slices*\nnames what remains, and three of those items already have accepted designs, so they can proceed\nunder invariant 8 with an ADR each and no new design document:\n\n| capability | accepted design | fixed already |\n|---|---|---|\n| PTY session kind | `docs/design/05-streams-sessions-and-endpoints.md` § 2 | frames: input, output, resize, signal, exit, protocol-error; a PTY never substitutes for pipes; both kinds share the finite frame/byte/queue/idle bounds |\n| sealed secret slots | `docs/design/06-authentication-secrets-and-trust.md` § *V1 decisions* 3 | sealed Linux `memfd` at a declared child descriptor; only the slot→descriptor mapping in the shaped environment; acquired after all admission checks; daemon copy closed after spawn; missing proof reports the capability absent |\n| network session authority | design 05 § *V1 decisions* 3–4 | proof-bound, ≤ 60 s, single redemption, one concurrent attachment; reconnect needs a fresh authority; WebSocket over TLS; non-loopback listeners need TLS/mTLS or a trusted tunnel (`architecture/deployment-postures.md`) |\n\n## Scope\n\nSix stories now decompose this epic, each an ADR, a successor bundle, host/daemon work and delegated-lane evidence — in\nthat order, which is the order invariant 8 requires:\n\n1. `story:pty-sessions` — smallest, wholly local, exercises the successor-bundle path again.\n2. `story:sealed-secret-slots` — the capability design 05 § *Progress* names as the reason a live\n   vendor harness is refused.\n3. `story:destination-bound-egress` — the other half of the vendor-harness pair; secrets without\n   egress unlock nothing (added by the 2026-08-29 review against atlas).\n4. `story:network-session-authority` — the last exit criterion, and the one with no consumer named.\n\nEvery story adds to **the next free successor** at the time it lands; no frozen byte changes\n(invariant 6). Two landing together share a bundle and the ADRs say so. This paragraph once named\nthe numbers — PTY `0.5.0`, slots `0.6.0`, network authority `0.7.0` — and every one of them was\nwrong within a day, because the order stories land in is not the order they were listed in. What\nactually happened: `0.5.0` sealed secret slots, `0.6.0` destination-bound egress, `0.7.0` delegated\ncontext and grant attribution, `0.8.0` the aperture byte ceiling (`CHANGELOG.md:30`, `:83`, `:119`,\n`:155`). A plan that pins a successor number is a plan that is wrong as soon as anything reorders. Each successor ships its renderer and checker and joins `scripts/gate.sh`. Each\ncapability is a fact the host verifies at startup; absent verification is a named refusal and the\ncapability document omits the fact (invariants 3, 4). Nothing here opens a second spawn path.\n\n## Out of Scope\n\nReconnect (design 05 defines it as \"a fresh authority\", so it falls out of the network story), and\nthe hosted trust-envelope verifier (design 06 § 1, phase 7).\n\n## Risks\n\n- Three successor bundles in quick succession is a lot of frozen directories; batching two into one\n  bundle trades that for a larger review. Decide per story when its predecessor lands.\n- `memfd` sealing is Linux-only; the portable lane must prove the typed refusal, not skip the case.\n- Channel binding for the network authority — TLS exporter (RFC 5705) or a client key — is an ADR\n  decision; the design says only \"proof-bound\".\n\n## Done When\n\nPlan 04 § *Exit evidence* is met, `README.md` § *Status* no longer lists PTY or network session\nauthority as absent, and `ROADMAP.md` phase 4 reads *complete*.\n\n## Correction — 2026-08-30\n\nTwo claims above were false and are rewritten rather than deleted, so the drift is visible.\n\nThe bundle sequence named three numbers and got all three wrong; the paragraph now says *next free\nsuccessor* and records what actually landed.\n\nThe scope said four stories. Six decompose this epic: the original four plus\n`story:aperture-byte-ceiling` (ADR 0014, implemented) and\n`story:secret-slot-probe-observes-what-it-claims` (implemented), both filed after the original\nscope was written. Four of the six are implemented; `story:pty-sessions` and\n`story:network-session-authority` remain drafts, each now carrying a proposed design\n(`docs/design/13-pty-sessions.md`, `docs/design/14-network-session-authority.md`) and each awaiting\noperator acceptance before code.\n"},"command":"update-entity","target":"01MEM0000000000000001"},"payload":{"actor":"human:timo","at":1788092453323,"causation":"cmd-protocol-artifact-body-epic-byte-plane-completion-1788092453323","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body","event_id":"epic:byte-plane-completion@4#0~27bb08a42a4dcf2e","executor":null,"recorded_at":"2026-08-30T12:20:53Z"}}
{"entity":"story","version":1,"id":"contract-bundle-oci-artifact","revision":8,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"body":"# Story: The 0.4.0 contract bundle is a signed, digest-pinned OCI artifact\n\n## Outcome\n\nA consumer pins the wire contract by OCI digest instead of copying a directory tree.\n\n## Context\n\n`docs/plan/04-direct-byte-plane.md` § *Slice C* says Agent \"consumes an exact independently\nverified copy\"; the copy is made by hand. `docs/design/07-specification-and-conformance.md`\n§ *Bundle* already fixes the packaging: `bundle.json` lists media type, byte length and digest of\nevery bundle path except itself, and the outer OCI manifest digest pins `bundle.json`. Depends on\n`story:signed-daemon-image` for the release workflow it publishes through.\n\n## Acceptance\n\n`cargo xtask package-bundle <version>` produces a deterministic OCI layout from\n`contracts/substrate-wire/<version>/` without writing into `contracts/`, two runs yield\nbyte-identical manifests, and the release workflow publishes and signs it at\n`ghcr.io/beyond10x/b10x-substrate-wire:<bundle-version>` with the digest in `CHANGELOG.md`.\n\nEvidence that satisfies it:\n\n- `xtask/src/package.rs` tests: identical manifests on two runs; a one-byte change in\n  any bundle file changes the manifest digest — written failing-first against a stub that embeds\n  a timestamp;\n- `bundle.json` matches design 07's shape and a checker asserts each digest and byte length;\n- `check-contract-bundle-0.4.0.py` and every earlier checker stay green (invariant 6: the packager\n  reads the bundle; the bundle does not learn about the packager);\n- the artifact is annotated `development`; publication does not make the bundle stable (atlas ADR\n  0019 governs contract release);\n- `bash scripts/gate.sh` exits 0.\n\n## Progress — 2026-08-29 (packager half)\n\n- `cargo xtask package-bundle <version> --out <dir>`: OCI Image Layout (`oci-layout`,\n  `index.json`, `blobs/sha256/…`); config **is** `bundle.json` verbatim, media type\n  `application/vnd.b10x.substrate-wire.bundle.v1+json`, so the manifest digest pins\n  `bundle.json` (design 07 § *Bundle*); one layer per bundle file, layer digest = the `sha256`\n  already in `bundle.json`; annotations `org.opencontainers.image.version`,\n  `dev.b10x.contract.status=development`, `ref.name` on the index entry. Refuses `--out` inside\n  `contracts/`, non-empty `--out` without `--force`, symlinks, a file set disagreeing with\n  `bundle.json`.\n- 14 tests; determinism test written failing-first\n  against a timestamp-embedding stub (`FAILED (failures=1)`), then green. Two runs on `0.4.0` →\n  `sha256:f94d15fc116587d991aab6de5628f6ee5baf872af4e7c79d2a35e2b17a8485c4`, `diff -r` empty.\n  `oras manifest fetch --oci-layout` resolves the same digest; `oras pull` round-trip matches\n  `contracts/substrate-wire/0.4.0` byte for byte.\n- `check-contract-bundle-0.4.0.py` → exit 0; `git status contracts/` → clean (invariant 6).\n- Gated by `cargo test --workspace` (`scripts/gate.sh:15`), not by a gate line of its own.\n- The `posix-tar` archive `packaging.json` declares is now the last layer (media type\n  `application/vnd.b10x.substrate-wire.bundle.tar`, title `0.4.0.tar`): ustar, directory entries\n  0755 ahead of files 0644, uid/gid 0, empty names, bytewise order, every mtime =\n  `SOURCE_DATE_EPOCH` = author seconds of the last commit touching the bundle (`1787605082`,\n  commit `4b5d411`; `--source-date-epoch` overrides, absent both → exit 2). Per-file layers and the\n  config are byte-identical to before; the manifest digest moves by design to\n  `sha256:3758e80bc39f1eb03b15c69410608c9ef1d2ba8095c7e707c6988dbb5894ab00`\n  (archive `sha256:91fb5524…`, 880,640 bytes). 21 tests; `tar -xf` of the blob reproduces\n  `contracts/substrate-wire/0.4.0` byte for byte. On a shallow CI checkout the epoch would be the\n  tip's author time — pin `--source-date-epoch` there.\n- **Open:** the publish/sign half — `release.yml`, `ghcr.io/beyond10x/b10x-substrate-wire`,\n  cosign, digest in `CHANGELOG.md` — waits on `story:signed-daemon-image`.\n\n## Correction — 2026-08-30\n\nThe two Python names this story was written against no longer exist. `ef22de0` (\"the bundle\npackager is cargo xtask package-bundle, and its Python is gone\") ported the packager and deleted\n`scripts/package-contract-bundle.py` and `scripts/test_package_contract_bundle.py`. Acceptance and\nthe progress notes above now name the Rust verb; the behaviour they assert is unchanged, and the\n`0.4.0` manifest digests recorded above were observed against the Python and have not been\nre-observed against the verb.\n\nCoverage is not lost in the move: `xtask/src/package.rs` carries 21 `#[test]`s (was 21 Python\ntests), and `cargo test --workspace` at `scripts/gate.sh:15` runs them. There is no\n`package-bundle` line in `scripts/gate.sh` and none is needed.\n\n**Still open, unchanged:** the publish/sign half — `release.yml`,\n`ghcr.io/beyond10x/b10x-substrate-wire`, cosign, digest in `CHANGELOG.md`. It waits on\n`story:signed-daemon-image`, which is a draft. This story cannot reach `implemented` until that\none is designed and built.\n\n## Unblocked — 2026-08-30\n\nThe paragraph above says this waits on `story:signed-daemon-image`, \"which is a draft\". It is\n`implemented` (closed at `9131c95`). `.github/workflows/release.yml` exists, has run, and published\n`ghcr.io/beyond10x/b10x-substrate-daemon:0.2.3` at\n`sha256:ab10158266b579d705ce8422c7d2a6e783cde950d30e100f61ca6befc4d0beda`, keyless-signed and\n`cosign verify`-ed (run 33304493276).\n\n**Nothing blocks this story.** What remains is its own second half: a bundle publish-and-sign job in\n`release.yml`, publishing `ghcr.io/beyond10x/b10x-substrate-wire:<bundle-version>` from\n`cargo xtask package-bundle`, with the digest in `CHANGELOG.md` and the artifact annotated\n`development` — a development bundle does not become a stable contract by being published (atlas\nADR 0019).\n\nOne thing the daemon release proved that this story must plan around: the workflow's final step\ncannot push to `main`, because `main` is protected and requires the `Full gate` check, which a\ndirect push can never satisfy. The daemon digest landed by pull request instead\n(`68d226f`). A bundle digest will need the same route or a different one, and the story should say\nwhich before the job is written.\n"},"args":{"changes":{"body":"# Story: The 0.4.0 contract bundle is a signed, digest-pinned OCI artifact\n\n## Outcome\n\nA consumer pins the wire contract by OCI digest instead of copying a directory tree.\n\n## Context\n\n`docs/plan/04-direct-byte-plane.md` § *Slice C* says Agent \"consumes an exact independently\nverified copy\"; the copy is made by hand. `docs/design/07-specification-and-conformance.md`\n§ *Bundle* already fixes the packaging: `bundle.json` lists media type, byte length and digest of\nevery bundle path except itself, and the outer OCI manifest digest pins `bundle.json`. Depends on\n`story:signed-daemon-image` for the release workflow it publishes through.\n\n## Acceptance\n\n`cargo xtask package-bundle <version>` produces a deterministic OCI layout from\n`contracts/substrate-wire/<version>/` without writing into `contracts/`, two runs yield\nbyte-identical manifests, and the release workflow publishes and signs it at\n`ghcr.io/beyond10x/b10x-substrate-wire:<bundle-version>` with the digest in `CHANGELOG.md`.\n\nEvidence that satisfies it:\n\n- `xtask/src/package.rs` tests: identical manifests on two runs; a one-byte change in\n  any bundle file changes the manifest digest — written failing-first against a stub that embeds\n  a timestamp;\n- `bundle.json` matches design 07's shape and a checker asserts each digest and byte length;\n- `check-contract-bundle-0.4.0.py` and every earlier checker stay green (invariant 6: the packager\n  reads the bundle; the bundle does not learn about the packager);\n- the artifact is annotated `development`; publication does not make the bundle stable (atlas ADR\n  0019 governs contract release);\n- `bash scripts/gate.sh` exits 0.\n\n## Progress — 2026-08-29 (packager half)\n\n- `cargo xtask package-bundle <version> --out <dir>`: OCI Image Layout (`oci-layout`,\n  `index.json`, `blobs/sha256/…`); config **is** `bundle.json` verbatim, media type\n  `application/vnd.b10x.substrate-wire.bundle.v1+json`, so the manifest digest pins\n  `bundle.json` (design 07 § *Bundle*); one layer per bundle file, layer digest = the `sha256`\n  already in `bundle.json`; annotations `org.opencontainers.image.version`,\n  `dev.b10x.contract.status=development`, `ref.name` on the index entry. Refuses `--out` inside\n  `contracts/`, non-empty `--out` without `--force`, symlinks, a file set disagreeing with\n  `bundle.json`.\n- 14 tests; determinism test written failing-first\n  against a timestamp-embedding stub (`FAILED (failures=1)`), then green. Two runs on `0.4.0` →\n  `sha256:f94d15fc116587d991aab6de5628f6ee5baf872af4e7c79d2a35e2b17a8485c4`, `diff -r` empty.\n  `oras manifest fetch --oci-layout` resolves the same digest; `oras pull` round-trip matches\n  `contracts/substrate-wire/0.4.0` byte for byte.\n- `check-contract-bundle-0.4.0.py` → exit 0; `git status contracts/` → clean (invariant 6).\n- Gated by `cargo test --workspace` (`scripts/gate.sh:15`), not by a gate line of its own.\n- The `posix-tar` archive `packaging.json` declares is now the last layer (media type\n  `application/vnd.b10x.substrate-wire.bundle.tar`, title `0.4.0.tar`): ustar, directory entries\n  0755 ahead of files 0644, uid/gid 0, empty names, bytewise order, every mtime =\n  `SOURCE_DATE_EPOCH` = author seconds of the last commit touching the bundle (`1787605082`,\n  commit `4b5d411`; `--source-date-epoch` overrides, absent both → exit 2). Per-file layers and the\n  config are byte-identical to before; the manifest digest moves by design to\n  `sha256:3758e80bc39f1eb03b15c69410608c9ef1d2ba8095c7e707c6988dbb5894ab00`\n  (archive `sha256:91fb5524…`, 880,640 bytes). 21 tests; `tar -xf` of the blob reproduces\n  `contracts/substrate-wire/0.4.0` byte for byte. On a shallow CI checkout the epoch would be the\n  tip's author time — pin `--source-date-epoch` there.\n- **Open:** the publish/sign half — `release.yml`, `ghcr.io/beyond10x/b10x-substrate-wire`,\n  cosign, digest in `CHANGELOG.md` — waits on `story:signed-daemon-image`.\n\n## Correction — 2026-08-30\n\nThe two Python names this story was written against no longer exist. `ef22de0` (\"the bundle\npackager is cargo xtask package-bundle, and its Python is gone\") ported the packager and deleted\n`scripts/package-contract-bundle.py` and `scripts/test_package_contract_bundle.py`. Acceptance and\nthe progress notes above now name the Rust verb; the behaviour they assert is unchanged, and the\n`0.4.0` manifest digests recorded above were observed against the Python and have not been\nre-observed against the verb.\n\nCoverage is not lost in the move: `xtask/src/package.rs` carries 21 `#[test]`s (was 21 Python\ntests), and `cargo test --workspace` at `scripts/gate.sh:15` runs them. There is no\n`package-bundle` line in `scripts/gate.sh` and none is needed.\n\n**Still open, unchanged:** the publish/sign half — `release.yml`,\n`ghcr.io/beyond10x/b10x-substrate-wire`, cosign, digest in `CHANGELOG.md`. It waits on\n`story:signed-daemon-image`, which is a draft. This story cannot reach `implemented` until that\none is designed and built.\n\n## Unblocked — 2026-08-30\n\nThe paragraph above says this waits on `story:signed-daemon-image`, \"which is a draft\". It is\n`implemented` (closed at `9131c95`). `.github/workflows/release.yml` exists, has run, and published\n`ghcr.io/beyond10x/b10x-substrate-daemon:0.2.3` at\n`sha256:ab10158266b579d705ce8422c7d2a6e783cde950d30e100f61ca6befc4d0beda`, keyless-signed and\n`cosign verify`-ed (run 33304493276).\n\n**Nothing blocks this story.** What remains is its own second half: a bundle publish-and-sign job in\n`release.yml`, publishing `ghcr.io/beyond10x/b10x-substrate-wire:<bundle-version>` from\n`cargo xtask package-bundle`, with the digest in `CHANGELOG.md` and the artifact annotated\n`development` — a development bundle does not become a stable contract by being published (atlas\nADR 0019).\n\nOne thing the daemon release proved that this story must plan around: the workflow's final step\ncannot push to `main`, because `main` is protected and requires the `Full gate` check, which a\ndirect push can never satisfy. The daemon digest landed by pull request instead\n(`68d226f`). A bundle digest will need the same route or a different one, and the story should say\nwhich before the job is written.\n"},"command":"update-entity","target":"01MEM0000000000000019"},"payload":{"actor":"human:timo","at":1788092470396,"causation":"cmd-protocol-artifact-body-story-contract-bundle-oci-artifact-1788092470396","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body","event_id":"story:contract-bundle-oci-artifact@8#0~3d864999293bc678","executor":null,"recorded_at":"2026-08-30T12:21:10Z"}}
{"entity":"story","version":1,"id":"destination-bound-egress","revision":10,"type":"aep.entity.update/v1","from_state":"implemented","to_state":"implemented","changed":{"body":"# Story: Destination-bound egress apertures are declared, verified and refused by name\n\n## Outcome\n\nA confined run reaches exactly the network destinations an operator declared for it — and, with\n`story:sealed-secret-slots`, a vendor harness (`codex`, `claude`) can run under substrate\nconfinement with its model credential and its model endpoint, which design 05 § *Progress* says is\nrefused until both capabilities exist.\n\n## Context\n\n`docs/design/04-security-and-isolation.md` § 6: egress, listening sockets and exposed endpoints\nare separate capabilities; ordinary execution defaults to **no egress**; \"an aperture is\ndeployment/operator authority\" matched to a destination. The delegated lane today proves\nnamespace no-egress and nothing else. Atlas O3/O4 want vendor harnesses driven under the same\ngovernor as the native loop; `autodev` dispatches only `codex` and `claude`\n(`atlas/ROADMAP.md`, arrow *autodev → the fleet*). A vendor harness that cannot reach its model\nis not confined, it is dead — so secrets and egress are one capability pair, and the review that\nfiled this story found the backlog held only the first half.\n\n## Acceptance\n\nA run started with a declared egress aperture reaches the declared destination and nothing else,\nthe applied aperture is an observation in the run's record, and a request for an aperture the\ndeployment did not declare is `unserved` with the aperture named — proven by the delegated lane\nwith a real listener inside and outside the aperture.\n\nEvidence that satisfies it, in order:\n\n1. **Before code**: an ADR fixing the aperture shape (host:port or name:port, protocol, whether\n   DNS resolution is inside the aperture), the operator declaration surface, and the refusal.\n2. A successor bundle adds the request field, the capability fact `network.egress.apertures`, the\n   observation, and the refusal; earlier bundle bytes unchanged.\n3. Failing-first tests: `egress_defaults_to_none`, `declared_aperture_is_reachable`,\n   `undeclared_destination_is_unreachable_and_named`,\n   `aperture_outside_operator_declaration_is_unserved`, `applied_aperture_is_observed`.\n4. The delegated lane runs the model-free fake app-server against a loopback endpoint inside the\n   aperture and proves a second endpoint outside it is unreachable.\n\n## Out of Scope\n\nPublic ingress, DNS, TLS termination and tunnels (design 05 § 4). Brokered connector artifacts.\n\n## Open Questions\n\nEnforcement mechanism inside the existing namespace no-egress posture: a veth pair plus nftables\nin the sandbox namespace, or a userspace proxy at a declared descriptor. Decides: the ADR. Default\nif nobody answers: **namespace + nftables**, because it keeps bytes out of the daemon.\n\n## Design draft — 2026-08-30\n\n`docs/design/10-destination-bound-egress.md` (proposed). Finding that overturns this story's\ndefault: option (a), namespace + nftables, needs root, and the daemon withholds `exec` when it\nruns as root (`crates/substrate-host/src/probe.rs:49-51`); bubblewrap 0.11.2 has no `--netns`, so\na prepared namespace cannot be handed in. The draft recommends (c), a per-run forwarder inside the\nsandbox netns, owing a `setns` spike on a delegated host before the ADR. `NetworkMode::Aperture`\nalready exists and is `unserved` twice (`substrate-wire/src/lib.rs:595-599`,\n`operations.rs:290-300`, `process.rs:810-815`); the only executed no-egress proof today is one\n`connect` to `1.1.1.1:53` (`crates/substrate-daemon/tests/runtime_vectors.rs:620-640`, ported from the retired\n`scripts/check-runtime-vectors.py:348-367` on 2026-08-30).\n\n## Progress — 2026-08-30, steps 2-4\n\nGate green (`bash scripts/gate.sh`, exit 0). `contracts/substrate-wire/0.6.0/` (213 files) is the\nsuccessor; `0.4.0` and `0.5.0` both still reproduce byte for byte (`diff -r` empty each), and\n`git status --short contracts/` shows only the new directory.\n\n**Mechanism (c), and it really ran here.** `strace -f -e trace=execve` counts **5 `execve` of\n`/usr/bin/bwrap`** across the 9 `egress::tests::*` — these are not vacuous passes. 40 bytes were\ncounted through the forwarder. `Sandbox::open` deliberately refuses to skip: a sandbox that reports\nno `child-pid` is `expect`-ed, not returned as absent, because silently skipping is exactly how\nthese cases would pass without running.\n\n**A third silent-failure trap, in the same class as the spike's two but not named by it.**\n`std::io::pipe()` is `O_CLOEXEC`, so the first version of the mechanism tests passed in 0.00 s\nhaving opened no sandbox at all. Fixed with `pipe2(…, 0)` and a *written* barrier byte — closing the\nwrite end only releases bwrap once every forked copy of it is gone. Recorded in\n`docs/design/10a-egress-mechanism-spike.md` § 8.\n\n**Trap 2 bites, and a test now catches it.** Injecting `child.id()` in place of the `--info-fd`\n`child-pid` fails `the_mechanism_is_proven_in_a_throwaway_sandbox`. Trap 1 did **not** reproduce for\nthe throwaway argv — bwrap nested no second user namespace there — so `NS_GET_USERNS` is kept as the\nsuperset, not as a demonstrated fix. Say so rather than claim a fix that was never exercised.\n\n**`xtask/src/render.rs` can never be edited again without breaking `0.5.0`.** A bundle's\n`generator.digest` is the sha256 of that file (`xtask/src/render.rs:308-312`), so a successor cannot\nadd a `{\"$wire\": …}` binding there. `MAX_EGRESS_APERTURES` is bound from `xtask/src/bundle.rs`\ninstead, which no bundle hashes. Recorded in `AGENTS.md` § *The gate*.\n\n**The story and the ADR disagree on the fact name; the ADR won.** This story said\n`network.egress.apertures`; ADR 0013 § *Decision* and design 10 § 6 say `exec.egress-apertures`, and\nno fact name in `contracts/…/schemas/capability.json` carries two dots. Shipped as\n`exec.egress-apertures`.\n\n**Test brittleness found and fixed by the orchestrator, not the implementer.**\n`egress_defaults_to_none` pinned the exact errno — `Refused` for a host listener, `Unreachable` for a\npublic address. Under `strace -f` this host answers `Unreachable` where it otherwise answers\n`Refused`, and the test went red. Both outcomes prove the same guarantee, so the assertions now hold\n*did not reach* and report the observed variant. A test that pins the variant goes red on a\ndifferent kernel — including CI's — for something that is not a regression.\n\n**Defaults taken.**\n\n- Per-run read-only CA snapshot behind `--ca-bundle`. Unset means no anchor, so verification fails\n  inside the sandbox: **absent, not unverified** (invariant 3).\n- The forwarder listens on the *declared* port, so the generated `/etc/hosts` makes the operator's\n  own URL work unchanged.\n- `network: \"aperture\"` with no name keeps the frozen `exec.network-unserved`.\n- `exec.aperture-byte-limit` (design 10 § 5 row 5) **deferred**: bytes are counted and observed, the\n  ceiling is not enforced. That is the one row of design 10 § 5 this change does not deliver.\n\n## The delegated lane does run here — 2026-08-30\n\nThe implementer concluded it could not: `/sys/fs/cgroup/user.slice/user-1000.slice/session-3.scope`\nis root-owned and `mkdir` gives `Permission denied`, so `check_confined_apertures` and\n`DELEGATED_CASES = 42` reported themselves **absent** (invariant 3). That conclusion was about one\ncgroup path, not about the host. Asking systemd for a delegated scope needs no privilege:\n\n    $ bash scripts/delegated-lane.sh\n    delegated-lane: root /sys/fs/cgroup/…/app.slice/run-….scope, controllers cpu memory pids\n    runtime clean-room: 42 HTTP cases, startup refusal, and dual-daemon refusal passed (delegated lane)\n    test result: ok. 1 passed; 0 failed\n\nThe script wraps `systemd-run --user -p Delegate=yes --scope`, moves itself into a child group so\nthe delegation root stays process-free, and sets `SUBSTRATE_VECTORS_CGROUP_ROOT`. It is checked in\nprecisely so nobody reaches that conclusion again: **an absent lane and a green one read the same\nway if you only look at `cargo test`.**\n\n**Acceptance step 4 is therefore executed, not written-but-unrun.** `check_confined_apertures`\n(`crates/substrate-daemon/tests/runtime_vectors.rs:1656`) drives the model-free app-server on a\nloopback endpoint inside the aperture and a second endpoint outside it, and it is one of the 42.\nPortable lane 29/29 separately.\n\n**Deferred and not covered by this story's acceptance:** `exec.aperture-byte-limit`\n(design 10 § 5 row 5). Bytes are counted and observed; the ceiling is not enforced. That is the one\nrow of design 10 § 5 this change does not deliver, and it wants its own story.\n\n**Delivered — 2026-08-30.** That story is `story:aperture-byte-ceiling`: accepted as ADR 0014,\nimplemented, merged at `a7c040a` with contract bundle `0.8.0`. `exec.aperture-byte-limit` is now a\nrefusal a run can actually receive.\n"},"args":{"changes":{"body":"# Story: Destination-bound egress apertures are declared, verified and refused by name\n\n## Outcome\n\nA confined run reaches exactly the network destinations an operator declared for it — and, with\n`story:sealed-secret-slots`, a vendor harness (`codex`, `claude`) can run under substrate\nconfinement with its model credential and its model endpoint, which design 05 § *Progress* says is\nrefused until both capabilities exist.\n\n## Context\n\n`docs/design/04-security-and-isolation.md` § 6: egress, listening sockets and exposed endpoints\nare separate capabilities; ordinary execution defaults to **no egress**; \"an aperture is\ndeployment/operator authority\" matched to a destination. The delegated lane today proves\nnamespace no-egress and nothing else. Atlas O3/O4 want vendor harnesses driven under the same\ngovernor as the native loop; `autodev` dispatches only `codex` and `claude`\n(`atlas/ROADMAP.md`, arrow *autodev → the fleet*). A vendor harness that cannot reach its model\nis not confined, it is dead — so secrets and egress are one capability pair, and the review that\nfiled this story found the backlog held only the first half.\n\n## Acceptance\n\nA run started with a declared egress aperture reaches the declared destination and nothing else,\nthe applied aperture is an observation in the run's record, and a request for an aperture the\ndeployment did not declare is `unserved` with the aperture named — proven by the delegated lane\nwith a real listener inside and outside the aperture.\n\nEvidence that satisfies it, in order:\n\n1. **Before code**: an ADR fixing the aperture shape (host:port or name:port, protocol, whether\n   DNS resolution is inside the aperture), the operator declaration surface, and the refusal.\n2. A successor bundle adds the request field, the capability fact `network.egress.apertures`, the\n   observation, and the refusal; earlier bundle bytes unchanged.\n3. Failing-first tests: `egress_defaults_to_none`, `declared_aperture_is_reachable`,\n   `undeclared_destination_is_unreachable_and_named`,\n   `aperture_outside_operator_declaration_is_unserved`, `applied_aperture_is_observed`.\n4. The delegated lane runs the model-free fake app-server against a loopback endpoint inside the\n   aperture and proves a second endpoint outside it is unreachable.\n\n## Out of Scope\n\nPublic ingress, DNS, TLS termination and tunnels (design 05 § 4). Brokered connector artifacts.\n\n## Open Questions\n\nEnforcement mechanism inside the existing namespace no-egress posture: a veth pair plus nftables\nin the sandbox namespace, or a userspace proxy at a declared descriptor. Decides: the ADR. Default\nif nobody answers: **namespace + nftables**, because it keeps bytes out of the daemon.\n\n## Design draft — 2026-08-30\n\n`docs/design/10-destination-bound-egress.md` (proposed). Finding that overturns this story's\ndefault: option (a), namespace + nftables, needs root, and the daemon withholds `exec` when it\nruns as root (`crates/substrate-host/src/probe.rs:49-51`); bubblewrap 0.11.2 has no `--netns`, so\na prepared namespace cannot be handed in. The draft recommends (c), a per-run forwarder inside the\nsandbox netns, owing a `setns` spike on a delegated host before the ADR. `NetworkMode::Aperture`\nalready exists and is `unserved` twice (`substrate-wire/src/lib.rs:595-599`,\n`operations.rs:290-300`, `process.rs:810-815`); the only executed no-egress proof today is one\n`connect` to `1.1.1.1:53` (`crates/substrate-daemon/tests/runtime_vectors.rs:620-640`, ported from the retired\n`scripts/check-runtime-vectors.py:348-367` on 2026-08-30).\n\n## Progress — 2026-08-30, steps 2-4\n\nGate green (`bash scripts/gate.sh`, exit 0). `contracts/substrate-wire/0.6.0/` (213 files) is the\nsuccessor; `0.4.0` and `0.5.0` both still reproduce byte for byte (`diff -r` empty each), and\n`git status --short contracts/` shows only the new directory.\n\n**Mechanism (c), and it really ran here.** `strace -f -e trace=execve` counts **5 `execve` of\n`/usr/bin/bwrap`** across the 9 `egress::tests::*` — these are not vacuous passes. 40 bytes were\ncounted through the forwarder. `Sandbox::open` deliberately refuses to skip: a sandbox that reports\nno `child-pid` is `expect`-ed, not returned as absent, because silently skipping is exactly how\nthese cases would pass without running.\n\n**A third silent-failure trap, in the same class as the spike's two but not named by it.**\n`std::io::pipe()` is `O_CLOEXEC`, so the first version of the mechanism tests passed in 0.00 s\nhaving opened no sandbox at all. Fixed with `pipe2(…, 0)` and a *written* barrier byte — closing the\nwrite end only releases bwrap once every forked copy of it is gone. Recorded in\n`docs/design/10a-egress-mechanism-spike.md` § 8.\n\n**Trap 2 bites, and a test now catches it.** Injecting `child.id()` in place of the `--info-fd`\n`child-pid` fails `the_mechanism_is_proven_in_a_throwaway_sandbox`. Trap 1 did **not** reproduce for\nthe throwaway argv — bwrap nested no second user namespace there — so `NS_GET_USERNS` is kept as the\nsuperset, not as a demonstrated fix. Say so rather than claim a fix that was never exercised.\n\n**`xtask/src/render.rs` can never be edited again without breaking `0.5.0`.** A bundle's\n`generator.digest` is the sha256 of that file (`xtask/src/render.rs:308-312`), so a successor cannot\nadd a `{\"$wire\": …}` binding there. `MAX_EGRESS_APERTURES` is bound from `xtask/src/bundle.rs`\ninstead, which no bundle hashes. Recorded in `AGENTS.md` § *The gate*.\n\n**The story and the ADR disagree on the fact name; the ADR won.** This story said\n`network.egress.apertures`; ADR 0013 § *Decision* and design 10 § 6 say `exec.egress-apertures`, and\nno fact name in `contracts/…/schemas/capability.json` carries two dots. Shipped as\n`exec.egress-apertures`.\n\n**Test brittleness found and fixed by the orchestrator, not the implementer.**\n`egress_defaults_to_none` pinned the exact errno — `Refused` for a host listener, `Unreachable` for a\npublic address. Under `strace -f` this host answers `Unreachable` where it otherwise answers\n`Refused`, and the test went red. Both outcomes prove the same guarantee, so the assertions now hold\n*did not reach* and report the observed variant. A test that pins the variant goes red on a\ndifferent kernel — including CI's — for something that is not a regression.\n\n**Defaults taken.**\n\n- Per-run read-only CA snapshot behind `--ca-bundle`. Unset means no anchor, so verification fails\n  inside the sandbox: **absent, not unverified** (invariant 3).\n- The forwarder listens on the *declared* port, so the generated `/etc/hosts` makes the operator's\n  own URL work unchanged.\n- `network: \"aperture\"` with no name keeps the frozen `exec.network-unserved`.\n- `exec.aperture-byte-limit` (design 10 § 5 row 5) **deferred**: bytes are counted and observed, the\n  ceiling is not enforced. That is the one row of design 10 § 5 this change does not deliver.\n\n## The delegated lane does run here — 2026-08-30\n\nThe implementer concluded it could not: `/sys/fs/cgroup/user.slice/user-1000.slice/session-3.scope`\nis root-owned and `mkdir` gives `Permission denied`, so `check_confined_apertures` and\n`DELEGATED_CASES = 42` reported themselves **absent** (invariant 3). That conclusion was about one\ncgroup path, not about the host. Asking systemd for a delegated scope needs no privilege:\n\n    $ bash scripts/delegated-lane.sh\n    delegated-lane: root /sys/fs/cgroup/…/app.slice/run-….scope, controllers cpu memory pids\n    runtime clean-room: 42 HTTP cases, startup refusal, and dual-daemon refusal passed (delegated lane)\n    test result: ok. 1 passed; 0 failed\n\nThe script wraps `systemd-run --user -p Delegate=yes --scope`, moves itself into a child group so\nthe delegation root stays process-free, and sets `SUBSTRATE_VECTORS_CGROUP_ROOT`. It is checked in\nprecisely so nobody reaches that conclusion again: **an absent lane and a green one read the same\nway if you only look at `cargo test`.**\n\n**Acceptance step 4 is therefore executed, not written-but-unrun.** `check_confined_apertures`\n(`crates/substrate-daemon/tests/runtime_vectors.rs:1656`) drives the model-free app-server on a\nloopback endpoint inside the aperture and a second endpoint outside it, and it is one of the 42.\nPortable lane 29/29 separately.\n\n**Deferred and not covered by this story's acceptance:** `exec.aperture-byte-limit`\n(design 10 § 5 row 5). Bytes are counted and observed; the ceiling is not enforced. That is the one\nrow of design 10 § 5 this change does not deliver, and it wants its own story.\n\n**Delivered — 2026-08-30.** That story is `story:aperture-byte-ceiling`: accepted as ADR 0014,\nimplemented, merged at `a7c040a` with contract bundle `0.8.0`. `exec.aperture-byte-limit` is now a\nrefusal a run can actually receive.\n"},"command":"update-entity","target":"01MEM0000000000000025"},"payload":{"actor":"human:timo","at":1788092470431,"causation":"cmd-protocol-artifact-body-story-destination-bound-egress-1788092470431","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body","event_id":"story:destination-bound-egress@10#0~3636b80781c7fd0f","executor":null,"recorded_at":"2026-08-30T12:21:10Z"}}
{"entity":"epic","version":1,"id":"release-hardening","revision":3,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"# Epic: Release hardening\n\n## Outcome\n\nWhat the repository says about itself is what a fresh clone, a fresh runner or a consumer can\nverify: a pull request cannot show green without `scripts/gate.sh`; local and CI clippy are the\nsame compiler; `STATUS.md` describes the standalone repository; a tag yields a signed daemon image\nand a signed contract-bundle artifact whose digests are in `CHANGELOG.md`.\n\n## Why Now\n\nSubstrate went public and cut `0.2.1` on 2026-08-29 (`CHANGELOG.md`). Its working agreement calls\nthe full gate \"the bar for `main`\" (`AGENTS.md` § *The gate*), but:\n\n- `.github/workflows/` holds one workflow, `pages.yml`, which builds the website. Nothing runs the\n  gate on a push or a pull request.\n- No `rust-toolchain.toml` exists; `AGENTS.md:116-119` documents the drift (\"a newer clippy can\n  fail a commit that passed locally\") and asks for `rustup update` instead of removing the cause.\n- `Dockerfile` builds a distroless `substrate-daemon`; nothing publishes, signs or digest-pins it.\n  `README.md` § *Status*: \"stable publication: **not done**\".\n- Agent consumes the 0.4.0 bundle as a hand-copied tree (`docs/plan/04-direct-byte-plane.md` §\n  *Slice C*); design 07 § *Bundle* already fixes the OCI shape and nothing implements it.\n- Two `AGENTS.md` facts contradict the scripts they describe (`AGENTS.md:112` vs\n  `scripts/gate.sh:20-23`; `AGENTS.md:172` vs `scripts/bot-token.sh:8`), and `STATUS.md` is\n  observed 2026-08-17, before extraction, still naming the monorepo as the source.\n\n## Scope\n\nSix stories, worked in this order because each makes the next checkable:\n\n1. `story:agents-md-matches-the-scripts` — docs only.\n2. `story:ci-runs-the-full-gate` — `gate.yml`, SHA-pinned actions, `contents: read`.\n3. `story:pinned-rust-toolchain` — `rust-toolchain.toml` = `1.97` = `Cargo.toml` `rust-version` =\n   `Dockerfile` builder tag; one commit per bump.\n4. `story:status-md-re-observed` — from fresh gate output.\n5. `story:signed-daemon-image` — `release.yml` on a bare-version tag, GHCR, cosign keyless.\n6. `story:contract-bundle-oci-artifact` — deterministic OCI layout, published beside the image.\n\n## Out of Scope\n\nAny route, schema, wire identifier, capability or frozen bundle byte. The development bundles stay\ndevelopment bundles; a stable-contract decision is its own ADR under atlas ADR 0019.\nBit-for-bit reproducible binaries: digests are recorded, reproduction is a later milestone.\n\n## Risks\n\n- `packages: write` and `id-token: write` widen a workflow's authority; scope them to the release\n  job only.\n- The delegated lane needs bubblewrap and cgroup delegation and cannot run on a hosted GitHub\n  runner; either a self-hosted runner joins, or CI asserts the portable lane and the delegated\n  lane stays a local pre-release step. The CI story records which.\n- Keyless signing depends on Sigstore availability; a release fails closed, never publishes\n  unsigned.\n\n## Done When\n\nFrom a fresh clone: `AGENTS.md` matches the scripts; a PR with a formatting error goes red; clippy\nruns on the pinned toolchain; `STATUS.md` names the standalone repository and the current tag; a\ntag `0.x.y` yields a signed image and a signed bundle artifact whose digests are in `CHANGELOG.md`.\n\n## Correction — 2026-08-30\n\nScope above says \"Six stories\". **Eight** decompose this epic: the six named plus\n`story:tooling-moves-to-cargo-xtask` (implemented — the gate's Python moved to `cargo xtask`) and\n`story:contract-gate-sees-route-paths` (draft — the successor check reads operation ids and never\nthe paths they are served at, so a route can move silently).\n\nSix of the eight are implemented. `story:contract-bundle-oci-artifact` is active and unblocked;\n`story:contract-gate-sees-route-paths` is a draft filed today.\n"},"args":{"changes":{"body":"# Epic: Release hardening\n\n## Outcome\n\nWhat the repository says about itself is what a fresh clone, a fresh runner or a consumer can\nverify: a pull request cannot show green without `scripts/gate.sh`; local and CI clippy are the\nsame compiler; `STATUS.md` describes the standalone repository; a tag yields a signed daemon image\nand a signed contract-bundle artifact whose digests are in `CHANGELOG.md`.\n\n## Why Now\n\nSubstrate went public and cut `0.2.1` on 2026-08-29 (`CHANGELOG.md`). Its working agreement calls\nthe full gate \"the bar for `main`\" (`AGENTS.md` § *The gate*), but:\n\n- `.github/workflows/` holds one workflow, `pages.yml`, which builds the website. Nothing runs the\n  gate on a push or a pull request.\n- No `rust-toolchain.toml` exists; `AGENTS.md:116-119` documents the drift (\"a newer clippy can\n  fail a commit that passed locally\") and asks for `rustup update` instead of removing the cause.\n- `Dockerfile` builds a distroless `substrate-daemon`; nothing publishes, signs or digest-pins it.\n  `README.md` § *Status*: \"stable publication: **not done**\".\n- Agent consumes the 0.4.0 bundle as a hand-copied tree (`docs/plan/04-direct-byte-plane.md` §\n  *Slice C*); design 07 § *Bundle* already fixes the OCI shape and nothing implements it.\n- Two `AGENTS.md` facts contradict the scripts they describe (`AGENTS.md:112` vs\n  `scripts/gate.sh:20-23`; `AGENTS.md:172` vs `scripts/bot-token.sh:8`), and `STATUS.md` is\n  observed 2026-08-17, before extraction, still naming the monorepo as the source.\n\n## Scope\n\nSix stories, worked in this order because each makes the next checkable:\n\n1. `story:agents-md-matches-the-scripts` — docs only.\n2. `story:ci-runs-the-full-gate` — `gate.yml`, SHA-pinned actions, `contents: read`.\n3. `story:pinned-rust-toolchain` — `rust-toolchain.toml` = `1.97` = `Cargo.toml` `rust-version` =\n   `Dockerfile` builder tag; one commit per bump.\n4. `story:status-md-re-observed` — from fresh gate output.\n5. `story:signed-daemon-image` — `release.yml` on a bare-version tag, GHCR, cosign keyless.\n6. `story:contract-bundle-oci-artifact` — deterministic OCI layout, published beside the image.\n\n## Out of Scope\n\nAny route, schema, wire identifier, capability or frozen bundle byte. The development bundles stay\ndevelopment bundles; a stable-contract decision is its own ADR under atlas ADR 0019.\nBit-for-bit reproducible binaries: digests are recorded, reproduction is a later milestone.\n\n## Risks\n\n- `packages: write` and `id-token: write` widen a workflow's authority; scope them to the release\n  job only.\n- The delegated lane needs bubblewrap and cgroup delegation and cannot run on a hosted GitHub\n  runner; either a self-hosted runner joins, or CI asserts the portable lane and the delegated\n  lane stays a local pre-release step. The CI story records which.\n- Keyless signing depends on Sigstore availability; a release fails closed, never publishes\n  unsigned.\n\n## Done When\n\nFrom a fresh clone: `AGENTS.md` matches the scripts; a PR with a formatting error goes red; clippy\nruns on the pinned toolchain; `STATUS.md` names the standalone repository and the current tag; a\ntag `0.x.y` yields a signed image and a signed bundle artifact whose digests are in `CHANGELOG.md`.\n\n## Correction — 2026-08-30\n\nScope above says \"Six stories\". **Eight** decompose this epic: the six named plus\n`story:tooling-moves-to-cargo-xtask` (implemented — the gate's Python moved to `cargo xtask`) and\n`story:contract-gate-sees-route-paths` (draft — the successor check reads operation ids and never\nthe paths they are served at, so a route can move silently).\n\nSix of the eight are implemented. `story:contract-bundle-oci-artifact` is active and unblocked;\n`story:contract-gate-sees-route-paths` is a draft filed today.\n"},"command":"update-entity","target":"01MEM0000000000000007"},"payload":{"actor":"human:timo","at":1788092482470,"causation":"cmd-protocol-artifact-body-epic-release-hardening-1788092482470","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body","event_id":"epic:release-hardening@3#0~975ffadadd5a41a0","executor":null,"recorded_at":"2026-08-30T12:21:22Z"}}
{"entity":"epic","version":1,"id":"container-driver-entry","revision":3,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"# Epic: Container driver entry\n\n## Outcome\n\nPlan 03's entry criteria are proven mechanically before any Docker code exists, so a second driver\ntests the contract instead of the contract bending around it. Plan 03 moves from *deferred* to\n*active* and `ROADMAP.md` phase 5 to *in progress* in the same change.\n\n## Why Now\n\n`docs/plan/03-container-driver.md` is *deferred until host conformance* and names four entry\ncriteria. The first — the minimum host slice passes public-wire and driver conformance — is met\n(`STATUS.md`, phase 3 complete). The other three are claims nobody has checked: driver ports\ncontain no host library types; capability predicates and applied-enforcement observations are\nstable; the security design states which Docker authorities are root-equivalent.\n\n`ROADMAP.md` forbids starting phase 5 while a phase-4 exit criterion is open, so this epic holds\none story and depends on `epic:byte-plane-completion`. It exists now so that the entry gate is\nwritten down before anyone writes a `docker` module.\n\n## Scope\n\n`story:docker-driver-entry-gate`: a structural test that no `substrate-host` type crosses the\ndriver port; the workspace/exec conformance journey as a driver-parameterised harness the host\ndriver passes unchanged; the design 04 section naming root-equivalent Docker authorities.\n\n## Out of Scope\n\nThe Docker driver itself, image pull/inspection, workload lifecycle — plan 03 § *Planned proof*,\nwhich starts once this epic is implemented.\n\n## Risks\n\nThe structural test's shape: a compile-time trait-bound check, a `cargo` dependency-graph\nassertion, or a source scan. Prefer the one that fails on the first offending `use`.\n\n## Done When\n\nThe story is implemented, and plan 03's status and `ROADMAP.md` phase 5 change together.\n\n## Correction — 2026-08-30\n\nThe body above says this epic holds one story and is done when that story is implemented. The\n2026-08-29 split gave it **two**: `story:driver-port-carries-no-host-types`, which is already\nimplemented, and `story:docker-driver-entry-gate`, which is a draft now carrying a proposed design\n(`docs/design/15-docker-driver-entry-gate.md`).\n\n\"Done when the story is implemented\" therefore reads as satisfied when it is not. This epic is done\nwhen **both** are, and the Docker gate is the one that remains — a gate stated before any Docker\ncode exists, whose design says sealed secret slots and egress apertures are **absent** on a\ncontainer driver rather than weakened.\n"},"args":{"changes":{"body":"# Epic: Container driver entry\n\n## Outcome\n\nPlan 03's entry criteria are proven mechanically before any Docker code exists, so a second driver\ntests the contract instead of the contract bending around it. Plan 03 moves from *deferred* to\n*active* and `ROADMAP.md` phase 5 to *in progress* in the same change.\n\n## Why Now\n\n`docs/plan/03-container-driver.md` is *deferred until host conformance* and names four entry\ncriteria. The first — the minimum host slice passes public-wire and driver conformance — is met\n(`STATUS.md`, phase 3 complete). The other three are claims nobody has checked: driver ports\ncontain no host library types; capability predicates and applied-enforcement observations are\nstable; the security design states which Docker authorities are root-equivalent.\n\n`ROADMAP.md` forbids starting phase 5 while a phase-4 exit criterion is open, so this epic holds\none story and depends on `epic:byte-plane-completion`. It exists now so that the entry gate is\nwritten down before anyone writes a `docker` module.\n\n## Scope\n\n`story:docker-driver-entry-gate`: a structural test that no `substrate-host` type crosses the\ndriver port; the workspace/exec conformance journey as a driver-parameterised harness the host\ndriver passes unchanged; the design 04 section naming root-equivalent Docker authorities.\n\n## Out of Scope\n\nThe Docker driver itself, image pull/inspection, workload lifecycle — plan 03 § *Planned proof*,\nwhich starts once this epic is implemented.\n\n## Risks\n\nThe structural test's shape: a compile-time trait-bound check, a `cargo` dependency-graph\nassertion, or a source scan. Prefer the one that fails on the first offending `use`.\n\n## Done When\n\nThe story is implemented, and plan 03's status and `ROADMAP.md` phase 5 change together.\n\n## Correction — 2026-08-30\n\nThe body above says this epic holds one story and is done when that story is implemented. The\n2026-08-29 split gave it **two**: `story:driver-port-carries-no-host-types`, which is already\nimplemented, and `story:docker-driver-entry-gate`, which is a draft now carrying a proposed design\n(`docs/design/15-docker-driver-entry-gate.md`).\n\n\"Done when the story is implemented\" therefore reads as satisfied when it is not. This epic is done\nwhen **both** are, and the Docker gate is the one that remains — a gate stated before any Docker\ncode exists, whose design says sealed secret slots and egress apertures are **absent** on a\ncontainer driver rather than weakened.\n"},"command":"update-entity","target":"01MEM0000000000000004"},"payload":{"actor":"human:timo","at":1788092482487,"causation":"cmd-protocol-artifact-body-epic-container-driver-entry-1788092482487","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body","event_id":"epic:container-driver-entry@3#0~8d243f5d12049382","executor":null,"recorded_at":"2026-08-30T12:21:22Z"}}
{"entity":"story","version":1,"id":"pty-sessions","revision":6,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"# Story: PTY sessions are a distinct session kind with resize\n\n## Outcome\n\nA human can attach a terminal to a confined process, resize it, and get the same lease, single\nattachment, bounded-output and whole-tree cleanup guarantees the raw-pipe session gives.\n\n## Context\n\n`docs/design/05-streams-sessions-and-endpoints.md` § 2 fixes the `pty` kind and its frames\n(input, output, resize, signal, exit, protocol-error) and says a PTY is never substituted for\npipes. `README.md` § *Status* lists PTY as **absent**; `docs/plan/04-direct-byte-plane.md`\n§ *Later phase-4 slices* lists it as remaining. Invariant 8 puts the ADR before code; invariant 6\nputs the wire change in a successor bundle.\n\n## Acceptance\n\nThe delegated lane runs an interactive shell through a `pty` session, echoes bytes, applies a\nresize the child observes, and exits on hangup — while the portable lane proves the typed\n`unserved` and every `0.4.0` byte is unchanged.\n\nEvidence that satisfies it, in order:\n\n1. A design document in `docs/design/`, proposed, fixing kind, frame set, resize bounds, never a\n   substitute for pipes, and the named refusal when the host cannot allocate a pty. It claims no\n   ADR number: `adr/` admits `accepted` and `superseded` only (`xtask/src/adrs.rs:12`), so the\n   number is assigned at acceptance.\n2. The next free successor bundle under `contracts/substrate-wire/` adds the `pty` kind and\n   `resize` frame; compatibility block names its predecessor with exact\n   `adds_routes`/`preserves_routes`; authored source under `xtask/bundle-source/<version>/` and\n   `cargo xtask check-bundle <version>` in `scripts/gate.sh`.\n3. `crates/substrate-host`: pty opened with `nix::pty::openpty` inside the existing\n   bubblewrap/cgroup/cleared-environment path. Failing-first tests:\n   `pty_session_refused_without_confinement`, `pty_resize_is_applied_and_observed` (child reads\n   `TIOCGWINSZ`), `pty_session_kills_tree_on_attachment_loss`.\n4. Daemon: the single-attachment Unix-WebSocket route family serves `pty`; `resize` outside the\n   declared bounds is a protocol error.\n5. Capability fact `sessions.pty` appears only after startup verified pty allocation (invariant 4).\n6. `STATUS.md`, `README.md` § *Status* and plan 04 § *Later phase-4 slices* updated in the same\n   change.\n\n## Open Questions\n\nBundle number: `0.5.0` if this lands first in the epic; the ADR states the actual number.\n\n## Correction — 2026-08-30\n\nThree claims in the evidence list above had gone stale and are rewritten, not deleted, so the\nchange is visible:\n\n| Claimed | Actually |\n|---|---|\n| `adr/0011-pty-sessions-are-a-distinct-session-kind.md` | `0011` is `adr/0011-delegated-context-and-grant-attribution.md`; the number was taken while this story sat in draft |\n| `contracts/substrate-wire/0.5.0/` | `0.5.0` is sealed secret slots and `0.6.0` is destination-bound egress; both are frozen (invariant 6) |\n| `render-contract-bundle-0.5.0.py`, `check-contract-bundle-0.5.0.py` | the Python renderer/checker pairs stop at `0.4.0`; from `0.5.0` on the check is `cargo xtask check-bundle <version>` (`scripts/gate.sh:24-28`) |\n\nThe story pins no numbers now, so it cannot go stale the same way again.\n\n## Scope\n\nDerived 2026-08-30 by `story-scoper`. Every line is **cited** or **inferred**.\n\n- **Primary surface:** `crates/substrate-host` and the `crates/substrate-daemon` session surface —\n  cited, evidence items 3 and 4 assign work to each by name.\n- **Files, inferred:** `crates/substrate-host/src/process.rs:260` (`start_pipe`, the analogue a pty\n  allocation sits beside); `crates/substrate-wire/src/lib.rs:1227` (`SessionMode`);\n  `crates/substrate-daemon/src/app/sessions.rs` and `app/routes.rs:69-85`;\n  `crates/substrate-store/src/sessions.rs` (ADR 0008 durable identity).\n- **Files, cited:** `scripts/gate.sh:24-28`, `xtask/src/adrs.rs:12`.\n- **Files, inferred:** `Cargo.toml:37` — `nix` is a workspace dependency at features\n  `[\"fs\", \"user\"]`; `nix::pty::openpty` needs `term`, and `substrate-host/Cargo.toml` does not\n  depend on `nix` at all.\n- **Symbols, cited:** `SessionMode`, `PipeSessionStartInput`, `PipeClientFrame`, `PipeServerFrame`,\n  `PipeSessionCapabilities`, `PipeSessionLimits` — all declared `crates/substrate-wire/src/lib.rs:1181-1310`.\n- **Also likely:** `contracts/substrate-wire/0.8.0/` and `xtask/bundle-source/0.8.0/` — inferred;\n  `0.7.0` is the frontier, one past what this story's own correction records.\n- **Confidence:** **high for where the work lands** — every cited path resolved in the tree — but no\n  design document and no ADR exist, so invariant 8 (`AGENTS.md:62-64`) blocks the code half.\n- **Would collide with:** any unit touching the `substrate-wire` session types, the daemon\n  `pipe-sessions` route family, `substrate-host` process spawning, `substrate-store` session\n  persistence, or the next contract bundle number. Broad by construction, not by widening.\n\n### Two contradictions found while scoping\n\n**1. `kind` or `mode`?** This story says a `pty` **kind**; design 05 § 2 and `adr/0007:22` say\n**mode**. The code has both, on different axes: `SessionKind` is the *resource* axis and holds one\nvariant `Session` (`crates/substrate-wire/src/lib.rs:1189-1192`), while `SessionMode` holds one\nvariant `Pipes` (`:1194-1198`). Which enum grows is unresolved, and it decides the wire shape.\n\n**2. This story asks for more than invariant 8 requires, and its parent says so.** Evidence item 1\ndemands a new design document. `epic:byte-plane-completion:29` says these proceed \"under invariant 8\nwith an ADR each and **no new design document**\", because design 05 § 2 already fixes the frames.\n`AGENTS.md:62-64` reads \"a design document **or** an ADR **before code**\" — so the epic's reading is\nthe admissible one and this story is over-specifying its own gate. Resolve before dispatching work.\n\n## Design draft — 2026-08-30\n\n`docs/design/13-pty-sessions.md`, **proposed**. Claims no ADR number: `adr/` admits `accepted` and\n`superseded` only (`xtask/src/adrs.rs:12`), so the number is assigned at acceptance.\n\nBoth contradictions this story carried are now decided:\n\n- **`SessionMode` grows a `Pty` variant**, not `SessionKind`. `SessionKind` is the *resource* axis\n  and mirrors `ExecKind`; growing it would fork the single `ses_…`/`ex_…` resource ADR 0008 built.\n- **The route family is reused**, `/v1/pipe-sessions/*` with a `mode` field defaulting to `pipes`,\n  so `adds_routes: 0`. A new family means seven duplicate mode-neutral operation ids and a `GET`\n  that would have to claim `absent: true` for a live session; renaming the path is a coordinated\n  migration with an ADR in atlas and removes seven routes, which is not `additive-v1`.\n\nAlso fixed: `sessions.pty` becomes a probe-verified `CapabilityFacts` member; pty unavailable is\n`unserved`/`session.pty-unserved` (501) and allocation failure `exhausted`/`session.pty-exhausted`\n(429, retriable) — **never a fall back to pipes**; resize is 1–1000 cols/rows in cells, rated on the\nexisting control window, with an initial window required and no 80×24 default; an output bound ends\nthe session through ADR 0014's refusal field, because design 05 gave the pty no `truncated` frame\nand a terminal has no offset to resume from.\n\n**Finding the draft turned up, verified in the tree.** The shared confinement path passes\n`--new-session` (`crates/substrate-host/src/process.rs:1113`), which `bwrap(1)` documents as calling\n`setsid()` and disconnecting the sandbox from the controlling terminal. Inheriting the pty slave as\nfd 0/1/2 therefore gives no job control and no hangup — which is what this story's acceptance needs.\nThe design takes the controlling terminal *inside* the sandbox after bwrap's setsid, and forbids\ndropping the flag.\n\nBundle `0.9.0` is **provisional**: design 14 names it too, and it belongs to whichever is accepted\nfirst.\n\nNot established, and left to the implementing work: which interposition acquires the controlling\nterminal. Kernel `SIGWINCH`/`TIOCSCTTY`/hangup semantics are labelled as kernel behaviour rather\nthan verified in this tree.\n\n## Citation refresh — 2026-08-30\n\n`SessionMode` was cited at `lib.rs:1196`; it is `:1227` at `5749353`. Line numbers in this store drift with every wire change — the symbol name is what survives, so cite both and trust the name.\n"},"args":{"changes":{"body":"# Story: PTY sessions are a distinct session kind with resize\n\n## Outcome\n\nA human can attach a terminal to a confined process, resize it, and get the same lease, single\nattachment, bounded-output and whole-tree cleanup guarantees the raw-pipe session gives.\n\n## Context\n\n`docs/design/05-streams-sessions-and-endpoints.md` § 2 fixes the `pty` kind and its frames\n(input, output, resize, signal, exit, protocol-error) and says a PTY is never substituted for\npipes. `README.md` § *Status* lists PTY as **absent**; `docs/plan/04-direct-byte-plane.md`\n§ *Later phase-4 slices* lists it as remaining. Invariant 8 puts the ADR before code; invariant 6\nputs the wire change in a successor bundle.\n\n## Acceptance\n\nThe delegated lane runs an interactive shell through a `pty` session, echoes bytes, applies a\nresize the child observes, and exits on hangup — while the portable lane proves the typed\n`unserved` and every `0.4.0` byte is unchanged.\n\nEvidence that satisfies it, in order:\n\n1. A design document in `docs/design/`, proposed, fixing kind, frame set, resize bounds, never a\n   substitute for pipes, and the named refusal when the host cannot allocate a pty. It claims no\n   ADR number: `adr/` admits `accepted` and `superseded` only (`xtask/src/adrs.rs:12`), so the\n   number is assigned at acceptance.\n2. The next free successor bundle under `contracts/substrate-wire/` adds the `pty` kind and\n   `resize` frame; compatibility block names its predecessor with exact\n   `adds_routes`/`preserves_routes`; authored source under `xtask/bundle-source/<version>/` and\n   `cargo xtask check-bundle <version>` in `scripts/gate.sh`.\n3. `crates/substrate-host`: pty opened with `nix::pty::openpty` inside the existing\n   bubblewrap/cgroup/cleared-environment path. Failing-first tests:\n   `pty_session_refused_without_confinement`, `pty_resize_is_applied_and_observed` (child reads\n   `TIOCGWINSZ`), `pty_session_kills_tree_on_attachment_loss`.\n4. Daemon: the single-attachment Unix-WebSocket route family serves `pty`; `resize` outside the\n   declared bounds is a protocol error.\n5. Capability fact `sessions.pty` appears only after startup verified pty allocation (invariant 4).\n6. `STATUS.md`, `README.md` § *Status* and plan 04 § *Later phase-4 slices* updated in the same\n   change.\n\n## Open Questions\n\nBundle number: `0.5.0` if this lands first in the epic; the ADR states the actual number.\n\n## Correction — 2026-08-30\n\nThree claims in the evidence list above had gone stale and are rewritten, not deleted, so the\nchange is visible:\n\n| Claimed | Actually |\n|---|---|\n| `adr/0011-pty-sessions-are-a-distinct-session-kind.md` | `0011` is `adr/0011-delegated-context-and-grant-attribution.md`; the number was taken while this story sat in draft |\n| `contracts/substrate-wire/0.5.0/` | `0.5.0` is sealed secret slots and `0.6.0` is destination-bound egress; both are frozen (invariant 6) |\n| `render-contract-bundle-0.5.0.py`, `check-contract-bundle-0.5.0.py` | the Python renderer/checker pairs stop at `0.4.0`; from `0.5.0` on the check is `cargo xtask check-bundle <version>` (`scripts/gate.sh:24-28`) |\n\nThe story pins no numbers now, so it cannot go stale the same way again.\n\n## Scope\n\nDerived 2026-08-30 by `story-scoper`. Every line is **cited** or **inferred**.\n\n- **Primary surface:** `crates/substrate-host` and the `crates/substrate-daemon` session surface —\n  cited, evidence items 3 and 4 assign work to each by name.\n- **Files, inferred:** `crates/substrate-host/src/process.rs:260` (`start_pipe`, the analogue a pty\n  allocation sits beside); `crates/substrate-wire/src/lib.rs:1227` (`SessionMode`);\n  `crates/substrate-daemon/src/app/sessions.rs` and `app/routes.rs:69-85`;\n  `crates/substrate-store/src/sessions.rs` (ADR 0008 durable identity).\n- **Files, cited:** `scripts/gate.sh:24-28`, `xtask/src/adrs.rs:12`.\n- **Files, inferred:** `Cargo.toml:37` — `nix` is a workspace dependency at features\n  `[\"fs\", \"user\"]`; `nix::pty::openpty` needs `term`, and `substrate-host/Cargo.toml` does not\n  depend on `nix` at all.\n- **Symbols, cited:** `SessionMode`, `PipeSessionStartInput`, `PipeClientFrame`, `PipeServerFrame`,\n  `PipeSessionCapabilities`, `PipeSessionLimits` — all declared `crates/substrate-wire/src/lib.rs:1181-1310`.\n- **Also likely:** `contracts/substrate-wire/0.8.0/` and `xtask/bundle-source/0.8.0/` — inferred;\n  `0.7.0` is the frontier, one past what this story's own correction records.\n- **Confidence:** **high for where the work lands** — every cited path resolved in the tree — but no\n  design document and no ADR exist, so invariant 8 (`AGENTS.md:62-64`) blocks the code half.\n- **Would collide with:** any unit touching the `substrate-wire` session types, the daemon\n  `pipe-sessions` route family, `substrate-host` process spawning, `substrate-store` session\n  persistence, or the next contract bundle number. Broad by construction, not by widening.\n\n### Two contradictions found while scoping\n\n**1. `kind` or `mode`?** This story says a `pty` **kind**; design 05 § 2 and `adr/0007:22` say\n**mode**. The code has both, on different axes: `SessionKind` is the *resource* axis and holds one\nvariant `Session` (`crates/substrate-wire/src/lib.rs:1189-1192`), while `SessionMode` holds one\nvariant `Pipes` (`:1194-1198`). Which enum grows is unresolved, and it decides the wire shape.\n\n**2. This story asks for more than invariant 8 requires, and its parent says so.** Evidence item 1\ndemands a new design document. `epic:byte-plane-completion:29` says these proceed \"under invariant 8\nwith an ADR each and **no new design document**\", because design 05 § 2 already fixes the frames.\n`AGENTS.md:62-64` reads \"a design document **or** an ADR **before code**\" — so the epic's reading is\nthe admissible one and this story is over-specifying its own gate. Resolve before dispatching work.\n\n## Design draft — 2026-08-30\n\n`docs/design/13-pty-sessions.md`, **proposed**. Claims no ADR number: `adr/` admits `accepted` and\n`superseded` only (`xtask/src/adrs.rs:12`), so the number is assigned at acceptance.\n\nBoth contradictions this story carried are now decided:\n\n- **`SessionMode` grows a `Pty` variant**, not `SessionKind`. `SessionKind` is the *resource* axis\n  and mirrors `ExecKind`; growing it would fork the single `ses_…`/`ex_…` resource ADR 0008 built.\n- **The route family is reused**, `/v1/pipe-sessions/*` with a `mode` field defaulting to `pipes`,\n  so `adds_routes: 0`. A new family means seven duplicate mode-neutral operation ids and a `GET`\n  that would have to claim `absent: true` for a live session; renaming the path is a coordinated\n  migration with an ADR in atlas and removes seven routes, which is not `additive-v1`.\n\nAlso fixed: `sessions.pty` becomes a probe-verified `CapabilityFacts` member; pty unavailable is\n`unserved`/`session.pty-unserved` (501) and allocation failure `exhausted`/`session.pty-exhausted`\n(429, retriable) — **never a fall back to pipes**; resize is 1–1000 cols/rows in cells, rated on the\nexisting control window, with an initial window required and no 80×24 default; an output bound ends\nthe session through ADR 0014's refusal field, because design 05 gave the pty no `truncated` frame\nand a terminal has no offset to resume from.\n\n**Finding the draft turned up, verified in the tree.** The shared confinement path passes\n`--new-session` (`crates/substrate-host/src/process.rs:1113`), which `bwrap(1)` documents as calling\n`setsid()` and disconnecting the sandbox from the controlling terminal. Inheriting the pty slave as\nfd 0/1/2 therefore gives no job control and no hangup — which is what this story's acceptance needs.\nThe design takes the controlling terminal *inside* the sandbox after bwrap's setsid, and forbids\ndropping the flag.\n\nBundle `0.9.0` is **provisional**: design 14 names it too, and it belongs to whichever is accepted\nfirst.\n\nNot established, and left to the implementing work: which interposition acquires the controlling\nterminal. Kernel `SIGWINCH`/`TIOCSCTTY`/hangup semantics are labelled as kernel behaviour rather\nthan verified in this tree.\n\n## Citation refresh — 2026-08-30\n\n`SessionMode` was cited at `lib.rs:1196`; it is `:1227` at `5749353`. Line numbers in this store drift with every wire change — the symbol name is what survives, so cite both and trust the name.\n"},"command":"update-entity","target":"01MEM0000000000000043"},"payload":{"actor":"human:timo","at":1788092496427,"causation":"cmd-protocol-artifact-body-story-pty-sessions-1788092496427","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body","event_id":"story:pty-sessions@6#0~fb61b57b568ac54b","executor":null,"recorded_at":"2026-08-30T12:21:36Z"}}
{"entity":"story","version":1,"id":"network-session-authority","revision":5,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"# Story: Network session transport over TLS with single-use proof-bound authority\n\n## Outcome\n\nA client on another machine attaches to a session over WebSocket/TLS with an authority that lives\nat most 60 s, redeems exactly once, and is bound to the redeeming channel; a reconnect always\nneeds a fresh one. This is the last phase-4 exit criterion.\n\n## Context\n\n`docs/design/05-streams-sessions-and-endpoints.md` § *V1 decisions* 3–4 fix the authority and the\ntransport; `architecture/deployment-postures.md` requires TLS/mTLS or a trusted tunnel for any\nnon-loopback control listener. Today only the owner-permissioned Unix socket serves sessions\n(plan 04 § *Slice B*). The static-bearer TCP listener is development-only\n(`crates/substrate-daemon/src/main.rs:63-64`, design 06 § 1) and must not gain this route.\n\n## Acceptance\n\nThe delegated lane drives a raw-pipe session end to end over TLS from a second network namespace,\nand the authority it used cannot be redeemed a second time, after 60 s, or over a different TLS\nsession.\n\nEvidence that satisfies it, in order:\n\n1. An ADR decides the proof binding (TLS exporter per RFC 5705, or a client key) and records that\n   reconnect is a new authority, never a resumed one.\n2. A successor bundle adds the network attachment route and the authority-redemption frame; earlier\n   bundle bytes unchanged.\n3. Failing-first tests: `network_session_listener_refuses_plaintext`,\n   `session_authority_redeems_exactly_once`, `session_authority_expires_after_60s` (tokio paused\n   time), `session_authority_bound_to_channel`, `second_concurrent_attachment_refused`.\n4. The authority value never appears in logs, events or error bodies — a test greps captured\n   diagnostics (design 05 § 3, \"non-loggable\").\n5. The development-only TCP listener does not serve the route — a test proves its absence there.\n\n## Out of Scope\n\nThe hosted trust-envelope verifier (design 06 § 1, atlas ADR 0015; phase 7): this story issues\nthe authority from the local bearer subject.\n\n## Scope\n\nDerived 2026-08-30 by `story-scoper`. Every line is **cited** or **inferred**.\n\n- **Primary surface:** `crates/substrate-daemon` — cited; both the listener and the attach route\n  live there.\n- **Files, cited:** `crates/substrate-daemon/src/runtime.rs:764` (`serve_tcp`), `:856`\n  (`require_tcp_bearer`), `src/app/routes.rs:33` (`router`), `src/app/sessions.rs:735`\n  (`pipe_session_attach`).\n- **Symbols, cited:** `serve_tcp`, `TcpAuthState`, `require_tcp_bearer`, `router`,\n  `pipe_session_attach`, `PipeAttachmentPermit`, `PipeAttachmentRefusal`.\n- **Also likely, inferred:** `crates/substrate-wire/src/lib.rs:1274`/`:1291` (`PipeClientFrame`,\n  `PipeServerFrame`), where an authority-redemption frame would be added;\n  `crates/substrate-store/src/schema.rs:154` and `src/sessions.rs:526`\n  (`claim_pipe_session_attachment`), where redeem-exactly-once becomes durable;\n  `crates/substrate-daemon/src/main.rs:192-222`, the clap flags beside the `--tcp-*` group.\n- **Dependency, cited:** the workspace has **no** TLS crate — `rustls`/`tokio-rustls` grep to\n  nothing — so the dependency and its configuration surface are unmade.\n- **Confidence:** **medium.** The crate and the two code sites are cited and certain, but there is\n  no accepted ADR (`adr/` holds 0001–0014) and `docs/design/05-streams-sessions-and-endpoints.md:65`\n  points at `architecture/adr/0016-operation-scoped-session-authority.md`, which does not exist in\n  this repository. The proof binding — TLS exporter or client key — is undecided, so every TLS-side\n  file above is a guess at an unmade decision.\n- **Would collide with:** any unit touching the listener surface (`runtime.rs` `serve`/`serve_tcp`)\n  or the shared `app/routes.rs` `router`; and any unit cutting a successor\n  `contracts/substrate-wire/` version — that bump is a single serialising surface across the epic.\n\n### Two corrections found while scoping\n\n**1. A cited line is stale.** This story cites `crates/substrate-daemon/src/main.rs:63-64` as the\ndevelopment-only TCP listener. Those two lines are `port,` and `})` — the tail of\n`parse_egress_aperture`. The listener is `crates/substrate-daemon/src/runtime.rs:736`.\n\n**2. Acceptance 5 is already false at HEAD.** This story says the development-only TCP transport\n\"must not gain this route\". It has it: `serve_tcp` builds its service with the same `router(app)`\nas the Unix path (`crates/substrate-daemon/src/runtime.rs:757` against `:508`), and that router\nregisters `/v1/pipe-sessions/{session_id}/attach` (`crates/substrate-daemon/src/app/routes.rs:76-77`).\n\nNot an open exposure: `serve_tcp` bails unless both `--tcp-development-only` and\n`--tcp-private-overlay` are set (`crates/substrate-daemon/src/runtime.rs:737-743`), so the route is\nreachable only in an explicitly acknowledged development profile on a private overlay. But the story\nasserts a property the code does not have, so either the acceptance must change or the router must\nsplit — and nothing in the tree specifies a split. Decide it in the ADR this story still owes.\n\n## Design draft — 2026-08-30\n\n`docs/design/14-network-session-authority.md`, **proposed**. Claims no ADR number.\n\n- **Proof binding: a client Ed25519 key, signing the RFC 5705 exporter.** The exporter alone proves\n  nothing — both ends of any TLS session derive a matching value, so a thief redeems on their own\n  connection; it stops a re-encrypting proxy and nothing else, and calling that \"proof-bound\" is\n  invariant 3's silent degradation. The key costs no new crate.\n- **TLS is `rustls` + `tokio-rustls`.** `native-tls` does not portably expose\n  `export_keying_material`, so choosing it would decide the binding by omission.\n- **Single redemption is consumed inside `claim_pipe_session_attachment`'s existing `Immediate`\n  transaction** (`crates/substrate-store/src/sessions.rs:526-546`), in new columns beside\n  `resource_json` and never in it — a second transaction leaves a window where the authority is\n  spent and the attachment is not, and `resource_json` is returned by `GET /v1/pipe-sessions/{id}`.\n\n**The acceptance this story carried was false, and it stands — the code changes.** The\ndevelopment-only TCP transport already serves the attach route: `serve_tcp` builds its service with\nthe same `router(app)` as the Unix path (`crates/substrate-daemon/src/runtime.rs:785` against\n`:514`), and that router registers `/v1/pipe-sessions/{session_id}/attach`\n(`crates/substrate-daemon/src/app/routes.rs:75-78`). The design splits the router by property: no\nlistener that cannot carry an authority confidentially serves the mint or the attach.\n\n**Why the split rather than restating the acceptance, verified independently by the orchestrator.**\n`--tcp-private-overlay` is a bare boolean an operator asserts; there is **no `is_loopback` call\nanywhere** in `crates/substrate-daemon/src/runtime.rs` or `src/main.rs`, so a non-loopback plaintext\nbind is accepted, and the bearer guarding it has no expiry or rotation (`read_bearer_digest`). On a\nwrongly-declared \"private\" address, one observer owns every confined process's stdin and stdout,\nindefinitely. A fifth startup `bail!` refusing non-loopback plaintext lands with the split.\n\nThe static-bearer TCP path is **kept and narrowed**, not superseded: removing `--tcp-listen` breaks\na documented posture and buys nothing here.\n\nBundle `0.9.0` is **provisional**: design 13 names it too.\n\nNot established: the exporter label string is a new wire-visible identifier, and whether it may\ncarry the former brand is atlas ADR 0001's call — deferred to the bundle cut.\n\n## Citation refresh — 2026-08-30\n\n`serve_tcp` was cited at `runtime.rs:736`; it is `:764` at `5749353`.\n"},"args":{"changes":{"body":"# Story: Network session transport over TLS with single-use proof-bound authority\n\n## Outcome\n\nA client on another machine attaches to a session over WebSocket/TLS with an authority that lives\nat most 60 s, redeems exactly once, and is bound to the redeeming channel; a reconnect always\nneeds a fresh one. This is the last phase-4 exit criterion.\n\n## Context\n\n`docs/design/05-streams-sessions-and-endpoints.md` § *V1 decisions* 3–4 fix the authority and the\ntransport; `architecture/deployment-postures.md` requires TLS/mTLS or a trusted tunnel for any\nnon-loopback control listener. Today only the owner-permissioned Unix socket serves sessions\n(plan 04 § *Slice B*). The static-bearer TCP listener is development-only\n(`crates/substrate-daemon/src/main.rs:63-64`, design 06 § 1) and must not gain this route.\n\n## Acceptance\n\nThe delegated lane drives a raw-pipe session end to end over TLS from a second network namespace,\nand the authority it used cannot be redeemed a second time, after 60 s, or over a different TLS\nsession.\n\nEvidence that satisfies it, in order:\n\n1. An ADR decides the proof binding (TLS exporter per RFC 5705, or a client key) and records that\n   reconnect is a new authority, never a resumed one.\n2. A successor bundle adds the network attachment route and the authority-redemption frame; earlier\n   bundle bytes unchanged.\n3. Failing-first tests: `network_session_listener_refuses_plaintext`,\n   `session_authority_redeems_exactly_once`, `session_authority_expires_after_60s` (tokio paused\n   time), `session_authority_bound_to_channel`, `second_concurrent_attachment_refused`.\n4. The authority value never appears in logs, events or error bodies — a test greps captured\n   diagnostics (design 05 § 3, \"non-loggable\").\n5. The development-only TCP listener does not serve the route — a test proves its absence there.\n\n## Out of Scope\n\nThe hosted trust-envelope verifier (design 06 § 1, atlas ADR 0015; phase 7): this story issues\nthe authority from the local bearer subject.\n\n## Scope\n\nDerived 2026-08-30 by `story-scoper`. Every line is **cited** or **inferred**.\n\n- **Primary surface:** `crates/substrate-daemon` — cited; both the listener and the attach route\n  live there.\n- **Files, cited:** `crates/substrate-daemon/src/runtime.rs:764` (`serve_tcp`), `:856`\n  (`require_tcp_bearer`), `src/app/routes.rs:33` (`router`), `src/app/sessions.rs:735`\n  (`pipe_session_attach`).\n- **Symbols, cited:** `serve_tcp`, `TcpAuthState`, `require_tcp_bearer`, `router`,\n  `pipe_session_attach`, `PipeAttachmentPermit`, `PipeAttachmentRefusal`.\n- **Also likely, inferred:** `crates/substrate-wire/src/lib.rs:1274`/`:1291` (`PipeClientFrame`,\n  `PipeServerFrame`), where an authority-redemption frame would be added;\n  `crates/substrate-store/src/schema.rs:154` and `src/sessions.rs:526`\n  (`claim_pipe_session_attachment`), where redeem-exactly-once becomes durable;\n  `crates/substrate-daemon/src/main.rs:192-222`, the clap flags beside the `--tcp-*` group.\n- **Dependency, cited:** the workspace has **no** TLS crate — `rustls`/`tokio-rustls` grep to\n  nothing — so the dependency and its configuration surface are unmade.\n- **Confidence:** **medium.** The crate and the two code sites are cited and certain, but there is\n  no accepted ADR (`adr/` holds 0001–0014) and `docs/design/05-streams-sessions-and-endpoints.md:65`\n  points at `architecture/adr/0016-operation-scoped-session-authority.md`, which does not exist in\n  this repository. The proof binding — TLS exporter or client key — is undecided, so every TLS-side\n  file above is a guess at an unmade decision.\n- **Would collide with:** any unit touching the listener surface (`runtime.rs` `serve`/`serve_tcp`)\n  or the shared `app/routes.rs` `router`; and any unit cutting a successor\n  `contracts/substrate-wire/` version — that bump is a single serialising surface across the epic.\n\n### Two corrections found while scoping\n\n**1. A cited line is stale.** This story cites `crates/substrate-daemon/src/main.rs:63-64` as the\ndevelopment-only TCP listener. Those two lines are `port,` and `})` — the tail of\n`parse_egress_aperture`. The listener is `crates/substrate-daemon/src/runtime.rs:736`.\n\n**2. Acceptance 5 is already false at HEAD.** This story says the development-only TCP transport\n\"must not gain this route\". It has it: `serve_tcp` builds its service with the same `router(app)`\nas the Unix path (`crates/substrate-daemon/src/runtime.rs:757` against `:508`), and that router\nregisters `/v1/pipe-sessions/{session_id}/attach` (`crates/substrate-daemon/src/app/routes.rs:76-77`).\n\nNot an open exposure: `serve_tcp` bails unless both `--tcp-development-only` and\n`--tcp-private-overlay` are set (`crates/substrate-daemon/src/runtime.rs:737-743`), so the route is\nreachable only in an explicitly acknowledged development profile on a private overlay. But the story\nasserts a property the code does not have, so either the acceptance must change or the router must\nsplit — and nothing in the tree specifies a split. Decide it in the ADR this story still owes.\n\n## Design draft — 2026-08-30\n\n`docs/design/14-network-session-authority.md`, **proposed**. Claims no ADR number.\n\n- **Proof binding: a client Ed25519 key, signing the RFC 5705 exporter.** The exporter alone proves\n  nothing — both ends of any TLS session derive a matching value, so a thief redeems on their own\n  connection; it stops a re-encrypting proxy and nothing else, and calling that \"proof-bound\" is\n  invariant 3's silent degradation. The key costs no new crate.\n- **TLS is `rustls` + `tokio-rustls`.** `native-tls` does not portably expose\n  `export_keying_material`, so choosing it would decide the binding by omission.\n- **Single redemption is consumed inside `claim_pipe_session_attachment`'s existing `Immediate`\n  transaction** (`crates/substrate-store/src/sessions.rs:526-546`), in new columns beside\n  `resource_json` and never in it — a second transaction leaves a window where the authority is\n  spent and the attachment is not, and `resource_json` is returned by `GET /v1/pipe-sessions/{id}`.\n\n**The acceptance this story carried was false, and it stands — the code changes.** The\ndevelopment-only TCP transport already serves the attach route: `serve_tcp` builds its service with\nthe same `router(app)` as the Unix path (`crates/substrate-daemon/src/runtime.rs:785` against\n`:514`), and that router registers `/v1/pipe-sessions/{session_id}/attach`\n(`crates/substrate-daemon/src/app/routes.rs:75-78`). The design splits the router by property: no\nlistener that cannot carry an authority confidentially serves the mint or the attach.\n\n**Why the split rather than restating the acceptance, verified independently by the orchestrator.**\n`--tcp-private-overlay` is a bare boolean an operator asserts; there is **no `is_loopback` call\nanywhere** in `crates/substrate-daemon/src/runtime.rs` or `src/main.rs`, so a non-loopback plaintext\nbind is accepted, and the bearer guarding it has no expiry or rotation (`read_bearer_digest`). On a\nwrongly-declared \"private\" address, one observer owns every confined process's stdin and stdout,\nindefinitely. A fifth startup `bail!` refusing non-loopback plaintext lands with the split.\n\nThe static-bearer TCP path is **kept and narrowed**, not superseded: removing `--tcp-listen` breaks\na documented posture and buys nothing here.\n\nBundle `0.9.0` is **provisional**: design 13 names it too.\n\nNot established: the exporter label string is a new wire-visible identifier, and whether it may\ncarry the former brand is atlas ADR 0001's call — deferred to the bundle cut.\n\n## Citation refresh — 2026-08-30\n\n`serve_tcp` was cited at `runtime.rs:736`; it is `:764` at `5749353`.\n"},"command":"update-entity","target":"01MEM0000000000000037"},"payload":{"actor":"human:timo","at":1788092496449,"causation":"cmd-protocol-artifact-body-story-network-session-authority-1788092496449","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body","event_id":"story:network-session-authority@5#0~e190a2eadde50c4b","executor":null,"recorded_at":"2026-08-30T12:21:36Z"}}
{"entity":"story","version":1,"id":"pty-sessions","revision":7,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"specification\":1}}","target":"01MEM0000000000000043","to":"proposed"},"payload":{"actor":"human:timo","at":1788093200256,"causation":"cmd-move-story-pty-sessions-1788093200256","change":{"change":"moved","decided_on":{"recorded":{"specification":1}},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:pty-sessions@7#0~e9ee2246d5677311","executor":null,"recorded_at":"2026-08-30T12:33:20Z"}}
{"entity":"story","version":1,"id":"network-session-authority","revision":6,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"specification\":1}}","target":"01MEM0000000000000037","to":"proposed"},"payload":{"actor":"human:timo","at":1788093200286,"causation":"cmd-move-story-network-session-authority-1788093200286","change":{"change":"moved","decided_on":{"recorded":{"specification":1}},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:network-session-authority@6#0~c192bdad350c489e","executor":null,"recorded_at":"2026-08-30T12:33:20Z"}}
{"entity":"story","version":1,"id":"docker-driver-entry-gate","revision":5,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"specification\":1}}","target":"01MEM0000000000000028","to":"proposed"},"payload":{"actor":"human:timo","at":1788093200308,"causation":"cmd-move-story-docker-driver-entry-gate-1788093200308","change":{"change":"moved","decided_on":{"recorded":{"specification":1}},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:docker-driver-entry-gate@5#0~cd6877fdcd6ed74a","executor":null,"recorded_at":"2026-08-30T12:33:20Z"}}
{"entity":"story","version":1,"id":"contract-gate-sees-route-paths","revision":3,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000022","to":"proposed"},"payload":{"actor":"human:timo","at":1788093345114,"causation":"cmd-move-story-contract-gate-sees-route-paths-1788093345114","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:contract-gate-sees-route-paths@3#0~b5cf5c548f340204","executor":null,"recorded_at":"2026-08-30T12:35:45Z"}}
{"entity":"story","version":1,"id":"contract-gate-sees-route-paths","revision":4,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000022","to":"active"},"payload":{"actor":"human:timo","at":1788093345143,"causation":"cmd-move-story-contract-gate-sees-route-paths-1788093345143","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:contract-gate-sees-route-paths@4#0~a8aa01b813a951a8","executor":null,"recorded_at":"2026-08-30T12:35:45Z"}}
{"entity":"story","version":1,"id":"pty-sessions","revision":8,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"specification\":1}}","target":"01MEM0000000000000043","to":"active"},"payload":{"actor":"human:timo","at":1788093345165,"causation":"cmd-move-story-pty-sessions-1788093345165","change":{"change":"moved","decided_on":{"recorded":{"specification":1}},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:pty-sessions@8#0~e29c72690ac18939","executor":null,"recorded_at":"2026-08-30T12:35:45Z"}}
{"entity":"story","version":1,"id":"pty-sessions","revision":9,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"body":"# Story: PTY sessions are a distinct session kind with resize\n\n## Outcome\n\nA human can attach a terminal to a confined process, resize it, and get the same lease, single\nattachment, bounded-output and whole-tree cleanup guarantees the raw-pipe session gives.\n\n## Context\n\n`docs/design/05-streams-sessions-and-endpoints.md` § 2 fixes the `pty` kind and its frames\n(input, output, resize, signal, exit, protocol-error) and says a PTY is never substituted for\npipes. `README.md` § *Status* lists PTY as **absent**; `docs/plan/04-direct-byte-plane.md`\n§ *Later phase-4 slices* lists it as remaining. Invariant 8 puts the ADR before code; invariant 6\nputs the wire change in a successor bundle.\n\n## Acceptance\n\nThe delegated lane runs an interactive shell through a `pty` session, echoes bytes, applies a\nresize the child observes, and exits on hangup — while the portable lane proves the typed\n`unserved` and every `0.4.0` byte is unchanged.\n\nEvidence that satisfies it, in order:\n\n1. A design document in `docs/design/`, proposed, fixing kind, frame set, resize bounds, never a\n   substitute for pipes, and the named refusal when the host cannot allocate a pty. It claims no\n   ADR number: `adr/` admits `accepted` and `superseded` only (`xtask/src/adrs.rs:12`), so the\n   number is assigned at acceptance.\n2. The next free successor bundle under `contracts/substrate-wire/` adds the `pty` kind and\n   `resize` frame; compatibility block names its predecessor with exact\n   `adds_routes`/`preserves_routes`; authored source under `xtask/bundle-source/<version>/` and\n   `cargo xtask check-bundle <version>` in `scripts/gate.sh`.\n3. `crates/substrate-host`: pty opened with `nix::pty::openpty` inside the existing\n   bubblewrap/cgroup/cleared-environment path. Failing-first tests:\n   `pty_session_refused_without_confinement`, `pty_resize_is_applied_and_observed` (child reads\n   `TIOCGWINSZ`), `pty_session_kills_tree_on_attachment_loss`.\n4. Daemon: the single-attachment Unix-WebSocket route family serves `pty`; `resize` outside the\n   declared bounds is a protocol error.\n5. Capability fact `sessions.pty` appears only after startup verified pty allocation (invariant 4).\n6. `STATUS.md`, `README.md` § *Status* and plan 04 § *Later phase-4 slices* updated in the same\n   change.\n\n## Open Questions\n\nBundle number: `0.5.0` if this lands first in the epic; the ADR states the actual number.\n\n## Correction — 2026-08-30\n\nThree claims in the evidence list above had gone stale and are rewritten, not deleted, so the\nchange is visible:\n\n| Claimed | Actually |\n|---|---|\n| `adr/0011-pty-sessions-are-a-distinct-session-kind.md` | `0011` is `adr/0011-delegated-context-and-grant-attribution.md`; the number was taken while this story sat in draft |\n| `contracts/substrate-wire/0.5.0/` | `0.5.0` is sealed secret slots and `0.6.0` is destination-bound egress; both are frozen (invariant 6) |\n| `render-contract-bundle-0.5.0.py`, `check-contract-bundle-0.5.0.py` | the Python renderer/checker pairs stop at `0.4.0`; from `0.5.0` on the check is `cargo xtask check-bundle <version>` (`scripts/gate.sh:24-28`) |\n\nThe story pins no numbers now, so it cannot go stale the same way again.\n\n## Scope\n\nDerived 2026-08-30 by `story-scoper`. Every line is **cited** or **inferred**.\n\n- **Primary surface:** `crates/substrate-host` and the `crates/substrate-daemon` session surface —\n  cited, evidence items 3 and 4 assign work to each by name.\n- **Files, inferred:** `crates/substrate-host/src/process.rs:260` (`start_pipe`, the analogue a pty\n  allocation sits beside); `crates/substrate-wire/src/lib.rs:1227` (`SessionMode`);\n  `crates/substrate-daemon/src/app/sessions.rs` and `app/routes.rs:69-85`;\n  `crates/substrate-store/src/sessions.rs` (ADR 0008 durable identity).\n- **Files, cited:** `scripts/gate.sh:24-28`, `xtask/src/adrs.rs:12`.\n- **Files, inferred:** `Cargo.toml:37` — `nix` is a workspace dependency at features\n  `[\"fs\", \"user\"]`; `nix::pty::openpty` needs `term`, and `substrate-host/Cargo.toml` does not\n  depend on `nix` at all.\n- **Symbols, cited:** `SessionMode`, `PipeSessionStartInput`, `PipeClientFrame`, `PipeServerFrame`,\n  `PipeSessionCapabilities`, `PipeSessionLimits` — all declared `crates/substrate-wire/src/lib.rs:1181-1310`.\n- **Also likely:** `contracts/substrate-wire/0.8.0/` and `xtask/bundle-source/0.8.0/` — inferred;\n  `0.7.0` is the frontier, one past what this story's own correction records.\n- **Confidence:** **high for where the work lands** — every cited path resolved in the tree — but no\n  design document and no ADR exist, so invariant 8 (`AGENTS.md:62-64`) blocks the code half.\n- **Would collide with:** any unit touching the `substrate-wire` session types, the daemon\n  `pipe-sessions` route family, `substrate-host` process spawning, `substrate-store` session\n  persistence, or the next contract bundle number. Broad by construction, not by widening.\n\n### Two contradictions found while scoping\n\n**1. `kind` or `mode`?** This story says a `pty` **kind**; design 05 § 2 and `adr/0007:22` say\n**mode**. The code has both, on different axes: `SessionKind` is the *resource* axis and holds one\nvariant `Session` (`crates/substrate-wire/src/lib.rs:1189-1192`), while `SessionMode` holds one\nvariant `Pipes` (`:1194-1198`). Which enum grows is unresolved, and it decides the wire shape.\n\n**2. This story asks for more than invariant 8 requires, and its parent says so.** Evidence item 1\ndemands a new design document. `epic:byte-plane-completion:29` says these proceed \"under invariant 8\nwith an ADR each and **no new design document**\", because design 05 § 2 already fixes the frames.\n`AGENTS.md:62-64` reads \"a design document **or** an ADR **before code**\" — so the epic's reading is\nthe admissible one and this story is over-specifying its own gate. Resolve before dispatching work.\n\n## Design draft — 2026-08-30\n\n`docs/design/13-pty-sessions.md`, **proposed**. Claims no ADR number: `adr/` admits `accepted` and\n`superseded` only (`xtask/src/adrs.rs:12`), so the number is assigned at acceptance.\n\nBoth contradictions this story carried are now decided:\n\n- **`SessionMode` grows a `Pty` variant**, not `SessionKind`. `SessionKind` is the *resource* axis\n  and mirrors `ExecKind`; growing it would fork the single `ses_…`/`ex_…` resource ADR 0008 built.\n- **The route family is reused**, `/v1/pipe-sessions/*` with a `mode` field defaulting to `pipes`,\n  so `adds_routes: 0`. A new family means seven duplicate mode-neutral operation ids and a `GET`\n  that would have to claim `absent: true` for a live session; renaming the path is a coordinated\n  migration with an ADR in atlas and removes seven routes, which is not `additive-v1`.\n\nAlso fixed: `sessions.pty` becomes a probe-verified `CapabilityFacts` member; pty unavailable is\n`unserved`/`session.pty-unserved` (501) and allocation failure `exhausted`/`session.pty-exhausted`\n(429, retriable) — **never a fall back to pipes**; resize is 1–1000 cols/rows in cells, rated on the\nexisting control window, with an initial window required and no 80×24 default; an output bound ends\nthe session through ADR 0014's refusal field, because design 05 gave the pty no `truncated` frame\nand a terminal has no offset to resume from.\n\n**Finding the draft turned up, verified in the tree.** The shared confinement path passes\n`--new-session` (`crates/substrate-host/src/process.rs:1113`), which `bwrap(1)` documents as calling\n`setsid()` and disconnecting the sandbox from the controlling terminal. Inheriting the pty slave as\nfd 0/1/2 therefore gives no job control and no hangup — which is what this story's acceptance needs.\nThe design takes the controlling terminal *inside* the sandbox after bwrap's setsid, and forbids\ndropping the flag.\n\nBundle `0.9.0` is **provisional**: design 14 names it too, and it belongs to whichever is accepted\nfirst.\n\nNot established, and left to the implementing work: which interposition acquires the controlling\nterminal. Kernel `SIGWINCH`/`TIOCSCTTY`/hangup semantics are labelled as kernel behaviour rather\nthan verified in this tree.\n\n## Citation refresh — 2026-08-30\n\n`SessionMode` was cited at `lib.rs:1196`; it is `:1227` at `5749353`. Line numbers in this store drift with every wire change — the symbol name is what survives, so cite both and trust the name.\n\n## Scope corrections — 2026-08-30, from the implementation\n\nThe `## Scope` section above marks five lines `inferred`. The implementing work checked all five\nagainst the tree before building on them. **Two were wrong.** They are corrected here rather than\nedited above, so the change is visible.\n\n| Inferred line | Verdict |\n|---|---|\n| `Cargo.toml:37` is `nix` at features `[\"fs\", \"user\"]`, and `substrate-host` does not depend on `nix` | **correct** |\n| `crates/substrate-wire/src/lib.rs` `SessionMode` | **correct** |\n| `crates/substrate-daemon/src/app/routes.rs:69-85` | **correct**, and untouched — the route family was reused, `adds_routes: 0` |\n| `crates/substrate-store/src/sessions.rs` needs work for ADR 0008 durable identity | **wrong.** The store serialises the whole `PipeSession` as JSON (`upsert_session`, `crates/substrate-store/src/sessions.rs:614`), so a new `SessionMode` variant persists with **zero** store changes. `substrate-store` was never touched |\n| \"`0.7.0` is the frontier\" | **wrong/stale.** `0.8.0` was the frontier; `0.9.0` is the successor this story cut |\n\nThe scope's own confidence line said **high for where the work lands**, and that held: every *cited*\npath resolved and the primary surfaces were right. What did not hold was the inferred half, in both\ndirections — one crate named that needed nothing, one version number a release behind. Recorded\nbecause the next wave selects on this section.\n"},"args":{"changes":{"body":"# Story: PTY sessions are a distinct session kind with resize\n\n## Outcome\n\nA human can attach a terminal to a confined process, resize it, and get the same lease, single\nattachment, bounded-output and whole-tree cleanup guarantees the raw-pipe session gives.\n\n## Context\n\n`docs/design/05-streams-sessions-and-endpoints.md` § 2 fixes the `pty` kind and its frames\n(input, output, resize, signal, exit, protocol-error) and says a PTY is never substituted for\npipes. `README.md` § *Status* lists PTY as **absent**; `docs/plan/04-direct-byte-plane.md`\n§ *Later phase-4 slices* lists it as remaining. Invariant 8 puts the ADR before code; invariant 6\nputs the wire change in a successor bundle.\n\n## Acceptance\n\nThe delegated lane runs an interactive shell through a `pty` session, echoes bytes, applies a\nresize the child observes, and exits on hangup — while the portable lane proves the typed\n`unserved` and every `0.4.0` byte is unchanged.\n\nEvidence that satisfies it, in order:\n\n1. A design document in `docs/design/`, proposed, fixing kind, frame set, resize bounds, never a\n   substitute for pipes, and the named refusal when the host cannot allocate a pty. It claims no\n   ADR number: `adr/` admits `accepted` and `superseded` only (`xtask/src/adrs.rs:12`), so the\n   number is assigned at acceptance.\n2. The next free successor bundle under `contracts/substrate-wire/` adds the `pty` kind and\n   `resize` frame; compatibility block names its predecessor with exact\n   `adds_routes`/`preserves_routes`; authored source under `xtask/bundle-source/<version>/` and\n   `cargo xtask check-bundle <version>` in `scripts/gate.sh`.\n3. `crates/substrate-host`: pty opened with `nix::pty::openpty` inside the existing\n   bubblewrap/cgroup/cleared-environment path. Failing-first tests:\n   `pty_session_refused_without_confinement`, `pty_resize_is_applied_and_observed` (child reads\n   `TIOCGWINSZ`), `pty_session_kills_tree_on_attachment_loss`.\n4. Daemon: the single-attachment Unix-WebSocket route family serves `pty`; `resize` outside the\n   declared bounds is a protocol error.\n5. Capability fact `sessions.pty` appears only after startup verified pty allocation (invariant 4).\n6. `STATUS.md`, `README.md` § *Status* and plan 04 § *Later phase-4 slices* updated in the same\n   change.\n\n## Open Questions\n\nBundle number: `0.5.0` if this lands first in the epic; the ADR states the actual number.\n\n## Correction — 2026-08-30\n\nThree claims in the evidence list above had gone stale and are rewritten, not deleted, so the\nchange is visible:\n\n| Claimed | Actually |\n|---|---|\n| `adr/0011-pty-sessions-are-a-distinct-session-kind.md` | `0011` is `adr/0011-delegated-context-and-grant-attribution.md`; the number was taken while this story sat in draft |\n| `contracts/substrate-wire/0.5.0/` | `0.5.0` is sealed secret slots and `0.6.0` is destination-bound egress; both are frozen (invariant 6) |\n| `render-contract-bundle-0.5.0.py`, `check-contract-bundle-0.5.0.py` | the Python renderer/checker pairs stop at `0.4.0`; from `0.5.0` on the check is `cargo xtask check-bundle <version>` (`scripts/gate.sh:24-28`) |\n\nThe story pins no numbers now, so it cannot go stale the same way again.\n\n## Scope\n\nDerived 2026-08-30 by `story-scoper`. Every line is **cited** or **inferred**.\n\n- **Primary surface:** `crates/substrate-host` and the `crates/substrate-daemon` session surface —\n  cited, evidence items 3 and 4 assign work to each by name.\n- **Files, inferred:** `crates/substrate-host/src/process.rs:260` (`start_pipe`, the analogue a pty\n  allocation sits beside); `crates/substrate-wire/src/lib.rs:1227` (`SessionMode`);\n  `crates/substrate-daemon/src/app/sessions.rs` and `app/routes.rs:69-85`;\n  `crates/substrate-store/src/sessions.rs` (ADR 0008 durable identity).\n- **Files, cited:** `scripts/gate.sh:24-28`, `xtask/src/adrs.rs:12`.\n- **Files, inferred:** `Cargo.toml:37` — `nix` is a workspace dependency at features\n  `[\"fs\", \"user\"]`; `nix::pty::openpty` needs `term`, and `substrate-host/Cargo.toml` does not\n  depend on `nix` at all.\n- **Symbols, cited:** `SessionMode`, `PipeSessionStartInput`, `PipeClientFrame`, `PipeServerFrame`,\n  `PipeSessionCapabilities`, `PipeSessionLimits` — all declared `crates/substrate-wire/src/lib.rs:1181-1310`.\n- **Also likely:** `contracts/substrate-wire/0.8.0/` and `xtask/bundle-source/0.8.0/` — inferred;\n  `0.7.0` is the frontier, one past what this story's own correction records.\n- **Confidence:** **high for where the work lands** — every cited path resolved in the tree — but no\n  design document and no ADR exist, so invariant 8 (`AGENTS.md:62-64`) blocks the code half.\n- **Would collide with:** any unit touching the `substrate-wire` session types, the daemon\n  `pipe-sessions` route family, `substrate-host` process spawning, `substrate-store` session\n  persistence, or the next contract bundle number. Broad by construction, not by widening.\n\n### Two contradictions found while scoping\n\n**1. `kind` or `mode`?** This story says a `pty` **kind**; design 05 § 2 and `adr/0007:22` say\n**mode**. The code has both, on different axes: `SessionKind` is the *resource* axis and holds one\nvariant `Session` (`crates/substrate-wire/src/lib.rs:1189-1192`), while `SessionMode` holds one\nvariant `Pipes` (`:1194-1198`). Which enum grows is unresolved, and it decides the wire shape.\n\n**2. This story asks for more than invariant 8 requires, and its parent says so.** Evidence item 1\ndemands a new design document. `epic:byte-plane-completion:29` says these proceed \"under invariant 8\nwith an ADR each and **no new design document**\", because design 05 § 2 already fixes the frames.\n`AGENTS.md:62-64` reads \"a design document **or** an ADR **before code**\" — so the epic's reading is\nthe admissible one and this story is over-specifying its own gate. Resolve before dispatching work.\n\n## Design draft — 2026-08-30\n\n`docs/design/13-pty-sessions.md`, **proposed**. Claims no ADR number: `adr/` admits `accepted` and\n`superseded` only (`xtask/src/adrs.rs:12`), so the number is assigned at acceptance.\n\nBoth contradictions this story carried are now decided:\n\n- **`SessionMode` grows a `Pty` variant**, not `SessionKind`. `SessionKind` is the *resource* axis\n  and mirrors `ExecKind`; growing it would fork the single `ses_…`/`ex_…` resource ADR 0008 built.\n- **The route family is reused**, `/v1/pipe-sessions/*` with a `mode` field defaulting to `pipes`,\n  so `adds_routes: 0`. A new family means seven duplicate mode-neutral operation ids and a `GET`\n  that would have to claim `absent: true` for a live session; renaming the path is a coordinated\n  migration with an ADR in atlas and removes seven routes, which is not `additive-v1`.\n\nAlso fixed: `sessions.pty` becomes a probe-verified `CapabilityFacts` member; pty unavailable is\n`unserved`/`session.pty-unserved` (501) and allocation failure `exhausted`/`session.pty-exhausted`\n(429, retriable) — **never a fall back to pipes**; resize is 1–1000 cols/rows in cells, rated on the\nexisting control window, with an initial window required and no 80×24 default; an output bound ends\nthe session through ADR 0014's refusal field, because design 05 gave the pty no `truncated` frame\nand a terminal has no offset to resume from.\n\n**Finding the draft turned up, verified in the tree.** The shared confinement path passes\n`--new-session` (`crates/substrate-host/src/process.rs:1113`), which `bwrap(1)` documents as calling\n`setsid()` and disconnecting the sandbox from the controlling terminal. Inheriting the pty slave as\nfd 0/1/2 therefore gives no job control and no hangup — which is what this story's acceptance needs.\nThe design takes the controlling terminal *inside* the sandbox after bwrap's setsid, and forbids\ndropping the flag.\n\nBundle `0.9.0` is **provisional**: design 14 names it too, and it belongs to whichever is accepted\nfirst.\n\nNot established, and left to the implementing work: which interposition acquires the controlling\nterminal. Kernel `SIGWINCH`/`TIOCSCTTY`/hangup semantics are labelled as kernel behaviour rather\nthan verified in this tree.\n\n## Citation refresh — 2026-08-30\n\n`SessionMode` was cited at `lib.rs:1196`; it is `:1227` at `5749353`. Line numbers in this store drift with every wire change — the symbol name is what survives, so cite both and trust the name.\n\n## Scope corrections — 2026-08-30, from the implementation\n\nThe `## Scope` section above marks five lines `inferred`. The implementing work checked all five\nagainst the tree before building on them. **Two were wrong.** They are corrected here rather than\nedited above, so the change is visible.\n\n| Inferred line | Verdict |\n|---|---|\n| `Cargo.toml:37` is `nix` at features `[\"fs\", \"user\"]`, and `substrate-host` does not depend on `nix` | **correct** |\n| `crates/substrate-wire/src/lib.rs` `SessionMode` | **correct** |\n| `crates/substrate-daemon/src/app/routes.rs:69-85` | **correct**, and untouched — the route family was reused, `adds_routes: 0` |\n| `crates/substrate-store/src/sessions.rs` needs work for ADR 0008 durable identity | **wrong.** The store serialises the whole `PipeSession` as JSON (`upsert_session`, `crates/substrate-store/src/sessions.rs:614`), so a new `SessionMode` variant persists with **zero** store changes. `substrate-store` was never touched |\n| \"`0.7.0` is the frontier\" | **wrong/stale.** `0.8.0` was the frontier; `0.9.0` is the successor this story cut |\n\nThe scope's own confidence line said **high for where the work lands**, and that held: every *cited*\npath resolved and the primary surfaces were right. What did not hold was the inferred half, in both\ndirections — one crate named that needed nothing, one version number a release behind. Recorded\nbecause the next wave selects on this section.\n"},"command":"update-entity","target":"01MEM0000000000000043"},"payload":{"actor":"human:timo","at":1788165461769,"causation":"cmd-protocol-artifact-body-story-pty-sessions-1788165461769","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body","event_id":"story:pty-sessions@9#0~e987dd251fa711e4","executor":null,"recorded_at":"2026-08-31T08:37:41Z"}}
{"entity":"story","version":1,"id":"aperture-install-failure-loses-its-errno","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Story: An aperture install failure names its stage and loses its errno\n\n## Outcome\n\nWhen `exec.aperture-install-failed` fires, the operator learns *why* and not only *where*, and the\nintermittent stage-6 failure in the delegated lane is either fixed or explained.\n\n## What was observed\n\n**Reported by the pty implementor on 2026-08-30, not reproduced by the coordinator.**\n`egress::tests::declared_aperture_is_reachable` (`crates/substrate-host/src/egress.rs:1537`) fails\nintermittently inside a delegated systemd scope:\n\n```\ninstall the aperture: DriverError { class: Failed, code: \"exec.aperture-install-failed\",\nmessage: \"The egress aperture could not be installed exactly as declared (stage 6).\" }\n```\n\nIts numbers, as given: **1 failure in 10** serial delegated runs; 0 in 3 isolated runs of `egress::`\nalone; 0 in the portable `cargo test --workspace`. None of these were re-run independently.\n\nIt became visible only because `scripts/delegated-lane.sh` stopped selecting host cases by the\nsubstring `pty` — that filter had been running 8 of 58 host lib cases, so this test had never\nexecuted in a delegated scope. The fix is on `wt/pty-sessions`.\n\n## Blast radius, verified\n\n`scripts/gate.sh` contains no reference to the delegated lane or to\n`SUBSTRATE_VECTORS_CGROUP_ROOT` (`grep`, 0 hits). **The whole gate is unaffected.** What is affected\nis `bash scripts/delegated-lane.sh`, which is run by hand and is the only thing that executes the\ndelegated cases at all — so a lane that is red one run in ten is a lane whose red gets discounted.\n\n## What the code says, verified\n\n- **Stage 6 is `bind`.** The sandbox helper returns a stage number as its exit status; `return 6`\n  follows `libc::bind(listener, …)` on `loopback_sockaddr(port)`, inside the sandbox's network\n  namespace after `setns(CLONE_NEWUSER)` and `setns(CLONE_NEWNET)`.\n- **`SO_REUSEADDR` is already set on that socket**, immediately before the bind. So the first\n  hypothesis anybody reaches for — a previous run's socket in `TIME_WAIT` — is ruled out, and the\n  next person should not spend the afternoon on it.\n- **The errno cannot escape, and `install_failed` is not the reason.** The helper's only channel to\n  its parent is `_exit(stage)` (`egress.rs:456-464` turns that exit status into the message), and an\n  exit code carries a stage, not an errno. The helper is a fork precisely because\n  `setns` with `CLONE_NEWUSER` refuses a threaded caller, so it cannot allocate or return a\n  `DriverError`. The `install_failed` helper at `egress.rs:1092` *does* interpolate `{error}`; it is\n  simply not on this path.\n- The design states the trade deliberately: \"The return value is the stage that failed, so an\n  install failure names *where* rather than leaving an operator to guess between a namespace, a bind\n  and a handback.\"\n\n**Not established:** where `port` is chosen, whether two runs in one delegated scope can choose the\nsame one, and what the bind's actual errno is. Everything about the cause is open.\n\n## Acceptance\n\nAn aperture install failure carries the failing stage **and** the errno, and the stage-6 failure in\nthe delegated lane is either eliminated or has a named cause recorded against this story.\n\nEvidence that satisfies it, in order:\n\n1. The helper's stage channel widens to carry an errno — the exit status is one byte and already\n   spent, so this needs a second channel (the existing handback socket, a pipe, or an `AtomicI32` in\n   the shared mapping the forwarder already maps). Written failing-first against a forced bind\n   failure.\n2. The message names both, and a test asserts the errno reaches the caller.\n3. `bash scripts/delegated-lane.sh` run at least 20 times serially, with the failure count recorded\n   here — before and after. One failure in ten is a claim a 10-run sample cannot separate from two\n   in ten.\n4. If the cause is a port collision, the port's selection is the fix and the errno is the evidence\n   that found it; if it is not, this story records what it was.\n\n## Provenance\n\nFound while running `story:pty-sessions` in the wave of 2026-08-30, in the lane that story repaired.\nNot caused by it: the test and the helper both predate the wave. The coordinator verified the four\ncode claims above against `wave/2026-08-30-byte-plane`; the failure rate and the failing run are the\nimplementor's, quoted.\n\n## Correction — 2026-08-30, the flake is wider than this story says\n\nThe observation above names **one** test at **1 failure in 10**. A second reporter — the adversary\non `story:pty-sessions`' fifth pass — puts it at **module-wide, roughly 1 run in 2**, with two\nfurther tests failing that this story does not name:\n\n| test | `file:line` | observed |\n|---|---|---|\n| `declared_aperture_is_reachable` | `crates/substrate-host/src/egress.rs:1537` | the original report |\n| `applied_aperture_is_observed` | `crates/substrate-host/src/egress.rs:1657` | `Unreachable` where `Served` was expected |\n| `a_declared_ceiling_stops_the_relay` | `crates/substrate-host/src/egress.rs` | named, no assertion text captured |\n\nBoth rates are **reported, not reproduced by the coordinator**, and they disagree by a factor of\nfive. Two reporters, two runs of different lengths, on one machine — the honest reading is that the\nrate is unmeasured, not that it is 1 in 2 rather than 1 in 10. Acceptance item 3 already asks for a\n20-run sample; that sample now has to cover the **module**, not the one test.\n\nWhat this changes about the story: the stage-6 `bind` in `declared_aperture_is_reachable` is no\nlonger necessarily the whole subject. `applied_aperture_is_observed` failing with `Unreachable` is a\ndifferent symptom — a relay that installed and did not carry — so a single cause is a hypothesis and\nnot a finding. The errno the helper cannot report (`_exit(stage)`) is still the first thing to fix\neither way: it is what would tell these two symptoms apart.\n\n**Still verified, unchanged:** `scripts/gate.sh` has no delegated-lane reference, so the whole gate\nis unaffected; stage 6 is `libc::bind` with `SO_REUSEADDR` already set; the errno is lost at the\n`_exit(stage)` boundary and not in `install_failed`.\n","summary":"The sandbox helper reports through _exit(stage), so a stage-6 bind failure carries no errno; the delegated lane fails on it about one run in ten.","title":"An aperture install failure names its stage and loses its errno"},"args":{"command":"create-entity","data":{"body":"# Story: An aperture install failure names its stage and loses its errno\n\n## Outcome\n\nWhen `exec.aperture-install-failed` fires, the operator learns *why* and not only *where*, and the\nintermittent stage-6 failure in the delegated lane is either fixed or explained.\n\n## What was observed\n\n**Reported by the pty implementor on 2026-08-30, not reproduced by the coordinator.**\n`egress::tests::declared_aperture_is_reachable` (`crates/substrate-host/src/egress.rs:1537`) fails\nintermittently inside a delegated systemd scope:\n\n```\ninstall the aperture: DriverError { class: Failed, code: \"exec.aperture-install-failed\",\nmessage: \"The egress aperture could not be installed exactly as declared (stage 6).\" }\n```\n\nIts numbers, as given: **1 failure in 10** serial delegated runs; 0 in 3 isolated runs of `egress::`\nalone; 0 in the portable `cargo test --workspace`. None of these were re-run independently.\n\nIt became visible only because `scripts/delegated-lane.sh` stopped selecting host cases by the\nsubstring `pty` — that filter had been running 8 of 58 host lib cases, so this test had never\nexecuted in a delegated scope. The fix is on `wt/pty-sessions`.\n\n## Blast radius, verified\n\n`scripts/gate.sh` contains no reference to the delegated lane or to\n`SUBSTRATE_VECTORS_CGROUP_ROOT` (`grep`, 0 hits). **The whole gate is unaffected.** What is affected\nis `bash scripts/delegated-lane.sh`, which is run by hand and is the only thing that executes the\ndelegated cases at all — so a lane that is red one run in ten is a lane whose red gets discounted.\n\n## What the code says, verified\n\n- **Stage 6 is `bind`.** The sandbox helper returns a stage number as its exit status; `return 6`\n  follows `libc::bind(listener, …)` on `loopback_sockaddr(port)`, inside the sandbox's network\n  namespace after `setns(CLONE_NEWUSER)` and `setns(CLONE_NEWNET)`.\n- **`SO_REUSEADDR` is already set on that socket**, immediately before the bind. So the first\n  hypothesis anybody reaches for — a previous run's socket in `TIME_WAIT` — is ruled out, and the\n  next person should not spend the afternoon on it.\n- **The errno cannot escape, and `install_failed` is not the reason.** The helper's only channel to\n  its parent is `_exit(stage)` (`egress.rs:456-464` turns that exit status into the message), and an\n  exit code carries a stage, not an errno. The helper is a fork precisely because\n  `setns` with `CLONE_NEWUSER` refuses a threaded caller, so it cannot allocate or return a\n  `DriverError`. The `install_failed` helper at `egress.rs:1092` *does* interpolate `{error}`; it is\n  simply not on this path.\n- The design states the trade deliberately: \"The return value is the stage that failed, so an\n  install failure names *where* rather than leaving an operator to guess between a namespace, a bind\n  and a handback.\"\n\n**Not established:** where `port` is chosen, whether two runs in one delegated scope can choose the\nsame one, and what the bind's actual errno is. Everything about the cause is open.\n\n## Acceptance\n\nAn aperture install failure carries the failing stage **and** the errno, and the stage-6 failure in\nthe delegated lane is either eliminated or has a named cause recorded against this story.\n\nEvidence that satisfies it, in order:\n\n1. The helper's stage channel widens to carry an errno — the exit status is one byte and already\n   spent, so this needs a second channel (the existing handback socket, a pipe, or an `AtomicI32` in\n   the shared mapping the forwarder already maps). Written failing-first against a forced bind\n   failure.\n2. The message names both, and a test asserts the errno reaches the caller.\n3. `bash scripts/delegated-lane.sh` run at least 20 times serially, with the failure count recorded\n   here — before and after. One failure in ten is a claim a 10-run sample cannot separate from two\n   in ten.\n4. If the cause is a port collision, the port's selection is the fix and the errno is the evidence\n   that found it; if it is not, this story records what it was.\n\n## Provenance\n\nFound while running `story:pty-sessions` in the wave of 2026-08-30, in the lane that story repaired.\nNot caused by it: the test and the helper both predate the wave. The coordinator verified the four\ncode claims above against `wave/2026-08-30-byte-plane`; the failure rate and the failing run are the\nimplementor's, quoted.\n\n## Correction — 2026-08-30, the flake is wider than this story says\n\nThe observation above names **one** test at **1 failure in 10**. A second reporter — the adversary\non `story:pty-sessions`' fifth pass — puts it at **module-wide, roughly 1 run in 2**, with two\nfurther tests failing that this story does not name:\n\n| test | `file:line` | observed |\n|---|---|---|\n| `declared_aperture_is_reachable` | `crates/substrate-host/src/egress.rs:1537` | the original report |\n| `applied_aperture_is_observed` | `crates/substrate-host/src/egress.rs:1657` | `Unreachable` where `Served` was expected |\n| `a_declared_ceiling_stops_the_relay` | `crates/substrate-host/src/egress.rs` | named, no assertion text captured |\n\nBoth rates are **reported, not reproduced by the coordinator**, and they disagree by a factor of\nfive. Two reporters, two runs of different lengths, on one machine — the honest reading is that the\nrate is unmeasured, not that it is 1 in 2 rather than 1 in 10. Acceptance item 3 already asks for a\n20-run sample; that sample now has to cover the **module**, not the one test.\n\nWhat this changes about the story: the stage-6 `bind` in `declared_aperture_is_reachable` is no\nlonger necessarily the whole subject. `applied_aperture_is_observed` failing with `Unreachable` is a\ndifferent symptom — a relay that installed and did not carry — so a single cause is a hypothesis and\nnot a finding. The errno the helper cannot report (`_exit(stage)`) is still the first thing to fix\neither way: it is what would tell these two symptoms apart.\n\n**Still verified, unchanged:** `scripts/gate.sh` has no delegated-lane reference, so the whole gate\nis unaffected; stage 6 is `libc::bind` with `SO_REUSEADDR` already set; the errno is lost at the\n`_exit(stage)` boundary and not in `install_failed`.\n","status":"draft","summary":"The sandbox helper reports through _exit(stage), so a stage-6 bind failure carries no errno; the delegated lane fails on it about one run in ten.","title":"An aperture install failure names its stage and loses its errno"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/aperture-install-failure-loses-its-errno"},"payload":{"actor":"human:timo","at":1788165461953,"causation":"cmd-new-story-aperture-install-failure-loses-its-errno","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:aperture-install-failure-loses-its-errno@1#0~78b864e7abcc06de","executor":null,"recorded_at":"2026-08-31T08:37:41Z"}}
{"entity":"story","version":1,"id":"aperture-install-failure-loses-its-errno","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:release-hardening"}]},"args":{"command":"create-relation","kind":"decomposes","source":"01MEM0000000000000115","target":"01MEM0000000000000007"},"payload":{"actor":"human:timo","at":1788165461962,"causation":"cmd-story-aperture-install-failure-loses-its-errno-decomposes-epic-release-hardening","change":{"change":"related","relation":"decomposes","target":"epic:release-hardening"},"correlation":"protocol-artifact-relate","event_id":"story:aperture-install-failure-loses-its-errno@1#0~f45e03a4600ab8df","executor":null,"recorded_at":"2026-08-31T08:37:41Z"}}
{"entity":"story","version":1,"id":"contract-gate-predecessor-adjacency","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Story: The contract gate refuses a bundle that names a non-adjacent predecessor\n\n## Outcome\n\nA successor bundle cannot make earlier routes invisible to the compatibility check by declaring an\nolder version as its predecessor.\n\n## Context\n\n`check_compatibility` reads `compatibility.predecessor` out of the bundle's own `bundle.json`\n(`xtask/src/bundle.rs:203-206`) and joins it straight onto `inputs.contracts_root`\n(`:216-219`). Nothing anywhere requires that string to name the adjacent released version:\n`grep -rn 'compatibility/predecessor\\|\"predecessor\"' xtask/src/bundle.rs` returns exactly one\nproduction site, `:204`. The per-version dispatch in `check_additions` (`:383-400`) branches on the\nbundle's *own* version and never on its predecessor; `PREDECESSOR: &str = \"0.7.0\"` at `:1258` is a\nconstant used by the file's own cases, not a released-order assertion.\n\nSo a `0.9.0` that declares `predecessor: \"0.5.0\"` is compared against `0.5.0`'s inventory. Every\nroute introduced in `0.6.0`, `0.7.0` and `0.8.0` becomes invisible to the drop check and — after\n`story:contract-gate-sees-route-paths` — to the moved-path check as well. The counts are not a\nbarrier: the bundle authors its own `adds_routes` and `preserves_routes`, so it declares the\ninflated numbers and they agree.\n\n## Provenance\n\nFound by `engineering-protocols:adversary` on 2026-08-30 while attacking\n`story:contract-gate-sees-route-paths`, reported as finding F6 and marked pre-existing — it is not\ncaused by that change. The two `file:line` claims above and the `grep` result were re-checked by the\ncoordinator against `wave/2026-08-30-byte-plane`, which does not carry that change.\n\n**Not observed as an exploit.** No bundle in `contracts/substrate-wire/` declares a non-adjacent\npredecessor, and no test drives one. What is verified is the *absence of a check*, not a failure of\none; nobody has rendered a bundle that takes this route.\n\n## Acceptance\n\nA bundle whose `compatibility.predecessor` is not the released version immediately below its own\nfails `cargo xtask check-bundle`, by name, and the failure says which version it named and which one\nit should have named.\n\nEvidence that satisfies it, in order:\n\n1. A failing-first case in `xtask/src/bundle.rs`'s existing `mod tests`: a scratch successor\n   declaring a two-step-back predecessor, refused by `check`, with the message naming both versions.\n2. A second case driving the same bundle through `run()` — the acceptance names\n   `cargo xtask check-bundle`, and a unit test on `check` is not that verb.\n3. The adjacency rule is derived from what is on disk under `contracts/substrate-wire/`, not from a\n   hard-coded table, so cutting `0.9.0` needs no edit here.\n4. `0.5.0` through `0.8.0` still verify unchanged, and `git status --porcelain contracts/` is empty\n   (invariant 6).\n\n## Open Questions\n\n`0.1.0` has no predecessor. Whether the first bundle is exempt by version or by the absence of any\nlower directory is undecided; both `0.2.0` and `0.3.0` pin `0.1.0` in their Python checkers\n(`scripts/check-contract-bundle-0.2.0.py:1528`), which suggests the rule already exists in prose for\nthe old bundles and was never carried into the Rust verb.\n","summary":"compatibility.predecessor is read from the bundle and never checked against released order, so an older predecessor hides every route added since it.","title":"The contract gate refuses a bundle that names a non-adjacent predecessor"},"args":{"command":"create-entity","data":{"body":"# Story: The contract gate refuses a bundle that names a non-adjacent predecessor\n\n## Outcome\n\nA successor bundle cannot make earlier routes invisible to the compatibility check by declaring an\nolder version as its predecessor.\n\n## Context\n\n`check_compatibility` reads `compatibility.predecessor` out of the bundle's own `bundle.json`\n(`xtask/src/bundle.rs:203-206`) and joins it straight onto `inputs.contracts_root`\n(`:216-219`). Nothing anywhere requires that string to name the adjacent released version:\n`grep -rn 'compatibility/predecessor\\|\"predecessor\"' xtask/src/bundle.rs` returns exactly one\nproduction site, `:204`. The per-version dispatch in `check_additions` (`:383-400`) branches on the\nbundle's *own* version and never on its predecessor; `PREDECESSOR: &str = \"0.7.0\"` at `:1258` is a\nconstant used by the file's own cases, not a released-order assertion.\n\nSo a `0.9.0` that declares `predecessor: \"0.5.0\"` is compared against `0.5.0`'s inventory. Every\nroute introduced in `0.6.0`, `0.7.0` and `0.8.0` becomes invisible to the drop check and — after\n`story:contract-gate-sees-route-paths` — to the moved-path check as well. The counts are not a\nbarrier: the bundle authors its own `adds_routes` and `preserves_routes`, so it declares the\ninflated numbers and they agree.\n\n## Provenance\n\nFound by `engineering-protocols:adversary` on 2026-08-30 while attacking\n`story:contract-gate-sees-route-paths`, reported as finding F6 and marked pre-existing — it is not\ncaused by that change. The two `file:line` claims above and the `grep` result were re-checked by the\ncoordinator against `wave/2026-08-30-byte-plane`, which does not carry that change.\n\n**Not observed as an exploit.** No bundle in `contracts/substrate-wire/` declares a non-adjacent\npredecessor, and no test drives one. What is verified is the *absence of a check*, not a failure of\none; nobody has rendered a bundle that takes this route.\n\n## Acceptance\n\nA bundle whose `compatibility.predecessor` is not the released version immediately below its own\nfails `cargo xtask check-bundle`, by name, and the failure says which version it named and which one\nit should have named.\n\nEvidence that satisfies it, in order:\n\n1. A failing-first case in `xtask/src/bundle.rs`'s existing `mod tests`: a scratch successor\n   declaring a two-step-back predecessor, refused by `check`, with the message naming both versions.\n2. A second case driving the same bundle through `run()` — the acceptance names\n   `cargo xtask check-bundle`, and a unit test on `check` is not that verb.\n3. The adjacency rule is derived from what is on disk under `contracts/substrate-wire/`, not from a\n   hard-coded table, so cutting `0.9.0` needs no edit here.\n4. `0.5.0` through `0.8.0` still verify unchanged, and `git status --porcelain contracts/` is empty\n   (invariant 6).\n\n## Open Questions\n\n`0.1.0` has no predecessor. Whether the first bundle is exempt by version or by the absence of any\nlower directory is undecided; both `0.2.0` and `0.3.0` pin `0.1.0` in their Python checkers\n(`scripts/check-contract-bundle-0.2.0.py:1528`), which suggests the rule already exists in prose for\nthe old bundles and was never carried into the Rust verb.\n","status":"draft","summary":"compatibility.predecessor is read from the bundle and never checked against released order, so an older predecessor hides every route added since it.","title":"The contract gate refuses a bundle that names a non-adjacent predecessor"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/contract-gate-predecessor-adjacency"},"payload":{"actor":"human:timo","at":1788165462137,"causation":"cmd-new-story-contract-gate-predecessor-adjacency","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:contract-gate-predecessor-adjacency@1#0~4082d30660aaba80","executor":null,"recorded_at":"2026-08-31T08:37:42Z"}}
{"entity":"story","version":1,"id":"contract-gate-predecessor-adjacency","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:release-hardening"}]},"args":{"command":"create-relation","kind":"decomposes","source":"01MEM0000000000000120","target":"01MEM0000000000000007"},"payload":{"actor":"human:timo","at":1788165462146,"causation":"cmd-story-contract-gate-predecessor-adjacency-decomposes-epic-release-hardening","change":{"change":"related","relation":"decomposes","target":"epic:release-hardening"},"correlation":"protocol-artifact-relate","event_id":"story:contract-gate-predecessor-adjacency@1#0~6a7b78d36d4f89b3","executor":null,"recorded_at":"2026-08-31T08:37:42Z"}}
{"entity":"story","version":1,"id":"file-routes-declare-a-template-the-daemon-does-not-serve","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Story: The file routes are served at a template the contract cannot declare\n\n## Outcome\n\n`operations.json` names the template the daemon actually registers, and a client can address every\nnested path the contract's own schema admits.\n\n## What is verified\n\nFour facts, each read from the tree at `wave/2026-08-30-byte-plane` by the coordinator:\n\n| | |\n|---|---|\n| the daemon registers | `/v1/workspaces/{workspace_id}/files/{*path}` — `crates/substrate-daemon/src/app/routes.rs:42`, and the `/v2/` form at `:48` |\n| every released bundle declares | `/v1/workspaces/{workspace_id}/files/{path}` — checked in `0.4.0` and `0.8.0` |\n| the renderer cannot render the daemon's form | `path_parameters` (`xtask/src/render.rs:653-662`) strips `{` and `}` and nothing else, so `{*path}` yields a property literally named `*path` and a `$ref` into `common.json` under that name, which resolves to nothing |\n| the schema expects nesting | `common.json` `$defs/relative-path` is a string with `maxLength: 4096` and `x-b10x-max-depth: 64`, whose pattern forbids a *leading* slash and any `..` segment — and therefore admits interior slashes |\n\nA matchit `{param}` matches one segment. So the contract declares a template that cannot express what\nits own schema admits: `a/b/c.txt` is a valid `relative-path`, the daemon serves it, and the registry\nsays the route takes a single segment.\n\n## Reported, not reproduced here\n\nFrom the fifth adversarial pass on `story:contract-gate-sees-route-paths`, driving real matchit\n0.8.4: a request for `/v1/workspaces/w1/files/a/b/c.txt` matches under `{*path}` and does not match\nunder `{path}`; and `check_classification` never notices the dangling reference, because address\ndocuments are registered and never compiled.\n\n## Why it cannot simply be corrected\n\n`xtask/src/render.rs` is effectively frozen: every released `bundle.json` records that file's sha256\nas `generator.digest`, so a change to it stops all eight released bundles being fixed points of their\nown sources at once. Teaching `path_parameters` about `*` is therefore not a one-line fix — it has to\nland together with whatever admits the released bundles afterwards, and that decision is not made\nanywhere in the tree.\n\nUntil then the gate refuses a wildcard segment by name — added under\n`story:contract-gate-sees-route-paths`, which found this. That refusal is a placeholder for this\nstory, not a resolution of it.\n\n## Acceptance\n\n`operations.json` declares the template the daemon registers, a client can address a nested path\nthrough the published address schema, and `cargo xtask check-bundle` verifies every released bundle\nunchanged.\n\nEvidence that satisfies it, in order:\n\n1. A recorded decision on how `generator.digest` survives a renderer change — the options visible\n   from here are pinning each released bundle to the renderer it was cut with, or re-recording the\n   digests as a deliberate one-off with the old bytes proven identical. Neither is chosen anywhere,\n   and this is the real content of the story.\n2. `path_parameters` handles a wildcard segment, generating a property under the bare name bound to\n   `relative-path`; written failing-first against the malformed document it produces today.\n3. `check_classification` compiles address documents, so a dangling reference in one is a failure —\n   that blind spot is what let this stay invisible.\n4. A successor bundle declaring the wildcard form, with the change from the single-segment template\n   treated as the widening it is: every URL the old template answered, the new one answers, at the\n   same operation.\n5. The `v2` route at `routes.rs:48` is covered by the same change or explicitly deferred.\n\n## Provenance\n\nFound by `engineering-protocols:adversary` on the fifth pass over\n`story:contract-gate-sees-route-paths`, 2026-08-30, while establishing that a false refusal in that\nunit was reachable. The four verified rows above were re-read by the coordinator; the matchit\nbehaviour and the `check_classification` blind spot are the adversary's, quoted.\n\n**Not observed as a live client failure.** Nobody has reported a nested-path request failing. What is\nestablished is that the registry and the router disagree, and that the request hash `hashing.json`\nnormalises against the registry template cannot match the URLs the daemon serves.\n","summary":"The daemon registers a wildcard file path and every bundle declares a single-segment one; the renderer cannot express the wildcard, and render.rs is frozen by generator.digest.","title":"The file routes are served at a template the contract cannot declare"},"args":{"command":"create-entity","data":{"body":"# Story: The file routes are served at a template the contract cannot declare\n\n## Outcome\n\n`operations.json` names the template the daemon actually registers, and a client can address every\nnested path the contract's own schema admits.\n\n## What is verified\n\nFour facts, each read from the tree at `wave/2026-08-30-byte-plane` by the coordinator:\n\n| | |\n|---|---|\n| the daemon registers | `/v1/workspaces/{workspace_id}/files/{*path}` — `crates/substrate-daemon/src/app/routes.rs:42`, and the `/v2/` form at `:48` |\n| every released bundle declares | `/v1/workspaces/{workspace_id}/files/{path}` — checked in `0.4.0` and `0.8.0` |\n| the renderer cannot render the daemon's form | `path_parameters` (`xtask/src/render.rs:653-662`) strips `{` and `}` and nothing else, so `{*path}` yields a property literally named `*path` and a `$ref` into `common.json` under that name, which resolves to nothing |\n| the schema expects nesting | `common.json` `$defs/relative-path` is a string with `maxLength: 4096` and `x-b10x-max-depth: 64`, whose pattern forbids a *leading* slash and any `..` segment — and therefore admits interior slashes |\n\nA matchit `{param}` matches one segment. So the contract declares a template that cannot express what\nits own schema admits: `a/b/c.txt` is a valid `relative-path`, the daemon serves it, and the registry\nsays the route takes a single segment.\n\n## Reported, not reproduced here\n\nFrom the fifth adversarial pass on `story:contract-gate-sees-route-paths`, driving real matchit\n0.8.4: a request for `/v1/workspaces/w1/files/a/b/c.txt` matches under `{*path}` and does not match\nunder `{path}`; and `check_classification` never notices the dangling reference, because address\ndocuments are registered and never compiled.\n\n## Why it cannot simply be corrected\n\n`xtask/src/render.rs` is effectively frozen: every released `bundle.json` records that file's sha256\nas `generator.digest`, so a change to it stops all eight released bundles being fixed points of their\nown sources at once. Teaching `path_parameters` about `*` is therefore not a one-line fix — it has to\nland together with whatever admits the released bundles afterwards, and that decision is not made\nanywhere in the tree.\n\nUntil then the gate refuses a wildcard segment by name — added under\n`story:contract-gate-sees-route-paths`, which found this. That refusal is a placeholder for this\nstory, not a resolution of it.\n\n## Acceptance\n\n`operations.json` declares the template the daemon registers, a client can address a nested path\nthrough the published address schema, and `cargo xtask check-bundle` verifies every released bundle\nunchanged.\n\nEvidence that satisfies it, in order:\n\n1. A recorded decision on how `generator.digest` survives a renderer change — the options visible\n   from here are pinning each released bundle to the renderer it was cut with, or re-recording the\n   digests as a deliberate one-off with the old bytes proven identical. Neither is chosen anywhere,\n   and this is the real content of the story.\n2. `path_parameters` handles a wildcard segment, generating a property under the bare name bound to\n   `relative-path`; written failing-first against the malformed document it produces today.\n3. `check_classification` compiles address documents, so a dangling reference in one is a failure —\n   that blind spot is what let this stay invisible.\n4. A successor bundle declaring the wildcard form, with the change from the single-segment template\n   treated as the widening it is: every URL the old template answered, the new one answers, at the\n   same operation.\n5. The `v2` route at `routes.rs:48` is covered by the same change or explicitly deferred.\n\n## Provenance\n\nFound by `engineering-protocols:adversary` on the fifth pass over\n`story:contract-gate-sees-route-paths`, 2026-08-30, while establishing that a false refusal in that\nunit was reachable. The four verified rows above were re-read by the coordinator; the matchit\nbehaviour and the `check_classification` blind spot are the adversary's, quoted.\n\n**Not observed as a live client failure.** Nobody has reported a nested-path request failing. What is\nestablished is that the registry and the router disagree, and that the request hash `hashing.json`\nnormalises against the registry template cannot match the URLs the daemon serves.\n","status":"draft","summary":"The daemon registers a wildcard file path and every bundle declares a single-segment one; the renderer cannot express the wildcard, and render.rs is frozen by generator.digest.","title":"The file routes are served at a template the contract cannot declare"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/file-routes-declare-a-template-the-daemon-does-not-serve"},"payload":{"actor":"human:timo","at":1788165462321,"causation":"cmd-new-story-file-routes-declare-a-template-the-daemon-does-not-serve","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:file-routes-declare-a-template-the-daemon-does-not-serve@1#0~ad15b53d7d69738c","executor":null,"recorded_at":"2026-08-31T08:37:42Z"}}
{"entity":"story","version":1,"id":"file-routes-declare-a-template-the-daemon-does-not-serve","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:release-hardening"}]},"args":{"command":"create-relation","kind":"decomposes","source":"01MEM0000000000000125","target":"01MEM0000000000000007"},"payload":{"actor":"human:timo","at":1788165462330,"causation":"cmd-story-file-routes-declare-a-template-the-daemon-does-not-serve-decomposes-epic-release-hardening","change":{"change":"related","relation":"decomposes","target":"epic:release-hardening"},"correlation":"protocol-artifact-relate","event_id":"story:file-routes-declare-a-template-the-daemon-does-not-serve@1#0~449a1255fa9f9f42","executor":null,"recorded_at":"2026-08-31T08:37:42Z"}}
{"entity":"story","version":1,"id":"full-review-git-recovery","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Story: Git and worktree recovery\n\n## Outcome\n\nThe repository has a clean object database; the relevant contract work is recovered; PTY remains a separate valid worktree; and no corrupt backup is deleted.\n\n## Acceptance\n\n- `git fsck --full` passes.\n- The contract-gate filesystem-only change is reconstructed and tested.\n- PTY retains its twelve commits and all pre-existing dirt in a separate worktree.\n- Recovery archives have SHA-256 manifests.\n","summary":"Corrupt local objects and unmerged worktrees are recovered without losing user work.","title":"The full-review work starts from a verified Git database"},"args":{"command":"create-entity","data":{"body":"# Story: Git and worktree recovery\n\n## Outcome\n\nThe repository has a clean object database; the relevant contract work is recovered; PTY remains a separate valid worktree; and no corrupt backup is deleted.\n\n## Acceptance\n\n- `git fsck --full` passes.\n- The contract-gate filesystem-only change is reconstructed and tested.\n- PTY retains its twelve commits and all pre-existing dirt in a separate worktree.\n- Recovery archives have SHA-256 manifests.\n","status":"draft","summary":"Corrupt local objects and unmerged worktrees are recovered without losing user work.","title":"The full-review work starts from a verified Git database"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/full-review-git-recovery"},"payload":{"actor":"human:timo","at":1788165462506,"causation":"cmd-new-story-full-review-git-recovery","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:full-review-git-recovery@1#0~837ac1294da4d2b2","executor":null,"recorded_at":"2026-08-31T08:37:42Z"}}
{"entity":"story","version":1,"id":"full-review-git-recovery","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:release-hardening"}]},"args":{"command":"create-relation","kind":"decomposes","source":"01MEM0000000000000130","target":"01MEM0000000000000007"},"payload":{"actor":"human:timo","at":1788165462514,"causation":"cmd-story-full-review-git-recovery-decomposes-epic-release-hardening","change":{"change":"related","relation":"decomposes","target":"epic:release-hardening"},"correlation":"protocol-artifact-relate","event_id":"story:full-review-git-recovery@1#0~0f8e5b2e6291d534","executor":null,"recorded_at":"2026-08-31T08:37:42Z"}}
{"entity":"story","version":1,"id":"confinement-runtime-hardening","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Story: Confinement and runtime hardening\n\n## Outcome\n\nHost roots cannot reach host IPC or shadow owned mounts; pipe, aperture, file and replay paths terminate and report facts without deadlock, unbounded allocation or optimistic degradation.\n\n## Acceptance\n\n- AF_UNIX host IPC is blocked and probe-verified.\n- Owned and overlapping mounts are refused component-wise.\n- Pipe queue saturation is a durable named terminal failure.\n- File reads remain bounded under concurrent growth.\n- Aperture cleanup, counters, source eviction and delegated tests are race-free.\n- Required delegated context is verified before replay.\n","summary":"Close host-root, pipe, file, aperture, replay and delegated-cgroup defects.","title":"Confinement and runtime paths fail closed under pressure and restart"},"args":{"command":"create-entity","data":{"body":"# Story: Confinement and runtime hardening\n\n## Outcome\n\nHost roots cannot reach host IPC or shadow owned mounts; pipe, aperture, file and replay paths terminate and report facts without deadlock, unbounded allocation or optimistic degradation.\n\n## Acceptance\n\n- AF_UNIX host IPC is blocked and probe-verified.\n- Owned and overlapping mounts are refused component-wise.\n- Pipe queue saturation is a durable named terminal failure.\n- File reads remain bounded under concurrent growth.\n- Aperture cleanup, counters, source eviction and delegated tests are race-free.\n- Required delegated context is verified before replay.\n","status":"draft","summary":"Close host-root, pipe, file, aperture, replay and delegated-cgroup defects.","title":"Confinement and runtime paths fail closed under pressure and restart"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/confinement-runtime-hardening"},"payload":{"actor":"human:timo","at":1788165462688,"causation":"cmd-new-story-confinement-runtime-hardening","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:confinement-runtime-hardening@1#0~88712d4def95061c","executor":null,"recorded_at":"2026-08-31T08:37:42Z"}}
{"entity":"story","version":1,"id":"confinement-runtime-hardening","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:byte-plane-completion"}]},"args":{"command":"create-relation","kind":"decomposes","source":"01MEM0000000000000135","target":"01MEM0000000000000001"},"payload":{"actor":"human:timo","at":1788165462698,"causation":"cmd-story-confinement-runtime-hardening-decomposes-epic-byte-plane-completion","change":{"change":"related","relation":"decomposes","target":"epic:byte-plane-completion"},"correlation":"protocol-artifact-relate","event_id":"story:confinement-runtime-hardening@1#0~6b2ac59ba43a0f83","executor":null,"recorded_at":"2026-08-31T08:37:42Z"}}
{"entity":"story","version":1,"id":"v2-contract-and-gate-hardening","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Story: V2 contracts and compatibility hardening\n\n## Outcome\n\nEvery served route is declared in a multi-major successor bundle and compatibility checks see paths, adjacency and transitive schema changes.\n\n## Acceptance\n\n- Five v2 routes are registered with api major 2 and v2 envelopes.\n- V1 bytes and all frozen bundles remain unchanged.\n- A versioned renderer supports catch-all paths without changing render.rs.\n- Adjacent predecessor, served paths and recursive ref closures are enforced.\n","summary":"Contract v2 workspace routes, wildcard paths, predecessor order and transitive schemas.","title":"The successor bundle declares every served API major and closes compatibility gaps"},"args":{"command":"create-entity","data":{"body":"# Story: V2 contracts and compatibility hardening\n\n## Outcome\n\nEvery served route is declared in a multi-major successor bundle and compatibility checks see paths, adjacency and transitive schema changes.\n\n## Acceptance\n\n- Five v2 routes are registered with api major 2 and v2 envelopes.\n- V1 bytes and all frozen bundles remain unchanged.\n- A versioned renderer supports catch-all paths without changing render.rs.\n- Adjacent predecessor, served paths and recursive ref closures are enforced.\n","status":"draft","summary":"Contract v2 workspace routes, wildcard paths, predecessor order and transitive schemas.","title":"The successor bundle declares every served API major and closes compatibility gaps"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/v2-contract-and-gate-hardening"},"payload":{"actor":"human:timo","at":1788165462873,"causation":"cmd-new-story-v2-contract-and-gate-hardening","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:v2-contract-and-gate-hardening@1#0~962711ea911f20c6","executor":null,"recorded_at":"2026-08-31T08:37:42Z"}}
{"entity":"story","version":1,"id":"v2-contract-and-gate-hardening","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:release-hardening"}]},"args":{"command":"create-relation","kind":"decomposes","source":"01MEM0000000000000140","target":"01MEM0000000000000007"},"payload":{"actor":"human:timo","at":1788165462883,"causation":"cmd-story-v2-contract-and-gate-hardening-decomposes-epic-release-hardening","change":{"change":"related","relation":"decomposes","target":"epic:release-hardening"},"correlation":"protocol-artifact-relate","event_id":"story:v2-contract-and-gate-hardening@1#0~a19861d18114ba71","executor":null,"recorded_at":"2026-08-31T08:37:42Z"}}
{"entity":"story","version":1,"id":"repository-gate-hardening","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Story: Repository gate hardening\n\n## Outcome\n\nThe full gate fails closed on corrupt history, secrets, advisories, publishable crates, incomplete Markdown links and non-Rust bot checks.\n\n## Acceptance\n\n- Secret scanning proves a non-empty, fsck-clean full history and exact commit count.\n- RustSec vulnerabilities fail the gate and h2 is absent.\n- All crates set publish=false.\n- CommonMark link forms are checked.\n- The bot-file checker is a cargo xtask verb.\n","summary":"Make every previously false-green repository check fail closed.","title":"The repository gate proves history, dependencies, links and publication fences"},"args":{"command":"create-entity","data":{"body":"# Story: Repository gate hardening\n\n## Outcome\n\nThe full gate fails closed on corrupt history, secrets, advisories, publishable crates, incomplete Markdown links and non-Rust bot checks.\n\n## Acceptance\n\n- Secret scanning proves a non-empty, fsck-clean full history and exact commit count.\n- RustSec vulnerabilities fail the gate and h2 is absent.\n- All crates set publish=false.\n- CommonMark link forms are checked.\n- The bot-file checker is a cargo xtask verb.\n","status":"draft","summary":"Make every previously false-green repository check fail closed.","title":"The repository gate proves history, dependencies, links and publication fences"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/repository-gate-hardening"},"payload":{"actor":"human:timo","at":1788165463058,"causation":"cmd-new-story-repository-gate-hardening","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:repository-gate-hardening@1#0~476a31e8f5cfd7f9","executor":null,"recorded_at":"2026-08-31T08:37:43Z"}}
{"entity":"story","version":1,"id":"repository-gate-hardening","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:release-hardening"}]},"args":{"command":"create-relation","kind":"decomposes","source":"01MEM0000000000000145","target":"01MEM0000000000000007"},"payload":{"actor":"human:timo","at":1788165463065,"causation":"cmd-story-repository-gate-hardening-decomposes-epic-release-hardening","change":{"change":"related","relation":"decomposes","target":"epic:release-hardening"},"correlation":"protocol-artifact-relate","event_id":"story:repository-gate-hardening@1#0~8a5b4b9e3e67f708","executor":null,"recorded_at":"2026-08-31T08:37:43Z"}}
{"entity":"story","version":1,"id":"release-publication-hardening","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Story: Release publication hardening\n\n## Outcome\n\nThe exact nonroot image tested by the gate is published once, anonymously retrievable, signed, immutable and truthfully summarized.\n\n## Acceptance\n\n- Fresh volumes satisfy the state-root ownership invariant.\n- Release-mode vectors exercise the exact image binary.\n- Existing release or image tags are never overwritten.\n- Anonymous pull and signature verification precede a draft release publication.\n- Failed or skipped steps never produce a success claim.\n- Changelog updates travel through a gated PR.\n","summary":"Harden GHCR visibility, image layout, tag races, summaries and changelog flow.","title":"Daemon releases are public, immutable, exact-binary tested and non-overwriting"},"args":{"command":"create-entity","data":{"body":"# Story: Release publication hardening\n\n## Outcome\n\nThe exact nonroot image tested by the gate is published once, anonymously retrievable, signed, immutable and truthfully summarized.\n\n## Acceptance\n\n- Fresh volumes satisfy the state-root ownership invariant.\n- Release-mode vectors exercise the exact image binary.\n- Existing release or image tags are never overwritten.\n- Anonymous pull and signature verification precede a draft release publication.\n- Failed or skipped steps never produce a success claim.\n- Changelog updates travel through a gated PR.\n","status":"draft","summary":"Harden GHCR visibility, image layout, tag races, summaries and changelog flow.","title":"Daemon releases are public, immutable, exact-binary tested and non-overwriting"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/release-publication-hardening"},"payload":{"actor":"human:timo","at":1788165463241,"causation":"cmd-new-story-release-publication-hardening","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:release-publication-hardening@1#0~5b665f377ac20d8f","executor":null,"recorded_at":"2026-08-31T08:37:43Z"}}
{"entity":"story","version":1,"id":"release-publication-hardening","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:release-hardening"}]},"args":{"command":"create-relation","kind":"decomposes","source":"01MEM0000000000000150","target":"01MEM0000000000000007"},"payload":{"actor":"human:timo","at":1788165463250,"causation":"cmd-story-release-publication-hardening-decomposes-epic-release-hardening","change":{"change":"related","relation":"decomposes","target":"epic:release-hardening"},"correlation":"protocol-artifact-relate","event_id":"story:release-publication-hardening@1#0~cc647b2b4920bce2","executor":null,"recorded_at":"2026-08-31T08:37:43Z"}}
{"entity":"story","version":1,"id":"aperture-install-failure-loses-its-errno","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000016","to":"proposed"},"payload":{"actor":"human:timo","at":1788165463426,"causation":"cmd-move-story-aperture-install-failure-loses-its-errno-1788165463426","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:aperture-install-failure-loses-its-errno@2#0~c531405e856b932f","executor":null,"recorded_at":"2026-08-31T08:37:43Z"}}
{"entity":"story","version":1,"id":"aperture-install-failure-loses-its-errno","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000016","to":"active"},"payload":{"actor":"human:timo","at":1788165463439,"causation":"cmd-move-story-aperture-install-failure-loses-its-errno-1788165463439","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:aperture-install-failure-loses-its-errno@3#0~c833b53be4960cdf","executor":null,"recorded_at":"2026-08-31T08:37:43Z"}}
{"entity":"story","version":1,"id":"contract-gate-predecessor-adjacency","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000028","to":"proposed"},"payload":{"actor":"human:timo","at":1788165463615,"causation":"cmd-move-story-contract-gate-predecessor-adjacency-1788165463615","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:contract-gate-predecessor-adjacency@2#0~36bd28f03013dbfa","executor":null,"recorded_at":"2026-08-31T08:37:43Z"}}
{"entity":"story","version":1,"id":"contract-gate-predecessor-adjacency","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000028","to":"active"},"payload":{"actor":"human:timo","at":1788165463624,"causation":"cmd-move-story-contract-gate-predecessor-adjacency-1788165463624","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:contract-gate-predecessor-adjacency@3#0~0bc80c40ef8e4bee","executor":null,"recorded_at":"2026-08-31T08:37:43Z"}}
{"entity":"story","version":1,"id":"file-routes-declare-a-template-the-daemon-does-not-serve","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000043","to":"proposed"},"payload":{"actor":"human:timo","at":1788165463802,"causation":"cmd-move-story-file-routes-declare-a-template-the-daemon-does-not-serve-1788165463802","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:file-routes-declare-a-template-the-daemon-does-not-serve@2#0~452cec5bb2cf4d41","executor":null,"recorded_at":"2026-08-31T08:37:43Z"}}
{"entity":"story","version":1,"id":"file-routes-declare-a-template-the-daemon-does-not-serve","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000043","to":"active"},"payload":{"actor":"human:timo","at":1788165463812,"causation":"cmd-move-story-file-routes-declare-a-template-the-daemon-does-not-serve-1788165463812","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:file-routes-declare-a-template-the-daemon-does-not-serve@3#0~f22f644cdebc4ca9","executor":null,"recorded_at":"2026-08-31T08:37:43Z"}}
{"entity":"story","version":1,"id":"full-review-git-recovery","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000046","to":"proposed"},"payload":{"actor":"human:timo","at":1788165463989,"causation":"cmd-move-story-full-review-git-recovery-1788165463989","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:full-review-git-recovery@2#0~6454e9f49733dcce","executor":null,"recorded_at":"2026-08-31T08:37:43Z"}}
{"entity":"story","version":1,"id":"full-review-git-recovery","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000046","to":"active"},"payload":{"actor":"human:timo","at":1788165463997,"causation":"cmd-move-story-full-review-git-recovery-1788165463997","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:full-review-git-recovery@3#0~875da470df07212a","executor":null,"recorded_at":"2026-08-31T08:37:43Z"}}
{"entity":"story","version":1,"id":"confinement-runtime-hardening","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000022","to":"proposed"},"payload":{"actor":"human:timo","at":1788165464174,"causation":"cmd-move-story-confinement-runtime-hardening-1788165464174","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:confinement-runtime-hardening@2#0~b5cf5c548f340204","executor":null,"recorded_at":"2026-08-31T08:37:44Z"}}
{"entity":"story","version":1,"id":"confinement-runtime-hardening","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000022","to":"active"},"payload":{"actor":"human:timo","at":1788165464184,"causation":"cmd-move-story-confinement-runtime-hardening-1788165464184","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:confinement-runtime-hardening@3#0~a8aa01b813a951a8","executor":null,"recorded_at":"2026-08-31T08:37:44Z"}}
{"entity":"story","version":1,"id":"v2-contract-and-gate-hardening","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000082","to":"proposed"},"payload":{"actor":"human:timo","at":1788165464362,"causation":"cmd-move-story-v2-contract-and-gate-hardening-1788165464362","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:v2-contract-and-gate-hardening@2#0~c176db20585c2096","executor":null,"recorded_at":"2026-08-31T08:37:44Z"}}
{"entity":"story","version":1,"id":"v2-contract-and-gate-hardening","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000082","to":"active"},"payload":{"actor":"human:timo","at":1788165464369,"causation":"cmd-move-story-v2-contract-and-gate-hardening-1788165464369","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:v2-contract-and-gate-hardening@3#0~37be4bd64ba4e5e2","executor":null,"recorded_at":"2026-08-31T08:37:44Z"}}
{"entity":"story","version":1,"id":"repository-gate-hardening","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000064","to":"proposed"},"payload":{"actor":"human:timo","at":1788165464547,"causation":"cmd-move-story-repository-gate-hardening-1788165464547","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:repository-gate-hardening@2#0~60376ba333507a6a","executor":null,"recorded_at":"2026-08-31T08:37:44Z"}}
{"entity":"story","version":1,"id":"repository-gate-hardening","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000064","to":"active"},"payload":{"actor":"human:timo","at":1788165464555,"causation":"cmd-move-story-repository-gate-hardening-1788165464555","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:repository-gate-hardening@3#0~ce5da4ae628174de","executor":null,"recorded_at":"2026-08-31T08:37:44Z"}}
{"entity":"story","version":1,"id":"release-publication-hardening","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000061","to":"proposed"},"payload":{"actor":"human:timo","at":1788165464732,"causation":"cmd-move-story-release-publication-hardening-1788165464732","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:release-publication-hardening@2#0~cf30e604c830bf45","executor":null,"recorded_at":"2026-08-31T08:37:44Z"}}
{"entity":"story","version":1,"id":"release-publication-hardening","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000061","to":"active"},"payload":{"actor":"human:timo","at":1788165464743,"causation":"cmd-move-story-release-publication-hardening-1788165464743","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:release-publication-hardening@3#0~ac5ae5261d286325","executor":null,"recorded_at":"2026-08-31T08:37:44Z"}}
{"entity":"story","version":1,"id":"apache-publication-and-pages-hardening","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Story: Substrate is Apache-2.0 with a hardened public distribution\n\n## Outcome\n\nA reader can use Substrate under Apache-2.0, find its intentionally public source and private\nsecurity-reporting path, build trustworthy public documentation, and pull a signed daemon image\nwithout authentication.\n\n## Context\n\nAtlas ADR 0010 grants Apache-2.0 across all beyond10x-owned Substrate history without rewriting any\nfrozen contract bytes. The repository and Pages site are already public, but GitHub secret scanning,\npush protection, private vulnerability reporting and Dependabot security updates are disabled; the\nGHCR image is not anonymously readable; and the website build has known transitive advisories.\n\n## Acceptance\n\n1. Root licence and Cargo metadata say Apache-2.0; third-party material keeps its own licence and the\n   daemon image carries deterministic notices.\n2. The full reachable history and every worktree intended to land pass the pinned secret scan; the\n   synthetic Axum WebSocket fixture needs no broader allowlist.\n3. GitHub secret protections, private reporting, dependency updates and action SHA pinning are\n   enabled, and anonymous clients can pull the signed daemon image.\n4. The public site builds from locked, audit-clean dependencies, documents only implemented behavior,\n   and links only the deliberately public repository, licence and security-reporting destinations.\n5. The complete repository gate, delegated lane, exact image tests and Pages deployment pass.\n\n## Out of Scope\n\nThis does not make a development wire bundle stable, publish crates to crates.io, promise a support\nSLA, or publish internal designs, ADRs, plans, reviews, work logs or contributor status material.\n\n## Open Questions\n\nNone. The operator selected Apache-2.0 for the complete beyond10x-owned history and selected staged\nlanding with explicit source and security links.\n","owner":"substrate","summary":"Relicense beyond10x-owned history, expose a safe public support surface, and gate the source, site and image for public use.","tags":["release","security","website"],"title":"Substrate is Apache-2.0 with a hardened public distribution"},"args":{"command":"create-entity","data":{"body":"# Story: Substrate is Apache-2.0 with a hardened public distribution\n\n## Outcome\n\nA reader can use Substrate under Apache-2.0, find its intentionally public source and private\nsecurity-reporting path, build trustworthy public documentation, and pull a signed daemon image\nwithout authentication.\n\n## Context\n\nAtlas ADR 0010 grants Apache-2.0 across all beyond10x-owned Substrate history without rewriting any\nfrozen contract bytes. The repository and Pages site are already public, but GitHub secret scanning,\npush protection, private vulnerability reporting and Dependabot security updates are disabled; the\nGHCR image is not anonymously readable; and the website build has known transitive advisories.\n\n## Acceptance\n\n1. Root licence and Cargo metadata say Apache-2.0; third-party material keeps its own licence and the\n   daemon image carries deterministic notices.\n2. The full reachable history and every worktree intended to land pass the pinned secret scan; the\n   synthetic Axum WebSocket fixture needs no broader allowlist.\n3. GitHub secret protections, private reporting, dependency updates and action SHA pinning are\n   enabled, and anonymous clients can pull the signed daemon image.\n4. The public site builds from locked, audit-clean dependencies, documents only implemented behavior,\n   and links only the deliberately public repository, licence and security-reporting destinations.\n5. The complete repository gate, delegated lane, exact image tests and Pages deployment pass.\n\n## Out of Scope\n\nThis does not make a development wire bundle stable, publish crates to crates.io, promise a support\nSLA, or publish internal designs, ADRs, plans, reviews, work logs or contributor status material.\n\n## Open Questions\n\nNone. The operator selected Apache-2.0 for the complete beyond10x-owned history and selected staged\nlanding with explicit source and security links.\n","owner":"substrate","status":"draft","summary":"Relicense beyond10x-owned history, expose a safe public support surface, and gate the source, site and image for public use.","tags":["release","security","website"],"title":"Substrate is Apache-2.0 with a hardened public distribution"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/apache-publication-and-pages-hardening"},"payload":{"actor":"human:timo","at":1788175094010,"causation":"cmd-new-story-apache-publication-and-pages-hardening","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:apache-publication-and-pages-hardening@1#0~12e38af003542b7f","executor":null,"recorded_at":"2026-08-31T11:18:14Z"}}
{"entity":"story","version":1,"id":"apache-publication-and-pages-hardening","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:release-hardening"}]},"args":{"command":"create-relation","kind":"decomposes","source":"01MEM0000000000000155","target":"01MEM0000000000000007"},"payload":{"actor":"human:timo","at":1788175094181,"causation":"cmd-story-apache-publication-and-pages-hardening-decomposes-epic-release-hardening","change":{"change":"related","relation":"decomposes","target":"epic:release-hardening"},"correlation":"protocol-artifact-relate","event_id":"story:apache-publication-and-pages-hardening@1#0~84379fd06ba63fd3","executor":null,"recorded_at":"2026-08-31T11:18:14Z"}}
{"entity":"epic","version":1,"id":"resource-bounded-execution","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Outcome\n\nSubstrate runs ordinary argv-based workloads with hard byte and inode ceilings on every persistent or disk-backed writable surface, reports exact kernel observations during and after the run, and teaches a public reader how to use those guarantees without implying a scheduler or policy engine.\n\n## Boundaries\n\nThis epic adds host-driver capability and development-contract behavior. It does not add product policy, fleet scheduling, mean-memory estimates, Prometheus exposition, a stable bundle release, or a contract-header migration.\n","owner":"substrate","summary":"Hard persistent and ephemeral storage ceilings, exact execution accounting, and public runnable examples for arbitrary confined commands.","tags":["confinement","o1","observability"],"title":"Resource-bounded general execution"},"args":{"command":"create-entity","data":{"body":"## Outcome\n\nSubstrate runs ordinary argv-based workloads with hard byte and inode ceilings on every persistent or disk-backed writable surface, reports exact kernel observations during and after the run, and teaches a public reader how to use those guarantees without implying a scheduler or policy engine.\n\n## Boundaries\n\nThis epic adds host-driver capability and development-contract behavior. It does not add product policy, fleet scheduling, mean-memory estimates, Prometheus exposition, a stable bundle release, or a contract-header migration.\n","owner":"substrate","status":"draft","summary":"Hard persistent and ephemeral storage ceilings, exact execution accounting, and public runnable examples for arbitrary confined commands.","tags":["confinement","o1","observability"],"title":"Resource-bounded general execution"},"entity_type":"aep.epic/v1","locator":"ep://planning/store/epic/resource-bounded-execution"},"payload":{"actor":"human:timo","at":1788205965484,"causation":"cmd-new-epic-resource-bounded-execution","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"epic:resource-bounded-execution@1#0~7cc9492d838b74f3","executor":null,"recorded_at":"2026-08-31T19:52:45Z"}}
{"entity":"story","version":1,"id":"workspace-and-scratch-quotas","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Intent\n\nAdd optional persistent workspace and ephemeral exec-scratch limits. The host advertises the capability only after proving project-quota byte, inode and inheritance enforcement over an operator-reserved project-id range.\n\n## Design prerequisite\n\nImplementation follows `docs/design/17-resource-accounting-and-storage-quotas.md` and ADR 0020. Frozen bundles remain untouched; bundle 0.11.0 is the successor.\n\n## Acceptance\n\nQuota identity is durable before dispatch, unsupported hosts refuse by name, concurrent writers share the same hard workspace ceiling, scratch is mounted only at `/scratch`, and allocation identities are reused only after bounded cleanup proves absence and zero usage.\n","owner":"substrate","summary":"Quota-enabled hosts enforce declared byte and inode ceilings on persistent workspaces and per-exec /scratch without scan-based approximation.","tags":["host","o1","wire"],"title":"Workspaces and exec scratch carry hard storage quotas"},"args":{"command":"create-entity","data":{"body":"## Intent\n\nAdd optional persistent workspace and ephemeral exec-scratch limits. The host advertises the capability only after proving project-quota byte, inode and inheritance enforcement over an operator-reserved project-id range.\n\n## Design prerequisite\n\nImplementation follows `docs/design/17-resource-accounting-and-storage-quotas.md` and ADR 0020. Frozen bundles remain untouched; bundle 0.11.0 is the successor.\n\n## Acceptance\n\nQuota identity is durable before dispatch, unsupported hosts refuse by name, concurrent writers share the same hard workspace ceiling, scratch is mounted only at `/scratch`, and allocation identities are reused only after bounded cleanup proves absence and zero usage.\n","owner":"substrate","status":"draft","summary":"Quota-enabled hosts enforce declared byte and inode ceilings on persistent workspaces and per-exec /scratch without scan-based approximation.","tags":["host","o1","wire"],"title":"Workspaces and exec scratch carry hard storage quotas"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/workspace-and-scratch-quotas"},"payload":{"actor":"human:timo","at":1788205965669,"causation":"cmd-new-story-workspace-and-scratch-quotas","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:workspace-and-scratch-quotas@1#0~2e6fb867fa9fa0c3","executor":null,"recorded_at":"2026-08-31T19:52:45Z"}}
{"entity":"story","version":1,"id":"workspace-and-scratch-quotas","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:resource-bounded-execution"}]},"args":{"command":"create-relation","kind":"decomposes","source":"01MEM0000000000000163","target":"01MEM0000000000000010"},"payload":{"actor":"human:timo","at":1788205965678,"causation":"cmd-story-workspace-and-scratch-quotas-decomposes-epic-resource-bounded-execution","change":{"change":"related","relation":"decomposes","target":"epic:resource-bounded-execution"},"correlation":"protocol-artifact-relate","event_id":"story:workspace-and-scratch-quotas@1#0~1c706b678c9b0b0c","executor":null,"recorded_at":"2026-08-31T19:52:45Z"}}
{"entity":"story","version":1,"id":"execution-resource-metrics","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Intent\n\nAdd explicit per-exec resource measurement, a read route and a latest-wins live stream. The terminal observation is durable; mean memory and retained time series stay absent because neither is an exact kernel terminal fact.\n\n## Design prerequisite\n\nImplementation follows `docs/design/17-resource-accounting-and-storage-quotas.md` and ADR 0021. Frozen bundles remain untouched; bundle 0.11.0 is the successor.\n\n## Acceptance\n\nLegacy request bytes remain unchanged, live observation cannot backpressure the run, terminal counters are captured before cgroup cleanup, restart gaps are named rather than filled, and timeout, CPU and OOM termination have distinct recorded refusals.\n","owner":"substrate","summary":"Opted-in runs expose monotonic wall time and kernel CPU, memory, process and I/O observations through GET and latest-wins WebSocket delivery.","tags":["daemon","o1","wire"],"title":"Execs expose exact live and terminal resource usage"},"args":{"command":"create-entity","data":{"body":"## Intent\n\nAdd explicit per-exec resource measurement, a read route and a latest-wins live stream. The terminal observation is durable; mean memory and retained time series stay absent because neither is an exact kernel terminal fact.\n\n## Design prerequisite\n\nImplementation follows `docs/design/17-resource-accounting-and-storage-quotas.md` and ADR 0021. Frozen bundles remain untouched; bundle 0.11.0 is the successor.\n\n## Acceptance\n\nLegacy request bytes remain unchanged, live observation cannot backpressure the run, terminal counters are captured before cgroup cleanup, restart gaps are named rather than filled, and timeout, CPU and OOM termination have distinct recorded refusals.\n","owner":"substrate","status":"draft","summary":"Opted-in runs expose monotonic wall time and kernel CPU, memory, process and I/O observations through GET and latest-wins WebSocket delivery.","tags":["daemon","o1","wire"],"title":"Execs expose exact live and terminal resource usage"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/execution-resource-metrics"},"payload":{"actor":"human:timo","at":1788205965854,"causation":"cmd-new-story-execution-resource-metrics","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:execution-resource-metrics@1#0~fe20beea27d352b5","executor":null,"recorded_at":"2026-08-31T19:52:45Z"}}
{"entity":"story","version":1,"id":"execution-resource-metrics","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:resource-bounded-execution"}]},"args":{"command":"create-relation","kind":"decomposes","source":"01MEM0000000000000168","target":"01MEM0000000000000010"},"payload":{"actor":"human:timo","at":1788205965863,"causation":"cmd-story-execution-resource-metrics-decomposes-epic-resource-bounded-execution","change":{"change":"related","relation":"decomposes","target":"epic:resource-bounded-execution"},"correlation":"protocol-artifact-relate","event_id":"story:execution-resource-metrics@1#0~4e955e3864ae5a6f","executor":null,"recorded_at":"2026-08-31T19:52:45Z"}}
{"entity":"story","version":1,"id":"public-practical-execution-docs","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"## Intent\n\nTurn the public site from a primarily conceptual overview into a hands-on guide for platform engineers while retaining the public projection boundary.\n\n## Acceptance\n\nThe site covers agent execution, build and test workers, converters, plugin runners, restricted automation and interactive terminals; every command matches the development contract; shipped, development and absent behavior remain visually distinct; and no internal design, ADR, planning or review material is published or linked.\n","owner":"substrate","summary":"Runnable terminal guides show arbitrary commands, storage and process bounds, output recovery and exact resource observations for agent and non-agent workloads.","tags":["docs","o1","website"],"title":"The public site teaches practical confined execution"},"args":{"command":"create-entity","data":{"body":"## Intent\n\nTurn the public site from a primarily conceptual overview into a hands-on guide for platform engineers while retaining the public projection boundary.\n\n## Acceptance\n\nThe site covers agent execution, build and test workers, converters, plugin runners, restricted automation and interactive terminals; every command matches the development contract; shipped, development and absent behavior remain visually distinct; and no internal design, ADR, planning or review material is published or linked.\n","owner":"substrate","status":"draft","summary":"Runnable terminal guides show arbitrary commands, storage and process bounds, output recovery and exact resource observations for agent and non-agent workloads.","tags":["docs","o1","website"],"title":"The public site teaches practical confined execution"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/public-practical-execution-docs"},"payload":{"actor":"human:timo","at":1788205966039,"causation":"cmd-new-story-public-practical-execution-docs","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:public-practical-execution-docs@1#0~9172bb13fba18bc1","executor":null,"recorded_at":"2026-08-31T19:52:46Z"}}
{"entity":"story","version":1,"id":"public-practical-execution-docs","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:resource-bounded-execution"}]},"args":{"command":"create-relation","kind":"decomposes","source":"01MEM0000000000000173","target":"01MEM0000000000000010"},"payload":{"actor":"human:timo","at":1788205966049,"causation":"cmd-story-public-practical-execution-docs-decomposes-epic-resource-bounded-execution","change":{"change":"related","relation":"decomposes","target":"epic:resource-bounded-execution"},"correlation":"protocol-artifact-relate","event_id":"story:public-practical-execution-docs@1#0~75dae23ba1933d47","executor":null,"recorded_at":"2026-08-31T19:52:46Z"}}
{"entity":"epic","version":1,"id":"resource-bounded-execution","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000010","to":"proposed"},"payload":{"actor":"human:timo","at":1788205966230,"causation":"cmd-move-epic-resource-bounded-execution-1788205966230","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"epic:resource-bounded-execution@2#0~0579e912cb63b379","executor":null,"recorded_at":"2026-08-31T19:52:46Z"}}
{"entity":"epic","version":1,"id":"resource-bounded-execution","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000010","to":"active"},"payload":{"actor":"human:timo","at":1788205966238,"causation":"cmd-move-epic-resource-bounded-execution-1788205966238","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"epic:resource-bounded-execution@3#0~647b0407ee3c1771","executor":null,"recorded_at":"2026-08-31T19:52:46Z"}}
{"entity":"story","version":1,"id":"workspace-and-scratch-quotas","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000097","to":"proposed"},"payload":{"actor":"human:timo","at":1788205966418,"causation":"cmd-move-story-workspace-and-scratch-quotas-1788205966418","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:workspace-and-scratch-quotas@2#0~0863d3a3540daea0","executor":null,"recorded_at":"2026-08-31T19:52:46Z"}}
{"entity":"story","version":1,"id":"workspace-and-scratch-quotas","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000097","to":"active"},"payload":{"actor":"human:timo","at":1788205966429,"causation":"cmd-move-story-workspace-and-scratch-quotas-1788205966429","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:workspace-and-scratch-quotas@3#0~025921c04218a40c","executor":null,"recorded_at":"2026-08-31T19:52:46Z"}}
{"entity":"story","version":1,"id":"execution-resource-metrics","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000049","to":"proposed"},"payload":{"actor":"human:timo","at":1788205966608,"causation":"cmd-move-story-execution-resource-metrics-1788205966608","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:execution-resource-metrics@2#0~ad137e1b66055433","executor":null,"recorded_at":"2026-08-31T19:52:46Z"}}
{"entity":"story","version":1,"id":"execution-resource-metrics","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000049","to":"active"},"payload":{"actor":"human:timo","at":1788205966618,"causation":"cmd-move-story-execution-resource-metrics-1788205966618","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:execution-resource-metrics@3#0~c64d49778958ac1b","executor":null,"recorded_at":"2026-08-31T19:52:46Z"}}
{"entity":"story","version":1,"id":"public-practical-execution-docs","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000070","to":"proposed"},"payload":{"actor":"human:timo","at":1788205966798,"causation":"cmd-move-story-public-practical-execution-docs-1788205966798","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:public-practical-execution-docs@2#0~0710c15da85fdf8f","executor":null,"recorded_at":"2026-08-31T19:52:46Z"}}
{"entity":"story","version":1,"id":"public-practical-execution-docs","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000070","to":"active"},"payload":{"actor":"human:timo","at":1788205966806,"causation":"cmd-move-story-public-practical-execution-docs-1788205966806","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:public-practical-execution-docs@3#0~6ea9aeb43e6711bf","executor":null,"recorded_at":"2026-08-31T19:52:46Z"}}
{"entity":"epic","version":1,"id":"rust-sdk","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Epic: Public Rust SDK\n\n## Outcome\n\nRust consumers create, control and observe confined workspaces and processes through an ergonomic, owner-released SDK. A managed local deployment is still a separate authenticated daemon child.\n\n## Boundaries\n\nThe SDK serves the daemon-advertised `substrate-wire/0.4.0` surface over an owner-private Unix socket. It chooses no product execution policy, exposes no driver API, and does not make the development contract stable.\n\n## Delivery\n\nTyped client and recovery, managed daemon ownership, approved package publication, and public adoption documentation are separate stories under this epic.\n","owner":"substrate","summary":"A typed Rust client makes governed workspace and process execution straightforward without collapsing the daemon boundary.","tags":["rust","sdk"],"title":"Public Rust SDK"},"args":{"command":"create-entity","data":{"body":"# Epic: Public Rust SDK\n\n## Outcome\n\nRust consumers create, control and observe confined workspaces and processes through an ergonomic, owner-released SDK. A managed local deployment is still a separate authenticated daemon child.\n\n## Boundaries\n\nThe SDK serves the daemon-advertised `substrate-wire/0.4.0` surface over an owner-private Unix socket. It chooses no product execution policy, exposes no driver API, and does not make the development contract stable.\n\n## Delivery\n\nTyped client and recovery, managed daemon ownership, approved package publication, and public adoption documentation are separate stories under this epic.\n","owner":"substrate","status":"draft","summary":"A typed Rust client makes governed workspace and process execution straightforward without collapsing the daemon boundary.","tags":["rust","sdk"],"title":"Public Rust SDK"},"entity_type":"aep.epic/v1","locator":"ep://planning/store/epic/rust-sdk"},"payload":{"actor":"human:timo","at":1788207602009,"causation":"cmd-new-epic-rust-sdk","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"epic:rust-sdk@1#0~9bbd81e41a7a9633","executor":null,"recorded_at":"2026-08-31T20:20:02Z"}}
{"entity":"story","version":1,"id":"rust-sdk-client","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Story: The Rust SDK exposes typed workspace and execution handles\n\n## Outcome\n\nA Rust caller connects over a Unix socket, verifies the advertised contract, creates an empty workspace, performs bounded file operations, runs or starts an argv-only process, observes output and events, and reconciles an unanswered mutation without assembling HTTP or JSON manually.\n\n## Acceptance\n\nThe SDK owns its public models, requires an explicit execution policy, preserves the daemon refusal taxonomy, reuses one caller-minted operation id across ambiguous transport recovery, and has no public escape hatch to the host driver or daemon application.\n","owner":"substrate","summary":"Builders, typed handles and ledger-aware recovery cover the daemon-advertised core contract.","tags":["sdk"],"title":"The Rust SDK exposes typed workspace and execution handles"},"args":{"command":"create-entity","data":{"body":"# Story: The Rust SDK exposes typed workspace and execution handles\n\n## Outcome\n\nA Rust caller connects over a Unix socket, verifies the advertised contract, creates an empty workspace, performs bounded file operations, runs or starts an argv-only process, observes output and events, and reconciles an unanswered mutation without assembling HTTP or JSON manually.\n\n## Acceptance\n\nThe SDK owns its public models, requires an explicit execution policy, preserves the daemon refusal taxonomy, reuses one caller-minted operation id across ambiguous transport recovery, and has no public escape hatch to the host driver or daemon application.\n","owner":"substrate","status":"draft","summary":"Builders, typed handles and ledger-aware recovery cover the daemon-advertised core contract.","tags":["sdk"],"title":"The Rust SDK exposes typed workspace and execution handles"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/rust-sdk-client"},"payload":{"actor":"human:timo","at":1788207602215,"causation":"cmd-new-story-rust-sdk-client","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:rust-sdk-client@1#0~d6cac507ff6fc1be","executor":null,"recorded_at":"2026-08-31T20:20:02Z"}}
{"entity":"story","version":1,"id":"rust-sdk-client","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:rust-sdk"}]},"args":{"command":"create-relation","kind":"decomposes","source":"01MEM0000000000000181","target":"01MEM0000000000000013"},"payload":{"actor":"human:timo","at":1788207602224,"causation":"cmd-story-rust-sdk-client-decomposes-epic-rust-sdk","change":{"change":"related","relation":"decomposes","target":"epic:rust-sdk"},"correlation":"protocol-artifact-relate","event_id":"story:rust-sdk-client@1#0~555c6f390a934019","executor":null,"recorded_at":"2026-08-31T20:20:02Z"}}
{"entity":"story","version":1,"id":"rust-sdk-managed-daemon","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Story: The Rust SDK owns a private daemon child\n\n## Outcome\n\nA caller supplies a durable data directory and deployment id, then receives a client connected to a private daemon child. External-binary and opt-in linked-current-executable modes have the same socket contract and lifecycle.\n\n## Acceptance\n\nThe child has a separate pid, admits only the invoking effective uid, proves readiness through `/v1/machine`, retains durable data, stops on explicit shutdown or owner loss, and is force-killed and reaped after a bounded grace period. Linked mode re-executes the application; it never executes a request in process.\n","owner":"substrate","summary":"External and linked-current-executable builders start, verify, stop and reap a separate daemon process.","tags":["sdk"],"title":"The Rust SDK owns a private daemon child"},"args":{"command":"create-entity","data":{"body":"# Story: The Rust SDK owns a private daemon child\n\n## Outcome\n\nA caller supplies a durable data directory and deployment id, then receives a client connected to a private daemon child. External-binary and opt-in linked-current-executable modes have the same socket contract and lifecycle.\n\n## Acceptance\n\nThe child has a separate pid, admits only the invoking effective uid, proves readiness through `/v1/machine`, retains durable data, stops on explicit shutdown or owner loss, and is force-killed and reaped after a bounded grace period. Linked mode re-executes the application; it never executes a request in process.\n","owner":"substrate","status":"draft","summary":"External and linked-current-executable builders start, verify, stop and reap a separate daemon process.","tags":["sdk"],"title":"The Rust SDK owns a private daemon child"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/rust-sdk-managed-daemon"},"payload":{"actor":"human:timo","at":1788207602403,"causation":"cmd-new-story-rust-sdk-managed-daemon","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:rust-sdk-managed-daemon@1#0~fbeac4e5ee65b64d","executor":null,"recorded_at":"2026-08-31T20:20:02Z"}}
{"entity":"story","version":1,"id":"rust-sdk-managed-daemon","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:rust-sdk"}]},"args":{"command":"create-relation","kind":"decomposes","source":"01MEM0000000000000186","target":"01MEM0000000000000013"},"payload":{"actor":"human:timo","at":1788207602411,"causation":"cmd-story-rust-sdk-managed-daemon-decomposes-epic-rust-sdk","change":{"change":"related","relation":"decomposes","target":"epic:rust-sdk"},"correlation":"protocol-artifact-relate","event_id":"story:rust-sdk-managed-daemon@1#0~47062ac1f44b2e12","executor":null,"recorded_at":"2026-08-31T20:20:02Z"}}
{"entity":"story","version":1,"id":"rust-sdk-managed-daemon","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:rust-sdk"},{"depends_on":"story:rust-sdk-client"}]},"args":{"command":"create-relation","kind":"depends_on","source":"01MEM0000000000000186","target":"01MEM0000000000000082"},"payload":{"actor":"human:timo","at":1788207602419,"causation":"cmd-story-rust-sdk-managed-daemon-depends_on-story-rust-sdk-client","change":{"change":"related","relation":"depends_on","target":"story:rust-sdk-client"},"correlation":"protocol-artifact-relate","event_id":"story:rust-sdk-managed-daemon@1#0~2bc61f8ecaa9c7d7","executor":null,"recorded_at":"2026-08-31T20:20:02Z"}}
{"entity":"story","version":1,"id":"rust-sdk-publication","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Story: The Rust SDK and runtime chain are owner-released Rust packages\n\n## Outcome\n\nConsumers depend on version-locked `b10x-substrate-*` packages rather than a sibling checkout. The default client stays transport-only; linked daemon support is opt-in.\n\n## Acceptance\n\nThe gate permits only the named package set, verifies package contents and downstream builds, and keeps every other workspace member non-publishable. A fully gated annotated release tag is published to crates.io manually in dependency order with an operator-held scoped token that never enters GitHub.\n","owner":"substrate","summary":"An explicit allowlist, package gate and manual token-off-GitHub release make the SDK consumable without sibling paths.","tags":["release","sdk"],"title":"The Rust SDK and runtime chain are owner-released Rust packages"},"args":{"command":"create-entity","data":{"body":"# Story: The Rust SDK and runtime chain are owner-released Rust packages\n\n## Outcome\n\nConsumers depend on version-locked `b10x-substrate-*` packages rather than a sibling checkout. The default client stays transport-only; linked daemon support is opt-in.\n\n## Acceptance\n\nThe gate permits only the named package set, verifies package contents and downstream builds, and keeps every other workspace member non-publishable. A fully gated annotated release tag is published to crates.io manually in dependency order with an operator-held scoped token that never enters GitHub.\n","owner":"substrate","status":"draft","summary":"An explicit allowlist, package gate and manual token-off-GitHub release make the SDK consumable without sibling paths.","tags":["release","sdk"],"title":"The Rust SDK and runtime chain are owner-released Rust packages"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/rust-sdk-publication"},"payload":{"actor":"human:timo","at":1788207602598,"causation":"cmd-new-story-rust-sdk-publication","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:rust-sdk-publication@1#0~d507ca14aac2770e","executor":null,"recorded_at":"2026-08-31T20:20:02Z"}}
{"entity":"story","version":1,"id":"rust-sdk-publication","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:rust-sdk"}]},"args":{"command":"create-relation","kind":"decomposes","source":"01MEM0000000000000193","target":"01MEM0000000000000013"},"payload":{"actor":"human:timo","at":1788207602606,"causation":"cmd-story-rust-sdk-publication-decomposes-epic-rust-sdk","change":{"change":"related","relation":"decomposes","target":"epic:rust-sdk"},"correlation":"protocol-artifact-relate","event_id":"story:rust-sdk-publication@1#0~9e813bd5a01ebc38","executor":null,"recorded_at":"2026-08-31T20:20:02Z"}}
{"entity":"story","version":1,"id":"rust-sdk-publication","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:rust-sdk"},{"depends_on":"story:contract-bundle-oci-artifact"}]},"args":{"command":"create-relation","kind":"depends_on","source":"01MEM0000000000000193","target":"01MEM0000000000000034"},"payload":{"actor":"human:timo","at":1788207602615,"causation":"cmd-story-rust-sdk-publication-depends_on-story-contract-bundle-oci-artifact","change":{"change":"related","relation":"depends_on","target":"story:contract-bundle-oci-artifact"},"correlation":"protocol-artifact-relate","event_id":"story:rust-sdk-publication@1#0~dac060e6aec565c4","executor":null,"recorded_at":"2026-08-31T20:20:02Z"}}
{"entity":"story","version":1,"id":"rust-sdk-public-docs","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Story: The public site teaches the Rust SDK journey\n\n## Outcome\n\nA reader arriving cold can connect to a daemon or start an owned child, create a workspace and run one explicitly bounded command from Rust.\n\n## Acceptance\n\nThe website is self-contained, marks the SDK and contract as development status, links no internal designs or work records, and states that managed means a separate daemon process rather than in-process execution.\n","owner":"substrate","summary":"Reader-facing examples cover connection, managed mode, explicit limits and current development boundaries.","tags":["docs","sdk"],"title":"The public site teaches the Rust SDK journey"},"args":{"command":"create-entity","data":{"body":"# Story: The public site teaches the Rust SDK journey\n\n## Outcome\n\nA reader arriving cold can connect to a daemon or start an owned child, create a workspace and run one explicitly bounded command from Rust.\n\n## Acceptance\n\nThe website is self-contained, marks the SDK and contract as development status, links no internal designs or work records, and states that managed means a separate daemon process rather than in-process execution.\n","owner":"substrate","status":"draft","summary":"Reader-facing examples cover connection, managed mode, explicit limits and current development boundaries.","tags":["docs","sdk"],"title":"The public site teaches the Rust SDK journey"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/rust-sdk-public-docs"},"payload":{"actor":"human:timo","at":1788207602796,"causation":"cmd-new-story-rust-sdk-public-docs","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:rust-sdk-public-docs@1#0~dedb4066d138d553","executor":null,"recorded_at":"2026-08-31T20:20:02Z"}}
{"entity":"story","version":1,"id":"rust-sdk-public-docs","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:rust-sdk"}]},"args":{"command":"create-relation","kind":"decomposes","source":"01MEM0000000000000200","target":"01MEM0000000000000013"},"payload":{"actor":"human:timo","at":1788207602804,"causation":"cmd-story-rust-sdk-public-docs-decomposes-epic-rust-sdk","change":{"change":"related","relation":"decomposes","target":"epic:rust-sdk"},"correlation":"protocol-artifact-relate","event_id":"story:rust-sdk-public-docs@1#0~f5ed7d3529988b4b","executor":null,"recorded_at":"2026-08-31T20:20:02Z"}}
{"entity":"story","version":1,"id":"rust-sdk-public-docs","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:rust-sdk"},{"depends_on":"story:rust-sdk-client"}]},"args":{"command":"create-relation","kind":"depends_on","source":"01MEM0000000000000200","target":"01MEM0000000000000082"},"payload":{"actor":"human:timo","at":1788207602811,"causation":"cmd-story-rust-sdk-public-docs-depends_on-story-rust-sdk-client","change":{"change":"related","relation":"depends_on","target":"story:rust-sdk-client"},"correlation":"protocol-artifact-relate","event_id":"story:rust-sdk-public-docs@1#0~0bd5a10da982f7aa","executor":null,"recorded_at":"2026-08-31T20:20:02Z"}}
{"entity":"story","version":1,"id":"rust-sdk-public-docs","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:rust-sdk"},{"depends_on":"story:rust-sdk-client"},{"depends_on":"story:rust-sdk-managed-daemon"}]},"args":{"command":"create-relation","kind":"depends_on","source":"01MEM0000000000000200","target":"01MEM0000000000000085"},"payload":{"actor":"human:timo","at":1788207602822,"causation":"cmd-story-rust-sdk-public-docs-depends_on-story-rust-sdk-managed-daemon","change":{"change":"related","relation":"depends_on","target":"story:rust-sdk-managed-daemon"},"correlation":"protocol-artifact-relate","event_id":"story:rust-sdk-public-docs@1#0~37256a0dc20b9787","executor":null,"recorded_at":"2026-08-31T20:20:02Z"}}
{"entity":"epic","version":1,"id":"mcp-test-surface","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Epic: MCP test surface\n\n## Outcome\n\nArbitrary MCP-capable harnesses can start an isolated, disposable Substrate daemon and exercise its public workspace, execution, output, metrics and cleanup behavior through a small MCP adapter.\n\n## Boundaries\n\nThe adapter is a client of the public Substrate wire contract. No MCP type or server implementation enters the daemon, host driver or wire crate; the surface is for local testing and evaluation, not a production multi-tenant ingress. The work starts after the current resource-bounded execution epic.\n","owner":"substrate","summary":"A disposable daemon and protocol-neutral MCP adapter make Substrate easy to exercise from Codex and other MCP-capable harnesses.","tags":["deferred","mcp","testing"],"title":"MCP test surface for arbitrary harnesses"},"args":{"command":"create-entity","data":{"body":"# Epic: MCP test surface\n\n## Outcome\n\nArbitrary MCP-capable harnesses can start an isolated, disposable Substrate daemon and exercise its public workspace, execution, output, metrics and cleanup behavior through a small MCP adapter.\n\n## Boundaries\n\nThe adapter is a client of the public Substrate wire contract. No MCP type or server implementation enters the daemon, host driver or wire crate; the surface is for local testing and evaluation, not a production multi-tenant ingress. The work starts after the current resource-bounded execution epic.\n","owner":"substrate","status":"draft","summary":"A disposable daemon and protocol-neutral MCP adapter make Substrate easy to exercise from Codex and other MCP-capable harnesses.","tags":["deferred","mcp","testing"],"title":"MCP test surface for arbitrary harnesses"},"entity_type":"aep.epic/v1","locator":"ep://planning/store/epic/mcp-test-surface"},"payload":{"actor":"human:timo","at":1788209266405,"causation":"cmd-new-epic-mcp-test-surface","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"epic:mcp-test-surface@1#0~772cd618eae4d16e","executor":null,"recorded_at":"2026-08-31T20:47:46Z"}}
{"entity":"story","version":1,"id":"mcp-managed-disposable-daemon","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Story: Launch a disposable daemon behind MCP\n\n## Outcome\n\nOne command starts a fresh private Substrate daemon, waits for its machine facts, exposes bounded workspace and execution operations as MCP tools, and tears down the socket, database, workspaces and child process when the MCP server exits.\n\n## Acceptance\n\nThe adapter verifies the advertised contract and capability snapshot, preserves operation IDs and named refusals, never bypasses the daemon through host-driver APIs, exposes output and exact resource observations without inventing fields, scopes all state to the disposable instance, and includes a Codex-compatible hands-on smoke test. Unsupported host capabilities remain absent or refused. No TCP listener or production authentication claim is introduced.\n","owner":"substrate","summary":"A local MCP adapter owns one fresh daemon lifecycle and projects its public operations for harness testing.","tags":["deferred","mcp","testing"],"title":"Launch a disposable Substrate daemon behind MCP"},"args":{"command":"create-entity","data":{"body":"# Story: Launch a disposable daemon behind MCP\n\n## Outcome\n\nOne command starts a fresh private Substrate daemon, waits for its machine facts, exposes bounded workspace and execution operations as MCP tools, and tears down the socket, database, workspaces and child process when the MCP server exits.\n\n## Acceptance\n\nThe adapter verifies the advertised contract and capability snapshot, preserves operation IDs and named refusals, never bypasses the daemon through host-driver APIs, exposes output and exact resource observations without inventing fields, scopes all state to the disposable instance, and includes a Codex-compatible hands-on smoke test. Unsupported host capabilities remain absent or refused. No TCP listener or production authentication claim is introduced.\n","owner":"substrate","status":"draft","summary":"A local MCP adapter owns one fresh daemon lifecycle and projects its public operations for harness testing.","tags":["deferred","mcp","testing"],"title":"Launch a disposable Substrate daemon behind MCP"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/mcp-managed-disposable-daemon"},"payload":{"actor":"human:timo","at":1788209266595,"causation":"cmd-new-story-mcp-managed-disposable-daemon","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:mcp-managed-disposable-daemon@1#0~558bfb74224f806e","executor":null,"recorded_at":"2026-08-31T20:47:46Z"}}
{"entity":"story","version":1,"id":"mcp-managed-disposable-daemon","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:mcp-test-surface"}]},"args":{"command":"create-relation","kind":"decomposes","source":"01MEM0000000000000212","target":"01MEM0000000000000007"},"payload":{"actor":"human:timo","at":1788209266603,"causation":"cmd-story-mcp-managed-disposable-daemon-decomposes-epic-mcp-test-surface","change":{"change":"related","relation":"decomposes","target":"epic:mcp-test-surface"},"correlation":"protocol-artifact-relate","event_id":"story:mcp-managed-disposable-daemon@1#0~5707ee85b1b6428b","executor":null,"recorded_at":"2026-08-31T20:47:46Z"}}
{"entity":"story","version":1,"id":"apache-publication-and-pages-hardening","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000022","to":"proposed"},"payload":{"actor":"human:timo","at":1788216555146,"causation":"cmd-move-story-apache-publication-and-pages-hardening-1788216555146","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:apache-publication-and-pages-hardening@2#0~b5cf5c548f340204","executor":null,"recorded_at":"2026-08-31T22:49:15Z"}}
{"entity":"story","version":1,"id":"apache-publication-and-pages-hardening","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000022","to":"active"},"payload":{"actor":"human:timo","at":1788216555163,"causation":"cmd-move-story-apache-publication-and-pages-hardening-1788216555163","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:apache-publication-and-pages-hardening@3#0~a8aa01b813a951a8","executor":null,"recorded_at":"2026-08-31T22:49:15Z"}}
{"entity":"story","version":1,"id":"rust-sdk-client","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000088","to":"proposed"},"payload":{"actor":"human:timo","at":1788216595036,"causation":"cmd-move-story-rust-sdk-client-1788216595036","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:rust-sdk-client@2#0~af12439ae99bf910","executor":null,"recorded_at":"2026-08-31T22:49:55Z"}}
{"entity":"story","version":1,"id":"rust-sdk-client","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000088","to":"active"},"payload":{"actor":"human:timo","at":1788216595050,"causation":"cmd-move-story-rust-sdk-client-1788216595050","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:rust-sdk-client@3#0~e7080621c9f929fc","executor":null,"recorded_at":"2026-08-31T22:49:55Z"}}
{"entity":"story","version":1,"id":"rust-sdk-managed-daemon","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000091","to":"proposed"},"payload":{"actor":"human:timo","at":1788216595263,"causation":"cmd-move-story-rust-sdk-managed-daemon-1788216595263","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:rust-sdk-managed-daemon@2#0~b7774a8295922c72","executor":null,"recorded_at":"2026-08-31T22:49:55Z"}}
{"entity":"story","version":1,"id":"rust-sdk-managed-daemon","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000091","to":"active"},"payload":{"actor":"human:timo","at":1788216595271,"causation":"cmd-move-story-rust-sdk-managed-daemon-1788216595271","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:rust-sdk-managed-daemon@3#0~c48ea9b6340156f6","executor":null,"recorded_at":"2026-08-31T22:49:55Z"}}
{"entity":"story","version":1,"id":"rust-sdk-public-docs","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000094","to":"proposed"},"payload":{"actor":"human:timo","at":1788216595481,"causation":"cmd-move-story-rust-sdk-public-docs-1788216595481","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:rust-sdk-public-docs@2#0~73334c424c3a2b0d","executor":null,"recorded_at":"2026-08-31T22:49:55Z"}}
{"entity":"story","version":1,"id":"rust-sdk-public-docs","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000094","to":"active"},"payload":{"actor":"human:timo","at":1788216595489,"causation":"cmd-move-story-rust-sdk-public-docs-1788216595489","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:rust-sdk-public-docs@3#0~2e1ab2a15349e83d","executor":null,"recorded_at":"2026-08-31T22:49:55Z"}}
{"entity":"story","version":1,"id":"apache-publication-and-pages-hardening","revision":3,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"https://github.com/beyond10x/substrate/actions/runs/33447207408","source":"required GitHub Full gate on main","target":"01MEM0000000000000022"},"payload":{"actor":"human:timo","at":1788215957000,"causation":"cmd-evidence-story-apache-publication-and-pages-hardening-test_result-1788216595617","change":{"change":"evidence","kind":"test_result","reference":"https://github.com/beyond10x/substrate/actions/runs/33447207408","source":"required GitHub Full gate on main"},"correlation":"protocol-artifact-evidence","event_id":"story:apache-publication-and-pages-hardening@3#0~3d1e37139e183434","executor":null,"recorded_at":"2026-08-31T22:39:17Z"}}
{"entity":"story","version":1,"id":"apache-publication-and-pages-hardening","revision":4,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1}}","target":"01MEM0000000000000022","to":"implemented"},"payload":{"actor":"human:timo","at":1788216595850,"causation":"cmd-move-story-apache-publication-and-pages-hardening-1788216595850","change":{"change":"moved","decided_on":{"recorded":{"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:apache-publication-and-pages-hardening@4#0~43ca67971a830749","executor":null,"recorded_at":"2026-08-31T22:49:55Z"}}
{"entity":"story","version":1,"id":"contract-gate-predecessor-adjacency","revision":3,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"https://github.com/beyond10x/substrate/actions/runs/33447207408","source":"required GitHub Full gate on main","target":"01MEM0000000000000040"},"payload":{"actor":"human:timo","at":1788215957000,"causation":"cmd-evidence-story-contract-gate-predecessor-adjacency-test_result-1788216595979","change":{"change":"evidence","kind":"test_result","reference":"https://github.com/beyond10x/substrate/actions/runs/33447207408","source":"required GitHub Full gate on main"},"correlation":"protocol-artifact-evidence","event_id":"story:contract-gate-predecessor-adjacency@3#0~961ed1257ab273a8","executor":null,"recorded_at":"2026-08-31T22:39:17Z"}}
{"entity":"story","version":1,"id":"contract-gate-predecessor-adjacency","revision":4,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1}}","target":"01MEM0000000000000040","to":"implemented"},"payload":{"actor":"human:timo","at":1788216596211,"causation":"cmd-move-story-contract-gate-predecessor-adjacency-1788216596211","change":{"change":"moved","decided_on":{"recorded":{"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:contract-gate-predecessor-adjacency@4#0~e6babbdcf498311d","executor":null,"recorded_at":"2026-08-31T22:49:56Z"}}
{"entity":"story","version":1,"id":"contract-gate-sees-route-paths","revision":4,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"https://github.com/beyond10x/substrate/actions/runs/33447207408","source":"required GitHub Full gate on main","target":"01MEM0000000000000043"},"payload":{"actor":"human:timo","at":1788215957000,"causation":"cmd-evidence-story-contract-gate-sees-route-paths-test_result-1788216596342","change":{"change":"evidence","kind":"test_result","reference":"https://github.com/beyond10x/substrate/actions/runs/33447207408","source":"required GitHub Full gate on main"},"correlation":"protocol-artifact-evidence","event_id":"story:contract-gate-sees-route-paths@4#0~7b495a256b427cb5","executor":null,"recorded_at":"2026-08-31T22:39:17Z"}}
{"entity":"story","version":1,"id":"contract-gate-sees-route-paths","revision":5,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1}}","target":"01MEM0000000000000043","to":"implemented"},"payload":{"actor":"human:timo","at":1788216596577,"causation":"cmd-move-story-contract-gate-sees-route-paths-1788216596577","change":{"change":"moved","decided_on":{"recorded":{"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:contract-gate-sees-route-paths@5#0~bfdfc4ba0afe08be","executor":null,"recorded_at":"2026-08-31T22:49:56Z"}}
{"entity":"story","version":1,"id":"execution-resource-metrics","revision":3,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"https://github.com/beyond10x/substrate/actions/runs/33447207408","source":"required GitHub Full gate on main","target":"01MEM0000000000000055"},"payload":{"actor":"human:timo","at":1788215957000,"causation":"cmd-evidence-story-execution-resource-metrics-test_result-1788216596706","change":{"change":"evidence","kind":"test_result","reference":"https://github.com/beyond10x/substrate/actions/runs/33447207408","source":"required GitHub Full gate on main"},"correlation":"protocol-artifact-evidence","event_id":"story:execution-resource-metrics@3#0~c610e41c77fff94a","executor":null,"recorded_at":"2026-08-31T22:39:17Z"}}
{"entity":"story","version":1,"id":"execution-resource-metrics","revision":4,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1}}","target":"01MEM0000000000000055","to":"implemented"},"payload":{"actor":"human:timo","at":1788216596939,"causation":"cmd-move-story-execution-resource-metrics-1788216596939","change":{"change":"moved","decided_on":{"recorded":{"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:execution-resource-metrics@4#0~7d6d16600432bcf7","executor":null,"recorded_at":"2026-08-31T22:49:56Z"}}
{"entity":"story","version":1,"id":"file-routes-declare-a-template-the-daemon-does-not-serve","revision":3,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"https://github.com/beyond10x/substrate/actions/runs/33447207408","source":"required GitHub Full gate on main","target":"01MEM0000000000000058"},"payload":{"actor":"human:timo","at":1788215957000,"causation":"cmd-evidence-story-file-routes-declare-a-template-the-daemon-does-not-serve-test_result-1788216597068","change":{"change":"evidence","kind":"test_result","reference":"https://github.com/beyond10x/substrate/actions/runs/33447207408","source":"required GitHub Full gate on main"},"correlation":"protocol-artifact-evidence","event_id":"story:file-routes-declare-a-template-the-daemon-does-not-serve@3#0~2640a71caebce80d","executor":null,"recorded_at":"2026-08-31T22:39:17Z"}}
{"entity":"story","version":1,"id":"file-routes-declare-a-template-the-daemon-does-not-serve","revision":4,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1}}","target":"01MEM0000000000000058","to":"implemented"},"payload":{"actor":"human:timo","at":1788216597300,"causation":"cmd-move-story-file-routes-declare-a-template-the-daemon-does-not-serve-1788216597300","change":{"change":"moved","decided_on":{"recorded":{"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:file-routes-declare-a-template-the-daemon-does-not-serve@4#0~8a9641defe2a2ad6","executor":null,"recorded_at":"2026-08-31T22:49:57Z"}}
{"entity":"story","version":1,"id":"full-review-git-recovery","revision":3,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"https://github.com/beyond10x/substrate/actions/runs/33447207408","source":"required GitHub Full gate on main","target":"01MEM0000000000000061"},"payload":{"actor":"human:timo","at":1788215957000,"causation":"cmd-evidence-story-full-review-git-recovery-test_result-1788216597427","change":{"change":"evidence","kind":"test_result","reference":"https://github.com/beyond10x/substrate/actions/runs/33447207408","source":"required GitHub Full gate on main"},"correlation":"protocol-artifact-evidence","event_id":"story:full-review-git-recovery@3#0~a2321834e77ce6fd","executor":null,"recorded_at":"2026-08-31T22:39:17Z"}}
{"entity":"story","version":1,"id":"full-review-git-recovery","revision":4,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1}}","target":"01MEM0000000000000061","to":"implemented"},"payload":{"actor":"human:timo","at":1788216597662,"causation":"cmd-move-story-full-review-git-recovery-1788216597662","change":{"change":"moved","decided_on":{"recorded":{"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:full-review-git-recovery@4#0~4aa5815e793c0876","executor":null,"recorded_at":"2026-08-31T22:49:57Z"}}
{"entity":"story","version":1,"id":"pty-sessions","revision":9,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"https://github.com/beyond10x/substrate/actions/runs/33447207408","source":"required GitHub Full gate on main","target":"01MEM0000000000000076"},"payload":{"actor":"human:timo","at":1788215957000,"causation":"cmd-evidence-story-pty-sessions-test_result-1788216597791","change":{"change":"evidence","kind":"test_result","reference":"https://github.com/beyond10x/substrate/actions/runs/33447207408","source":"required GitHub Full gate on main"},"correlation":"protocol-artifact-evidence","event_id":"story:pty-sessions@9#0~5bc29d2e76ffb66d","executor":null,"recorded_at":"2026-08-31T22:39:17Z"}}
{"entity":"story","version":1,"id":"pty-sessions","revision":10,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1,\"specification\":1}}","target":"01MEM0000000000000076","to":"implemented"},"payload":{"actor":"human:timo","at":1788216598022,"causation":"cmd-move-story-pty-sessions-1788216598022","change":{"change":"moved","decided_on":{"recorded":{"specification":1,"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:pty-sessions@10#0~8f9fdc958065e676","executor":null,"recorded_at":"2026-08-31T22:49:58Z"}}
{"entity":"story","version":1,"id":"public-practical-execution-docs","revision":3,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"https://github.com/beyond10x/substrate/actions/runs/33447207408","source":"required GitHub Full gate on main","target":"01MEM0000000000000079"},"payload":{"actor":"human:timo","at":1788215957000,"causation":"cmd-evidence-story-public-practical-execution-docs-test_result-1788216598151","change":{"change":"evidence","kind":"test_result","reference":"https://github.com/beyond10x/substrate/actions/runs/33447207408","source":"required GitHub Full gate on main"},"correlation":"protocol-artifact-evidence","event_id":"story:public-practical-execution-docs@3#0~0dcd282e4ad788a4","executor":null,"recorded_at":"2026-08-31T22:39:17Z"}}
{"entity":"story","version":1,"id":"public-practical-execution-docs","revision":4,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1}}","target":"01MEM0000000000000079","to":"implemented"},"payload":{"actor":"human:timo","at":1788216598385,"causation":"cmd-move-story-public-practical-execution-docs-1788216598385","change":{"change":"moved","decided_on":{"recorded":{"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:public-practical-execution-docs@4#0~d377c2c40a24cab1","executor":null,"recorded_at":"2026-08-31T22:49:58Z"}}
{"entity":"story","version":1,"id":"release-publication-hardening","revision":3,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"https://github.com/beyond10x/substrate/actions/runs/33446364706","source":"required GitHub Full gate on main","target":"01MEM0000000000000082"},"payload":{"actor":"human:timo","at":1788215957000,"causation":"cmd-evidence-story-release-publication-hardening-test_result-1788216598511","change":{"change":"evidence","kind":"test_result","reference":"https://github.com/beyond10x/substrate/actions/runs/33446364706","source":"required GitHub Full gate on main"},"correlation":"protocol-artifact-evidence","event_id":"story:release-publication-hardening@3#0~54238d2cab7a105a","executor":null,"recorded_at":"2026-08-31T22:39:17Z"}}
{"entity":"story","version":1,"id":"release-publication-hardening","revision":4,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1}}","target":"01MEM0000000000000082","to":"implemented"},"payload":{"actor":"human:timo","at":1788216598746,"causation":"cmd-move-story-release-publication-hardening-1788216598746","change":{"change":"moved","decided_on":{"recorded":{"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:release-publication-hardening@4#0~e476593274933d57","executor":null,"recorded_at":"2026-08-31T22:49:58Z"}}
{"entity":"story","version":1,"id":"repository-gate-hardening","revision":3,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"https://github.com/beyond10x/substrate/actions/runs/33447207408","source":"required GitHub Full gate on main","target":"01MEM0000000000000085"},"payload":{"actor":"human:timo","at":1788215957000,"causation":"cmd-evidence-story-repository-gate-hardening-test_result-1788216598875","change":{"change":"evidence","kind":"test_result","reference":"https://github.com/beyond10x/substrate/actions/runs/33447207408","source":"required GitHub Full gate on main"},"correlation":"protocol-artifact-evidence","event_id":"story:repository-gate-hardening@3#0~051503c044210517","executor":null,"recorded_at":"2026-08-31T22:39:17Z"}}
{"entity":"story","version":1,"id":"repository-gate-hardening","revision":4,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1}}","target":"01MEM0000000000000085","to":"implemented"},"payload":{"actor":"human:timo","at":1788216599117,"causation":"cmd-move-story-repository-gate-hardening-1788216599117","change":{"change":"moved","decided_on":{"recorded":{"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:repository-gate-hardening@4#0~c53b479e64f045e0","executor":null,"recorded_at":"2026-08-31T22:49:59Z"}}
{"entity":"story","version":1,"id":"rust-sdk-client","revision":3,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"https://github.com/beyond10x/substrate/actions/runs/33447207408","source":"required GitHub Full gate on main","target":"01MEM0000000000000088"},"payload":{"actor":"human:timo","at":1788215957000,"causation":"cmd-evidence-story-rust-sdk-client-test_result-1788216599245","change":{"change":"evidence","kind":"test_result","reference":"https://github.com/beyond10x/substrate/actions/runs/33447207408","source":"required GitHub Full gate on main"},"correlation":"protocol-artifact-evidence","event_id":"story:rust-sdk-client@3#0~3072d0c05cb5e91c","executor":null,"recorded_at":"2026-08-31T22:39:17Z"}}
{"entity":"story","version":1,"id":"rust-sdk-client","revision":4,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1}}","target":"01MEM0000000000000088","to":"implemented"},"payload":{"actor":"human:timo","at":1788216599480,"causation":"cmd-move-story-rust-sdk-client-1788216599480","change":{"change":"moved","decided_on":{"recorded":{"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:rust-sdk-client@4#0~964338e589eac631","executor":null,"recorded_at":"2026-08-31T22:49:59Z"}}
{"entity":"story","version":1,"id":"rust-sdk-managed-daemon","revision":3,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"https://github.com/beyond10x/substrate/actions/runs/33447207408","source":"required GitHub Full gate on main","target":"01MEM0000000000000091"},"payload":{"actor":"human:timo","at":1788215957000,"causation":"cmd-evidence-story-rust-sdk-managed-daemon-test_result-1788216599611","change":{"change":"evidence","kind":"test_result","reference":"https://github.com/beyond10x/substrate/actions/runs/33447207408","source":"required GitHub Full gate on main"},"correlation":"protocol-artifact-evidence","event_id":"story:rust-sdk-managed-daemon@3#0~942fbfb9bbd4d86a","executor":null,"recorded_at":"2026-08-31T22:39:17Z"}}
{"entity":"story","version":1,"id":"rust-sdk-managed-daemon","revision":4,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1}}","target":"01MEM0000000000000091","to":"implemented"},"payload":{"actor":"human:timo","at":1788216599845,"causation":"cmd-move-story-rust-sdk-managed-daemon-1788216599845","change":{"change":"moved","decided_on":{"recorded":{"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:rust-sdk-managed-daemon@4#0~a4fd21e583f87ae7","executor":null,"recorded_at":"2026-08-31T22:49:59Z"}}
{"entity":"story","version":1,"id":"rust-sdk-public-docs","revision":3,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"https://github.com/beyond10x/substrate/actions/runs/33447207408","source":"required GitHub Full gate on main","target":"01MEM0000000000000094"},"payload":{"actor":"human:timo","at":1788215957000,"causation":"cmd-evidence-story-rust-sdk-public-docs-test_result-1788216599974","change":{"change":"evidence","kind":"test_result","reference":"https://github.com/beyond10x/substrate/actions/runs/33447207408","source":"required GitHub Full gate on main"},"correlation":"protocol-artifact-evidence","event_id":"story:rust-sdk-public-docs@3#0~ae393ab9ca976fc5","executor":null,"recorded_at":"2026-08-31T22:39:17Z"}}
{"entity":"story","version":1,"id":"rust-sdk-public-docs","revision":4,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1}}","target":"01MEM0000000000000094","to":"implemented"},"payload":{"actor":"human:timo","at":1788216600210,"causation":"cmd-move-story-rust-sdk-public-docs-1788216600210","change":{"change":"moved","decided_on":{"recorded":{"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:rust-sdk-public-docs@4#0~82ab3adb8ac4043e","executor":null,"recorded_at":"2026-08-31T22:50:00Z"}}
{"entity":"story","version":1,"id":"v2-contract-and-gate-hardening","revision":3,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"https://github.com/beyond10x/substrate/actions/runs/33447207408","source":"required GitHub Full gate on main","target":"01MEM0000000000000115"},"payload":{"actor":"human:timo","at":1788215957000,"causation":"cmd-evidence-story-v2-contract-and-gate-hardening-test_result-1788216600339","change":{"change":"evidence","kind":"test_result","reference":"https://github.com/beyond10x/substrate/actions/runs/33447207408","source":"required GitHub Full gate on main"},"correlation":"protocol-artifact-evidence","event_id":"story:v2-contract-and-gate-hardening@3#0~51d24800fc60e941","executor":null,"recorded_at":"2026-08-31T22:39:17Z"}}
{"entity":"story","version":1,"id":"v2-contract-and-gate-hardening","revision":4,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1}}","target":"01MEM0000000000000115","to":"implemented"},"payload":{"actor":"human:timo","at":1788216600572,"causation":"cmd-move-story-v2-contract-and-gate-hardening-1788216600572","change":{"change":"moved","decided_on":{"recorded":{"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:v2-contract-and-gate-hardening@4#0~8526232eb7a43172","executor":null,"recorded_at":"2026-08-31T22:50:00Z"}}
{"entity":"story","version":1,"id":"workspace-and-scratch-quotas","revision":3,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"https://github.com/beyond10x/substrate/actions/runs/33447207408","source":"required GitHub Full gate on main","target":"01MEM0000000000000118"},"payload":{"actor":"human:timo","at":1788215957000,"causation":"cmd-evidence-story-workspace-and-scratch-quotas-test_result-1788216600701","change":{"change":"evidence","kind":"test_result","reference":"https://github.com/beyond10x/substrate/actions/runs/33447207408","source":"required GitHub Full gate on main"},"correlation":"protocol-artifact-evidence","event_id":"story:workspace-and-scratch-quotas@3#0~c2605d013c1e0636","executor":null,"recorded_at":"2026-08-31T22:39:17Z"}}
{"entity":"story","version":1,"id":"workspace-and-scratch-quotas","revision":4,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1}}","target":"01MEM0000000000000118","to":"implemented"},"payload":{"actor":"human:timo","at":1788216600936,"causation":"cmd-move-story-workspace-and-scratch-quotas-1788216600936","change":{"change":"moved","decided_on":{"recorded":{"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:workspace-and-scratch-quotas@4#0~02a181b76ea252eb","executor":null,"recorded_at":"2026-08-31T22:50:00Z"}}
{"entity":"epic","version":1,"id":"resource-bounded-execution","revision":3,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"https://github.com/beyond10x/substrate/actions/runs/33447207408","source":"required GitHub Full gate on main","target":"01MEM0000000000000013"},"payload":{"actor":"human:timo","at":1788215957000,"causation":"cmd-evidence-epic-resource-bounded-execution-test_result-1788216609981","change":{"change":"evidence","kind":"test_result","reference":"https://github.com/beyond10x/substrate/actions/runs/33447207408","source":"required GitHub Full gate on main"},"correlation":"protocol-artifact-evidence","event_id":"epic:resource-bounded-execution@3#0~90359dfbda5d467c","executor":null,"recorded_at":"2026-08-31T22:39:17Z"}}
{"entity":"epic","version":1,"id":"resource-bounded-execution","revision":4,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1}}","target":"01MEM0000000000000013","to":"implemented"},"payload":{"actor":"human:timo","at":1788216610226,"causation":"cmd-move-epic-resource-bounded-execution-1788216610226","change":{"change":"moved","decided_on":{"recorded":{"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"epic:resource-bounded-execution@4#0~7b149e8637f603a1","executor":null,"recorded_at":"2026-08-31T22:50:10Z"}}
{"entity":"epic","version":1,"id":"byte-plane-completion","revision":5,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000001","to":"proposed"},"payload":{"actor":"human:timo","at":1788216610442,"causation":"cmd-move-epic-byte-plane-completion-1788216610442","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"epic:byte-plane-completion@5#0~081adaf6578f49c7","executor":null,"recorded_at":"2026-08-31T22:50:10Z"}}
{"entity":"epic","version":1,"id":"byte-plane-completion","revision":6,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000001","to":"active"},"payload":{"actor":"human:timo","at":1788216610453,"causation":"cmd-move-epic-byte-plane-completion-1788216610453","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"epic:byte-plane-completion@6#0~8c7e859d3507c067","executor":null,"recorded_at":"2026-08-31T22:50:10Z"}}
{"entity":"epic","version":1,"id":"release-hardening","revision":4,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000010","to":"proposed"},"payload":{"actor":"human:timo","at":1788216610666,"causation":"cmd-move-epic-release-hardening-1788216610666","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"epic:release-hardening@4#0~0579e912cb63b379","executor":null,"recorded_at":"2026-08-31T22:50:10Z"}}
{"entity":"epic","version":1,"id":"release-hardening","revision":5,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000010","to":"active"},"payload":{"actor":"human:timo","at":1788216610675,"causation":"cmd-move-epic-release-hardening-1788216610675","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"epic:release-hardening@5#0~647b0407ee3c1771","executor":null,"recorded_at":"2026-08-31T22:50:10Z"}}
{"entity":"epic","version":1,"id":"rust-sdk","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000016","to":"proposed"},"payload":{"actor":"human:timo","at":1788216610889,"causation":"cmd-move-epic-rust-sdk-1788216610889","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"epic:rust-sdk@2#0~c531405e856b932f","executor":null,"recorded_at":"2026-08-31T22:50:10Z"}}
{"entity":"epic","version":1,"id":"rust-sdk","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000016","to":"active"},"payload":{"actor":"human:timo","at":1788216610900,"causation":"cmd-move-epic-rust-sdk-1788216610900","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"epic:rust-sdk@3#0~c833b53be4960cdf","executor":null,"recorded_at":"2026-08-31T22:50:10Z"}}
{"entity":"epic","version":1,"id":"container-driver-entry","revision":4,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000004","to":"proposed"},"payload":{"actor":"human:timo","at":1788216611115,"causation":"cmd-move-epic-container-driver-entry-1788216611115","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"epic:container-driver-entry@4#0~d1b8d2c3ff0c3c64","executor":null,"recorded_at":"2026-08-31T22:50:11Z"}}
{"entity":"epic","version":1,"id":"mcp-test-surface","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000007","to":"proposed"},"payload":{"actor":"human:timo","at":1788216611328,"causation":"cmd-move-epic-mcp-test-surface-1788216611328","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"epic:mcp-test-surface@2#0~3e73cefca43bd891","executor":null,"recorded_at":"2026-08-31T22:50:11Z"}}
{"entity":"story","version":1,"id":"mcp-managed-disposable-daemon","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000067","to":"proposed"},"payload":{"actor":"human:timo","at":1788216611541,"causation":"cmd-move-story-mcp-managed-disposable-daemon-1788216611541","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:mcp-managed-disposable-daemon@2#0~7165723cefde4fd3","executor":null,"recorded_at":"2026-08-31T22:50:11Z"}}
{"entity":"story","version":1,"id":"rust-sdk-publication","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000097","to":"proposed"},"payload":{"actor":"human:timo","at":1788216611753,"causation":"cmd-move-story-rust-sdk-publication-1788216611753","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:rust-sdk-publication@2#0~0863d3a3540daea0","executor":null,"recorded_at":"2026-08-31T22:50:11Z"}}
{"entity":"story","version":1,"id":"rust-sdk-publication","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000097","to":"active"},"payload":{"actor":"human:timo","at":1788216611764,"causation":"cmd-move-story-rust-sdk-publication-1788216611764","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:rust-sdk-publication@3#0~025921c04218a40c","executor":null,"recorded_at":"2026-08-31T22:50:11Z"}}
{"entity":"epic","version":1,"id":"remote-serving","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Epic: Remote serving and hosted trust\n\n## Outcome\n\nAgent-platform, Devcenter-facing composition, and other independently deployed services can address a Substrate instance over a production HTTPS/WSS endpoint without weakening the local Unix-socket trust model.\n\n## Boundaries\n\nSubstrate authenticates and authorizes execution-data-plane operations; it does not store product sessions, choose tenant policy, schedule a fleet, or embed Identity. The Unix socket remains supported. Static-bearer plaintext TCP remains development-only and cannot be enabled as a hosted posture.\n\n## Delivery\n\nThe track promotes an implemented contract frontier, adds a rustls control listener, verifies the accepted hosted trust envelope, extends the Rust SDK with remote transport, publishes operational probes and metrics, and proves the service from a clean-room client. Every wire change cuts a successor bundle; no frozen bundle is changed.\n","owner":"substrate","summary":"Production HTTPS/WSS, scoped Identity admission, remote SDK transport and black-box conformance make Substrate safely addressable by agent-platform and other services.","tags":["devcenter","remote","security"],"title":"Remote serving and hosted trust"},"args":{"command":"create-entity","data":{"body":"# Epic: Remote serving and hosted trust\n\n## Outcome\n\nAgent-platform, Devcenter-facing composition, and other independently deployed services can address a Substrate instance over a production HTTPS/WSS endpoint without weakening the local Unix-socket trust model.\n\n## Boundaries\n\nSubstrate authenticates and authorizes execution-data-plane operations; it does not store product sessions, choose tenant policy, schedule a fleet, or embed Identity. The Unix socket remains supported. Static-bearer plaintext TCP remains development-only and cannot be enabled as a hosted posture.\n\n## Delivery\n\nThe track promotes an implemented contract frontier, adds a rustls control listener, verifies the accepted hosted trust envelope, extends the Rust SDK with remote transport, publishes operational probes and metrics, and proves the service from a clean-room client. Every wire change cuts a successor bundle; no frozen bundle is changed.\n","owner":"substrate","status":"draft","summary":"Production HTTPS/WSS, scoped Identity admission, remote SDK transport and black-box conformance make Substrate safely addressable by agent-platform and other services.","tags":["devcenter","remote","security"],"title":"Remote serving and hosted trust"},"entity_type":"aep.epic/v1","locator":"ep://planning/store/epic/remote-serving"},"payload":{"actor":"human:timo","at":1788216677510,"causation":"cmd-new-epic-remote-serving","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"epic:remote-serving@1#0~0f6e648cd0447117","executor":null,"recorded_at":"2026-08-31T22:51:17Z"}}
{"entity":"epic","version":1,"id":"remote-serving","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"depends_on":"epic:release-hardening"}]},"args":{"command":"create-relation","kind":"depends_on","source":"01MEM0000000000000217","target":"01MEM0000000000000010"},"payload":{"actor":"human:timo","at":1788216677519,"causation":"cmd-epic-remote-serving-depends_on-epic-release-hardening","change":{"change":"related","relation":"depends_on","target":"epic:release-hardening"},"correlation":"protocol-artifact-relate","event_id":"epic:remote-serving@1#0~b44d7a64b3c66221","executor":null,"recorded_at":"2026-08-31T22:51:17Z"}}
{"entity":"epic","version":1,"id":"kubernetes-deployment-and-driver","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Epic: Kubernetes deployment and namespace driver\n\n## Outcome\n\nSubstrate runs in Kubernetes in two explicit profiles: a node-bound daemon profile for host-backed execution and a namespace-scoped controller/driver profile for Kubernetes-backed workspace and exec resources.\n\n## Boundaries\n\nThe node-bound profile gives each stateful daemon one stable address and durable volume; mutation traffic is never round-robin load-balanced across independent ledgers. The namespace driver owns only configured namespaces and applies the same durable-before-dispatch contract as every driver. Fleet placement, cross-cluster scheduling, product quotas and billing remain outside Substrate.\n\n## Delivery\n\nA signed public Helm chart establishes the node-bound profile first. A separate entry gate then fixes RBAC, ownership, cleanup, capability facts and refusal behavior before a namespace workspace/exec slice is implemented.\n","owner":"substrate","summary":"A public node-bound serving profile and a namespace-scoped driver expose truthful Kubernetes-backed execution without turning Substrate into a fleet scheduler.","tags":["devcenter","driver","kubernetes"],"title":"Kubernetes deployment and namespace driver"},"args":{"command":"create-entity","data":{"body":"# Epic: Kubernetes deployment and namespace driver\n\n## Outcome\n\nSubstrate runs in Kubernetes in two explicit profiles: a node-bound daemon profile for host-backed execution and a namespace-scoped controller/driver profile for Kubernetes-backed workspace and exec resources.\n\n## Boundaries\n\nThe node-bound profile gives each stateful daemon one stable address and durable volume; mutation traffic is never round-robin load-balanced across independent ledgers. The namespace driver owns only configured namespaces and applies the same durable-before-dispatch contract as every driver. Fleet placement, cross-cluster scheduling, product quotas and billing remain outside Substrate.\n\n## Delivery\n\nA signed public Helm chart establishes the node-bound profile first. A separate entry gate then fixes RBAC, ownership, cleanup, capability facts and refusal behavior before a namespace workspace/exec slice is implemented.\n","owner":"substrate","status":"draft","summary":"A public node-bound serving profile and a namespace-scoped driver expose truthful Kubernetes-backed execution without turning Substrate into a fleet scheduler.","tags":["devcenter","driver","kubernetes"],"title":"Kubernetes deployment and namespace driver"},"entity_type":"aep.epic/v1","locator":"ep://planning/store/epic/kubernetes-deployment-and-driver"},"payload":{"actor":"human:timo","at":1788216677729,"causation":"cmd-new-epic-kubernetes-deployment-and-driver","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"epic:kubernetes-deployment-and-driver@1#0~fc344c9696fa2b2d","executor":null,"recorded_at":"2026-08-31T22:51:17Z"}}
{"entity":"epic","version":1,"id":"kubernetes-deployment-and-driver","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"depends_on":"epic:remote-serving"}]},"args":{"command":"create-relation","kind":"depends_on","source":"01MEM0000000000000222","target":"01MEM0000000000000013"},"payload":{"actor":"human:timo","at":1788216677739,"causation":"cmd-epic-kubernetes-deployment-and-driver-depends_on-epic-remote-serving","change":{"change":"related","relation":"depends_on","target":"epic:remote-serving"},"correlation":"protocol-artifact-relate","event_id":"epic:kubernetes-deployment-and-driver@1#0~19e6a71ff6d9e630","executor":null,"recorded_at":"2026-08-31T22:51:17Z"}}
{"entity":"epic","version":1,"id":"firecracker-driver","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Epic: Direct Firecracker driver\n\n## Outcome\n\nA Substrate driver can execute one bounded job in a fresh Firecracker microVM while preserving durable operation semantics, immutable input provenance, least-authority mounts, resource bounds and exact observations.\n\n## Boundaries\n\nThe driver talks directly to Firecracker and its jailer; it does not shell out to a general VM manager or ask Kubernetes to emulate the driver contract. Images and kernel/rootfs artifacts are operator-selected immutable deployment inputs, never caller-controlled host paths. Missing /dev/kvm, jailer prerequisites or artifact verification are named unserved refusals.\n\n## Environment constraint\n\nThe currently observed development EKS nodes are T3a/T4g instances with no RuntimeClass, KVM device or microVM device plugin. Live conformance therefore requires a dedicated tainted KVM-capable node pool; absence on the present nodes is expected evidence, not a pass.\n\n## Delivery\n\nAn entry and immutable-boot-artifact gate precedes a minimal workspace/exec slice. PTY, egress, secret slots and multi-VM workload orchestration are later stories.\n","owner":"substrate","summary":"A KVM- and jailer-gated direct Firecracker backend runs immutable boot artifacts and confined workspace/exec slices with truthful capability facts.","tags":["driver","firecracker","microvm"],"title":"Direct Firecracker driver"},"args":{"command":"create-entity","data":{"body":"# Epic: Direct Firecracker driver\n\n## Outcome\n\nA Substrate driver can execute one bounded job in a fresh Firecracker microVM while preserving durable operation semantics, immutable input provenance, least-authority mounts, resource bounds and exact observations.\n\n## Boundaries\n\nThe driver talks directly to Firecracker and its jailer; it does not shell out to a general VM manager or ask Kubernetes to emulate the driver contract. Images and kernel/rootfs artifacts are operator-selected immutable deployment inputs, never caller-controlled host paths. Missing /dev/kvm, jailer prerequisites or artifact verification are named unserved refusals.\n\n## Environment constraint\n\nThe currently observed development EKS nodes are T3a/T4g instances with no RuntimeClass, KVM device or microVM device plugin. Live conformance therefore requires a dedicated tainted KVM-capable node pool; absence on the present nodes is expected evidence, not a pass.\n\n## Delivery\n\nAn entry and immutable-boot-artifact gate precedes a minimal workspace/exec slice. PTY, egress, secret slots and multi-VM workload orchestration are later stories.\n","owner":"substrate","status":"draft","summary":"A KVM- and jailer-gated direct Firecracker backend runs immutable boot artifacts and confined workspace/exec slices with truthful capability facts.","tags":["driver","firecracker","microvm"],"title":"Direct Firecracker driver"},"entity_type":"aep.epic/v1","locator":"ep://planning/store/epic/firecracker-driver"},"payload":{"actor":"human:timo","at":1788216677952,"causation":"cmd-new-epic-firecracker-driver","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"epic:firecracker-driver@1#0~056d5730c3c9be70","executor":null,"recorded_at":"2026-08-31T22:51:17Z"}}
{"entity":"epic","version":1,"id":"firecracker-driver","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"depends_on":"epic:remote-serving"}]},"args":{"command":"create-relation","kind":"depends_on","source":"01MEM0000000000000227","target":"01MEM0000000000000016"},"payload":{"actor":"human:timo","at":1788216677959,"causation":"cmd-epic-firecracker-driver-depends_on-epic-remote-serving","change":{"change":"related","relation":"depends_on","target":"epic:remote-serving"},"correlation":"protocol-artifact-relate","event_id":"epic:firecracker-driver@1#0~9cd806c788ab48e6","executor":null,"recorded_at":"2026-08-31T22:51:17Z"}}
{"entity":"story","version":1,"id":"promote-development-contract-frontier","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Story: The daemon advertises the implemented contract frontier\n\n## Outcome\n\nThe daemon advertises the newest fully implemented, gated additive contract bundle instead of substrate-wire/0.4.0, and a consumer can pin that claim without learning repository internals.\n\n## Context\n\nDevelopment bundles through 0.11.0 are already implemented while the header remains 0.4.0. The substrate-hardening worktree is preparing an additional 0.12.0 scoped-workspace-access successor and SDK support. This story selects the actual current successor when its dependencies merge; it does not assume an unmerged bundle number.\n\n## Acceptance\n\nBefore the header moves, an ADR or accepted design names the affected consumers and migration order. Every advertised operation, schema, refusal, fact and executable vector passes the shipped-binary clean-room lane. A clean-room Rust SDK fixture pins the new claim and rejects both an older and an unknown claim. The header changes atomically with release notes and consumer notification. Frozen bundles remain byte-identical, and the promoted bundle is still described as development unless a separate stability decision says otherwise.\n\n## Out of Scope\n\nNew capability behavior, contract stability, protocol identifier renames and a generic version-negotiation protocol.\n","owner":"substrate","summary":"A clean-room consumer pins the current additive bundle before the daemon advances x-b10x-contract from 0.4.0.","tags":["contract","release","remote"],"title":"The daemon advertises the implemented contract frontier"},"args":{"command":"create-entity","data":{"body":"# Story: The daemon advertises the implemented contract frontier\n\n## Outcome\n\nThe daemon advertises the newest fully implemented, gated additive contract bundle instead of substrate-wire/0.4.0, and a consumer can pin that claim without learning repository internals.\n\n## Context\n\nDevelopment bundles through 0.11.0 are already implemented while the header remains 0.4.0. The substrate-hardening worktree is preparing an additional 0.12.0 scoped-workspace-access successor and SDK support. This story selects the actual current successor when its dependencies merge; it does not assume an unmerged bundle number.\n\n## Acceptance\n\nBefore the header moves, an ADR or accepted design names the affected consumers and migration order. Every advertised operation, schema, refusal, fact and executable vector passes the shipped-binary clean-room lane. A clean-room Rust SDK fixture pins the new claim and rejects both an older and an unknown claim. The header changes atomically with release notes and consumer notification. Frozen bundles remain byte-identical, and the promoted bundle is still described as development unless a separate stability decision says otherwise.\n\n## Out of Scope\n\nNew capability behavior, contract stability, protocol identifier renames and a generic version-negotiation protocol.\n","owner":"substrate","status":"draft","summary":"A clean-room consumer pins the current additive bundle before the daemon advances x-b10x-contract from 0.4.0.","tags":["contract","release","remote"],"title":"The daemon advertises the implemented contract frontier"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/promote-development-contract-frontier"},"payload":{"actor":"human:timo","at":1788216719585,"causation":"cmd-new-story-promote-development-contract-frontier","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:promote-development-contract-frontier@1#0~bdf62be732feb7c3","executor":null,"recorded_at":"2026-08-31T22:51:59Z"}}
{"entity":"story","version":1,"id":"promote-development-contract-frontier","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:release-hardening"}]},"args":{"command":"create-relation","kind":"decomposes","source":"01MEM0000000000000232","target":"01MEM0000000000000016"},"payload":{"actor":"human:timo","at":1788216719601,"causation":"cmd-story-promote-development-contract-frontier-decomposes-epic-release-hardening","change":{"change":"related","relation":"decomposes","target":"epic:release-hardening"},"correlation":"protocol-artifact-relate","event_id":"story:promote-development-contract-frontier@1#0~85b09ac36e3f33d7","executor":null,"recorded_at":"2026-08-31T22:51:59Z"}}
{"entity":"story","version":1,"id":"promote-development-contract-frontier","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:release-hardening"},{"depends_on":"story:confinement-runtime-hardening"}]},"args":{"command":"create-relation","kind":"depends_on","source":"01MEM0000000000000232","target":"01MEM0000000000000043"},"payload":{"actor":"human:timo","at":1788216719610,"causation":"cmd-story-promote-development-contract-frontier-depends_on-story-confinement-runtime-hardening","change":{"change":"related","relation":"depends_on","target":"story:confinement-runtime-hardening"},"correlation":"protocol-artifact-relate","event_id":"story:promote-development-contract-frontier@1#0~b1a50accbc736626","executor":null,"recorded_at":"2026-08-31T22:51:59Z"}}
{"entity":"story","version":1,"id":"promote-development-contract-frontier","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:release-hardening"},{"depends_on":"story:confinement-runtime-hardening"},{"depends_on":"story:aperture-install-failure-loses-its-errno"}]},"args":{"command":"create-relation","kind":"depends_on","source":"01MEM0000000000000232","target":"01MEM0000000000000037"},"payload":{"actor":"human:timo","at":1788216719618,"causation":"cmd-story-promote-development-contract-frontier-depends_on-story-aperture-install-failure-loses-its-errno","change":{"change":"related","relation":"depends_on","target":"story:aperture-install-failure-loses-its-errno"},"correlation":"protocol-artifact-relate","event_id":"story:promote-development-contract-frontier@1#0~334ad6e467b03791","executor":null,"recorded_at":"2026-08-31T22:51:59Z"}}
{"entity":"story","version":1,"id":"promote-development-contract-frontier","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:release-hardening"},{"depends_on":"story:confinement-runtime-hardening"},{"depends_on":"story:aperture-install-failure-loses-its-errno"},{"depends_on":"story:contract-bundle-oci-artifact"}]},"args":{"command":"create-relation","kind":"depends_on","source":"01MEM0000000000000232","target":"01MEM0000000000000046"},"payload":{"actor":"human:timo","at":1788216719626,"causation":"cmd-story-promote-development-contract-frontier-depends_on-story-contract-bundle-oci-artifact","change":{"change":"related","relation":"depends_on","target":"story:contract-bundle-oci-artifact"},"correlation":"protocol-artifact-relate","event_id":"story:promote-development-contract-frontier@1#0~86633dcca3f6fcd9","executor":null,"recorded_at":"2026-08-31T22:51:59Z"}}
{"entity":"story","version":1,"id":"sdk-promoted-contract-parity","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Story: The Rust SDK covers the promoted contract\n\n## Outcome\n\nA Rust caller can use every supported operation and confinement option in the contract the daemon advertises, with typed requests, observations and refusals.\n\n## Current overlap\n\nThe substrate-hardening worktree already adds SDK builders for workspace access, apertures, scratch, measurements, read-only roots, secret slots and capsules, plus exact applied and usage observations. That code is input to this story and must be reviewed or merged, not reimplemented. This story closes the remaining promoted-version and PTY/metrics parity after that work lands.\n\n## Acceptance\n\nThe SDK supports PTY sessions and resize, workspace and scratch quotas, exact measurement selection and terminal usage, live metrics retrieval/streaming, workspace access, apertures, read-only roots, secret slots and execution capsules where the advertised bundle supports them. It preserves operation IDs across ambiguous transport outcomes and returns named refusals without collapsing them into strings. A clean-room journey uses only the published SDK API against the shipped daemon binary and asserts the promoted header. Driver names and host-library types never enter the public API.\n\n## Out of Scope\n\nRemote HTTPS transport, product policy helpers and automatic limit selection.\n","owner":"substrate","summary":"Typed builders and observations cover the promoted PTY, confinement, quota and metrics surface without exposing driver internals.","tags":["contract","sdk"],"title":"The Rust SDK covers the promoted contract"},"args":{"command":"create-entity","data":{"body":"# Story: The Rust SDK covers the promoted contract\n\n## Outcome\n\nA Rust caller can use every supported operation and confinement option in the contract the daemon advertises, with typed requests, observations and refusals.\n\n## Current overlap\n\nThe substrate-hardening worktree already adds SDK builders for workspace access, apertures, scratch, measurements, read-only roots, secret slots and capsules, plus exact applied and usage observations. That code is input to this story and must be reviewed or merged, not reimplemented. This story closes the remaining promoted-version and PTY/metrics parity after that work lands.\n\n## Acceptance\n\nThe SDK supports PTY sessions and resize, workspace and scratch quotas, exact measurement selection and terminal usage, live metrics retrieval/streaming, workspace access, apertures, read-only roots, secret slots and execution capsules where the advertised bundle supports them. It preserves operation IDs across ambiguous transport outcomes and returns named refusals without collapsing them into strings. A clean-room journey uses only the published SDK API against the shipped daemon binary and asserts the promoted header. Driver names and host-library types never enter the public API.\n\n## Out of Scope\n\nRemote HTTPS transport, product policy helpers and automatic limit selection.\n","owner":"substrate","status":"draft","summary":"Typed builders and observations cover the promoted PTY, confinement, quota and metrics surface without exposing driver internals.","tags":["contract","sdk"],"title":"The Rust SDK covers the promoted contract"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/sdk-promoted-contract-parity"},"payload":{"actor":"human:timo","at":1788216719847,"causation":"cmd-new-story-sdk-promoted-contract-parity","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:sdk-promoted-contract-parity@1#0~5827ea85e143dba4","executor":null,"recorded_at":"2026-08-31T22:51:59Z"}}
{"entity":"story","version":1,"id":"sdk-promoted-contract-parity","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:rust-sdk"}]},"args":{"command":"create-relation","kind":"decomposes","source":"01MEM0000000000000243","target":"01MEM0000000000000025"},"payload":{"actor":"human:timo","at":1788216719857,"causation":"cmd-story-sdk-promoted-contract-parity-decomposes-epic-rust-sdk","change":{"change":"related","relation":"decomposes","target":"epic:rust-sdk"},"correlation":"protocol-artifact-relate","event_id":"story:sdk-promoted-contract-parity@1#0~1560d2ce00c05ad9","executor":null,"recorded_at":"2026-08-31T22:51:59Z"}}
{"entity":"story","version":1,"id":"sdk-promoted-contract-parity","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:rust-sdk"},{"depends_on":"story:promote-development-contract-frontier"}]},"args":{"command":"create-relation","kind":"depends_on","source":"01MEM0000000000000243","target":"01MEM0000000000000085"},"payload":{"actor":"human:timo","at":1788216719865,"causation":"cmd-story-sdk-promoted-contract-parity-depends_on-story-promote-development-contract-frontier","change":{"change":"related","relation":"depends_on","target":"story:promote-development-contract-frontier"},"correlation":"protocol-artifact-relate","event_id":"story:sdk-promoted-contract-parity@1#0~bbdf6742ba62b778","executor":null,"recorded_at":"2026-08-31T22:51:59Z"}}
{"entity":"story","version":1,"id":"production-tls-control-listener","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Story: Production control traffic uses TLS\n\n## Outcome\n\nA remotely deployed daemon serves its HTTP and WebSocket control plane over TLS with explicit operator-provided identity and trust configuration.\n\n## Design gate\n\nAn accepted design or ADR fixes certificate loading and rotation, client authentication mode, listener binding, forwarded-address treatment, WebSocket behavior, and the exact startup refusals before code. The Unix socket and kernel-peer-credential subject derivation remain unchanged.\n\n## Acceptance\n\nThe daemon uses rustls and accepts no production plaintext listener. A non-loopback static-bearer configuration is refused at startup. Certificate and key files must be explicit, owner-safe and reloadable without exposing bytes in logs, events or metrics. Tests cover valid TLS, unknown CA, wrong server name, expired/not-yet-valid certificates, missing key material, plaintext attempts, WebSocket upgrade, rotation and shutdown. Development plaintext remains loopback-only and visibly marked development.\n\n## Out of Scope\n\nPublic ingress controllers, ACME automation, service-mesh-specific configuration and hosted token verification.\n","owner":"substrate","summary":"A rustls HTTPS/WSS listener with explicit certificates replaces development plaintext for non-loopback deployments.","tags":["remote","security","tls"],"title":"Production control traffic uses TLS"},"args":{"command":"create-entity","data":{"body":"# Story: Production control traffic uses TLS\n\n## Outcome\n\nA remotely deployed daemon serves its HTTP and WebSocket control plane over TLS with explicit operator-provided identity and trust configuration.\n\n## Design gate\n\nAn accepted design or ADR fixes certificate loading and rotation, client authentication mode, listener binding, forwarded-address treatment, WebSocket behavior, and the exact startup refusals before code. The Unix socket and kernel-peer-credential subject derivation remain unchanged.\n\n## Acceptance\n\nThe daemon uses rustls and accepts no production plaintext listener. A non-loopback static-bearer configuration is refused at startup. Certificate and key files must be explicit, owner-safe and reloadable without exposing bytes in logs, events or metrics. Tests cover valid TLS, unknown CA, wrong server name, expired/not-yet-valid certificates, missing key material, plaintext attempts, WebSocket upgrade, rotation and shutdown. Development plaintext remains loopback-only and visibly marked development.\n\n## Out of Scope\n\nPublic ingress controllers, ACME automation, service-mesh-specific configuration and hosted token verification.\n","owner":"substrate","status":"draft","summary":"A rustls HTTPS/WSS listener with explicit certificates replaces development plaintext for non-loopback deployments.","tags":["remote","security","tls"],"title":"Production control traffic uses TLS"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/production-tls-control-listener"},"payload":{"actor":"human:timo","at":1788216720080,"causation":"cmd-new-story-production-tls-control-listener","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:production-tls-control-listener@1#0~5ca5159d8d7f5479","executor":null,"recorded_at":"2026-08-31T22:52:00Z"}}
{"entity":"story","version":1,"id":"production-tls-control-listener","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:remote-serving"}]},"args":{"command":"create-relation","kind":"decomposes","source":"01MEM0000000000000250","target":"01MEM0000000000000019"},"payload":{"actor":"human:timo","at":1788216720090,"causation":"cmd-story-production-tls-control-listener-decomposes-epic-remote-serving","change":{"change":"related","relation":"decomposes","target":"epic:remote-serving"},"correlation":"protocol-artifact-relate","event_id":"story:production-tls-control-listener@1#0~2a18772c52dd7b3a","executor":null,"recorded_at":"2026-08-31T22:52:00Z"}}
{"entity":"story","version":1,"id":"production-tls-control-listener","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:remote-serving"},{"depends_on":"story:promote-development-contract-frontier"}]},"args":{"command":"create-relation","kind":"depends_on","source":"01MEM0000000000000250","target":"01MEM0000000000000085"},"payload":{"actor":"human:timo","at":1788216720098,"causation":"cmd-story-production-tls-control-listener-depends_on-story-promote-development-contract-frontier","change":{"change":"related","relation":"depends_on","target":"story:promote-development-contract-frontier"},"correlation":"protocol-artifact-relate","event_id":"story:production-tls-control-listener@1#0~84952f359ae7e96c","executor":null,"recorded_at":"2026-08-31T22:52:00Z"}}
{"entity":"story","version":1,"id":"hosted-trust-envelope-admission","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Story: Hosted requests use the accepted trust envelope\n\n## Outcome\n\nA production network request is admitted only from an Identity-issued, short-lived, scoped trust envelope, and Substrate derives its subject and authority from the verified envelope rather than caller-written HTTP data.\n\n## Coordination gate\n\nBefore code, an accepted Substrate design and the corresponding Identity audience/profile decision agree on the exact audience, key distribution, revocation semantics and scopes. The existing profile uses audience urn:b10x:substrate, EdDSA, a five-minute maximum lifetime, a sixty-second revocation objective, one audience, and observe/workspaces/exec scopes. Session authority requires a coordinated profile revision; Substrate must not invent it locally.\n\n## Acceptance\n\nThe verifier fails closed on signature, issuer, audience, time, key, revocation and scope errors with named refusals. Tenant, actor, uid and subject headers or body fields never override verified claims. Keys rotate without restart and stale-key behavior is bounded. Authorization is checked per route and mutation before durable admission. Unix-socket requests continue to derive local:<uid> from kernel peer credentials and need no hosted envelope. Conformance vectors prove every negative case and that credential bytes and claims do not enter diagnostics or metrics.\n\n## Out of Scope\n\nIdentity implementation, browser sessions, product roles, fleet placement and connector grants.\n","owner":"substrate","summary":"Identity-issued short-lived scoped authority derives the remote subject and refuses audience, signature, expiry, revocation and scope failures by name.","tags":["identity","remote","security"],"title":"Hosted requests use the accepted trust envelope"},"args":{"command":"create-entity","data":{"body":"# Story: Hosted requests use the accepted trust envelope\n\n## Outcome\n\nA production network request is admitted only from an Identity-issued, short-lived, scoped trust envelope, and Substrate derives its subject and authority from the verified envelope rather than caller-written HTTP data.\n\n## Coordination gate\n\nBefore code, an accepted Substrate design and the corresponding Identity audience/profile decision agree on the exact audience, key distribution, revocation semantics and scopes. The existing profile uses audience urn:b10x:substrate, EdDSA, a five-minute maximum lifetime, a sixty-second revocation objective, one audience, and observe/workspaces/exec scopes. Session authority requires a coordinated profile revision; Substrate must not invent it locally.\n\n## Acceptance\n\nThe verifier fails closed on signature, issuer, audience, time, key, revocation and scope errors with named refusals. Tenant, actor, uid and subject headers or body fields never override verified claims. Keys rotate without restart and stale-key behavior is bounded. Authorization is checked per route and mutation before durable admission. Unix-socket requests continue to derive local:<uid> from kernel peer credentials and need no hosted envelope. Conformance vectors prove every negative case and that credential bytes and claims do not enter diagnostics or metrics.\n\n## Out of Scope\n\nIdentity implementation, browser sessions, product roles, fleet placement and connector grants.\n","owner":"substrate","status":"draft","summary":"Identity-issued short-lived scoped authority derives the remote subject and refuses audience, signature, expiry, revocation and scope failures by name.","tags":["identity","remote","security"],"title":"Hosted requests use the accepted trust envelope"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/hosted-trust-envelope-admission"},"payload":{"actor":"human:timo","at":1788216720313,"causation":"cmd-new-story-hosted-trust-envelope-admission","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:hosted-trust-envelope-admission@1#0~5afda5862a53593b","executor":null,"recorded_at":"2026-08-31T22:52:00Z"}}
{"entity":"story","version":1,"id":"hosted-trust-envelope-admission","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:remote-serving"}]},"args":{"command":"create-relation","kind":"decomposes","source":"01MEM0000000000000257","target":"01MEM0000000000000019"},"payload":{"actor":"human:timo","at":1788216720322,"causation":"cmd-story-hosted-trust-envelope-admission-decomposes-epic-remote-serving","change":{"change":"related","relation":"decomposes","target":"epic:remote-serving"},"correlation":"protocol-artifact-relate","event_id":"story:hosted-trust-envelope-admission@1#0~c7c8921a2851b9e5","executor":null,"recorded_at":"2026-08-31T22:52:00Z"}}
{"entity":"story","version":1,"id":"hosted-trust-envelope-admission","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:remote-serving"},{"depends_on":"story:production-tls-control-listener"}]},"args":{"command":"create-relation","kind":"depends_on","source":"01MEM0000000000000257","target":"01MEM0000000000000085"},"payload":{"actor":"human:timo","at":1788216720337,"causation":"cmd-story-hosted-trust-envelope-admission-depends_on-story-production-tls-control-listener","change":{"change":"related","relation":"depends_on","target":"story:production-tls-control-listener"},"correlation":"protocol-artifact-relate","event_id":"story:hosted-trust-envelope-admission@1#0~d4ba0e67b16bdf13","executor":null,"recorded_at":"2026-08-31T22:52:00Z"}}
{"entity":"story","version":1,"id":"remote-sdk-transport","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Story: The Rust SDK connects over HTTPS and WSS\n\n## Outcome\n\nA Rust service can address a remote Substrate instance with the same typed workspace, operation, execution, event and session handles used over a Unix socket.\n\n## Acceptance\n\nThe builder requires an explicit HTTPS endpoint, trust roots and expected server identity; it never disables verification. An asynchronous token provider supplies short-lived authority per request and can refresh after an authorization failure without replaying a completed mutation. HTTP and WSS share TLS/auth configuration. Operation IDs survive timeouts, disconnects and token refresh. Tests cover unknown roots, name mismatch, expired authority, rotation, lost mutation responses, event gaps and WebSocket reconnect requiring fresh session authority. Unix and managed-daemon modes remain source-compatible.\n\n## Out of Scope\n\nCredential storage, Identity login flows, load balancing, automatic fleet selection and policy defaults.\n","owner":"substrate","summary":"Explicit roots, server identity and an asynchronous token provider extend the SDK without changing its Unix-socket behavior.","tags":["remote","sdk","tls"],"title":"The Rust SDK connects over HTTPS and WSS"},"args":{"command":"create-entity","data":{"body":"# Story: The Rust SDK connects over HTTPS and WSS\n\n## Outcome\n\nA Rust service can address a remote Substrate instance with the same typed workspace, operation, execution, event and session handles used over a Unix socket.\n\n## Acceptance\n\nThe builder requires an explicit HTTPS endpoint, trust roots and expected server identity; it never disables verification. An asynchronous token provider supplies short-lived authority per request and can refresh after an authorization failure without replaying a completed mutation. HTTP and WSS share TLS/auth configuration. Operation IDs survive timeouts, disconnects and token refresh. Tests cover unknown roots, name mismatch, expired authority, rotation, lost mutation responses, event gaps and WebSocket reconnect requiring fresh session authority. Unix and managed-daemon modes remain source-compatible.\n\n## Out of Scope\n\nCredential storage, Identity login flows, load balancing, automatic fleet selection and policy defaults.\n","owner":"substrate","status":"draft","summary":"Explicit roots, server identity and an asynchronous token provider extend the SDK without changing its Unix-socket behavior.","tags":["remote","sdk","tls"],"title":"The Rust SDK connects over HTTPS and WSS"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/remote-sdk-transport"},"payload":{"actor":"human:timo","at":1788216720565,"causation":"cmd-new-story-remote-sdk-transport","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:remote-sdk-transport@1#0~0c6a0ecae25fc151","executor":null,"recorded_at":"2026-08-31T22:52:00Z"}}
{"entity":"story","version":1,"id":"remote-sdk-transport","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:remote-serving"}]},"args":{"command":"create-relation","kind":"decomposes","source":"01MEM0000000000000264","target":"01MEM0000000000000019"},"payload":{"actor":"human:timo","at":1788216720573,"causation":"cmd-story-remote-sdk-transport-decomposes-epic-remote-serving","change":{"change":"related","relation":"decomposes","target":"epic:remote-serving"},"correlation":"protocol-artifact-relate","event_id":"story:remote-sdk-transport@1#0~86d16eda00b4de93","executor":null,"recorded_at":"2026-08-31T22:52:00Z"}}
{"entity":"story","version":1,"id":"remote-sdk-transport","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:remote-serving"},{"depends_on":"story:production-tls-control-listener"}]},"args":{"command":"create-relation","kind":"depends_on","source":"01MEM0000000000000264","target":"01MEM0000000000000088"},"payload":{"actor":"human:timo","at":1788216720582,"causation":"cmd-story-remote-sdk-transport-depends_on-story-production-tls-control-listener","change":{"change":"related","relation":"depends_on","target":"story:production-tls-control-listener"},"correlation":"protocol-artifact-relate","event_id":"story:remote-sdk-transport@1#0~c0f4f9443b5c35be","executor":null,"recorded_at":"2026-08-31T22:52:00Z"}}
{"entity":"story","version":1,"id":"remote-sdk-transport","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:remote-serving"},{"depends_on":"story:production-tls-control-listener"},{"depends_on":"story:hosted-trust-envelope-admission"}]},"args":{"command":"create-relation","kind":"depends_on","source":"01MEM0000000000000264","target":"01MEM0000000000000073"},"payload":{"actor":"human:timo","at":1788216720590,"causation":"cmd-story-remote-sdk-transport-depends_on-story-hosted-trust-envelope-admission","change":{"change":"related","relation":"depends_on","target":"story:hosted-trust-envelope-admission"},"correlation":"protocol-artifact-relate","event_id":"story:remote-sdk-transport@1#0~aa918dc6c6807214","executor":null,"recorded_at":"2026-08-31T22:52:00Z"}}
{"entity":"story","version":1,"id":"remote-sdk-transport","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:remote-serving"},{"depends_on":"story:production-tls-control-listener"},{"depends_on":"story:hosted-trust-envelope-admission"},{"depends_on":"story:sdk-promoted-contract-parity"}]},"args":{"command":"create-relation","kind":"depends_on","source":"01MEM0000000000000264","target":"01MEM0000000000000118"},"payload":{"actor":"human:timo","at":1788216720600,"causation":"cmd-story-remote-sdk-transport-depends_on-story-sdk-promoted-contract-parity","change":{"change":"related","relation":"depends_on","target":"story:sdk-promoted-contract-parity"},"correlation":"protocol-artifact-relate","event_id":"story:remote-sdk-transport@1#0~3ff3f8d3fee89228","executor":null,"recorded_at":"2026-08-31T22:52:00Z"}}
{"entity":"story","version":1,"id":"node-bound-kubernetes-serving-profile","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Story: Kubernetes serves one stable address per node-bound daemon\n\n## Outcome\n\nOperators can install a public, signed Helm chart that gives each host-backed Substrate daemon a stable network identity, durable state and an explicit selected-node relationship.\n\n## Acceptance\n\nThe chart deploys a StatefulSet on a dedicated labelled and tainted node pool with pod anti-affinity, one owner-private PVC per ordinal, a headless Service and stable per-pod DNS. Callers address a specific pod identity; no generic Service round-robins mutation requests across independent operation ledgers. TLS identity and hosted trust are mandatory outside loopback. Values expose explicit driver prerequisites and refuse unsupported privilege, cgroup, mount or quota postures rather than claiming absent facts. RBAC is minimal, default NetworkPolicy denies unrelated ingress/egress, Pod Security settings are documented, and upgrades preserve identity and state. The chart is packaged, provenance-attested, anonymously fetchable and tested by install/upgrade/uninstall conformance.\n\n## Boundaries\n\nThis profile runs the host driver inside Kubernetes. It is not the namespace-scoped Kubernetes driver, a fleet scheduler or high availability for one ledger.\n","owner":"substrate","summary":"A signed public Helm chart deploys stateful host-backed daemons without round-robin mutation routing.","tags":["helm","kubernetes","remote"],"title":"Kubernetes serves one stable address per node-bound daemon"},"args":{"command":"create-entity","data":{"body":"# Story: Kubernetes serves one stable address per node-bound daemon\n\n## Outcome\n\nOperators can install a public, signed Helm chart that gives each host-backed Substrate daemon a stable network identity, durable state and an explicit selected-node relationship.\n\n## Acceptance\n\nThe chart deploys a StatefulSet on a dedicated labelled and tainted node pool with pod anti-affinity, one owner-private PVC per ordinal, a headless Service and stable per-pod DNS. Callers address a specific pod identity; no generic Service round-robins mutation requests across independent operation ledgers. TLS identity and hosted trust are mandatory outside loopback. Values expose explicit driver prerequisites and refuse unsupported privilege, cgroup, mount or quota postures rather than claiming absent facts. RBAC is minimal, default NetworkPolicy denies unrelated ingress/egress, Pod Security settings are documented, and upgrades preserve identity and state. The chart is packaged, provenance-attested, anonymously fetchable and tested by install/upgrade/uninstall conformance.\n\n## Boundaries\n\nThis profile runs the host driver inside Kubernetes. It is not the namespace-scoped Kubernetes driver, a fleet scheduler or high availability for one ledger.\n","owner":"substrate","status":"draft","summary":"A signed public Helm chart deploys stateful host-backed daemons without round-robin mutation routing.","tags":["helm","kubernetes","remote"],"title":"Kubernetes serves one stable address per node-bound daemon"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/node-bound-kubernetes-serving-profile"},"payload":{"actor":"human:timo","at":1788216750532,"causation":"cmd-new-story-node-bound-kubernetes-serving-profile","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:node-bound-kubernetes-serving-profile@1#0~2f942ce6fa56843e","executor":null,"recorded_at":"2026-08-31T22:52:30Z"}}
{"entity":"story","version":1,"id":"node-bound-kubernetes-serving-profile","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:kubernetes-deployment-and-driver"}]},"args":{"command":"create-relation","kind":"decomposes","source":"01MEM0000000000000275","target":"01MEM0000000000000010"},"payload":{"actor":"human:timo","at":1788216750547,"causation":"cmd-story-node-bound-kubernetes-serving-profile-decomposes-epic-kubernetes-deployment-and-driver","change":{"change":"related","relation":"decomposes","target":"epic:kubernetes-deployment-and-driver"},"correlation":"protocol-artifact-relate","event_id":"story:node-bound-kubernetes-serving-profile@1#0~96057b8031bfc3e2","executor":null,"recorded_at":"2026-08-31T22:52:30Z"}}
{"entity":"story","version":1,"id":"node-bound-kubernetes-serving-profile","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:kubernetes-deployment-and-driver"},{"depends_on":"story:production-tls-control-listener"}]},"args":{"command":"create-relation","kind":"depends_on","source":"01MEM0000000000000275","target":"01MEM0000000000000088"},"payload":{"actor":"human:timo","at":1788216750554,"causation":"cmd-story-node-bound-kubernetes-serving-profile-depends_on-story-production-tls-control-listener","change":{"change":"related","relation":"depends_on","target":"story:production-tls-control-listener"},"correlation":"protocol-artifact-relate","event_id":"story:node-bound-kubernetes-serving-profile@1#0~999f378ac9e307e8","executor":null,"recorded_at":"2026-08-31T22:52:30Z"}}
{"entity":"story","version":1,"id":"node-bound-kubernetes-serving-profile","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:kubernetes-deployment-and-driver"},{"depends_on":"story:production-tls-control-listener"},{"depends_on":"story:hosted-trust-envelope-admission"}]},"args":{"command":"create-relation","kind":"depends_on","source":"01MEM0000000000000275","target":"01MEM0000000000000073"},"payload":{"actor":"human:timo","at":1788216750562,"causation":"cmd-story-node-bound-kubernetes-serving-profile-depends_on-story-hosted-trust-envelope-admission","change":{"change":"related","relation":"depends_on","target":"story:hosted-trust-envelope-admission"},"correlation":"protocol-artifact-relate","event_id":"story:node-bound-kubernetes-serving-profile@1#0~f53f08b49a81f516","executor":null,"recorded_at":"2026-08-31T22:52:30Z"}}
{"entity":"story","version":1,"id":"operational-health-and-telemetry","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Story: Operators get safe health and service telemetry\n\n## Outcome\n\nKubernetes and service operators can tell whether a daemon process is alive, ready to admit durable work and healthy under load without scraping product execution records.\n\n## Acceptance\n\nA liveness endpoint reports only process-loop health. Readiness checks durable store access, schema compatibility, required driver probes and maintenance-loop viability without mutating caller resources. A separate OpenMetrics endpoint exposes bounded service counters, histograms and gauges for request outcomes, refusal classes, operation latency, queue saturation, ledger maintenance, driver dispatch and process resource use. Labels exclude subject, tenant, workspace, exec, operation, argv, path, destination, token and secret data. Tests assert metric-name stability, bounded label sets, no sensitive values, readiness transitions during store/driver failure, and correct Kubernetes probe behavior.\n\n## Contract boundary\n\nThe operational endpoint is distinct from the authenticated per-execution metrics routes in the development wire contract. It does not add a second product observation ledger or promise mean-memory estimates the kernel does not supply exactly.\n","owner":"substrate","summary":"Dedicated liveness, readiness and OpenMetrics endpoints report service health without leaking subjects, credentials or high-cardinality resource identifiers.","tags":["observability","operations","remote"],"title":"Operators get safe health and service telemetry"},"args":{"command":"create-entity","data":{"body":"# Story: Operators get safe health and service telemetry\n\n## Outcome\n\nKubernetes and service operators can tell whether a daemon process is alive, ready to admit durable work and healthy under load without scraping product execution records.\n\n## Acceptance\n\nA liveness endpoint reports only process-loop health. Readiness checks durable store access, schema compatibility, required driver probes and maintenance-loop viability without mutating caller resources. A separate OpenMetrics endpoint exposes bounded service counters, histograms and gauges for request outcomes, refusal classes, operation latency, queue saturation, ledger maintenance, driver dispatch and process resource use. Labels exclude subject, tenant, workspace, exec, operation, argv, path, destination, token and secret data. Tests assert metric-name stability, bounded label sets, no sensitive values, readiness transitions during store/driver failure, and correct Kubernetes probe behavior.\n\n## Contract boundary\n\nThe operational endpoint is distinct from the authenticated per-execution metrics routes in the development wire contract. It does not add a second product observation ledger or promise mean-memory estimates the kernel does not supply exactly.\n","owner":"substrate","status":"draft","summary":"Dedicated liveness, readiness and OpenMetrics endpoints report service health without leaking subjects, credentials or high-cardinality resource identifiers.","tags":["observability","operations","remote"],"title":"Operators get safe health and service telemetry"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/operational-health-and-telemetry"},"payload":{"actor":"human:timo","at":1788216750792,"causation":"cmd-new-story-operational-health-and-telemetry","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:operational-health-and-telemetry@1#0~874191ae7466e525","executor":null,"recorded_at":"2026-08-31T22:52:30Z"}}
{"entity":"story","version":1,"id":"operational-health-and-telemetry","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:remote-serving"}]},"args":{"command":"create-relation","kind":"decomposes","source":"01MEM0000000000000284","target":"01MEM0000000000000019"},"payload":{"actor":"human:timo","at":1788216750801,"causation":"cmd-story-operational-health-and-telemetry-decomposes-epic-remote-serving","change":{"change":"related","relation":"decomposes","target":"epic:remote-serving"},"correlation":"protocol-artifact-relate","event_id":"story:operational-health-and-telemetry@1#0~ea6b37e91544253d","executor":null,"recorded_at":"2026-08-31T22:52:30Z"}}
{"entity":"story","version":1,"id":"operational-health-and-telemetry","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:remote-serving"},{"depends_on":"story:production-tls-control-listener"}]},"args":{"command":"create-relation","kind":"depends_on","source":"01MEM0000000000000284","target":"01MEM0000000000000091"},"payload":{"actor":"human:timo","at":1788216750808,"causation":"cmd-story-operational-health-and-telemetry-depends_on-story-production-tls-control-listener","change":{"change":"related","relation":"depends_on","target":"story:production-tls-control-listener"},"correlation":"protocol-artifact-relate","event_id":"story:operational-health-and-telemetry@1#0~887af9193c13c2c6","executor":null,"recorded_at":"2026-08-31T22:52:30Z"}}
{"entity":"story","version":1,"id":"remote-clean-room-conformance","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Story: A clean-room client proves remote Substrate\n\n## Outcome\n\nA deployed Substrate endpoint can be certified from outside its process, container and repository checkout using only its public contract and a scoped test authority.\n\n## Acceptance\n\nA Rust conformance runner accepts an HTTPS endpoint, trust roots and a token-provider command or file descriptor without persisting credentials. It verifies machine facts, workspace/file/exec lifecycle, operation-id recovery after a deliberately lost response, restart-to-unknown behavior, event ordering and retention gaps, TLS/auth negatives, raw-pipe or PTY WSS attachment when advertised, exact resource observations and cleanup. Capability-dependent cases are reported passed, failed or absent; absent is never counted as passed. The runner produces a machine-readable signed report with endpoint identity, advertised contract, capability snapshot digest and case inventory but no credential, argv output or tenant data.\n\n## Boundaries\n\nThe runner is a client and links no daemon, host driver or store implementation. It does not provision clusters or choose a production fleet.\n","owner":"substrate","summary":"A black-box Rust runner verifies HTTPS/WSS, auth, durable recovery and event semantics against any deployed instance.","tags":["conformance","remote","testing"],"title":"A clean-room client proves remote Substrate"},"args":{"command":"create-entity","data":{"body":"# Story: A clean-room client proves remote Substrate\n\n## Outcome\n\nA deployed Substrate endpoint can be certified from outside its process, container and repository checkout using only its public contract and a scoped test authority.\n\n## Acceptance\n\nA Rust conformance runner accepts an HTTPS endpoint, trust roots and a token-provider command or file descriptor without persisting credentials. It verifies machine facts, workspace/file/exec lifecycle, operation-id recovery after a deliberately lost response, restart-to-unknown behavior, event ordering and retention gaps, TLS/auth negatives, raw-pipe or PTY WSS attachment when advertised, exact resource observations and cleanup. Capability-dependent cases are reported passed, failed or absent; absent is never counted as passed. The runner produces a machine-readable signed report with endpoint identity, advertised contract, capability snapshot digest and case inventory but no credential, argv output or tenant data.\n\n## Boundaries\n\nThe runner is a client and links no daemon, host driver or store implementation. It does not provision clusters or choose a production fleet.\n","owner":"substrate","status":"draft","summary":"A black-box Rust runner verifies HTTPS/WSS, auth, durable recovery and event semantics against any deployed instance.","tags":["conformance","remote","testing"],"title":"A clean-room client proves remote Substrate"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/remote-clean-room-conformance"},"payload":{"actor":"human:timo","at":1788216751091,"causation":"cmd-new-story-remote-clean-room-conformance","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:remote-clean-room-conformance@1#0~0a6149f6ad671d60","executor":null,"recorded_at":"2026-08-31T22:52:31Z"}}
{"entity":"story","version":1,"id":"remote-clean-room-conformance","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:remote-serving"}]},"args":{"command":"create-relation","kind":"decomposes","source":"01MEM0000000000000291","target":"01MEM0000000000000019"},"payload":{"actor":"human:timo","at":1788216751101,"causation":"cmd-story-remote-clean-room-conformance-decomposes-epic-remote-serving","change":{"change":"related","relation":"decomposes","target":"epic:remote-serving"},"correlation":"protocol-artifact-relate","event_id":"story:remote-clean-room-conformance@1#0~d166ba6a2f3687df","executor":null,"recorded_at":"2026-08-31T22:52:31Z"}}
{"entity":"story","version":1,"id":"remote-clean-room-conformance","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:remote-serving"},{"depends_on":"story:remote-sdk-transport"}]},"args":{"command":"create-relation","kind":"depends_on","source":"01MEM0000000000000291","target":"01MEM0000000000000109"},"payload":{"actor":"human:timo","at":1788216751110,"causation":"cmd-story-remote-clean-room-conformance-depends_on-story-remote-sdk-transport","change":{"change":"related","relation":"depends_on","target":"story:remote-sdk-transport"},"correlation":"protocol-artifact-relate","event_id":"story:remote-clean-room-conformance@1#0~c5d2854593a47326","executor":null,"recorded_at":"2026-08-31T22:52:31Z"}}
{"entity":"story","version":1,"id":"remote-clean-room-conformance","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:remote-serving"},{"depends_on":"story:remote-sdk-transport"},{"depends_on":"story:node-bound-kubernetes-serving-profile"}]},"args":{"command":"create-relation","kind":"depends_on","source":"01MEM0000000000000291","target":"01MEM0000000000000085"},"payload":{"actor":"human:timo","at":1788216751124,"causation":"cmd-story-remote-clean-room-conformance-depends_on-story-node-bound-kubernetes-serving-profile","change":{"change":"related","relation":"depends_on","target":"story:node-bound-kubernetes-serving-profile"},"correlation":"protocol-artifact-relate","event_id":"story:remote-clean-room-conformance@1#0~1a15b9360b125cc1","executor":null,"recorded_at":"2026-08-31T22:52:31Z"}}
{"entity":"story","version":1,"id":"remote-clean-room-conformance","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:remote-serving"},{"depends_on":"story:remote-sdk-transport"},{"depends_on":"story:node-bound-kubernetes-serving-profile"},{"depends_on":"story:operational-health-and-telemetry"}]},"args":{"command":"create-relation","kind":"depends_on","source":"01MEM0000000000000291","target":"01MEM0000000000000088"},"payload":{"actor":"human:timo","at":1788216751136,"causation":"cmd-story-remote-clean-room-conformance-depends_on-story-operational-health-and-telemetry","change":{"change":"related","relation":"depends_on","target":"story:operational-health-and-telemetry"},"correlation":"protocol-artifact-relate","event_id":"story:remote-clean-room-conformance@1#0~8aa3ce364acf79b6","executor":null,"recorded_at":"2026-08-31T22:52:31Z"}}
{"entity":"story","version":1,"id":"docker-workspace-and-exec-slice","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Story: The Docker driver serves workspace and exec\n\n## Outcome\n\nThe same public workspace, file, operation, event and bounded argv-only exec contract can run through a Docker-backed driver without a caller learning which driver answered.\n\n## Acceptance\n\nThe daemon connects only to the explicitly configured Docker socket and publishes the accepted root-equivalent authority fact. Every mutation is durable before the Docker API call. The driver builds a closed container specification: pinned image digest, argv array, cleared/shaped environment, read-only system image, explicit writable workspace, non-root user where the image permits it, no privileged mode, host namespaces, arbitrary bind mounts, device requests or caller-supplied runtime options. Limits, timeout, output draining, whole-container cleanup and applied observations pass the shared driver conformance journey. Unsupported sealed slots and destination apertures remain absent and requests receive named unserved refusals. Restart reconciliation binds observations to immutable container identity.\n\n## Design gate\n\nThe accepted Docker entry design and its mechanism probes must be complete before implementation. Any additional wire capability or refusal requires its successor bundle and design closure first.\n","owner":"substrate","summary":"An explicit Docker socket backs the shared workspace/exec contract with closed container specs, durable dispatch and truthful capability facts.","tags":["docker","driver"],"title":"The Docker driver serves workspace and exec"},"args":{"command":"create-entity","data":{"body":"# Story: The Docker driver serves workspace and exec\n\n## Outcome\n\nThe same public workspace, file, operation, event and bounded argv-only exec contract can run through a Docker-backed driver without a caller learning which driver answered.\n\n## Acceptance\n\nThe daemon connects only to the explicitly configured Docker socket and publishes the accepted root-equivalent authority fact. Every mutation is durable before the Docker API call. The driver builds a closed container specification: pinned image digest, argv array, cleared/shaped environment, read-only system image, explicit writable workspace, non-root user where the image permits it, no privileged mode, host namespaces, arbitrary bind mounts, device requests or caller-supplied runtime options. Limits, timeout, output draining, whole-container cleanup and applied observations pass the shared driver conformance journey. Unsupported sealed slots and destination apertures remain absent and requests receive named unserved refusals. Restart reconciliation binds observations to immutable container identity.\n\n## Design gate\n\nThe accepted Docker entry design and its mechanism probes must be complete before implementation. Any additional wire capability or refusal requires its successor bundle and design closure first.\n","owner":"substrate","status":"draft","summary":"An explicit Docker socket backs the shared workspace/exec contract with closed container specs, durable dispatch and truthful capability facts.","tags":["docker","driver"],"title":"The Docker driver serves workspace and exec"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/docker-workspace-and-exec-slice"},"payload":{"actor":"human:timo","at":1788216807488,"causation":"cmd-new-story-docker-workspace-and-exec-slice","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:docker-workspace-and-exec-slice@1#0~2fb7c268d6d5d67f","executor":null,"recorded_at":"2026-08-31T22:53:27Z"}}
{"entity":"story","version":1,"id":"docker-workspace-and-exec-slice","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:container-driver-entry"}]},"args":{"command":"create-relation","kind":"decomposes","source":"01MEM0000000000000302","target":"01MEM0000000000000004"},"payload":{"actor":"human:timo","at":1788216807497,"causation":"cmd-story-docker-workspace-and-exec-slice-decomposes-epic-container-driver-entry","change":{"change":"related","relation":"decomposes","target":"epic:container-driver-entry"},"correlation":"protocol-artifact-relate","event_id":"story:docker-workspace-and-exec-slice@1#0~615f7ed3e28afde8","executor":null,"recorded_at":"2026-08-31T22:53:27Z"}}
{"entity":"story","version":1,"id":"docker-workspace-and-exec-slice","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:container-driver-entry"},{"depends_on":"story:docker-driver-entry-gate"}]},"args":{"command":"create-relation","kind":"depends_on","source":"01MEM0000000000000302","target":"01MEM0000000000000058"},"payload":{"actor":"human:timo","at":1788216807505,"causation":"cmd-story-docker-workspace-and-exec-slice-depends_on-story-docker-driver-entry-gate","change":{"change":"related","relation":"depends_on","target":"story:docker-driver-entry-gate"},"correlation":"protocol-artifact-relate","event_id":"story:docker-workspace-and-exec-slice@1#0~ca4b889bf11e319e","executor":null,"recorded_at":"2026-08-31T22:53:27Z"}}
{"entity":"story","version":1,"id":"docker-workspace-and-exec-slice","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:container-driver-entry"},{"depends_on":"story:docker-driver-entry-gate"},{"depends_on":"story:remote-clean-room-conformance"}]},"args":{"command":"create-relation","kind":"depends_on","source":"01MEM0000000000000302","target":"01MEM0000000000000109"},"payload":{"actor":"human:timo","at":1788216807516,"causation":"cmd-story-docker-workspace-and-exec-slice-depends_on-story-remote-clean-room-conformance","change":{"change":"related","relation":"depends_on","target":"story:remote-clean-room-conformance"},"correlation":"protocol-artifact-relate","event_id":"story:docker-workspace-and-exec-slice@1#0~24029fa73368e93f","executor":null,"recorded_at":"2026-08-31T22:53:27Z"}}
{"entity":"story","version":1,"id":"docker-image-backed-workload-slice","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Story: Docker serves immutable image-backed workloads\n\n## Outcome\n\nA caller can create, start, observe, stop and retire a minimal image-backed workload through Substrate while every runtime input is resolved to immutable evidence.\n\n## Design gate\n\nBefore code, an accepted design fixes the workload and image resource state machines, digest resolution, pull authority, restart policy, lease behavior, event vocabulary, output bounds, reconciliation and refusal order. A successor bundle adds only those closed operations.\n\n## Acceptance\n\nAn operator allowlists registries and credentials outside caller payloads. A caller may name an allowed image reference, but the driver resolves it once to a digest and records that digest before workload dispatch. Mutable tags are never retained as execution identity. The closed container spec inherits the Docker exec slice authority restrictions. Workload operations are durable before Docker mutation, idempotent by operation ID, reconcile by immutable container identity after restart, and terminalize with exact observed state. Pull, digest, platform, capacity, policy and capability failures are distinct named refusals. Shared black-box conformance covers lost answers, restart, lease expiry, cleanup and unauthorized runtime options.\n\n## Out of Scope\n\nImage builds, Compose semantics, arbitrary Docker APIs, fleet scheduling and Kubernetes workloads.\n","owner":"substrate","summary":"Digest-resolved images and a minimal durable workload lifecycle extend the Docker driver without mutable-tag or caller-option authority.","tags":["docker","images","workloads"],"title":"Docker serves immutable image-backed workloads"},"args":{"command":"create-entity","data":{"body":"# Story: Docker serves immutable image-backed workloads\n\n## Outcome\n\nA caller can create, start, observe, stop and retire a minimal image-backed workload through Substrate while every runtime input is resolved to immutable evidence.\n\n## Design gate\n\nBefore code, an accepted design fixes the workload and image resource state machines, digest resolution, pull authority, restart policy, lease behavior, event vocabulary, output bounds, reconciliation and refusal order. A successor bundle adds only those closed operations.\n\n## Acceptance\n\nAn operator allowlists registries and credentials outside caller payloads. A caller may name an allowed image reference, but the driver resolves it once to a digest and records that digest before workload dispatch. Mutable tags are never retained as execution identity. The closed container spec inherits the Docker exec slice authority restrictions. Workload operations are durable before Docker mutation, idempotent by operation ID, reconcile by immutable container identity after restart, and terminalize with exact observed state. Pull, digest, platform, capacity, policy and capability failures are distinct named refusals. Shared black-box conformance covers lost answers, restart, lease expiry, cleanup and unauthorized runtime options.\n\n## Out of Scope\n\nImage builds, Compose semantics, arbitrary Docker APIs, fleet scheduling and Kubernetes workloads.\n","owner":"substrate","status":"draft","summary":"Digest-resolved images and a minimal durable workload lifecycle extend the Docker driver without mutable-tag or caller-option authority.","tags":["docker","images","workloads"],"title":"Docker serves immutable image-backed workloads"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/docker-image-backed-workload-slice"},"payload":{"actor":"human:timo","at":1788216807737,"causation":"cmd-new-story-docker-image-backed-workload-slice","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:docker-image-backed-workload-slice@1#0~308e0954900d38da","executor":null,"recorded_at":"2026-08-31T22:53:27Z"}}
{"entity":"story","version":1,"id":"docker-image-backed-workload-slice","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:container-driver-entry"}]},"args":{"command":"create-relation","kind":"decomposes","source":"01MEM0000000000000311","target":"01MEM0000000000000004"},"payload":{"actor":"human:timo","at":1788216807746,"causation":"cmd-story-docker-image-backed-workload-slice-decomposes-epic-container-driver-entry","change":{"change":"related","relation":"decomposes","target":"epic:container-driver-entry"},"correlation":"protocol-artifact-relate","event_id":"story:docker-image-backed-workload-slice@1#0~d27974a0bf8ead98","executor":null,"recorded_at":"2026-08-31T22:53:27Z"}}
{"entity":"story","version":1,"id":"docker-image-backed-workload-slice","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:container-driver-entry"},{"depends_on":"story:docker-workspace-and-exec-slice"}]},"args":{"command":"create-relation","kind":"depends_on","source":"01MEM0000000000000311","target":"01MEM0000000000000061"},"payload":{"actor":"human:timo","at":1788216807755,"causation":"cmd-story-docker-image-backed-workload-slice-depends_on-story-docker-workspace-and-exec-slice","change":{"change":"related","relation":"depends_on","target":"story:docker-workspace-and-exec-slice"},"correlation":"protocol-artifact-relate","event_id":"story:docker-image-backed-workload-slice@1#0~194f8bcf0997dafe","executor":null,"recorded_at":"2026-08-31T22:53:27Z"}}
{"entity":"story","version":1,"id":"kubernetes-namespace-driver-entry-gate","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Story: The Kubernetes namespace driver has a closed authority gate\n\n## Outcome\n\nA namespace-scoped Kubernetes driver can be implemented against an accepted, mechanically checked authority and lifecycle design rather than discovering its contract through cluster side effects.\n\n## Acceptance\n\nAn accepted design or ADR fixes configured namespace ownership, ServiceAccount and minimal RBAC, object naming and labels, server-side-apply ownership, finalizers, durable-before-API dispatch, immutable backend identity, watch gaps, restart reconciliation, cancellation, garbage collection, quotas, NetworkPolicy defaults and refusal order. A startup probe publishes only facts it proves with disposable resources and cleans them. Requests cannot select namespaces, service accounts, host paths, privileged settings, node names, runtime classes or arbitrary pod fields. The development cluster conformance creates no cluster-scoped resource and leaves no object after cleanup.\n\n## Environment boundary\n\nThe currently observed dev cluster has healthy general-purpose nodes but no RuntimeClass or KVM device. That is sufficient for the namespace driver entry gate and ordinary pod execution; it makes no microVM claim.\n","owner":"substrate","summary":"RBAC, namespace ownership, resource identity, reconciliation and cleanup are fixed and probed before Kubernetes-backed execution code.","tags":["driver","kubernetes","security"],"title":"The Kubernetes namespace driver has a closed authority gate"},"args":{"command":"create-entity","data":{"body":"# Story: The Kubernetes namespace driver has a closed authority gate\n\n## Outcome\n\nA namespace-scoped Kubernetes driver can be implemented against an accepted, mechanically checked authority and lifecycle design rather than discovering its contract through cluster side effects.\n\n## Acceptance\n\nAn accepted design or ADR fixes configured namespace ownership, ServiceAccount and minimal RBAC, object naming and labels, server-side-apply ownership, finalizers, durable-before-API dispatch, immutable backend identity, watch gaps, restart reconciliation, cancellation, garbage collection, quotas, NetworkPolicy defaults and refusal order. A startup probe publishes only facts it proves with disposable resources and cleans them. Requests cannot select namespaces, service accounts, host paths, privileged settings, node names, runtime classes or arbitrary pod fields. The development cluster conformance creates no cluster-scoped resource and leaves no object after cleanup.\n\n## Environment boundary\n\nThe currently observed dev cluster has healthy general-purpose nodes but no RuntimeClass or KVM device. That is sufficient for the namespace driver entry gate and ordinary pod execution; it makes no microVM claim.\n","owner":"substrate","status":"draft","summary":"RBAC, namespace ownership, resource identity, reconciliation and cleanup are fixed and probed before Kubernetes-backed execution code.","tags":["driver","kubernetes","security"],"title":"The Kubernetes namespace driver has a closed authority gate"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/kubernetes-namespace-driver-entry-gate"},"payload":{"actor":"human:timo","at":1788216807978,"causation":"cmd-new-story-kubernetes-namespace-driver-entry-gate","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:kubernetes-namespace-driver-entry-gate@1#0~ae2e5b055de3fa6c","executor":null,"recorded_at":"2026-08-31T22:53:27Z"}}
{"entity":"story","version":1,"id":"kubernetes-namespace-driver-entry-gate","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:kubernetes-deployment-and-driver"}]},"args":{"command":"create-relation","kind":"decomposes","source":"01MEM0000000000000318","target":"01MEM0000000000000010"},"payload":{"actor":"human:timo","at":1788216807987,"causation":"cmd-story-kubernetes-namespace-driver-entry-gate-decomposes-epic-kubernetes-deployment-and-driver","change":{"change":"related","relation":"decomposes","target":"epic:kubernetes-deployment-and-driver"},"correlation":"protocol-artifact-relate","event_id":"story:kubernetes-namespace-driver-entry-gate@1#0~a6a79b4e9873b93c","executor":null,"recorded_at":"2026-08-31T22:53:27Z"}}
{"entity":"story","version":1,"id":"kubernetes-namespace-driver-entry-gate","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:kubernetes-deployment-and-driver"},{"depends_on":"story:node-bound-kubernetes-serving-profile"}]},"args":{"command":"create-relation","kind":"depends_on","source":"01MEM0000000000000318","target":"01MEM0000000000000091"},"payload":{"actor":"human:timo","at":1788216807994,"causation":"cmd-story-kubernetes-namespace-driver-entry-gate-depends_on-story-node-bound-kubernetes-serving-profile","change":{"change":"related","relation":"depends_on","target":"story:node-bound-kubernetes-serving-profile"},"correlation":"protocol-artifact-relate","event_id":"story:kubernetes-namespace-driver-entry-gate@1#0~5dd8904e294fa1d0","executor":null,"recorded_at":"2026-08-31T22:53:27Z"}}
{"entity":"story","version":1,"id":"kubernetes-namespace-driver-entry-gate","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:kubernetes-deployment-and-driver"},{"depends_on":"story:node-bound-kubernetes-serving-profile"},{"depends_on":"story:remote-clean-room-conformance"}]},"args":{"command":"create-relation","kind":"depends_on","source":"01MEM0000000000000318","target":"01MEM0000000000000115"},"payload":{"actor":"human:timo","at":1788216808003,"causation":"cmd-story-kubernetes-namespace-driver-entry-gate-depends_on-story-remote-clean-room-conformance","change":{"change":"related","relation":"depends_on","target":"story:remote-clean-room-conformance"},"correlation":"protocol-artifact-relate","event_id":"story:kubernetes-namespace-driver-entry-gate@1#0~8d303be64c115da7","executor":null,"recorded_at":"2026-08-31T22:53:28Z"}}
{"entity":"story","version":1,"id":"kubernetes-namespace-driver-entry-gate","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:kubernetes-deployment-and-driver"},{"depends_on":"story:node-bound-kubernetes-serving-profile"},{"depends_on":"story:remote-clean-room-conformance"},{"depends_on":"story:driver-port-carries-no-host-types"}]},"args":{"command":"create-relation","kind":"depends_on","source":"01MEM0000000000000318","target":"01MEM0000000000000067"},"payload":{"actor":"human:timo","at":1788216808011,"causation":"cmd-story-kubernetes-namespace-driver-entry-gate-depends_on-story-driver-port-carries-no-host-types","change":{"change":"related","relation":"depends_on","target":"story:driver-port-carries-no-host-types"},"correlation":"protocol-artifact-relate","event_id":"story:kubernetes-namespace-driver-entry-gate@1#0~f7cd9e245249ca09","executor":null,"recorded_at":"2026-08-31T22:53:28Z"}}
{"entity":"story","version":1,"id":"kubernetes-namespace-workspace-and-exec-slice","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Story: Kubernetes namespaces serve workspace and exec\n\n## Outcome\n\nA configured namespace can back Substrate workspaces with PVCs and bounded argv-only executions with one-shot Rust executor pods while clients continue to use the same public contract.\n\n## Acceptance\n\nWorkspace creation durably records intent before PVC creation and records PVC UID as backend identity. File operations use a Rust executor path with beneath/no-link/no-mount semantics or name an unserved guarantee; no shell command performs file I/O. Exec start durably records intent before Pod creation, uses an immutable image digest, argv, cleared/shaped environment, non-root security context, read-only root filesystem, explicit workspace mount, resource requests/limits, deadline and default-deny network. Output is drained and bounded, cancellation deletes the whole execution object set, and restart reconciliation handles watch gaps by UID and resourceVersion. Capability facts describe verified properties, never the Kubernetes driver name. Shared conformance proves idempotency, lost answers, quota/capacity refusal, cleanup and absent unsupported capabilities.\n\n## Out of Scope\n\nDeployments, Services, ingress, image builds, arbitrary manifests, hostPath, privileged pods and cluster scheduling policy.\n","owner":"substrate","summary":"PVC-backed workspaces and one-shot executor pods implement the shared contract with durable Kubernetes dispatch and exact observations.","tags":["driver","exec","kubernetes"],"title":"Kubernetes namespaces serve workspace and exec"},"args":{"command":"create-entity","data":{"body":"# Story: Kubernetes namespaces serve workspace and exec\n\n## Outcome\n\nA configured namespace can back Substrate workspaces with PVCs and bounded argv-only executions with one-shot Rust executor pods while clients continue to use the same public contract.\n\n## Acceptance\n\nWorkspace creation durably records intent before PVC creation and records PVC UID as backend identity. File operations use a Rust executor path with beneath/no-link/no-mount semantics or name an unserved guarantee; no shell command performs file I/O. Exec start durably records intent before Pod creation, uses an immutable image digest, argv, cleared/shaped environment, non-root security context, read-only root filesystem, explicit workspace mount, resource requests/limits, deadline and default-deny network. Output is drained and bounded, cancellation deletes the whole execution object set, and restart reconciliation handles watch gaps by UID and resourceVersion. Capability facts describe verified properties, never the Kubernetes driver name. Shared conformance proves idempotency, lost answers, quota/capacity refusal, cleanup and absent unsupported capabilities.\n\n## Out of Scope\n\nDeployments, Services, ingress, image builds, arbitrary manifests, hostPath, privileged pods and cluster scheduling policy.\n","owner":"substrate","status":"draft","summary":"PVC-backed workspaces and one-shot executor pods implement the shared contract with durable Kubernetes dispatch and exact observations.","tags":["driver","exec","kubernetes"],"title":"Kubernetes namespaces serve workspace and exec"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/kubernetes-namespace-workspace-and-exec-slice"},"payload":{"actor":"human:timo","at":1788216808239,"causation":"cmd-new-story-kubernetes-namespace-workspace-and-exec-slice","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:kubernetes-namespace-workspace-and-exec-slice@1#0~16f1be0863176cdb","executor":null,"recorded_at":"2026-08-31T22:53:28Z"}}
{"entity":"story","version":1,"id":"kubernetes-namespace-workspace-and-exec-slice","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:kubernetes-deployment-and-driver"}]},"args":{"command":"create-relation","kind":"decomposes","source":"01MEM0000000000000329","target":"01MEM0000000000000010"},"payload":{"actor":"human:timo","at":1788216808250,"causation":"cmd-story-kubernetes-namespace-workspace-and-exec-slice-decomposes-epic-kubernetes-deployment-and-driver","change":{"change":"related","relation":"decomposes","target":"epic:kubernetes-deployment-and-driver"},"correlation":"protocol-artifact-relate","event_id":"story:kubernetes-namespace-workspace-and-exec-slice@1#0~bae929625943e6a8","executor":null,"recorded_at":"2026-08-31T22:53:28Z"}}
{"entity":"story","version":1,"id":"kubernetes-namespace-workspace-and-exec-slice","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:kubernetes-deployment-and-driver"},{"depends_on":"story:kubernetes-namespace-driver-entry-gate"}]},"args":{"command":"create-relation","kind":"depends_on","source":"01MEM0000000000000329","target":"01MEM0000000000000082"},"payload":{"actor":"human:timo","at":1788216808257,"causation":"cmd-story-kubernetes-namespace-workspace-and-exec-slice-depends_on-story-kubernetes-namespace-driver-entry-gate","change":{"change":"related","relation":"depends_on","target":"story:kubernetes-namespace-driver-entry-gate"},"correlation":"protocol-artifact-relate","event_id":"story:kubernetes-namespace-workspace-and-exec-slice@1#0~2efeb21ea46fb1fc","executor":null,"recorded_at":"2026-08-31T22:53:28Z"}}
{"entity":"story","version":1,"id":"firecracker-entry-and-boot-artifact-gate","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Story: Firecracker starts only from verified immutable boot artifacts\n\n## Outcome\n\nThe repository has an accepted and executable entry gate for a direct Firecracker driver, including the host and artifact properties needed to make a microVM confinement claim.\n\n## Acceptance\n\nAn accepted design or ADR fixes supported architectures and Firecracker version, /dev/kvm access, jailer uid/gid and chroot layout, seccomp and cgroup ownership, kernel command line, tap/network default, vsock identity, boot timeout, crash cleanup, snapshot stance, and boot-artifact provenance. Kernel and rootfs inputs are operator-configured OCI artifacts or files pinned by digest; their bytes are verified before dispatch and callers cannot supply host paths. Startup probes exercise KVM and the jailer in a disposable VM and publish facts only after success. Missing KVM, unsupported CPU, unsafe permissions, unverified artifacts and failed jailer setup are separate named unserved refusals.\n\n## Development environment\n\nLive conformance runs only on a dedicated tainted KVM-capable node pool. The current T3a/T4g EKS nodes have no KVM surface; on them the portable lane must report the Firecracker cases absent and must never report them passed.\n","owner":"substrate","summary":"KVM, jailer, kernel/rootfs provenance and host isolation are probed and refused by name before the direct driver exists.","tags":["driver","firecracker","security"],"title":"Firecracker starts only from verified immutable boot artifacts"},"args":{"command":"create-entity","data":{"body":"# Story: Firecracker starts only from verified immutable boot artifacts\n\n## Outcome\n\nThe repository has an accepted and executable entry gate for a direct Firecracker driver, including the host and artifact properties needed to make a microVM confinement claim.\n\n## Acceptance\n\nAn accepted design or ADR fixes supported architectures and Firecracker version, /dev/kvm access, jailer uid/gid and chroot layout, seccomp and cgroup ownership, kernel command line, tap/network default, vsock identity, boot timeout, crash cleanup, snapshot stance, and boot-artifact provenance. Kernel and rootfs inputs are operator-configured OCI artifacts or files pinned by digest; their bytes are verified before dispatch and callers cannot supply host paths. Startup probes exercise KVM and the jailer in a disposable VM and publish facts only after success. Missing KVM, unsupported CPU, unsafe permissions, unverified artifacts and failed jailer setup are separate named unserved refusals.\n\n## Development environment\n\nLive conformance runs only on a dedicated tainted KVM-capable node pool. The current T3a/T4g EKS nodes have no KVM surface; on them the portable lane must report the Firecracker cases absent and must never report them passed.\n","owner":"substrate","status":"draft","summary":"KVM, jailer, kernel/rootfs provenance and host isolation are probed and refused by name before the direct driver exists.","tags":["driver","firecracker","security"],"title":"Firecracker starts only from verified immutable boot artifacts"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/firecracker-entry-and-boot-artifact-gate"},"payload":{"actor":"human:timo","at":1788216808481,"causation":"cmd-new-story-firecracker-entry-and-boot-artifact-gate","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:firecracker-entry-and-boot-artifact-gate@1#0~acb6649f97ee93d4","executor":null,"recorded_at":"2026-08-31T22:53:28Z"}}
{"entity":"story","version":1,"id":"firecracker-entry-and-boot-artifact-gate","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:firecracker-driver"}]},"args":{"command":"create-relation","kind":"decomposes","source":"01MEM0000000000000336","target":"01MEM0000000000000007"},"payload":{"actor":"human:timo","at":1788216808490,"causation":"cmd-story-firecracker-entry-and-boot-artifact-gate-decomposes-epic-firecracker-driver","change":{"change":"related","relation":"decomposes","target":"epic:firecracker-driver"},"correlation":"protocol-artifact-relate","event_id":"story:firecracker-entry-and-boot-artifact-gate@1#0~9a007288b876fe3c","executor":null,"recorded_at":"2026-08-31T22:53:28Z"}}
{"entity":"story","version":1,"id":"firecracker-entry-and-boot-artifact-gate","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:firecracker-driver"},{"depends_on":"story:remote-clean-room-conformance"}]},"args":{"command":"create-relation","kind":"depends_on","source":"01MEM0000000000000336","target":"01MEM0000000000000121"},"payload":{"actor":"human:timo","at":1788216808498,"causation":"cmd-story-firecracker-entry-and-boot-artifact-gate-depends_on-story-remote-clean-room-conformance","change":{"change":"related","relation":"depends_on","target":"story:remote-clean-room-conformance"},"correlation":"protocol-artifact-relate","event_id":"story:firecracker-entry-and-boot-artifact-gate@1#0~9f10242b0a274958","executor":null,"recorded_at":"2026-08-31T22:53:28Z"}}
{"entity":"story","version":1,"id":"firecracker-entry-and-boot-artifact-gate","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:firecracker-driver"},{"depends_on":"story:remote-clean-room-conformance"},{"depends_on":"story:driver-port-carries-no-host-types"}]},"args":{"command":"create-relation","kind":"depends_on","source":"01MEM0000000000000336","target":"01MEM0000000000000067"},"payload":{"actor":"human:timo","at":1788216808505,"causation":"cmd-story-firecracker-entry-and-boot-artifact-gate-depends_on-story-driver-port-carries-no-host-types","change":{"change":"related","relation":"depends_on","target":"story:driver-port-carries-no-host-types"},"correlation":"protocol-artifact-relate","event_id":"story:firecracker-entry-and-boot-artifact-gate@1#0~65c8626026edcbf9","executor":null,"recorded_at":"2026-08-31T22:53:28Z"}}
{"entity":"story","version":1,"id":"firecracker-workspace-and-exec-slice","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Story: Firecracker serves one microVM per exec\n\n## Outcome\n\nA direct Firecracker driver runs one Substrate execution in a fresh microVM and returns the same durable operation, output, limit and observation semantics as the host driver where the backend can prove them.\n\n## Acceptance\n\nThe host records the operation before creating any VM resource. A Rust guest agent reached over authenticated instance-bound vsock receives only a validated argv, shaped environment and limits; no shell or general remote command protocol exists. The verified kernel/rootfs and execution capsule are read-only. The workspace is a separately bounded writable device or filesystem whose exact design is accepted before code. vCPU, memory, process count, disk, output and wall-time limits are enforced at the VM and host-cgroup layers, and exact available usage is observed without inventing mean memory. Timeout, cancellation, attachment loss and daemon restart clean the Firecracker process, jail and transient devices and reconcile by immutable VM identity. Shared conformance runs on the dedicated KVM node and portable cases name absence elsewhere.\n\n## Initial capability boundary\n\nNo egress, secret slots, PTY, snapshots, suspend/resume or multi-VM workload lifecycle in this slice. Their facts remain absent and requests are refused by name.\n","owner":"substrate","summary":"A Rust guest agent over vsock runs a bounded argv-only command with a read-only boot closure and writable workspace, then reports exact evidence.","tags":["exec","firecracker","microvm"],"title":"Firecracker serves one microVM per exec"},"args":{"command":"create-entity","data":{"body":"# Story: Firecracker serves one microVM per exec\n\n## Outcome\n\nA direct Firecracker driver runs one Substrate execution in a fresh microVM and returns the same durable operation, output, limit and observation semantics as the host driver where the backend can prove them.\n\n## Acceptance\n\nThe host records the operation before creating any VM resource. A Rust guest agent reached over authenticated instance-bound vsock receives only a validated argv, shaped environment and limits; no shell or general remote command protocol exists. The verified kernel/rootfs and execution capsule are read-only. The workspace is a separately bounded writable device or filesystem whose exact design is accepted before code. vCPU, memory, process count, disk, output and wall-time limits are enforced at the VM and host-cgroup layers, and exact available usage is observed without inventing mean memory. Timeout, cancellation, attachment loss and daemon restart clean the Firecracker process, jail and transient devices and reconcile by immutable VM identity. Shared conformance runs on the dedicated KVM node and portable cases name absence elsewhere.\n\n## Initial capability boundary\n\nNo egress, secret slots, PTY, snapshots, suspend/resume or multi-VM workload lifecycle in this slice. Their facts remain absent and requests are refused by name.\n","owner":"substrate","status":"draft","summary":"A Rust guest agent over vsock runs a bounded argv-only command with a read-only boot closure and writable workspace, then reports exact evidence.","tags":["exec","firecracker","microvm"],"title":"Firecracker serves one microVM per exec"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/firecracker-workspace-and-exec-slice"},"payload":{"actor":"human:timo","at":1788216808733,"causation":"cmd-new-story-firecracker-workspace-and-exec-slice","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:firecracker-workspace-and-exec-slice@1#0~5c1bc5f0f17d51e8","executor":null,"recorded_at":"2026-08-31T22:53:28Z"}}
{"entity":"story","version":1,"id":"firecracker-workspace-and-exec-slice","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:firecracker-driver"}]},"args":{"command":"create-relation","kind":"decomposes","source":"01MEM0000000000000345","target":"01MEM0000000000000007"},"payload":{"actor":"human:timo","at":1788216808741,"causation":"cmd-story-firecracker-workspace-and-exec-slice-decomposes-epic-firecracker-driver","change":{"change":"related","relation":"decomposes","target":"epic:firecracker-driver"},"correlation":"protocol-artifact-relate","event_id":"story:firecracker-workspace-and-exec-slice@1#0~12a77f78d71ac9ba","executor":null,"recorded_at":"2026-08-31T22:53:28Z"}}
{"entity":"story","version":1,"id":"firecracker-workspace-and-exec-slice","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:firecracker-driver"},{"depends_on":"story:firecracker-entry-and-boot-artifact-gate"}]},"args":{"command":"create-relation","kind":"depends_on","source":"01MEM0000000000000345","target":"01MEM0000000000000076"},"payload":{"actor":"human:timo","at":1788216808749,"causation":"cmd-story-firecracker-workspace-and-exec-slice-depends_on-story-firecracker-entry-and-boot-artifact-gate","change":{"change":"related","relation":"depends_on","target":"story:firecracker-entry-and-boot-artifact-gate"},"correlation":"protocol-artifact-relate","event_id":"story:firecracker-workspace-and-exec-slice@1#0~d690ac420f16d81d","executor":null,"recorded_at":"2026-08-31T22:53:28Z"}}
{"entity":"story","version":1,"id":"network-session-authority","revision":6,"type":"aep.relation.create/v1","from_state":"proposed","to_state":"proposed","changed":{"relations":[{"decomposes":"epic:byte-plane-completion"},{"depends_on":"story:production-tls-control-listener"}]},"args":{"command":"create-relation","kind":"depends_on","source":"01MEM0000000000000100","target":"01MEM0000000000000112"},"payload":{"actor":"human:timo","at":1788216841114,"causation":"cmd-story-network-session-authority-depends_on-story-production-tls-control-listener","change":{"change":"related","relation":"depends_on","target":"story:production-tls-control-listener"},"correlation":"protocol-artifact-relate","event_id":"story:network-session-authority@6#0~532d29bfa120bc17","executor":null,"recorded_at":"2026-08-31T22:54:01Z"}}
{"entity":"story","version":1,"id":"network-session-authority","revision":6,"type":"aep.relation.create/v1","from_state":"proposed","to_state":"proposed","changed":{"relations":[{"decomposes":"epic:byte-plane-completion"},{"depends_on":"story:production-tls-control-listener"},{"depends_on":"story:hosted-trust-envelope-admission"}]},"args":{"command":"create-relation","kind":"depends_on","source":"01MEM0000000000000100","target":"01MEM0000000000000085"},"payload":{"actor":"human:timo","at":1788216841284,"causation":"cmd-story-network-session-authority-depends_on-story-hosted-trust-envelope-admission","change":{"change":"related","relation":"depends_on","target":"story:hosted-trust-envelope-admission"},"correlation":"protocol-artifact-relate","event_id":"story:network-session-authority@6#0~fb9ae051c0258124","executor":null,"recorded_at":"2026-08-31T22:54:01Z"}}
{"entity":"story","version":1,"id":"node-bound-kubernetes-serving-profile","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:kubernetes-deployment-and-driver"},{"depends_on":"story:production-tls-control-listener"},{"depends_on":"story:hosted-trust-envelope-admission"},{"depends_on":"story:network-session-authority"}]},"args":{"command":"create-relation","kind":"depends_on","source":"01MEM0000000000000103","target":"01MEM0000000000000100"},"payload":{"actor":"human:timo","at":1788216841454,"causation":"cmd-story-node-bound-kubernetes-serving-profile-depends_on-story-network-session-authority","change":{"change":"related","relation":"depends_on","target":"story:network-session-authority"},"correlation":"protocol-artifact-relate","event_id":"story:node-bound-kubernetes-serving-profile@1#0~dc8636689003896b","executor":null,"recorded_at":"2026-08-31T22:54:01Z"}}
{"entity":"story","version":1,"id":"mcp-managed-disposable-daemon","revision":2,"type":"aep.relation.create/v1","from_state":"proposed","to_state":"proposed","changed":{"relations":[{"decomposes":"epic:mcp-test-surface"},{"depends_on":"story:sdk-promoted-contract-parity"}]},"args":{"command":"create-relation","kind":"depends_on","source":"01MEM0000000000000097","target":"01MEM0000000000000148"},"payload":{"actor":"human:timo","at":1788216841625,"causation":"cmd-story-mcp-managed-disposable-daemon-depends_on-story-sdk-promoted-contract-parity","change":{"change":"related","relation":"depends_on","target":"story:sdk-promoted-contract-parity"},"correlation":"protocol-artifact-relate","event_id":"story:mcp-managed-disposable-daemon@2#0~2ec09251b7b8de79","executor":null,"recorded_at":"2026-08-31T22:54:01Z"}}
{"entity":"epic","version":1,"id":"mcp-test-surface","revision":2,"type":"aep.relation.create/v1","from_state":"proposed","to_state":"proposed","changed":{"relations":[{"depends_on":"epic:resource-bounded-execution"}]},"args":{"command":"create-relation","kind":"depends_on","source":"01MEM0000000000000013","target":"01MEM0000000000000022"},"payload":{"actor":"human:timo","at":1788216841801,"causation":"cmd-epic-mcp-test-surface-depends_on-epic-resource-bounded-execution","change":{"change":"related","relation":"depends_on","target":"epic:resource-bounded-execution"},"correlation":"protocol-artifact-relate","event_id":"epic:mcp-test-surface@2#0~438c8e964506668a","executor":null,"recorded_at":"2026-08-31T22:54:01Z"}}
{"entity":"story","version":1,"id":"confinement-runtime-hardening","revision":4,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"body":"# Story: Confinement and runtime hardening\n\n## Outcome\n\nHost roots cannot reach host IPC or shadow owned mounts; pipe, aperture, file and replay paths terminate and report facts without deadlock, unbounded allocation or optimistic degradation.\n\n## Acceptance\n\n- AF_UNIX host IPC is blocked and probe-verified.\n- Owned and overlapping mounts are refused component-wise.\n- Pipe queue saturation is a durable named terminal failure.\n- File reads remain bounded under concurrent growth.\n- Aperture cleanup, counters, source eviction and delegated tests are race-free.\n- Required delegated context is verified before replay.\n\n## Current work — 2026-09-01\n\nThe uncommitted substrate-hardening worktree on wave/hardening-2026-09-01 is an in-flight slice of this story, not a competing implementation. It contains accepted ADR 0023, a proposed 0.12.0 successor, probe-gated read-only/scoped workspace writes and SDK coverage for current confinement options. This branch must merge or be deliberately superseded before this story can close. The roadmap stories for contract promotion and SDK parity consume its result and must not reimplement it. The worktree is based at the 0.3.1 tag and is two main commits behind, so integration must preserve the later 0.3.1 digest record.\n"},"args":{"changes":{"body":"# Story: Confinement and runtime hardening\n\n## Outcome\n\nHost roots cannot reach host IPC or shadow owned mounts; pipe, aperture, file and replay paths terminate and report facts without deadlock, unbounded allocation or optimistic degradation.\n\n## Acceptance\n\n- AF_UNIX host IPC is blocked and probe-verified.\n- Owned and overlapping mounts are refused component-wise.\n- Pipe queue saturation is a durable named terminal failure.\n- File reads remain bounded under concurrent growth.\n- Aperture cleanup, counters, source eviction and delegated tests are race-free.\n- Required delegated context is verified before replay.\n\n## Current work — 2026-09-01\n\nThe uncommitted substrate-hardening worktree on wave/hardening-2026-09-01 is an in-flight slice of this story, not a competing implementation. It contains accepted ADR 0023, a proposed 0.12.0 successor, probe-gated read-only/scoped workspace writes and SDK coverage for current confinement options. This branch must merge or be deliberately superseded before this story can close. The roadmap stories for contract promotion and SDK parity consume its result and must not reimplement it. The worktree is based at the 0.3.1 tag and is two main commits behind, so integration must preserve the later 0.3.1 digest record.\n"},"command":"update-entity","target":"01MEM0000000000000043"},"payload":{"actor":"human:timo","at":1788216859731,"causation":"cmd-protocol-artifact-body-section-story-confinement-runtime-hardening-1788216859731","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:confinement-runtime-hardening@4#0~ece60b4c440816a5","executor":null,"recorded_at":"2026-08-31T22:54:19Z"}}
{"entity":"story","version":1,"id":"rust-sdk-publication","revision":4,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"body":"# Story: The Rust SDK and runtime chain are owner-released Rust packages\n\n## Outcome\n\nConsumers depend on version-locked `b10x-substrate-*` packages rather than a sibling checkout. The default client stays transport-only; linked daemon support is opt-in.\n\n## Acceptance\n\nThe gate permits only the named package set, verifies package contents and downstream builds, and keeps every other workspace member non-publishable. A fully gated annotated release tag is published to crates.io manually in dependency order with an operator-held scoped token that never enters GitHub.\n\n## Current state — 2026-09-01\n\nThe implementation, package allowlist, exact internal version edges, package-content checks, README material and public SDK journeys shipped on main and pass the required Full gate. This story stays active only for the external publication proof: publish the owner-released package chain to crates.io in dependency order from a fully gated annotated tag, verify anonymous clean-room installation, and record the registry versions and digests. No GitHub secret or repository-held crates.io token is introduced.\n"},"args":{"changes":{"body":"# Story: The Rust SDK and runtime chain are owner-released Rust packages\n\n## Outcome\n\nConsumers depend on version-locked `b10x-substrate-*` packages rather than a sibling checkout. The default client stays transport-only; linked daemon support is opt-in.\n\n## Acceptance\n\nThe gate permits only the named package set, verifies package contents and downstream builds, and keeps every other workspace member non-publishable. A fully gated annotated release tag is published to crates.io manually in dependency order with an operator-held scoped token that never enters GitHub.\n\n## Current state — 2026-09-01\n\nThe implementation, package allowlist, exact internal version edges, package-content checks, README material and public SDK journeys shipped on main and pass the required Full gate. This story stays active only for the external publication proof: publish the owner-released package chain to crates.io in dependency order from a fully gated annotated tag, verify anonymous clean-room installation, and record the registry versions and digests. No GitHub secret or repository-held crates.io token is introduced.\n"},"command":"update-entity","target":"01MEM0000000000000145"},"payload":{"actor":"human:timo","at":1788216859912,"causation":"cmd-protocol-artifact-body-section-story-rust-sdk-publication-1788216859912","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:rust-sdk-publication@4#0~301428b161aae261","executor":null,"recorded_at":"2026-08-31T22:54:19Z"}}
{"entity":"epic","version":1,"id":"byte-plane-completion","revision":7,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"body":"# Epic: Byte-plane completion\n\n## Outcome\n\n`ROADMAP.md` phase 4 reads *complete*: `README.md` § *Status* no longer lists PTY or network\nsession authority as absent, and design 05's condition for a live vendor harness — secret and\negress-adjacent capabilities — is met on the secret side.\n\n## Why Now\n\n`ROADMAP.md` orders the phases and forbids starting a later one while an earlier exit criterion is\nopen. Phase 4 is *in progress*: raw-pipe sessions, the distinct session resource, the read-only\nexecution capsule and the delegated model-free Agent lane are green\n(`docs/plan/04-direct-byte-plane.md` § *Slices A–D*). The same plan's § *Later phase-4 slices*\nnames what remains, and three of those items already have accepted designs, so they can proceed\nunder invariant 8 with an ADR each and no new design document:\n\n| capability | accepted design | fixed already |\n|---|---|---|\n| PTY session kind | `docs/design/05-streams-sessions-and-endpoints.md` § 2 | frames: input, output, resize, signal, exit, protocol-error; a PTY never substitutes for pipes; both kinds share the finite frame/byte/queue/idle bounds |\n| sealed secret slots | `docs/design/06-authentication-secrets-and-trust.md` § *V1 decisions* 3 | sealed Linux `memfd` at a declared child descriptor; only the slot→descriptor mapping in the shaped environment; acquired after all admission checks; daemon copy closed after spawn; missing proof reports the capability absent |\n| network session authority | design 05 § *V1 decisions* 3–4 | proof-bound, ≤ 60 s, single redemption, one concurrent attachment; reconnect needs a fresh authority; WebSocket over TLS; non-loopback listeners need TLS/mTLS or a trusted tunnel (`architecture/deployment-postures.md`) |\n\n## Scope\n\nSix stories now decompose this epic, each an ADR, a successor bundle, host/daemon work and delegated-lane evidence — in\nthat order, which is the order invariant 8 requires:\n\n1. `story:pty-sessions` — smallest, wholly local, exercises the successor-bundle path again.\n2. `story:sealed-secret-slots` — the capability design 05 § *Progress* names as the reason a live\n   vendor harness is refused.\n3. `story:destination-bound-egress` — the other half of the vendor-harness pair; secrets without\n   egress unlock nothing (added by the 2026-08-29 review against atlas).\n4. `story:network-session-authority` — the last exit criterion, and the one with no consumer named.\n\nEvery story adds to **the next free successor** at the time it lands; no frozen byte changes\n(invariant 6). Two landing together share a bundle and the ADRs say so. This paragraph once named\nthe numbers — PTY `0.5.0`, slots `0.6.0`, network authority `0.7.0` — and every one of them was\nwrong within a day, because the order stories land in is not the order they were listed in. What\nactually happened: `0.5.0` sealed secret slots, `0.6.0` destination-bound egress, `0.7.0` delegated\ncontext and grant attribution, `0.8.0` the aperture byte ceiling (`CHANGELOG.md:30`, `:83`, `:119`,\n`:155`). A plan that pins a successor number is a plan that is wrong as soon as anything reorders. Each successor ships its renderer and checker and joins `scripts/gate.sh`. Each\ncapability is a fact the host verifies at startup; absent verification is a named refusal and the\ncapability document omits the fact (invariants 3, 4). Nothing here opens a second spawn path.\n\n## Out of Scope\n\nReconnect (design 05 defines it as \"a fresh authority\", so it falls out of the network story), and\nthe hosted trust-envelope verifier (design 06 § 1, phase 7).\n\n## Risks\n\n- Three successor bundles in quick succession is a lot of frozen directories; batching two into one\n  bundle trades that for a larger review. Decide per story when its predecessor lands.\n- `memfd` sealing is Linux-only; the portable lane must prove the typed refusal, not skip the case.\n- Channel binding for the network authority — TLS exporter (RFC 5705) or a client key — is an ADR\n  decision; the design says only \"proof-bound\".\n\n## Done When\n\nPlan 04 § *Exit evidence* is met, `README.md` § *Status* no longer lists PTY or network session\nauthority as absent, and `ROADMAP.md` phase 4 reads *complete*.\n\n## Correction — 2026-08-30\n\nTwo claims above were false and are rewritten rather than deleted, so the drift is visible.\n\nThe bundle sequence named three numbers and got all three wrong; the paragraph now says *next free\nsuccessor* and records what actually landed.\n\nThe scope said four stories. Six decompose this epic: the original four plus\n`story:aperture-byte-ceiling` (ADR 0014, implemented) and\n`story:secret-slot-probe-observes-what-it-claims` (implemented), both filed after the original\nscope was written. Four of the six are implemented; `story:pty-sessions` and\n`story:network-session-authority` remain drafts, each now carrying a proposed design\n(`docs/design/13-pty-sessions.md`, `docs/design/14-network-session-authority.md`) and each awaiting\noperator acceptance before code.\n\n## Reconciled state — 2026-09-01\n\nPTY, sealed secret slots, destination-bound egress, the aperture ceiling and their probe evidence are implemented. Network session authority is the only remaining phase-4 capability. Its implementation now depends on the production TLS listener and hosted trust-envelope admission because the accepted proof-bound authority is meaningful only on the production remote path. Phase 4 closes when that story and its clean-room evidence close.\n"},"args":{"changes":{"body":"# Epic: Byte-plane completion\n\n## Outcome\n\n`ROADMAP.md` phase 4 reads *complete*: `README.md` § *Status* no longer lists PTY or network\nsession authority as absent, and design 05's condition for a live vendor harness — secret and\negress-adjacent capabilities — is met on the secret side.\n\n## Why Now\n\n`ROADMAP.md` orders the phases and forbids starting a later one while an earlier exit criterion is\nopen. Phase 4 is *in progress*: raw-pipe sessions, the distinct session resource, the read-only\nexecution capsule and the delegated model-free Agent lane are green\n(`docs/plan/04-direct-byte-plane.md` § *Slices A–D*). The same plan's § *Later phase-4 slices*\nnames what remains, and three of those items already have accepted designs, so they can proceed\nunder invariant 8 with an ADR each and no new design document:\n\n| capability | accepted design | fixed already |\n|---|---|---|\n| PTY session kind | `docs/design/05-streams-sessions-and-endpoints.md` § 2 | frames: input, output, resize, signal, exit, protocol-error; a PTY never substitutes for pipes; both kinds share the finite frame/byte/queue/idle bounds |\n| sealed secret slots | `docs/design/06-authentication-secrets-and-trust.md` § *V1 decisions* 3 | sealed Linux `memfd` at a declared child descriptor; only the slot→descriptor mapping in the shaped environment; acquired after all admission checks; daemon copy closed after spawn; missing proof reports the capability absent |\n| network session authority | design 05 § *V1 decisions* 3–4 | proof-bound, ≤ 60 s, single redemption, one concurrent attachment; reconnect needs a fresh authority; WebSocket over TLS; non-loopback listeners need TLS/mTLS or a trusted tunnel (`architecture/deployment-postures.md`) |\n\n## Scope\n\nSix stories now decompose this epic, each an ADR, a successor bundle, host/daemon work and delegated-lane evidence — in\nthat order, which is the order invariant 8 requires:\n\n1. `story:pty-sessions` — smallest, wholly local, exercises the successor-bundle path again.\n2. `story:sealed-secret-slots` — the capability design 05 § *Progress* names as the reason a live\n   vendor harness is refused.\n3. `story:destination-bound-egress` — the other half of the vendor-harness pair; secrets without\n   egress unlock nothing (added by the 2026-08-29 review against atlas).\n4. `story:network-session-authority` — the last exit criterion, and the one with no consumer named.\n\nEvery story adds to **the next free successor** at the time it lands; no frozen byte changes\n(invariant 6). Two landing together share a bundle and the ADRs say so. This paragraph once named\nthe numbers — PTY `0.5.0`, slots `0.6.0`, network authority `0.7.0` — and every one of them was\nwrong within a day, because the order stories land in is not the order they were listed in. What\nactually happened: `0.5.0` sealed secret slots, `0.6.0` destination-bound egress, `0.7.0` delegated\ncontext and grant attribution, `0.8.0` the aperture byte ceiling (`CHANGELOG.md:30`, `:83`, `:119`,\n`:155`). A plan that pins a successor number is a plan that is wrong as soon as anything reorders. Each successor ships its renderer and checker and joins `scripts/gate.sh`. Each\ncapability is a fact the host verifies at startup; absent verification is a named refusal and the\ncapability document omits the fact (invariants 3, 4). Nothing here opens a second spawn path.\n\n## Out of Scope\n\nReconnect (design 05 defines it as \"a fresh authority\", so it falls out of the network story), and\nthe hosted trust-envelope verifier (design 06 § 1, phase 7).\n\n## Risks\n\n- Three successor bundles in quick succession is a lot of frozen directories; batching two into one\n  bundle trades that for a larger review. Decide per story when its predecessor lands.\n- `memfd` sealing is Linux-only; the portable lane must prove the typed refusal, not skip the case.\n- Channel binding for the network authority — TLS exporter (RFC 5705) or a client key — is an ADR\n  decision; the design says only \"proof-bound\".\n\n## Done When\n\nPlan 04 § *Exit evidence* is met, `README.md` § *Status* no longer lists PTY or network session\nauthority as absent, and `ROADMAP.md` phase 4 reads *complete*.\n\n## Correction — 2026-08-30\n\nTwo claims above were false and are rewritten rather than deleted, so the drift is visible.\n\nThe bundle sequence named three numbers and got all three wrong; the paragraph now says *next free\nsuccessor* and records what actually landed.\n\nThe scope said four stories. Six decompose this epic: the original four plus\n`story:aperture-byte-ceiling` (ADR 0014, implemented) and\n`story:secret-slot-probe-observes-what-it-claims` (implemented), both filed after the original\nscope was written. Four of the six are implemented; `story:pty-sessions` and\n`story:network-session-authority` remain drafts, each now carrying a proposed design\n(`docs/design/13-pty-sessions.md`, `docs/design/14-network-session-authority.md`) and each awaiting\noperator acceptance before code.\n\n## Reconciled state — 2026-09-01\n\nPTY, sealed secret slots, destination-bound egress, the aperture ceiling and their probe evidence are implemented. Network session authority is the only remaining phase-4 capability. Its implementation now depends on the production TLS listener and hosted trust-envelope admission because the accepted proof-bound authority is meaningful only on the production remote path. Phase 4 closes when that story and its clean-room evidence close.\n"},"command":"update-entity","target":"01MEM0000000000000001"},"payload":{"actor":"human:timo","at":1788216860086,"causation":"cmd-protocol-artifact-body-section-epic-byte-plane-completion-1788216860086","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"epic:byte-plane-completion@7#0~358e838a11275791","executor":null,"recorded_at":"2026-08-31T22:54:20Z"}}
{"entity":"epic","version":1,"id":"release-hardening","revision":6,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"body":"# Epic: Release hardening\n\n## Outcome\n\nWhat the repository says about itself is what a fresh clone, a fresh runner or a consumer can\nverify: a pull request cannot show green without `scripts/gate.sh`; local and CI clippy are the\nsame compiler; `STATUS.md` describes the standalone repository; a tag yields a signed daemon image\nand a signed contract-bundle artifact whose digests are in `CHANGELOG.md`.\n\n## Why Now\n\nSubstrate went public and cut `0.2.1` on 2026-08-29 (`CHANGELOG.md`). Its working agreement calls\nthe full gate \"the bar for `main`\" (`AGENTS.md` § *The gate*), but:\n\n- `.github/workflows/` holds one workflow, `pages.yml`, which builds the website. Nothing runs the\n  gate on a push or a pull request.\n- No `rust-toolchain.toml` exists; `AGENTS.md:116-119` documents the drift (\"a newer clippy can\n  fail a commit that passed locally\") and asks for `rustup update` instead of removing the cause.\n- `Dockerfile` builds a distroless `substrate-daemon`; nothing publishes, signs or digest-pins it.\n  `README.md` § *Status*: \"stable publication: **not done**\".\n- Agent consumes the 0.4.0 bundle as a hand-copied tree (`docs/plan/04-direct-byte-plane.md` §\n  *Slice C*); design 07 § *Bundle* already fixes the OCI shape and nothing implements it.\n- Two `AGENTS.md` facts contradict the scripts they describe (`AGENTS.md:112` vs\n  `scripts/gate.sh:20-23`; `AGENTS.md:172` vs `scripts/bot-token.sh:8`), and `STATUS.md` is\n  observed 2026-08-17, before extraction, still naming the monorepo as the source.\n\n## Scope\n\nSix stories, worked in this order because each makes the next checkable:\n\n1. `story:agents-md-matches-the-scripts` — docs only.\n2. `story:ci-runs-the-full-gate` — `gate.yml`, SHA-pinned actions, `contents: read`.\n3. `story:pinned-rust-toolchain` — `rust-toolchain.toml` = `1.97` = `Cargo.toml` `rust-version` =\n   `Dockerfile` builder tag; one commit per bump.\n4. `story:status-md-re-observed` — from fresh gate output.\n5. `story:signed-daemon-image` — `release.yml` on a bare-version tag, GHCR, cosign keyless.\n6. `story:contract-bundle-oci-artifact` — deterministic OCI layout, published beside the image.\n\n## Out of Scope\n\nAny route, schema, wire identifier, capability or frozen bundle byte. The development bundles stay\ndevelopment bundles; a stable-contract decision is its own ADR under atlas ADR 0019.\nBit-for-bit reproducible binaries: digests are recorded, reproduction is a later milestone.\n\n## Risks\n\n- `packages: write` and `id-token: write` widen a workflow's authority; scope them to the release\n  job only.\n- The delegated lane needs bubblewrap and cgroup delegation and cannot run on a hosted GitHub\n  runner; either a self-hosted runner joins, or CI asserts the portable lane and the delegated\n  lane stays a local pre-release step. The CI story records which.\n- Keyless signing depends on Sigstore availability; a release fails closed, never publishes\n  unsigned.\n\n## Done When\n\nFrom a fresh clone: `AGENTS.md` matches the scripts; a PR with a formatting error goes red; clippy\nruns on the pinned toolchain; `STATUS.md` names the standalone repository and the current tag; a\ntag `0.x.y` yields a signed image and a signed bundle artifact whose digests are in `CHANGELOG.md`.\n\n## Correction — 2026-08-30\n\nScope above says \"Six stories\". **Eight** decompose this epic: the six named plus\n`story:tooling-moves-to-cargo-xtask` (implemented — the gate's Python moved to `cargo xtask`) and\n`story:contract-gate-sees-route-paths` (draft — the successor check reads operation ids and never\nthe paths they are served at, so a route can move silently).\n\nSix of the eight are implemented. `story:contract-bundle-oci-artifact` is active and unblocked;\n`story:contract-gate-sees-route-paths` is a draft filed today.\n\n## Reconciled state — 2026-09-01\n\nThe CI, toolchain, repository recovery, Apache-2.0/publication, route/compatibility gates, daemon image and release-publication stories are implemented on main. The epic remains active for the signed contract-bundle OCI artifact and the separately coordinated move of the daemon's advertised contract frontier. A signed daemon image does not make a development bundle stable.\n"},"args":{"changes":{"body":"# Epic: Release hardening\n\n## Outcome\n\nWhat the repository says about itself is what a fresh clone, a fresh runner or a consumer can\nverify: a pull request cannot show green without `scripts/gate.sh`; local and CI clippy are the\nsame compiler; `STATUS.md` describes the standalone repository; a tag yields a signed daemon image\nand a signed contract-bundle artifact whose digests are in `CHANGELOG.md`.\n\n## Why Now\n\nSubstrate went public and cut `0.2.1` on 2026-08-29 (`CHANGELOG.md`). Its working agreement calls\nthe full gate \"the bar for `main`\" (`AGENTS.md` § *The gate*), but:\n\n- `.github/workflows/` holds one workflow, `pages.yml`, which builds the website. Nothing runs the\n  gate on a push or a pull request.\n- No `rust-toolchain.toml` exists; `AGENTS.md:116-119` documents the drift (\"a newer clippy can\n  fail a commit that passed locally\") and asks for `rustup update` instead of removing the cause.\n- `Dockerfile` builds a distroless `substrate-daemon`; nothing publishes, signs or digest-pins it.\n  `README.md` § *Status*: \"stable publication: **not done**\".\n- Agent consumes the 0.4.0 bundle as a hand-copied tree (`docs/plan/04-direct-byte-plane.md` §\n  *Slice C*); design 07 § *Bundle* already fixes the OCI shape and nothing implements it.\n- Two `AGENTS.md` facts contradict the scripts they describe (`AGENTS.md:112` vs\n  `scripts/gate.sh:20-23`; `AGENTS.md:172` vs `scripts/bot-token.sh:8`), and `STATUS.md` is\n  observed 2026-08-17, before extraction, still naming the monorepo as the source.\n\n## Scope\n\nSix stories, worked in this order because each makes the next checkable:\n\n1. `story:agents-md-matches-the-scripts` — docs only.\n2. `story:ci-runs-the-full-gate` — `gate.yml`, SHA-pinned actions, `contents: read`.\n3. `story:pinned-rust-toolchain` — `rust-toolchain.toml` = `1.97` = `Cargo.toml` `rust-version` =\n   `Dockerfile` builder tag; one commit per bump.\n4. `story:status-md-re-observed` — from fresh gate output.\n5. `story:signed-daemon-image` — `release.yml` on a bare-version tag, GHCR, cosign keyless.\n6. `story:contract-bundle-oci-artifact` — deterministic OCI layout, published beside the image.\n\n## Out of Scope\n\nAny route, schema, wire identifier, capability or frozen bundle byte. The development bundles stay\ndevelopment bundles; a stable-contract decision is its own ADR under atlas ADR 0019.\nBit-for-bit reproducible binaries: digests are recorded, reproduction is a later milestone.\n\n## Risks\n\n- `packages: write` and `id-token: write` widen a workflow's authority; scope them to the release\n  job only.\n- The delegated lane needs bubblewrap and cgroup delegation and cannot run on a hosted GitHub\n  runner; either a self-hosted runner joins, or CI asserts the portable lane and the delegated\n  lane stays a local pre-release step. The CI story records which.\n- Keyless signing depends on Sigstore availability; a release fails closed, never publishes\n  unsigned.\n\n## Done When\n\nFrom a fresh clone: `AGENTS.md` matches the scripts; a PR with a formatting error goes red; clippy\nruns on the pinned toolchain; `STATUS.md` names the standalone repository and the current tag; a\ntag `0.x.y` yields a signed image and a signed bundle artifact whose digests are in `CHANGELOG.md`.\n\n## Correction — 2026-08-30\n\nScope above says \"Six stories\". **Eight** decompose this epic: the six named plus\n`story:tooling-moves-to-cargo-xtask` (implemented — the gate's Python moved to `cargo xtask`) and\n`story:contract-gate-sees-route-paths` (draft — the successor check reads operation ids and never\nthe paths they are served at, so a route can move silently).\n\nSix of the eight are implemented. `story:contract-bundle-oci-artifact` is active and unblocked;\n`story:contract-gate-sees-route-paths` is a draft filed today.\n\n## Reconciled state — 2026-09-01\n\nThe CI, toolchain, repository recovery, Apache-2.0/publication, route/compatibility gates, daemon image and release-publication stories are implemented on main. The epic remains active for the signed contract-bundle OCI artifact and the separately coordinated move of the daemon's advertised contract frontier. A signed daemon image does not make a development bundle stable.\n"},"command":"update-entity","target":"01MEM0000000000000016"},"payload":{"actor":"human:timo","at":1788216860265,"causation":"cmd-protocol-artifact-body-section-epic-release-hardening-1788216860265","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"epic:release-hardening@6#0~839ff8073a33161a","executor":null,"recorded_at":"2026-08-31T22:54:20Z"}}
{"entity":"epic","version":1,"id":"rust-sdk","revision":4,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"body":"# Epic: Public Rust SDK\n\n## Outcome\n\nRust consumers create, control and observe confined workspaces and processes through an ergonomic, owner-released SDK. A managed local deployment is still a separate authenticated daemon child.\n\n## Boundaries\n\nThe SDK serves the daemon-advertised `substrate-wire/0.4.0` surface over an owner-private Unix socket. It chooses no product execution policy, exposes no driver API, and does not make the development contract stable.\n\n## Delivery\n\nTyped client and recovery, managed daemon ownership, approved package publication, and public adoption documentation are separate stories under this epic.\n\n## Reconciled state — 2026-09-01\n\nTyped local client handles, managed daemon ownership and the public SDK journey are implemented. The epic remains active for registry publication evidence and parity with the contract frontier the daemon eventually advertises. Remote HTTPS/WSS transport is deliberately owned by epic:remote-serving so the local SDK boundary is not confused with hosted authentication.\n"},"args":{"changes":{"body":"# Epic: Public Rust SDK\n\n## Outcome\n\nRust consumers create, control and observe confined workspaces and processes through an ergonomic, owner-released SDK. A managed local deployment is still a separate authenticated daemon child.\n\n## Boundaries\n\nThe SDK serves the daemon-advertised `substrate-wire/0.4.0` surface over an owner-private Unix socket. It chooses no product execution policy, exposes no driver API, and does not make the development contract stable.\n\n## Delivery\n\nTyped client and recovery, managed daemon ownership, approved package publication, and public adoption documentation are separate stories under this epic.\n\n## Reconciled state — 2026-09-01\n\nTyped local client handles, managed daemon ownership and the public SDK journey are implemented. The epic remains active for registry publication evidence and parity with the contract frontier the daemon eventually advertises. Remote HTTPS/WSS transport is deliberately owned by epic:remote-serving so the local SDK boundary is not confused with hosted authentication.\n"},"command":"update-entity","target":"01MEM0000000000000025"},"payload":{"actor":"human:timo","at":1788216860439,"causation":"cmd-protocol-artifact-body-section-epic-rust-sdk-1788216860439","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"epic:rust-sdk@4#0~f1b55498dab20027","executor":null,"recorded_at":"2026-08-31T22:54:20Z"}}
{"entity":"epic","version":1,"id":"container-driver-entry","revision":5,"type":"aep.entity.update/v1","from_state":"proposed","to_state":"proposed","changed":{"body":"# Epic: Container driver entry\n\n## Outcome\n\nPlan 03's entry criteria are proven mechanically before any Docker code exists, so a second driver\ntests the contract instead of the contract bending around it. Plan 03 moves from *deferred* to\n*active* and `ROADMAP.md` phase 5 to *in progress* in the same change.\n\n## Why Now\n\n`docs/plan/03-container-driver.md` is *deferred until host conformance* and names four entry\ncriteria. The first — the minimum host slice passes public-wire and driver conformance — is met\n(`STATUS.md`, phase 3 complete). The other three are claims nobody has checked: driver ports\ncontain no host library types; capability predicates and applied-enforcement observations are\nstable; the security design states which Docker authorities are root-equivalent.\n\n`ROADMAP.md` forbids starting phase 5 while a phase-4 exit criterion is open, so this epic holds\none story and depends on `epic:byte-plane-completion`. It exists now so that the entry gate is\nwritten down before anyone writes a `docker` module.\n\n## Scope\n\n`story:docker-driver-entry-gate`: a structural test that no `substrate-host` type crosses the\ndriver port; the workspace/exec conformance journey as a driver-parameterised harness the host\ndriver passes unchanged; the design 04 section naming root-equivalent Docker authorities.\n\n## Out of Scope\n\nThe Docker driver itself, image pull/inspection, workload lifecycle — plan 03 § *Planned proof*,\nwhich starts once this epic is implemented.\n\n## Risks\n\nThe structural test's shape: a compile-time trait-bound check, a `cargo` dependency-graph\nassertion, or a source scan. Prefer the one that fails on the first offending `use`.\n\n## Done When\n\nThe story is implemented, and plan 03's status and `ROADMAP.md` phase 5 change together.\n\n## Correction — 2026-08-30\n\nThe body above says this epic holds one story and is done when that story is implemented. The\n2026-08-29 split gave it **two**: `story:driver-port-carries-no-host-types`, which is already\nimplemented, and `story:docker-driver-entry-gate`, which is a draft now carrying a proposed design\n(`docs/design/15-docker-driver-entry-gate.md`).\n\n\"Done when the story is implemented\" therefore reads as satisfied when it is not. This epic is done\nwhen **both** are, and the Docker gate is the one that remains — a gate stated before any Docker\ncode exists, whose design says sealed secret slots and egress apertures are **absent** on a\ncontainer driver rather than weakened.\n\n## Reconciled delivery — 2026-09-01\n\nThe structural driver-port story is implemented. The proposed Docker entry gate remains first, followed by a workspace/exec slice and then an immutable image-backed workload slice. Both implementation slices depend on shared black-box conformance and retain absent facts for secret slots and apertures until a separate mechanism proves them.\n"},"args":{"changes":{"body":"# Epic: Container driver entry\n\n## Outcome\n\nPlan 03's entry criteria are proven mechanically before any Docker code exists, so a second driver\ntests the contract instead of the contract bending around it. Plan 03 moves from *deferred* to\n*active* and `ROADMAP.md` phase 5 to *in progress* in the same change.\n\n## Why Now\n\n`docs/plan/03-container-driver.md` is *deferred until host conformance* and names four entry\ncriteria. The first — the minimum host slice passes public-wire and driver conformance — is met\n(`STATUS.md`, phase 3 complete). The other three are claims nobody has checked: driver ports\ncontain no host library types; capability predicates and applied-enforcement observations are\nstable; the security design states which Docker authorities are root-equivalent.\n\n`ROADMAP.md` forbids starting phase 5 while a phase-4 exit criterion is open, so this epic holds\none story and depends on `epic:byte-plane-completion`. It exists now so that the entry gate is\nwritten down before anyone writes a `docker` module.\n\n## Scope\n\n`story:docker-driver-entry-gate`: a structural test that no `substrate-host` type crosses the\ndriver port; the workspace/exec conformance journey as a driver-parameterised harness the host\ndriver passes unchanged; the design 04 section naming root-equivalent Docker authorities.\n\n## Out of Scope\n\nThe Docker driver itself, image pull/inspection, workload lifecycle — plan 03 § *Planned proof*,\nwhich starts once this epic is implemented.\n\n## Risks\n\nThe structural test's shape: a compile-time trait-bound check, a `cargo` dependency-graph\nassertion, or a source scan. Prefer the one that fails on the first offending `use`.\n\n## Done When\n\nThe story is implemented, and plan 03's status and `ROADMAP.md` phase 5 change together.\n\n## Correction — 2026-08-30\n\nThe body above says this epic holds one story and is done when that story is implemented. The\n2026-08-29 split gave it **two**: `story:driver-port-carries-no-host-types`, which is already\nimplemented, and `story:docker-driver-entry-gate`, which is a draft now carrying a proposed design\n(`docs/design/15-docker-driver-entry-gate.md`).\n\n\"Done when the story is implemented\" therefore reads as satisfied when it is not. This epic is done\nwhen **both** are, and the Docker gate is the one that remains — a gate stated before any Docker\ncode exists, whose design says sealed secret slots and egress apertures are **absent** on a\ncontainer driver rather than weakened.\n\n## Reconciled delivery — 2026-09-01\n\nThe structural driver-port story is implemented. The proposed Docker entry gate remains first, followed by a workspace/exec slice and then an immutable image-backed workload slice. Both implementation slices depend on shared black-box conformance and retain absent facts for secret slots and apertures until a separate mechanism proves them.\n"},"command":"update-entity","target":"01MEM0000000000000004"},"payload":{"actor":"human:timo","at":1788216860613,"causation":"cmd-protocol-artifact-body-section-epic-container-driver-entry-1788216860613","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"epic:container-driver-entry@5#0~d8048f3777a67b81","executor":null,"recorded_at":"2026-08-31T22:54:20Z"}}
{"entity":"epic","version":1,"id":"mcp-test-surface","revision":3,"type":"aep.entity.update/v1","from_state":"proposed","to_state":"proposed","changed":{"body":"# Epic: MCP test surface\n\n## Outcome\n\nArbitrary MCP-capable harnesses can start an isolated, disposable Substrate daemon and exercise its public workspace, execution, output, metrics and cleanup behavior through a small MCP adapter.\n\n## Boundaries\n\nThe adapter is a client of the public Substrate wire contract. No MCP type or server implementation enters the daemon, host driver or wire crate; the surface is for local testing and evaluation, not a production multi-tenant ingress. The work starts after the current resource-bounded execution epic.\n\n## Reconciled state — 2026-09-01\n\nResource-bounded execution is implemented, so the former deferral is cleared. The MCP surface remains a proposed local testing adapter after SDK contract parity; it is not a production ingress and does not block remote HTTPS serving or driver work.\n"},"args":{"changes":{"body":"# Epic: MCP test surface\n\n## Outcome\n\nArbitrary MCP-capable harnesses can start an isolated, disposable Substrate daemon and exercise its public workspace, execution, output, metrics and cleanup behavior through a small MCP adapter.\n\n## Boundaries\n\nThe adapter is a client of the public Substrate wire contract. No MCP type or server implementation enters the daemon, host driver or wire crate; the surface is for local testing and evaluation, not a production multi-tenant ingress. The work starts after the current resource-bounded execution epic.\n\n## Reconciled state — 2026-09-01\n\nResource-bounded execution is implemented, so the former deferral is cleared. The MCP surface remains a proposed local testing adapter after SDK contract parity; it is not a production ingress and does not block remote HTTPS serving or driver work.\n"},"command":"update-entity","target":"01MEM0000000000000013"},"payload":{"actor":"human:timo","at":1788216860792,"causation":"cmd-protocol-artifact-body-section-epic-mcp-test-surface-1788216860792","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"epic:mcp-test-surface@3#0~73a20f94f7acd55f","executor":null,"recorded_at":"2026-08-31T22:54:20Z"}}
{"entity":"epic","version":1,"id":"remote-serving","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000019","to":"proposed"},"payload":{"actor":"human:timo","at":1788216870319,"causation":"cmd-move-epic-remote-serving-1788216870319","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"epic:remote-serving@2#0~ad09e0b292243592","executor":null,"recorded_at":"2026-08-31T22:54:30Z"}}
{"entity":"epic","version":1,"id":"kubernetes-deployment-and-driver","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000010","to":"proposed"},"payload":{"actor":"human:timo","at":1788216870561,"causation":"cmd-move-epic-kubernetes-deployment-and-driver-1788216870561","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"epic:kubernetes-deployment-and-driver@2#0~0579e912cb63b379","executor":null,"recorded_at":"2026-08-31T22:54:30Z"}}
{"entity":"epic","version":1,"id":"firecracker-driver","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000007","to":"proposed"},"payload":{"actor":"human:timo","at":1788216870794,"causation":"cmd-move-epic-firecracker-driver-1788216870794","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"epic:firecracker-driver@2#0~3e73cefca43bd891","executor":null,"recorded_at":"2026-08-31T22:54:30Z"}}
{"entity":"story","version":1,"id":"promote-development-contract-frontier","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000115","to":"proposed"},"payload":{"actor":"human:timo","at":1788216871027,"causation":"cmd-move-story-promote-development-contract-frontier-1788216871027","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:promote-development-contract-frontier@2#0~fe1e7a9802390899","executor":null,"recorded_at":"2026-08-31T22:54:31Z"}}
{"entity":"story","version":1,"id":"sdk-promoted-contract-parity","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000148","to":"proposed"},"payload":{"actor":"human:timo","at":1788216871261,"causation":"cmd-move-story-sdk-promoted-contract-parity-1788216871261","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:sdk-promoted-contract-parity@2#0~df842a0e2a46e643","executor":null,"recorded_at":"2026-08-31T22:54:31Z"}}
{"entity":"story","version":1,"id":"production-tls-control-listener","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000112","to":"proposed"},"payload":{"actor":"human:timo","at":1788216871696,"causation":"cmd-move-story-production-tls-control-listener-1788216871696","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:production-tls-control-listener@2#0~4cf8928ace45ae28","executor":null,"recorded_at":"2026-08-31T22:54:31Z"}}
{"entity":"story","version":1,"id":"hosted-trust-envelope-admission","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000085","to":"proposed"},"payload":{"actor":"human:timo","at":1788216872139,"causation":"cmd-move-story-hosted-trust-envelope-admission-1788216872139","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:hosted-trust-envelope-admission@2#0~f6306d6c361634d3","executor":null,"recorded_at":"2026-08-31T22:54:32Z"}}
{"entity":"story","version":1,"id":"remote-sdk-transport","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000130","to":"proposed"},"payload":{"actor":"human:timo","at":1788216872379,"causation":"cmd-move-story-remote-sdk-transport-1788216872379","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:remote-sdk-transport@2#0~4ba1dbf3a4689da4","executor":null,"recorded_at":"2026-08-31T22:54:32Z"}}
{"entity":"story","version":1,"id":"node-bound-kubernetes-serving-profile","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000103","to":"proposed"},"payload":{"actor":"human:timo","at":1788216872612,"causation":"cmd-move-story-node-bound-kubernetes-serving-profile-1788216872612","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:node-bound-kubernetes-serving-profile@2#0~2a5bc5f52c4eb9b6","executor":null,"recorded_at":"2026-08-31T22:54:32Z"}}
{"entity":"story","version":1,"id":"operational-health-and-telemetry","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000106","to":"proposed"},"payload":{"actor":"human:timo","at":1788216872844,"causation":"cmd-move-story-operational-health-and-telemetry-1788216872844","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:operational-health-and-telemetry@2#0~7a1f801a74903869","executor":null,"recorded_at":"2026-08-31T22:54:32Z"}}
{"entity":"story","version":1,"id":"remote-clean-room-conformance","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000127","to":"proposed"},"payload":{"actor":"human:timo","at":1788216873078,"causation":"cmd-move-story-remote-clean-room-conformance-1788216873078","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:remote-clean-room-conformance@2#0~1416f128df1360b4","executor":null,"recorded_at":"2026-08-31T22:54:33Z"}}
{"entity":"story","version":1,"id":"docker-workspace-and-exec-slice","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000064","to":"proposed"},"payload":{"actor":"human:timo","at":1788216873309,"causation":"cmd-move-story-docker-workspace-and-exec-slice-1788216873309","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:docker-workspace-and-exec-slice@2#0~60376ba333507a6a","executor":null,"recorded_at":"2026-08-31T22:54:33Z"}}
{"entity":"story","version":1,"id":"docker-image-backed-workload-slice","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000061","to":"proposed"},"payload":{"actor":"human:timo","at":1788216873540,"causation":"cmd-move-story-docker-image-backed-workload-slice-1788216873540","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:docker-image-backed-workload-slice@2#0~cf30e604c830bf45","executor":null,"recorded_at":"2026-08-31T22:54:33Z"}}
{"entity":"story","version":1,"id":"kubernetes-namespace-driver-entry-gate","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000088","to":"proposed"},"payload":{"actor":"human:timo","at":1788216873775,"causation":"cmd-move-story-kubernetes-namespace-driver-entry-gate-1788216873775","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:kubernetes-namespace-driver-entry-gate@2#0~af12439ae99bf910","executor":null,"recorded_at":"2026-08-31T22:54:33Z"}}
{"entity":"story","version":1,"id":"kubernetes-namespace-workspace-and-exec-slice","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000091","to":"proposed"},"payload":{"actor":"human:timo","at":1788216874011,"causation":"cmd-move-story-kubernetes-namespace-workspace-and-exec-slice-1788216874011","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:kubernetes-namespace-workspace-and-exec-slice@2#0~b7774a8295922c72","executor":null,"recorded_at":"2026-08-31T22:54:34Z"}}
{"entity":"story","version":1,"id":"firecracker-entry-and-boot-artifact-gate","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000076","to":"proposed"},"payload":{"actor":"human:timo","at":1788216874248,"causation":"cmd-move-story-firecracker-entry-and-boot-artifact-gate-1788216874248","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:firecracker-entry-and-boot-artifact-gate@2#0~a66eef4ad0737fd9","executor":null,"recorded_at":"2026-08-31T22:54:34Z"}}
{"entity":"story","version":1,"id":"firecracker-workspace-and-exec-slice","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000079","to":"proposed"},"payload":{"actor":"human:timo","at":1788216874488,"causation":"cmd-move-story-firecracker-workspace-and-exec-slice-1788216874488","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:firecracker-workspace-and-exec-slice@2#0~8398d8740dd5c9a0","executor":null,"recorded_at":"2026-08-31T22:54:34Z"}}
{"entity":"story","version":1,"id":"confinement-runtime-hardening","revision":5,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"body":"# Story: Confinement and runtime hardening\n\n## Outcome\n\nHost roots cannot reach host IPC or shadow owned mounts; pipe, aperture, file and replay paths terminate and report facts without deadlock, unbounded allocation or optimistic degradation.\n\n## Acceptance\n\n- AF_UNIX host IPC is blocked and probe-verified.\n- Owned and overlapping mounts are refused component-wise.\n- Pipe queue saturation is a durable named terminal failure.\n- File reads remain bounded under concurrent growth.\n- Aperture cleanup, counters, source eviction and delegated tests are race-free.\n- Required delegated context is verified before replay.\n\n## Current work — 2026-09-01\n\nThe uncommitted substrate-hardening worktree on wave/hardening-2026-09-01 is preparing the Substrate 0.4.0 software release as an in-flight slice of this story, not a competing implementation. It contains accepted ADR 0023, a proposed substrate-wire/0.12.0 successor, probe-gated read-only/scoped workspace writes and SDK coverage for current confinement options. This branch must merge or be deliberately superseded before this story can close. The roadmap stories for contract promotion and SDK parity consume its result and must not reimplement it. The worktree is based at the 0.3.1 tag and is two main commits behind, so integration must preserve the later 0.3.1 digest record.\n"},"args":{"changes":{"body":"# Story: Confinement and runtime hardening\n\n## Outcome\n\nHost roots cannot reach host IPC or shadow owned mounts; pipe, aperture, file and replay paths terminate and report facts without deadlock, unbounded allocation or optimistic degradation.\n\n## Acceptance\n\n- AF_UNIX host IPC is blocked and probe-verified.\n- Owned and overlapping mounts are refused component-wise.\n- Pipe queue saturation is a durable named terminal failure.\n- File reads remain bounded under concurrent growth.\n- Aperture cleanup, counters, source eviction and delegated tests are race-free.\n- Required delegated context is verified before replay.\n\n## Current work — 2026-09-01\n\nThe uncommitted substrate-hardening worktree on wave/hardening-2026-09-01 is preparing the Substrate 0.4.0 software release as an in-flight slice of this story, not a competing implementation. It contains accepted ADR 0023, a proposed substrate-wire/0.12.0 successor, probe-gated read-only/scoped workspace writes and SDK coverage for current confinement options. This branch must merge or be deliberately superseded before this story can close. The roadmap stories for contract promotion and SDK parity consume its result and must not reimplement it. The worktree is based at the 0.3.1 tag and is two main commits behind, so integration must preserve the later 0.3.1 digest record.\n"},"command":"update-entity","target":"01MEM0000000000000043"},"payload":{"actor":"human:timo","at":1788217085067,"causation":"cmd-protocol-artifact-body-section-story-confinement-runtime-hardening-1788217085067","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:confinement-runtime-hardening@5#0~c9abf147564388dd","executor":null,"recorded_at":"2026-08-31T22:58:05Z"}}
{"entity":"story","version":1,"id":"confinement-runtime-hardening","revision":6,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"body":"# Story: Confinement and runtime hardening\n\n## Outcome\n\nHost roots cannot reach host IPC or shadow owned mounts; pipe, aperture, file and replay paths terminate and report facts without deadlock, unbounded allocation or optimistic degradation.\n\n## Acceptance\n\n- AF_UNIX host IPC is blocked and probe-verified.\n- Owned and overlapping mounts are refused component-wise.\n- Pipe queue saturation is a durable named terminal failure.\n- File reads remain bounded under concurrent growth.\n- Aperture cleanup, counters, source eviction and delegated tests are race-free.\n- Required delegated context is verified before replay.\n\n## Current work — 2026-09-01\n\nThe unmerged substrate-hardening worktree on wave/hardening-2026-09-01 is preparing the Substrate 0.4.0 software release as an in-flight slice of this story, not a competing implementation. Commit f2bad01 is clean and pushed, and contains accepted ADR 0023, substrate-wire/0.12.0, probe-gated read-only/scoped workspace writes and SDK coverage for current confinement options. This branch must merge or be deliberately superseded before this story can close. The roadmap stories for contract promotion and SDK parity consume its result and must not reimplement it. The branch is one commit ahead of the 0.3.1 tag while main has two later release-record commits, so integration must preserve the 0.3.1 digest record.\n"},"args":{"changes":{"body":"# Story: Confinement and runtime hardening\n\n## Outcome\n\nHost roots cannot reach host IPC or shadow owned mounts; pipe, aperture, file and replay paths terminate and report facts without deadlock, unbounded allocation or optimistic degradation.\n\n## Acceptance\n\n- AF_UNIX host IPC is blocked and probe-verified.\n- Owned and overlapping mounts are refused component-wise.\n- Pipe queue saturation is a durable named terminal failure.\n- File reads remain bounded under concurrent growth.\n- Aperture cleanup, counters, source eviction and delegated tests are race-free.\n- Required delegated context is verified before replay.\n\n## Current work — 2026-09-01\n\nThe unmerged substrate-hardening worktree on wave/hardening-2026-09-01 is preparing the Substrate 0.4.0 software release as an in-flight slice of this story, not a competing implementation. Commit f2bad01 is clean and pushed, and contains accepted ADR 0023, substrate-wire/0.12.0, probe-gated read-only/scoped workspace writes and SDK coverage for current confinement options. This branch must merge or be deliberately superseded before this story can close. The roadmap stories for contract promotion and SDK parity consume its result and must not reimplement it. The branch is one commit ahead of the 0.3.1 tag while main has two later release-record commits, so integration must preserve the 0.3.1 digest record.\n"},"command":"update-entity","target":"01MEM0000000000000043"},"payload":{"actor":"human:timo","at":1788217225574,"causation":"cmd-protocol-artifact-body-section-story-confinement-runtime-hardening-1788217225574","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:confinement-runtime-hardening@6#0~88f9568871b59fc5","executor":null,"recorded_at":"2026-08-31T23:00:25Z"}}
{"entity":"story","version":1,"id":"confinement-runtime-hardening","revision":7,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"body":"# Story: Confinement and runtime hardening\n\n## Outcome\n\nHost roots cannot reach host IPC or shadow owned mounts; pipe, aperture, file and replay paths terminate and report facts without deadlock, unbounded allocation or optimistic degradation.\n\n## Acceptance\n\n- AF_UNIX host IPC is blocked and probe-verified.\n- Owned and overlapping mounts are refused component-wise.\n- Pipe queue saturation is a durable named terminal failure.\n- File reads remain bounded under concurrent growth.\n- Aperture cleanup, counters, source eviction and delegated tests are race-free.\n- Required delegated context is verified before replay.\n\n## Current work — 2026-09-01\n\nThe unmerged substrate-hardening worktree on wave/hardening-2026-09-01 is preparing the Substrate 0.4.0 software release as an in-flight slice of this story, not a competing implementation. Commit f2bad01 is clean and pushed, and contains accepted ADR 0023, substrate-wire/0.12.0, probe-gated read-only/scoped workspace writes and SDK coverage for current confinement options. This branch must merge or be deliberately superseded before this story can close. The roadmap stories for contract promotion and SDK parity consume its result and must not reimplement it. The branch is one commit ahead of the 0.3.1 tag while main has two later release-record commits, so integration must preserve the 0.3.1 digest record.\n\n## Current delivery — 2026-09-01\n\nThe exact-workspace-execution slice shipped through PR 57 and is merged on main at annotated tag 0.4.0 (commit 31340a6). It contains accepted ADR 0023, substrate-wire/0.12.0, probe-gated read-only/scoped workspace writes and SDK coverage for the current confinement options. The roadmap stories for contract promotion and SDK parity consume this result and must not reimplement it. This delivery replaces the earlier worktree note; closing the broader story still requires its complete acceptance evidence, not merely the release tag.\n"},"args":{"changes":{"body":"# Story: Confinement and runtime hardening\n\n## Outcome\n\nHost roots cannot reach host IPC or shadow owned mounts; pipe, aperture, file and replay paths terminate and report facts without deadlock, unbounded allocation or optimistic degradation.\n\n## Acceptance\n\n- AF_UNIX host IPC is blocked and probe-verified.\n- Owned and overlapping mounts are refused component-wise.\n- Pipe queue saturation is a durable named terminal failure.\n- File reads remain bounded under concurrent growth.\n- Aperture cleanup, counters, source eviction and delegated tests are race-free.\n- Required delegated context is verified before replay.\n\n## Current work — 2026-09-01\n\nThe unmerged substrate-hardening worktree on wave/hardening-2026-09-01 is preparing the Substrate 0.4.0 software release as an in-flight slice of this story, not a competing implementation. Commit f2bad01 is clean and pushed, and contains accepted ADR 0023, substrate-wire/0.12.0, probe-gated read-only/scoped workspace writes and SDK coverage for current confinement options. This branch must merge or be deliberately superseded before this story can close. The roadmap stories for contract promotion and SDK parity consume its result and must not reimplement it. The branch is one commit ahead of the 0.3.1 tag while main has two later release-record commits, so integration must preserve the 0.3.1 digest record.\n\n## Current delivery — 2026-09-01\n\nThe exact-workspace-execution slice shipped through PR 57 and is merged on main at annotated tag 0.4.0 (commit 31340a6). It contains accepted ADR 0023, substrate-wire/0.12.0, probe-gated read-only/scoped workspace writes and SDK coverage for the current confinement options. The roadmap stories for contract promotion and SDK parity consume this result and must not reimplement it. This delivery replaces the earlier worktree note; closing the broader story still requires its complete acceptance evidence, not merely the release tag.\n"},"command":"update-entity","target":"01MEM0000000000000043"},"payload":{"actor":"human:timo","at":1788218497116,"causation":"cmd-protocol-artifact-body-section-story-confinement-runtime-hardening-1788218497116","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:confinement-runtime-hardening@7#0~2117402f1dbd5587","executor":null,"recorded_at":"2026-08-31T23:21:37Z"}}
{"entity":"story","version":1,"id":"confinement-runtime-hardening","revision":8,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"body":"# Story: Confinement and runtime hardening\n\n## Outcome\n\nHost roots cannot reach host IPC or shadow owned mounts; pipe, aperture, file and replay paths terminate and report facts without deadlock, unbounded allocation or optimistic degradation.\n\n## Acceptance\n\n- AF_UNIX host IPC is blocked and probe-verified.\n- Owned and overlapping mounts are refused component-wise.\n- Pipe queue saturation is a durable named terminal failure.\n- File reads remain bounded under concurrent growth.\n- Aperture cleanup, counters, source eviction and delegated tests are race-free.\n- Required delegated context is verified before replay.\n\n## Current work — 2026-09-01\n\nSuperseded by the merged delivery record below. The former worktree is no longer unmerged.\n\n## Current delivery — 2026-09-01\n\nThe exact-workspace-execution slice shipped through PR 57 and is merged on main at annotated tag 0.4.0 (commit 31340a6). It contains accepted ADR 0023, substrate-wire/0.12.0, probe-gated read-only/scoped workspace writes and SDK coverage for the current confinement options. The roadmap stories for contract promotion and SDK parity consume this result and must not reimplement it. This delivery replaces the earlier worktree note; closing the broader story still requires its complete acceptance evidence, not merely the release tag.\n"},"args":{"changes":{"body":"# Story: Confinement and runtime hardening\n\n## Outcome\n\nHost roots cannot reach host IPC or shadow owned mounts; pipe, aperture, file and replay paths terminate and report facts without deadlock, unbounded allocation or optimistic degradation.\n\n## Acceptance\n\n- AF_UNIX host IPC is blocked and probe-verified.\n- Owned and overlapping mounts are refused component-wise.\n- Pipe queue saturation is a durable named terminal failure.\n- File reads remain bounded under concurrent growth.\n- Aperture cleanup, counters, source eviction and delegated tests are race-free.\n- Required delegated context is verified before replay.\n\n## Current work — 2026-09-01\n\nSuperseded by the merged delivery record below. The former worktree is no longer unmerged.\n\n## Current delivery — 2026-09-01\n\nThe exact-workspace-execution slice shipped through PR 57 and is merged on main at annotated tag 0.4.0 (commit 31340a6). It contains accepted ADR 0023, substrate-wire/0.12.0, probe-gated read-only/scoped workspace writes and SDK coverage for the current confinement options. The roadmap stories for contract promotion and SDK parity consume this result and must not reimplement it. This delivery replaces the earlier worktree note; closing the broader story still requires its complete acceptance evidence, not merely the release tag.\n"},"command":"update-entity","target":"01MEM0000000000000043"},"payload":{"actor":"human:timo","at":1788218525797,"causation":"cmd-protocol-artifact-body-section-story-confinement-runtime-hardening-1788218525797","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:confinement-runtime-hardening@8#0~3977361dfcf1d902","executor":null,"recorded_at":"2026-08-31T23:22:05Z"}}
{"entity":"story","version":1,"id":"confinement-runtime-hardening","revision":9,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"body":"# Story: Confinement and runtime hardening\n\n## Outcome\n\nHost roots cannot reach host IPC or shadow owned mounts; pipe, aperture, file and replay paths terminate and report facts without deadlock, unbounded allocation or optimistic degradation.\n\n## Acceptance\n\n- AF_UNIX host IPC is blocked and probe-verified.\n- Owned and overlapping mounts are refused component-wise.\n- Pipe queue saturation is a durable named terminal failure.\n- File reads remain bounded under concurrent growth.\n- Aperture cleanup, counters, source eviction and delegated tests are race-free.\n- Required delegated context is verified before replay.\n\n## Current work — 2026-09-01\n\nSuperseded by the merged delivery record below. The former worktree is no longer unmerged.\n\n## Current delivery — 2026-09-01\n\nThe exact-workspace-execution slice shipped through PR 57 and is merged on main at annotated tag 0.4.0 (commit 31340a6). It contains accepted ADR 0023, substrate-wire/0.12.0, probe-gated read-only/scoped workspace writes and SDK coverage for the current confinement options. The roadmap stories for contract promotion and SDK parity consume this result and must not reimplement it. This delivery replaces the earlier worktree note; closing the broader story still requires its complete acceptance evidence, not merely the release tag.\n\n## Completion evidence — 2026-09-01\n\nThe 0.4.0 release at commit 31340a6 was re-audited against every acceptance item before implementation. Existing coverage already proved the AF_UNIX sentinel probe, bounded concurrent-growth reads, stale aperture cleanup, post-quiescence counters, idle-only source eviction, and the delegated cgroup lane.\n\nThe audit found two residual enforcement gaps: later-owned mounts (`/scratch`, the aperture hosts file, and the generated CA bundle) were not in the component-wise root collision set; and reusable AF_UNIX datagram socketpairs plus x32-tagged socket syscalls could evade the socket refusal. Failing-first regressions reproduced both gaps. Commit 218d655 closes them while preserving AF_UNIX stream socketpairs for process-local IPC and retaining the existing `exec.read-only-root-invalid` refusal.\n\nCommit 7b94844 adds boundary-level evidence that pipe queue saturation persists `session.output-backpressure` as the durable terminal observation and that required delegated context is rejected when absent or malformed before a successful request can replay. The original attribution row remains immutable.\n\nVerification on this branch:\n\n- `cargo test -p b10x-substrate-host --lib --locked`: 67 passed.\n- `cargo test -p b10x-substrate-daemon --test contract_vectors --locked`: 28 passed.\n- `bash scripts/delegated-lane.sh`: exit 0 under a delegated cgroup v2 root; 67 host unit tests, 11 host integration cases, and 8 shipped-daemon runtime-vector tests passed, including the 20-attempt aperture ceiling race.\n- Focused formatting and clippy checks for every changed crate passed with warnings denied.\n\nNo wire schema, refusal code, released contract bundle, or accepted design decision changed.\n"},"args":{"changes":{"body":"# Story: Confinement and runtime hardening\n\n## Outcome\n\nHost roots cannot reach host IPC or shadow owned mounts; pipe, aperture, file and replay paths terminate and report facts without deadlock, unbounded allocation or optimistic degradation.\n\n## Acceptance\n\n- AF_UNIX host IPC is blocked and probe-verified.\n- Owned and overlapping mounts are refused component-wise.\n- Pipe queue saturation is a durable named terminal failure.\n- File reads remain bounded under concurrent growth.\n- Aperture cleanup, counters, source eviction and delegated tests are race-free.\n- Required delegated context is verified before replay.\n\n## Current work — 2026-09-01\n\nSuperseded by the merged delivery record below. The former worktree is no longer unmerged.\n\n## Current delivery — 2026-09-01\n\nThe exact-workspace-execution slice shipped through PR 57 and is merged on main at annotated tag 0.4.0 (commit 31340a6). It contains accepted ADR 0023, substrate-wire/0.12.0, probe-gated read-only/scoped workspace writes and SDK coverage for the current confinement options. The roadmap stories for contract promotion and SDK parity consume this result and must not reimplement it. This delivery replaces the earlier worktree note; closing the broader story still requires its complete acceptance evidence, not merely the release tag.\n\n## Completion evidence — 2026-09-01\n\nThe 0.4.0 release at commit 31340a6 was re-audited against every acceptance item before implementation. Existing coverage already proved the AF_UNIX sentinel probe, bounded concurrent-growth reads, stale aperture cleanup, post-quiescence counters, idle-only source eviction, and the delegated cgroup lane.\n\nThe audit found two residual enforcement gaps: later-owned mounts (`/scratch`, the aperture hosts file, and the generated CA bundle) were not in the component-wise root collision set; and reusable AF_UNIX datagram socketpairs plus x32-tagged socket syscalls could evade the socket refusal. Failing-first regressions reproduced both gaps. Commit 218d655 closes them while preserving AF_UNIX stream socketpairs for process-local IPC and retaining the existing `exec.read-only-root-invalid` refusal.\n\nCommit 7b94844 adds boundary-level evidence that pipe queue saturation persists `session.output-backpressure` as the durable terminal observation and that required delegated context is rejected when absent or malformed before a successful request can replay. The original attribution row remains immutable.\n\nVerification on this branch:\n\n- `cargo test -p b10x-substrate-host --lib --locked`: 67 passed.\n- `cargo test -p b10x-substrate-daemon --test contract_vectors --locked`: 28 passed.\n- `bash scripts/delegated-lane.sh`: exit 0 under a delegated cgroup v2 root; 67 host unit tests, 11 host integration cases, and 8 shipped-daemon runtime-vector tests passed, including the 20-attempt aperture ceiling race.\n- Focused formatting and clippy checks for every changed crate passed with warnings denied.\n\nNo wire schema, refusal code, released contract bundle, or accepted design decision changed.\n"},"command":"update-entity","target":"01MEM0000000000000043"},"payload":{"actor":"human:timo","at":1788220341713,"causation":"cmd-protocol-artifact-body-section-story-confinement-runtime-hardening-1788220341713","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:confinement-runtime-hardening@9#0~028ef3684a64dce2","executor":null,"recorded_at":"2026-08-31T23:52:21Z"}}
{"entity":"story","version":1,"id":"confinement-runtime-hardening","revision":9,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"7b94844","source":"bash scripts/delegated-lane.sh: exit 0; host and shipped-daemon delegated lanes passed","target":"01MEM0000000000000043"},"payload":{"actor":"human:timo","at":1788220355000,"causation":"cmd-evidence-story-confinement-runtime-hardening-test_result-1788220356047","change":{"change":"evidence","kind":"test_result","reference":"7b94844","source":"bash scripts/delegated-lane.sh: exit 0; host and shipped-daemon delegated lanes passed"},"correlation":"protocol-artifact-evidence","event_id":"story:confinement-runtime-hardening@9#0~3691d9b2a2234663","executor":null,"recorded_at":"2026-08-31T23:52:35Z"}}
{"entity":"story","version":1,"id":"confinement-runtime-hardening","revision":10,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1}}","target":"01MEM0000000000000043","to":"implemented"},"payload":{"actor":"human:timo","at":1788220356404,"causation":"cmd-move-story-confinement-runtime-hardening-1788220356404","change":{"change":"moved","decided_on":{"recorded":{"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:confinement-runtime-hardening@10#0~bfdfc4ba0afe08be","executor":null,"recorded_at":"2026-08-31T23:52:36Z"}}
{"entity":"story","version":1,"id":"aperture-install-failure-loses-its-errno","revision":4,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"body":"# Story: An aperture install failure names its stage and loses its errno\n\n## Outcome\n\nWhen `exec.aperture-install-failed` fires, the operator learns *why* and not only *where*, and the\nintermittent stage-6 failure in the delegated lane is either fixed or explained.\n\n## What was observed\n\n**Reported by the pty implementor on 2026-08-30, not reproduced by the coordinator.**\n`egress::tests::declared_aperture_is_reachable` (`crates/substrate-host/src/egress.rs:1537`) fails\nintermittently inside a delegated systemd scope:\n\n```\ninstall the aperture: DriverError { class: Failed, code: \"exec.aperture-install-failed\",\nmessage: \"The egress aperture could not be installed exactly as declared (stage 6).\" }\n```\n\nIts numbers, as given: **1 failure in 10** serial delegated runs; 0 in 3 isolated runs of `egress::`\nalone; 0 in the portable `cargo test --workspace`. None of these were re-run independently.\n\nIt became visible only because `scripts/delegated-lane.sh` stopped selecting host cases by the\nsubstring `pty` — that filter had been running 8 of 58 host lib cases, so this test had never\nexecuted in a delegated scope. The fix is on `wt/pty-sessions`.\n\n## Blast radius, verified\n\n`scripts/gate.sh` contains no reference to the delegated lane or to\n`SUBSTRATE_VECTORS_CGROUP_ROOT` (`grep`, 0 hits). **The whole gate is unaffected.** What is affected\nis `bash scripts/delegated-lane.sh`, which is run by hand and is the only thing that executes the\ndelegated cases at all — so a lane that is red one run in ten is a lane whose red gets discounted.\n\n## What the code says, verified\n\n- **Stage 6 is `bind`.** The sandbox helper returns a stage number as its exit status; `return 6`\n  follows `libc::bind(listener, …)` on `loopback_sockaddr(port)`, inside the sandbox's network\n  namespace after `setns(CLONE_NEWUSER)` and `setns(CLONE_NEWNET)`.\n- **`SO_REUSEADDR` is already set on that socket**, immediately before the bind. So the first\n  hypothesis anybody reaches for — a previous run's socket in `TIME_WAIT` — is ruled out, and the\n  next person should not spend the afternoon on it.\n- **The errno cannot escape, and `install_failed` is not the reason.** The helper's only channel to\n  its parent is `_exit(stage)` (`egress.rs:456-464` turns that exit status into the message), and an\n  exit code carries a stage, not an errno. The helper is a fork precisely because\n  `setns` with `CLONE_NEWUSER` refuses a threaded caller, so it cannot allocate or return a\n  `DriverError`. The `install_failed` helper at `egress.rs:1092` *does* interpolate `{error}`; it is\n  simply not on this path.\n- The design states the trade deliberately: \"The return value is the stage that failed, so an\n  install failure names *where* rather than leaving an operator to guess between a namespace, a bind\n  and a handback.\"\n\n**Not established:** where `port` is chosen, whether two runs in one delegated scope can choose the\nsame one, and what the bind's actual errno is. Everything about the cause is open.\n\n## Acceptance\n\nAn aperture install failure carries the failing stage **and** the errno, and the stage-6 failure in\nthe delegated lane is either eliminated or has a named cause recorded against this story.\n\nEvidence that satisfies it, in order:\n\n1. The helper's stage channel widens to carry an errno — the exit status is one byte and already\n   spent, so this needs a second channel (the existing handback socket, a pipe, or an `AtomicI32` in\n   the shared mapping the forwarder already maps). Written failing-first against a forced bind\n   failure.\n2. The message names both, and a test asserts the errno reaches the caller.\n3. `bash scripts/delegated-lane.sh` run at least 20 times serially, with the failure count recorded\n   here — before and after. One failure in ten is a claim a 10-run sample cannot separate from two\n   in ten.\n4. If the cause is a port collision, the port's selection is the fix and the errno is the evidence\n   that found it; if it is not, this story records what it was.\n\n## Provenance\n\nFound while running `story:pty-sessions` in the wave of 2026-08-30, in the lane that story repaired.\nNot caused by it: the test and the helper both predate the wave. The coordinator verified the four\ncode claims above against `wave/2026-08-30-byte-plane`; the failure rate and the failing run are the\nimplementor's, quoted.\n\n## Correction — 2026-08-30, the flake is wider than this story says\n\nThe observation above names **one** test at **1 failure in 10**. A second reporter — the adversary\non `story:pty-sessions`' fifth pass — puts it at **module-wide, roughly 1 run in 2**, with two\nfurther tests failing that this story does not name:\n\n| test | `file:line` | observed |\n|---|---|---|\n| `declared_aperture_is_reachable` | `crates/substrate-host/src/egress.rs:1537` | the original report |\n| `applied_aperture_is_observed` | `crates/substrate-host/src/egress.rs:1657` | `Unreachable` where `Served` was expected |\n| `a_declared_ceiling_stops_the_relay` | `crates/substrate-host/src/egress.rs` | named, no assertion text captured |\n\nBoth rates are **reported, not reproduced by the coordinator**, and they disagree by a factor of\nfive. Two reporters, two runs of different lengths, on one machine — the honest reading is that the\nrate is unmeasured, not that it is 1 in 2 rather than 1 in 10. Acceptance item 3 already asks for a\n20-run sample; that sample now has to cover the **module**, not the one test.\n\nWhat this changes about the story: the stage-6 `bind` in `declared_aperture_is_reachable` is no\nlonger necessarily the whole subject. `applied_aperture_is_observed` failing with `Unreachable` is a\ndifferent symptom — a relay that installed and did not carry — so a single cause is a hypothesis and\nnot a finding. The errno the helper cannot report (`_exit(stage)`) is still the first thing to fix\neither way: it is what would tell these two symptoms apart.\n\n**Still verified, unchanged:** `scripts/gate.sh` has no delegated-lane reference, so the whole gate\nis unaffected; stage 6 is `libc::bind` with `SO_REUSEADDR` already set; the errno is lost at the\n`_exit(stage)` boundary and not in `install_failed`.\n\n## Implementation evidence — 2026-09-01\n\nThe inherited implementation had begun sending `[stage, errno]` over the descriptor handback, but\nthe production pair was still `SOCK_STREAM`, the receiver assumed one read returned the complete\nrecord, and coverage injected a payload rather than forcing the helper to fail. The failing-first\ntest `helper_handback_preserves_record_boundaries` observed `SO_TYPE=1` (`SOCK_STREAM`) where a\nrecord-preserving channel was required.\n\nThe completed path uses `SOCK_SEQPACKET | SOCK_CLOEXEC`; failure records and `SCM_RIGHTS` success\nhandbacks are distinct packets. Sends suppress `SIGPIPE`, receive and reap retry `EINTR`, truncation\nand unexpected control messages fail closed, and the exit status remains the fallback when no\nrecord arrives. `a_forced_bind_failure_names_its_stage_and_errno` opens one listener inside a held\nsandbox, forces a second bind on the same tuple, and asserts the caller receives class `failed`,\ncode `exec.aperture-install-failed`, stage 6, numeric `EADDRINUSE`, and its decoded OS error.\n\n### Named cause and fix\n\nThe first local `bash scripts/delegated-lane.sh` reproduction failed in\n`an_aperture_without_a_ceiling_passes_the_same_traffic` at the in-namespace connect. The widened\nhelper channel then caught the original bind symptom as stage 6, errno 99\n(`EADDRNOTAVAIL`, `Cannot assign requested address`). Together with the reported\n`Reach::Unreachable` (`ENETUNREACH`), this identifies one readiness race: bubblewrap publishes the\nchild pid while its concurrent child has not necessarily raised `lo` yet. The helper now reads\n`IFF_UP` and retries only `EADDRNOTAVAIL`, using one bounded one-second budget. It does not configure\nthe interface or widen the loopback-only network floor; every other bind errno is immediate.\n\nThe module harness also detached every `AppServer` and `Firehose` thread: each `Drop` discarded its\n`JoinHandle` while the thread retained its listener and blocked in `accept`. The affected test was\n0/100 failures in isolation but failed once in a 20-run module sample with the prior server threads\npresent. Both fixtures now stop, wake and join their thread, so the module sample does not accumulate\nlisteners or scheduling pressure.\n\n### Evidence\n\n- Failing first: `cargo test -p b10x-substrate-host --locked\n  egress::tests::helper_handback_preserves_record_boundaries -- --exact --nocapture` ran one test and\n  failed with `left: 1`, `right: 5` before the production pair changed.\n- Focused final: `cargo fmt --all --check`, `cargo clippy -p b10x-substrate-host --all-targets\n  --locked -- -D warnings`, and `cargo test -p b10x-substrate-host --locked --lib egress::tests:: --\n  --test-threads=1 --nocapture` passed; the module ran 17 tests.\n- Required delegated module sample: 20 serial executions inside one `Delegate=yes` systemd scope,\n  after moving the runner into a child cgroup and enabling `cpu`, `memory` and `pids`, passed\n  **20/20 with 0 failures**. Earlier intermediate samples that failed were discarded rather than\n  counted green.\n- Integrated delegated lane: `bash scripts/delegated-lane.sh` passed 66 host unit tests, all host\n  integration tests, and 7/7 clean-room daemon tests. The lane printed its delegated cgroup root and\n  controllers, so this was executed rather than absent.\n- Full repository gate: `bash scripts/gate.sh` passed, including release tests, fmt, workspace\n  clippy, full-history secrets, advisories, licences, package assembly, all twelve bundle fixed\n  points, 2,557 classified contract JSON documents, and toolchain pins.\n\nThis closes the accepted outcome in code and evidence. The story remains `active` until the operator\nchooses the lifecycle move.\n"},"args":{"changes":{"body":"# Story: An aperture install failure names its stage and loses its errno\n\n## Outcome\n\nWhen `exec.aperture-install-failed` fires, the operator learns *why* and not only *where*, and the\nintermittent stage-6 failure in the delegated lane is either fixed or explained.\n\n## What was observed\n\n**Reported by the pty implementor on 2026-08-30, not reproduced by the coordinator.**\n`egress::tests::declared_aperture_is_reachable` (`crates/substrate-host/src/egress.rs:1537`) fails\nintermittently inside a delegated systemd scope:\n\n```\ninstall the aperture: DriverError { class: Failed, code: \"exec.aperture-install-failed\",\nmessage: \"The egress aperture could not be installed exactly as declared (stage 6).\" }\n```\n\nIts numbers, as given: **1 failure in 10** serial delegated runs; 0 in 3 isolated runs of `egress::`\nalone; 0 in the portable `cargo test --workspace`. None of these were re-run independently.\n\nIt became visible only because `scripts/delegated-lane.sh` stopped selecting host cases by the\nsubstring `pty` — that filter had been running 8 of 58 host lib cases, so this test had never\nexecuted in a delegated scope. The fix is on `wt/pty-sessions`.\n\n## Blast radius, verified\n\n`scripts/gate.sh` contains no reference to the delegated lane or to\n`SUBSTRATE_VECTORS_CGROUP_ROOT` (`grep`, 0 hits). **The whole gate is unaffected.** What is affected\nis `bash scripts/delegated-lane.sh`, which is run by hand and is the only thing that executes the\ndelegated cases at all — so a lane that is red one run in ten is a lane whose red gets discounted.\n\n## What the code says, verified\n\n- **Stage 6 is `bind`.** The sandbox helper returns a stage number as its exit status; `return 6`\n  follows `libc::bind(listener, …)` on `loopback_sockaddr(port)`, inside the sandbox's network\n  namespace after `setns(CLONE_NEWUSER)` and `setns(CLONE_NEWNET)`.\n- **`SO_REUSEADDR` is already set on that socket**, immediately before the bind. So the first\n  hypothesis anybody reaches for — a previous run's socket in `TIME_WAIT` — is ruled out, and the\n  next person should not spend the afternoon on it.\n- **The errno cannot escape, and `install_failed` is not the reason.** The helper's only channel to\n  its parent is `_exit(stage)` (`egress.rs:456-464` turns that exit status into the message), and an\n  exit code carries a stage, not an errno. The helper is a fork precisely because\n  `setns` with `CLONE_NEWUSER` refuses a threaded caller, so it cannot allocate or return a\n  `DriverError`. The `install_failed` helper at `egress.rs:1092` *does* interpolate `{error}`; it is\n  simply not on this path.\n- The design states the trade deliberately: \"The return value is the stage that failed, so an\n  install failure names *where* rather than leaving an operator to guess between a namespace, a bind\n  and a handback.\"\n\n**Not established:** where `port` is chosen, whether two runs in one delegated scope can choose the\nsame one, and what the bind's actual errno is. Everything about the cause is open.\n\n## Acceptance\n\nAn aperture install failure carries the failing stage **and** the errno, and the stage-6 failure in\nthe delegated lane is either eliminated or has a named cause recorded against this story.\n\nEvidence that satisfies it, in order:\n\n1. The helper's stage channel widens to carry an errno — the exit status is one byte and already\n   spent, so this needs a second channel (the existing handback socket, a pipe, or an `AtomicI32` in\n   the shared mapping the forwarder already maps). Written failing-first against a forced bind\n   failure.\n2. The message names both, and a test asserts the errno reaches the caller.\n3. `bash scripts/delegated-lane.sh` run at least 20 times serially, with the failure count recorded\n   here — before and after. One failure in ten is a claim a 10-run sample cannot separate from two\n   in ten.\n4. If the cause is a port collision, the port's selection is the fix and the errno is the evidence\n   that found it; if it is not, this story records what it was.\n\n## Provenance\n\nFound while running `story:pty-sessions` in the wave of 2026-08-30, in the lane that story repaired.\nNot caused by it: the test and the helper both predate the wave. The coordinator verified the four\ncode claims above against `wave/2026-08-30-byte-plane`; the failure rate and the failing run are the\nimplementor's, quoted.\n\n## Correction — 2026-08-30, the flake is wider than this story says\n\nThe observation above names **one** test at **1 failure in 10**. A second reporter — the adversary\non `story:pty-sessions`' fifth pass — puts it at **module-wide, roughly 1 run in 2**, with two\nfurther tests failing that this story does not name:\n\n| test | `file:line` | observed |\n|---|---|---|\n| `declared_aperture_is_reachable` | `crates/substrate-host/src/egress.rs:1537` | the original report |\n| `applied_aperture_is_observed` | `crates/substrate-host/src/egress.rs:1657` | `Unreachable` where `Served` was expected |\n| `a_declared_ceiling_stops_the_relay` | `crates/substrate-host/src/egress.rs` | named, no assertion text captured |\n\nBoth rates are **reported, not reproduced by the coordinator**, and they disagree by a factor of\nfive. Two reporters, two runs of different lengths, on one machine — the honest reading is that the\nrate is unmeasured, not that it is 1 in 2 rather than 1 in 10. Acceptance item 3 already asks for a\n20-run sample; that sample now has to cover the **module**, not the one test.\n\nWhat this changes about the story: the stage-6 `bind` in `declared_aperture_is_reachable` is no\nlonger necessarily the whole subject. `applied_aperture_is_observed` failing with `Unreachable` is a\ndifferent symptom — a relay that installed and did not carry — so a single cause is a hypothesis and\nnot a finding. The errno the helper cannot report (`_exit(stage)`) is still the first thing to fix\neither way: it is what would tell these two symptoms apart.\n\n**Still verified, unchanged:** `scripts/gate.sh` has no delegated-lane reference, so the whole gate\nis unaffected; stage 6 is `libc::bind` with `SO_REUSEADDR` already set; the errno is lost at the\n`_exit(stage)` boundary and not in `install_failed`.\n\n## Implementation evidence — 2026-09-01\n\nThe inherited implementation had begun sending `[stage, errno]` over the descriptor handback, but\nthe production pair was still `SOCK_STREAM`, the receiver assumed one read returned the complete\nrecord, and coverage injected a payload rather than forcing the helper to fail. The failing-first\ntest `helper_handback_preserves_record_boundaries` observed `SO_TYPE=1` (`SOCK_STREAM`) where a\nrecord-preserving channel was required.\n\nThe completed path uses `SOCK_SEQPACKET | SOCK_CLOEXEC`; failure records and `SCM_RIGHTS` success\nhandbacks are distinct packets. Sends suppress `SIGPIPE`, receive and reap retry `EINTR`, truncation\nand unexpected control messages fail closed, and the exit status remains the fallback when no\nrecord arrives. `a_forced_bind_failure_names_its_stage_and_errno` opens one listener inside a held\nsandbox, forces a second bind on the same tuple, and asserts the caller receives class `failed`,\ncode `exec.aperture-install-failed`, stage 6, numeric `EADDRINUSE`, and its decoded OS error.\n\n### Named cause and fix\n\nThe first local `bash scripts/delegated-lane.sh` reproduction failed in\n`an_aperture_without_a_ceiling_passes_the_same_traffic` at the in-namespace connect. The widened\nhelper channel then caught the original bind symptom as stage 6, errno 99\n(`EADDRNOTAVAIL`, `Cannot assign requested address`). Together with the reported\n`Reach::Unreachable` (`ENETUNREACH`), this identifies one readiness race: bubblewrap publishes the\nchild pid while its concurrent child has not necessarily raised `lo` yet. The helper now reads\n`IFF_UP` and retries only `EADDRNOTAVAIL`, using one bounded one-second budget. It does not configure\nthe interface or widen the loopback-only network floor; every other bind errno is immediate.\n\nThe module harness also detached every `AppServer` and `Firehose` thread: each `Drop` discarded its\n`JoinHandle` while the thread retained its listener and blocked in `accept`. The affected test was\n0/100 failures in isolation but failed once in a 20-run module sample with the prior server threads\npresent. Both fixtures now stop, wake and join their thread, so the module sample does not accumulate\nlisteners or scheduling pressure.\n\n### Evidence\n\n- Failing first: `cargo test -p b10x-substrate-host --locked\n  egress::tests::helper_handback_preserves_record_boundaries -- --exact --nocapture` ran one test and\n  failed with `left: 1`, `right: 5` before the production pair changed.\n- Focused final: `cargo fmt --all --check`, `cargo clippy -p b10x-substrate-host --all-targets\n  --locked -- -D warnings`, and `cargo test -p b10x-substrate-host --locked --lib egress::tests:: --\n  --test-threads=1 --nocapture` passed; the module ran 17 tests.\n- Required delegated module sample: 20 serial executions inside one `Delegate=yes` systemd scope,\n  after moving the runner into a child cgroup and enabling `cpu`, `memory` and `pids`, passed\n  **20/20 with 0 failures**. Earlier intermediate samples that failed were discarded rather than\n  counted green.\n- Integrated delegated lane: `bash scripts/delegated-lane.sh` passed 66 host unit tests, all host\n  integration tests, and 7/7 clean-room daemon tests. The lane printed its delegated cgroup root and\n  controllers, so this was executed rather than absent.\n- Full repository gate: `bash scripts/gate.sh` passed, including release tests, fmt, workspace\n  clippy, full-history secrets, advisories, licences, package assembly, all twelve bundle fixed\n  points, 2,557 classified contract JSON documents, and toolchain pins.\n\nThis closes the accepted outcome in code and evidence. The story remains `active` until the operator\nchooses the lifecycle move.\n"},"command":"update-entity","target":"01MEM0000000000000037"},"payload":{"actor":"human:timo","at":1788220844529,"causation":"cmd-protocol-artifact-body-story-aperture-install-failure-loses-its-errno-1788220844529","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body","event_id":"story:aperture-install-failure-loses-its-errno@4#0~f1c4e52a437fca48","executor":null,"recorded_at":"2026-09-01T00:00:44Z"}}
{"entity":"story","version":1,"id":"contract-bundle-oci-artifact","revision":9,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"body":"# Story: The 0.4.0 contract bundle is a signed, digest-pinned OCI artifact\n\n## Outcome\n\nA consumer pins the wire contract by OCI digest instead of copying a directory tree.\n\n## Context\n\n`docs/plan/04-direct-byte-plane.md` § *Slice C* says Agent \"consumes an exact independently\nverified copy\"; the copy is made by hand. `docs/design/07-specification-and-conformance.md`\n§ *Bundle* already fixes the packaging: `bundle.json` lists media type, byte length and digest of\nevery bundle path except itself, and the outer OCI manifest digest pins `bundle.json`. Depends on\n`story:signed-daemon-image` for the release workflow it publishes through.\n\n## Acceptance\n\n`cargo xtask package-bundle <version>` produces a deterministic OCI layout from\n`contracts/substrate-wire/<version>/` without writing into `contracts/`, two runs yield\nbyte-identical manifests, and the release workflow publishes and signs it at\n`ghcr.io/beyond10x/b10x-substrate-wire:<bundle-version>` with the digest in `CHANGELOG.md`.\n\nEvidence that satisfies it:\n\n- `xtask/src/package.rs` tests: identical manifests on two runs; a one-byte change in\n  any bundle file changes the manifest digest — written failing-first against a stub that embeds\n  a timestamp;\n- `bundle.json` matches design 07's shape and a checker asserts each digest and byte length;\n- `check-contract-bundle-0.4.0.py` and every earlier checker stay green (invariant 6: the packager\n  reads the bundle; the bundle does not learn about the packager);\n- the artifact is annotated `development`; publication does not make the bundle stable (atlas ADR\n  0019 governs contract release);\n- `bash scripts/gate.sh` exits 0.\n\n## Progress — 2026-08-29 (packager half)\n\n- `cargo xtask package-bundle <version> --out <dir>`: OCI Image Layout (`oci-layout`,\n  `index.json`, `blobs/sha256/…`); config **is** `bundle.json` verbatim, media type\n  `application/vnd.b10x.substrate-wire.bundle.v1+json`, so the manifest digest pins\n  `bundle.json` (design 07 § *Bundle*); one layer per bundle file, layer digest = the `sha256`\n  already in `bundle.json`; annotations `org.opencontainers.image.version`,\n  `dev.b10x.contract.status=development`, `ref.name` on the index entry. Refuses `--out` inside\n  `contracts/`, non-empty `--out` without `--force`, symlinks, a file set disagreeing with\n  `bundle.json`.\n- 14 tests; determinism test written failing-first\n  against a timestamp-embedding stub (`FAILED (failures=1)`), then green. Two runs on `0.4.0` →\n  `sha256:f94d15fc116587d991aab6de5628f6ee5baf872af4e7c79d2a35e2b17a8485c4`, `diff -r` empty.\n  `oras manifest fetch --oci-layout` resolves the same digest; `oras pull` round-trip matches\n  `contracts/substrate-wire/0.4.0` byte for byte.\n- `check-contract-bundle-0.4.0.py` → exit 0; `git status contracts/` → clean (invariant 6).\n- Gated by `cargo test --workspace` (`scripts/gate.sh:15`), not by a gate line of its own.\n- The `posix-tar` archive `packaging.json` declares is now the last layer (media type\n  `application/vnd.b10x.substrate-wire.bundle.tar`, title `0.4.0.tar`): ustar, directory entries\n  0755 ahead of files 0644, uid/gid 0, empty names, bytewise order, every mtime =\n  `SOURCE_DATE_EPOCH` = author seconds of the last commit touching the bundle (`1787605082`,\n  commit `4b5d411`; `--source-date-epoch` overrides, absent both → exit 2). Per-file layers and the\n  config are byte-identical to before; the manifest digest moves by design to\n  `sha256:3758e80bc39f1eb03b15c69410608c9ef1d2ba8095c7e707c6988dbb5894ab00`\n  (archive `sha256:91fb5524…`, 880,640 bytes). 21 tests; `tar -xf` of the blob reproduces\n  `contracts/substrate-wire/0.4.0` byte for byte. On a shallow CI checkout the epoch would be the\n  tip's author time — pin `--source-date-epoch` there.\n- **Open:** the publish/sign half — `release.yml`, `ghcr.io/beyond10x/b10x-substrate-wire`,\n  cosign, digest in `CHANGELOG.md` — waits on `story:signed-daemon-image`.\n\n## Correction — 2026-08-30\n\nThe two Python names this story was written against no longer exist. `ef22de0` (\"the bundle\npackager is cargo xtask package-bundle, and its Python is gone\") ported the packager and deleted\n`scripts/package-contract-bundle.py` and `scripts/test_package_contract_bundle.py`. Acceptance and\nthe progress notes above now name the Rust verb; the behaviour they assert is unchanged, and the\n`0.4.0` manifest digests recorded above were observed against the Python and have not been\nre-observed against the verb.\n\nCoverage is not lost in the move: `xtask/src/package.rs` carries 21 `#[test]`s (was 21 Python\ntests), and `cargo test --workspace` at `scripts/gate.sh:15` runs them. There is no\n`package-bundle` line in `scripts/gate.sh` and none is needed.\n\n**Still open, unchanged:** the publish/sign half — `release.yml`,\n`ghcr.io/beyond10x/b10x-substrate-wire`, cosign, digest in `CHANGELOG.md`. It waits on\n`story:signed-daemon-image`, which is a draft. This story cannot reach `implemented` until that\none is designed and built.\n\n## Unblocked — 2026-08-30\n\nThe paragraph above says this waits on `story:signed-daemon-image`, \"which is a draft\". It is\n`implemented` (closed at `9131c95`). `.github/workflows/release.yml` exists, has run, and published\n`ghcr.io/beyond10x/b10x-substrate-daemon:0.2.3` at\n`sha256:ab10158266b579d705ce8422c7d2a6e783cde950d30e100f61ca6befc4d0beda`, keyless-signed and\n`cosign verify`-ed (run 33304493276).\n\n**Nothing blocks this story.** What remains is its own second half: a bundle publish-and-sign job in\n`release.yml`, publishing `ghcr.io/beyond10x/b10x-substrate-wire:<bundle-version>` from\n`cargo xtask package-bundle`, with the digest in `CHANGELOG.md` and the artifact annotated\n`development` — a development bundle does not become a stable contract by being published (atlas\nADR 0019).\n\nOne thing the daemon release proved that this story must plan around: the workflow's final step\ncannot push to `main`, because `main` is protected and requires the `Full gate` check, which a\ndirect push can never satisfy. The daemon digest landed by pull request instead\n(`68d226f`). A bundle digest will need the same route or a different one, and the story should say\nwhich before the job is written.\n\n## Implementation — 2026-09-01 (publish/sign half)\n\n- `.github/workflows/release.yml` explicitly pins current development bundle `0.12.0`; a successor\n  must move that reviewable pin rather than being selected by directory order at release time.\n- One globally serialized release job refuses an existing GitHub release, daemon image tag or\n  contract-bundle tag before publication. The bundle absence check fails closed on authentication,\n  transport and registry errors and repeats immediately before upload; a canonical tag is never an\n  overwrite target.\n- The job runs `cargo xtask package-bundle`, checks the manifest digest and\n  `dev.b10x.contract.status=development` annotation locally, and uses pinned ORAS 1.3.3 to copy the\n  exact OCI Image Layout to `ghcr.io/beyond10x/b10x-substrate-wire:<bundle-version>`. It then\n  resolves the remote tag back to the packaged digest and checks the remote development annotation.\n- Cosign signs the bundle by digest with the tag-triggered workflow's OIDC identity and verifies the\n  exact certificate identity and GitHub Actions issuer before `gh release create`. Release notes\n  carry both immutable digests, both verification commands and the explicit non-stability statement.\n- Protected `main` is not pushed or bypassed. After both signatures verify, the workflow emits the\n  exact daemon and bundle digest lines for a workstation's `b10x-bot` pull request, which receives\n  the required `Full gate` check.\n- `xtask/tests/release_workflow.rs` fails closed offline on the current-bundle pin, exact-layout copy,\n  two write-once checks, digest signing/verification order, development wording, protected-main\n  route and commit-pinned actions. A local ORAS layout-to-layout round trip reproduced manifest\n  `sha256:dd901e848c821aca7d55f7b8cf5ee893e1d99a1428b348e32e7ed1045a375319`; `contracts/` stayed\n  clean.\n- **Live evidence remains intentionally absent:** this implementation task forbids publishing or\n  cutting a tag. `ghcr.io/beyond10x/b10x-substrate-wire:0.12.0` was still not found on 2026-09-01.\n  The story stays `active` until an eligible release observes the remote digest and verified\n  signature and a fully gated bot pull request records that observed digest in `CHANGELOG.md`.\n"},"args":{"changes":{"body":"# Story: The 0.4.0 contract bundle is a signed, digest-pinned OCI artifact\n\n## Outcome\n\nA consumer pins the wire contract by OCI digest instead of copying a directory tree.\n\n## Context\n\n`docs/plan/04-direct-byte-plane.md` § *Slice C* says Agent \"consumes an exact independently\nverified copy\"; the copy is made by hand. `docs/design/07-specification-and-conformance.md`\n§ *Bundle* already fixes the packaging: `bundle.json` lists media type, byte length and digest of\nevery bundle path except itself, and the outer OCI manifest digest pins `bundle.json`. Depends on\n`story:signed-daemon-image` for the release workflow it publishes through.\n\n## Acceptance\n\n`cargo xtask package-bundle <version>` produces a deterministic OCI layout from\n`contracts/substrate-wire/<version>/` without writing into `contracts/`, two runs yield\nbyte-identical manifests, and the release workflow publishes and signs it at\n`ghcr.io/beyond10x/b10x-substrate-wire:<bundle-version>` with the digest in `CHANGELOG.md`.\n\nEvidence that satisfies it:\n\n- `xtask/src/package.rs` tests: identical manifests on two runs; a one-byte change in\n  any bundle file changes the manifest digest — written failing-first against a stub that embeds\n  a timestamp;\n- `bundle.json` matches design 07's shape and a checker asserts each digest and byte length;\n- `check-contract-bundle-0.4.0.py` and every earlier checker stay green (invariant 6: the packager\n  reads the bundle; the bundle does not learn about the packager);\n- the artifact is annotated `development`; publication does not make the bundle stable (atlas ADR\n  0019 governs contract release);\n- `bash scripts/gate.sh` exits 0.\n\n## Progress — 2026-08-29 (packager half)\n\n- `cargo xtask package-bundle <version> --out <dir>`: OCI Image Layout (`oci-layout`,\n  `index.json`, `blobs/sha256/…`); config **is** `bundle.json` verbatim, media type\n  `application/vnd.b10x.substrate-wire.bundle.v1+json`, so the manifest digest pins\n  `bundle.json` (design 07 § *Bundle*); one layer per bundle file, layer digest = the `sha256`\n  already in `bundle.json`; annotations `org.opencontainers.image.version`,\n  `dev.b10x.contract.status=development`, `ref.name` on the index entry. Refuses `--out` inside\n  `contracts/`, non-empty `--out` without `--force`, symlinks, a file set disagreeing with\n  `bundle.json`.\n- 14 tests; determinism test written failing-first\n  against a timestamp-embedding stub (`FAILED (failures=1)`), then green. Two runs on `0.4.0` →\n  `sha256:f94d15fc116587d991aab6de5628f6ee5baf872af4e7c79d2a35e2b17a8485c4`, `diff -r` empty.\n  `oras manifest fetch --oci-layout` resolves the same digest; `oras pull` round-trip matches\n  `contracts/substrate-wire/0.4.0` byte for byte.\n- `check-contract-bundle-0.4.0.py` → exit 0; `git status contracts/` → clean (invariant 6).\n- Gated by `cargo test --workspace` (`scripts/gate.sh:15`), not by a gate line of its own.\n- The `posix-tar` archive `packaging.json` declares is now the last layer (media type\n  `application/vnd.b10x.substrate-wire.bundle.tar`, title `0.4.0.tar`): ustar, directory entries\n  0755 ahead of files 0644, uid/gid 0, empty names, bytewise order, every mtime =\n  `SOURCE_DATE_EPOCH` = author seconds of the last commit touching the bundle (`1787605082`,\n  commit `4b5d411`; `--source-date-epoch` overrides, absent both → exit 2). Per-file layers and the\n  config are byte-identical to before; the manifest digest moves by design to\n  `sha256:3758e80bc39f1eb03b15c69410608c9ef1d2ba8095c7e707c6988dbb5894ab00`\n  (archive `sha256:91fb5524…`, 880,640 bytes). 21 tests; `tar -xf` of the blob reproduces\n  `contracts/substrate-wire/0.4.0` byte for byte. On a shallow CI checkout the epoch would be the\n  tip's author time — pin `--source-date-epoch` there.\n- **Open:** the publish/sign half — `release.yml`, `ghcr.io/beyond10x/b10x-substrate-wire`,\n  cosign, digest in `CHANGELOG.md` — waits on `story:signed-daemon-image`.\n\n## Correction — 2026-08-30\n\nThe two Python names this story was written against no longer exist. `ef22de0` (\"the bundle\npackager is cargo xtask package-bundle, and its Python is gone\") ported the packager and deleted\n`scripts/package-contract-bundle.py` and `scripts/test_package_contract_bundle.py`. Acceptance and\nthe progress notes above now name the Rust verb; the behaviour they assert is unchanged, and the\n`0.4.0` manifest digests recorded above were observed against the Python and have not been\nre-observed against the verb.\n\nCoverage is not lost in the move: `xtask/src/package.rs` carries 21 `#[test]`s (was 21 Python\ntests), and `cargo test --workspace` at `scripts/gate.sh:15` runs them. There is no\n`package-bundle` line in `scripts/gate.sh` and none is needed.\n\n**Still open, unchanged:** the publish/sign half — `release.yml`,\n`ghcr.io/beyond10x/b10x-substrate-wire`, cosign, digest in `CHANGELOG.md`. It waits on\n`story:signed-daemon-image`, which is a draft. This story cannot reach `implemented` until that\none is designed and built.\n\n## Unblocked — 2026-08-30\n\nThe paragraph above says this waits on `story:signed-daemon-image`, \"which is a draft\". It is\n`implemented` (closed at `9131c95`). `.github/workflows/release.yml` exists, has run, and published\n`ghcr.io/beyond10x/b10x-substrate-daemon:0.2.3` at\n`sha256:ab10158266b579d705ce8422c7d2a6e783cde950d30e100f61ca6befc4d0beda`, keyless-signed and\n`cosign verify`-ed (run 33304493276).\n\n**Nothing blocks this story.** What remains is its own second half: a bundle publish-and-sign job in\n`release.yml`, publishing `ghcr.io/beyond10x/b10x-substrate-wire:<bundle-version>` from\n`cargo xtask package-bundle`, with the digest in `CHANGELOG.md` and the artifact annotated\n`development` — a development bundle does not become a stable contract by being published (atlas\nADR 0019).\n\nOne thing the daemon release proved that this story must plan around: the workflow's final step\ncannot push to `main`, because `main` is protected and requires the `Full gate` check, which a\ndirect push can never satisfy. The daemon digest landed by pull request instead\n(`68d226f`). A bundle digest will need the same route or a different one, and the story should say\nwhich before the job is written.\n\n## Implementation — 2026-09-01 (publish/sign half)\n\n- `.github/workflows/release.yml` explicitly pins current development bundle `0.12.0`; a successor\n  must move that reviewable pin rather than being selected by directory order at release time.\n- One globally serialized release job refuses an existing GitHub release, daemon image tag or\n  contract-bundle tag before publication. The bundle absence check fails closed on authentication,\n  transport and registry errors and repeats immediately before upload; a canonical tag is never an\n  overwrite target.\n- The job runs `cargo xtask package-bundle`, checks the manifest digest and\n  `dev.b10x.contract.status=development` annotation locally, and uses pinned ORAS 1.3.3 to copy the\n  exact OCI Image Layout to `ghcr.io/beyond10x/b10x-substrate-wire:<bundle-version>`. It then\n  resolves the remote tag back to the packaged digest and checks the remote development annotation.\n- Cosign signs the bundle by digest with the tag-triggered workflow's OIDC identity and verifies the\n  exact certificate identity and GitHub Actions issuer before `gh release create`. Release notes\n  carry both immutable digests, both verification commands and the explicit non-stability statement.\n- Protected `main` is not pushed or bypassed. After both signatures verify, the workflow emits the\n  exact daemon and bundle digest lines for a workstation's `b10x-bot` pull request, which receives\n  the required `Full gate` check.\n- `xtask/tests/release_workflow.rs` fails closed offline on the current-bundle pin, exact-layout copy,\n  two write-once checks, digest signing/verification order, development wording, protected-main\n  route and commit-pinned actions. A local ORAS layout-to-layout round trip reproduced manifest\n  `sha256:dd901e848c821aca7d55f7b8cf5ee893e1d99a1428b348e32e7ed1045a375319`; `contracts/` stayed\n  clean.\n- **Live evidence remains intentionally absent:** this implementation task forbids publishing or\n  cutting a tag. `ghcr.io/beyond10x/b10x-substrate-wire:0.12.0` was still not found on 2026-09-01.\n  The story stays `active` until an eligible release observes the remote digest and verified\n  signature and a fully gated bot pull request records that observed digest in `CHANGELOG.md`.\n"},"command":"update-entity","target":"01MEM0000000000000046"},"payload":{"actor":"human:timo","at":1788220886406,"causation":"cmd-protocol-artifact-body-story-contract-bundle-oci-artifact-1788220886406","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body","event_id":"story:contract-bundle-oci-artifact@9#0~22d3eed95bea5f6c","executor":null,"recorded_at":"2026-09-01T00:01:26Z"}}
{"entity":"story","version":1,"id":"contract-bundle-oci-artifact","revision":9,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"scripts/gate.sh","source":"bash scripts/gate.sh — dedicated contract-bundle worktree, exit 0, gate: passed","target":"01MEM0000000000000046"},"payload":{"actor":"human:timo","at":1788220886000,"causation":"cmd-evidence-story-contract-bundle-oci-artifact-test_result-1788220886538","change":{"change":"evidence","kind":"test_result","reference":"scripts/gate.sh","source":"bash scripts/gate.sh — dedicated contract-bundle worktree, exit 0, gate: passed"},"correlation":"protocol-artifact-evidence","event_id":"story:contract-bundle-oci-artifact@9#0~3332373040bd579c","executor":null,"recorded_at":"2026-09-01T00:01:26Z"}}
{"entity":"story","version":1,"id":"contract-bundle-oci-artifact","revision":10,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"body":"# Story: The 0.4.0 contract bundle is a signed, digest-pinned OCI artifact\n\n## Outcome\n\nA consumer pins the wire contract by OCI digest instead of copying a directory tree.\n\n## Context\n\n`docs/plan/04-direct-byte-plane.md` § *Slice C* says Agent \"consumes an exact independently\nverified copy\"; the copy is made by hand. `docs/design/07-specification-and-conformance.md`\n§ *Bundle* already fixes the packaging: `bundle.json` lists media type, byte length and digest of\nevery bundle path except itself, and the outer OCI manifest digest pins `bundle.json`. Depends on\n`story:signed-daemon-image` for the release workflow it publishes through.\n\n## Acceptance\n\n`cargo xtask package-bundle <version>` produces a deterministic OCI layout from\n`contracts/substrate-wire/<version>/` without writing into `contracts/`, two runs yield\nbyte-identical manifests, and the release workflow publishes and signs it at\n`ghcr.io/beyond10x/b10x-substrate-wire:<bundle-version>` with the digest in `CHANGELOG.md`.\n\nEvidence that satisfies it:\n\n- `xtask/src/package.rs` tests: identical manifests on two runs; a one-byte change in\n  any bundle file changes the manifest digest — written failing-first against a stub that embeds\n  a timestamp;\n- `bundle.json` matches design 07's shape and a checker asserts each digest and byte length;\n- `check-contract-bundle-0.4.0.py` and every earlier checker stay green (invariant 6: the packager\n  reads the bundle; the bundle does not learn about the packager);\n- the artifact is annotated `development`; publication does not make the bundle stable (atlas ADR\n  0019 governs contract release);\n- `bash scripts/gate.sh` exits 0.\n\n## Progress — 2026-08-29 (packager half)\n\n- `cargo xtask package-bundle <version> --out <dir>`: OCI Image Layout (`oci-layout`,\n  `index.json`, `blobs/sha256/…`); config **is** `bundle.json` verbatim, media type\n  `application/vnd.b10x.substrate-wire.bundle.v1+json`, so the manifest digest pins\n  `bundle.json` (design 07 § *Bundle*); one layer per bundle file, layer digest = the `sha256`\n  already in `bundle.json`; annotations `org.opencontainers.image.version`,\n  `dev.b10x.contract.status=development`, `ref.name` on the index entry. Refuses `--out` inside\n  `contracts/`, non-empty `--out` without `--force`, symlinks, a file set disagreeing with\n  `bundle.json`.\n- 14 tests; determinism test written failing-first\n  against a timestamp-embedding stub (`FAILED (failures=1)`), then green. Two runs on `0.4.0` →\n  `sha256:f94d15fc116587d991aab6de5628f6ee5baf872af4e7c79d2a35e2b17a8485c4`, `diff -r` empty.\n  `oras manifest fetch --oci-layout` resolves the same digest; `oras pull` round-trip matches\n  `contracts/substrate-wire/0.4.0` byte for byte.\n- `check-contract-bundle-0.4.0.py` → exit 0; `git status contracts/` → clean (invariant 6).\n- Gated by `cargo test --workspace` (`scripts/gate.sh:15`), not by a gate line of its own.\n- The `posix-tar` archive `packaging.json` declares is now the last layer (media type\n  `application/vnd.b10x.substrate-wire.bundle.tar`, title `0.4.0.tar`): ustar, directory entries\n  0755 ahead of files 0644, uid/gid 0, empty names, bytewise order, every mtime =\n  `SOURCE_DATE_EPOCH` = author seconds of the last commit touching the bundle (`1787605082`,\n  commit `4b5d411`; `--source-date-epoch` overrides, absent both → exit 2). Per-file layers and the\n  config are byte-identical to before; the manifest digest moves by design to\n  `sha256:3758e80bc39f1eb03b15c69410608c9ef1d2ba8095c7e707c6988dbb5894ab00`\n  (archive `sha256:91fb5524…`, 880,640 bytes). 21 tests; `tar -xf` of the blob reproduces\n  `contracts/substrate-wire/0.4.0` byte for byte. On a shallow CI checkout the epoch would be the\n  tip's author time — pin `--source-date-epoch` there.\n- **Open:** the publish/sign half — `release.yml`, `ghcr.io/beyond10x/b10x-substrate-wire`,\n  cosign, digest in `CHANGELOG.md` — waits on `story:signed-daemon-image`.\n\n## Correction — 2026-08-30\n\nThe two Python names this story was written against no longer exist. `ef22de0` (\"the bundle\npackager is cargo xtask package-bundle, and its Python is gone\") ported the packager and deleted\n`scripts/package-contract-bundle.py` and `scripts/test_package_contract_bundle.py`. Acceptance and\nthe progress notes above now name the Rust verb; the behaviour they assert is unchanged, and the\n`0.4.0` manifest digests recorded above were observed against the Python and have not been\nre-observed against the verb.\n\nCoverage is not lost in the move: `xtask/src/package.rs` carries 21 `#[test]`s (was 21 Python\ntests), and `cargo test --workspace` at `scripts/gate.sh:15` runs them. There is no\n`package-bundle` line in `scripts/gate.sh` and none is needed.\n\n**Still open, unchanged:** the publish/sign half — `release.yml`,\n`ghcr.io/beyond10x/b10x-substrate-wire`, cosign, digest in `CHANGELOG.md`. It waits on\n`story:signed-daemon-image`, which is a draft. This story cannot reach `implemented` until that\none is designed and built.\n\n## Unblocked — 2026-08-30\n\nThe paragraph above says this waits on `story:signed-daemon-image`, \"which is a draft\". It is\n`implemented` (closed at `9131c95`). `.github/workflows/release.yml` exists, has run, and published\n`ghcr.io/beyond10x/b10x-substrate-daemon:0.2.3` at\n`sha256:ab10158266b579d705ce8422c7d2a6e783cde950d30e100f61ca6befc4d0beda`, keyless-signed and\n`cosign verify`-ed (run 33304493276).\n\n**Nothing blocks this story.** What remains is its own second half: a bundle publish-and-sign job in\n`release.yml`, publishing `ghcr.io/beyond10x/b10x-substrate-wire:<bundle-version>` from\n`cargo xtask package-bundle`, with the digest in `CHANGELOG.md` and the artifact annotated\n`development` — a development bundle does not become a stable contract by being published (atlas\nADR 0019).\n\nOne thing the daemon release proved that this story must plan around: the workflow's final step\ncannot push to `main`, because `main` is protected and requires the `Full gate` check, which a\ndirect push can never satisfy. The daemon digest landed by pull request instead\n(`68d226f`). A bundle digest will need the same route or a different one, and the story should say\nwhich before the job is written.\n\n## Implementation — 2026-09-01 (publish/sign half)\n\n- `.github/workflows/release.yml` explicitly pins current development bundle `0.12.0`; a successor\n  must move that reviewable pin rather than being selected by directory order at release time.\n- One globally serialized release job refuses an existing GitHub release or daemon image tag. A\n  contract-bundle tag is reused only when it already resolves to the deterministic local digest; a\n  mismatched digest, authentication failure, transport failure or ambiguous registry answer refuses.\n  The canonical bundle tag is never an overwrite target.\n- The job runs `cargo xtask package-bundle`, checks the manifest digest and\n  `dev.b10x.contract.status=development` annotation locally, and uses pinned ORAS 1.3.3 to copy the\n  exact OCI Image Layout to `ghcr.io/beyond10x/b10x-substrate-wire:<bundle-version>` when absent.\n  It resolves the remote tag back to the packaged digest, checks the development annotation, and\n  proves anonymous retrieval before mutating the daemon-image tag; a visibility failure is retryable\n  without overwriting either artifact.\n- Cosign signs the bundle by digest with the tag-triggered workflow's OIDC identity and verifies the\n  exact certificate identity and GitHub Actions issuer before `gh release create`. Release notes\n  carry both immutable digests, both verification commands and the explicit non-stability statement.\n- Protected `main` is not pushed or bypassed. After both signatures verify, the workflow emits the\n  exact daemon and bundle digest lines for a workstation's `b10x-bot` pull request, which receives\n  the required `Full gate` check.\n- `xtask/tests/release_workflow.rs` fails closed offline on the current-bundle pin, exact-layout copy,\n  two write-once-or-identical-reuse checks, anonymous-access ordering, digest signing/verification order, development wording, protected-main\n  route and commit-pinned actions. A local ORAS layout-to-layout round trip reproduced manifest\n  `sha256:dd901e848c821aca7d55f7b8cf5ee893e1d99a1428b348e32e7ed1045a375319`; `contracts/` stayed\n  clean.\n- **Live evidence remains intentionally absent:** this implementation task forbids publishing or\n  cutting a tag. `ghcr.io/beyond10x/b10x-substrate-wire:0.12.0` was still not found on 2026-09-01.\n  The story stays `active` until an eligible release observes the remote digest and verified\n  signature and a fully gated bot pull request records that observed digest in `CHANGELOG.md`.\n"},"args":{"changes":{"body":"# Story: The 0.4.0 contract bundle is a signed, digest-pinned OCI artifact\n\n## Outcome\n\nA consumer pins the wire contract by OCI digest instead of copying a directory tree.\n\n## Context\n\n`docs/plan/04-direct-byte-plane.md` § *Slice C* says Agent \"consumes an exact independently\nverified copy\"; the copy is made by hand. `docs/design/07-specification-and-conformance.md`\n§ *Bundle* already fixes the packaging: `bundle.json` lists media type, byte length and digest of\nevery bundle path except itself, and the outer OCI manifest digest pins `bundle.json`. Depends on\n`story:signed-daemon-image` for the release workflow it publishes through.\n\n## Acceptance\n\n`cargo xtask package-bundle <version>` produces a deterministic OCI layout from\n`contracts/substrate-wire/<version>/` without writing into `contracts/`, two runs yield\nbyte-identical manifests, and the release workflow publishes and signs it at\n`ghcr.io/beyond10x/b10x-substrate-wire:<bundle-version>` with the digest in `CHANGELOG.md`.\n\nEvidence that satisfies it:\n\n- `xtask/src/package.rs` tests: identical manifests on two runs; a one-byte change in\n  any bundle file changes the manifest digest — written failing-first against a stub that embeds\n  a timestamp;\n- `bundle.json` matches design 07's shape and a checker asserts each digest and byte length;\n- `check-contract-bundle-0.4.0.py` and every earlier checker stay green (invariant 6: the packager\n  reads the bundle; the bundle does not learn about the packager);\n- the artifact is annotated `development`; publication does not make the bundle stable (atlas ADR\n  0019 governs contract release);\n- `bash scripts/gate.sh` exits 0.\n\n## Progress — 2026-08-29 (packager half)\n\n- `cargo xtask package-bundle <version> --out <dir>`: OCI Image Layout (`oci-layout`,\n  `index.json`, `blobs/sha256/…`); config **is** `bundle.json` verbatim, media type\n  `application/vnd.b10x.substrate-wire.bundle.v1+json`, so the manifest digest pins\n  `bundle.json` (design 07 § *Bundle*); one layer per bundle file, layer digest = the `sha256`\n  already in `bundle.json`; annotations `org.opencontainers.image.version`,\n  `dev.b10x.contract.status=development`, `ref.name` on the index entry. Refuses `--out` inside\n  `contracts/`, non-empty `--out` without `--force`, symlinks, a file set disagreeing with\n  `bundle.json`.\n- 14 tests; determinism test written failing-first\n  against a timestamp-embedding stub (`FAILED (failures=1)`), then green. Two runs on `0.4.0` →\n  `sha256:f94d15fc116587d991aab6de5628f6ee5baf872af4e7c79d2a35e2b17a8485c4`, `diff -r` empty.\n  `oras manifest fetch --oci-layout` resolves the same digest; `oras pull` round-trip matches\n  `contracts/substrate-wire/0.4.0` byte for byte.\n- `check-contract-bundle-0.4.0.py` → exit 0; `git status contracts/` → clean (invariant 6).\n- Gated by `cargo test --workspace` (`scripts/gate.sh:15`), not by a gate line of its own.\n- The `posix-tar` archive `packaging.json` declares is now the last layer (media type\n  `application/vnd.b10x.substrate-wire.bundle.tar`, title `0.4.0.tar`): ustar, directory entries\n  0755 ahead of files 0644, uid/gid 0, empty names, bytewise order, every mtime =\n  `SOURCE_DATE_EPOCH` = author seconds of the last commit touching the bundle (`1787605082`,\n  commit `4b5d411`; `--source-date-epoch` overrides, absent both → exit 2). Per-file layers and the\n  config are byte-identical to before; the manifest digest moves by design to\n  `sha256:3758e80bc39f1eb03b15c69410608c9ef1d2ba8095c7e707c6988dbb5894ab00`\n  (archive `sha256:91fb5524…`, 880,640 bytes). 21 tests; `tar -xf` of the blob reproduces\n  `contracts/substrate-wire/0.4.0` byte for byte. On a shallow CI checkout the epoch would be the\n  tip's author time — pin `--source-date-epoch` there.\n- **Open:** the publish/sign half — `release.yml`, `ghcr.io/beyond10x/b10x-substrate-wire`,\n  cosign, digest in `CHANGELOG.md` — waits on `story:signed-daemon-image`.\n\n## Correction — 2026-08-30\n\nThe two Python names this story was written against no longer exist. `ef22de0` (\"the bundle\npackager is cargo xtask package-bundle, and its Python is gone\") ported the packager and deleted\n`scripts/package-contract-bundle.py` and `scripts/test_package_contract_bundle.py`. Acceptance and\nthe progress notes above now name the Rust verb; the behaviour they assert is unchanged, and the\n`0.4.0` manifest digests recorded above were observed against the Python and have not been\nre-observed against the verb.\n\nCoverage is not lost in the move: `xtask/src/package.rs` carries 21 `#[test]`s (was 21 Python\ntests), and `cargo test --workspace` at `scripts/gate.sh:15` runs them. There is no\n`package-bundle` line in `scripts/gate.sh` and none is needed.\n\n**Still open, unchanged:** the publish/sign half — `release.yml`,\n`ghcr.io/beyond10x/b10x-substrate-wire`, cosign, digest in `CHANGELOG.md`. It waits on\n`story:signed-daemon-image`, which is a draft. This story cannot reach `implemented` until that\none is designed and built.\n\n## Unblocked — 2026-08-30\n\nThe paragraph above says this waits on `story:signed-daemon-image`, \"which is a draft\". It is\n`implemented` (closed at `9131c95`). `.github/workflows/release.yml` exists, has run, and published\n`ghcr.io/beyond10x/b10x-substrate-daemon:0.2.3` at\n`sha256:ab10158266b579d705ce8422c7d2a6e783cde950d30e100f61ca6befc4d0beda`, keyless-signed and\n`cosign verify`-ed (run 33304493276).\n\n**Nothing blocks this story.** What remains is its own second half: a bundle publish-and-sign job in\n`release.yml`, publishing `ghcr.io/beyond10x/b10x-substrate-wire:<bundle-version>` from\n`cargo xtask package-bundle`, with the digest in `CHANGELOG.md` and the artifact annotated\n`development` — a development bundle does not become a stable contract by being published (atlas\nADR 0019).\n\nOne thing the daemon release proved that this story must plan around: the workflow's final step\ncannot push to `main`, because `main` is protected and requires the `Full gate` check, which a\ndirect push can never satisfy. The daemon digest landed by pull request instead\n(`68d226f`). A bundle digest will need the same route or a different one, and the story should say\nwhich before the job is written.\n\n## Implementation — 2026-09-01 (publish/sign half)\n\n- `.github/workflows/release.yml` explicitly pins current development bundle `0.12.0`; a successor\n  must move that reviewable pin rather than being selected by directory order at release time.\n- One globally serialized release job refuses an existing GitHub release or daemon image tag. A\n  contract-bundle tag is reused only when it already resolves to the deterministic local digest; a\n  mismatched digest, authentication failure, transport failure or ambiguous registry answer refuses.\n  The canonical bundle tag is never an overwrite target.\n- The job runs `cargo xtask package-bundle`, checks the manifest digest and\n  `dev.b10x.contract.status=development` annotation locally, and uses pinned ORAS 1.3.3 to copy the\n  exact OCI Image Layout to `ghcr.io/beyond10x/b10x-substrate-wire:<bundle-version>` when absent.\n  It resolves the remote tag back to the packaged digest, checks the development annotation, and\n  proves anonymous retrieval before mutating the daemon-image tag; a visibility failure is retryable\n  without overwriting either artifact.\n- Cosign signs the bundle by digest with the tag-triggered workflow's OIDC identity and verifies the\n  exact certificate identity and GitHub Actions issuer before `gh release create`. Release notes\n  carry both immutable digests, both verification commands and the explicit non-stability statement.\n- Protected `main` is not pushed or bypassed. After both signatures verify, the workflow emits the\n  exact daemon and bundle digest lines for a workstation's `b10x-bot` pull request, which receives\n  the required `Full gate` check.\n- `xtask/tests/release_workflow.rs` fails closed offline on the current-bundle pin, exact-layout copy,\n  two write-once-or-identical-reuse checks, anonymous-access ordering, digest signing/verification order, development wording, protected-main\n  route and commit-pinned actions. A local ORAS layout-to-layout round trip reproduced manifest\n  `sha256:dd901e848c821aca7d55f7b8cf5ee893e1d99a1428b348e32e7ed1045a375319`; `contracts/` stayed\n  clean.\n- **Live evidence remains intentionally absent:** this implementation task forbids publishing or\n  cutting a tag. `ghcr.io/beyond10x/b10x-substrate-wire:0.12.0` was still not found on 2026-09-01.\n  The story stays `active` until an eligible release observes the remote digest and verified\n  signature and a fully gated bot pull request records that observed digest in `CHANGELOG.md`.\n"},"command":"update-entity","target":"01MEM0000000000000046"},"payload":{"actor":"human:timo","at":1788221105067,"causation":"cmd-protocol-artifact-body-story-contract-bundle-oci-artifact-1788221105067","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body","event_id":"story:contract-bundle-oci-artifact@10#0~2209ba69137a2e8f","executor":null,"recorded_at":"2026-09-01T00:05:05Z"}}
{"entity":"story","version":1,"id":"aperture-install-failure-loses-its-errno","revision":5,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"tags":["wave/remote-foundation-01"]},"args":{"changes":{"tags":["wave/remote-foundation-01"]},"command":"update-entity","target":"01MEM0000000000000037"},"payload":{"actor":"human:timo","at":1788223579415,"causation":"cmd-protocol-artifact-set-story-aperture-install-failure-loses-its-errno-1788223579415","change":{"change":"body_replaced"},"correlation":"protocol-artifact-set","event_id":"story:aperture-install-failure-loses-its-errno@5#0~c6be8f1aed0b4c10","executor":null,"recorded_at":"2026-09-01T00:46:19Z"}}
{"entity":"story","version":1,"id":"contract-bundle-oci-artifact","revision":11,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"tags":["contracts","release","wave/remote-foundation-01"]},"args":{"changes":{"tags":["contracts","release","wave/remote-foundation-01"]},"command":"update-entity","target":"01MEM0000000000000046"},"payload":{"actor":"human:timo","at":1788223579542,"causation":"cmd-protocol-artifact-set-story-contract-bundle-oci-artifact-1788223579542","change":{"change":"body_replaced"},"correlation":"protocol-artifact-set","event_id":"story:contract-bundle-oci-artifact@11#0~d43790f17785f092","executor":null,"recorded_at":"2026-09-01T00:46:19Z"}}
{"entity":"story","version":1,"id":"rust-sdk-publication","revision":5,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"tags":["release","sdk","wave/remote-foundation-01"]},"args":{"changes":{"tags":["release","sdk","wave/remote-foundation-01"]},"command":"update-entity","target":"01MEM0000000000000145"},"payload":{"actor":"human:timo","at":1788223579689,"causation":"cmd-protocol-artifact-set-story-rust-sdk-publication-1788223579689","change":{"change":"body_replaced"},"correlation":"protocol-artifact-set","event_id":"story:rust-sdk-publication@5#0~59dfc77516b96cc1","executor":null,"recorded_at":"2026-09-01T00:46:19Z"}}
{"entity":"story","version":1,"id":"promote-development-contract-frontier","revision":3,"type":"aep.entity.update/v1","from_state":"proposed","to_state":"proposed","changed":{"tags":["contract","release","remote","wave/remote-foundation-01"]},"args":{"changes":{"tags":["contract","release","remote","wave/remote-foundation-01"]},"command":"update-entity","target":"01MEM0000000000000115"},"payload":{"actor":"human:timo","at":1788223579943,"causation":"cmd-protocol-artifact-set-story-promote-development-contract-frontier-1788223579943","change":{"change":"body_replaced"},"correlation":"protocol-artifact-set","event_id":"story:promote-development-contract-frontier@3#0~7458037e39621152","executor":null,"recorded_at":"2026-09-01T00:46:19Z"}}
{"entity":"story","version":1,"id":"sdk-promoted-contract-parity","revision":3,"type":"aep.entity.update/v1","from_state":"proposed","to_state":"proposed","changed":{"tags":["contract","sdk","wave/remote-foundation-01"]},"args":{"changes":{"tags":["contract","sdk","wave/remote-foundation-01"]},"command":"update-entity","target":"01MEM0000000000000148"},"payload":{"actor":"human:timo","at":1788223580115,"causation":"cmd-protocol-artifact-set-story-sdk-promoted-contract-parity-1788223580115","change":{"change":"body_replaced"},"correlation":"protocol-artifact-set","event_id":"story:sdk-promoted-contract-parity@3#0~d7cf48ad22a72b6b","executor":null,"recorded_at":"2026-09-01T00:46:20Z"}}
{"entity":"story","version":1,"id":"mcp-managed-disposable-daemon","revision":3,"type":"aep.entity.update/v1","from_state":"proposed","to_state":"proposed","changed":{"tags":["deferred","mcp","testing","wave/remote-foundation-01"]},"args":{"changes":{"tags":["deferred","mcp","testing","wave/remote-foundation-01"]},"command":"update-entity","target":"01MEM0000000000000097"},"payload":{"actor":"human:timo","at":1788223580239,"causation":"cmd-protocol-artifact-set-story-mcp-managed-disposable-daemon-1788223580239","change":{"change":"body_replaced"},"correlation":"protocol-artifact-set","event_id":"story:mcp-managed-disposable-daemon@3#0~1f8acb65ff2022af","executor":null,"recorded_at":"2026-09-01T00:46:20Z"}}
{"entity":"story","version":1,"id":"production-tls-control-listener","revision":3,"type":"aep.entity.update/v1","from_state":"proposed","to_state":"proposed","changed":{"tags":["remote","security","tls","wave/remote-foundation-01"]},"args":{"changes":{"tags":["remote","security","tls","wave/remote-foundation-01"]},"command":"update-entity","target":"01MEM0000000000000112"},"payload":{"actor":"human:timo","at":1788223580362,"causation":"cmd-protocol-artifact-set-story-production-tls-control-listener-1788223580362","change":{"change":"body_replaced"},"correlation":"protocol-artifact-set","event_id":"story:production-tls-control-listener@3#0~439e354ad4c4d92d","executor":null,"recorded_at":"2026-09-01T00:46:20Z"}}
{"entity":"story","version":1,"id":"hosted-trust-envelope-admission","revision":3,"type":"aep.entity.update/v1","from_state":"proposed","to_state":"proposed","changed":{"tags":["identity","remote","security","wave/remote-foundation-01"]},"args":{"changes":{"tags":["identity","remote","security","wave/remote-foundation-01"]},"command":"update-entity","target":"01MEM0000000000000085"},"payload":{"actor":"human:timo","at":1788223580485,"causation":"cmd-protocol-artifact-set-story-hosted-trust-envelope-admission-1788223580485","change":{"change":"body_replaced"},"correlation":"protocol-artifact-set","event_id":"story:hosted-trust-envelope-admission@3#0~4bce7bd4425b0dc3","executor":null,"recorded_at":"2026-09-01T00:46:20Z"}}
{"entity":"story","version":1,"id":"network-session-authority","revision":7,"type":"aep.entity.update/v1","from_state":"proposed","to_state":"proposed","changed":{"tags":["daemon","wave/remote-foundation-01","wire"]},"args":{"changes":{"tags":["daemon","wave/remote-foundation-01","wire"]},"command":"update-entity","target":"01MEM0000000000000100"},"payload":{"actor":"human:timo","at":1788223580608,"causation":"cmd-protocol-artifact-set-story-network-session-authority-1788223580608","change":{"change":"body_replaced"},"correlation":"protocol-artifact-set","event_id":"story:network-session-authority@7#0~8d5102cfcfedafea","executor":null,"recorded_at":"2026-09-01T00:46:20Z"}}
{"entity":"story","version":1,"id":"remote-sdk-transport","revision":3,"type":"aep.entity.update/v1","from_state":"proposed","to_state":"proposed","changed":{"tags":["remote","sdk","tls","wave/remote-foundation-01"]},"args":{"changes":{"tags":["remote","sdk","tls","wave/remote-foundation-01"]},"command":"update-entity","target":"01MEM0000000000000130"},"payload":{"actor":"human:timo","at":1788223580731,"causation":"cmd-protocol-artifact-set-story-remote-sdk-transport-1788223580731","change":{"change":"body_replaced"},"correlation":"protocol-artifact-set","event_id":"story:remote-sdk-transport@3#0~54dcec94c2be732f","executor":null,"recorded_at":"2026-09-01T00:46:20Z"}}
{"entity":"story","version":1,"id":"aperture-install-failure-loses-its-errno","revision":5,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"ec4701738e7604eb0b04cba6f33b23c58d3e510f","source":"20/20 delegated module sample, integrated delegated lane, and full repository gate","target":"01MEM0000000000000037"},"payload":{"actor":"human:timo","at":1788223593000,"causation":"cmd-evidence-story-aperture-install-failure-loses-its-errno-test_result-1788223593854","change":{"change":"evidence","kind":"test_result","reference":"ec4701738e7604eb0b04cba6f33b23c58d3e510f","source":"20/20 delegated module sample, integrated delegated lane, and full repository gate"},"correlation":"protocol-artifact-evidence","event_id":"story:aperture-install-failure-loses-its-errno@5#0~6f847a70a5d72de9","executor":null,"recorded_at":"2026-09-01T00:46:33Z"}}
{"entity":"story","version":1,"id":"aperture-install-failure-loses-its-errno","revision":6,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1}}","target":"01MEM0000000000000037","to":"implemented"},"payload":{"actor":"human:timo","at":1788223594066,"causation":"cmd-move-story-aperture-install-failure-loses-its-errno-1788223594066","change":{"change":"moved","decided_on":{"recorded":{"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:aperture-install-failure-loses-its-errno@6#0~1f6635a2846c83e7","executor":null,"recorded_at":"2026-09-01T00:46:34Z"}}
{"entity":"story","version":1,"id":"contract-bundle-oci-artifact","revision":12,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"body":"# Story: The 0.4.0 contract bundle is a signed, digest-pinned OCI artifact\n\n## Outcome\n\nA consumer pins the wire contract by OCI digest instead of copying a directory tree.\n\n## Context\n\n`docs/plan/04-direct-byte-plane.md` § *Slice C* says Agent \"consumes an exact independently\nverified copy\"; the copy is made by hand. `docs/design/07-specification-and-conformance.md`\n§ *Bundle* already fixes the packaging: `bundle.json` lists media type, byte length and digest of\nevery bundle path except itself, and the outer OCI manifest digest pins `bundle.json`. Depends on\n`story:signed-daemon-image` for the release workflow it publishes through.\n\n## Acceptance\n\n`cargo xtask package-bundle <version>` produces a deterministic OCI layout from\n`contracts/substrate-wire/<version>/` without writing into `contracts/`, two runs yield\nbyte-identical manifests, and the release workflow publishes and signs it at\n`ghcr.io/beyond10x/b10x-substrate-wire:<bundle-version>` with the digest in `CHANGELOG.md`.\n\nEvidence that satisfies it:\n\n- `xtask/src/package.rs` tests: identical manifests on two runs; a one-byte change in\n  any bundle file changes the manifest digest — written failing-first against a stub that embeds\n  a timestamp;\n- `bundle.json` matches design 07's shape and a checker asserts each digest and byte length;\n- `check-contract-bundle-0.4.0.py` and every earlier checker stay green (invariant 6: the packager\n  reads the bundle; the bundle does not learn about the packager);\n- the artifact is annotated `development`; publication does not make the bundle stable (atlas ADR\n  0019 governs contract release);\n- `bash scripts/gate.sh` exits 0.\n\n## Progress — 2026-08-29 (packager half)\n\n- `cargo xtask package-bundle <version> --out <dir>`: OCI Image Layout (`oci-layout`,\n  `index.json`, `blobs/sha256/…`); config **is** `bundle.json` verbatim, media type\n  `application/vnd.b10x.substrate-wire.bundle.v1+json`, so the manifest digest pins\n  `bundle.json` (design 07 § *Bundle*); one layer per bundle file, layer digest = the `sha256`\n  already in `bundle.json`; annotations `org.opencontainers.image.version`,\n  `dev.b10x.contract.status=development`, `ref.name` on the index entry. Refuses `--out` inside\n  `contracts/`, non-empty `--out` without `--force`, symlinks, a file set disagreeing with\n  `bundle.json`.\n- 14 tests; determinism test written failing-first\n  against a timestamp-embedding stub (`FAILED (failures=1)`), then green. Two runs on `0.4.0` →\n  `sha256:f94d15fc116587d991aab6de5628f6ee5baf872af4e7c79d2a35e2b17a8485c4`, `diff -r` empty.\n  `oras manifest fetch --oci-layout` resolves the same digest; `oras pull` round-trip matches\n  `contracts/substrate-wire/0.4.0` byte for byte.\n- `check-contract-bundle-0.4.0.py` → exit 0; `git status contracts/` → clean (invariant 6).\n- Gated by `cargo test --workspace` (`scripts/gate.sh:15`), not by a gate line of its own.\n- The `posix-tar` archive `packaging.json` declares is now the last layer (media type\n  `application/vnd.b10x.substrate-wire.bundle.tar`, title `0.4.0.tar`): ustar, directory entries\n  0755 ahead of files 0644, uid/gid 0, empty names, bytewise order, every mtime =\n  `SOURCE_DATE_EPOCH` = author seconds of the last commit touching the bundle (`1787605082`,\n  commit `4b5d411`; `--source-date-epoch` overrides, absent both → exit 2). Per-file layers and the\n  config are byte-identical to before; the manifest digest moves by design to\n  `sha256:3758e80bc39f1eb03b15c69410608c9ef1d2ba8095c7e707c6988dbb5894ab00`\n  (archive `sha256:91fb5524…`, 880,640 bytes). 21 tests; `tar -xf` of the blob reproduces\n  `contracts/substrate-wire/0.4.0` byte for byte. On a shallow CI checkout the epoch would be the\n  tip's author time — pin `--source-date-epoch` there.\n- **Open:** the publish/sign half — `release.yml`, `ghcr.io/beyond10x/b10x-substrate-wire`,\n  cosign, digest in `CHANGELOG.md` — waits on `story:signed-daemon-image`.\n\n## Correction — 2026-08-30\n\nThe two Python names this story was written against no longer exist. `ef22de0` (\"the bundle\npackager is cargo xtask package-bundle, and its Python is gone\") ported the packager and deleted\n`scripts/package-contract-bundle.py` and `scripts/test_package_contract_bundle.py`. Acceptance and\nthe progress notes above now name the Rust verb; the behaviour they assert is unchanged, and the\n`0.4.0` manifest digests recorded above were observed against the Python and have not been\nre-observed against the verb.\n\nCoverage is not lost in the move: `xtask/src/package.rs` carries 21 `#[test]`s (was 21 Python\ntests), and `cargo test --workspace` at `scripts/gate.sh:15` runs them. There is no\n`package-bundle` line in `scripts/gate.sh` and none is needed.\n\n**Still open, unchanged:** the publish/sign half — `release.yml`,\n`ghcr.io/beyond10x/b10x-substrate-wire`, cosign, digest in `CHANGELOG.md`. It waits on\n`story:signed-daemon-image`, which is a draft. This story cannot reach `implemented` until that\none is designed and built.\n\n## Unblocked — 2026-08-30\n\nThe paragraph above says this waits on `story:signed-daemon-image`, \"which is a draft\". It is\n`implemented` (closed at `9131c95`). `.github/workflows/release.yml` exists, has run, and published\n`ghcr.io/beyond10x/b10x-substrate-daemon:0.2.3` at\n`sha256:ab10158266b579d705ce8422c7d2a6e783cde950d30e100f61ca6befc4d0beda`, keyless-signed and\n`cosign verify`-ed (run 33304493276).\n\n**Nothing blocks this story.** What remains is its own second half: a bundle publish-and-sign job in\n`release.yml`, publishing `ghcr.io/beyond10x/b10x-substrate-wire:<bundle-version>` from\n`cargo xtask package-bundle`, with the digest in `CHANGELOG.md` and the artifact annotated\n`development` — a development bundle does not become a stable contract by being published (atlas\nADR 0019).\n\nOne thing the daemon release proved that this story must plan around: the workflow's final step\ncannot push to `main`, because `main` is protected and requires the `Full gate` check, which a\ndirect push can never satisfy. The daemon digest landed by pull request instead\n(`68d226f`). A bundle digest will need the same route or a different one, and the story should say\nwhich before the job is written.\n\n## Implementation — 2026-09-01 (publish/sign half)\n\n- `.github/workflows/release.yml` explicitly pins current development bundle `0.12.0`; a successor\n  must move that reviewable pin rather than being selected by directory order at release time.\n- One globally serialized release job refuses an existing GitHub release or daemon image tag. A\n  contract-bundle tag is reused only when it already resolves to the deterministic local digest; a\n  mismatched digest, authentication failure, transport failure or ambiguous registry answer refuses.\n  The canonical bundle tag is never an overwrite target.\n- The job runs `cargo xtask package-bundle`, checks the manifest digest and\n  `dev.b10x.contract.status=development` annotation locally, and uses pinned ORAS 1.3.3 to copy the\n  exact OCI Image Layout to `ghcr.io/beyond10x/b10x-substrate-wire:<bundle-version>` when absent.\n  It resolves the remote tag back to the packaged digest, checks the development annotation, and\n  proves anonymous retrieval before mutating the daemon-image tag; a visibility failure is retryable\n  without overwriting either artifact.\n- Cosign signs the bundle by digest with the tag-triggered workflow's OIDC identity and verifies the\n  exact certificate identity and GitHub Actions issuer before `gh release create`. Release notes\n  carry both immutable digests, both verification commands and the explicit non-stability statement.\n- Protected `main` is not pushed or bypassed. After both signatures verify, the workflow emits the\n  exact daemon and bundle digest lines for a workstation's `b10x-bot` pull request, which receives\n  the required `Full gate` check.\n- `xtask/tests/release_workflow.rs` fails closed offline on the current-bundle pin, exact-layout copy,\n  two write-once-or-identical-reuse checks, anonymous-access ordering, digest signing/verification order, development wording, protected-main\n  route and commit-pinned actions. A local ORAS layout-to-layout round trip reproduced manifest\n  `sha256:dd901e848c821aca7d55f7b8cf5ee893e1d99a1428b348e32e7ed1045a375319`; `contracts/` stayed\n  clean.\n- **Live evidence remains intentionally absent:** this implementation task forbids publishing or\n  cutting a tag. `ghcr.io/beyond10x/b10x-substrate-wire:0.12.0` was still not found on 2026-09-01.\n  The story stays `active` until an eligible release observes the remote digest and verified\n  signature and a fully gated bot pull request records that observed digest in `CHANGELOG.md`.\n\n## Live release evidence — 2026-09-01\n\n- Annotated tag `0.4.1` names fully gated protected-main merge `961be39`; release preflight passed against gate run `33457279192`.\n- The first publication attempt copied deterministic bundle `0.12.0` to public GHCR digest `sha256:dd901e848c821aca7d55f7b8cf5ee893e1d99a1428b348e32e7ed1045a375319`, then the authenticated immediate ORAS readback failed before signing. A failed-job rerun proved the tag exists at the identical digest but hit the same readback boundary; no daemon image or GitHub release was announced.\n- The follow-up changes GHCR login identity from the repository owner to `github.actor` and makes the post-publish proof explicitly anonymous with a bounded retry. Offline workflow tests pin both properties. Live signing, anonymous ORAS proof and the protected-main changelog pull request remain required before implementation status is earned.\n"},"args":{"changes":{"body":"# Story: The 0.4.0 contract bundle is a signed, digest-pinned OCI artifact\n\n## Outcome\n\nA consumer pins the wire contract by OCI digest instead of copying a directory tree.\n\n## Context\n\n`docs/plan/04-direct-byte-plane.md` § *Slice C* says Agent \"consumes an exact independently\nverified copy\"; the copy is made by hand. `docs/design/07-specification-and-conformance.md`\n§ *Bundle* already fixes the packaging: `bundle.json` lists media type, byte length and digest of\nevery bundle path except itself, and the outer OCI manifest digest pins `bundle.json`. Depends on\n`story:signed-daemon-image` for the release workflow it publishes through.\n\n## Acceptance\n\n`cargo xtask package-bundle <version>` produces a deterministic OCI layout from\n`contracts/substrate-wire/<version>/` without writing into `contracts/`, two runs yield\nbyte-identical manifests, and the release workflow publishes and signs it at\n`ghcr.io/beyond10x/b10x-substrate-wire:<bundle-version>` with the digest in `CHANGELOG.md`.\n\nEvidence that satisfies it:\n\n- `xtask/src/package.rs` tests: identical manifests on two runs; a one-byte change in\n  any bundle file changes the manifest digest — written failing-first against a stub that embeds\n  a timestamp;\n- `bundle.json` matches design 07's shape and a checker asserts each digest and byte length;\n- `check-contract-bundle-0.4.0.py` and every earlier checker stay green (invariant 6: the packager\n  reads the bundle; the bundle does not learn about the packager);\n- the artifact is annotated `development`; publication does not make the bundle stable (atlas ADR\n  0019 governs contract release);\n- `bash scripts/gate.sh` exits 0.\n\n## Progress — 2026-08-29 (packager half)\n\n- `cargo xtask package-bundle <version> --out <dir>`: OCI Image Layout (`oci-layout`,\n  `index.json`, `blobs/sha256/…`); config **is** `bundle.json` verbatim, media type\n  `application/vnd.b10x.substrate-wire.bundle.v1+json`, so the manifest digest pins\n  `bundle.json` (design 07 § *Bundle*); one layer per bundle file, layer digest = the `sha256`\n  already in `bundle.json`; annotations `org.opencontainers.image.version`,\n  `dev.b10x.contract.status=development`, `ref.name` on the index entry. Refuses `--out` inside\n  `contracts/`, non-empty `--out` without `--force`, symlinks, a file set disagreeing with\n  `bundle.json`.\n- 14 tests; determinism test written failing-first\n  against a timestamp-embedding stub (`FAILED (failures=1)`), then green. Two runs on `0.4.0` →\n  `sha256:f94d15fc116587d991aab6de5628f6ee5baf872af4e7c79d2a35e2b17a8485c4`, `diff -r` empty.\n  `oras manifest fetch --oci-layout` resolves the same digest; `oras pull` round-trip matches\n  `contracts/substrate-wire/0.4.0` byte for byte.\n- `check-contract-bundle-0.4.0.py` → exit 0; `git status contracts/` → clean (invariant 6).\n- Gated by `cargo test --workspace` (`scripts/gate.sh:15`), not by a gate line of its own.\n- The `posix-tar` archive `packaging.json` declares is now the last layer (media type\n  `application/vnd.b10x.substrate-wire.bundle.tar`, title `0.4.0.tar`): ustar, directory entries\n  0755 ahead of files 0644, uid/gid 0, empty names, bytewise order, every mtime =\n  `SOURCE_DATE_EPOCH` = author seconds of the last commit touching the bundle (`1787605082`,\n  commit `4b5d411`; `--source-date-epoch` overrides, absent both → exit 2). Per-file layers and the\n  config are byte-identical to before; the manifest digest moves by design to\n  `sha256:3758e80bc39f1eb03b15c69410608c9ef1d2ba8095c7e707c6988dbb5894ab00`\n  (archive `sha256:91fb5524…`, 880,640 bytes). 21 tests; `tar -xf` of the blob reproduces\n  `contracts/substrate-wire/0.4.0` byte for byte. On a shallow CI checkout the epoch would be the\n  tip's author time — pin `--source-date-epoch` there.\n- **Open:** the publish/sign half — `release.yml`, `ghcr.io/beyond10x/b10x-substrate-wire`,\n  cosign, digest in `CHANGELOG.md` — waits on `story:signed-daemon-image`.\n\n## Correction — 2026-08-30\n\nThe two Python names this story was written against no longer exist. `ef22de0` (\"the bundle\npackager is cargo xtask package-bundle, and its Python is gone\") ported the packager and deleted\n`scripts/package-contract-bundle.py` and `scripts/test_package_contract_bundle.py`. Acceptance and\nthe progress notes above now name the Rust verb; the behaviour they assert is unchanged, and the\n`0.4.0` manifest digests recorded above were observed against the Python and have not been\nre-observed against the verb.\n\nCoverage is not lost in the move: `xtask/src/package.rs` carries 21 `#[test]`s (was 21 Python\ntests), and `cargo test --workspace` at `scripts/gate.sh:15` runs them. There is no\n`package-bundle` line in `scripts/gate.sh` and none is needed.\n\n**Still open, unchanged:** the publish/sign half — `release.yml`,\n`ghcr.io/beyond10x/b10x-substrate-wire`, cosign, digest in `CHANGELOG.md`. It waits on\n`story:signed-daemon-image`, which is a draft. This story cannot reach `implemented` until that\none is designed and built.\n\n## Unblocked — 2026-08-30\n\nThe paragraph above says this waits on `story:signed-daemon-image`, \"which is a draft\". It is\n`implemented` (closed at `9131c95`). `.github/workflows/release.yml` exists, has run, and published\n`ghcr.io/beyond10x/b10x-substrate-daemon:0.2.3` at\n`sha256:ab10158266b579d705ce8422c7d2a6e783cde950d30e100f61ca6befc4d0beda`, keyless-signed and\n`cosign verify`-ed (run 33304493276).\n\n**Nothing blocks this story.** What remains is its own second half: a bundle publish-and-sign job in\n`release.yml`, publishing `ghcr.io/beyond10x/b10x-substrate-wire:<bundle-version>` from\n`cargo xtask package-bundle`, with the digest in `CHANGELOG.md` and the artifact annotated\n`development` — a development bundle does not become a stable contract by being published (atlas\nADR 0019).\n\nOne thing the daemon release proved that this story must plan around: the workflow's final step\ncannot push to `main`, because `main` is protected and requires the `Full gate` check, which a\ndirect push can never satisfy. The daemon digest landed by pull request instead\n(`68d226f`). A bundle digest will need the same route or a different one, and the story should say\nwhich before the job is written.\n\n## Implementation — 2026-09-01 (publish/sign half)\n\n- `.github/workflows/release.yml` explicitly pins current development bundle `0.12.0`; a successor\n  must move that reviewable pin rather than being selected by directory order at release time.\n- One globally serialized release job refuses an existing GitHub release or daemon image tag. A\n  contract-bundle tag is reused only when it already resolves to the deterministic local digest; a\n  mismatched digest, authentication failure, transport failure or ambiguous registry answer refuses.\n  The canonical bundle tag is never an overwrite target.\n- The job runs `cargo xtask package-bundle`, checks the manifest digest and\n  `dev.b10x.contract.status=development` annotation locally, and uses pinned ORAS 1.3.3 to copy the\n  exact OCI Image Layout to `ghcr.io/beyond10x/b10x-substrate-wire:<bundle-version>` when absent.\n  It resolves the remote tag back to the packaged digest, checks the development annotation, and\n  proves anonymous retrieval before mutating the daemon-image tag; a visibility failure is retryable\n  without overwriting either artifact.\n- Cosign signs the bundle by digest with the tag-triggered workflow's OIDC identity and verifies the\n  exact certificate identity and GitHub Actions issuer before `gh release create`. Release notes\n  carry both immutable digests, both verification commands and the explicit non-stability statement.\n- Protected `main` is not pushed or bypassed. After both signatures verify, the workflow emits the\n  exact daemon and bundle digest lines for a workstation's `b10x-bot` pull request, which receives\n  the required `Full gate` check.\n- `xtask/tests/release_workflow.rs` fails closed offline on the current-bundle pin, exact-layout copy,\n  two write-once-or-identical-reuse checks, anonymous-access ordering, digest signing/verification order, development wording, protected-main\n  route and commit-pinned actions. A local ORAS layout-to-layout round trip reproduced manifest\n  `sha256:dd901e848c821aca7d55f7b8cf5ee893e1d99a1428b348e32e7ed1045a375319`; `contracts/` stayed\n  clean.\n- **Live evidence remains intentionally absent:** this implementation task forbids publishing or\n  cutting a tag. `ghcr.io/beyond10x/b10x-substrate-wire:0.12.0` was still not found on 2026-09-01.\n  The story stays `active` until an eligible release observes the remote digest and verified\n  signature and a fully gated bot pull request records that observed digest in `CHANGELOG.md`.\n\n## Live release evidence — 2026-09-01\n\n- Annotated tag `0.4.1` names fully gated protected-main merge `961be39`; release preflight passed against gate run `33457279192`.\n- The first publication attempt copied deterministic bundle `0.12.0` to public GHCR digest `sha256:dd901e848c821aca7d55f7b8cf5ee893e1d99a1428b348e32e7ed1045a375319`, then the authenticated immediate ORAS readback failed before signing. A failed-job rerun proved the tag exists at the identical digest but hit the same readback boundary; no daemon image or GitHub release was announced.\n- The follow-up changes GHCR login identity from the repository owner to `github.actor` and makes the post-publish proof explicitly anonymous with a bounded retry. Offline workflow tests pin both properties. Live signing, anonymous ORAS proof and the protected-main changelog pull request remain required before implementation status is earned.\n"},"command":"update-entity","target":"01MEM0000000000000046"},"payload":{"actor":"human:timo","at":1788225612012,"causation":"cmd-protocol-artifact-body-append-story-contract-bundle-oci-artifact-1788225612012","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-append","event_id":"story:contract-bundle-oci-artifact@12#0~a7cc53fc4f18c7c1","executor":null,"recorded_at":"2026-09-01T01:20:12Z"}}
{"entity":"story","version":1,"id":"production-tls-control-listener","revision":4,"type":"aep.entity.update/v1","from_state":"proposed","to_state":"proposed","changed":{"body":"# Story: Production control traffic uses TLS\n\n## Outcome\n\nA remotely deployed daemon serves its HTTP and WebSocket control plane over TLS with explicit operator-provided identity and trust configuration.\n\n## Design gate\n\nAn accepted design or ADR fixes certificate loading and rotation, client authentication mode, listener binding, forwarded-address treatment, WebSocket behavior, and the exact startup refusals before code. The Unix socket and kernel-peer-credential subject derivation remain unchanged.\n\n## Acceptance\n\nThe daemon uses rustls and accepts no production plaintext listener. A non-loopback static-bearer configuration is refused at startup. Certificate and key files must be explicit, owner-safe and reloadable without exposing bytes in logs, events or metrics. Tests cover valid TLS, unknown CA, wrong server name, expired/not-yet-valid certificates, missing key material, plaintext attempts, WebSocket upgrade, rotation and shutdown. Development plaintext remains loopback-only and visibly marked development.\n\n## Out of Scope\n\nPublic ingress controllers, ACME automation, service-mesh-specific configuration and hosted token verification.\n\n## Design closure — 2026-09-01\n\n- [ADR 0024](../../../adr/0024-production-network-control-uses-server-authenticated-tls.md) accepts the approved server-authenticated TLS posture: a distinct TLS 1.3 HTTPS/WSS listener, no mTLS principal mapping, explicit owner-safe key material, atomic SIGHUP rotation, no forwarded-address trust and named startup/reload refusals.\n- Implementation remains sequenced after `story:promote-development-contract-frontier`; no listener code starts while that dependency is proposed.\n"},"args":{"changes":{"body":"# Story: Production control traffic uses TLS\n\n## Outcome\n\nA remotely deployed daemon serves its HTTP and WebSocket control plane over TLS with explicit operator-provided identity and trust configuration.\n\n## Design gate\n\nAn accepted design or ADR fixes certificate loading and rotation, client authentication mode, listener binding, forwarded-address treatment, WebSocket behavior, and the exact startup refusals before code. The Unix socket and kernel-peer-credential subject derivation remain unchanged.\n\n## Acceptance\n\nThe daemon uses rustls and accepts no production plaintext listener. A non-loopback static-bearer configuration is refused at startup. Certificate and key files must be explicit, owner-safe and reloadable without exposing bytes in logs, events or metrics. Tests cover valid TLS, unknown CA, wrong server name, expired/not-yet-valid certificates, missing key material, plaintext attempts, WebSocket upgrade, rotation and shutdown. Development plaintext remains loopback-only and visibly marked development.\n\n## Out of Scope\n\nPublic ingress controllers, ACME automation, service-mesh-specific configuration and hosted token verification.\n\n## Design closure — 2026-09-01\n\n- [ADR 0024](../../../adr/0024-production-network-control-uses-server-authenticated-tls.md) accepts the approved server-authenticated TLS posture: a distinct TLS 1.3 HTTPS/WSS listener, no mTLS principal mapping, explicit owner-safe key material, atomic SIGHUP rotation, no forwarded-address trust and named startup/reload refusals.\n- Implementation remains sequenced after `story:promote-development-contract-frontier`; no listener code starts while that dependency is proposed.\n"},"command":"update-entity","target":"01MEM0000000000000112"},"payload":{"actor":"human:timo","at":1788226412289,"causation":"cmd-protocol-artifact-body-append-story-production-tls-control-listener-1788226412289","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-append","event_id":"story:production-tls-control-listener@4#0~eb17e1863531114c","executor":null,"recorded_at":"2026-09-01T01:33:32Z"}}
{"entity":"story","version":1,"id":"rust-sdk-publication","revision":6,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"body":"# Story: The Rust SDK and runtime chain are owner-released Rust packages\n\n## Outcome\n\nConsumers depend on version-locked `b10x-substrate-*` packages rather than a sibling checkout. The default client stays transport-only; linked daemon support is opt-in.\n\n## Acceptance\n\nThe gate permits only the named package set, verifies package contents and downstream builds, and keeps every other workspace member non-publishable. A fully gated annotated release tag is published to crates.io manually in dependency order with an operator-held scoped token that never enters GitHub.\n\n## Current state — 2026-09-01\n\nThe implementation, package allowlist, exact internal version edges, package-content checks, README material and public SDK journeys shipped on main and pass the required Full gate. This story stays active only for the external publication proof: publish the owner-released package chain to crates.io in dependency order from a fully gated annotated tag, verify anonymous clean-room installation, and record the registry versions and digests. No GitHub secret or repository-held crates.io token is introduced.\n\n## Publication readiness audit — 2026-09-01\n\n- Annotated tag `0.4.1` resolves to fully gated commit `961be39c34ef1f9b655a0b5a58a108dabc722d5c`; current main changes no package inputs after that tag.\n- Anonymous crates.io API and search returned no package for all five approved names. This is current availability, not a reservation; first-publication races remain possible.\n- Publish from a clean detached `0.4.1` checkout in dependency order: `b10x-substrate-wire`, `b10x-substrate-store`, `b10x-substrate-host`, `b10x-substrate-daemon`, `b10x-substrate-sdk`.\n- Two package runs were byte-identical. Predicted registry checksums are wire `c622fcee231eb2fe7c238b122642b3e9b2734d2d0023e2d1e0e4d2bf3c710c88`, store `fe785c9fd580c40ec8f207fa2edc19c9543bf8bc9c9a802ffb9ca5ffd1bffd10`, host `9aefc892172da3d348953b93dd07ada7d2deb43e13010e66937c66e4b9bdd16e`, daemon `f134c26509711060c6257ecd17e04101116f23571737c595021e01f396299866`, SDK `e45d37f8eb28f10b39c1f910f6715d467f90736108e904e781f59031f527adab`.\n- Temporary-registry dependency resolution and SDK `linked-daemon` verification passed. The owner-private Cargo credentials file exists, but token validity and `publish-new` scope remain intentionally unproven until the operator-authorized live publication.\n- Closure still requires the contract-bundle dependency, five live registry checksums, and credential-free clean-room default SDK, linked-daemon and daemon installation tests.\n"},"args":{"changes":{"body":"# Story: The Rust SDK and runtime chain are owner-released Rust packages\n\n## Outcome\n\nConsumers depend on version-locked `b10x-substrate-*` packages rather than a sibling checkout. The default client stays transport-only; linked daemon support is opt-in.\n\n## Acceptance\n\nThe gate permits only the named package set, verifies package contents and downstream builds, and keeps every other workspace member non-publishable. A fully gated annotated release tag is published to crates.io manually in dependency order with an operator-held scoped token that never enters GitHub.\n\n## Current state — 2026-09-01\n\nThe implementation, package allowlist, exact internal version edges, package-content checks, README material and public SDK journeys shipped on main and pass the required Full gate. This story stays active only for the external publication proof: publish the owner-released package chain to crates.io in dependency order from a fully gated annotated tag, verify anonymous clean-room installation, and record the registry versions and digests. No GitHub secret or repository-held crates.io token is introduced.\n\n## Publication readiness audit — 2026-09-01\n\n- Annotated tag `0.4.1` resolves to fully gated commit `961be39c34ef1f9b655a0b5a58a108dabc722d5c`; current main changes no package inputs after that tag.\n- Anonymous crates.io API and search returned no package for all five approved names. This is current availability, not a reservation; first-publication races remain possible.\n- Publish from a clean detached `0.4.1` checkout in dependency order: `b10x-substrate-wire`, `b10x-substrate-store`, `b10x-substrate-host`, `b10x-substrate-daemon`, `b10x-substrate-sdk`.\n- Two package runs were byte-identical. Predicted registry checksums are wire `c622fcee231eb2fe7c238b122642b3e9b2734d2d0023e2d1e0e4d2bf3c710c88`, store `fe785c9fd580c40ec8f207fa2edc19c9543bf8bc9c9a802ffb9ca5ffd1bffd10`, host `9aefc892172da3d348953b93dd07ada7d2deb43e13010e66937c66e4b9bdd16e`, daemon `f134c26509711060c6257ecd17e04101116f23571737c595021e01f396299866`, SDK `e45d37f8eb28f10b39c1f910f6715d467f90736108e904e781f59031f527adab`.\n- Temporary-registry dependency resolution and SDK `linked-daemon` verification passed. The owner-private Cargo credentials file exists, but token validity and `publish-new` scope remain intentionally unproven until the operator-authorized live publication.\n- Closure still requires the contract-bundle dependency, five live registry checksums, and credential-free clean-room default SDK, linked-daemon and daemon installation tests.\n"},"command":"update-entity","target":"01MEM0000000000000145"},"payload":{"actor":"human:timo","at":1788226550994,"causation":"cmd-protocol-artifact-body-append-story-rust-sdk-publication-1788226550994","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-append","event_id":"story:rust-sdk-publication@6#0~4c25889b1fbb58c8","executor":null,"recorded_at":"2026-09-01T01:35:50Z"}}
{"entity":"story","version":1,"id":"sdk-promoted-contract-parity","revision":4,"type":"aep.entity.update/v1","from_state":"proposed","to_state":"proposed","changed":{"body":"# Story: The Rust SDK covers the promoted contract\n\n## Outcome\n\nA Rust caller can use every supported operation and confinement option in the contract the daemon advertises, with typed requests, observations and refusals.\n\n## Current overlap\n\nThe substrate-hardening worktree already adds SDK builders for workspace access, apertures, scratch, measurements, read-only roots, secret slots and capsules, plus exact applied and usage observations. That code is input to this story and must be reviewed or merged, not reimplemented. This story closes the remaining promoted-version and PTY/metrics parity after that work lands.\n\n## Acceptance\n\nThe SDK supports PTY sessions and resize, workspace and scratch quotas, exact measurement selection and terminal usage, live metrics retrieval/streaming, workspace access, apertures, read-only roots, secret slots and execution capsules where the advertised bundle supports them. It preserves operation IDs across ambiguous transport outcomes and returns named refusals without collapsing them into strings. A clean-room journey uses only the published SDK API against the shipped daemon binary and asserts the promoted header. Driver names and host-library types never enter the public API.\n\n## Out of Scope\n\nRemote HTTPS transport, product policy helpers and automatic limit selection.\n\n## Remote/MCP prerequisites — 2026-09-01\n\nBefore this story can close, the public SDK must additionally expose caller-supplied operation ids for file write/delete, workspace destroy and lease renewal, exec signal/retire and lease renewal; bounded exec-output paging; metrics retrieval; serializable SDK-owned observations or an exact projector; capability facts that preserve absence instead of collapsing it to `false`; and SDK-owned public bounds for generated schemas.\n\nManaged-daemon shutdown must retain child and temporary-root ownership across kill/wait errors. A red test proves that unknown child absence never drops the temporary state, and startup cleanup observes the same rule. The daemon/adapter cleanup path must also prove no detached exec supervisor, workload process or cgroup survives orderly shutdown.\n"},"args":{"changes":{"body":"# Story: The Rust SDK covers the promoted contract\n\n## Outcome\n\nA Rust caller can use every supported operation and confinement option in the contract the daemon advertises, with typed requests, observations and refusals.\n\n## Current overlap\n\nThe substrate-hardening worktree already adds SDK builders for workspace access, apertures, scratch, measurements, read-only roots, secret slots and capsules, plus exact applied and usage observations. That code is input to this story and must be reviewed or merged, not reimplemented. This story closes the remaining promoted-version and PTY/metrics parity after that work lands.\n\n## Acceptance\n\nThe SDK supports PTY sessions and resize, workspace and scratch quotas, exact measurement selection and terminal usage, live metrics retrieval/streaming, workspace access, apertures, read-only roots, secret slots and execution capsules where the advertised bundle supports them. It preserves operation IDs across ambiguous transport outcomes and returns named refusals without collapsing them into strings. A clean-room journey uses only the published SDK API against the shipped daemon binary and asserts the promoted header. Driver names and host-library types never enter the public API.\n\n## Out of Scope\n\nRemote HTTPS transport, product policy helpers and automatic limit selection.\n\n## Remote/MCP prerequisites — 2026-09-01\n\nBefore this story can close, the public SDK must additionally expose caller-supplied operation ids for file write/delete, workspace destroy and lease renewal, exec signal/retire and lease renewal; bounded exec-output paging; metrics retrieval; serializable SDK-owned observations or an exact projector; capability facts that preserve absence instead of collapsing it to `false`; and SDK-owned public bounds for generated schemas.\n\nManaged-daemon shutdown must retain child and temporary-root ownership across kill/wait errors. A red test proves that unknown child absence never drops the temporary state, and startup cleanup observes the same rule. The daemon/adapter cleanup path must also prove no detached exec supervisor, workload process or cgroup survives orderly shutdown.\n"},"command":"update-entity","target":"01MEM0000000000000148"},"payload":{"actor":"human:timo","at":1788226678679,"causation":"cmd-protocol-artifact-body-append-story-sdk-promoted-contract-parity-1788226678679","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-append","event_id":"story:sdk-promoted-contract-parity@4#0~a0a3b8829a6b559f","executor":null,"recorded_at":"2026-09-01T01:37:58Z"}}
{"entity":"story","version":1,"id":"mcp-managed-disposable-daemon","revision":4,"type":"aep.entity.update/v1","from_state":"proposed","to_state":"proposed","changed":{"body":"# Story: Launch a disposable daemon behind MCP\n\n## Outcome\n\nOne command starts a fresh private Substrate daemon, waits for its machine facts, exposes bounded workspace and execution operations as MCP tools, and tears down the socket, database, workspaces and child process when the MCP server exits.\n\n## Acceptance\n\nThe adapter verifies the advertised contract and capability snapshot, preserves operation IDs and named refusals, never bypasses the daemon through host-driver APIs, exposes output and exact resource observations without inventing fields, scopes all state to the disposable instance, and includes a Codex-compatible hands-on smoke test. Unsupported host capabilities remain absent or refused. No TCP listener or production authentication claim is introduced.\n\n## Design-closure audit — 2026-09-01\n\nImplementation requires `docs/design/19-mcp-disposable-test-adapter.md` to be accepted after SDK parity. The adapter remains a private, development-only stdio composition over the SDK and Unix socket; it is not production ingress and does not imply public OCI publication.\n\nThe design must fix: a private SDK-only crate boundary; a custom bounded JSONL transport rather than an unbounded stdio helper; caller-supplied operation ids on every mutation; exact refusal/observation projection; no model-selected host roots, secret slots or apertures; tracked exec/workspace teardown before daemon shutdown; an exclusive process-free delegated cgroup root; and honest SIGKILL semantics. Portable OCI smoke proves the named sandbox refusal; positive execution stays in the native delegated lane until a separate container-runtime design supplies the confinement prerequisites.\n\nAcceptance additionally depends on the managed-daemon child-absence fix and public SDK output/metrics/absence-preserving facts. `cargo xtask check-mcp-boundary` must prove the crate is private, depends on Substrate only through the SDK, enables no HTTP/OAuth MCP features and uses no unbounded rmcp stdio transport.\n"},"args":{"changes":{"body":"# Story: Launch a disposable daemon behind MCP\n\n## Outcome\n\nOne command starts a fresh private Substrate daemon, waits for its machine facts, exposes bounded workspace and execution operations as MCP tools, and tears down the socket, database, workspaces and child process when the MCP server exits.\n\n## Acceptance\n\nThe adapter verifies the advertised contract and capability snapshot, preserves operation IDs and named refusals, never bypasses the daemon through host-driver APIs, exposes output and exact resource observations without inventing fields, scopes all state to the disposable instance, and includes a Codex-compatible hands-on smoke test. Unsupported host capabilities remain absent or refused. No TCP listener or production authentication claim is introduced.\n\n## Design-closure audit — 2026-09-01\n\nImplementation requires `docs/design/19-mcp-disposable-test-adapter.md` to be accepted after SDK parity. The adapter remains a private, development-only stdio composition over the SDK and Unix socket; it is not production ingress and does not imply public OCI publication.\n\nThe design must fix: a private SDK-only crate boundary; a custom bounded JSONL transport rather than an unbounded stdio helper; caller-supplied operation ids on every mutation; exact refusal/observation projection; no model-selected host roots, secret slots or apertures; tracked exec/workspace teardown before daemon shutdown; an exclusive process-free delegated cgroup root; and honest SIGKILL semantics. Portable OCI smoke proves the named sandbox refusal; positive execution stays in the native delegated lane until a separate container-runtime design supplies the confinement prerequisites.\n\nAcceptance additionally depends on the managed-daemon child-absence fix and public SDK output/metrics/absence-preserving facts. `cargo xtask check-mcp-boundary` must prove the crate is private, depends on Substrate only through the SDK, enables no HTTP/OAuth MCP features and uses no unbounded rmcp stdio transport.\n"},"command":"update-entity","target":"01MEM0000000000000097"},"payload":{"actor":"human:timo","at":1788226678806,"causation":"cmd-protocol-artifact-body-append-story-mcp-managed-disposable-daemon-1788226678806","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-append","event_id":"story:mcp-managed-disposable-daemon@4#0~4d5745a7df464613","executor":null,"recorded_at":"2026-09-01T01:37:58Z"}}
{"entity":"story","version":1,"id":"contract-bundle-oci-artifact","revision":13,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"body":"# Story: The 0.4.0 contract bundle is a signed, digest-pinned OCI artifact\n\n## Outcome\n\nA consumer pins the wire contract by OCI digest instead of copying a directory tree.\n\n## Context\n\n`docs/plan/04-direct-byte-plane.md` § *Slice C* says Agent \"consumes an exact independently\nverified copy\"; the copy is made by hand. `docs/design/07-specification-and-conformance.md`\n§ *Bundle* already fixes the packaging: `bundle.json` lists media type, byte length and digest of\nevery bundle path except itself, and the outer OCI manifest digest pins `bundle.json`. Depends on\n`story:signed-daemon-image` for the release workflow it publishes through.\n\n## Acceptance\n\n`cargo xtask package-bundle <version>` produces a deterministic OCI layout from\n`contracts/substrate-wire/<version>/` without writing into `contracts/`, two runs yield\nbyte-identical manifests, and the release workflow publishes and signs it at\n`ghcr.io/beyond10x/b10x-substrate-wire:<bundle-version>` with the digest in `CHANGELOG.md`.\n\nEvidence that satisfies it:\n\n- `xtask/src/package.rs` tests: identical manifests on two runs; a one-byte change in\n  any bundle file changes the manifest digest — written failing-first against a stub that embeds\n  a timestamp;\n- `bundle.json` matches design 07's shape and a checker asserts each digest and byte length;\n- `check-contract-bundle-0.4.0.py` and every earlier checker stay green (invariant 6: the packager\n  reads the bundle; the bundle does not learn about the packager);\n- the artifact is annotated `development`; publication does not make the bundle stable (atlas ADR\n  0019 governs contract release);\n- `bash scripts/gate.sh` exits 0.\n\n## Progress — 2026-08-29 (packager half)\n\n- `cargo xtask package-bundle <version> --out <dir>`: OCI Image Layout (`oci-layout`,\n  `index.json`, `blobs/sha256/…`); config **is** `bundle.json` verbatim, media type\n  `application/vnd.b10x.substrate-wire.bundle.v1+json`, so the manifest digest pins\n  `bundle.json` (design 07 § *Bundle*); one layer per bundle file, layer digest = the `sha256`\n  already in `bundle.json`; annotations `org.opencontainers.image.version`,\n  `dev.b10x.contract.status=development`, `ref.name` on the index entry. Refuses `--out` inside\n  `contracts/`, non-empty `--out` without `--force`, symlinks, a file set disagreeing with\n  `bundle.json`.\n- 14 tests; determinism test written failing-first\n  against a timestamp-embedding stub (`FAILED (failures=1)`), then green. Two runs on `0.4.0` →\n  `sha256:f94d15fc116587d991aab6de5628f6ee5baf872af4e7c79d2a35e2b17a8485c4`, `diff -r` empty.\n  `oras manifest fetch --oci-layout` resolves the same digest; `oras pull` round-trip matches\n  `contracts/substrate-wire/0.4.0` byte for byte.\n- `check-contract-bundle-0.4.0.py` → exit 0; `git status contracts/` → clean (invariant 6).\n- Gated by `cargo test --workspace` (`scripts/gate.sh:15`), not by a gate line of its own.\n- The `posix-tar` archive `packaging.json` declares is now the last layer (media type\n  `application/vnd.b10x.substrate-wire.bundle.tar`, title `0.4.0.tar`): ustar, directory entries\n  0755 ahead of files 0644, uid/gid 0, empty names, bytewise order, every mtime =\n  `SOURCE_DATE_EPOCH` = author seconds of the last commit touching the bundle (`1787605082`,\n  commit `4b5d411`; `--source-date-epoch` overrides, absent both → exit 2). Per-file layers and the\n  config are byte-identical to before; the manifest digest moves by design to\n  `sha256:3758e80bc39f1eb03b15c69410608c9ef1d2ba8095c7e707c6988dbb5894ab00`\n  (archive `sha256:91fb5524…`, 880,640 bytes). 21 tests; `tar -xf` of the blob reproduces\n  `contracts/substrate-wire/0.4.0` byte for byte. On a shallow CI checkout the epoch would be the\n  tip's author time — pin `--source-date-epoch` there.\n- **Open:** the publish/sign half — `release.yml`, `ghcr.io/beyond10x/b10x-substrate-wire`,\n  cosign, digest in `CHANGELOG.md` — waits on `story:signed-daemon-image`.\n\n## Correction — 2026-08-30\n\nThe two Python names this story was written against no longer exist. `ef22de0` (\"the bundle\npackager is cargo xtask package-bundle, and its Python is gone\") ported the packager and deleted\n`scripts/package-contract-bundle.py` and `scripts/test_package_contract_bundle.py`. Acceptance and\nthe progress notes above now name the Rust verb; the behaviour they assert is unchanged, and the\n`0.4.0` manifest digests recorded above were observed against the Python and have not been\nre-observed against the verb.\n\nCoverage is not lost in the move: `xtask/src/package.rs` carries 21 `#[test]`s (was 21 Python\ntests), and `cargo test --workspace` at `scripts/gate.sh:15` runs them. There is no\n`package-bundle` line in `scripts/gate.sh` and none is needed.\n\n**Still open, unchanged:** the publish/sign half — `release.yml`,\n`ghcr.io/beyond10x/b10x-substrate-wire`, cosign, digest in `CHANGELOG.md`. It waits on\n`story:signed-daemon-image`, which is a draft. This story cannot reach `implemented` until that\none is designed and built.\n\n## Unblocked — 2026-08-30\n\nThe paragraph above says this waits on `story:signed-daemon-image`, \"which is a draft\". It is\n`implemented` (closed at `9131c95`). `.github/workflows/release.yml` exists, has run, and published\n`ghcr.io/beyond10x/b10x-substrate-daemon:0.2.3` at\n`sha256:ab10158266b579d705ce8422c7d2a6e783cde950d30e100f61ca6befc4d0beda`, keyless-signed and\n`cosign verify`-ed (run 33304493276).\n\n**Nothing blocks this story.** What remains is its own second half: a bundle publish-and-sign job in\n`release.yml`, publishing `ghcr.io/beyond10x/b10x-substrate-wire:<bundle-version>` from\n`cargo xtask package-bundle`, with the digest in `CHANGELOG.md` and the artifact annotated\n`development` — a development bundle does not become a stable contract by being published (atlas\nADR 0019).\n\nOne thing the daemon release proved that this story must plan around: the workflow's final step\ncannot push to `main`, because `main` is protected and requires the `Full gate` check, which a\ndirect push can never satisfy. The daemon digest landed by pull request instead\n(`68d226f`). A bundle digest will need the same route or a different one, and the story should say\nwhich before the job is written.\n\n## Implementation — 2026-09-01 (publish/sign half)\n\n- `.github/workflows/release.yml` explicitly pins current development bundle `0.12.0`; a successor\n  must move that reviewable pin rather than being selected by directory order at release time.\n- One globally serialized release job refuses an existing GitHub release or daemon image tag. A\n  contract-bundle tag is reused only when it already resolves to the deterministic local digest; a\n  mismatched digest, authentication failure, transport failure or ambiguous registry answer refuses.\n  The canonical bundle tag is never an overwrite target.\n- The job runs `cargo xtask package-bundle`, checks the manifest digest and\n  `dev.b10x.contract.status=development` annotation locally, and uses pinned ORAS 1.3.3 to copy the\n  exact OCI Image Layout to `ghcr.io/beyond10x/b10x-substrate-wire:<bundle-version>` when absent.\n  It resolves the remote tag back to the packaged digest, checks the development annotation, and\n  proves anonymous retrieval before mutating the daemon-image tag; a visibility failure is retryable\n  without overwriting either artifact.\n- Cosign signs the bundle by digest with the tag-triggered workflow's OIDC identity and verifies the\n  exact certificate identity and GitHub Actions issuer before `gh release create`. Release notes\n  carry both immutable digests, both verification commands and the explicit non-stability statement.\n- Protected `main` is not pushed or bypassed. After both signatures verify, the workflow emits the\n  exact daemon and bundle digest lines for a workstation's `b10x-bot` pull request, which receives\n  the required `Full gate` check.\n- `xtask/tests/release_workflow.rs` fails closed offline on the current-bundle pin, exact-layout copy,\n  two write-once-or-identical-reuse checks, anonymous-access ordering, digest signing/verification order, development wording, protected-main\n  route and commit-pinned actions. A local ORAS layout-to-layout round trip reproduced manifest\n  `sha256:dd901e848c821aca7d55f7b8cf5ee893e1d99a1428b348e32e7ed1045a375319`; `contracts/` stayed\n  clean.\n- **Live evidence remains intentionally absent:** this implementation task forbids publishing or\n  cutting a tag. `ghcr.io/beyond10x/b10x-substrate-wire:0.12.0` was still not found on 2026-09-01.\n  The story stays `active` until an eligible release observes the remote digest and verified\n  signature and a fully gated bot pull request records that observed digest in `CHANGELOG.md`.\n\n## Live release evidence — 2026-09-01\n\n- Annotated tag `0.4.1` names fully gated protected-main merge `961be39`; release preflight passed against gate run `33457279192`.\n- The first publication attempt copied deterministic bundle `0.12.0` to public GHCR digest `sha256:dd901e848c821aca7d55f7b8cf5ee893e1d99a1428b348e32e7ed1045a375319`, then the authenticated immediate ORAS readback failed before signing. A failed-job rerun proved the tag exists at the identical digest but hit the same readback boundary; no daemon image or GitHub release was announced.\n- The follow-up changes GHCR login identity from the repository owner to `github.actor` and makes the post-publish proof explicitly anonymous with a bounded retry. Offline workflow tests pin both properties. Live signing, anonymous ORAS proof and the protected-main changelog pull request remain required before implementation status is earned.\n\n## 0.4.2 recovery evidence — 2026-09-01\n\n- Annotated tag `0.4.2` names protected-main merge `0687551`; the exact push gate run `33460337507` passed before the tag was created.\n- Release run `33460657507` anonymously resolved the existing write-once `0.12.0` tag at `sha256:dd901e848c821aca7d55f7b8cf5ee893e1d99a1428b348e32e7ed1045a375319`, then failed while asking `oras manifest fetch --descriptor` for the development/version annotations. OCI registry descriptors carry digest, media type and size; those annotations are members of the manifest content. The run stopped before signing, daemon publication or GitHub release creation.\n- The recovery change fetches the anonymous manifest after independently comparing the resolved digest, validates its two annotations, and adds an explicit `workflow_dispatch` recovery for an existing annotated tag. Recovery is accepted only from `refs/heads/main`, re-verifies the immutable tag, its main ancestry, its tagged `Cargo.toml` version and the exact recorded gate success, and never moves the tag.\n- The corrected command passed live, credential-free against the public GHCR object. All 10 focused workflow tests and `bash scripts/gate.sh` passed with 2,557 JSON documents across all 12 frozen bundles. Live keyless signing, daemon publication, GitHub release readback and the protected-main changelog pull request remain required before this story can move to `implemented`.\n"},"args":{"changes":{"body":"# Story: The 0.4.0 contract bundle is a signed, digest-pinned OCI artifact\n\n## Outcome\n\nA consumer pins the wire contract by OCI digest instead of copying a directory tree.\n\n## Context\n\n`docs/plan/04-direct-byte-plane.md` § *Slice C* says Agent \"consumes an exact independently\nverified copy\"; the copy is made by hand. `docs/design/07-specification-and-conformance.md`\n§ *Bundle* already fixes the packaging: `bundle.json` lists media type, byte length and digest of\nevery bundle path except itself, and the outer OCI manifest digest pins `bundle.json`. Depends on\n`story:signed-daemon-image` for the release workflow it publishes through.\n\n## Acceptance\n\n`cargo xtask package-bundle <version>` produces a deterministic OCI layout from\n`contracts/substrate-wire/<version>/` without writing into `contracts/`, two runs yield\nbyte-identical manifests, and the release workflow publishes and signs it at\n`ghcr.io/beyond10x/b10x-substrate-wire:<bundle-version>` with the digest in `CHANGELOG.md`.\n\nEvidence that satisfies it:\n\n- `xtask/src/package.rs` tests: identical manifests on two runs; a one-byte change in\n  any bundle file changes the manifest digest — written failing-first against a stub that embeds\n  a timestamp;\n- `bundle.json` matches design 07's shape and a checker asserts each digest and byte length;\n- `check-contract-bundle-0.4.0.py` and every earlier checker stay green (invariant 6: the packager\n  reads the bundle; the bundle does not learn about the packager);\n- the artifact is annotated `development`; publication does not make the bundle stable (atlas ADR\n  0019 governs contract release);\n- `bash scripts/gate.sh` exits 0.\n\n## Progress — 2026-08-29 (packager half)\n\n- `cargo xtask package-bundle <version> --out <dir>`: OCI Image Layout (`oci-layout`,\n  `index.json`, `blobs/sha256/…`); config **is** `bundle.json` verbatim, media type\n  `application/vnd.b10x.substrate-wire.bundle.v1+json`, so the manifest digest pins\n  `bundle.json` (design 07 § *Bundle*); one layer per bundle file, layer digest = the `sha256`\n  already in `bundle.json`; annotations `org.opencontainers.image.version`,\n  `dev.b10x.contract.status=development`, `ref.name` on the index entry. Refuses `--out` inside\n  `contracts/`, non-empty `--out` without `--force`, symlinks, a file set disagreeing with\n  `bundle.json`.\n- 14 tests; determinism test written failing-first\n  against a timestamp-embedding stub (`FAILED (failures=1)`), then green. Two runs on `0.4.0` →\n  `sha256:f94d15fc116587d991aab6de5628f6ee5baf872af4e7c79d2a35e2b17a8485c4`, `diff -r` empty.\n  `oras manifest fetch --oci-layout` resolves the same digest; `oras pull` round-trip matches\n  `contracts/substrate-wire/0.4.0` byte for byte.\n- `check-contract-bundle-0.4.0.py` → exit 0; `git status contracts/` → clean (invariant 6).\n- Gated by `cargo test --workspace` (`scripts/gate.sh:15`), not by a gate line of its own.\n- The `posix-tar` archive `packaging.json` declares is now the last layer (media type\n  `application/vnd.b10x.substrate-wire.bundle.tar`, title `0.4.0.tar`): ustar, directory entries\n  0755 ahead of files 0644, uid/gid 0, empty names, bytewise order, every mtime =\n  `SOURCE_DATE_EPOCH` = author seconds of the last commit touching the bundle (`1787605082`,\n  commit `4b5d411`; `--source-date-epoch` overrides, absent both → exit 2). Per-file layers and the\n  config are byte-identical to before; the manifest digest moves by design to\n  `sha256:3758e80bc39f1eb03b15c69410608c9ef1d2ba8095c7e707c6988dbb5894ab00`\n  (archive `sha256:91fb5524…`, 880,640 bytes). 21 tests; `tar -xf` of the blob reproduces\n  `contracts/substrate-wire/0.4.0` byte for byte. On a shallow CI checkout the epoch would be the\n  tip's author time — pin `--source-date-epoch` there.\n- **Open:** the publish/sign half — `release.yml`, `ghcr.io/beyond10x/b10x-substrate-wire`,\n  cosign, digest in `CHANGELOG.md` — waits on `story:signed-daemon-image`.\n\n## Correction — 2026-08-30\n\nThe two Python names this story was written against no longer exist. `ef22de0` (\"the bundle\npackager is cargo xtask package-bundle, and its Python is gone\") ported the packager and deleted\n`scripts/package-contract-bundle.py` and `scripts/test_package_contract_bundle.py`. Acceptance and\nthe progress notes above now name the Rust verb; the behaviour they assert is unchanged, and the\n`0.4.0` manifest digests recorded above were observed against the Python and have not been\nre-observed against the verb.\n\nCoverage is not lost in the move: `xtask/src/package.rs` carries 21 `#[test]`s (was 21 Python\ntests), and `cargo test --workspace` at `scripts/gate.sh:15` runs them. There is no\n`package-bundle` line in `scripts/gate.sh` and none is needed.\n\n**Still open, unchanged:** the publish/sign half — `release.yml`,\n`ghcr.io/beyond10x/b10x-substrate-wire`, cosign, digest in `CHANGELOG.md`. It waits on\n`story:signed-daemon-image`, which is a draft. This story cannot reach `implemented` until that\none is designed and built.\n\n## Unblocked — 2026-08-30\n\nThe paragraph above says this waits on `story:signed-daemon-image`, \"which is a draft\". It is\n`implemented` (closed at `9131c95`). `.github/workflows/release.yml` exists, has run, and published\n`ghcr.io/beyond10x/b10x-substrate-daemon:0.2.3` at\n`sha256:ab10158266b579d705ce8422c7d2a6e783cde950d30e100f61ca6befc4d0beda`, keyless-signed and\n`cosign verify`-ed (run 33304493276).\n\n**Nothing blocks this story.** What remains is its own second half: a bundle publish-and-sign job in\n`release.yml`, publishing `ghcr.io/beyond10x/b10x-substrate-wire:<bundle-version>` from\n`cargo xtask package-bundle`, with the digest in `CHANGELOG.md` and the artifact annotated\n`development` — a development bundle does not become a stable contract by being published (atlas\nADR 0019).\n\nOne thing the daemon release proved that this story must plan around: the workflow's final step\ncannot push to `main`, because `main` is protected and requires the `Full gate` check, which a\ndirect push can never satisfy. The daemon digest landed by pull request instead\n(`68d226f`). A bundle digest will need the same route or a different one, and the story should say\nwhich before the job is written.\n\n## Implementation — 2026-09-01 (publish/sign half)\n\n- `.github/workflows/release.yml` explicitly pins current development bundle `0.12.0`; a successor\n  must move that reviewable pin rather than being selected by directory order at release time.\n- One globally serialized release job refuses an existing GitHub release or daemon image tag. A\n  contract-bundle tag is reused only when it already resolves to the deterministic local digest; a\n  mismatched digest, authentication failure, transport failure or ambiguous registry answer refuses.\n  The canonical bundle tag is never an overwrite target.\n- The job runs `cargo xtask package-bundle`, checks the manifest digest and\n  `dev.b10x.contract.status=development` annotation locally, and uses pinned ORAS 1.3.3 to copy the\n  exact OCI Image Layout to `ghcr.io/beyond10x/b10x-substrate-wire:<bundle-version>` when absent.\n  It resolves the remote tag back to the packaged digest, checks the development annotation, and\n  proves anonymous retrieval before mutating the daemon-image tag; a visibility failure is retryable\n  without overwriting either artifact.\n- Cosign signs the bundle by digest with the tag-triggered workflow's OIDC identity and verifies the\n  exact certificate identity and GitHub Actions issuer before `gh release create`. Release notes\n  carry both immutable digests, both verification commands and the explicit non-stability statement.\n- Protected `main` is not pushed or bypassed. After both signatures verify, the workflow emits the\n  exact daemon and bundle digest lines for a workstation's `b10x-bot` pull request, which receives\n  the required `Full gate` check.\n- `xtask/tests/release_workflow.rs` fails closed offline on the current-bundle pin, exact-layout copy,\n  two write-once-or-identical-reuse checks, anonymous-access ordering, digest signing/verification order, development wording, protected-main\n  route and commit-pinned actions. A local ORAS layout-to-layout round trip reproduced manifest\n  `sha256:dd901e848c821aca7d55f7b8cf5ee893e1d99a1428b348e32e7ed1045a375319`; `contracts/` stayed\n  clean.\n- **Live evidence remains intentionally absent:** this implementation task forbids publishing or\n  cutting a tag. `ghcr.io/beyond10x/b10x-substrate-wire:0.12.0` was still not found on 2026-09-01.\n  The story stays `active` until an eligible release observes the remote digest and verified\n  signature and a fully gated bot pull request records that observed digest in `CHANGELOG.md`.\n\n## Live release evidence — 2026-09-01\n\n- Annotated tag `0.4.1` names fully gated protected-main merge `961be39`; release preflight passed against gate run `33457279192`.\n- The first publication attempt copied deterministic bundle `0.12.0` to public GHCR digest `sha256:dd901e848c821aca7d55f7b8cf5ee893e1d99a1428b348e32e7ed1045a375319`, then the authenticated immediate ORAS readback failed before signing. A failed-job rerun proved the tag exists at the identical digest but hit the same readback boundary; no daemon image or GitHub release was announced.\n- The follow-up changes GHCR login identity from the repository owner to `github.actor` and makes the post-publish proof explicitly anonymous with a bounded retry. Offline workflow tests pin both properties. Live signing, anonymous ORAS proof and the protected-main changelog pull request remain required before implementation status is earned.\n\n## 0.4.2 recovery evidence — 2026-09-01\n\n- Annotated tag `0.4.2` names protected-main merge `0687551`; the exact push gate run `33460337507` passed before the tag was created.\n- Release run `33460657507` anonymously resolved the existing write-once `0.12.0` tag at `sha256:dd901e848c821aca7d55f7b8cf5ee893e1d99a1428b348e32e7ed1045a375319`, then failed while asking `oras manifest fetch --descriptor` for the development/version annotations. OCI registry descriptors carry digest, media type and size; those annotations are members of the manifest content. The run stopped before signing, daemon publication or GitHub release creation.\n- The recovery change fetches the anonymous manifest after independently comparing the resolved digest, validates its two annotations, and adds an explicit `workflow_dispatch` recovery for an existing annotated tag. Recovery is accepted only from `refs/heads/main`, re-verifies the immutable tag, its main ancestry, its tagged `Cargo.toml` version and the exact recorded gate success, and never moves the tag.\n- The corrected command passed live, credential-free against the public GHCR object. All 10 focused workflow tests and `bash scripts/gate.sh` passed with 2,557 JSON documents across all 12 frozen bundles. Live keyless signing, daemon publication, GitHub release readback and the protected-main changelog pull request remain required before this story can move to `implemented`.\n"},"command":"update-entity","target":"01MEM0000000000000046"},"payload":{"actor":"human:timo","at":1788228398102,"causation":"cmd-protocol-artifact-body-append-story-contract-bundle-oci-artifact-1788228398102","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-append","event_id":"story:contract-bundle-oci-artifact@13#0~e4ea997ec0dae8dc","executor":null,"recorded_at":"2026-09-01T02:06:38Z"}}
{"entity":"story","version":1,"id":"contract-bundle-oci-artifact","revision":14,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"body":"# Story: The 0.4.0 contract bundle is a signed, digest-pinned OCI artifact\n\n## Outcome\n\nA consumer pins the wire contract by OCI digest instead of copying a directory tree.\n\n## Context\n\n`docs/plan/04-direct-byte-plane.md` § *Slice C* says Agent \"consumes an exact independently\nverified copy\"; the copy is made by hand. `docs/design/07-specification-and-conformance.md`\n§ *Bundle* already fixes the packaging: `bundle.json` lists media type, byte length and digest of\nevery bundle path except itself, and the outer OCI manifest digest pins `bundle.json`. Depends on\n`story:signed-daemon-image` for the release workflow it publishes through.\n\n## Acceptance\n\n`cargo xtask package-bundle <version>` produces a deterministic OCI layout from\n`contracts/substrate-wire/<version>/` without writing into `contracts/`, two runs yield\nbyte-identical manifests, and the release workflow publishes and signs it at\n`ghcr.io/beyond10x/b10x-substrate-wire:<bundle-version>` with the digest in `CHANGELOG.md`.\n\nEvidence that satisfies it:\n\n- `xtask/src/package.rs` tests: identical manifests on two runs; a one-byte change in\n  any bundle file changes the manifest digest — written failing-first against a stub that embeds\n  a timestamp;\n- `bundle.json` matches design 07's shape and a checker asserts each digest and byte length;\n- `check-contract-bundle-0.4.0.py` and every earlier checker stay green (invariant 6: the packager\n  reads the bundle; the bundle does not learn about the packager);\n- the artifact is annotated `development`; publication does not make the bundle stable (atlas ADR\n  0019 governs contract release);\n- `bash scripts/gate.sh` exits 0.\n\n## Progress — 2026-08-29 (packager half)\n\n- `cargo xtask package-bundle <version> --out <dir>`: OCI Image Layout (`oci-layout`,\n  `index.json`, `blobs/sha256/…`); config **is** `bundle.json` verbatim, media type\n  `application/vnd.b10x.substrate-wire.bundle.v1+json`, so the manifest digest pins\n  `bundle.json` (design 07 § *Bundle*); one layer per bundle file, layer digest = the `sha256`\n  already in `bundle.json`; annotations `org.opencontainers.image.version`,\n  `dev.b10x.contract.status=development`, `ref.name` on the index entry. Refuses `--out` inside\n  `contracts/`, non-empty `--out` without `--force`, symlinks, a file set disagreeing with\n  `bundle.json`.\n- 14 tests; determinism test written failing-first\n  against a timestamp-embedding stub (`FAILED (failures=1)`), then green. Two runs on `0.4.0` →\n  `sha256:f94d15fc116587d991aab6de5628f6ee5baf872af4e7c79d2a35e2b17a8485c4`, `diff -r` empty.\n  `oras manifest fetch --oci-layout` resolves the same digest; `oras pull` round-trip matches\n  `contracts/substrate-wire/0.4.0` byte for byte.\n- `check-contract-bundle-0.4.0.py` → exit 0; `git status contracts/` → clean (invariant 6).\n- Gated by `cargo test --workspace` (`scripts/gate.sh:15`), not by a gate line of its own.\n- The `posix-tar` archive `packaging.json` declares is now the last layer (media type\n  `application/vnd.b10x.substrate-wire.bundle.tar`, title `0.4.0.tar`): ustar, directory entries\n  0755 ahead of files 0644, uid/gid 0, empty names, bytewise order, every mtime =\n  `SOURCE_DATE_EPOCH` = author seconds of the last commit touching the bundle (`1787605082`,\n  commit `4b5d411`; `--source-date-epoch` overrides, absent both → exit 2). Per-file layers and the\n  config are byte-identical to before; the manifest digest moves by design to\n  `sha256:3758e80bc39f1eb03b15c69410608c9ef1d2ba8095c7e707c6988dbb5894ab00`\n  (archive `sha256:91fb5524…`, 880,640 bytes). 21 tests; `tar -xf` of the blob reproduces\n  `contracts/substrate-wire/0.4.0` byte for byte. On a shallow CI checkout the epoch would be the\n  tip's author time — pin `--source-date-epoch` there.\n- **Open:** the publish/sign half — `release.yml`, `ghcr.io/beyond10x/b10x-substrate-wire`,\n  cosign, digest in `CHANGELOG.md` — waits on `story:signed-daemon-image`.\n\n## Correction — 2026-08-30\n\nThe two Python names this story was written against no longer exist. `ef22de0` (\"the bundle\npackager is cargo xtask package-bundle, and its Python is gone\") ported the packager and deleted\n`scripts/package-contract-bundle.py` and `scripts/test_package_contract_bundle.py`. Acceptance and\nthe progress notes above now name the Rust verb; the behaviour they assert is unchanged, and the\n`0.4.0` manifest digests recorded above were observed against the Python and have not been\nre-observed against the verb.\n\nCoverage is not lost in the move: `xtask/src/package.rs` carries 21 `#[test]`s (was 21 Python\ntests), and `cargo test --workspace` at `scripts/gate.sh:15` runs them. There is no\n`package-bundle` line in `scripts/gate.sh` and none is needed.\n\n**Still open, unchanged:** the publish/sign half — `release.yml`,\n`ghcr.io/beyond10x/b10x-substrate-wire`, cosign, digest in `CHANGELOG.md`. It waits on\n`story:signed-daemon-image`, which is a draft. This story cannot reach `implemented` until that\none is designed and built.\n\n## Unblocked — 2026-08-30\n\nThe paragraph above says this waits on `story:signed-daemon-image`, \"which is a draft\". It is\n`implemented` (closed at `9131c95`). `.github/workflows/release.yml` exists, has run, and published\n`ghcr.io/beyond10x/b10x-substrate-daemon:0.2.3` at\n`sha256:ab10158266b579d705ce8422c7d2a6e783cde950d30e100f61ca6befc4d0beda`, keyless-signed and\n`cosign verify`-ed (run 33304493276).\n\n**Nothing blocks this story.** What remains is its own second half: a bundle publish-and-sign job in\n`release.yml`, publishing `ghcr.io/beyond10x/b10x-substrate-wire:<bundle-version>` from\n`cargo xtask package-bundle`, with the digest in `CHANGELOG.md` and the artifact annotated\n`development` — a development bundle does not become a stable contract by being published (atlas\nADR 0019).\n\nOne thing the daemon release proved that this story must plan around: the workflow's final step\ncannot push to `main`, because `main` is protected and requires the `Full gate` check, which a\ndirect push can never satisfy. The daemon digest landed by pull request instead\n(`68d226f`). A bundle digest will need the same route or a different one, and the story should say\nwhich before the job is written.\n\n## Implementation — 2026-09-01 (publish/sign half)\n\n- `.github/workflows/release.yml` explicitly pins current development bundle `0.12.0`; a successor\n  must move that reviewable pin rather than being selected by directory order at release time.\n- One globally serialized release job refuses an existing GitHub release or daemon image tag. A\n  contract-bundle tag is reused only when it already resolves to the deterministic local digest; a\n  mismatched digest, authentication failure, transport failure or ambiguous registry answer refuses.\n  The canonical bundle tag is never an overwrite target.\n- The job runs `cargo xtask package-bundle`, checks the manifest digest and\n  `dev.b10x.contract.status=development` annotation locally, and uses pinned ORAS 1.3.3 to copy the\n  exact OCI Image Layout to `ghcr.io/beyond10x/b10x-substrate-wire:<bundle-version>` when absent.\n  It resolves the remote tag back to the packaged digest, checks the development annotation, and\n  proves anonymous retrieval before mutating the daemon-image tag; a visibility failure is retryable\n  without overwriting either artifact.\n- Cosign signs the bundle by digest with the tag-triggered workflow's OIDC identity and verifies the\n  exact certificate identity and GitHub Actions issuer before `gh release create`. Release notes\n  carry both immutable digests, both verification commands and the explicit non-stability statement.\n- Protected `main` is not pushed or bypassed. After both signatures verify, the workflow emits the\n  exact daemon and bundle digest lines for a workstation's `b10x-bot` pull request, which receives\n  the required `Full gate` check.\n- `xtask/tests/release_workflow.rs` fails closed offline on the current-bundle pin, exact-layout copy,\n  two write-once-or-identical-reuse checks, anonymous-access ordering, digest signing/verification order, development wording, protected-main\n  route and commit-pinned actions. A local ORAS layout-to-layout round trip reproduced manifest\n  `sha256:dd901e848c821aca7d55f7b8cf5ee893e1d99a1428b348e32e7ed1045a375319`; `contracts/` stayed\n  clean.\n- **Live evidence remains intentionally absent:** this implementation task forbids publishing or\n  cutting a tag. `ghcr.io/beyond10x/b10x-substrate-wire:0.12.0` was still not found on 2026-09-01.\n  The story stays `active` until an eligible release observes the remote digest and verified\n  signature and a fully gated bot pull request records that observed digest in `CHANGELOG.md`.\n\n## Live release evidence — 2026-09-01\n\n- Annotated tag `0.4.1` names fully gated protected-main merge `961be39`; release preflight passed against gate run `33457279192`.\n- The first publication attempt copied deterministic bundle `0.12.0` to public GHCR digest `sha256:dd901e848c821aca7d55f7b8cf5ee893e1d99a1428b348e32e7ed1045a375319`, then the authenticated immediate ORAS readback failed before signing. A failed-job rerun proved the tag exists at the identical digest but hit the same readback boundary; no daemon image or GitHub release was announced.\n- The follow-up changes GHCR login identity from the repository owner to `github.actor` and makes the post-publish proof explicitly anonymous with a bounded retry. Offline workflow tests pin both properties. Live signing, anonymous ORAS proof and the protected-main changelog pull request remain required before implementation status is earned.\n\n## 0.4.2 recovery evidence — 2026-09-01\n\n- Annotated tag `0.4.2` names protected-main merge `0687551`; the exact push gate run `33460337507` passed before the tag was created.\n- Release run `33460657507` anonymously resolved the existing write-once `0.12.0` tag at `sha256:dd901e848c821aca7d55f7b8cf5ee893e1d99a1428b348e32e7ed1045a375319`, then failed while asking `oras manifest fetch --descriptor` for the development/version annotations. OCI registry descriptors carry digest, media type and size; those annotations are members of the manifest content. The run stopped before signing, daemon publication or GitHub release creation.\n- The recovery change fetches the anonymous manifest after independently comparing the resolved digest, validates its two annotations, and adds an explicit `workflow_dispatch` recovery for an existing annotated tag. Recovery is accepted only from `refs/heads/main`, re-verifies the immutable tag, its main ancestry, its tagged `Cargo.toml` version and the exact recorded gate success, and never moves the tag.\n- The corrected command passed live, credential-free against the public GHCR object. All 10 focused workflow tests and `bash scripts/gate.sh` passed with 2,557 JSON documents across all 12 frozen bundles. Live keyless signing, daemon publication, GitHub release readback and the protected-main changelog pull request remain required before this story can move to `implemented`.\n\n## Successful signed publication — 2026-09-01\n\n- Protected-main merge `693f7d5` carried the recovery workflow after Full gate run `33461841062` passed. Administrator-triggered workflow dispatch `33462225323` selected existing immutable tag `0.4.2`; its preflight re-verified annotated tag commit `0687551` and exact source gate `33460337507`.\n- The run reused development bundle `ghcr.io/beyond10x/b10x-substrate-wire:0.12.0` at `sha256:dd901e848c821aca7d55f7b8cf5ee893e1d99a1428b348e32e7ed1045a375319`, verified its manifest annotations without credentials, keyless-signed the digest, verified the certificate identity `https://github.com/beyond10x/substrate/.github/workflows/release.yml@refs/heads/main`, and proved anonymous retrieval.\n- The same run built tagged source `0687551`, passed the shipped-binary runtime-vector smoke test, and published `ghcr.io/beyond10x/b10x-substrate-daemon:0.4.2` at `sha256:1aac0c63c1f1e7dae2dff8f1f20a06b4d7f5461b61bb172b4a8a3f137cd2f6d1`. It keyless-signed and verified that digest under the same workflow identity and proved anonymous retrieval before announcement.\n- GitHub release `0.4.2` is public at `https://github.com/beyond10x/substrate/releases/tag/0.4.2`, non-draft, and API readback reports author `github-actions[bot]`. A separate local credential-free ORAS descriptor readback succeeded for both digests. This evidence branch records the two exact lines in `CHANGELOG.md`; the story remains active until that protected-main pull request merges.\n"},"args":{"changes":{"body":"# Story: The 0.4.0 contract bundle is a signed, digest-pinned OCI artifact\n\n## Outcome\n\nA consumer pins the wire contract by OCI digest instead of copying a directory tree.\n\n## Context\n\n`docs/plan/04-direct-byte-plane.md` § *Slice C* says Agent \"consumes an exact independently\nverified copy\"; the copy is made by hand. `docs/design/07-specification-and-conformance.md`\n§ *Bundle* already fixes the packaging: `bundle.json` lists media type, byte length and digest of\nevery bundle path except itself, and the outer OCI manifest digest pins `bundle.json`. Depends on\n`story:signed-daemon-image` for the release workflow it publishes through.\n\n## Acceptance\n\n`cargo xtask package-bundle <version>` produces a deterministic OCI layout from\n`contracts/substrate-wire/<version>/` without writing into `contracts/`, two runs yield\nbyte-identical manifests, and the release workflow publishes and signs it at\n`ghcr.io/beyond10x/b10x-substrate-wire:<bundle-version>` with the digest in `CHANGELOG.md`.\n\nEvidence that satisfies it:\n\n- `xtask/src/package.rs` tests: identical manifests on two runs; a one-byte change in\n  any bundle file changes the manifest digest — written failing-first against a stub that embeds\n  a timestamp;\n- `bundle.json` matches design 07's shape and a checker asserts each digest and byte length;\n- `check-contract-bundle-0.4.0.py` and every earlier checker stay green (invariant 6: the packager\n  reads the bundle; the bundle does not learn about the packager);\n- the artifact is annotated `development`; publication does not make the bundle stable (atlas ADR\n  0019 governs contract release);\n- `bash scripts/gate.sh` exits 0.\n\n## Progress — 2026-08-29 (packager half)\n\n- `cargo xtask package-bundle <version> --out <dir>`: OCI Image Layout (`oci-layout`,\n  `index.json`, `blobs/sha256/…`); config **is** `bundle.json` verbatim, media type\n  `application/vnd.b10x.substrate-wire.bundle.v1+json`, so the manifest digest pins\n  `bundle.json` (design 07 § *Bundle*); one layer per bundle file, layer digest = the `sha256`\n  already in `bundle.json`; annotations `org.opencontainers.image.version`,\n  `dev.b10x.contract.status=development`, `ref.name` on the index entry. Refuses `--out` inside\n  `contracts/`, non-empty `--out` without `--force`, symlinks, a file set disagreeing with\n  `bundle.json`.\n- 14 tests; determinism test written failing-first\n  against a timestamp-embedding stub (`FAILED (failures=1)`), then green. Two runs on `0.4.0` →\n  `sha256:f94d15fc116587d991aab6de5628f6ee5baf872af4e7c79d2a35e2b17a8485c4`, `diff -r` empty.\n  `oras manifest fetch --oci-layout` resolves the same digest; `oras pull` round-trip matches\n  `contracts/substrate-wire/0.4.0` byte for byte.\n- `check-contract-bundle-0.4.0.py` → exit 0; `git status contracts/` → clean (invariant 6).\n- Gated by `cargo test --workspace` (`scripts/gate.sh:15`), not by a gate line of its own.\n- The `posix-tar` archive `packaging.json` declares is now the last layer (media type\n  `application/vnd.b10x.substrate-wire.bundle.tar`, title `0.4.0.tar`): ustar, directory entries\n  0755 ahead of files 0644, uid/gid 0, empty names, bytewise order, every mtime =\n  `SOURCE_DATE_EPOCH` = author seconds of the last commit touching the bundle (`1787605082`,\n  commit `4b5d411`; `--source-date-epoch` overrides, absent both → exit 2). Per-file layers and the\n  config are byte-identical to before; the manifest digest moves by design to\n  `sha256:3758e80bc39f1eb03b15c69410608c9ef1d2ba8095c7e707c6988dbb5894ab00`\n  (archive `sha256:91fb5524…`, 880,640 bytes). 21 tests; `tar -xf` of the blob reproduces\n  `contracts/substrate-wire/0.4.0` byte for byte. On a shallow CI checkout the epoch would be the\n  tip's author time — pin `--source-date-epoch` there.\n- **Open:** the publish/sign half — `release.yml`, `ghcr.io/beyond10x/b10x-substrate-wire`,\n  cosign, digest in `CHANGELOG.md` — waits on `story:signed-daemon-image`.\n\n## Correction — 2026-08-30\n\nThe two Python names this story was written against no longer exist. `ef22de0` (\"the bundle\npackager is cargo xtask package-bundle, and its Python is gone\") ported the packager and deleted\n`scripts/package-contract-bundle.py` and `scripts/test_package_contract_bundle.py`. Acceptance and\nthe progress notes above now name the Rust verb; the behaviour they assert is unchanged, and the\n`0.4.0` manifest digests recorded above were observed against the Python and have not been\nre-observed against the verb.\n\nCoverage is not lost in the move: `xtask/src/package.rs` carries 21 `#[test]`s (was 21 Python\ntests), and `cargo test --workspace` at `scripts/gate.sh:15` runs them. There is no\n`package-bundle` line in `scripts/gate.sh` and none is needed.\n\n**Still open, unchanged:** the publish/sign half — `release.yml`,\n`ghcr.io/beyond10x/b10x-substrate-wire`, cosign, digest in `CHANGELOG.md`. It waits on\n`story:signed-daemon-image`, which is a draft. This story cannot reach `implemented` until that\none is designed and built.\n\n## Unblocked — 2026-08-30\n\nThe paragraph above says this waits on `story:signed-daemon-image`, \"which is a draft\". It is\n`implemented` (closed at `9131c95`). `.github/workflows/release.yml` exists, has run, and published\n`ghcr.io/beyond10x/b10x-substrate-daemon:0.2.3` at\n`sha256:ab10158266b579d705ce8422c7d2a6e783cde950d30e100f61ca6befc4d0beda`, keyless-signed and\n`cosign verify`-ed (run 33304493276).\n\n**Nothing blocks this story.** What remains is its own second half: a bundle publish-and-sign job in\n`release.yml`, publishing `ghcr.io/beyond10x/b10x-substrate-wire:<bundle-version>` from\n`cargo xtask package-bundle`, with the digest in `CHANGELOG.md` and the artifact annotated\n`development` — a development bundle does not become a stable contract by being published (atlas\nADR 0019).\n\nOne thing the daemon release proved that this story must plan around: the workflow's final step\ncannot push to `main`, because `main` is protected and requires the `Full gate` check, which a\ndirect push can never satisfy. The daemon digest landed by pull request instead\n(`68d226f`). A bundle digest will need the same route or a different one, and the story should say\nwhich before the job is written.\n\n## Implementation — 2026-09-01 (publish/sign half)\n\n- `.github/workflows/release.yml` explicitly pins current development bundle `0.12.0`; a successor\n  must move that reviewable pin rather than being selected by directory order at release time.\n- One globally serialized release job refuses an existing GitHub release or daemon image tag. A\n  contract-bundle tag is reused only when it already resolves to the deterministic local digest; a\n  mismatched digest, authentication failure, transport failure or ambiguous registry answer refuses.\n  The canonical bundle tag is never an overwrite target.\n- The job runs `cargo xtask package-bundle`, checks the manifest digest and\n  `dev.b10x.contract.status=development` annotation locally, and uses pinned ORAS 1.3.3 to copy the\n  exact OCI Image Layout to `ghcr.io/beyond10x/b10x-substrate-wire:<bundle-version>` when absent.\n  It resolves the remote tag back to the packaged digest, checks the development annotation, and\n  proves anonymous retrieval before mutating the daemon-image tag; a visibility failure is retryable\n  without overwriting either artifact.\n- Cosign signs the bundle by digest with the tag-triggered workflow's OIDC identity and verifies the\n  exact certificate identity and GitHub Actions issuer before `gh release create`. Release notes\n  carry both immutable digests, both verification commands and the explicit non-stability statement.\n- Protected `main` is not pushed or bypassed. After both signatures verify, the workflow emits the\n  exact daemon and bundle digest lines for a workstation's `b10x-bot` pull request, which receives\n  the required `Full gate` check.\n- `xtask/tests/release_workflow.rs` fails closed offline on the current-bundle pin, exact-layout copy,\n  two write-once-or-identical-reuse checks, anonymous-access ordering, digest signing/verification order, development wording, protected-main\n  route and commit-pinned actions. A local ORAS layout-to-layout round trip reproduced manifest\n  `sha256:dd901e848c821aca7d55f7b8cf5ee893e1d99a1428b348e32e7ed1045a375319`; `contracts/` stayed\n  clean.\n- **Live evidence remains intentionally absent:** this implementation task forbids publishing or\n  cutting a tag. `ghcr.io/beyond10x/b10x-substrate-wire:0.12.0` was still not found on 2026-09-01.\n  The story stays `active` until an eligible release observes the remote digest and verified\n  signature and a fully gated bot pull request records that observed digest in `CHANGELOG.md`.\n\n## Live release evidence — 2026-09-01\n\n- Annotated tag `0.4.1` names fully gated protected-main merge `961be39`; release preflight passed against gate run `33457279192`.\n- The first publication attempt copied deterministic bundle `0.12.0` to public GHCR digest `sha256:dd901e848c821aca7d55f7b8cf5ee893e1d99a1428b348e32e7ed1045a375319`, then the authenticated immediate ORAS readback failed before signing. A failed-job rerun proved the tag exists at the identical digest but hit the same readback boundary; no daemon image or GitHub release was announced.\n- The follow-up changes GHCR login identity from the repository owner to `github.actor` and makes the post-publish proof explicitly anonymous with a bounded retry. Offline workflow tests pin both properties. Live signing, anonymous ORAS proof and the protected-main changelog pull request remain required before implementation status is earned.\n\n## 0.4.2 recovery evidence — 2026-09-01\n\n- Annotated tag `0.4.2` names protected-main merge `0687551`; the exact push gate run `33460337507` passed before the tag was created.\n- Release run `33460657507` anonymously resolved the existing write-once `0.12.0` tag at `sha256:dd901e848c821aca7d55f7b8cf5ee893e1d99a1428b348e32e7ed1045a375319`, then failed while asking `oras manifest fetch --descriptor` for the development/version annotations. OCI registry descriptors carry digest, media type and size; those annotations are members of the manifest content. The run stopped before signing, daemon publication or GitHub release creation.\n- The recovery change fetches the anonymous manifest after independently comparing the resolved digest, validates its two annotations, and adds an explicit `workflow_dispatch` recovery for an existing annotated tag. Recovery is accepted only from `refs/heads/main`, re-verifies the immutable tag, its main ancestry, its tagged `Cargo.toml` version and the exact recorded gate success, and never moves the tag.\n- The corrected command passed live, credential-free against the public GHCR object. All 10 focused workflow tests and `bash scripts/gate.sh` passed with 2,557 JSON documents across all 12 frozen bundles. Live keyless signing, daemon publication, GitHub release readback and the protected-main changelog pull request remain required before this story can move to `implemented`.\n\n## Successful signed publication — 2026-09-01\n\n- Protected-main merge `693f7d5` carried the recovery workflow after Full gate run `33461841062` passed. Administrator-triggered workflow dispatch `33462225323` selected existing immutable tag `0.4.2`; its preflight re-verified annotated tag commit `0687551` and exact source gate `33460337507`.\n- The run reused development bundle `ghcr.io/beyond10x/b10x-substrate-wire:0.12.0` at `sha256:dd901e848c821aca7d55f7b8cf5ee893e1d99a1428b348e32e7ed1045a375319`, verified its manifest annotations without credentials, keyless-signed the digest, verified the certificate identity `https://github.com/beyond10x/substrate/.github/workflows/release.yml@refs/heads/main`, and proved anonymous retrieval.\n- The same run built tagged source `0687551`, passed the shipped-binary runtime-vector smoke test, and published `ghcr.io/beyond10x/b10x-substrate-daemon:0.4.2` at `sha256:1aac0c63c1f1e7dae2dff8f1f20a06b4d7f5461b61bb172b4a8a3f137cd2f6d1`. It keyless-signed and verified that digest under the same workflow identity and proved anonymous retrieval before announcement.\n- GitHub release `0.4.2` is public at `https://github.com/beyond10x/substrate/releases/tag/0.4.2`, non-draft, and API readback reports author `github-actions[bot]`. A separate local credential-free ORAS descriptor readback succeeded for both digests. This evidence branch records the two exact lines in `CHANGELOG.md`; the story remains active until that protected-main pull request merges.\n"},"command":"update-entity","target":"01MEM0000000000000046"},"payload":{"actor":"human:timo","at":1788229731911,"causation":"cmd-protocol-artifact-body-append-story-contract-bundle-oci-artifact-1788229731911","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-append","event_id":"story:contract-bundle-oci-artifact@14#0~d87a08084e3982f9","executor":null,"recorded_at":"2026-09-01T02:28:51Z"}}
{"entity":"story","version":1,"id":"contract-bundle-oci-artifact","revision":15,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"body":"# Story: The 0.4.0 contract bundle is a signed, digest-pinned OCI artifact\n\n## Outcome\n\nA consumer pins the wire contract by OCI digest instead of copying a directory tree.\n\n## Context\n\n`docs/plan/04-direct-byte-plane.md` § *Slice C* says Agent \"consumes an exact independently\nverified copy\"; the copy is made by hand. `docs/design/07-specification-and-conformance.md`\n§ *Bundle* already fixes the packaging: `bundle.json` lists media type, byte length and digest of\nevery bundle path except itself, and the outer OCI manifest digest pins `bundle.json`. Depends on\n`story:signed-daemon-image` for the release workflow it publishes through.\n\n## Acceptance\n\n`cargo xtask package-bundle <version>` produces a deterministic OCI layout from\n`contracts/substrate-wire/<version>/` without writing into `contracts/`, two runs yield\nbyte-identical manifests, and the release workflow publishes and signs it at\n`ghcr.io/beyond10x/b10x-substrate-wire:<bundle-version>` with the digest in `CHANGELOG.md`.\n\nEvidence that satisfies it:\n\n- `xtask/src/package.rs` tests: identical manifests on two runs; a one-byte change in\n  any bundle file changes the manifest digest — written failing-first against a stub that embeds\n  a timestamp;\n- `bundle.json` matches design 07's shape and a checker asserts each digest and byte length;\n- `check-contract-bundle-0.4.0.py` and every earlier checker stay green (invariant 6: the packager\n  reads the bundle; the bundle does not learn about the packager);\n- the artifact is annotated `development`; publication does not make the bundle stable (atlas ADR\n  0019 governs contract release);\n- `bash scripts/gate.sh` exits 0.\n\n## Progress — 2026-08-29 (packager half)\n\n- `cargo xtask package-bundle <version> --out <dir>`: OCI Image Layout (`oci-layout`,\n  `index.json`, `blobs/sha256/…`); config **is** `bundle.json` verbatim, media type\n  `application/vnd.b10x.substrate-wire.bundle.v1+json`, so the manifest digest pins\n  `bundle.json` (design 07 § *Bundle*); one layer per bundle file, layer digest = the `sha256`\n  already in `bundle.json`; annotations `org.opencontainers.image.version`,\n  `dev.b10x.contract.status=development`, `ref.name` on the index entry. Refuses `--out` inside\n  `contracts/`, non-empty `--out` without `--force`, symlinks, a file set disagreeing with\n  `bundle.json`.\n- 14 tests; determinism test written failing-first\n  against a timestamp-embedding stub (`FAILED (failures=1)`), then green. Two runs on `0.4.0` →\n  `sha256:f94d15fc116587d991aab6de5628f6ee5baf872af4e7c79d2a35e2b17a8485c4`, `diff -r` empty.\n  `oras manifest fetch --oci-layout` resolves the same digest; `oras pull` round-trip matches\n  `contracts/substrate-wire/0.4.0` byte for byte.\n- `check-contract-bundle-0.4.0.py` → exit 0; `git status contracts/` → clean (invariant 6).\n- Gated by `cargo test --workspace` (`scripts/gate.sh:15`), not by a gate line of its own.\n- The `posix-tar` archive `packaging.json` declares is now the last layer (media type\n  `application/vnd.b10x.substrate-wire.bundle.tar`, title `0.4.0.tar`): ustar, directory entries\n  0755 ahead of files 0644, uid/gid 0, empty names, bytewise order, every mtime =\n  `SOURCE_DATE_EPOCH` = author seconds of the last commit touching the bundle (`1787605082`,\n  commit `4b5d411`; `--source-date-epoch` overrides, absent both → exit 2). Per-file layers and the\n  config are byte-identical to before; the manifest digest moves by design to\n  `sha256:3758e80bc39f1eb03b15c69410608c9ef1d2ba8095c7e707c6988dbb5894ab00`\n  (archive `sha256:91fb5524…`, 880,640 bytes). 21 tests; `tar -xf` of the blob reproduces\n  `contracts/substrate-wire/0.4.0` byte for byte. On a shallow CI checkout the epoch would be the\n  tip's author time — pin `--source-date-epoch` there.\n- **Open:** the publish/sign half — `release.yml`, `ghcr.io/beyond10x/b10x-substrate-wire`,\n  cosign, digest in `CHANGELOG.md` — waits on `story:signed-daemon-image`.\n\n## Correction — 2026-08-30\n\nThe two Python names this story was written against no longer exist. `ef22de0` (\"the bundle\npackager is cargo xtask package-bundle, and its Python is gone\") ported the packager and deleted\n`scripts/package-contract-bundle.py` and `scripts/test_package_contract_bundle.py`. Acceptance and\nthe progress notes above now name the Rust verb; the behaviour they assert is unchanged, and the\n`0.4.0` manifest digests recorded above were observed against the Python and have not been\nre-observed against the verb.\n\nCoverage is not lost in the move: `xtask/src/package.rs` carries 21 `#[test]`s (was 21 Python\ntests), and `cargo test --workspace` at `scripts/gate.sh:15` runs them. There is no\n`package-bundle` line in `scripts/gate.sh` and none is needed.\n\n**Still open, unchanged:** the publish/sign half — `release.yml`,\n`ghcr.io/beyond10x/b10x-substrate-wire`, cosign, digest in `CHANGELOG.md`. It waits on\n`story:signed-daemon-image`, which is a draft. This story cannot reach `implemented` until that\none is designed and built.\n\n## Unblocked — 2026-08-30\n\nThe paragraph above says this waits on `story:signed-daemon-image`, \"which is a draft\". It is\n`implemented` (closed at `9131c95`). `.github/workflows/release.yml` exists, has run, and published\n`ghcr.io/beyond10x/b10x-substrate-daemon:0.2.3` at\n`sha256:ab10158266b579d705ce8422c7d2a6e783cde950d30e100f61ca6befc4d0beda`, keyless-signed and\n`cosign verify`-ed (run 33304493276).\n\n**Nothing blocks this story.** What remains is its own second half: a bundle publish-and-sign job in\n`release.yml`, publishing `ghcr.io/beyond10x/b10x-substrate-wire:<bundle-version>` from\n`cargo xtask package-bundle`, with the digest in `CHANGELOG.md` and the artifact annotated\n`development` — a development bundle does not become a stable contract by being published (atlas\nADR 0019).\n\nOne thing the daemon release proved that this story must plan around: the workflow's final step\ncannot push to `main`, because `main` is protected and requires the `Full gate` check, which a\ndirect push can never satisfy. The daemon digest landed by pull request instead\n(`68d226f`). A bundle digest will need the same route or a different one, and the story should say\nwhich before the job is written.\n\n## Implementation — 2026-09-01 (publish/sign half)\n\n- `.github/workflows/release.yml` explicitly pins current development bundle `0.12.0`; a successor\n  must move that reviewable pin rather than being selected by directory order at release time.\n- One globally serialized release job refuses an existing GitHub release or daemon image tag. A\n  contract-bundle tag is reused only when it already resolves to the deterministic local digest; a\n  mismatched digest, authentication failure, transport failure or ambiguous registry answer refuses.\n  The canonical bundle tag is never an overwrite target.\n- The job runs `cargo xtask package-bundle`, checks the manifest digest and\n  `dev.b10x.contract.status=development` annotation locally, and uses pinned ORAS 1.3.3 to copy the\n  exact OCI Image Layout to `ghcr.io/beyond10x/b10x-substrate-wire:<bundle-version>` when absent.\n  It resolves the remote tag back to the packaged digest, checks the development annotation, and\n  proves anonymous retrieval before mutating the daemon-image tag; a visibility failure is retryable\n  without overwriting either artifact.\n- Cosign signs the bundle by digest with the tag-triggered workflow's OIDC identity and verifies the\n  exact certificate identity and GitHub Actions issuer before `gh release create`. Release notes\n  carry both immutable digests, both verification commands and the explicit non-stability statement.\n- Protected `main` is not pushed or bypassed. After both signatures verify, the workflow emits the\n  exact daemon and bundle digest lines for a workstation's `b10x-bot` pull request, which receives\n  the required `Full gate` check.\n- `xtask/tests/release_workflow.rs` fails closed offline on the current-bundle pin, exact-layout copy,\n  two write-once-or-identical-reuse checks, anonymous-access ordering, digest signing/verification order, development wording, protected-main\n  route and commit-pinned actions. A local ORAS layout-to-layout round trip reproduced manifest\n  `sha256:dd901e848c821aca7d55f7b8cf5ee893e1d99a1428b348e32e7ed1045a375319`; `contracts/` stayed\n  clean.\n- **Live evidence remains intentionally absent:** this implementation task forbids publishing or\n  cutting a tag. `ghcr.io/beyond10x/b10x-substrate-wire:0.12.0` was still not found on 2026-09-01.\n  The story stays `active` until an eligible release observes the remote digest and verified\n  signature and a fully gated bot pull request records that observed digest in `CHANGELOG.md`.\n\n## Live release evidence — 2026-09-01\n\n- Annotated tag `0.4.1` names fully gated protected-main merge `961be39`; release preflight passed against gate run `33457279192`.\n- The first publication attempt copied deterministic bundle `0.12.0` to public GHCR digest `sha256:dd901e848c821aca7d55f7b8cf5ee893e1d99a1428b348e32e7ed1045a375319`, then the authenticated immediate ORAS readback failed before signing. A failed-job rerun proved the tag exists at the identical digest but hit the same readback boundary; no daemon image or GitHub release was announced.\n- The follow-up changes GHCR login identity from the repository owner to `github.actor` and makes the post-publish proof explicitly anonymous with a bounded retry. Offline workflow tests pin both properties. Live signing, anonymous ORAS proof and the protected-main changelog pull request remain required before implementation status is earned.\n\n## 0.4.2 recovery evidence — 2026-09-01\n\n- Annotated tag `0.4.2` names protected-main merge `0687551`; the exact push gate run `33460337507` passed before the tag was created.\n- Release run `33460657507` anonymously resolved the existing write-once `0.12.0` tag at `sha256:dd901e848c821aca7d55f7b8cf5ee893e1d99a1428b348e32e7ed1045a375319`, then failed while asking `oras manifest fetch --descriptor` for the development/version annotations. OCI registry descriptors carry digest, media type and size; those annotations are members of the manifest content. The run stopped before signing, daemon publication or GitHub release creation.\n- The recovery change fetches the anonymous manifest after independently comparing the resolved digest, validates its two annotations, and adds an explicit `workflow_dispatch` recovery for an existing annotated tag. Recovery is accepted only from `refs/heads/main`, re-verifies the immutable tag, its main ancestry, its tagged `Cargo.toml` version and the exact recorded gate success, and never moves the tag.\n- The corrected command passed live, credential-free against the public GHCR object. All 10 focused workflow tests and `bash scripts/gate.sh` passed with 2,557 JSON documents across all 12 frozen bundles. Live keyless signing, daemon publication, GitHub release readback and the protected-main changelog pull request remain required before this story can move to `implemented`.\n\n## Successful signed publication — 2026-09-01\n\n- Protected-main merge `693f7d5` carried the recovery workflow after Full gate run `33461841062` passed. Administrator-triggered workflow dispatch `33462225323` selected existing immutable tag `0.4.2`; its preflight re-verified annotated tag commit `0687551` and exact source gate `33460337507`.\n- The run reused development bundle `ghcr.io/beyond10x/b10x-substrate-wire:0.12.0` at `sha256:dd901e848c821aca7d55f7b8cf5ee893e1d99a1428b348e32e7ed1045a375319`, verified its manifest annotations without credentials, keyless-signed the digest, verified the certificate identity `https://github.com/beyond10x/substrate/.github/workflows/release.yml@refs/heads/main`, and proved anonymous retrieval.\n- The same run built tagged source `0687551`, passed the shipped-binary runtime-vector smoke test, and published `ghcr.io/beyond10x/b10x-substrate-daemon:0.4.2` at `sha256:1aac0c63c1f1e7dae2dff8f1f20a06b4d7f5461b61bb172b4a8a3f137cd2f6d1`. It keyless-signed and verified that digest under the same workflow identity and proved anonymous retrieval before announcement.\n- GitHub release `0.4.2` is public at `https://github.com/beyond10x/substrate/releases/tag/0.4.2`, non-draft, and API readback reports author `github-actions[bot]`. A separate local credential-free ORAS descriptor readback succeeded for both digests. This evidence branch records the two exact lines in `CHANGELOG.md`; the story remains active until that protected-main pull request merges.\n\n## Closure — 2026-09-01\n\n- Release evidence merged through protected `main` in PR #65 at `551bd7ef7a678951e09497e187608503738d98c6`.\n- The exact merge commit passed the Full gate in run `33463570207` (5m36s).\n- GitHub release `0.4.2`, daemon digest `sha256:1aac0c63c1f1e7dae2dff8f1f20a06b4d7f5461b61bb172b4a8a3f137cd2f6d1`, and development bundle digest `sha256:dd901e848c821aca7d55f7b8cf5ee893e1d99a1428b348e32e7ed1045a375319` are recorded in `CHANGELOG.md` and `STATUS.md` on `main`.\n- The release workflow verified both keyless signatures before publishing the GitHub release; anonymous digest readback succeeded for both artifacts.\n"},"args":{"changes":{"body":"# Story: The 0.4.0 contract bundle is a signed, digest-pinned OCI artifact\n\n## Outcome\n\nA consumer pins the wire contract by OCI digest instead of copying a directory tree.\n\n## Context\n\n`docs/plan/04-direct-byte-plane.md` § *Slice C* says Agent \"consumes an exact independently\nverified copy\"; the copy is made by hand. `docs/design/07-specification-and-conformance.md`\n§ *Bundle* already fixes the packaging: `bundle.json` lists media type, byte length and digest of\nevery bundle path except itself, and the outer OCI manifest digest pins `bundle.json`. Depends on\n`story:signed-daemon-image` for the release workflow it publishes through.\n\n## Acceptance\n\n`cargo xtask package-bundle <version>` produces a deterministic OCI layout from\n`contracts/substrate-wire/<version>/` without writing into `contracts/`, two runs yield\nbyte-identical manifests, and the release workflow publishes and signs it at\n`ghcr.io/beyond10x/b10x-substrate-wire:<bundle-version>` with the digest in `CHANGELOG.md`.\n\nEvidence that satisfies it:\n\n- `xtask/src/package.rs` tests: identical manifests on two runs; a one-byte change in\n  any bundle file changes the manifest digest — written failing-first against a stub that embeds\n  a timestamp;\n- `bundle.json` matches design 07's shape and a checker asserts each digest and byte length;\n- `check-contract-bundle-0.4.0.py` and every earlier checker stay green (invariant 6: the packager\n  reads the bundle; the bundle does not learn about the packager);\n- the artifact is annotated `development`; publication does not make the bundle stable (atlas ADR\n  0019 governs contract release);\n- `bash scripts/gate.sh` exits 0.\n\n## Progress — 2026-08-29 (packager half)\n\n- `cargo xtask package-bundle <version> --out <dir>`: OCI Image Layout (`oci-layout`,\n  `index.json`, `blobs/sha256/…`); config **is** `bundle.json` verbatim, media type\n  `application/vnd.b10x.substrate-wire.bundle.v1+json`, so the manifest digest pins\n  `bundle.json` (design 07 § *Bundle*); one layer per bundle file, layer digest = the `sha256`\n  already in `bundle.json`; annotations `org.opencontainers.image.version`,\n  `dev.b10x.contract.status=development`, `ref.name` on the index entry. Refuses `--out` inside\n  `contracts/`, non-empty `--out` without `--force`, symlinks, a file set disagreeing with\n  `bundle.json`.\n- 14 tests; determinism test written failing-first\n  against a timestamp-embedding stub (`FAILED (failures=1)`), then green. Two runs on `0.4.0` →\n  `sha256:f94d15fc116587d991aab6de5628f6ee5baf872af4e7c79d2a35e2b17a8485c4`, `diff -r` empty.\n  `oras manifest fetch --oci-layout` resolves the same digest; `oras pull` round-trip matches\n  `contracts/substrate-wire/0.4.0` byte for byte.\n- `check-contract-bundle-0.4.0.py` → exit 0; `git status contracts/` → clean (invariant 6).\n- Gated by `cargo test --workspace` (`scripts/gate.sh:15`), not by a gate line of its own.\n- The `posix-tar` archive `packaging.json` declares is now the last layer (media type\n  `application/vnd.b10x.substrate-wire.bundle.tar`, title `0.4.0.tar`): ustar, directory entries\n  0755 ahead of files 0644, uid/gid 0, empty names, bytewise order, every mtime =\n  `SOURCE_DATE_EPOCH` = author seconds of the last commit touching the bundle (`1787605082`,\n  commit `4b5d411`; `--source-date-epoch` overrides, absent both → exit 2). Per-file layers and the\n  config are byte-identical to before; the manifest digest moves by design to\n  `sha256:3758e80bc39f1eb03b15c69410608c9ef1d2ba8095c7e707c6988dbb5894ab00`\n  (archive `sha256:91fb5524…`, 880,640 bytes). 21 tests; `tar -xf` of the blob reproduces\n  `contracts/substrate-wire/0.4.0` byte for byte. On a shallow CI checkout the epoch would be the\n  tip's author time — pin `--source-date-epoch` there.\n- **Open:** the publish/sign half — `release.yml`, `ghcr.io/beyond10x/b10x-substrate-wire`,\n  cosign, digest in `CHANGELOG.md` — waits on `story:signed-daemon-image`.\n\n## Correction — 2026-08-30\n\nThe two Python names this story was written against no longer exist. `ef22de0` (\"the bundle\npackager is cargo xtask package-bundle, and its Python is gone\") ported the packager and deleted\n`scripts/package-contract-bundle.py` and `scripts/test_package_contract_bundle.py`. Acceptance and\nthe progress notes above now name the Rust verb; the behaviour they assert is unchanged, and the\n`0.4.0` manifest digests recorded above were observed against the Python and have not been\nre-observed against the verb.\n\nCoverage is not lost in the move: `xtask/src/package.rs` carries 21 `#[test]`s (was 21 Python\ntests), and `cargo test --workspace` at `scripts/gate.sh:15` runs them. There is no\n`package-bundle` line in `scripts/gate.sh` and none is needed.\n\n**Still open, unchanged:** the publish/sign half — `release.yml`,\n`ghcr.io/beyond10x/b10x-substrate-wire`, cosign, digest in `CHANGELOG.md`. It waits on\n`story:signed-daemon-image`, which is a draft. This story cannot reach `implemented` until that\none is designed and built.\n\n## Unblocked — 2026-08-30\n\nThe paragraph above says this waits on `story:signed-daemon-image`, \"which is a draft\". It is\n`implemented` (closed at `9131c95`). `.github/workflows/release.yml` exists, has run, and published\n`ghcr.io/beyond10x/b10x-substrate-daemon:0.2.3` at\n`sha256:ab10158266b579d705ce8422c7d2a6e783cde950d30e100f61ca6befc4d0beda`, keyless-signed and\n`cosign verify`-ed (run 33304493276).\n\n**Nothing blocks this story.** What remains is its own second half: a bundle publish-and-sign job in\n`release.yml`, publishing `ghcr.io/beyond10x/b10x-substrate-wire:<bundle-version>` from\n`cargo xtask package-bundle`, with the digest in `CHANGELOG.md` and the artifact annotated\n`development` — a development bundle does not become a stable contract by being published (atlas\nADR 0019).\n\nOne thing the daemon release proved that this story must plan around: the workflow's final step\ncannot push to `main`, because `main` is protected and requires the `Full gate` check, which a\ndirect push can never satisfy. The daemon digest landed by pull request instead\n(`68d226f`). A bundle digest will need the same route or a different one, and the story should say\nwhich before the job is written.\n\n## Implementation — 2026-09-01 (publish/sign half)\n\n- `.github/workflows/release.yml` explicitly pins current development bundle `0.12.0`; a successor\n  must move that reviewable pin rather than being selected by directory order at release time.\n- One globally serialized release job refuses an existing GitHub release or daemon image tag. A\n  contract-bundle tag is reused only when it already resolves to the deterministic local digest; a\n  mismatched digest, authentication failure, transport failure or ambiguous registry answer refuses.\n  The canonical bundle tag is never an overwrite target.\n- The job runs `cargo xtask package-bundle`, checks the manifest digest and\n  `dev.b10x.contract.status=development` annotation locally, and uses pinned ORAS 1.3.3 to copy the\n  exact OCI Image Layout to `ghcr.io/beyond10x/b10x-substrate-wire:<bundle-version>` when absent.\n  It resolves the remote tag back to the packaged digest, checks the development annotation, and\n  proves anonymous retrieval before mutating the daemon-image tag; a visibility failure is retryable\n  without overwriting either artifact.\n- Cosign signs the bundle by digest with the tag-triggered workflow's OIDC identity and verifies the\n  exact certificate identity and GitHub Actions issuer before `gh release create`. Release notes\n  carry both immutable digests, both verification commands and the explicit non-stability statement.\n- Protected `main` is not pushed or bypassed. After both signatures verify, the workflow emits the\n  exact daemon and bundle digest lines for a workstation's `b10x-bot` pull request, which receives\n  the required `Full gate` check.\n- `xtask/tests/release_workflow.rs` fails closed offline on the current-bundle pin, exact-layout copy,\n  two write-once-or-identical-reuse checks, anonymous-access ordering, digest signing/verification order, development wording, protected-main\n  route and commit-pinned actions. A local ORAS layout-to-layout round trip reproduced manifest\n  `sha256:dd901e848c821aca7d55f7b8cf5ee893e1d99a1428b348e32e7ed1045a375319`; `contracts/` stayed\n  clean.\n- **Live evidence remains intentionally absent:** this implementation task forbids publishing or\n  cutting a tag. `ghcr.io/beyond10x/b10x-substrate-wire:0.12.0` was still not found on 2026-09-01.\n  The story stays `active` until an eligible release observes the remote digest and verified\n  signature and a fully gated bot pull request records that observed digest in `CHANGELOG.md`.\n\n## Live release evidence — 2026-09-01\n\n- Annotated tag `0.4.1` names fully gated protected-main merge `961be39`; release preflight passed against gate run `33457279192`.\n- The first publication attempt copied deterministic bundle `0.12.0` to public GHCR digest `sha256:dd901e848c821aca7d55f7b8cf5ee893e1d99a1428b348e32e7ed1045a375319`, then the authenticated immediate ORAS readback failed before signing. A failed-job rerun proved the tag exists at the identical digest but hit the same readback boundary; no daemon image or GitHub release was announced.\n- The follow-up changes GHCR login identity from the repository owner to `github.actor` and makes the post-publish proof explicitly anonymous with a bounded retry. Offline workflow tests pin both properties. Live signing, anonymous ORAS proof and the protected-main changelog pull request remain required before implementation status is earned.\n\n## 0.4.2 recovery evidence — 2026-09-01\n\n- Annotated tag `0.4.2` names protected-main merge `0687551`; the exact push gate run `33460337507` passed before the tag was created.\n- Release run `33460657507` anonymously resolved the existing write-once `0.12.0` tag at `sha256:dd901e848c821aca7d55f7b8cf5ee893e1d99a1428b348e32e7ed1045a375319`, then failed while asking `oras manifest fetch --descriptor` for the development/version annotations. OCI registry descriptors carry digest, media type and size; those annotations are members of the manifest content. The run stopped before signing, daemon publication or GitHub release creation.\n- The recovery change fetches the anonymous manifest after independently comparing the resolved digest, validates its two annotations, and adds an explicit `workflow_dispatch` recovery for an existing annotated tag. Recovery is accepted only from `refs/heads/main`, re-verifies the immutable tag, its main ancestry, its tagged `Cargo.toml` version and the exact recorded gate success, and never moves the tag.\n- The corrected command passed live, credential-free against the public GHCR object. All 10 focused workflow tests and `bash scripts/gate.sh` passed with 2,557 JSON documents across all 12 frozen bundles. Live keyless signing, daemon publication, GitHub release readback and the protected-main changelog pull request remain required before this story can move to `implemented`.\n\n## Successful signed publication — 2026-09-01\n\n- Protected-main merge `693f7d5` carried the recovery workflow after Full gate run `33461841062` passed. Administrator-triggered workflow dispatch `33462225323` selected existing immutable tag `0.4.2`; its preflight re-verified annotated tag commit `0687551` and exact source gate `33460337507`.\n- The run reused development bundle `ghcr.io/beyond10x/b10x-substrate-wire:0.12.0` at `sha256:dd901e848c821aca7d55f7b8cf5ee893e1d99a1428b348e32e7ed1045a375319`, verified its manifest annotations without credentials, keyless-signed the digest, verified the certificate identity `https://github.com/beyond10x/substrate/.github/workflows/release.yml@refs/heads/main`, and proved anonymous retrieval.\n- The same run built tagged source `0687551`, passed the shipped-binary runtime-vector smoke test, and published `ghcr.io/beyond10x/b10x-substrate-daemon:0.4.2` at `sha256:1aac0c63c1f1e7dae2dff8f1f20a06b4d7f5461b61bb172b4a8a3f137cd2f6d1`. It keyless-signed and verified that digest under the same workflow identity and proved anonymous retrieval before announcement.\n- GitHub release `0.4.2` is public at `https://github.com/beyond10x/substrate/releases/tag/0.4.2`, non-draft, and API readback reports author `github-actions[bot]`. A separate local credential-free ORAS descriptor readback succeeded for both digests. This evidence branch records the two exact lines in `CHANGELOG.md`; the story remains active until that protected-main pull request merges.\n\n## Closure — 2026-09-01\n\n- Release evidence merged through protected `main` in PR #65 at `551bd7ef7a678951e09497e187608503738d98c6`.\n- The exact merge commit passed the Full gate in run `33463570207` (5m36s).\n- GitHub release `0.4.2`, daemon digest `sha256:1aac0c63c1f1e7dae2dff8f1f20a06b4d7f5461b61bb172b4a8a3f137cd2f6d1`, and development bundle digest `sha256:dd901e848c821aca7d55f7b8cf5ee893e1d99a1428b348e32e7ed1045a375319` are recorded in `CHANGELOG.md` and `STATUS.md` on `main`.\n- The release workflow verified both keyless signatures before publishing the GitHub release; anonymous digest readback succeeded for both artifacts.\n"},"command":"update-entity","target":"01MEM0000000000000046"},"payload":{"actor":"human:timo","at":1788230933426,"causation":"cmd-protocol-artifact-body-append-story-contract-bundle-oci-artifact-1788230933426","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-append","event_id":"story:contract-bundle-oci-artifact@15#0~927f9c44b9a22a00","executor":null,"recorded_at":"2026-09-01T02:48:53Z"}}
{"entity":"story","version":1,"id":"contract-bundle-oci-artifact","revision":16,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":3}}","target":"01MEM0000000000000046","to":"implemented"},"payload":{"actor":"human:timo","at":1788230938519,"causation":"cmd-move-story-contract-bundle-oci-artifact-1788230938519","change":{"change":"moved","decided_on":{"recorded":{"test_result":3}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:contract-bundle-oci-artifact@16#0~5dd26ec998b206cd","executor":null,"recorded_at":"2026-09-01T02:48:58Z"}}
{"entity":"story","version":1,"id":"promote-development-contract-frontier","revision":4,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000115","to":"active"},"payload":{"actor":"human:timo","at":1788231949646,"causation":"cmd-move-story-promote-development-contract-frontier-1788231949646","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:promote-development-contract-frontier@4#0~fd94195065decc91","executor":null,"recorded_at":"2026-09-01T03:05:49Z"}}
{"entity":"story","version":1,"id":"promote-development-contract-frontier","revision":5,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"body":"# Story: The daemon advertises the implemented contract frontier\n\n## Outcome\n\nThe daemon advertises the newest fully implemented, gated additive contract bundle instead of substrate-wire/0.4.0, and a consumer can pin that claim without learning repository internals.\n\n## Context\n\nDevelopment bundles through 0.11.0 are already implemented while the header remains 0.4.0. The substrate-hardening worktree is preparing an additional 0.12.0 scoped-workspace-access successor and SDK support. This story selects the actual current successor when its dependencies merge; it does not assume an unmerged bundle number.\n\n## Acceptance\n\nBefore the header moves, an ADR or accepted design names the affected consumers and migration order. Every advertised operation, schema, refusal, fact and executable vector passes the shipped-binary clean-room lane. A clean-room Rust SDK fixture pins the new claim and rejects both an older and an unknown claim. The header changes atomically with release notes and consumer notification. Frozen bundles remain byte-identical, and the promoted bundle is still described as development unless a separate stability decision says otherwise.\n\n## Out of Scope\n\nNew capability behavior, contract stability, protocol identifier renames and a generic version-negotiation protocol.\n\n## Implementation evidence — 2026-09-01\n\n- Atlas ADR 0019 names the daemon, Rust SDK and Harness migration order and authorises the exact `0.11.0`-to-`0.12.0` lineage bridge.\n- The wire crate owns one atomic advertised name/digest pair; daemon and SDK consume it, and tests hash the frozen inner `contracts/substrate-wire/0.12.0/bundle.json` bytes.\n- `cargo xtask check-bundle 0.12.0` now compares all 33 route declarations with `0.11.0` and re-runs the quota/metrics acceptance inventory. Mutation tests prove changed route authority and removed behavior are refused.\n- The shipped-binary clean-room runner probes every declared HTTP and WebSocket operation, asserts the promoted pair on every response or upgrade, and completed 68 portable cases plus 95 delegated cases.\n- The SDK accepts only the exact promoted pair and refuses missing, older, unknown and wrong-digest claims before operation-body decoding.\n- `bash scripts/gate.sh`, `bash scripts/delegated-lane.sh`, `npm run typecheck` and `npm run build` passed in the implementation worktree.\n- Public release notes and website pages distinguish the inner contract digest from the outer signed OCI digest and continue to label the bundle development.\n- Harness compatibility observation remains required before this story moves from active to implemented.\n"},"args":{"changes":{"body":"# Story: The daemon advertises the implemented contract frontier\n\n## Outcome\n\nThe daemon advertises the newest fully implemented, gated additive contract bundle instead of substrate-wire/0.4.0, and a consumer can pin that claim without learning repository internals.\n\n## Context\n\nDevelopment bundles through 0.11.0 are already implemented while the header remains 0.4.0. The substrate-hardening worktree is preparing an additional 0.12.0 scoped-workspace-access successor and SDK support. This story selects the actual current successor when its dependencies merge; it does not assume an unmerged bundle number.\n\n## Acceptance\n\nBefore the header moves, an ADR or accepted design names the affected consumers and migration order. Every advertised operation, schema, refusal, fact and executable vector passes the shipped-binary clean-room lane. A clean-room Rust SDK fixture pins the new claim and rejects both an older and an unknown claim. The header changes atomically with release notes and consumer notification. Frozen bundles remain byte-identical, and the promoted bundle is still described as development unless a separate stability decision says otherwise.\n\n## Out of Scope\n\nNew capability behavior, contract stability, protocol identifier renames and a generic version-negotiation protocol.\n\n## Implementation evidence — 2026-09-01\n\n- Atlas ADR 0019 names the daemon, Rust SDK and Harness migration order and authorises the exact `0.11.0`-to-`0.12.0` lineage bridge.\n- The wire crate owns one atomic advertised name/digest pair; daemon and SDK consume it, and tests hash the frozen inner `contracts/substrate-wire/0.12.0/bundle.json` bytes.\n- `cargo xtask check-bundle 0.12.0` now compares all 33 route declarations with `0.11.0` and re-runs the quota/metrics acceptance inventory. Mutation tests prove changed route authority and removed behavior are refused.\n- The shipped-binary clean-room runner probes every declared HTTP and WebSocket operation, asserts the promoted pair on every response or upgrade, and completed 68 portable cases plus 95 delegated cases.\n- The SDK accepts only the exact promoted pair and refuses missing, older, unknown and wrong-digest claims before operation-body decoding.\n- `bash scripts/gate.sh`, `bash scripts/delegated-lane.sh`, `npm run typecheck` and `npm run build` passed in the implementation worktree.\n- Public release notes and website pages distinguish the inner contract digest from the outer signed OCI digest and continue to label the bundle development.\n- Harness compatibility observation remains required before this story moves from active to implemented.\n"},"command":"update-entity","target":"01MEM0000000000000115"},"payload":{"actor":"human:timo","at":1788233055291,"causation":"cmd-protocol-artifact-body-append-story-promote-development-contract-frontier-1788233055291","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-append","event_id":"story:promote-development-contract-frontier@5#0~75dede36c842a351","executor":null,"recorded_at":"2026-09-01T03:24:15Z"}}
{"entity":"story","version":1,"id":"promote-development-contract-frontier","revision":6,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"body":"# Story: The daemon advertises the implemented contract frontier\n\n## Outcome\n\nThe daemon advertises the newest fully implemented, gated additive contract bundle instead of substrate-wire/0.4.0, and a consumer can pin that claim without learning repository internals.\n\n## Context\n\nDevelopment bundles through 0.11.0 are already implemented while the header remains 0.4.0. The substrate-hardening worktree is preparing an additional 0.12.0 scoped-workspace-access successor and SDK support. This story selects the actual current successor when its dependencies merge; it does not assume an unmerged bundle number.\n\n## Acceptance\n\nBefore the header moves, an ADR or accepted design names the affected consumers and migration order. Every advertised operation, schema, refusal, fact and executable vector passes the shipped-binary clean-room lane. A clean-room Rust SDK fixture pins the new claim and rejects both an older and an unknown claim. The header changes atomically with release notes and consumer notification. Frozen bundles remain byte-identical, and the promoted bundle is still described as development unless a separate stability decision says otherwise.\n\n## Out of Scope\n\nNew capability behavior, contract stability, protocol identifier renames and a generic version-negotiation protocol.\n\n## Implementation evidence — 2026-09-01\n\n- Atlas ADR 0019 names the daemon, Rust SDK and Harness migration order and authorises the exact `0.11.0`-to-`0.12.0` lineage bridge.\n- The wire crate owns one atomic advertised name/digest pair; daemon and SDK consume it, and tests hash the frozen inner `contracts/substrate-wire/0.12.0/bundle.json` bytes.\n- `cargo xtask check-bundle 0.12.0` now compares all 33 route declarations with `0.11.0` and re-runs the quota/metrics acceptance inventory. Mutation tests prove changed route authority and removed behavior are refused.\n- The shipped-binary clean-room runner probes every declared HTTP and WebSocket operation, asserts the promoted pair on every response or upgrade, and completed 68 portable cases plus 95 delegated cases.\n- The SDK accepts only the exact promoted pair and refuses missing, older, unknown and wrong-digest claims before operation-body decoding.\n- `bash scripts/gate.sh`, `bash scripts/delegated-lane.sh`, `npm run typecheck` and `npm run build` passed in the implementation worktree.\n- Public release notes and website pages distinguish the inner contract digest from the outer signed OCI digest and continue to label the bundle development.\n- Harness compatibility observation remains required before this story moves from active to implemented.\n\n## Consumer observation — 2026-09-01\n\n- Substrate promotion merged through protected `main` as `79d3e4b`; exact-main full-gate run `33466513213` concluded success.\n- Harness kept its released Substrate `0.4.0` host/wire pin. Its standing live Unix-socket test against `79d3e4b` proved workspace create, write and read compatibility.\n- The observation found that Harness published `run` while always requesting `resource-usage` on a host where Substrate correctly withheld that fact and answered `exec.metrics-unserved`. Harness PR 9 made publication require the exact fact instead of advertising an unusable tool.\n- Harness compatibility fix merged as `c587bcf`; exact-main run `33467112033` passed the full gate and MSRV 1.97 job. No Metaharness or product-composition change was required.\n"},"args":{"changes":{"body":"# Story: The daemon advertises the implemented contract frontier\n\n## Outcome\n\nThe daemon advertises the newest fully implemented, gated additive contract bundle instead of substrate-wire/0.4.0, and a consumer can pin that claim without learning repository internals.\n\n## Context\n\nDevelopment bundles through 0.11.0 are already implemented while the header remains 0.4.0. The substrate-hardening worktree is preparing an additional 0.12.0 scoped-workspace-access successor and SDK support. This story selects the actual current successor when its dependencies merge; it does not assume an unmerged bundle number.\n\n## Acceptance\n\nBefore the header moves, an ADR or accepted design names the affected consumers and migration order. Every advertised operation, schema, refusal, fact and executable vector passes the shipped-binary clean-room lane. A clean-room Rust SDK fixture pins the new claim and rejects both an older and an unknown claim. The header changes atomically with release notes and consumer notification. Frozen bundles remain byte-identical, and the promoted bundle is still described as development unless a separate stability decision says otherwise.\n\n## Out of Scope\n\nNew capability behavior, contract stability, protocol identifier renames and a generic version-negotiation protocol.\n\n## Implementation evidence — 2026-09-01\n\n- Atlas ADR 0019 names the daemon, Rust SDK and Harness migration order and authorises the exact `0.11.0`-to-`0.12.0` lineage bridge.\n- The wire crate owns one atomic advertised name/digest pair; daemon and SDK consume it, and tests hash the frozen inner `contracts/substrate-wire/0.12.0/bundle.json` bytes.\n- `cargo xtask check-bundle 0.12.0` now compares all 33 route declarations with `0.11.0` and re-runs the quota/metrics acceptance inventory. Mutation tests prove changed route authority and removed behavior are refused.\n- The shipped-binary clean-room runner probes every declared HTTP and WebSocket operation, asserts the promoted pair on every response or upgrade, and completed 68 portable cases plus 95 delegated cases.\n- The SDK accepts only the exact promoted pair and refuses missing, older, unknown and wrong-digest claims before operation-body decoding.\n- `bash scripts/gate.sh`, `bash scripts/delegated-lane.sh`, `npm run typecheck` and `npm run build` passed in the implementation worktree.\n- Public release notes and website pages distinguish the inner contract digest from the outer signed OCI digest and continue to label the bundle development.\n- Harness compatibility observation remains required before this story moves from active to implemented.\n\n## Consumer observation — 2026-09-01\n\n- Substrate promotion merged through protected `main` as `79d3e4b`; exact-main full-gate run `33466513213` concluded success.\n- Harness kept its released Substrate `0.4.0` host/wire pin. Its standing live Unix-socket test against `79d3e4b` proved workspace create, write and read compatibility.\n- The observation found that Harness published `run` while always requesting `resource-usage` on a host where Substrate correctly withheld that fact and answered `exec.metrics-unserved`. Harness PR 9 made publication require the exact fact instead of advertising an unusable tool.\n- Harness compatibility fix merged as `c587bcf`; exact-main run `33467112033` passed the full gate and MSRV 1.97 job. No Metaharness or product-composition change was required.\n"},"command":"update-entity","target":"01MEM0000000000000115"},"payload":{"actor":"human:timo","at":1788234237552,"causation":"cmd-protocol-artifact-body-append-story-promote-development-contract-frontier-1788234237552","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-append","event_id":"story:promote-development-contract-frontier@6#0~83b8677cd4eeddc7","executor":null,"recorded_at":"2026-09-01T03:43:57Z"}}
{"entity":"story","version":1,"id":"promote-development-contract-frontier","revision":6,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"https://github.com/beyond10x/substrate/actions/runs/33466513213","source":"Substrate Full gate on merged main 79d3e4b","target":"01MEM0000000000000115"},"payload":{"actor":"human:timo","at":1788234256000,"causation":"cmd-evidence-story-promote-development-contract-frontier-test_result-1788234256149","change":{"change":"evidence","kind":"test_result","reference":"https://github.com/beyond10x/substrate/actions/runs/33466513213","source":"Substrate Full gate on merged main 79d3e4b"},"correlation":"protocol-artifact-evidence","event_id":"story:promote-development-contract-frontier@6#0~e40e5871690b9eb3","executor":null,"recorded_at":"2026-09-01T03:44:16Z"}}
{"entity":"story","version":1,"id":"promote-development-contract-frontier","revision":6,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"https://github.com/beyond10x/harness/actions/runs/33467112033","source":"Harness Gate and MSRV on merged main c587bcf","target":"01MEM0000000000000115"},"payload":{"actor":"human:timo","at":1788234256000,"causation":"cmd-evidence-story-promote-development-contract-frontier-test_result-1788234256273","change":{"change":"evidence","kind":"test_result","reference":"https://github.com/beyond10x/harness/actions/runs/33467112033","source":"Harness Gate and MSRV on merged main c587bcf"},"correlation":"protocol-artifact-evidence","event_id":"story:promote-development-contract-frontier@6#0~5c661340980ca1dd","executor":null,"recorded_at":"2026-09-01T03:44:16Z"}}
{"entity":"story","version":1,"id":"promote-development-contract-frontier","revision":7,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":2}}","target":"01MEM0000000000000115","to":"implemented"},"payload":{"actor":"human:timo","at":1788234256481,"causation":"cmd-move-story-promote-development-contract-frontier-1788234256481","change":{"change":"moved","decided_on":{"recorded":{"test_result":2}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:promote-development-contract-frontier@7#0~853d440b07dc61ff","executor":null,"recorded_at":"2026-09-01T03:44:16Z"}}
{"entity":"story","version":1,"id":"sdk-promoted-contract-parity","revision":5,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000148","to":"active"},"payload":{"actor":"human:timo","at":1788234949680,"causation":"cmd-move-story-sdk-promoted-contract-parity-1788234949680","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:sdk-promoted-contract-parity@5#0~119433ba78c8f2eb","executor":null,"recorded_at":"2026-09-01T03:55:49Z"}}
{"entity":"story","version":1,"id":"sdk-promoted-contract-parity","revision":5,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"scripts/gate.sh","source":"bash scripts/gate.sh","target":"01MEM0000000000000148"},"payload":{"actor":"human:timo","at":1788236515000,"causation":"cmd-evidence-story-sdk-promoted-contract-parity-test_result-1788236515923","change":{"change":"evidence","kind":"test_result","reference":"scripts/gate.sh","source":"bash scripts/gate.sh"},"correlation":"protocol-artifact-evidence","event_id":"story:sdk-promoted-contract-parity@5#0~94dc9a37229213fb","executor":null,"recorded_at":"2026-09-01T04:21:55Z"}}
{"entity":"story","version":1,"id":"sdk-promoted-contract-parity","revision":5,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"scripts/delegated-lane.sh","source":"bash scripts/delegated-lane.sh","target":"01MEM0000000000000148"},"payload":{"actor":"human:timo","at":1788236515000,"causation":"cmd-evidence-story-sdk-promoted-contract-parity-test_result-1788236516054","change":{"change":"evidence","kind":"test_result","reference":"scripts/delegated-lane.sh","source":"bash scripts/delegated-lane.sh"},"correlation":"protocol-artifact-evidence","event_id":"story:sdk-promoted-contract-parity@5#0~a74d3f1907672533","executor":null,"recorded_at":"2026-09-01T04:21:55Z"}}
{"entity":"story","version":1,"id":"sdk-promoted-contract-parity","revision":6,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":2}}","target":"01MEM0000000000000148","to":"implemented"},"payload":{"actor":"human:timo","at":1788236522608,"causation":"cmd-move-story-sdk-promoted-contract-parity-1788236522608","change":{"change":"moved","decided_on":{"recorded":{"test_result":2}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:sdk-promoted-contract-parity@6#0~ec538e1b87aedde1","executor":null,"recorded_at":"2026-09-01T04:22:02Z"}}
{"entity":"story","version":1,"id":"mcp-managed-disposable-daemon","revision":5,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000097","to":"active"},"payload":{"actor":"human:timo","at":1788237023795,"causation":"cmd-move-story-mcp-managed-disposable-daemon-1788237023795","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:mcp-managed-disposable-daemon@5#0~025921c04218a40c","executor":null,"recorded_at":"2026-09-01T04:30:23Z"}}
{"entity":"story","version":1,"id":"mcp-managed-disposable-daemon","revision":5,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"scripts/gate.sh","source":"bash scripts/gate.sh","target":"01MEM0000000000000097"},"payload":{"actor":"human:timo","at":1788239639000,"causation":"cmd-evidence-story-mcp-managed-disposable-daemon-test_result-1788239639797","change":{"change":"evidence","kind":"test_result","reference":"scripts/gate.sh","source":"bash scripts/gate.sh"},"correlation":"protocol-artifact-evidence","event_id":"story:mcp-managed-disposable-daemon@5#0~5d47ed86771d5d64","executor":null,"recorded_at":"2026-09-01T05:13:59Z"}}
{"entity":"story","version":1,"id":"mcp-managed-disposable-daemon","revision":5,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"scripts/delegated-lane.sh","source":"bash scripts/delegated-lane.sh","target":"01MEM0000000000000097"},"payload":{"actor":"human:timo","at":1788239639000,"causation":"cmd-evidence-story-mcp-managed-disposable-daemon-test_result-1788239639933","change":{"change":"evidence","kind":"test_result","reference":"scripts/delegated-lane.sh","source":"bash scripts/delegated-lane.sh"},"correlation":"protocol-artifact-evidence","event_id":"story:mcp-managed-disposable-daemon@5#0~90b921e036183edc","executor":null,"recorded_at":"2026-09-01T05:13:59Z"}}
{"entity":"story","version":1,"id":"mcp-managed-disposable-daemon","revision":5,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"crates/substrate-mcp/tests/stdio.rs","source":"containerized stdio clean-room journey","target":"01MEM0000000000000097"},"payload":{"actor":"human:timo","at":1788239639000,"causation":"cmd-evidence-story-mcp-managed-disposable-daemon-test_result-1788239640058","change":{"change":"evidence","kind":"test_result","reference":"crates/substrate-mcp/tests/stdio.rs","source":"containerized stdio clean-room journey"},"correlation":"protocol-artifact-evidence","event_id":"story:mcp-managed-disposable-daemon@5#0~e2fcf4a6afb4d035","executor":null,"recorded_at":"2026-09-01T05:13:59Z"}}
{"entity":"story","version":1,"id":"mcp-managed-disposable-daemon","revision":5,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"xtask/src/mcp_smoke.rs","source":"cargo xtask mcp-codex-smoke","target":"01MEM0000000000000097"},"payload":{"actor":"human:timo","at":1788239640000,"causation":"cmd-evidence-story-mcp-managed-disposable-daemon-test_result-1788239640192","change":{"change":"evidence","kind":"test_result","reference":"xtask/src/mcp_smoke.rs","source":"cargo xtask mcp-codex-smoke"},"correlation":"protocol-artifact-evidence","event_id":"story:mcp-managed-disposable-daemon@5#0~1bcce5c1f31a7da7","executor":null,"recorded_at":"2026-09-01T05:14:00Z"}}
{"entity":"story","version":1,"id":"mcp-managed-disposable-daemon","revision":5,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"website/package.json","source":"npm run typecheck && npm run build","target":"01MEM0000000000000097"},"payload":{"actor":"human:timo","at":1788239640000,"causation":"cmd-evidence-story-mcp-managed-disposable-daemon-test_result-1788239640320","change":{"change":"evidence","kind":"test_result","reference":"website/package.json","source":"npm run typecheck && npm run build"},"correlation":"protocol-artifact-evidence","event_id":"story:mcp-managed-disposable-daemon@5#0~0035a74f4209c196","executor":null,"recorded_at":"2026-09-01T05:14:00Z"}}
{"entity":"story","version":1,"id":"mcp-managed-disposable-daemon","revision":6,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":5}}","target":"01MEM0000000000000097","to":"implemented"},"payload":{"actor":"human:timo","at":1788239640528,"causation":"cmd-move-story-mcp-managed-disposable-daemon-1788239640528","change":{"change":"moved","decided_on":{"recorded":{"test_result":5}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:mcp-managed-disposable-daemon@6#0~1a0012aaafcc2e99","executor":null,"recorded_at":"2026-09-01T05:14:00Z"}}
{"entity":"story","version":1,"id":"mcp-managed-disposable-daemon","revision":7,"type":"aep.entity.update/v1","from_state":"implemented","to_state":"implemented","changed":{"body":"# Story: Launch a disposable daemon behind MCP\n\n## Outcome\n\nOne command starts a fresh private Substrate daemon, waits for its machine facts, exposes bounded workspace and execution operations as MCP tools, and tears down the socket, database, workspaces and child process when the MCP server exits.\n\n## Acceptance\n\nThe adapter verifies the advertised contract and capability snapshot, preserves operation IDs and named refusals, never bypasses the daemon through host-driver APIs, exposes output and exact resource observations without inventing fields, scopes all state to the disposable instance, and includes a Codex-compatible hands-on smoke test. Unsupported host capabilities remain absent or refused. No TCP listener or production authentication claim is introduced.\n\n## Design-closure audit — 2026-09-01\n\nImplementation requires `docs/design/19-mcp-disposable-test-adapter.md` to be accepted after SDK parity. The adapter remains a private, development-only stdio composition over the SDK and Unix socket; it is not production ingress. ADR 0025 separately accepts public OCI distribution of that test-only binary without making the crate publishable or the MCP surface stable.\n\nThe design must fix: a private SDK-only crate boundary; a custom bounded JSONL transport rather than an unbounded stdio helper; caller-supplied operation ids on every mutation; exact refusal/observation projection; no model-selected host roots, secret slots or apertures; tracked exec/workspace teardown before daemon shutdown; an exclusive process-free delegated cgroup root; and honest SIGKILL semantics. Portable OCI smoke proves the named sandbox refusal; positive execution stays in the native delegated lane until a separate container-runtime design supplies the confinement prerequisites.\n\nAcceptance additionally depends on the managed-daemon child-absence fix and public SDK output/metrics/absence-preserving facts. `cargo xtask check-mcp-boundary` must prove the crate is private, depends on Substrate only through the SDK, enables no HTTP/OAuth MCP features and uses no unbounded rmcp stdio transport.\n"},"args":{"changes":{"body":"# Story: Launch a disposable daemon behind MCP\n\n## Outcome\n\nOne command starts a fresh private Substrate daemon, waits for its machine facts, exposes bounded workspace and execution operations as MCP tools, and tears down the socket, database, workspaces and child process when the MCP server exits.\n\n## Acceptance\n\nThe adapter verifies the advertised contract and capability snapshot, preserves operation IDs and named refusals, never bypasses the daemon through host-driver APIs, exposes output and exact resource observations without inventing fields, scopes all state to the disposable instance, and includes a Codex-compatible hands-on smoke test. Unsupported host capabilities remain absent or refused. No TCP listener or production authentication claim is introduced.\n\n## Design-closure audit — 2026-09-01\n\nImplementation requires `docs/design/19-mcp-disposable-test-adapter.md` to be accepted after SDK parity. The adapter remains a private, development-only stdio composition over the SDK and Unix socket; it is not production ingress. ADR 0025 separately accepts public OCI distribution of that test-only binary without making the crate publishable or the MCP surface stable.\n\nThe design must fix: a private SDK-only crate boundary; a custom bounded JSONL transport rather than an unbounded stdio helper; caller-supplied operation ids on every mutation; exact refusal/observation projection; no model-selected host roots, secret slots or apertures; tracked exec/workspace teardown before daemon shutdown; an exclusive process-free delegated cgroup root; and honest SIGKILL semantics. Portable OCI smoke proves the named sandbox refusal; positive execution stays in the native delegated lane until a separate container-runtime design supplies the confinement prerequisites.\n\nAcceptance additionally depends on the managed-daemon child-absence fix and public SDK output/metrics/absence-preserving facts. `cargo xtask check-mcp-boundary` must prove the crate is private, depends on Substrate only through the SDK, enables no HTTP/OAuth MCP features and uses no unbounded rmcp stdio transport.\n"},"command":"update-entity","target":"01MEM0000000000000097"},"payload":{"actor":"human:timo","at":1788239671030,"causation":"cmd-protocol-artifact-body-section-story-mcp-managed-disposable-daemon-1788239671030","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:mcp-managed-disposable-daemon@7#0~8837f77838ea6ee4","executor":null,"recorded_at":"2026-09-01T05:14:31Z"}}
{"entity":"story","version":1,"id":"production-tls-control-listener","revision":5,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000112","to":"active"},"payload":{"actor":"human:timo","at":1788240931083,"causation":"cmd-move-story-production-tls-control-listener-1788240931083","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:production-tls-control-listener@5#0~d3ab3b4e51863304","executor":null,"recorded_at":"2026-09-01T05:35:31Z"}}
{"entity":"story","version":1,"id":"production-tls-control-listener","revision":6,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"body":"# Story: Production control traffic uses TLS\n\n## Outcome\n\nA remotely deployed daemon serves its HTTP and WebSocket control plane over TLS with explicit operator-provided identity and trust configuration.\n\n## Design gate\n\nAn accepted design or ADR fixes certificate loading and rotation, client authentication mode, listener binding, forwarded-address treatment, WebSocket behavior, and the exact startup refusals before code. The Unix socket and kernel-peer-credential subject derivation remain unchanged.\n\n## Acceptance\n\nThe daemon uses rustls and accepts no production plaintext listener. A non-loopback static-bearer configuration is refused at startup. Certificate and key files must be explicit, owner-safe and reloadable without exposing bytes in logs, events or metrics. Tests cover valid TLS, unknown CA, wrong server name, expired/not-yet-valid certificates, missing key material, plaintext attempts, WebSocket upgrade, rotation and shutdown. Development plaintext remains loopback-only and visibly marked development.\n\n## Out of Scope\n\nPublic ingress controllers, ACME automation, service-mesh-specific configuration and hosted token verification.\n\n## Design closure — 2026-09-01\n\n- [ADR 0024](../../../adr/0024-production-network-control-uses-server-authenticated-tls.md) accepts the approved server-authenticated TLS posture: a distinct TLS 1.3 HTTPS/WSS listener, no mTLS principal mapping, explicit owner-safe key material, atomic SIGHUP rotation, no forwarded-address trust and named startup/reload refusals.\n- Implementation remains sequenced after `story:promote-development-contract-frontier`; no listener code starts while that dependency is proposed.\n\n## Implementation evidence — 2026-09-01\n\n- `TlsDaemonConfig` and the CLI expose a distinct production listener with explicit bind, certificate-chain and private-key paths. The server enables rustls TLS 1.3 and HTTP/1.1 only; the development static-bearer listener now refuses every non-loopback address as `tls.listener-config-invalid`.\n- Identity loading opens both paths with `O_NOFOLLOW`, bounds their bytes, requires regular non-empty files, requires the key to be owned by the effective uid with no group/other mode bits, validates every certificate time window, requires exactly one supported private key and proves that it matches the leaf before binding.\n- SIGHUP parses and matches the pair as one replacement snapshot. Valid replacements affect only new connections; invalid replacements retain the prior snapshot and emit `tls.reload-invalid` without parser output or material bytes.\n- Production HTTPS/WSS remains behind a fail-closed pre-admission response until the dependent hosted trust-envelope verifier derives a remote subject. Forwarded and caller-written identity headers are never consulted, and no certificate becomes a caller principal.\n- `crates/substrate-daemon/tests/tls_listener.rs` drives the shipped daemon and proves valid TLS, TLS 1.3/HTTP1.1 negotiation, unknown CA, wrong name, expired and not-yet-valid material, missing/unsafe/mismatched key refusal, plaintext receiving no HTTP response, WSS upgrade attempts over the authenticated connection, valid and invalid rotation, existing-connection snapshot retention and bounded SIGTERM shutdown.\n- Public README, status, roadmap and deployment documentation distinguish the implemented transport from absent hosted admission. `npm ci`, `npm run typecheck` and `npm run build` pass; the rendered guide was inspected at 1440px and 390px widths.\n- `bash scripts/gate.sh` completed with `gate: passed`, including release-mode workspace tests, strict Clippy, full-history secret scanning, RustSec/HTTP2 policy, deterministic third-party notices, package archives and all twelve frozen bundle checks.\n"},"args":{"changes":{"body":"# Story: Production control traffic uses TLS\n\n## Outcome\n\nA remotely deployed daemon serves its HTTP and WebSocket control plane over TLS with explicit operator-provided identity and trust configuration.\n\n## Design gate\n\nAn accepted design or ADR fixes certificate loading and rotation, client authentication mode, listener binding, forwarded-address treatment, WebSocket behavior, and the exact startup refusals before code. The Unix socket and kernel-peer-credential subject derivation remain unchanged.\n\n## Acceptance\n\nThe daemon uses rustls and accepts no production plaintext listener. A non-loopback static-bearer configuration is refused at startup. Certificate and key files must be explicit, owner-safe and reloadable without exposing bytes in logs, events or metrics. Tests cover valid TLS, unknown CA, wrong server name, expired/not-yet-valid certificates, missing key material, plaintext attempts, WebSocket upgrade, rotation and shutdown. Development plaintext remains loopback-only and visibly marked development.\n\n## Out of Scope\n\nPublic ingress controllers, ACME automation, service-mesh-specific configuration and hosted token verification.\n\n## Design closure — 2026-09-01\n\n- [ADR 0024](../../../adr/0024-production-network-control-uses-server-authenticated-tls.md) accepts the approved server-authenticated TLS posture: a distinct TLS 1.3 HTTPS/WSS listener, no mTLS principal mapping, explicit owner-safe key material, atomic SIGHUP rotation, no forwarded-address trust and named startup/reload refusals.\n- Implementation remains sequenced after `story:promote-development-contract-frontier`; no listener code starts while that dependency is proposed.\n\n## Implementation evidence — 2026-09-01\n\n- `TlsDaemonConfig` and the CLI expose a distinct production listener with explicit bind, certificate-chain and private-key paths. The server enables rustls TLS 1.3 and HTTP/1.1 only; the development static-bearer listener now refuses every non-loopback address as `tls.listener-config-invalid`.\n- Identity loading opens both paths with `O_NOFOLLOW`, bounds their bytes, requires regular non-empty files, requires the key to be owned by the effective uid with no group/other mode bits, validates every certificate time window, requires exactly one supported private key and proves that it matches the leaf before binding.\n- SIGHUP parses and matches the pair as one replacement snapshot. Valid replacements affect only new connections; invalid replacements retain the prior snapshot and emit `tls.reload-invalid` without parser output or material bytes.\n- Production HTTPS/WSS remains behind a fail-closed pre-admission response until the dependent hosted trust-envelope verifier derives a remote subject. Forwarded and caller-written identity headers are never consulted, and no certificate becomes a caller principal.\n- `crates/substrate-daemon/tests/tls_listener.rs` drives the shipped daemon and proves valid TLS, TLS 1.3/HTTP1.1 negotiation, unknown CA, wrong name, expired and not-yet-valid material, missing/unsafe/mismatched key refusal, plaintext receiving no HTTP response, WSS upgrade attempts over the authenticated connection, valid and invalid rotation, existing-connection snapshot retention and bounded SIGTERM shutdown.\n- Public README, status, roadmap and deployment documentation distinguish the implemented transport from absent hosted admission. `npm ci`, `npm run typecheck` and `npm run build` pass; the rendered guide was inspected at 1440px and 390px widths.\n- `bash scripts/gate.sh` completed with `gate: passed`, including release-mode workspace tests, strict Clippy, full-history secret scanning, RustSec/HTTP2 policy, deterministic third-party notices, package archives and all twelve frozen bundle checks.\n"},"command":"update-entity","target":"01MEM0000000000000112"},"payload":{"actor":"human:timo","at":1788242246241,"causation":"cmd-protocol-artifact-body-append-story-production-tls-control-listener-1788242246241","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-append","event_id":"story:production-tls-control-listener@6#0~183fa554c5cb9359","executor":null,"recorded_at":"2026-09-01T05:57:26Z"}}
{"entity":"epic","version":1,"id":"remote-serving","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000019","to":"active"},"payload":{"actor":"human:timo","at":1788242252821,"causation":"cmd-move-epic-remote-serving-1788242252821","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"epic:remote-serving@3#0~cde98c14b0d87a96","executor":null,"recorded_at":"2026-09-01T05:57:32Z"}}
{"entity":"story","version":1,"id":"production-tls-control-listener","revision":7,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"body":"# Story: Production control traffic uses TLS\n\n## Outcome\n\nA remotely deployed daemon serves its HTTP and WebSocket control plane over TLS with explicit operator-provided identity and trust configuration.\n\n## Design gate\n\nAn accepted design or ADR fixes certificate loading and rotation, client authentication mode, listener binding, forwarded-address treatment, WebSocket behavior, and the exact startup refusals before code. The Unix socket and kernel-peer-credential subject derivation remain unchanged.\n\n## Acceptance\n\nThe daemon uses rustls and accepts no production plaintext listener. A non-loopback static-bearer configuration is refused at startup. Certificate and key files must be explicit, owner-safe and reloadable without exposing bytes in logs, events or metrics. Tests cover valid TLS, unknown CA, wrong server name, expired/not-yet-valid certificates, missing key material, plaintext attempts, WebSocket upgrade, rotation and shutdown. Development plaintext remains loopback-only and visibly marked development.\n\n## Out of Scope\n\nPublic ingress controllers, ACME automation, service-mesh-specific configuration and hosted token verification.\n\n## Design closure — 2026-09-01\n\n- [ADR 0024](../../../adr/0024-production-network-control-uses-server-authenticated-tls.md) accepts the approved server-authenticated TLS posture: a distinct TLS 1.3 HTTPS/WSS listener, no mTLS principal mapping, explicit owner-safe key material, atomic SIGHUP rotation, no forwarded-address trust and named startup/reload refusals.\n- Implementation remains sequenced after `story:promote-development-contract-frontier`; no listener code starts while that dependency is proposed.\n\n## Implementation evidence — 2026-09-01\n\n- `TlsDaemonConfig` and the CLI expose a distinct production listener with explicit bind, certificate-chain and private-key paths. The server enables rustls TLS 1.3 and HTTP/1.1 only; the development static-bearer listener now refuses every non-loopback address as `tls.listener-config-invalid`.\n- Identity loading opens both paths with `O_NOFOLLOW`, bounds their bytes, requires regular non-empty files, requires the key to be owned by the effective uid with no group/other mode bits, validates every certificate time window, requires exactly one supported private key and proves that it matches the leaf before binding.\n- SIGHUP parses and matches the pair as one replacement snapshot. Valid replacements affect only new connections; invalid replacements retain the prior snapshot and emit `tls.reload-invalid` without parser output or material bytes.\n- Production HTTPS/WSS remains behind a fail-closed pre-admission response until the dependent hosted trust-envelope verifier derives a remote subject. Forwarded and caller-written identity headers are never consulted, and no certificate becomes a caller principal.\n- `crates/substrate-daemon/tests/tls_listener.rs` drives the shipped daemon and proves valid TLS, TLS 1.3/HTTP1.1 negotiation, unknown CA, wrong name, expired and not-yet-valid material, missing/unsafe/mismatched key refusal, plaintext receiving no HTTP response, WSS upgrade attempts over the authenticated connection, valid and invalid rotation, existing-connection snapshot retention and bounded SIGTERM shutdown.\n- Public README, status, roadmap and deployment documentation distinguish the implemented transport from absent hosted admission. `npm ci`, `npm run typecheck` and `npm run build` pass; the rendered guide was inspected at 1440px and 390px widths.\n- `bash scripts/gate.sh` completed with `gate: passed`, including release-mode workspace tests, strict Clippy, full-history secret scanning, RustSec/HTTP2 policy, deterministic third-party notices, package archives and all twelve frozen bundle checks.\n\n- `bash scripts/delegated-lane.sh` also passed after the full gate: host-driver confinement, PTY, managed SDK, MCP cleanup and the shipped-daemon delegated runtime inventory remained green.\n"},"args":{"changes":{"body":"# Story: Production control traffic uses TLS\n\n## Outcome\n\nA remotely deployed daemon serves its HTTP and WebSocket control plane over TLS with explicit operator-provided identity and trust configuration.\n\n## Design gate\n\nAn accepted design or ADR fixes certificate loading and rotation, client authentication mode, listener binding, forwarded-address treatment, WebSocket behavior, and the exact startup refusals before code. The Unix socket and kernel-peer-credential subject derivation remain unchanged.\n\n## Acceptance\n\nThe daemon uses rustls and accepts no production plaintext listener. A non-loopback static-bearer configuration is refused at startup. Certificate and key files must be explicit, owner-safe and reloadable without exposing bytes in logs, events or metrics. Tests cover valid TLS, unknown CA, wrong server name, expired/not-yet-valid certificates, missing key material, plaintext attempts, WebSocket upgrade, rotation and shutdown. Development plaintext remains loopback-only and visibly marked development.\n\n## Out of Scope\n\nPublic ingress controllers, ACME automation, service-mesh-specific configuration and hosted token verification.\n\n## Design closure — 2026-09-01\n\n- [ADR 0024](../../../adr/0024-production-network-control-uses-server-authenticated-tls.md) accepts the approved server-authenticated TLS posture: a distinct TLS 1.3 HTTPS/WSS listener, no mTLS principal mapping, explicit owner-safe key material, atomic SIGHUP rotation, no forwarded-address trust and named startup/reload refusals.\n- Implementation remains sequenced after `story:promote-development-contract-frontier`; no listener code starts while that dependency is proposed.\n\n## Implementation evidence — 2026-09-01\n\n- `TlsDaemonConfig` and the CLI expose a distinct production listener with explicit bind, certificate-chain and private-key paths. The server enables rustls TLS 1.3 and HTTP/1.1 only; the development static-bearer listener now refuses every non-loopback address as `tls.listener-config-invalid`.\n- Identity loading opens both paths with `O_NOFOLLOW`, bounds their bytes, requires regular non-empty files, requires the key to be owned by the effective uid with no group/other mode bits, validates every certificate time window, requires exactly one supported private key and proves that it matches the leaf before binding.\n- SIGHUP parses and matches the pair as one replacement snapshot. Valid replacements affect only new connections; invalid replacements retain the prior snapshot and emit `tls.reload-invalid` without parser output or material bytes.\n- Production HTTPS/WSS remains behind a fail-closed pre-admission response until the dependent hosted trust-envelope verifier derives a remote subject. Forwarded and caller-written identity headers are never consulted, and no certificate becomes a caller principal.\n- `crates/substrate-daemon/tests/tls_listener.rs` drives the shipped daemon and proves valid TLS, TLS 1.3/HTTP1.1 negotiation, unknown CA, wrong name, expired and not-yet-valid material, missing/unsafe/mismatched key refusal, plaintext receiving no HTTP response, WSS upgrade attempts over the authenticated connection, valid and invalid rotation, existing-connection snapshot retention and bounded SIGTERM shutdown.\n- Public README, status, roadmap and deployment documentation distinguish the implemented transport from absent hosted admission. `npm ci`, `npm run typecheck` and `npm run build` pass; the rendered guide was inspected at 1440px and 390px widths.\n- `bash scripts/gate.sh` completed with `gate: passed`, including release-mode workspace tests, strict Clippy, full-history secret scanning, RustSec/HTTP2 policy, deterministic third-party notices, package archives and all twelve frozen bundle checks.\n\n- `bash scripts/delegated-lane.sh` also passed after the full gate: host-driver confinement, PTY, managed SDK, MCP cleanup and the shipped-daemon delegated runtime inventory remained green.\n"},"command":"update-entity","target":"01MEM0000000000000112"},"payload":{"actor":"human:timo","at":1788242333820,"causation":"cmd-protocol-artifact-body-append-story-production-tls-control-listener-1788242333820","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-append","event_id":"story:production-tls-control-listener@7#0~433e00a57cb5c988","executor":null,"recorded_at":"2026-09-01T05:58:53Z"}}
{"entity":"story","version":1,"id":"production-tls-control-listener","revision":7,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"scripts/gate.sh","source":"local bash scripts/gate.sh: gate passed","target":"01MEM0000000000000112"},"payload":{"actor":"human:timo","at":1788242351000,"causation":"cmd-evidence-story-production-tls-control-listener-test_result-1788242352097","change":{"change":"evidence","kind":"test_result","reference":"scripts/gate.sh","source":"local bash scripts/gate.sh: gate passed"},"correlation":"protocol-artifact-evidence","event_id":"story:production-tls-control-listener@7#0~c0bad7c44c3d4f84","executor":null,"recorded_at":"2026-09-01T05:59:11Z"}}
{"entity":"story","version":1,"id":"production-tls-control-listener","revision":7,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"scripts/delegated-lane.sh","source":"local bash scripts/delegated-lane.sh: passed","target":"01MEM0000000000000112"},"payload":{"actor":"human:timo","at":1788242352000,"causation":"cmd-evidence-story-production-tls-control-listener-test_result-1788242352230","change":{"change":"evidence","kind":"test_result","reference":"scripts/delegated-lane.sh","source":"local bash scripts/delegated-lane.sh: passed"},"correlation":"protocol-artifact-evidence","event_id":"story:production-tls-control-listener@7#0~84b41be35a5a8e09","executor":null,"recorded_at":"2026-09-01T05:59:12Z"}}
{"entity":"story","version":1,"id":"production-tls-control-listener","revision":8,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":2}}","target":"01MEM0000000000000112","to":"implemented"},"payload":{"actor":"human:timo","at":1788242352440,"causation":"cmd-move-story-production-tls-control-listener-1788242352440","change":{"change":"moved","decided_on":{"recorded":{"test_result":2}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:production-tls-control-listener@8#0~7dfd1a9703c1c548","executor":null,"recorded_at":"2026-09-01T05:59:12Z"}}
{"entity":"story","version":1,"id":"production-tls-control-listener","revision":9,"type":"aep.entity.update/v1","from_state":"implemented","to_state":"implemented","changed":{"body":"# Story: Production control traffic uses TLS\n\n## Outcome\n\nA remotely deployed daemon serves its HTTP and WebSocket control plane over TLS with explicit operator-provided identity and trust configuration.\n\n## Design gate\n\nAn accepted design or ADR fixes certificate loading and rotation, client authentication mode, listener binding, forwarded-address treatment, WebSocket behavior, and the exact startup refusals before code. The Unix socket and kernel-peer-credential subject derivation remain unchanged.\n\n## Acceptance\n\nThe daemon uses rustls and accepts no production plaintext listener. A non-loopback static-bearer configuration is refused at startup. Certificate and key files must be explicit, owner-safe and reloadable without exposing bytes in logs, events or metrics. Tests cover valid TLS, unknown CA, wrong server name, expired/not-yet-valid certificates, missing key material, plaintext attempts, WebSocket upgrade, rotation and shutdown. Development plaintext remains loopback-only and visibly marked development.\n\n## Out of Scope\n\nPublic ingress controllers, ACME automation, service-mesh-specific configuration and hosted token verification.\n\n## Design closure — 2026-09-01\n\n- [ADR 0024](../../../adr/0024-production-network-control-uses-server-authenticated-tls.md) accepts the approved server-authenticated TLS posture: a distinct TLS 1.3 HTTPS/WSS listener, no mTLS principal mapping, explicit owner-safe key material, atomic SIGHUP rotation, no forwarded-address trust and named startup/reload refusals.\n- Implementation remains sequenced after `story:promote-development-contract-frontier`; no listener code starts while that dependency is proposed.\n\n## Implementation evidence — 2026-09-01\n\n- `TlsDaemonConfig` and the CLI expose a distinct production listener with explicit bind, certificate-chain and private-key paths. The server enables rustls TLS 1.3 and HTTP/1.1 only; the development static-bearer listener now refuses every non-loopback address as `tls.listener-config-invalid`.\n- Identity loading opens both paths with `O_NOFOLLOW`, bounds their bytes, requires regular non-empty files, requires the key to be owned by the effective uid with no group/other mode bits, validates every certificate time window, requires exactly one supported private key and proves that it matches the leaf before binding.\n- SIGHUP parses and matches the pair as one replacement snapshot. Valid replacements affect only new connections; invalid replacements retain the prior snapshot and emit `tls.reload-invalid` without parser output or material bytes.\n- Production HTTPS/WSS remains behind a fail-closed pre-admission response until the dependent hosted trust-envelope verifier derives a remote subject. Forwarded and caller-written identity headers are never consulted, and no certificate becomes a caller principal.\n- `crates/substrate-daemon/tests/tls_listener.rs` drives the shipped daemon and proves valid TLS, TLS 1.3/HTTP/1.1 negotiation, unknown CA, wrong name, expired and not-yet-valid material, missing/unsafe/mismatched key refusal, plaintext receiving no HTTP response, WSS upgrade attempts over the authenticated connection, valid and invalid rotation, existing-connection snapshot retention and bounded SIGTERM shutdown.\n- Public README, status, roadmap and deployment documentation distinguish the implemented transport from absent hosted admission. `npm ci`, `npm run typecheck` and `npm run build` pass; the rendered guide was inspected at 1440px and 390px widths.\n- `bash scripts/gate.sh` completed with `gate: passed`, including release-mode workspace tests, strict Clippy, full-history secret scanning, RustSec/HTTP2 policy, deterministic third-party notices, package archives and all twelve frozen bundle checks.\n- `bash scripts/delegated-lane.sh` also passed after the full gate: host-driver confinement, PTY, managed SDK, MCP cleanup and the shipped-daemon delegated runtime inventory remained green.\n"},"args":{"changes":{"body":"# Story: Production control traffic uses TLS\n\n## Outcome\n\nA remotely deployed daemon serves its HTTP and WebSocket control plane over TLS with explicit operator-provided identity and trust configuration.\n\n## Design gate\n\nAn accepted design or ADR fixes certificate loading and rotation, client authentication mode, listener binding, forwarded-address treatment, WebSocket behavior, and the exact startup refusals before code. The Unix socket and kernel-peer-credential subject derivation remain unchanged.\n\n## Acceptance\n\nThe daemon uses rustls and accepts no production plaintext listener. A non-loopback static-bearer configuration is refused at startup. Certificate and key files must be explicit, owner-safe and reloadable without exposing bytes in logs, events or metrics. Tests cover valid TLS, unknown CA, wrong server name, expired/not-yet-valid certificates, missing key material, plaintext attempts, WebSocket upgrade, rotation and shutdown. Development plaintext remains loopback-only and visibly marked development.\n\n## Out of Scope\n\nPublic ingress controllers, ACME automation, service-mesh-specific configuration and hosted token verification.\n\n## Design closure — 2026-09-01\n\n- [ADR 0024](../../../adr/0024-production-network-control-uses-server-authenticated-tls.md) accepts the approved server-authenticated TLS posture: a distinct TLS 1.3 HTTPS/WSS listener, no mTLS principal mapping, explicit owner-safe key material, atomic SIGHUP rotation, no forwarded-address trust and named startup/reload refusals.\n- Implementation remains sequenced after `story:promote-development-contract-frontier`; no listener code starts while that dependency is proposed.\n\n## Implementation evidence — 2026-09-01\n\n- `TlsDaemonConfig` and the CLI expose a distinct production listener with explicit bind, certificate-chain and private-key paths. The server enables rustls TLS 1.3 and HTTP/1.1 only; the development static-bearer listener now refuses every non-loopback address as `tls.listener-config-invalid`.\n- Identity loading opens both paths with `O_NOFOLLOW`, bounds their bytes, requires regular non-empty files, requires the key to be owned by the effective uid with no group/other mode bits, validates every certificate time window, requires exactly one supported private key and proves that it matches the leaf before binding.\n- SIGHUP parses and matches the pair as one replacement snapshot. Valid replacements affect only new connections; invalid replacements retain the prior snapshot and emit `tls.reload-invalid` without parser output or material bytes.\n- Production HTTPS/WSS remains behind a fail-closed pre-admission response until the dependent hosted trust-envelope verifier derives a remote subject. Forwarded and caller-written identity headers are never consulted, and no certificate becomes a caller principal.\n- `crates/substrate-daemon/tests/tls_listener.rs` drives the shipped daemon and proves valid TLS, TLS 1.3/HTTP/1.1 negotiation, unknown CA, wrong name, expired and not-yet-valid material, missing/unsafe/mismatched key refusal, plaintext receiving no HTTP response, WSS upgrade attempts over the authenticated connection, valid and invalid rotation, existing-connection snapshot retention and bounded SIGTERM shutdown.\n- Public README, status, roadmap and deployment documentation distinguish the implemented transport from absent hosted admission. `npm ci`, `npm run typecheck` and `npm run build` pass; the rendered guide was inspected at 1440px and 390px widths.\n- `bash scripts/gate.sh` completed with `gate: passed`, including release-mode workspace tests, strict Clippy, full-history secret scanning, RustSec/HTTP2 policy, deterministic third-party notices, package archives and all twelve frozen bundle checks.\n- `bash scripts/delegated-lane.sh` also passed after the full gate: host-driver confinement, PTY, managed SDK, MCP cleanup and the shipped-daemon delegated runtime inventory remained green.\n"},"command":"update-entity","target":"01MEM0000000000000112"},"payload":{"actor":"human:timo","at":1788242415844,"causation":"cmd-protocol-artifact-body-section-story-production-tls-control-listener-1788242415844","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:production-tls-control-listener@9#0~ad3f85e98990a54d","executor":null,"recorded_at":"2026-09-01T06:00:15Z"}}
{"entity":"story","version":1,"id":"hosted-trust-envelope-admission","revision":4,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000085","to":"active"},"payload":{"actor":"human:timo","at":1788243325348,"causation":"cmd-move-story-hosted-trust-envelope-admission-1788243325348","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:hosted-trust-envelope-admission@4#0~f8e4177148932f3b","executor":null,"recorded_at":"2026-09-01T06:15:25Z"}}
{"entity":"story","version":1,"id":"hosted-trust-envelope-admission","revision":5,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"body":"# Story: Hosted requests use the accepted trust envelope\n\n## Outcome\n\nA production network request is admitted only from an Identity-issued, five-minute, audience- and scope-bound opaque access credential. Substrate derives its subject and authority from Identity's current resolved authority rather than caller-written HTTP data.\n\n## Coordination decision\n\nIdentity decision 0004 provides the corresponding released profile: deployment-registered opaque audiences and scopes, five-minute verifier-only access credentials, exact-audience online resolution and total logout revocation. ADR 0026 fixes Substrate's relying-party side: exact audience `urn:b10x:substrate`, scopes `observe`, `workspaces` and `exec`, explicit HTTPS trust roots, no redirect or proxy, and fail-closed per-request resolution. The earlier proposed EdDSA/key-distribution profile is not implemented because it is not Identity's released seam.\n\n## Acceptance\n\nThe production TLS listener refuses a missing credential, an invalid/resolved authority, a missing route scope, or Identity unavailability by a safe name before durable admission. Issuer, audience, time window, five-minute lifetime, subject, tenant, actor and canonical scope bytes are validated locally after resolution. Tenant, actor, uid and subject headers or body fields never override verified claims. Revocation takes effect on the next request because no authority cache exists. Unix-socket requests continue to derive `local:<uid>` from kernel peer credentials and never contact Identity. Conformance tests prove negative cases, exact per-route scopes, revocation, spoof resistance and that credentials and authority documents do not enter diagnostics or metrics.\n\n## Out of Scope\n\nIdentity implementation, browser sessions, product roles, fleet placement, connector grants and signed-token key distribution.\n"},"args":{"changes":{"body":"# Story: Hosted requests use the accepted trust envelope\n\n## Outcome\n\nA production network request is admitted only from an Identity-issued, five-minute, audience- and scope-bound opaque access credential. Substrate derives its subject and authority from Identity's current resolved authority rather than caller-written HTTP data.\n\n## Coordination decision\n\nIdentity decision 0004 provides the corresponding released profile: deployment-registered opaque audiences and scopes, five-minute verifier-only access credentials, exact-audience online resolution and total logout revocation. ADR 0026 fixes Substrate's relying-party side: exact audience `urn:b10x:substrate`, scopes `observe`, `workspaces` and `exec`, explicit HTTPS trust roots, no redirect or proxy, and fail-closed per-request resolution. The earlier proposed EdDSA/key-distribution profile is not implemented because it is not Identity's released seam.\n\n## Acceptance\n\nThe production TLS listener refuses a missing credential, an invalid/resolved authority, a missing route scope, or Identity unavailability by a safe name before durable admission. Issuer, audience, time window, five-minute lifetime, subject, tenant, actor and canonical scope bytes are validated locally after resolution. Tenant, actor, uid and subject headers or body fields never override verified claims. Revocation takes effect on the next request because no authority cache exists. Unix-socket requests continue to derive `local:<uid>` from kernel peer credentials and never contact Identity. Conformance tests prove negative cases, exact per-route scopes, revocation, spoof resistance and that credentials and authority documents do not enter diagnostics or metrics.\n\n## Out of Scope\n\nIdentity implementation, browser sessions, product roles, fleet placement, connector grants and signed-token key distribution.\n"},"command":"update-entity","target":"01MEM0000000000000085"},"payload":{"actor":"human:timo","at":1788243444195,"causation":"cmd-protocol-artifact-body-story-hosted-trust-envelope-admission-1788243444195","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body","event_id":"story:hosted-trust-envelope-admission@5#0~d804851a069e84cb","executor":null,"recorded_at":"2026-09-01T06:17:24Z"}}
{"entity":"story","version":1,"id":"hosted-trust-envelope-admission","revision":6,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"body":"# Story: Hosted requests use the accepted trust envelope\n\n## Outcome\n\nA production network request is admitted only from an Identity-issued, five-minute, audience- and scope-bound opaque access credential. Substrate derives its subject and authority from Identity's current resolved authority rather than caller-written HTTP data.\n\n## Coordination decision\n\nIdentity decision 0004 provides the corresponding released profile: deployment-registered opaque audiences and scopes, five-minute verifier-only access credentials, exact-audience online resolution and total logout revocation. ADR 0026 fixes Substrate's relying-party side: exact audience `urn:b10x:substrate`, scopes `observe`, `workspaces` and `exec`, explicit HTTPS trust roots, no redirect or proxy, and fail-closed per-request resolution. The earlier proposed EdDSA/key-distribution profile is not implemented because it is not Identity's released seam.\n\n## Acceptance\n\nThe production TLS listener refuses a missing credential, an invalid/resolved authority, a missing route scope, or Identity unavailability by a safe name before durable admission. Issuer, audience, time window, five-minute lifetime, subject, tenant, actor and canonical scope bytes are validated locally after resolution. Tenant, actor, uid and subject headers or body fields never override verified claims. Revocation takes effect on the next request because no authority cache exists. Unix-socket requests continue to derive `local:<uid>` from kernel peer credentials and never contact Identity. Conformance tests prove negative cases, exact per-route scopes, revocation, spoof resistance and that credentials and authority documents do not enter diagnostics or metrics.\n\n## Out of Scope\n\nIdentity implementation, browser sessions, product roles, fleet placement, connector grants and signed-token key distribution.\n\n## Implementation evidence\n\nThe implementation is coordinated by accepted ADR 0026 and contract bundle `substrate-wire/0.13.0`. The production TLS listener resolves each opaque `identity_access_v1_` credential against Identity over TLS 1.3 with explicit trust roots and no proxy, redirect or authority cache. It derives non-reversible tenant-bound subject and actor references, removes caller authority before dispatch, and enforces the exact `observe`, `workspaces` and `exec` route scopes.\n\nBlack-box tests of the shipped daemon prove valid admission, missing and invalid credentials, insufficient scope before durable operation creation, Identity unavailability, immediate revocation, spoofed authority headers, WebSocket admission, certificate rotation and diagnostic redaction. Unit tests cover origin and trust-root validation, closed resolved-authority parsing, exact time and lifetime bounds, credential shape, scope mapping and subject derivation. `bash scripts/gate.sh` passed with all 13 contract bundles and 2,820 classified JSON documents; `bash scripts/delegated-lane.sh` passed under delegated cgroup v2 confinement.\n"},"args":{"changes":{"body":"# Story: Hosted requests use the accepted trust envelope\n\n## Outcome\n\nA production network request is admitted only from an Identity-issued, five-minute, audience- and scope-bound opaque access credential. Substrate derives its subject and authority from Identity's current resolved authority rather than caller-written HTTP data.\n\n## Coordination decision\n\nIdentity decision 0004 provides the corresponding released profile: deployment-registered opaque audiences and scopes, five-minute verifier-only access credentials, exact-audience online resolution and total logout revocation. ADR 0026 fixes Substrate's relying-party side: exact audience `urn:b10x:substrate`, scopes `observe`, `workspaces` and `exec`, explicit HTTPS trust roots, no redirect or proxy, and fail-closed per-request resolution. The earlier proposed EdDSA/key-distribution profile is not implemented because it is not Identity's released seam.\n\n## Acceptance\n\nThe production TLS listener refuses a missing credential, an invalid/resolved authority, a missing route scope, or Identity unavailability by a safe name before durable admission. Issuer, audience, time window, five-minute lifetime, subject, tenant, actor and canonical scope bytes are validated locally after resolution. Tenant, actor, uid and subject headers or body fields never override verified claims. Revocation takes effect on the next request because no authority cache exists. Unix-socket requests continue to derive `local:<uid>` from kernel peer credentials and never contact Identity. Conformance tests prove negative cases, exact per-route scopes, revocation, spoof resistance and that credentials and authority documents do not enter diagnostics or metrics.\n\n## Out of Scope\n\nIdentity implementation, browser sessions, product roles, fleet placement, connector grants and signed-token key distribution.\n\n## Implementation evidence\n\nThe implementation is coordinated by accepted ADR 0026 and contract bundle `substrate-wire/0.13.0`. The production TLS listener resolves each opaque `identity_access_v1_` credential against Identity over TLS 1.3 with explicit trust roots and no proxy, redirect or authority cache. It derives non-reversible tenant-bound subject and actor references, removes caller authority before dispatch, and enforces the exact `observe`, `workspaces` and `exec` route scopes.\n\nBlack-box tests of the shipped daemon prove valid admission, missing and invalid credentials, insufficient scope before durable operation creation, Identity unavailability, immediate revocation, spoofed authority headers, WebSocket admission, certificate rotation and diagnostic redaction. Unit tests cover origin and trust-root validation, closed resolved-authority parsing, exact time and lifetime bounds, credential shape, scope mapping and subject derivation. `bash scripts/gate.sh` passed with all 13 contract bundles and 2,820 classified JSON documents; `bash scripts/delegated-lane.sh` passed under delegated cgroup v2 confinement.\n"},"command":"update-entity","target":"01MEM0000000000000085"},"payload":{"actor":"human:timo","at":1788245260759,"causation":"cmd-protocol-artifact-body-section-story-hosted-trust-envelope-admission-1788245260759","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:hosted-trust-envelope-admission@6#0~f6ef08f4b4ebbaf4","executor":null,"recorded_at":"2026-09-01T06:47:40Z"}}
{"entity":"story","version":1,"id":"hosted-trust-envelope-admission","revision":6,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"scripts/gate.sh","source":"local bash scripts/gate.sh: gate passed","target":"01MEM0000000000000085"},"payload":{"actor":"human:timo","at":1788245260000,"causation":"cmd-evidence-story-hosted-trust-envelope-admission-test_result-1788245260890","change":{"change":"evidence","kind":"test_result","reference":"scripts/gate.sh","source":"local bash scripts/gate.sh: gate passed"},"correlation":"protocol-artifact-evidence","event_id":"story:hosted-trust-envelope-admission@6#0~056e54af02fc860b","executor":null,"recorded_at":"2026-09-01T06:47:40Z"}}
{"entity":"story","version":1,"id":"hosted-trust-envelope-admission","revision":6,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"scripts/delegated-lane.sh","source":"local bash scripts/delegated-lane.sh: delegated cgroup v2 lane passed","target":"01MEM0000000000000085"},"payload":{"actor":"human:timo","at":1788245260000,"causation":"cmd-evidence-story-hosted-trust-envelope-admission-test_result-1788245261015","change":{"change":"evidence","kind":"test_result","reference":"scripts/delegated-lane.sh","source":"local bash scripts/delegated-lane.sh: delegated cgroup v2 lane passed"},"correlation":"protocol-artifact-evidence","event_id":"story:hosted-trust-envelope-admission@6#0~c2b5093b127128ad","executor":null,"recorded_at":"2026-09-01T06:47:40Z"}}
{"entity":"story","version":1,"id":"hosted-trust-envelope-admission","revision":7,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":2}}","target":"01MEM0000000000000085","to":"implemented"},"payload":{"actor":"human:timo","at":1788245261223,"causation":"cmd-move-story-hosted-trust-envelope-admission-1788245261223","change":{"change":"moved","decided_on":{"recorded":{"test_result":2}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:hosted-trust-envelope-admission@7#0~7aff2109027de261","executor":null,"recorded_at":"2026-09-01T06:47:41Z"}}
{"entity":"story","version":1,"id":"network-session-authority","revision":8,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"specification\":1}}","target":"01MEM0000000000000100","to":"active"},"payload":{"actor":"human:timo","at":1788246009527,"causation":"cmd-move-story-network-session-authority-1788246009527","change":{"change":"moved","decided_on":{"recorded":{"specification":1}},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:network-session-authority@8#0~b76cef092eea91df","executor":null,"recorded_at":"2026-09-01T07:00:09Z"}}
{"entity":"story","version":1,"id":"network-session-authority","revision":9,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"body":"# Story: Network session transport over TLS with single-use proof-bound authority\n\n## Outcome\n\nA hosted client attaches to a raw-pipe or PTY session over WSS only with an authority that lives at most 60 seconds, redeems exactly once, and proves both its ephemeral Ed25519 key and the exact TLS channel. A reconnect always needs a new session and a fresh authority.\n\n## Design closure\n\nAccepted ADR 0027 and `docs/design/14-network-session-authority.md` close the protocol. A hosted-only mint route takes the client public key and returns a one-time bearer outside the durable operation ledger. The attach upgrade carries the authority id, bearer, timestamp and exporter-bound signature in bounded headers so named HTTP refusals happen before upgrade. The store retains only the bearer verifier, public key, expiry and redemption outcome and consumes authority plus attachment right in one transaction.\n\nThis replaces the earlier first-WebSocket-frame draft: a frame arrives after HTTP upgrade and therefore cannot produce its promised HTTP 401/409 answer. It also avoids returning secret authority bytes from the replayable session-start operation.\n\n## Acceptance\n\n1. A successor bundle adds the hosted mint route, exact header and transcript vocabulary, 60-second lifetime, 10-second proof skew, four-authority cap, and the four named refusal codes while leaving earlier bundles unchanged.\n2. The hosted TLS path mints only for a ready session owned by the verified Identity scope and requiring `exec`; Unix never mints and keeps kernel-authenticated attachment compatibility.\n3. The TLS attach path verifies the exact channel exporter and bound Ed25519 key before upgrade, attachment capacity, or durable claim. Authority redemption and the one attachment right are consumed in one `IMMEDIATE` transaction.\n4. Missing, expired, replayed, malformed, and wrong-channel authority fail by name. The development static-bearer TCP route set serves neither mint nor attach and binds only on loopback.\n5. Tests prove at-most-60-second expiry with paused time, exact single redemption, wrong-channel refusal, concurrent attachment refusal, Unix compatibility, TLS raw-pipe operation, and absence of authority, signature, key, and transcript bytes from captured diagnostics.\n6. `bash scripts/gate.sh` and `bash scripts/delegated-lane.sh` pass, and the planning store validates.\n\n## Out of Scope\n\nIdentity implementation and login, credential storage, product policy, resumable attachments, TLS-terminating proxies, and remote SDK automation.\n"},"args":{"changes":{"body":"# Story: Network session transport over TLS with single-use proof-bound authority\n\n## Outcome\n\nA hosted client attaches to a raw-pipe or PTY session over WSS only with an authority that lives at most 60 seconds, redeems exactly once, and proves both its ephemeral Ed25519 key and the exact TLS channel. A reconnect always needs a new session and a fresh authority.\n\n## Design closure\n\nAccepted ADR 0027 and `docs/design/14-network-session-authority.md` close the protocol. A hosted-only mint route takes the client public key and returns a one-time bearer outside the durable operation ledger. The attach upgrade carries the authority id, bearer, timestamp and exporter-bound signature in bounded headers so named HTTP refusals happen before upgrade. The store retains only the bearer verifier, public key, expiry and redemption outcome and consumes authority plus attachment right in one transaction.\n\nThis replaces the earlier first-WebSocket-frame draft: a frame arrives after HTTP upgrade and therefore cannot produce its promised HTTP 401/409 answer. It also avoids returning secret authority bytes from the replayable session-start operation.\n\n## Acceptance\n\n1. A successor bundle adds the hosted mint route, exact header and transcript vocabulary, 60-second lifetime, 10-second proof skew, four-authority cap, and the four named refusal codes while leaving earlier bundles unchanged.\n2. The hosted TLS path mints only for a ready session owned by the verified Identity scope and requiring `exec`; Unix never mints and keeps kernel-authenticated attachment compatibility.\n3. The TLS attach path verifies the exact channel exporter and bound Ed25519 key before upgrade, attachment capacity, or durable claim. Authority redemption and the one attachment right are consumed in one `IMMEDIATE` transaction.\n4. Missing, expired, replayed, malformed, and wrong-channel authority fail by name. The development static-bearer TCP route set serves neither mint nor attach and binds only on loopback.\n5. Tests prove at-most-60-second expiry with paused time, exact single redemption, wrong-channel refusal, concurrent attachment refusal, Unix compatibility, TLS raw-pipe operation, and absence of authority, signature, key, and transcript bytes from captured diagnostics.\n6. `bash scripts/gate.sh` and `bash scripts/delegated-lane.sh` pass, and the planning store validates.\n\n## Out of Scope\n\nIdentity implementation and login, credential storage, product policy, resumable attachments, TLS-terminating proxies, and remote SDK automation.\n"},"command":"update-entity","target":"01MEM0000000000000100"},"payload":{"actor":"human:timo","at":1788246211829,"causation":"cmd-protocol-artifact-body-story-network-session-authority-1788246211829","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body","event_id":"story:network-session-authority@9#0~b741800260447fe4","executor":null,"recorded_at":"2026-09-01T07:03:31Z"}}
{"entity":"story","version":1,"id":"network-session-authority","revision":9,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"scripts/gate.sh","source":"bash scripts/gate.sh","target":"01MEM0000000000000100"},"payload":{"actor":"human:timo","at":1788249081000,"causation":"cmd-evidence-story-network-session-authority-test_result-1788249081454","change":{"change":"evidence","kind":"test_result","reference":"scripts/gate.sh","source":"bash scripts/gate.sh"},"correlation":"protocol-artifact-evidence","event_id":"story:network-session-authority@9#0~e5af1a1640340ff7","executor":null,"recorded_at":"2026-09-01T07:51:21Z"}}
{"entity":"story","version":1,"id":"network-session-authority","revision":9,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"scripts/delegated-lane.sh","source":"bash scripts/delegated-lane.sh","target":"01MEM0000000000000100"},"payload":{"actor":"human:timo","at":1788249081000,"causation":"cmd-evidence-story-network-session-authority-test_result-1788249081714","change":{"change":"evidence","kind":"test_result","reference":"scripts/delegated-lane.sh","source":"bash scripts/delegated-lane.sh"},"correlation":"protocol-artifact-evidence","event_id":"story:network-session-authority@9#0~2df9cef5ff897fff","executor":null,"recorded_at":"2026-09-01T07:51:21Z"}}
{"entity":"story","version":1,"id":"network-session-authority","revision":9,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"artifact","reference":"contracts/substrate-wire/0.14.0/bundle.json","source":"cargo xtask check-bundle 0.14.0","target":"01MEM0000000000000100"},"payload":{"actor":"human:timo","at":1788249081000,"causation":"cmd-evidence-story-network-session-authority-artifact-1788249081972","change":{"change":"evidence","kind":"artifact","reference":"contracts/substrate-wire/0.14.0/bundle.json","source":"cargo xtask check-bundle 0.14.0"},"correlation":"protocol-artifact-evidence","event_id":"story:network-session-authority@9#0~acfecded6a0c59a2","executor":null,"recorded_at":"2026-09-01T07:51:21Z"}}
{"entity":"story","version":1,"id":"network-session-authority","revision":10,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":2,\"artifact\":1,\"specification\":1}}","target":"01MEM0000000000000100","to":"implemented"},"payload":{"actor":"human:timo","at":1788249091309,"causation":"cmd-move-story-network-session-authority-1788249091309","change":{"change":"moved","decided_on":{"recorded":{"artifact":1,"specification":1,"test_result":2}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:network-session-authority@10#0~1116cdd8dda27bfc","executor":null,"recorded_at":"2026-09-01T07:51:31Z"}}
{"entity":"story","version":1,"id":"canonical-session-routes","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Story: Session is the canonical route and SDK resource\n\n## Outcome\n\nThe public daemon and Rust SDK call the leased mode-neutral resource a session. /v1/sessions is canonical for pipes and PTY modes; /v1/pipe-sessions remains only as a declared compatibility alias until a separately coordinated removal.\n\n## Design closure\n\nThe operator accepted docs/design/16-sessions-are-not-pipe-sessions.md. Its additive migration preserves old clients, declares alias_of, makes canonical request hashing path-independent across the two spellings, and gives the gate exact route-path and alias drift evidence. A new ADR records the acceptance before implementation.\n\n## Acceptance\n\n1. A successor bundle adds canonical /v1/sessions routes, declares every old /v1/pipe-sessions entry as an exact alias, and leaves all earlier bundles unchanged.\n2. The daemon registers both route families to the same handlers and normalizes mutation hashes to canonical /v1/sessions templates.\n3. SDK-owned public names and examples use Session; source-compatible PipeSession aliases remain for existing callers, and the SDK sends canonical routes.\n4. Cross-path replay with one operation id observes one durable operation. Unix, development TCP and hosted TLS route sets preserve their existing security posture.\n5. The full gate, delegated lane and planning validation pass.\n\n## Out of Scope\n\nRemoving the legacy paths or frozen wire type names, and coordinating that later breaking change with consumers.\n","owner":"substrate","summary":"The mode-neutral /v1/sessions family becomes canonical while pipe-session remains a declared compatibility alias.","tags":["session","wave/remote-foundation-01","wire"],"title":"Session is the canonical route and SDK resource"},"args":{"command":"create-entity","data":{"body":"# Story: Session is the canonical route and SDK resource\n\n## Outcome\n\nThe public daemon and Rust SDK call the leased mode-neutral resource a session. /v1/sessions is canonical for pipes and PTY modes; /v1/pipe-sessions remains only as a declared compatibility alias until a separately coordinated removal.\n\n## Design closure\n\nThe operator accepted docs/design/16-sessions-are-not-pipe-sessions.md. Its additive migration preserves old clients, declares alias_of, makes canonical request hashing path-independent across the two spellings, and gives the gate exact route-path and alias drift evidence. A new ADR records the acceptance before implementation.\n\n## Acceptance\n\n1. A successor bundle adds canonical /v1/sessions routes, declares every old /v1/pipe-sessions entry as an exact alias, and leaves all earlier bundles unchanged.\n2. The daemon registers both route families to the same handlers and normalizes mutation hashes to canonical /v1/sessions templates.\n3. SDK-owned public names and examples use Session; source-compatible PipeSession aliases remain for existing callers, and the SDK sends canonical routes.\n4. Cross-path replay with one operation id observes one durable operation. Unix, development TCP and hosted TLS route sets preserve their existing security posture.\n5. The full gate, delegated lane and planning validation pass.\n\n## Out of Scope\n\nRemoving the legacy paths or frozen wire type names, and coordinating that later breaking change with consumers.\n","owner":"substrate","status":"draft","summary":"The mode-neutral /v1/sessions family becomes canonical while pipe-session remains a declared compatibility alias.","tags":["session","wave/remote-foundation-01","wire"],"title":"Session is the canonical route and SDK resource"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/canonical-session-routes"},"payload":{"actor":"human:timo","at":1788250466506,"causation":"cmd-new-story-canonical-session-routes","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:canonical-session-routes@1#0~cbe40fcf121e8473","executor":null,"recorded_at":"2026-09-01T08:14:26Z"}}
{"entity":"story","version":1,"id":"canonical-session-routes","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:remote-serving"}]},"args":{"command":"create-relation","kind":"decomposes","source":"01MEM0000000000000362","target":"01MEM0000000000000019"},"payload":{"actor":"human:timo","at":1788250466522,"causation":"cmd-story-canonical-session-routes-decomposes-epic-remote-serving","change":{"change":"related","relation":"decomposes","target":"epic:remote-serving"},"correlation":"protocol-artifact-relate","event_id":"story:canonical-session-routes@1#0~c5915b94004e83d0","executor":null,"recorded_at":"2026-09-01T08:14:26Z"}}
{"entity":"story","version":1,"id":"canonical-session-routes","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:remote-serving"},{"depends_on":"story:contract-gate-sees-route-paths"}]},"args":{"command":"create-relation","kind":"depends_on","source":"01MEM0000000000000362","target":"01MEM0000000000000052"},"payload":{"actor":"human:timo","at":1788250466532,"causation":"cmd-story-canonical-session-routes-depends_on-story-contract-gate-sees-route-paths","change":{"change":"related","relation":"depends_on","target":"story:contract-gate-sees-route-paths"},"correlation":"protocol-artifact-relate","event_id":"story:canonical-session-routes@1#0~5c0427a689615662","executor":null,"recorded_at":"2026-09-01T08:14:26Z"}}
{"entity":"story","version":1,"id":"canonical-session-routes","revision":1,"type":"aep.evidence.record/v1","from_state":"draft","to_state":"draft","changed":{},"args":{"command":"record-evidence","kind":"specification","reference":"docs/design/16-sessions-are-not-pipe-sessions.md","source":"operator acceptance of session naming","target":"01MEM0000000000000040"},"payload":{"actor":"human:timo","at":1788250472000,"causation":"cmd-evidence-story-canonical-session-routes-specification-1788250472165","change":{"change":"evidence","kind":"specification","reference":"docs/design/16-sessions-are-not-pipe-sessions.md","source":"operator acceptance of session naming"},"correlation":"protocol-artifact-evidence","event_id":"story:canonical-session-routes@1#0~d120284ba2b0f5a6","executor":null,"recorded_at":"2026-09-01T08:14:32Z"}}
{"entity":"story","version":1,"id":"canonical-session-routes","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"specification\":1}}","target":"01MEM0000000000000040","to":"proposed"},"payload":{"actor":"human:timo","at":1788250472521,"causation":"cmd-move-story-canonical-session-routes-1788250472521","change":{"change":"moved","decided_on":{"recorded":{"specification":1}},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:canonical-session-routes@2#0~7d33260e3037d6e4","executor":null,"recorded_at":"2026-09-01T08:14:32Z"}}
{"entity":"story","version":1,"id":"canonical-session-routes","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"specification\":1}}","target":"01MEM0000000000000040","to":"active"},"payload":{"actor":"human:timo","at":1788250472870,"causation":"cmd-move-story-canonical-session-routes-1788250472870","change":{"change":"moved","decided_on":{"recorded":{"specification":1}},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:canonical-session-routes@3#0~250ba9a3227f7088","executor":null,"recorded_at":"2026-09-01T08:14:32Z"}}
{"entity":"story","version":1,"id":"canonical-session-routes","revision":4,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"summary":"The breaking 0.15.0 contract exposes the leased resource only at /v1/sessions and the SDK uses Session naming."},"args":{"changes":{"summary":"The breaking 0.15.0 contract exposes the leased resource only at /v1/sessions and the SDK uses Session naming."},"command":"update-entity","target":"01MEM0000000000000040"},"payload":{"actor":"human:timo","at":1788251528408,"causation":"cmd-protocol-artifact-set-story-canonical-session-routes-1788251528408","change":{"change":"body_replaced"},"correlation":"protocol-artifact-set","event_id":"story:canonical-session-routes@4#0~848ac5081e5615a4","executor":null,"recorded_at":"2026-09-01T08:32:08Z"}}
{"entity":"story","version":1,"id":"canonical-session-routes","revision":5,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"body":"# Story: Session is the canonical route and SDK resource\n\n## Outcome\n\nThe public daemon, contract and Rust SDK call the leased mode-neutral resource a session. The eight operations for pipes and PTY modes are served only beneath `/v1/sessions`; `/v1/pipe-sessions` is removed without a compatibility route or SDK alias.\n\n## Design closure\n\nThe operator accepted the breaking migration in `docs/design/16-sessions-are-not-pipe-sessions.md`, Substrate ADR 0028 and Atlas ADR 0022. The successor is closed to the exact eight path replacements, preserves all non-session routes, keeps durable `session.*` operation ids, and leaves every earlier bundle byte-identical.\n\n## Acceptance\n\n1. Successor bundle `0.15.0` names predecessor `0.14.0`, records exactly eight removed and eight added route addresses, and the checker refuses any other removal, addition or semantic drift.\n2. The daemon registers only `/v1/sessions`; every legacy `/v1/pipe-sessions` request returns the ordinary route-not-found response on Unix, development TCP and hosted TLS.\n3. Public Rust SDK and current Rust wire resource names use `Session` without source-compatibility aliases, and the SDK sends only canonical routes.\n4. Runtime vectors, contract-derived route inventory, MCP adapter, examples and public documentation use the canonical family while frozen predecessor bytes remain unchanged.\n5. The full gate, genuinely delegated lane and planning validation pass.\n\n## Out of Scope\n\nChanging the `pipes` session mode or its pipe-channel frame vocabulary. Those names describe the I/O mode, not the leased resource.\n"},"args":{"changes":{"body":"# Story: Session is the canonical route and SDK resource\n\n## Outcome\n\nThe public daemon, contract and Rust SDK call the leased mode-neutral resource a session. The eight operations for pipes and PTY modes are served only beneath `/v1/sessions`; `/v1/pipe-sessions` is removed without a compatibility route or SDK alias.\n\n## Design closure\n\nThe operator accepted the breaking migration in `docs/design/16-sessions-are-not-pipe-sessions.md`, Substrate ADR 0028 and Atlas ADR 0022. The successor is closed to the exact eight path replacements, preserves all non-session routes, keeps durable `session.*` operation ids, and leaves every earlier bundle byte-identical.\n\n## Acceptance\n\n1. Successor bundle `0.15.0` names predecessor `0.14.0`, records exactly eight removed and eight added route addresses, and the checker refuses any other removal, addition or semantic drift.\n2. The daemon registers only `/v1/sessions`; every legacy `/v1/pipe-sessions` request returns the ordinary route-not-found response on Unix, development TCP and hosted TLS.\n3. Public Rust SDK and current Rust wire resource names use `Session` without source-compatibility aliases, and the SDK sends only canonical routes.\n4. Runtime vectors, contract-derived route inventory, MCP adapter, examples and public documentation use the canonical family while frozen predecessor bytes remain unchanged.\n5. The full gate, genuinely delegated lane and planning validation pass.\n\n## Out of Scope\n\nChanging the `pipes` session mode or its pipe-channel frame vocabulary. Those names describe the I/O mode, not the leased resource.\n"},"command":"update-entity","target":"01MEM0000000000000040"},"payload":{"actor":"human:timo","at":1788251528547,"causation":"cmd-protocol-artifact-body-story-canonical-session-routes-1788251528547","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body","event_id":"story:canonical-session-routes@5#0~2a3a2c389bb7a0a4","executor":null,"recorded_at":"2026-09-01T08:32:08Z"}}
{"entity":"story","version":1,"id":"canonical-session-routes","revision":6,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"summary":"The breaking 0.15.0 contract serves the existing session operations only at /v1/sessions."},"args":{"changes":{"summary":"The breaking 0.15.0 contract serves the existing session operations only at /v1/sessions."},"command":"update-entity","target":"01MEM0000000000000040"},"payload":{"actor":"human:timo","at":1788251634976,"causation":"cmd-protocol-artifact-set-story-canonical-session-routes-1788251634976","change":{"change":"body_replaced"},"correlation":"protocol-artifact-set","event_id":"story:canonical-session-routes@6#0~996a68a1defba4cc","executor":null,"recorded_at":"2026-09-01T08:33:54Z"}}
{"entity":"story","version":1,"id":"canonical-session-routes","revision":7,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"body":"# Story: Rename the session route family\n\n## Outcome\n\nThe existing eight session operations move from `/v1/pipe-sessions` to `/v1/sessions`. The old paths are not registered and there is no compatibility alias.\n\n## Design closure\n\nThe operator accepted the breaking route migration in `docs/design/16-sessions-are-not-pipe-sessions.md`, Substrate ADR 0028 and Atlas ADR 0022. The successor is closed to the exact eight path replacements, preserves all non-session routes and operation semantics, and leaves every earlier bundle byte-identical.\n\n## Acceptance\n\n1. Successor bundle `0.15.0` names predecessor `0.14.0`, records exactly eight removed and eight added route addresses, and the checker refuses any other removal, addition or semantic drift.\n2. The daemon registers only `/v1/sessions`; every legacy `/v1/pipe-sessions` request returns the ordinary route-not-found response on Unix, development TCP and hosted TLS.\n3. The SDK sends only the renamed routes; tests, contract-derived runtime inventory, MCP references, examples and public documentation use them.\n4. Operation ids, request/result schemas, Rust resource types, modes and byte-plane vocabulary do not change as part of this route-only migration.\n5. The full gate, genuinely delegated lane and planning validation pass.\n\n## Out of Scope\n\nRenaming Rust types, schema authority files, operation ids, or the `pipes` I/O mode.\n"},"args":{"changes":{"body":"# Story: Rename the session route family\n\n## Outcome\n\nThe existing eight session operations move from `/v1/pipe-sessions` to `/v1/sessions`. The old paths are not registered and there is no compatibility alias.\n\n## Design closure\n\nThe operator accepted the breaking route migration in `docs/design/16-sessions-are-not-pipe-sessions.md`, Substrate ADR 0028 and Atlas ADR 0022. The successor is closed to the exact eight path replacements, preserves all non-session routes and operation semantics, and leaves every earlier bundle byte-identical.\n\n## Acceptance\n\n1. Successor bundle `0.15.0` names predecessor `0.14.0`, records exactly eight removed and eight added route addresses, and the checker refuses any other removal, addition or semantic drift.\n2. The daemon registers only `/v1/sessions`; every legacy `/v1/pipe-sessions` request returns the ordinary route-not-found response on Unix, development TCP and hosted TLS.\n3. The SDK sends only the renamed routes; tests, contract-derived runtime inventory, MCP references, examples and public documentation use them.\n4. Operation ids, request/result schemas, Rust resource types, modes and byte-plane vocabulary do not change as part of this route-only migration.\n5. The full gate, genuinely delegated lane and planning validation pass.\n\n## Out of Scope\n\nRenaming Rust types, schema authority files, operation ids, or the `pipes` I/O mode.\n"},"command":"update-entity","target":"01MEM0000000000000040"},"payload":{"actor":"human:timo","at":1788251635103,"causation":"cmd-protocol-artifact-body-story-canonical-session-routes-1788251635103","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body","event_id":"story:canonical-session-routes@7#0~d6b09b6d7a708a08","executor":null,"recorded_at":"2026-09-01T08:33:55Z"}}
{"entity":"story","version":1,"id":"canonical-session-routes","revision":7,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"scripts/gate.sh","source":"bash scripts/gate.sh: passed","target":"01MEM0000000000000040"},"payload":{"actor":"human:timo","at":1788253051000,"causation":"cmd-evidence-story-canonical-session-routes-test_result-1788253051387","change":{"change":"evidence","kind":"test_result","reference":"scripts/gate.sh","source":"bash scripts/gate.sh: passed"},"correlation":"protocol-artifact-evidence","event_id":"story:canonical-session-routes@7#0~a90edc994137a20e","executor":null,"recorded_at":"2026-09-01T08:57:31Z"}}
{"entity":"story","version":1,"id":"canonical-session-routes","revision":7,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"scripts/delegated-lane.sh","source":"bash scripts/delegated-lane.sh: delegated clean-room passed","target":"01MEM0000000000000040"},"payload":{"actor":"human:timo","at":1788253051000,"causation":"cmd-evidence-story-canonical-session-routes-test_result-1788253051523","change":{"change":"evidence","kind":"test_result","reference":"scripts/delegated-lane.sh","source":"bash scripts/delegated-lane.sh: delegated clean-room passed"},"correlation":"protocol-artifact-evidence","event_id":"story:canonical-session-routes@7#0~548389758af42f46","executor":null,"recorded_at":"2026-09-01T08:57:31Z"}}
{"entity":"story","version":1,"id":"canonical-session-routes","revision":8,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":2,\"specification\":1}}","target":"01MEM0000000000000040","to":"implemented"},"payload":{"actor":"human:timo","at":1788253051736,"causation":"cmd-move-story-canonical-session-routes-1788253051736","change":{"change":"moved","decided_on":{"recorded":{"specification":1,"test_result":2}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:canonical-session-routes@8#0~2456e0b19c5a80dc","executor":null,"recorded_at":"2026-09-01T08:57:31Z"}}
{"entity":"story","version":1,"id":"remote-sdk-transport","revision":4,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000130","to":"active"},"payload":{"actor":"human:timo","at":1788249893624,"causation":"cmd-move-story-remote-sdk-transport-1788249893624","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:remote-sdk-transport@4#0~2b08abc63630e648","executor":null,"recorded_at":"2026-09-01T08:04:53Z"}}
{"entity":"story","version":1,"id":"remote-sdk-transport","revision":4,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"local:2026-09-01/full-gate","source":"bash scripts/gate.sh","target":"01MEM0000000000000133"},"payload":{"actor":"human:timo","at":1788255160000,"causation":"cmd-evidence-story-remote-sdk-transport-test_result-1788255160542","change":{"change":"evidence","kind":"test_result","reference":"local:2026-09-01/full-gate","source":"bash scripts/gate.sh"},"correlation":"protocol-artifact-evidence","event_id":"story:remote-sdk-transport@4#0~7f518bf9f7008c65","executor":null,"recorded_at":"2026-09-01T09:32:40Z"}}
{"entity":"story","version":1,"id":"remote-sdk-transport","revision":4,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"local:2026-09-01/delegated-remote-wss","source":"bash scripts/delegated-lane.sh","target":"01MEM0000000000000133"},"payload":{"actor":"human:timo","at":1788255160000,"causation":"cmd-evidence-story-remote-sdk-transport-test_result-1788255160671","change":{"change":"evidence","kind":"test_result","reference":"local:2026-09-01/delegated-remote-wss","source":"bash scripts/delegated-lane.sh"},"correlation":"protocol-artifact-evidence","event_id":"story:remote-sdk-transport@4#0~71be34495eb61fd7","executor":null,"recorded_at":"2026-09-01T09:32:40Z"}}
{"entity":"story","version":1,"id":"remote-sdk-transport","revision":4,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"website/","source":"npm ci && npm run typecheck && npm run build","target":"01MEM0000000000000133"},"payload":{"actor":"human:timo","at":1788255160000,"causation":"cmd-evidence-story-remote-sdk-transport-test_result-1788255160800","change":{"change":"evidence","kind":"test_result","reference":"website/","source":"npm ci && npm run typecheck && npm run build"},"correlation":"protocol-artifact-evidence","event_id":"story:remote-sdk-transport@4#0~c2846fdf224ca7b6","executor":null,"recorded_at":"2026-09-01T09:32:40Z"}}
{"entity":"story","version":1,"id":"remote-sdk-transport","revision":5,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":3}}","target":"01MEM0000000000000133","to":"implemented"},"payload":{"actor":"human:timo","at":1788255166893,"causation":"cmd-move-story-remote-sdk-transport-1788255166893","change":{"change":"moved","decided_on":{"recorded":{"test_result":3}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:remote-sdk-transport@5#0~7b847aa818e53058","executor":null,"recorded_at":"2026-09-01T09:32:46Z"}}
{"entity":"story","version":1,"id":"rust-sdk-publication","revision":7,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"body":"# Story: The Rust SDK and runtime chain are owner-released Rust packages\n\n## Outcome\n\nConsumers depend on version-locked `b10x-substrate-*` packages rather than a sibling checkout. The default client stays transport-only; linked daemon support is opt-in.\n\n## Acceptance\n\nThe gate permits only the named package set, verifies package contents and downstream builds, and keeps every other workspace member non-publishable. A fully gated annotated release tag is published to crates.io manually in dependency order with an operator-held scoped token that never enters GitHub.\n\n## Current state — 2026-09-01\n\nThe implementation, package allowlist, exact internal version edges, package-content checks, README material and public SDK journeys shipped on main and pass the required Full gate. This story stays active only for the external publication proof: publish the owner-released package chain to crates.io in dependency order from a fully gated annotated tag, verify anonymous clean-room installation, and record the registry versions and digests. No GitHub secret or repository-held crates.io token is introduced.\n\n## Publication readiness audit — 2026-09-01\n\n- Annotated tag `0.4.1` resolves to fully gated commit `961be39c34ef1f9b655a0b5a58a108dabc722d5c`; current main changes no package inputs after that tag.\n- Anonymous crates.io API and search returned no package for all five approved names. This is current availability, not a reservation; first-publication races remain possible.\n- Publish from a clean detached `0.4.1` checkout in dependency order: `b10x-substrate-wire`, `b10x-substrate-store`, `b10x-substrate-host`, `b10x-substrate-daemon`, `b10x-substrate-sdk`.\n- Two package runs were byte-identical. Predicted registry checksums are wire `c622fcee231eb2fe7c238b122642b3e9b2734d2d0023e2d1e0e4d2bf3c710c88`, store `fe785c9fd580c40ec8f207fa2edc19c9543bf8bc9c9a802ffb9ca5ffd1bffd10`, host `9aefc892172da3d348953b93dd07ada7d2deb43e13010e66937c66e4b9bdd16e`, daemon `f134c26509711060c6257ecd17e04101116f23571737c595021e01f396299866`, SDK `e45d37f8eb28f10b39c1f910f6715d467f90736108e904e781f59031f527adab`.\n- Temporary-registry dependency resolution and SDK `linked-daemon` verification passed. The owner-private Cargo credentials file exists, but token validity and `publish-new` scope remain intentionally unproven until the operator-authorized live publication.\n- Closure still requires the contract-bundle dependency, five live registry checksums, and credential-free clean-room default SDK, linked-daemon and daemon installation tests.\n\n## Disposition — source distribution replaces registry publication\n\nNo b10x Substrate crate was published to crates.io. The operator clarified that b10x does not use\nthat registry as a general distribution surface, so completing the original acceptance would create\nan unwanted permanent external release rather than close a product need.\n\nADR 0030 replaces registry publication with source consumption from a local path or exact Git\nrevision and keeps signed GitHub/GHCR artifacts as the release surfaces. Every workspace package is\nnow non-publishable, and `cargo xtask check-packages` fails closed on any future `publish = true`\nwhile retaining the five runtime packages' names, exact internal edges, SPDX metadata, READMEs and\npublic documentation targets.\n\nThis story is archived as deliberately superseded, not marked implemented: its crates.io outcome\ndid not happen and is no longer desired.\n"},"args":{"changes":{"body":"# Story: The Rust SDK and runtime chain are owner-released Rust packages\n\n## Outcome\n\nConsumers depend on version-locked `b10x-substrate-*` packages rather than a sibling checkout. The default client stays transport-only; linked daemon support is opt-in.\n\n## Acceptance\n\nThe gate permits only the named package set, verifies package contents and downstream builds, and keeps every other workspace member non-publishable. A fully gated annotated release tag is published to crates.io manually in dependency order with an operator-held scoped token that never enters GitHub.\n\n## Current state — 2026-09-01\n\nThe implementation, package allowlist, exact internal version edges, package-content checks, README material and public SDK journeys shipped on main and pass the required Full gate. This story stays active only for the external publication proof: publish the owner-released package chain to crates.io in dependency order from a fully gated annotated tag, verify anonymous clean-room installation, and record the registry versions and digests. No GitHub secret or repository-held crates.io token is introduced.\n\n## Publication readiness audit — 2026-09-01\n\n- Annotated tag `0.4.1` resolves to fully gated commit `961be39c34ef1f9b655a0b5a58a108dabc722d5c`; current main changes no package inputs after that tag.\n- Anonymous crates.io API and search returned no package for all five approved names. This is current availability, not a reservation; first-publication races remain possible.\n- Publish from a clean detached `0.4.1` checkout in dependency order: `b10x-substrate-wire`, `b10x-substrate-store`, `b10x-substrate-host`, `b10x-substrate-daemon`, `b10x-substrate-sdk`.\n- Two package runs were byte-identical. Predicted registry checksums are wire `c622fcee231eb2fe7c238b122642b3e9b2734d2d0023e2d1e0e4d2bf3c710c88`, store `fe785c9fd580c40ec8f207fa2edc19c9543bf8bc9c9a802ffb9ca5ffd1bffd10`, host `9aefc892172da3d348953b93dd07ada7d2deb43e13010e66937c66e4b9bdd16e`, daemon `f134c26509711060c6257ecd17e04101116f23571737c595021e01f396299866`, SDK `e45d37f8eb28f10b39c1f910f6715d467f90736108e904e781f59031f527adab`.\n- Temporary-registry dependency resolution and SDK `linked-daemon` verification passed. The owner-private Cargo credentials file exists, but token validity and `publish-new` scope remain intentionally unproven until the operator-authorized live publication.\n- Closure still requires the contract-bundle dependency, five live registry checksums, and credential-free clean-room default SDK, linked-daemon and daemon installation tests.\n\n## Disposition — source distribution replaces registry publication\n\nNo b10x Substrate crate was published to crates.io. The operator clarified that b10x does not use\nthat registry as a general distribution surface, so completing the original acceptance would create\nan unwanted permanent external release rather than close a product need.\n\nADR 0030 replaces registry publication with source consumption from a local path or exact Git\nrevision and keeps signed GitHub/GHCR artifacts as the release surfaces. Every workspace package is\nnow non-publishable, and `cargo xtask check-packages` fails closed on any future `publish = true`\nwhile retaining the five runtime packages' names, exact internal edges, SPDX metadata, READMEs and\npublic documentation targets.\n\nThis story is archived as deliberately superseded, not marked implemented: its crates.io outcome\ndid not happen and is no longer desired.\n"},"command":"update-entity","target":"01MEM0000000000000148"},"payload":{"actor":"human:timo","at":1788256073184,"causation":"cmd-protocol-artifact-body-section-story-rust-sdk-publication-1788256073184","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:rust-sdk-publication@7#0~8457252b568dba03","executor":null,"recorded_at":"2026-09-01T09:47:53Z"}}
{"entity":"story","version":1,"id":"rust-sdk-publication","revision":7,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"specification","reference":"adr/0030-rust-crates-are-source-distributed-and-non-publishable.md","source":"ADR 0030: source-distributed and non-publishable","target":"01MEM0000000000000148"},"payload":{"actor":"human:timo","at":1788256073000,"causation":"cmd-evidence-story-rust-sdk-publication-specification-1788256073323","change":{"change":"evidence","kind":"specification","reference":"adr/0030-rust-crates-are-source-distributed-and-non-publishable.md","source":"ADR 0030: source-distributed and non-publishable"},"correlation":"protocol-artifact-evidence","event_id":"story:rust-sdk-publication@7#0~e6072a527a9fb892","executor":null,"recorded_at":"2026-09-01T09:47:53Z"}}
{"entity":"story","version":1,"id":"rust-sdk-publication","revision":7,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"xtask/src/packages.rs","source":"cargo xtask check-packages","target":"01MEM0000000000000148"},"payload":{"actor":"human:timo","at":1788256073000,"causation":"cmd-evidence-story-rust-sdk-publication-test_result-1788256073453","change":{"change":"evidence","kind":"test_result","reference":"xtask/src/packages.rs","source":"cargo xtask check-packages"},"correlation":"protocol-artifact-evidence","event_id":"story:rust-sdk-publication@7#0~44b042603e2fffc5","executor":null,"recorded_at":"2026-09-01T09:47:53Z"}}
{"entity":"story","version":1,"id":"rust-sdk-publication","revision":8,"type":"aep.status.move/v1","from_state":"active","to_state":"archived","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1,\"specification\":1}}","target":"01MEM0000000000000148","to":"archived"},"payload":{"actor":"human:timo","at":1788256073710,"causation":"cmd-move-story-rust-sdk-publication-1788256073710","change":{"change":"moved","decided_on":{"recorded":{"specification":1,"test_result":1}},"from":"active","to":"archived"},"correlation":"protocol-artifact-move","event_id":"story:rust-sdk-publication@8#0~fc6b23a5a60bbb88","executor":null,"recorded_at":"2026-09-01T09:47:53Z"}}
{"entity":"epic","version":1,"id":"rust-sdk","revision":5,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"body":"# Epic: Public Rust SDK\n\n## Outcome\n\nRust consumers create, control and observe confined workspaces and processes through an ergonomic, owner-released SDK. A managed local deployment is still a separate authenticated daemon child.\n\n## Boundaries\n\nThe SDK serves the daemon-advertised `substrate-wire/0.4.0` surface over an owner-private Unix socket. It chooses no product execution policy, exposes no driver API, and does not make the development contract stable.\n\n## Delivery\n\nTyped client and recovery, managed daemon ownership, approved package publication, and public adoption documentation are separate stories under this epic.\n\n## Reconciled state — 2026-09-01\n\nTyped local and remote clients, managed daemon ownership, the public SDK journey and parity with the\ndaemon-advertised contract frontier are implemented. Registry publication was deliberately archived:\nADR 0030 replaces it with source consumption by local path or exact Git revision, and the repository\ngate refuses every publishable workspace member. Remote HTTPS/WSS transport remains owned by\n`epic:remote-serving`; its implementation does not keep this SDK epic open.\n"},"args":{"changes":{"body":"# Epic: Public Rust SDK\n\n## Outcome\n\nRust consumers create, control and observe confined workspaces and processes through an ergonomic, owner-released SDK. A managed local deployment is still a separate authenticated daemon child.\n\n## Boundaries\n\nThe SDK serves the daemon-advertised `substrate-wire/0.4.0` surface over an owner-private Unix socket. It chooses no product execution policy, exposes no driver API, and does not make the development contract stable.\n\n## Delivery\n\nTyped client and recovery, managed daemon ownership, approved package publication, and public adoption documentation are separate stories under this epic.\n\n## Reconciled state — 2026-09-01\n\nTyped local and remote clients, managed daemon ownership, the public SDK journey and parity with the\ndaemon-advertised contract frontier are implemented. Registry publication was deliberately archived:\nADR 0030 replaces it with source consumption by local path or exact Git revision, and the repository\ngate refuses every publishable workspace member. Remote HTTPS/WSS transport remains owned by\n`epic:remote-serving`; its implementation does not keep this SDK epic open.\n"},"command":"update-entity","target":"01MEM0000000000000025"},"payload":{"actor":"human:timo","at":1788256481073,"causation":"cmd-protocol-artifact-body-section-epic-rust-sdk-1788256481073","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"epic:rust-sdk@5#0~961663209aad478f","executor":null,"recorded_at":"2026-09-01T09:54:41Z"}}
{"entity":"epic","version":1,"id":"release-hardening","revision":7,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"body":"# Epic: Release hardening\n\n## Outcome\n\nWhat the repository says about itself is what a fresh clone, a fresh runner or a consumer can\nverify: a pull request cannot show green without `scripts/gate.sh`; local and CI clippy are the\nsame compiler; `STATUS.md` describes the standalone repository; a tag yields a signed daemon image\nand a signed contract-bundle artifact whose digests are in `CHANGELOG.md`.\n\n## Why Now\n\nSubstrate went public and cut `0.2.1` on 2026-08-29 (`CHANGELOG.md`). Its working agreement calls\nthe full gate \"the bar for `main`\" (`AGENTS.md` § *The gate*), but:\n\n- `.github/workflows/` holds one workflow, `pages.yml`, which builds the website. Nothing runs the\n  gate on a push or a pull request.\n- No `rust-toolchain.toml` exists; `AGENTS.md:116-119` documents the drift (\"a newer clippy can\n  fail a commit that passed locally\") and asks for `rustup update` instead of removing the cause.\n- `Dockerfile` builds a distroless `substrate-daemon`; nothing publishes, signs or digest-pins it.\n  `README.md` § *Status*: \"stable publication: **not done**\".\n- Agent consumes the 0.4.0 bundle as a hand-copied tree (`docs/plan/04-direct-byte-plane.md` §\n  *Slice C*); design 07 § *Bundle* already fixes the OCI shape and nothing implements it.\n- Two `AGENTS.md` facts contradict the scripts they describe (`AGENTS.md:112` vs\n  `scripts/gate.sh:20-23`; `AGENTS.md:172` vs `scripts/bot-token.sh:8`), and `STATUS.md` is\n  observed 2026-08-17, before extraction, still naming the monorepo as the source.\n\n## Scope\n\nSix stories, worked in this order because each makes the next checkable:\n\n1. `story:agents-md-matches-the-scripts` — docs only.\n2. `story:ci-runs-the-full-gate` — `gate.yml`, SHA-pinned actions, `contents: read`.\n3. `story:pinned-rust-toolchain` — `rust-toolchain.toml` = `1.97` = `Cargo.toml` `rust-version` =\n   `Dockerfile` builder tag; one commit per bump.\n4. `story:status-md-re-observed` — from fresh gate output.\n5. `story:signed-daemon-image` — `release.yml` on a bare-version tag, GHCR, cosign keyless.\n6. `story:contract-bundle-oci-artifact` — deterministic OCI layout, published beside the image.\n\n## Out of Scope\n\nAny route, schema, wire identifier, capability or frozen bundle byte. The development bundles stay\ndevelopment bundles; a stable-contract decision is its own ADR under atlas ADR 0019.\nBit-for-bit reproducible binaries: digests are recorded, reproduction is a later milestone.\n\n## Risks\n\n- `packages: write` and `id-token: write` widen a workflow's authority; scope them to the release\n  job only.\n- The delegated lane needs bubblewrap and cgroup delegation and cannot run on a hosted GitHub\n  runner; either a self-hosted runner joins, or CI asserts the portable lane and the delegated\n  lane stays a local pre-release step. The CI story records which.\n- Keyless signing depends on Sigstore availability; a release fails closed, never publishes\n  unsigned.\n\n## Done When\n\nFrom a fresh clone: `AGENTS.md` matches the scripts; a PR with a formatting error goes red; clippy\nruns on the pinned toolchain; `STATUS.md` names the standalone repository and the current tag; a\ntag `0.x.y` yields a signed image and a signed bundle artifact whose digests are in `CHANGELOG.md`.\n\n## Correction — 2026-08-30\n\nScope above says \"Six stories\". **Eight** decompose this epic: the six named plus\n`story:tooling-moves-to-cargo-xtask` (implemented — the gate's Python moved to `cargo xtask`) and\n`story:contract-gate-sees-route-paths` (draft — the successor check reads operation ids and never\nthe paths they are served at, so a route can move silently).\n\nSix of the eight are implemented. `story:contract-bundle-oci-artifact` is active and unblocked;\n`story:contract-gate-sees-route-paths` is a draft filed today.\n\n## Reconciled state — 2026-09-01\n\nEvery decomposing story is implemented: CI and toolchain checks, repository recovery and hardening,\nroute and compatibility gates, the promoted advertised frontier, public Apache-2.0 distribution,\nand signed daemon, MCP and development contract-bundle artifacts. A signed development bundle is\nstill not a stable contract. Later releases reuse these mechanisms; they do not keep this epic open.\n"},"args":{"changes":{"body":"# Epic: Release hardening\n\n## Outcome\n\nWhat the repository says about itself is what a fresh clone, a fresh runner or a consumer can\nverify: a pull request cannot show green without `scripts/gate.sh`; local and CI clippy are the\nsame compiler; `STATUS.md` describes the standalone repository; a tag yields a signed daemon image\nand a signed contract-bundle artifact whose digests are in `CHANGELOG.md`.\n\n## Why Now\n\nSubstrate went public and cut `0.2.1` on 2026-08-29 (`CHANGELOG.md`). Its working agreement calls\nthe full gate \"the bar for `main`\" (`AGENTS.md` § *The gate*), but:\n\n- `.github/workflows/` holds one workflow, `pages.yml`, which builds the website. Nothing runs the\n  gate on a push or a pull request.\n- No `rust-toolchain.toml` exists; `AGENTS.md:116-119` documents the drift (\"a newer clippy can\n  fail a commit that passed locally\") and asks for `rustup update` instead of removing the cause.\n- `Dockerfile` builds a distroless `substrate-daemon`; nothing publishes, signs or digest-pins it.\n  `README.md` § *Status*: \"stable publication: **not done**\".\n- Agent consumes the 0.4.0 bundle as a hand-copied tree (`docs/plan/04-direct-byte-plane.md` §\n  *Slice C*); design 07 § *Bundle* already fixes the OCI shape and nothing implements it.\n- Two `AGENTS.md` facts contradict the scripts they describe (`AGENTS.md:112` vs\n  `scripts/gate.sh:20-23`; `AGENTS.md:172` vs `scripts/bot-token.sh:8`), and `STATUS.md` is\n  observed 2026-08-17, before extraction, still naming the monorepo as the source.\n\n## Scope\n\nSix stories, worked in this order because each makes the next checkable:\n\n1. `story:agents-md-matches-the-scripts` — docs only.\n2. `story:ci-runs-the-full-gate` — `gate.yml`, SHA-pinned actions, `contents: read`.\n3. `story:pinned-rust-toolchain` — `rust-toolchain.toml` = `1.97` = `Cargo.toml` `rust-version` =\n   `Dockerfile` builder tag; one commit per bump.\n4. `story:status-md-re-observed` — from fresh gate output.\n5. `story:signed-daemon-image` — `release.yml` on a bare-version tag, GHCR, cosign keyless.\n6. `story:contract-bundle-oci-artifact` — deterministic OCI layout, published beside the image.\n\n## Out of Scope\n\nAny route, schema, wire identifier, capability or frozen bundle byte. The development bundles stay\ndevelopment bundles; a stable-contract decision is its own ADR under atlas ADR 0019.\nBit-for-bit reproducible binaries: digests are recorded, reproduction is a later milestone.\n\n## Risks\n\n- `packages: write` and `id-token: write` widen a workflow's authority; scope them to the release\n  job only.\n- The delegated lane needs bubblewrap and cgroup delegation and cannot run on a hosted GitHub\n  runner; either a self-hosted runner joins, or CI asserts the portable lane and the delegated\n  lane stays a local pre-release step. The CI story records which.\n- Keyless signing depends on Sigstore availability; a release fails closed, never publishes\n  unsigned.\n\n## Done When\n\nFrom a fresh clone: `AGENTS.md` matches the scripts; a PR with a formatting error goes red; clippy\nruns on the pinned toolchain; `STATUS.md` names the standalone repository and the current tag; a\ntag `0.x.y` yields a signed image and a signed bundle artifact whose digests are in `CHANGELOG.md`.\n\n## Correction — 2026-08-30\n\nScope above says \"Six stories\". **Eight** decompose this epic: the six named plus\n`story:tooling-moves-to-cargo-xtask` (implemented — the gate's Python moved to `cargo xtask`) and\n`story:contract-gate-sees-route-paths` (draft — the successor check reads operation ids and never\nthe paths they are served at, so a route can move silently).\n\nSix of the eight are implemented. `story:contract-bundle-oci-artifact` is active and unblocked;\n`story:contract-gate-sees-route-paths` is a draft filed today.\n\n## Reconciled state — 2026-09-01\n\nEvery decomposing story is implemented: CI and toolchain checks, repository recovery and hardening,\nroute and compatibility gates, the promoted advertised frontier, public Apache-2.0 distribution,\nand signed daemon, MCP and development contract-bundle artifacts. A signed development bundle is\nstill not a stable contract. Later releases reuse these mechanisms; they do not keep this epic open.\n"},"command":"update-entity","target":"01MEM0000000000000016"},"payload":{"actor":"human:timo","at":1788256481202,"causation":"cmd-protocol-artifact-body-section-epic-release-hardening-1788256481202","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"epic:release-hardening@7#0~bc713ba9a5a8e79f","executor":null,"recorded_at":"2026-09-01T09:54:41Z"}}
{"entity":"story","version":1,"id":"rust-sdk-publication","revision":8,"type":"aep.evidence.record/v1","from_state":"archived","to_state":"archived","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"scripts/gate.sh","source":"bash scripts/gate.sh","target":"01MEM0000000000000148"},"payload":{"actor":"human:timo","at":1788256481000,"causation":"cmd-evidence-story-rust-sdk-publication-test_result-1788256481327","change":{"change":"evidence","kind":"test_result","reference":"scripts/gate.sh","source":"bash scripts/gate.sh"},"correlation":"protocol-artifact-evidence","event_id":"story:rust-sdk-publication@8#0~94dc9a37229213fb","executor":null,"recorded_at":"2026-09-01T09:54:41Z"}}
{"entity":"epic","version":1,"id":"rust-sdk","revision":5,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"scripts/gate.sh","source":"bash scripts/gate.sh","target":"01MEM0000000000000025"},"payload":{"actor":"human:timo","at":1788256481000,"causation":"cmd-evidence-epic-rust-sdk-test_result-1788256481456","change":{"change":"evidence","kind":"test_result","reference":"scripts/gate.sh","source":"bash scripts/gate.sh"},"correlation":"protocol-artifact-evidence","event_id":"epic:rust-sdk@5#0~1b969f5cb52a4d71","executor":null,"recorded_at":"2026-09-01T09:54:41Z"}}
{"entity":"epic","version":1,"id":"release-hardening","revision":7,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"scripts/gate.sh","source":"bash scripts/gate.sh","target":"01MEM0000000000000016"},"payload":{"actor":"human:timo","at":1788256481000,"causation":"cmd-evidence-epic-release-hardening-test_result-1788256481584","change":{"change":"evidence","kind":"test_result","reference":"scripts/gate.sh","source":"bash scripts/gate.sh"},"correlation":"protocol-artifact-evidence","event_id":"epic:release-hardening@7#0~2a5cd442867c6efb","executor":null,"recorded_at":"2026-09-01T09:54:41Z"}}
{"entity":"epic","version":1,"id":"rust-sdk","revision":6,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1}}","target":"01MEM0000000000000025","to":"implemented"},"payload":{"actor":"human:timo","at":1788256486792,"causation":"cmd-move-epic-rust-sdk-1788256486792","change":{"change":"moved","decided_on":{"recorded":{"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"epic:rust-sdk@6#0~b78db637c661d32e","executor":null,"recorded_at":"2026-09-01T09:54:46Z"}}
{"entity":"epic","version":1,"id":"release-hardening","revision":8,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1}}","target":"01MEM0000000000000016","to":"implemented"},"payload":{"actor":"human:timo","at":1788256487009,"causation":"cmd-move-epic-release-hardening-1788256487009","change":{"change":"moved","decided_on":{"recorded":{"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"epic:release-hardening@8#0~5eeef370d6c71538","executor":null,"recorded_at":"2026-09-01T09:54:47Z"}}
{"entity":"story","version":1,"id":"verified-delivery-build-latency","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Compile and validate releases without duplicate build work\n\n## Observed baseline\n\nRelease run `33498193209` took 12m15s in its publication job. Its daemon image step took 5m26s and its MCP image step took another 4m22s. The two Dockerfiles used different Cargo target-cache identities and therefore compiled the shared Rust graph twice. Main gate run `33497345920` took 9m27s: 68s restored a 4.0 GiB broad Cargo cache, 59s saved it, the release test build took 3m30s, and sequential bundle plus JSON checks took about 1m45s.\n\n## Required outcome\n\n- One pinned container builder stage compiles the daemon and MCP binaries together; named minimal runtime targets keep the daemon and stdio-only MCP image boundaries distinct.\n- The release still runs the clean-room daemon vectors against the extracted shipped daemon binary and both MCP stdio journeys against the extracted binary and final container image.\n- Current bundle fixed-point checks and closed JSON classification execute concurrently with bounded worker counts and deterministic reporting.\n- CI caches dependency artifacts rather than uploading the complete workspace target tree, while keeping every gate command and refusal.\n- Repository tests assert the shared build boundary, minimal MCP image, digest signing, anonymous readback, and write-once behavior.\n\n## Evidence to close\n\nA green full local gate, a green pull-request Full gate with step timings, and a green protected-main Full gate. The first run establishes correctness; later runs establish cache effectiveness.\n","summary":"Reduce gate and release wall time while preserving exact shipped-binary, contract, signing, and branch-protection evidence.","tags":["ci","performance","release"],"title":"Compile and validate releases without duplicate build work"},"args":{"command":"create-entity","data":{"body":"# Compile and validate releases without duplicate build work\n\n## Observed baseline\n\nRelease run `33498193209` took 12m15s in its publication job. Its daemon image step took 5m26s and its MCP image step took another 4m22s. The two Dockerfiles used different Cargo target-cache identities and therefore compiled the shared Rust graph twice. Main gate run `33497345920` took 9m27s: 68s restored a 4.0 GiB broad Cargo cache, 59s saved it, the release test build took 3m30s, and sequential bundle plus JSON checks took about 1m45s.\n\n## Required outcome\n\n- One pinned container builder stage compiles the daemon and MCP binaries together; named minimal runtime targets keep the daemon and stdio-only MCP image boundaries distinct.\n- The release still runs the clean-room daemon vectors against the extracted shipped daemon binary and both MCP stdio journeys against the extracted binary and final container image.\n- Current bundle fixed-point checks and closed JSON classification execute concurrently with bounded worker counts and deterministic reporting.\n- CI caches dependency artifacts rather than uploading the complete workspace target tree, while keeping every gate command and refusal.\n- Repository tests assert the shared build boundary, minimal MCP image, digest signing, anonymous readback, and write-once behavior.\n\n## Evidence to close\n\nA green full local gate, a green pull-request Full gate with step timings, and a green protected-main Full gate. The first run establishes correctness; later runs establish cache effectiveness.\n","status":"draft","summary":"Reduce gate and release wall time while preserving exact shipped-binary, contract, signing, and branch-protection evidence.","tags":["ci","performance","release"],"title":"Compile and validate releases without duplicate build work"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/verified-delivery-build-latency"},"payload":{"actor":"human:timo","at":1788260162318,"causation":"cmd-new-story-verified-delivery-build-latency","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:verified-delivery-build-latency@1#0~c1be7b2c7591ded1","executor":null,"recorded_at":"2026-09-01T10:56:02Z"}}
{"entity":"story","version":1,"id":"verified-delivery-build-latency","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"derived_from":"epic:release-hardening"}]},"args":{"command":"create-relation","kind":"derived_from","source":"01MEM0000000000000369","target":"01MEM0000000000000016"},"payload":{"actor":"human:timo","at":1788260162333,"causation":"cmd-story-verified-delivery-build-latency-derived_from-epic-release-hardening","change":{"change":"related","relation":"derived_from","target":"epic:release-hardening"},"correlation":"protocol-artifact-relate","event_id":"story:verified-delivery-build-latency@1#0~4a79b2d7d8f1690a","executor":null,"recorded_at":"2026-09-01T10:56:02Z"}}
{"entity":"story","version":1,"id":"verified-delivery-build-latency","revision":2,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000172","to":"proposed"},"payload":{"actor":"human:timo","at":1788260162545,"causation":"cmd-move-story-verified-delivery-build-latency-1788260162545","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:verified-delivery-build-latency@2#0~d749d744e919c1ee","executor":null,"recorded_at":"2026-09-01T10:56:02Z"}}
{"entity":"story","version":1,"id":"verified-delivery-build-latency","revision":3,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000172","to":"active"},"payload":{"actor":"human:timo","at":1788260162555,"causation":"cmd-move-story-verified-delivery-build-latency-1788260162555","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:verified-delivery-build-latency@3#0~22ff5ef3f0124eca","executor":null,"recorded_at":"2026-09-01T10:56:02Z"}}
{"entity":"story","version":1,"id":"verified-delivery-build-latency","revision":3,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"Dockerfile; cargo xtask check-bundles; cargo xtask check-json","source":"local bounded contract and shared-image build measurements","target":"01MEM0000000000000172"},"payload":{"actor":"human:timo","at":1788260842000,"causation":"cmd-evidence-story-verified-delivery-build-latency-test_result-1788260842815","change":{"change":"evidence","kind":"test_result","reference":"Dockerfile; cargo xtask check-bundles; cargo xtask check-json","source":"local bounded contract and shared-image build measurements"},"correlation":"protocol-artifact-evidence","event_id":"story:verified-delivery-build-latency@3#0~991643f619fc64b0","executor":null,"recorded_at":"2026-09-01T11:07:22Z"}}
{"entity":"story","version":1,"id":"aperture-install-failure-loses-its-errno","revision":6,"type":"aep.evidence.record/v1","from_state":"implemented","to_state":"implemented","changed":{},"args":{"command":"record-evidence","kind":"deployment_result","reference":"https://github.com/beyond10x/substrate/releases/tag/0.5.0","source":"Substrate 0.5.0 release run 33498193209","target":"01MEM0000000000000037"},"payload":{"actor":"human:timo","at":1788260851000,"causation":"cmd-evidence-story-aperture-install-failure-loses-its-errno-deployment_result-1788260851750","change":{"change":"evidence","kind":"deployment_result","reference":"https://github.com/beyond10x/substrate/releases/tag/0.5.0","source":"Substrate 0.5.0 release run 33498193209"},"correlation":"protocol-artifact-evidence","event_id":"story:aperture-install-failure-loses-its-errno@6#0~5d1b151f32f0a36e","executor":null,"recorded_at":"2026-09-01T11:07:31Z"}}
{"entity":"story","version":1,"id":"canonical-session-routes","revision":8,"type":"aep.evidence.record/v1","from_state":"implemented","to_state":"implemented","changed":{},"args":{"command":"record-evidence","kind":"deployment_result","reference":"https://github.com/beyond10x/substrate/releases/tag/0.5.0","source":"Substrate 0.5.0 release run 33498193209","target":"01MEM0000000000000040"},"payload":{"actor":"human:timo","at":1788260851000,"causation":"cmd-evidence-story-canonical-session-routes-deployment_result-1788260852024","change":{"change":"evidence","kind":"deployment_result","reference":"https://github.com/beyond10x/substrate/releases/tag/0.5.0","source":"Substrate 0.5.0 release run 33498193209"},"correlation":"protocol-artifact-evidence","event_id":"story:canonical-session-routes@8#0~226ce7f5747def14","executor":null,"recorded_at":"2026-09-01T11:07:31Z"}}
{"entity":"story","version":1,"id":"contract-bundle-oci-artifact","revision":16,"type":"aep.evidence.record/v1","from_state":"implemented","to_state":"implemented","changed":{},"args":{"command":"record-evidence","kind":"deployment_result","reference":"https://github.com/beyond10x/substrate/releases/tag/0.5.0","source":"Substrate 0.5.0 release run 33498193209","target":"01MEM0000000000000049"},"payload":{"actor":"human:timo","at":1788260852000,"causation":"cmd-evidence-story-contract-bundle-oci-artifact-deployment_result-1788260852291","change":{"change":"evidence","kind":"deployment_result","reference":"https://github.com/beyond10x/substrate/releases/tag/0.5.0","source":"Substrate 0.5.0 release run 33498193209"},"correlation":"protocol-artifact-evidence","event_id":"story:contract-bundle-oci-artifact@16#0~5fdb06f59782e2cb","executor":null,"recorded_at":"2026-09-01T11:07:32Z"}}
{"entity":"story","version":1,"id":"hosted-trust-envelope-admission","revision":7,"type":"aep.evidence.record/v1","from_state":"implemented","to_state":"implemented","changed":{},"args":{"command":"record-evidence","kind":"deployment_result","reference":"https://github.com/beyond10x/substrate/releases/tag/0.5.0","source":"Substrate 0.5.0 release run 33498193209","target":"01MEM0000000000000088"},"payload":{"actor":"human:timo","at":1788260852000,"causation":"cmd-evidence-story-hosted-trust-envelope-admission-deployment_result-1788260852562","change":{"change":"evidence","kind":"deployment_result","reference":"https://github.com/beyond10x/substrate/releases/tag/0.5.0","source":"Substrate 0.5.0 release run 33498193209"},"correlation":"protocol-artifact-evidence","event_id":"story:hosted-trust-envelope-admission@7#0~bd19f8586c42e7b0","executor":null,"recorded_at":"2026-09-01T11:07:32Z"}}
{"entity":"story","version":1,"id":"mcp-managed-disposable-daemon","revision":7,"type":"aep.evidence.record/v1","from_state":"implemented","to_state":"implemented","changed":{},"args":{"command":"record-evidence","kind":"deployment_result","reference":"https://github.com/beyond10x/substrate/releases/tag/0.5.0","source":"Substrate 0.5.0 release run 33498193209","target":"01MEM0000000000000100"},"payload":{"actor":"human:timo","at":1788260852000,"causation":"cmd-evidence-story-mcp-managed-disposable-daemon-deployment_result-1788260852834","change":{"change":"evidence","kind":"deployment_result","reference":"https://github.com/beyond10x/substrate/releases/tag/0.5.0","source":"Substrate 0.5.0 release run 33498193209"},"correlation":"protocol-artifact-evidence","event_id":"story:mcp-managed-disposable-daemon@7#0~971fbde189fc35b7","executor":null,"recorded_at":"2026-09-01T11:07:32Z"}}
{"entity":"story","version":1,"id":"network-session-authority","revision":10,"type":"aep.evidence.record/v1","from_state":"implemented","to_state":"implemented","changed":{},"args":{"command":"record-evidence","kind":"deployment_result","reference":"https://github.com/beyond10x/substrate/releases/tag/0.5.0","source":"Substrate 0.5.0 release run 33498193209","target":"01MEM0000000000000103"},"payload":{"actor":"human:timo","at":1788260852000,"causation":"cmd-evidence-story-network-session-authority-deployment_result-1788260853105","change":{"change":"evidence","kind":"deployment_result","reference":"https://github.com/beyond10x/substrate/releases/tag/0.5.0","source":"Substrate 0.5.0 release run 33498193209"},"correlation":"protocol-artifact-evidence","event_id":"story:network-session-authority@10#0~8e7598e18513162e","executor":null,"recorded_at":"2026-09-01T11:07:32Z"}}
{"entity":"story","version":1,"id":"production-tls-control-listener","revision":9,"type":"aep.evidence.record/v1","from_state":"implemented","to_state":"implemented","changed":{},"args":{"command":"record-evidence","kind":"deployment_result","reference":"https://github.com/beyond10x/substrate/releases/tag/0.5.0","source":"Substrate 0.5.0 release run 33498193209","target":"01MEM0000000000000115"},"payload":{"actor":"human:timo","at":1788260853000,"causation":"cmd-evidence-story-production-tls-control-listener-deployment_result-1788260853374","change":{"change":"evidence","kind":"deployment_result","reference":"https://github.com/beyond10x/substrate/releases/tag/0.5.0","source":"Substrate 0.5.0 release run 33498193209"},"correlation":"protocol-artifact-evidence","event_id":"story:production-tls-control-listener@9#0~6579b2ea8b3b762d","executor":null,"recorded_at":"2026-09-01T11:07:33Z"}}
{"entity":"story","version":1,"id":"promote-development-contract-frontier","revision":7,"type":"aep.evidence.record/v1","from_state":"implemented","to_state":"implemented","changed":{},"args":{"command":"record-evidence","kind":"deployment_result","reference":"https://github.com/beyond10x/substrate/releases/tag/0.5.0","source":"Substrate 0.5.0 release run 33498193209","target":"01MEM0000000000000118"},"payload":{"actor":"human:timo","at":1788260853000,"causation":"cmd-evidence-story-promote-development-contract-frontier-deployment_result-1788260853641","change":{"change":"evidence","kind":"deployment_result","reference":"https://github.com/beyond10x/substrate/releases/tag/0.5.0","source":"Substrate 0.5.0 release run 33498193209"},"correlation":"protocol-artifact-evidence","event_id":"story:promote-development-contract-frontier@7#0~06fdefea55f1ceea","executor":null,"recorded_at":"2026-09-01T11:07:33Z"}}
{"entity":"story","version":1,"id":"remote-sdk-transport","revision":5,"type":"aep.evidence.record/v1","from_state":"implemented","to_state":"implemented","changed":{},"args":{"command":"record-evidence","kind":"deployment_result","reference":"https://github.com/beyond10x/substrate/releases/tag/0.5.0","source":"Substrate 0.5.0 release run 33498193209","target":"01MEM0000000000000133"},"payload":{"actor":"human:timo","at":1788260853000,"causation":"cmd-evidence-story-remote-sdk-transport-deployment_result-1788260853909","change":{"change":"evidence","kind":"deployment_result","reference":"https://github.com/beyond10x/substrate/releases/tag/0.5.0","source":"Substrate 0.5.0 release run 33498193209"},"correlation":"protocol-artifact-evidence","event_id":"story:remote-sdk-transport@5#0~c72cd0d887608311","executor":null,"recorded_at":"2026-09-01T11:07:33Z"}}
{"entity":"story","version":1,"id":"rust-sdk-publication","revision":8,"type":"aep.evidence.record/v1","from_state":"archived","to_state":"archived","changed":{},"args":{"command":"record-evidence","kind":"deployment_result","reference":"https://github.com/beyond10x/substrate/releases/tag/0.5.0","source":"Substrate 0.5.0 release run 33498193209","target":"01MEM0000000000000148"},"payload":{"actor":"human:timo","at":1788260854000,"causation":"cmd-evidence-story-rust-sdk-publication-deployment_result-1788260854176","change":{"change":"evidence","kind":"deployment_result","reference":"https://github.com/beyond10x/substrate/releases/tag/0.5.0","source":"Substrate 0.5.0 release run 33498193209"},"correlation":"protocol-artifact-evidence","event_id":"story:rust-sdk-publication@8#0~464d3e026c5a39bb","executor":null,"recorded_at":"2026-09-01T11:07:34Z"}}
{"entity":"story","version":1,"id":"sdk-promoted-contract-parity","revision":6,"type":"aep.evidence.record/v1","from_state":"implemented","to_state":"implemented","changed":{},"args":{"command":"record-evidence","kind":"deployment_result","reference":"https://github.com/beyond10x/substrate/releases/tag/0.5.0","source":"Substrate 0.5.0 release run 33498193209","target":"01MEM0000000000000151"},"payload":{"actor":"human:timo","at":1788260854000,"causation":"cmd-evidence-story-sdk-promoted-contract-parity-deployment_result-1788260854447","change":{"change":"evidence","kind":"deployment_result","reference":"https://github.com/beyond10x/substrate/releases/tag/0.5.0","source":"Substrate 0.5.0 release run 33498193209"},"correlation":"protocol-artifact-evidence","event_id":"story:sdk-promoted-contract-parity@6#0~ad8f470b3372e9f5","executor":null,"recorded_at":"2026-09-01T11:07:34Z"}}
{"entity":"story","version":1,"id":"verified-delivery-build-latency","revision":3,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"scripts/gate.sh","source":"bash scripts/gate.sh (local)","target":"01MEM0000000000000172"},"payload":{"actor":"human:timo","at":1788261030000,"causation":"cmd-evidence-story-verified-delivery-build-latency-test_result-1788261030327","change":{"change":"evidence","kind":"test_result","reference":"scripts/gate.sh","source":"bash scripts/gate.sh (local)"},"correlation":"protocol-artifact-evidence","event_id":"story:verified-delivery-build-latency@3#0~bb31b02049683930","executor":null,"recorded_at":"2026-09-01T11:10:30Z"}}
{"entity":"story","version":1,"id":"verified-delivery-build-latency","revision":4,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"body":"# Compile and validate releases without duplicate build work\n\n## Observed baseline\n\nRelease run `33498193209` took 12m15s in its publication job. Its daemon image step took 5m26s and its MCP image step took another 4m22s. The two Dockerfiles used different Cargo target-cache identities and therefore compiled the shared Rust graph twice. Main gate run `33497345920` took 9m27s: 68s restored a 4.0 GiB broad Cargo cache, 59s saved it, the release test build took 3m30s, and sequential bundle plus JSON checks took about 1m45s.\n\n## Required outcome\n\n- One pinned container builder stage compiles the daemon and MCP binaries together; named minimal runtime targets keep the daemon and stdio-only MCP image boundaries distinct.\n- The release still runs the clean-room daemon vectors against the extracted shipped daemon binary and both MCP stdio journeys against the extracted binary and final container image.\n- Current bundle fixed-point checks and closed JSON classification execute concurrently with bounded worker counts and deterministic reporting.\n- CI caches dependency artifacts rather than uploading the complete workspace target tree, while keeping every gate command and refusal.\n- Repository tests assert the shared build boundary, minimal MCP image, digest signing, anonymous readback, and write-once behavior.\n\n## Evidence to close\n\nA green full local gate, a green pull-request Full gate with step timings, and a green protected-main Full gate. The first run establishes correctness; later runs establish cache effectiveness.\n\n## Additional profile reuse\n\nClippy now runs with `--release`, matching the immediately preceding release-profile test build. The repository has no `cfg(debug_assertions)` implementation branch, so this preserves the linted source and target set while avoiding a second dependency-profile graph. The first local release-profile Clippy run took 16.8s; the previous hosted development-profile step took 49.4s.\n"},"args":{"changes":{"body":"# Compile and validate releases without duplicate build work\n\n## Observed baseline\n\nRelease run `33498193209` took 12m15s in its publication job. Its daemon image step took 5m26s and its MCP image step took another 4m22s. The two Dockerfiles used different Cargo target-cache identities and therefore compiled the shared Rust graph twice. Main gate run `33497345920` took 9m27s: 68s restored a 4.0 GiB broad Cargo cache, 59s saved it, the release test build took 3m30s, and sequential bundle plus JSON checks took about 1m45s.\n\n## Required outcome\n\n- One pinned container builder stage compiles the daemon and MCP binaries together; named minimal runtime targets keep the daemon and stdio-only MCP image boundaries distinct.\n- The release still runs the clean-room daemon vectors against the extracted shipped daemon binary and both MCP stdio journeys against the extracted binary and final container image.\n- Current bundle fixed-point checks and closed JSON classification execute concurrently with bounded worker counts and deterministic reporting.\n- CI caches dependency artifacts rather than uploading the complete workspace target tree, while keeping every gate command and refusal.\n- Repository tests assert the shared build boundary, minimal MCP image, digest signing, anonymous readback, and write-once behavior.\n\n## Evidence to close\n\nA green full local gate, a green pull-request Full gate with step timings, and a green protected-main Full gate. The first run establishes correctness; later runs establish cache effectiveness.\n\n## Additional profile reuse\n\nClippy now runs with `--release`, matching the immediately preceding release-profile test build. The repository has no `cfg(debug_assertions)` implementation branch, so this preserves the linted source and target set while avoiding a second dependency-profile graph. The first local release-profile Clippy run took 16.8s; the previous hosted development-profile step took 49.4s.\n"},"command":"update-entity","target":"01MEM0000000000000172"},"payload":{"actor":"human:timo","at":1788262217220,"causation":"cmd-protocol-artifact-body-append-story-verified-delivery-build-latency-1788262217220","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-append","event_id":"story:verified-delivery-build-latency@4#0~7ba6bc484e015832","executor":null,"recorded_at":"2026-09-01T11:30:17Z"}}
{"entity":"story","version":1,"id":"verified-delivery-build-latency","revision":5,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"body":"# Compile and validate releases without duplicate build work\n\n## Observed baseline\n\nRelease run `33498193209` took 12m15s in its publication job. Its daemon image step took 5m26s and its MCP image step took another 4m22s. The two Dockerfiles used different Cargo target-cache identities and therefore compiled the shared Rust graph twice. Main gate run `33497345920` took 9m27s: 68s restored a 4.0 GiB broad Cargo cache, 59s saved it, the release test build took 3m30s, and sequential bundle plus JSON checks took about 1m45s.\n\n## Required outcome\n\n- One pinned container builder stage compiles the daemon and MCP binaries together; named minimal runtime targets keep the daemon and stdio-only MCP image boundaries distinct.\n- The release still runs the clean-room daemon vectors against the extracted shipped daemon binary and both MCP stdio journeys against the extracted binary and final container image.\n- Current bundle fixed-point checks and closed JSON classification execute concurrently with bounded worker counts and deterministic reporting.\n- CI caches dependency artifacts rather than uploading the complete workspace target tree, while keeping every gate command and refusal.\n- Repository tests assert the shared build boundary, minimal MCP image, digest signing, anonymous readback, and write-once behavior.\n\n## Evidence to close\n\nA green full local gate, a green pull-request Full gate with step timings, and a green protected-main Full gate. The first run establishes correctness; later runs establish cache effectiveness.\n\n## Additional profile reuse\n\nClippy now runs with `--release`, matching the immediately preceding release-profile test build. The repository has no `cfg(debug_assertions)` implementation branch, so this preserves the linted source and target set while avoiding a second dependency-profile graph. The first local release-profile Clippy run took 16.8s; the previous hosted development-profile step took 49.4s.\n\n## Hosted result\n\nThe protected-main seed run `33502295385` was green in 12m02s and saved a 578,678,197-byte cache. Protected-main run `33503894885` restored that cache in about four seconds, reused `cargo-about`, and completed green in 7m09s. Against baseline main run `33497345920` at 9m27s, the steady-state wall time fell by 2m18s (24%). The remaining dominant step is the exact release-profile workspace test build at 4m07s; no test, refusal, contract, security, licence, or shipped-binary check was removed.\n"},"args":{"changes":{"body":"# Compile and validate releases without duplicate build work\n\n## Observed baseline\n\nRelease run `33498193209` took 12m15s in its publication job. Its daemon image step took 5m26s and its MCP image step took another 4m22s. The two Dockerfiles used different Cargo target-cache identities and therefore compiled the shared Rust graph twice. Main gate run `33497345920` took 9m27s: 68s restored a 4.0 GiB broad Cargo cache, 59s saved it, the release test build took 3m30s, and sequential bundle plus JSON checks took about 1m45s.\n\n## Required outcome\n\n- One pinned container builder stage compiles the daemon and MCP binaries together; named minimal runtime targets keep the daemon and stdio-only MCP image boundaries distinct.\n- The release still runs the clean-room daemon vectors against the extracted shipped daemon binary and both MCP stdio journeys against the extracted binary and final container image.\n- Current bundle fixed-point checks and closed JSON classification execute concurrently with bounded worker counts and deterministic reporting.\n- CI caches dependency artifacts rather than uploading the complete workspace target tree, while keeping every gate command and refusal.\n- Repository tests assert the shared build boundary, minimal MCP image, digest signing, anonymous readback, and write-once behavior.\n\n## Evidence to close\n\nA green full local gate, a green pull-request Full gate with step timings, and a green protected-main Full gate. The first run establishes correctness; later runs establish cache effectiveness.\n\n## Additional profile reuse\n\nClippy now runs with `--release`, matching the immediately preceding release-profile test build. The repository has no `cfg(debug_assertions)` implementation branch, so this preserves the linted source and target set while avoiding a second dependency-profile graph. The first local release-profile Clippy run took 16.8s; the previous hosted development-profile step took 49.4s.\n\n## Hosted result\n\nThe protected-main seed run `33502295385` was green in 12m02s and saved a 578,678,197-byte cache. Protected-main run `33503894885` restored that cache in about four seconds, reused `cargo-about`, and completed green in 7m09s. Against baseline main run `33497345920` at 9m27s, the steady-state wall time fell by 2m18s (24%). The remaining dominant step is the exact release-profile workspace test build at 4m07s; no test, refusal, contract, security, licence, or shipped-binary check was removed.\n"},"command":"update-entity","target":"01MEM0000000000000172"},"payload":{"actor":"human:timo","at":1788263495764,"causation":"cmd-protocol-artifact-body-section-story-verified-delivery-build-latency-1788263495764","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:verified-delivery-build-latency@5#0~2cbfebd7275d0cad","executor":null,"recorded_at":"2026-09-01T11:51:35Z"}}
{"entity":"story","version":1,"id":"verified-delivery-build-latency","revision":5,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"a0521ef25f79a377f746c7a27ce78f0abf6dfe46","source":"local bash scripts/gate.sh at a0521ef","target":"01MEM0000000000000172"},"payload":{"actor":"human:timo","at":1788263495000,"causation":"cmd-evidence-story-verified-delivery-build-latency-test_result-1788263495898","change":{"change":"evidence","kind":"test_result","reference":"a0521ef25f79a377f746c7a27ce78f0abf6dfe46","source":"local bash scripts/gate.sh at a0521ef"},"correlation":"protocol-artifact-evidence","event_id":"story:verified-delivery-build-latency@5#0~7a0c79ffbb7f4d18","executor":null,"recorded_at":"2026-09-01T11:51:35Z"}}
{"entity":"story","version":1,"id":"verified-delivery-build-latency","revision":5,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"https://github.com/beyond10x/substrate/actions/runs/33503894885","source":"protected-main Full gate success","target":"01MEM0000000000000172"},"payload":{"actor":"human:timo","at":1788263495000,"causation":"cmd-evidence-story-verified-delivery-build-latency-test_result-1788263496029","change":{"change":"evidence","kind":"test_result","reference":"https://github.com/beyond10x/substrate/actions/runs/33503894885","source":"protected-main Full gate success"},"correlation":"protocol-artifact-evidence","event_id":"story:verified-delivery-build-latency@5#0~1e55fec1c53ba573","executor":null,"recorded_at":"2026-09-01T11:51:35Z"}}
{"entity":"story","version":1,"id":"verified-delivery-build-latency","revision":5,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"metric_observation","reference":"https://github.com/beyond10x/substrate/actions/runs/33503894885","source":"warm protected-main gate: 7m09s versus 9m27s baseline","target":"01MEM0000000000000172"},"payload":{"actor":"human:timo","at":1788263496000,"causation":"cmd-evidence-story-verified-delivery-build-latency-metric_observation-1788263496160","change":{"change":"evidence","kind":"metric_observation","reference":"https://github.com/beyond10x/substrate/actions/runs/33503894885","source":"warm protected-main gate: 7m09s versus 9m27s baseline"},"correlation":"protocol-artifact-evidence","event_id":"story:verified-delivery-build-latency@5#0~a6b30d07f94a44ba","executor":null,"recorded_at":"2026-09-01T11:51:36Z"}}
{"entity":"story","version":1,"id":"verified-delivery-build-latency","revision":6,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":4,\"metric_observation\":1}}","target":"01MEM0000000000000172","to":"implemented"},"payload":{"actor":"human:timo","at":1788263496377,"causation":"cmd-move-story-verified-delivery-build-latency-1788263496377","change":{"change":"moved","decided_on":{"recorded":{"metric_observation":1,"test_result":4}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:verified-delivery-build-latency@6#0~1dc6fd9b682dc356","executor":null,"recorded_at":"2026-09-01T11:51:36Z"}}
{"entity":"epic","version":1,"id":"review-2026-09-03-findings","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Epic: Close the 2026-09-03 in-depth review findings\n\n## Outcome\n\nEvery finding of the 2026-09-03 whole-repository review (`~/.cache/substrate-review/2026-09-03-substrate-in-depth-review.md`) is either closed by a change that a\ntest or probe proves, or recorded as an accepted decision. Two correctness defects, three\nauthenticated-client capacity gaps, three confinement hardening gaps, three design constraints\nand three minor costs; the review's own register carries the file and line for each.\n\n## Why Now\n\nThe review read every file under `crates/` at `cc5671c` and ran both gates green\n(`scripts/gate.sh`: 491 tests; `scripts/delegated-lane.sh`: 96 tests), so the findings are\ndefects the suite does not see. Finding 1 was reproduced at runtime: the same two line-patch edits\nproduce two different files depending on their order in the request. Findings 3 to 5 are reachable\nby any authenticated local uid. The 0.5.1 release on `origin/main` changes none of the cited code\nexcept `crates/substrate-host/src/fs.rs` executable-mode preservation.\n\n## Scope\n\nOne story per finding, in the register's order. Correctness first, then capacity, then hardening,\nthen the three decisions, then the minor costs:\n\n1. `story:line-patch-edits-apply-in-line-order`\n2. `story:directory-listing-survives-special-entries`\n3. `story:metrics-streams-are-bounded`\n4. `story:upgraded-connections-keep-their-permit`\n5. `story:lease-cleanup-reads-exec-state-only`\n6. `story:confined-processes-cannot-nest-user-namespaces`\n7. `story:seccomp-denies-af-vsock`\n8. `story:exec-oom-kills-the-whole-tree`\n9. `story:session-attachment-lifetime-is-an-accepted-decision`\n10. `story:hosted-admission-reuses-identity-authority`\n11. `story:daemon-image-serves-exec-or-says-it-cannot`\n12. `story:backend-recheck-hashes-only-on-change`\n13. `story:events-table-has-one-index`\n14. `story:unattached-claimed-session-is-contained`\n\n## Out of Scope\n\nAny change to a frozen contract bundle. Where a story needs a wire change (story 2 may), it cuts a\nsuccessor bundle under invariant 6 and says so in its own body. Product policy, fleet scheduling\nand the remote-serving track stay where they are.\n\n## Risks\n\n- Stories 6 and 7 tighten confinement; a host whose bubblewrap or kernel refuses the new flag must\n  answer a named refusal, never a quieter sandbox (invariant 3).\n- Story 4 rests on hyper 1.11.1 upgrade semantics inferred from the library, not observed; the\n  story's first step is a test that observes it.\n- Stories 9 to 11 may end as recorded decisions rather than code; that is a valid close.\n","owner":"substrate","summary":"Fourteen review findings: two correctness defects, three authenticated-client capacity gaps, three confinement hardening gaps, three design constraints to record, three minor costs.","tags":["hardening","review"],"title":"Close the 2026-09-03 in-depth review findings"},"args":{"command":"create-entity","data":{"body":"# Epic: Close the 2026-09-03 in-depth review findings\n\n## Outcome\n\nEvery finding of the 2026-09-03 whole-repository review (`~/.cache/substrate-review/2026-09-03-substrate-in-depth-review.md`) is either closed by a change that a\ntest or probe proves, or recorded as an accepted decision. Two correctness defects, three\nauthenticated-client capacity gaps, three confinement hardening gaps, three design constraints\nand three minor costs; the review's own register carries the file and line for each.\n\n## Why Now\n\nThe review read every file under `crates/` at `cc5671c` and ran both gates green\n(`scripts/gate.sh`: 491 tests; `scripts/delegated-lane.sh`: 96 tests), so the findings are\ndefects the suite does not see. Finding 1 was reproduced at runtime: the same two line-patch edits\nproduce two different files depending on their order in the request. Findings 3 to 5 are reachable\nby any authenticated local uid. The 0.5.1 release on `origin/main` changes none of the cited code\nexcept `crates/substrate-host/src/fs.rs` executable-mode preservation.\n\n## Scope\n\nOne story per finding, in the register's order. Correctness first, then capacity, then hardening,\nthen the three decisions, then the minor costs:\n\n1. `story:line-patch-edits-apply-in-line-order`\n2. `story:directory-listing-survives-special-entries`\n3. `story:metrics-streams-are-bounded`\n4. `story:upgraded-connections-keep-their-permit`\n5. `story:lease-cleanup-reads-exec-state-only`\n6. `story:confined-processes-cannot-nest-user-namespaces`\n7. `story:seccomp-denies-af-vsock`\n8. `story:exec-oom-kills-the-whole-tree`\n9. `story:session-attachment-lifetime-is-an-accepted-decision`\n10. `story:hosted-admission-reuses-identity-authority`\n11. `story:daemon-image-serves-exec-or-says-it-cannot`\n12. `story:backend-recheck-hashes-only-on-change`\n13. `story:events-table-has-one-index`\n14. `story:unattached-claimed-session-is-contained`\n\n## Out of Scope\n\nAny change to a frozen contract bundle. Where a story needs a wire change (story 2 may), it cuts a\nsuccessor bundle under invariant 6 and says so in its own body. Product policy, fleet scheduling\nand the remote-serving track stay where they are.\n\n## Risks\n\n- Stories 6 and 7 tighten confinement; a host whose bubblewrap or kernel refuses the new flag must\n  answer a named refusal, never a quieter sandbox (invariant 3).\n- Story 4 rests on hyper 1.11.1 upgrade semantics inferred from the library, not observed; the\n  story's first step is a test that observes it.\n- Stories 9 to 11 may end as recorded decisions rather than code; that is a valid close.\n","owner":"substrate","status":"draft","summary":"Fourteen review findings: two correctness defects, three authenticated-client capacity gaps, three confinement hardening gaps, three design constraints to record, three minor costs.","tags":["hardening","review"],"title":"Close the 2026-09-03 in-depth review findings"},"entity_type":"aep.epic/v1","locator":"ep://planning/store/epic/review-2026-09-03-findings"},"payload":{"actor":"human:timo","at":1788484037560,"causation":"cmd-new-epic-review-2026-09-03-findings","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"epic:review-2026-09-03-findings@1#0~57fd52be87e50144","executor":null,"recorded_at":"2026-09-04T01:07:17Z"}}
{"entity":"story","version":1,"id":"line-patch-edits-apply-in-line-order","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Story: Line patch edits apply in line order\n\n## Context\n\n`apply_line_patch` validates edit ranges on a sorted copy but applies the edits by iterating the\nrequest in reverse **input** order (`crates/substrate-host/src/fs.rs:1226`). An edit list that is\nnot ascending shifts later edits onto the wrong lines. Reproduced outside the crate with the\nfunction copied verbatim: on a six-line file, `[replace line 5, insert before line 2]` yields\n`L1 A B L2 X L4 L5 L6` while the ascending order of the same edits yields `L1 A B L2 L3 L4 X L6`.\nThe published schema `contracts/substrate-wire/0.15.0/schemas/inputs/workspace-file-patch-v2.json`\nstates no ordering rule for `edits`, and no released vector exercises two edits.\n\n## Acceptance\n\n`POST /v2/workspaces/{id}/file-patches/{path}` with the same set of non-overlapping edits produces\nbyte-identical files for every permutation of the list, proven by a `fs::tests` case that submits\ntwo permutations of a two-edit patch.\n\n## Notes\n\nSmallest fix: sort by `(start_line, kind)` descending before the apply loop. Alternative that\nneeds a successor bundle: refuse a non-ascending list with a new `workspace.patch-order` code.\nEither way the released bytes of `0.15.0` do not change.\n","summary":"apply_line_patch iterates edits in reverse input order; unsorted edits land on the wrong lines (fs.rs:1226), reproduced at runtime.","tags":["review"],"title":"A line patch applies the same result whatever order its edits arrive in"},"args":{"command":"create-entity","data":{"body":"# Story: Line patch edits apply in line order\n\n## Context\n\n`apply_line_patch` validates edit ranges on a sorted copy but applies the edits by iterating the\nrequest in reverse **input** order (`crates/substrate-host/src/fs.rs:1226`). An edit list that is\nnot ascending shifts later edits onto the wrong lines. Reproduced outside the crate with the\nfunction copied verbatim: on a six-line file, `[replace line 5, insert before line 2]` yields\n`L1 A B L2 X L4 L5 L6` while the ascending order of the same edits yields `L1 A B L2 L3 L4 X L6`.\nThe published schema `contracts/substrate-wire/0.15.0/schemas/inputs/workspace-file-patch-v2.json`\nstates no ordering rule for `edits`, and no released vector exercises two edits.\n\n## Acceptance\n\n`POST /v2/workspaces/{id}/file-patches/{path}` with the same set of non-overlapping edits produces\nbyte-identical files for every permutation of the list, proven by a `fs::tests` case that submits\ntwo permutations of a two-edit patch.\n\n## Notes\n\nSmallest fix: sort by `(start_line, kind)` descending before the apply loop. Alternative that\nneeds a successor bundle: refuse a non-ascending list with a new `workspace.patch-order` code.\nEither way the released bytes of `0.15.0` do not change.\n","status":"draft","summary":"apply_line_patch iterates edits in reverse input order; unsorted edits land on the wrong lines (fs.rs:1226), reproduced at runtime.","tags":["review"],"title":"A line patch applies the same result whatever order its edits arrive in"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/line-patch-edits-apply-in-line-order"},"payload":{"actor":"human:timo","at":1788484037805,"causation":"cmd-new-story-line-patch-edits-apply-in-line-order","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:line-patch-edits-apply-in-line-order@1#0~fe86fa6027d28f58","executor":null,"recorded_at":"2026-09-04T01:07:17Z"}}
{"entity":"story","version":1,"id":"line-patch-edits-apply-in-line-order","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:review-2026-09-03-findings"}]},"args":{"command":"create-relation","kind":"decomposes","source":"01MEM0000000000000377","target":"01MEM0000000000000025"},"payload":{"actor":"human:timo","at":1788484037814,"causation":"cmd-story-line-patch-edits-apply-in-line-order-decomposes-epic-review-2026-09-03-findings","change":{"change":"related","relation":"decomposes","target":"epic:review-2026-09-03-findings"},"correlation":"protocol-artifact-relate","event_id":"story:line-patch-edits-apply-in-line-order@1#0~98e2a61ea01d2dcd","executor":null,"recorded_at":"2026-09-04T01:07:17Z"}}
{"entity":"story","version":1,"id":"directory-listing-survives-special-entries","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Story: Directory listing survives special entries\n\n## Context\n\n`list_directory` and `walk_tree` return `workspace.path-escape` for any entry that is not a\nregular file, directory or symlink, and for any name that is not UTF-8\n(`crates/substrate-host/src/fs.rs:768`, `:773`, `:803`, `:813`). A confined process can create a\nFIFO (`mkfifo`) or a byte-named file in `/workspace`; afterwards every directory page of that\ndirectory and the whole `GET /v2/workspaces/{id}/tree` refuse. The refusal names an escape that did\nnot happen, and the client cannot list its own workspace. Code reading only; not reproduced at\nruntime.\n\n## Acceptance\n\nA directory holding a FIFO, a socket and a non-UTF-8 name lists its regular entries with `200` on\nboth the v1 directory page and the v2 tree, and a test creates those three entries and reads both.\n\n## Notes\n\nThe wire `DirectoryEntryKind` has three members (`crates/substrate-wire/src/lib.rs:1045`). Naming\na fourth is a contract change and needs a successor bundle under invariant 6; skipping such\nentries with the existing `truncated` flag on the tree, and omitting them from the page, needs\nnone. The story body of whichever path is taken records the choice before code (invariant 8).\n","summary":"list_directory and walk_tree answer workspace.path-escape for special entries a confined process can create (fs.rs:768-813).","tags":["review"],"title":"A FIFO, socket, device or non-UTF-8 name never makes a directory page or tree refuse"},"args":{"command":"create-entity","data":{"body":"# Story: Directory listing survives special entries\n\n## Context\n\n`list_directory` and `walk_tree` return `workspace.path-escape` for any entry that is not a\nregular file, directory or symlink, and for any name that is not UTF-8\n(`crates/substrate-host/src/fs.rs:768`, `:773`, `:803`, `:813`). A confined process can create a\nFIFO (`mkfifo`) or a byte-named file in `/workspace`; afterwards every directory page of that\ndirectory and the whole `GET /v2/workspaces/{id}/tree` refuse. The refusal names an escape that did\nnot happen, and the client cannot list its own workspace. Code reading only; not reproduced at\nruntime.\n\n## Acceptance\n\nA directory holding a FIFO, a socket and a non-UTF-8 name lists its regular entries with `200` on\nboth the v1 directory page and the v2 tree, and a test creates those three entries and reads both.\n\n## Notes\n\nThe wire `DirectoryEntryKind` has three members (`crates/substrate-wire/src/lib.rs:1045`). Naming\na fourth is a contract change and needs a successor bundle under invariant 6; skipping such\nentries with the existing `truncated` flag on the tree, and omitting them from the page, needs\nnone. The story body of whichever path is taken records the choice before code (invariant 8).\n","status":"draft","summary":"list_directory and walk_tree answer workspace.path-escape for special entries a confined process can create (fs.rs:768-813).","tags":["review"],"title":"A FIFO, socket, device or non-UTF-8 name never makes a directory page or tree refuse"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/directory-listing-survives-special-entries"},"payload":{"actor":"human:timo","at":1788484038022,"causation":"cmd-new-story-directory-listing-survives-special-entries","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:directory-listing-survives-special-entries@1#0~558c0a2d24e759d7","executor":null,"recorded_at":"2026-09-04T01:07:18Z"}}
{"entity":"story","version":1,"id":"directory-listing-survives-special-entries","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:review-2026-09-03-findings"}]},"args":{"command":"create-relation","kind":"decomposes","source":"01MEM0000000000000382","target":"01MEM0000000000000025"},"payload":{"actor":"human:timo","at":1788484038031,"causation":"cmd-story-directory-listing-survives-special-entries-decomposes-epic-review-2026-09-03-findings","change":{"change":"related","relation":"decomposes","target":"epic:review-2026-09-03-findings"},"correlation":"protocol-artifact-relate","event_id":"story:directory-listing-survives-special-entries@1#0~b6c68a3b4abed445","executor":null,"recorded_at":"2026-09-04T01:07:18Z"}}
{"entity":"story","version":1,"id":"metrics-streams-are-bounded","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Story: Metrics streams are bounded\n\n## Context\n\n`GET /v1/metrics/stream` upgrades to a WebSocket with no permit, no lifetime and the library's\ndefault frame and message bounds (`crates/substrate-daemon/src/app/metrics.rs:75-90`). Each open\nstream runs `load_exec_usage` once a second, which is an `observe_exec` plus a `put_exec` SQLite\nwrite (`metrics.rs:98-` and `:215-`), until the exec ends, up to the 24 h exec timeout. Event\nstreams are capped at 64 global and 4 per subject (`app/events.rs:47-48`) and pipe attachments at\n32 (`app/sessions.rs:63`); metrics streams at nothing. One authenticated uid can open as many as\nthe kernel allows.\n\n## Acceptance\n\nOpening one more metrics stream than a published per-subject cap answers a `429` with a named\n`exhausted` refusal, a stream ends at its lifetime, an oversized client message closes it, and a\ntest proves all three.\n\n## Notes\n\nReuse the `EventStreamLimits` shape and `EventStreamPolicy` bounds; the metrics stream is the one\nupgrade that has none of them.\n","summary":"GET /v1/metrics/stream upgrades with no cap, no lifetime and default frame bounds; one SQLite write per stream per second (metrics.rs:75-98).","tags":["review"],"title":"Metrics WebSocket streams carry a permit, a lifetime and frame bounds"},"args":{"command":"create-entity","data":{"body":"# Story: Metrics streams are bounded\n\n## Context\n\n`GET /v1/metrics/stream` upgrades to a WebSocket with no permit, no lifetime and the library's\ndefault frame and message bounds (`crates/substrate-daemon/src/app/metrics.rs:75-90`). Each open\nstream runs `load_exec_usage` once a second, which is an `observe_exec` plus a `put_exec` SQLite\nwrite (`metrics.rs:98-` and `:215-`), until the exec ends, up to the 24 h exec timeout. Event\nstreams are capped at 64 global and 4 per subject (`app/events.rs:47-48`) and pipe attachments at\n32 (`app/sessions.rs:63`); metrics streams at nothing. One authenticated uid can open as many as\nthe kernel allows.\n\n## Acceptance\n\nOpening one more metrics stream than a published per-subject cap answers a `429` with a named\n`exhausted` refusal, a stream ends at its lifetime, an oversized client message closes it, and a\ntest proves all three.\n\n## Notes\n\nReuse the `EventStreamLimits` shape and `EventStreamPolicy` bounds; the metrics stream is the one\nupgrade that has none of them.\n","status":"draft","summary":"GET /v1/metrics/stream upgrades with no cap, no lifetime and default frame bounds; one SQLite write per stream per second (metrics.rs:75-98).","tags":["review"],"title":"Metrics WebSocket streams carry a permit, a lifetime and frame bounds"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/metrics-streams-are-bounded"},"payload":{"actor":"human:timo","at":1788484038241,"causation":"cmd-new-story-metrics-streams-are-bounded","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:metrics-streams-are-bounded@1#0~ac5ca52d3264bdf0","executor":null,"recorded_at":"2026-09-04T01:07:18Z"}}
{"entity":"story","version":1,"id":"metrics-streams-are-bounded","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:review-2026-09-03-findings"}]},"args":{"command":"create-relation","kind":"decomposes","source":"01MEM0000000000000387","target":"01MEM0000000000000025"},"payload":{"actor":"human:timo","at":1788484038249,"causation":"cmd-story-metrics-streams-are-bounded-decomposes-epic-review-2026-09-03-findings","change":{"change":"related","relation":"decomposes","target":"epic:review-2026-09-03-findings"},"correlation":"protocol-artifact-relate","event_id":"story:metrics-streams-are-bounded@1#0~7862f73157e3f578","executor":null,"recorded_at":"2026-09-04T01:07:18Z"}}
{"entity":"story","version":1,"id":"upgraded-connections-keep-their-permit","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Story: Upgraded connections keep their permit\n\n## Context\n\nEvery listener wraps the hyper connection future in `enforce_connection_lifetime`, which owns the\nper-uid and global `ConnectionPermit` (`crates/substrate-daemon/src/runtime.rs:596`, `:919`,\n`:1084`; limits at `:53-56`). hyper 1.11.1 (`Cargo.lock:1141`) resolves an upgradeable connection\nfuture when it hands the socket to the upgrade, so the permit is released the moment a WebSocket\nupgrade succeeds and the upgraded socket runs in a task the transport budget no longer counts.\nInferred from the library's semantics and consistent with the separate 1 h attachment lifetime the\nsession code needs (`app/sessions.rs:70`); not observed at runtime.\n\n## Acceptance\n\nA test holds the per-uid connection budget with upgraded WebSockets and shows the next plain HTTP\nconnection from that uid is refused at accept, or the story records the observation that the\npermit already survives the upgrade and closes with no code change.\n\n## Notes\n\nFirst step is the observation, not the fix. If the permit is released, either move it into the\nupgrade task or give every upgrade its own bounded permit as event streams and pipe attachments\nalready have.\n","summary":"The connection permit lives in enforce_connection_lifetime, which hyper resolves at upgrade; upgraded sockets are uncounted (runtime.rs:596,919,1084).","tags":["review"],"title":"A WebSocket upgrade stays inside the transport connection budget"},"args":{"command":"create-entity","data":{"body":"# Story: Upgraded connections keep their permit\n\n## Context\n\nEvery listener wraps the hyper connection future in `enforce_connection_lifetime`, which owns the\nper-uid and global `ConnectionPermit` (`crates/substrate-daemon/src/runtime.rs:596`, `:919`,\n`:1084`; limits at `:53-56`). hyper 1.11.1 (`Cargo.lock:1141`) resolves an upgradeable connection\nfuture when it hands the socket to the upgrade, so the permit is released the moment a WebSocket\nupgrade succeeds and the upgraded socket runs in a task the transport budget no longer counts.\nInferred from the library's semantics and consistent with the separate 1 h attachment lifetime the\nsession code needs (`app/sessions.rs:70`); not observed at runtime.\n\n## Acceptance\n\nA test holds the per-uid connection budget with upgraded WebSockets and shows the next plain HTTP\nconnection from that uid is refused at accept, or the story records the observation that the\npermit already survives the upgrade and closes with no code change.\n\n## Notes\n\nFirst step is the observation, not the fix. If the permit is released, either move it into the\nupgrade task or give every upgrade its own bounded permit as event streams and pipe attachments\nalready have.\n","status":"draft","summary":"The connection permit lives in enforce_connection_lifetime, which hyper resolves at upgrade; upgraded sockets are uncounted (runtime.rs:596,919,1084).","tags":["review"],"title":"A WebSocket upgrade stays inside the transport connection budget"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/upgraded-connections-keep-their-permit"},"payload":{"actor":"human:timo","at":1788484038461,"causation":"cmd-new-story-upgraded-connections-keep-their-permit","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:upgraded-connections-keep-their-permit@1#0~eaab4fe641afa195","executor":null,"recorded_at":"2026-09-04T01:07:18Z"}}
{"entity":"story","version":1,"id":"upgraded-connections-keep-their-permit","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:review-2026-09-03-findings"}]},"args":{"command":"create-relation","kind":"decomposes","source":"01MEM0000000000000392","target":"01MEM0000000000000025"},"payload":{"actor":"human:timo","at":1788484038470,"causation":"cmd-story-upgraded-connections-keep-their-permit-decomposes-epic-review-2026-09-03-findings","change":{"change":"related","relation":"decomposes","target":"epic:review-2026-09-03-findings"},"correlation":"protocol-artifact-relate","event_id":"story:upgraded-connections-keep-their-permit@1#0~8913a80bec50bf7e","executor":null,"recorded_at":"2026-09-04T01:07:18Z"}}
{"entity":"story","version":1,"id":"lease-cleanup-reads-exec-state-only","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Story: Lease cleanup reads exec state only\n\n## Context\n\nWorkspace lease cleanup calls `execs_for_workspace`, which loads every exec row of the workspace\nthrough `load_exec`, including the `stdout` and `stderr` blobs\n(`crates/substrate-store/src/execs.rs:545`). The caller reads only `resource.state` and\n`resource.id` (`crates/substrate-daemon/src/app/service.rs:688-700`). The bounds allow 2048 execs\nper subject (`crates/substrate-wire/src/lib.rs:28`) with 1 MiB per stream (`:22`), so one expiring\nworkspace can pull 4 GiB into daemon memory in one sweep. Code reading only.\n\n## Acceptance\n\nA workspace lease expiry over the maximum exec count completes with the daemon's resident memory\ngrowth bounded by the exec metadata alone, proven by a store query that returns `(id, state)` and\na test asserting no output column is read on that path.\n\n## Notes\n\n`workspace_has_nonterminal_execs` in `store/workspaces.rs` already parses only `resource_json`;\nthe same shape serves here.\n","summary":"execs_for_workspace loads stdout/stderr blobs the caller never reads; worst case 4 GiB per sweep (store execs.rs:545, service.rs:688).","tags":["review"],"title":"Workspace lease cleanup reads exec ids and states without output blobs"},"args":{"command":"create-entity","data":{"body":"# Story: Lease cleanup reads exec state only\n\n## Context\n\nWorkspace lease cleanup calls `execs_for_workspace`, which loads every exec row of the workspace\nthrough `load_exec`, including the `stdout` and `stderr` blobs\n(`crates/substrate-store/src/execs.rs:545`). The caller reads only `resource.state` and\n`resource.id` (`crates/substrate-daemon/src/app/service.rs:688-700`). The bounds allow 2048 execs\nper subject (`crates/substrate-wire/src/lib.rs:28`) with 1 MiB per stream (`:22`), so one expiring\nworkspace can pull 4 GiB into daemon memory in one sweep. Code reading only.\n\n## Acceptance\n\nA workspace lease expiry over the maximum exec count completes with the daemon's resident memory\ngrowth bounded by the exec metadata alone, proven by a store query that returns `(id, state)` and\na test asserting no output column is read on that path.\n\n## Notes\n\n`workspace_has_nonterminal_execs` in `store/workspaces.rs` already parses only `resource_json`;\nthe same shape serves here.\n","status":"draft","summary":"execs_for_workspace loads stdout/stderr blobs the caller never reads; worst case 4 GiB per sweep (store execs.rs:545, service.rs:688).","tags":["review"],"title":"Workspace lease cleanup reads exec ids and states without output blobs"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/lease-cleanup-reads-exec-state-only"},"payload":{"actor":"human:timo","at":1788484038680,"causation":"cmd-new-story-lease-cleanup-reads-exec-state-only","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:lease-cleanup-reads-exec-state-only@1#0~f16b2a391fcc2d33","executor":null,"recorded_at":"2026-09-04T01:07:18Z"}}
{"entity":"story","version":1,"id":"lease-cleanup-reads-exec-state-only","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:review-2026-09-03-findings"}]},"args":{"command":"create-relation","kind":"decomposes","source":"01MEM0000000000000397","target":"01MEM0000000000000025"},"payload":{"actor":"human:timo","at":1788484038689,"causation":"cmd-story-lease-cleanup-reads-exec-state-only-decomposes-epic-review-2026-09-03-findings","change":{"change":"related","relation":"decomposes","target":"epic:review-2026-09-03-findings"},"correlation":"protocol-artifact-relate","event_id":"story:lease-cleanup-reads-exec-state-only@1#0~41f535fec7639333","executor":null,"recorded_at":"2026-09-04T01:07:18Z"}}
{"entity":"story","version":1,"id":"confined-processes-cannot-nest-user-namespaces","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Story: Confined processes cannot nest user namespaces\n\n## Context\n\nThe exec argv passes `--unshare-user` and not `--disable-userns`\n(`crates/substrate-host/src/process.rs:1813-1838`); the three probe argv lists in `probe.rs`,\n`pty.rs` and `egress.rs` match it. The installed bubblewrap 0.11.2 lists `--disable-userns` and\n`--assert-userns-disabled` (`bwrap --help`). A confined process can therefore create nested user\nnamespaces, which is the entry point of most unprivileged kernel privilege escalations. The\ncurrent seccomp profile does not block `unshare` or `clone` with `CLONE_NEWUSER`\n(`crates/substrate-host/src/seccomp.rs:70-99`).\n\n## Acceptance\n\nInside an admitted exec, `unshare -U` fails, the probe passes `--assert-userns-disabled` and\nwithholds the exec facts on a host whose bubblewrap cannot honour it, and a delegated-lane case\nobserves both.\n\n## Notes\n\nInvariant 3: a bubblewrap too old for the flag is a named refusal, never a sandbox without it.\n","summary":"bwrap argv lacks --disable-userns although bwrap 0.11.2 supports it (process.rs:1813-1838).","tags":["review"],"title":"The sandbox passes --disable-userns and the probe asserts it"},"args":{"command":"create-entity","data":{"body":"# Story: Confined processes cannot nest user namespaces\n\n## Context\n\nThe exec argv passes `--unshare-user` and not `--disable-userns`\n(`crates/substrate-host/src/process.rs:1813-1838`); the three probe argv lists in `probe.rs`,\n`pty.rs` and `egress.rs` match it. The installed bubblewrap 0.11.2 lists `--disable-userns` and\n`--assert-userns-disabled` (`bwrap --help`). A confined process can therefore create nested user\nnamespaces, which is the entry point of most unprivileged kernel privilege escalations. The\ncurrent seccomp profile does not block `unshare` or `clone` with `CLONE_NEWUSER`\n(`crates/substrate-host/src/seccomp.rs:70-99`).\n\n## Acceptance\n\nInside an admitted exec, `unshare -U` fails, the probe passes `--assert-userns-disabled` and\nwithholds the exec facts on a host whose bubblewrap cannot honour it, and a delegated-lane case\nobserves both.\n\n## Notes\n\nInvariant 3: a bubblewrap too old for the flag is a named refusal, never a sandbox without it.\n","status":"draft","summary":"bwrap argv lacks --disable-userns although bwrap 0.11.2 supports it (process.rs:1813-1838).","tags":["review"],"title":"The sandbox passes --disable-userns and the probe asserts it"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/confined-processes-cannot-nest-user-namespaces"},"payload":{"actor":"human:timo","at":1788484038916,"causation":"cmd-new-story-confined-processes-cannot-nest-user-namespaces","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:confined-processes-cannot-nest-user-namespaces@1#0~2fe36057026e7639","executor":null,"recorded_at":"2026-09-04T01:07:18Z"}}
{"entity":"story","version":1,"id":"confined-processes-cannot-nest-user-namespaces","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:review-2026-09-03-findings"}]},"args":{"command":"create-relation","kind":"decomposes","source":"01MEM0000000000000402","target":"01MEM0000000000000025"},"payload":{"actor":"human:timo","at":1788484038925,"causation":"cmd-story-confined-processes-cannot-nest-user-namespaces-decomposes-epic-review-2026-09-03-findings","change":{"change":"related","relation":"decomposes","target":"epic:review-2026-09-03-findings"},"correlation":"protocol-artifact-relate","event_id":"story:confined-processes-cannot-nest-user-namespaces@1#0~2087d5f3f6547c8a","executor":null,"recorded_at":"2026-09-04T01:07:18Z"}}
{"entity":"story","version":1,"id":"seccomp-denies-af-vsock","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Story: seccomp denies AF_VSOCK\n\n## Context\n\nThe seccomp profile denies `io_uring_setup`, `socket(AF_UNIX, …)` and\n`socketpair(AF_UNIX, SOCK_DGRAM)` and allows every other socket family\n(`crates/substrate-host/src/seccomp.rs:70-99`). `AF_VSOCK` is not confined by a network namespace,\nso on a virtual-machine host that exposes a vsock transport a confined process can open a socket to\nthe hypervisor side while `--unshare-net` holds. The observed development nodes are EKS virtual\nmachines (`STATUS.md` § Current state). Inferred from the kernel's vsock namespace model; not\ntested on such a host.\n\n## Acceptance\n\n`socket(AF_VSOCK, SOCK_STREAM, 0)` inside an admitted exec fails with `EACCES`, the probe's sentinel\ncase includes it, and `seccomp::tests` carries the same case beside the existing `AF_UNIX` ones.\n\n## Notes\n\nOne more `jump` against `libc::AF_VSOCK` in `filters`. Consider the same for `AF_NETLINK` and\n`AF_PACKET` and record in the story why each is or is not refused.\n","summary":"AF_VSOCK is not netns-confined and the profile allows it (seccomp.rs:70-99); inferred, untested on a vsock host.","tags":["review"],"title":"The seccomp profile refuses AF_VSOCK sockets and a probe proves it"},"args":{"command":"create-entity","data":{"body":"# Story: seccomp denies AF_VSOCK\n\n## Context\n\nThe seccomp profile denies `io_uring_setup`, `socket(AF_UNIX, …)` and\n`socketpair(AF_UNIX, SOCK_DGRAM)` and allows every other socket family\n(`crates/substrate-host/src/seccomp.rs:70-99`). `AF_VSOCK` is not confined by a network namespace,\nso on a virtual-machine host that exposes a vsock transport a confined process can open a socket to\nthe hypervisor side while `--unshare-net` holds. The observed development nodes are EKS virtual\nmachines (`STATUS.md` § Current state). Inferred from the kernel's vsock namespace model; not\ntested on such a host.\n\n## Acceptance\n\n`socket(AF_VSOCK, SOCK_STREAM, 0)` inside an admitted exec fails with `EACCES`, the probe's sentinel\ncase includes it, and `seccomp::tests` carries the same case beside the existing `AF_UNIX` ones.\n\n## Notes\n\nOne more `jump` against `libc::AF_VSOCK` in `filters`. Consider the same for `AF_NETLINK` and\n`AF_PACKET` and record in the story why each is or is not refused.\n","status":"draft","summary":"AF_VSOCK is not netns-confined and the profile allows it (seccomp.rs:70-99); inferred, untested on a vsock host.","tags":["review"],"title":"The seccomp profile refuses AF_VSOCK sockets and a probe proves it"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/seccomp-denies-af-vsock"},"payload":{"actor":"human:timo","at":1788484039150,"causation":"cmd-new-story-seccomp-denies-af-vsock","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:seccomp-denies-af-vsock@1#0~a7ebf021fcc04fdd","executor":null,"recorded_at":"2026-09-04T01:07:19Z"}}
{"entity":"story","version":1,"id":"seccomp-denies-af-vsock","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:review-2026-09-03-findings"}]},"args":{"command":"create-relation","kind":"decomposes","source":"01MEM0000000000000407","target":"01MEM0000000000000025"},"payload":{"actor":"human:timo","at":1788484039158,"causation":"cmd-story-seccomp-denies-af-vsock-decomposes-epic-review-2026-09-03-findings","change":{"change":"related","relation":"decomposes","target":"epic:review-2026-09-03-findings"},"correlation":"protocol-artifact-relate","event_id":"story:seccomp-denies-af-vsock@1#0~64ff783244d4eb47","executor":null,"recorded_at":"2026-09-04T01:07:19Z"}}
{"entity":"story","version":1,"id":"exec-oom-kills-the-whole-tree","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Story: An exec OOM kills the whole tree\n\n## Context\n\n`Cgroup::create` writes `pids.max`, `memory.max`, `memory.swap.max=0` and `cpu.max`\n(`crates/substrate-host/src/process.rs:2848-2860`) and not `memory.oom.group=1`. The kernel then\nkills one process on OOM and the rest of the tree continues, which is not the whole-tree semantics\nthe safety envelope names (`AGENTS.md` § Safety envelope). The `exec.memory-limit` refusal is\nderived from `memory_oom_kills` in the terminal usage, which exists only when the client asked for\n`measurements` (`process.rs`, `run_child` and `record_resource_bound`), so an OOM on a run without\nmeasurements is reported as an ordinary exit. `cpu.max` is clamped to one CPU\n(`process.rs:2858`), which is a documented choice worth stating on the capability fact.\n\n## Acceptance\n\nAn exec whose child exceeds `memory.max` ends with every process in its cgroup gone and\n`refusal.code = exec.memory-limit` on the observation whether or not `measurements` was requested,\nproven in the delegated lane.\n\n## Notes\n\n`memory.oom.group` is a cgroup v2 file present on every kernel the probe already requires; probe\nit and withhold the exec facts where it is absent.\n","summary":"No memory.oom.group=1; exec.memory-limit recorded only when measurements requested (process.rs:2848-2860).","tags":["review"],"title":"An exec OOM kills the whole cgroup and is named on the observation regardless of measurements"},"args":{"command":"create-entity","data":{"body":"# Story: An exec OOM kills the whole tree\n\n## Context\n\n`Cgroup::create` writes `pids.max`, `memory.max`, `memory.swap.max=0` and `cpu.max`\n(`crates/substrate-host/src/process.rs:2848-2860`) and not `memory.oom.group=1`. The kernel then\nkills one process on OOM and the rest of the tree continues, which is not the whole-tree semantics\nthe safety envelope names (`AGENTS.md` § Safety envelope). The `exec.memory-limit` refusal is\nderived from `memory_oom_kills` in the terminal usage, which exists only when the client asked for\n`measurements` (`process.rs`, `run_child` and `record_resource_bound`), so an OOM on a run without\nmeasurements is reported as an ordinary exit. `cpu.max` is clamped to one CPU\n(`process.rs:2858`), which is a documented choice worth stating on the capability fact.\n\n## Acceptance\n\nAn exec whose child exceeds `memory.max` ends with every process in its cgroup gone and\n`refusal.code = exec.memory-limit` on the observation whether or not `measurements` was requested,\nproven in the delegated lane.\n\n## Notes\n\n`memory.oom.group` is a cgroup v2 file present on every kernel the probe already requires; probe\nit and withhold the exec facts where it is absent.\n","status":"draft","summary":"No memory.oom.group=1; exec.memory-limit recorded only when measurements requested (process.rs:2848-2860).","tags":["review"],"title":"An exec OOM kills the whole cgroup and is named on the observation regardless of measurements"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/exec-oom-kills-the-whole-tree"},"payload":{"actor":"human:timo","at":1788484039375,"causation":"cmd-new-story-exec-oom-kills-the-whole-tree","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:exec-oom-kills-the-whole-tree@1#0~215ca0239fff180f","executor":null,"recorded_at":"2026-09-04T01:07:19Z"}}
{"entity":"story","version":1,"id":"exec-oom-kills-the-whole-tree","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:review-2026-09-03-findings"}]},"args":{"command":"create-relation","kind":"decomposes","source":"01MEM0000000000000412","target":"01MEM0000000000000025"},"payload":{"actor":"human:timo","at":1788484039385,"causation":"cmd-story-exec-oom-kills-the-whole-tree-decomposes-epic-review-2026-09-03-findings","change":{"change":"related","relation":"decomposes","target":"epic:review-2026-09-03-findings"},"correlation":"protocol-artifact-relate","event_id":"story:exec-oom-kills-the-whole-tree@1#0~35212ac54cf60ff1","executor":null,"recorded_at":"2026-09-04T01:07:19Z"}}
{"entity":"story","version":1,"id":"session-attachment-lifetime-is-an-accepted-decision","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Story: The session attachment lifetime is an accepted decision\n\n## Context\n\nA session attachment lives at most one hour (`crates/substrate-daemon/src/app/sessions.rs:70`);\nwhen that expires, or on any disconnect or protocol error, the process tree is killed\n(`sessions.rs:1301`) and the one-shot attachment right is consumed\n(`crates/substrate-store/src/sessions.rs`, `claim_pipe_session_attachment_inner`). No re-attach\nexists. ADR 0008 states the kill-on-loss rule; nothing states the one-hour figure or that an\ninteractive agent session longer than an hour is out of scope.\n\n## Acceptance\n\nEither an ADR or an amendment to ADR 0008 records the attachment lifetime, the no-re-attach rule\nand the consumer they were chosen for, or a story is filed to lift the bound; the design doc and\nthe published capability document agree with the code.\n\n## Notes\n\nIf re-attach is wanted, it is a contract change (a new attachment state and refusal) and needs a\nsuccessor bundle.\n","summary":"Attachments live 1 h, any disconnect kills the tree, no re-attach exists (sessions.rs:70,1301); the figure is undocumented.","tags":["review"],"title":"The 1 h attachment lifetime and kill-on-disconnect are a recorded decision"},"args":{"command":"create-entity","data":{"body":"# Story: The session attachment lifetime is an accepted decision\n\n## Context\n\nA session attachment lives at most one hour (`crates/substrate-daemon/src/app/sessions.rs:70`);\nwhen that expires, or on any disconnect or protocol error, the process tree is killed\n(`sessions.rs:1301`) and the one-shot attachment right is consumed\n(`crates/substrate-store/src/sessions.rs`, `claim_pipe_session_attachment_inner`). No re-attach\nexists. ADR 0008 states the kill-on-loss rule; nothing states the one-hour figure or that an\ninteractive agent session longer than an hour is out of scope.\n\n## Acceptance\n\nEither an ADR or an amendment to ADR 0008 records the attachment lifetime, the no-re-attach rule\nand the consumer they were chosen for, or a story is filed to lift the bound; the design doc and\nthe published capability document agree with the code.\n\n## Notes\n\nIf re-attach is wanted, it is a contract change (a new attachment state and refusal) and needs a\nsuccessor bundle.\n","status":"draft","summary":"Attachments live 1 h, any disconnect kills the tree, no re-attach exists (sessions.rs:70,1301); the figure is undocumented.","tags":["review"],"title":"The 1 h attachment lifetime and kill-on-disconnect are a recorded decision"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/session-attachment-lifetime-is-an-accepted-decision"},"payload":{"actor":"human:timo","at":1788484039601,"causation":"cmd-new-story-session-attachment-lifetime-is-an-accepted-decision","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:session-attachment-lifetime-is-an-accepted-decision@1#0~bae68799b787ff2f","executor":null,"recorded_at":"2026-09-04T01:07:19Z"}}
{"entity":"story","version":1,"id":"session-attachment-lifetime-is-an-accepted-decision","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:review-2026-09-03-findings"}]},"args":{"command":"create-relation","kind":"decomposes","source":"01MEM0000000000000417","target":"01MEM0000000000000025"},"payload":{"actor":"human:timo","at":1788484039609,"causation":"cmd-story-session-attachment-lifetime-is-an-accepted-decision-decomposes-epic-review-2026-09-03-findings","change":{"change":"related","relation":"decomposes","target":"epic:review-2026-09-03-findings"},"correlation":"protocol-artifact-relate","event_id":"story:session-attachment-lifetime-is-an-accepted-decision@1#0~3b7a788f0fe2daac","executor":null,"recorded_at":"2026-09-04T01:07:19Z"}}
{"entity":"story","version":1,"id":"hosted-admission-reuses-identity-authority","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Story: Hosted admission reuses a resolved Identity authority\n\n## Context\n\nEvery request on the production TLS listener resolves its credential against Identity with a\nfresh TCP connect, TLS 1.3 handshake and HTTP/1.1 request carrying `Connection: close`\n(`crates/substrate-daemon/src/hosted.rs:170-194`). Nothing is cached, although the authority\ncarries `jti`, `exp` and a lifetime bound of 5 min (`hosted.rs:42-43`, `validate_authority`).\nPer-request latency includes one Identity round trip, and Identity load equals Substrate request\nrate. ADR 0026 chose online resolution over introspection caching; it does not state that\nconnection reuse or a bounded per-credential cache is excluded.\n\n## Acceptance\n\nEither two requests within one authority's remaining lifetime cause at most one Identity\nresolution, with revocation latency bounded and stated on the capability document, or an ADR\nrecords why every request must resolve online and the cost is written into the deployment guide.\n\n## Notes\n\nConnection reuse alone (a small keep-alive pool to Identity) removes the handshake without any\nchange to the trust posture.\n","summary":"Every TLS request opens a fresh connection to Identity with Connection: close and no cache (hosted.rs:170-194).","tags":["review"],"title":"Hosted admission reuses a resolved Identity authority within its lifetime, or an ADR records why not"},"args":{"command":"create-entity","data":{"body":"# Story: Hosted admission reuses a resolved Identity authority\n\n## Context\n\nEvery request on the production TLS listener resolves its credential against Identity with a\nfresh TCP connect, TLS 1.3 handshake and HTTP/1.1 request carrying `Connection: close`\n(`crates/substrate-daemon/src/hosted.rs:170-194`). Nothing is cached, although the authority\ncarries `jti`, `exp` and a lifetime bound of 5 min (`hosted.rs:42-43`, `validate_authority`).\nPer-request latency includes one Identity round trip, and Identity load equals Substrate request\nrate. ADR 0026 chose online resolution over introspection caching; it does not state that\nconnection reuse or a bounded per-credential cache is excluded.\n\n## Acceptance\n\nEither two requests within one authority's remaining lifetime cause at most one Identity\nresolution, with revocation latency bounded and stated on the capability document, or an ADR\nrecords why every request must resolve online and the cost is written into the deployment guide.\n\n## Notes\n\nConnection reuse alone (a small keep-alive pool to Identity) removes the handshake without any\nchange to the trust posture.\n","status":"draft","summary":"Every TLS request opens a fresh connection to Identity with Connection: close and no cache (hosted.rs:170-194).","tags":["review"],"title":"Hosted admission reuses a resolved Identity authority within its lifetime, or an ADR records why not"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/hosted-admission-reuses-identity-authority"},"payload":{"actor":"human:timo","at":1788484039828,"causation":"cmd-new-story-hosted-admission-reuses-identity-authority","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:hosted-admission-reuses-identity-authority@1#0~9919e1adb4e4dca9","executor":null,"recorded_at":"2026-09-04T01:07:19Z"}}
{"entity":"story","version":1,"id":"hosted-admission-reuses-identity-authority","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:review-2026-09-03-findings"}]},"args":{"command":"create-relation","kind":"decomposes","source":"01MEM0000000000000422","target":"01MEM0000000000000025"},"payload":{"actor":"human:timo","at":1788484039836,"causation":"cmd-story-hosted-admission-reuses-identity-authority-decomposes-epic-review-2026-09-03-findings","change":{"change":"related","relation":"decomposes","target":"epic:review-2026-09-03-findings"},"correlation":"protocol-artifact-relate","event_id":"story:hosted-admission-reuses-identity-authority@1#0~6a06571abc8988d0","executor":null,"recorded_at":"2026-09-04T01:07:19Z"}}
{"entity":"story","version":1,"id":"daemon-image-serves-exec-or-says-it-cannot","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Story: The daemon image serves exec or says it cannot\n\n## Context\n\nExec capability requires the configured bubblewrap binary and `/usr/bin/socat` on the host\n(`crates/substrate-host/src/probe.rs:319-320`; documented at `README.md:280`). The published\ndaemon image is `gcr.io/distroless/cc-debian12:nonroot` (`Dockerfile:12`) and installs neither,\nnor `/usr/bin/env`, which the sandbox argv execs (`process.rs`, `command`). Inferred from the base\nimage, not pulled: that image can only answer `exec.sandbox-unavailable`, and a reader of the\nrelease notes is not told so.\n\n## Acceptance\n\nEither the daemon image carries bubblewrap, socat and coreutils and the delegated lane runs against\nthe shipped image, or `README.md`, `STATUS.md` and the GHCR release notes state that the published\nimage serves workspaces only and name what a host must add to serve exec.\n\n## Notes\n\nThe `socat` dependency exists only to prove the seccomp `AF_UNIX` refusal; a small Rust probe\nchild would remove it.\n","summary":"Exec needs bwrap and /usr/bin/socat (probe.rs:319); the distroless image (Dockerfile:12) has neither; inferred from the base image.","tags":["review"],"title":"The published daemon image carries bubblewrap and socat, or documents that it serves no exec"},"args":{"command":"create-entity","data":{"body":"# Story: The daemon image serves exec or says it cannot\n\n## Context\n\nExec capability requires the configured bubblewrap binary and `/usr/bin/socat` on the host\n(`crates/substrate-host/src/probe.rs:319-320`; documented at `README.md:280`). The published\ndaemon image is `gcr.io/distroless/cc-debian12:nonroot` (`Dockerfile:12`) and installs neither,\nnor `/usr/bin/env`, which the sandbox argv execs (`process.rs`, `command`). Inferred from the base\nimage, not pulled: that image can only answer `exec.sandbox-unavailable`, and a reader of the\nrelease notes is not told so.\n\n## Acceptance\n\nEither the daemon image carries bubblewrap, socat and coreutils and the delegated lane runs against\nthe shipped image, or `README.md`, `STATUS.md` and the GHCR release notes state that the published\nimage serves workspaces only and name what a host must add to serve exec.\n\n## Notes\n\nThe `socat` dependency exists only to prove the seccomp `AF_UNIX` refusal; a small Rust probe\nchild would remove it.\n","status":"draft","summary":"Exec needs bwrap and /usr/bin/socat (probe.rs:319); the distroless image (Dockerfile:12) has neither; inferred from the base image.","tags":["review"],"title":"The published daemon image carries bubblewrap and socat, or documents that it serves no exec"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/daemon-image-serves-exec-or-says-it-cannot"},"payload":{"actor":"human:timo","at":1788484040054,"causation":"cmd-new-story-daemon-image-serves-exec-or-says-it-cannot","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:daemon-image-serves-exec-or-says-it-cannot@1#0~df34636d4c6b4a02","executor":null,"recorded_at":"2026-09-04T01:07:20Z"}}
{"entity":"story","version":1,"id":"daemon-image-serves-exec-or-says-it-cannot","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:review-2026-09-03-findings"}]},"args":{"command":"create-relation","kind":"decomposes","source":"01MEM0000000000000427","target":"01MEM0000000000000025"},"payload":{"actor":"human:timo","at":1788484040063,"causation":"cmd-story-daemon-image-serves-exec-or-says-it-cannot-decomposes-epic-review-2026-09-03-findings","change":{"change":"related","relation":"decomposes","target":"epic:review-2026-09-03-findings"},"correlation":"protocol-artifact-relate","event_id":"story:daemon-image-serves-exec-or-says-it-cannot@1#0~b5367387f5255e5d","executor":null,"recorded_at":"2026-09-04T01:07:20Z"}}
{"entity":"story","version":1,"id":"backend-recheck-hashes-only-on-change","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Story: Backend recheck hashes only on change\n\n## Context\n\nEvery exec admission calls `recheck_backend`, which calls `backend_binding`, which reads the\nwhole bubblewrap binary and computes its SHA-256 (`crates/substrate-host/src/process.rs:1753`;\n`probe.rs:267`). The binding already records device, inode and size. On a busy daemon this is one\nfile read and hash per exec start on the admission path.\n\n## Acceptance\n\nAn exec admission whose bubblewrap device, inode, size and modification time match the probed\nbinding does not read the binary, while a replaced binary is still refused with\n`exec.capability-stale`, both proven by a test.\n\n## Notes\n\nKeep the full hash in the startup probe; the recheck compares metadata first and hashes only when\nit differs.\n","summary":"recheck_backend re-reads and SHA-256s the bwrap binary on every exec admission (process.rs:1753, probe.rs:267).","tags":["review"],"title":"Exec admission rechecks the backend binary without re-hashing it when its metadata is unchanged"},"args":{"command":"create-entity","data":{"body":"# Story: Backend recheck hashes only on change\n\n## Context\n\nEvery exec admission calls `recheck_backend`, which calls `backend_binding`, which reads the\nwhole bubblewrap binary and computes its SHA-256 (`crates/substrate-host/src/process.rs:1753`;\n`probe.rs:267`). The binding already records device, inode and size. On a busy daemon this is one\nfile read and hash per exec start on the admission path.\n\n## Acceptance\n\nAn exec admission whose bubblewrap device, inode, size and modification time match the probed\nbinding does not read the binary, while a replaced binary is still refused with\n`exec.capability-stale`, both proven by a test.\n\n## Notes\n\nKeep the full hash in the startup probe; the recheck compares metadata first and hashes only when\nit differs.\n","status":"draft","summary":"recheck_backend re-reads and SHA-256s the bwrap binary on every exec admission (process.rs:1753, probe.rs:267).","tags":["review"],"title":"Exec admission rechecks the backend binary without re-hashing it when its metadata is unchanged"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/backend-recheck-hashes-only-on-change"},"payload":{"actor":"human:timo","at":1788484040283,"causation":"cmd-new-story-backend-recheck-hashes-only-on-change","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:backend-recheck-hashes-only-on-change@1#0~ad4866469b0f7599","executor":null,"recorded_at":"2026-09-04T01:07:20Z"}}
{"entity":"story","version":1,"id":"backend-recheck-hashes-only-on-change","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:review-2026-09-03-findings"}]},"args":{"command":"create-relation","kind":"decomposes","source":"01MEM0000000000000432","target":"01MEM0000000000000025"},"payload":{"actor":"human:timo","at":1788484040291,"causation":"cmd-story-backend-recheck-hashes-only-on-change-decomposes-epic-review-2026-09-03-findings","change":{"change":"related","relation":"decomposes","target":"epic:review-2026-09-03-findings"},"correlation":"protocol-artifact-relate","event_id":"story:backend-recheck-hashes-only-on-change@1#0~40ba3e75d9e86737","executor":null,"recorded_at":"2026-09-04T01:07:20Z"}}
{"entity":"story","version":1,"id":"events-table-has-one-index","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Story: The events table has one index\n\n## Context\n\n`events` is a `WITHOUT ROWID` table with primary key `(deployment, subject, seq)`; the schema also\ncreates `events_subject_sequence` on the same three columns\n(`crates/substrate-store/src/schema.rs:203-206`). Every event append writes two identical B-trees,\nand the migration at `schema.rs:303` recreates the duplicate.\n\n## Acceptance\n\n`PRAGMA index_list(events)` shows no index whose columns equal the primary key, on a fresh store\nand on a store migrated from the current schema, proven by a store test.\n\n## Notes\n\n`DROP INDEX IF EXISTS events_subject_sequence` in a migration step; the primary key serves the\nsame query.\n","summary":"events_subject_sequence duplicates the WITHOUT ROWID primary key (schema.rs:203-206).","tags":["review"],"title":"The events table carries no index duplicating its primary key"},"args":{"command":"create-entity","data":{"body":"# Story: The events table has one index\n\n## Context\n\n`events` is a `WITHOUT ROWID` table with primary key `(deployment, subject, seq)`; the schema also\ncreates `events_subject_sequence` on the same three columns\n(`crates/substrate-store/src/schema.rs:203-206`). Every event append writes two identical B-trees,\nand the migration at `schema.rs:303` recreates the duplicate.\n\n## Acceptance\n\n`PRAGMA index_list(events)` shows no index whose columns equal the primary key, on a fresh store\nand on a store migrated from the current schema, proven by a store test.\n\n## Notes\n\n`DROP INDEX IF EXISTS events_subject_sequence` in a migration step; the primary key serves the\nsame query.\n","status":"draft","summary":"events_subject_sequence duplicates the WITHOUT ROWID primary key (schema.rs:203-206).","tags":["review"],"title":"The events table carries no index duplicating its primary key"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/events-table-has-one-index"},"payload":{"actor":"human:timo","at":1788484040512,"causation":"cmd-new-story-events-table-has-one-index","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:events-table-has-one-index@1#0~72382612600fd191","executor":null,"recorded_at":"2026-09-04T01:07:20Z"}}
{"entity":"story","version":1,"id":"events-table-has-one-index","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:review-2026-09-03-findings"}]},"args":{"command":"create-relation","kind":"decomposes","source":"01MEM0000000000000437","target":"01MEM0000000000000025"},"payload":{"actor":"human:timo","at":1788484040521,"causation":"cmd-story-events-table-has-one-index-decomposes-epic-review-2026-09-03-findings","change":{"change":"related","relation":"decomposes","target":"epic:review-2026-09-03-findings"},"correlation":"protocol-artifact-relate","event_id":"story:events-table-has-one-index@1#0~96c916beb1db18fa","executor":null,"recorded_at":"2026-09-04T01:07:20Z"}}
{"entity":"story","version":1,"id":"unattached-claimed-session-is-contained","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Story: A claimed but unattached session is contained\n\n## Context\n\n`pipe_session_attach` consumes the durable attachment claim before the WebSocket upgrade completes\n(`crates/substrate-daemon/src/app/sessions.rs`, `claim_pipe_session_attachment` then\n`ws.on_upgrade`). If the client drops between the `101` and the upgrade, the `on_upgrade` closure\nnever runs, the permit is dropped, the claim stays `attached`, and the process runs unattached\nuntil its lease or timeout ends it. No test covers this path.\n\n## Acceptance\n\nA client that drops after the attachment claim and before the upgrade leaves no running process\nwithin one maintenance tick, and the session reads as terminal with a named refusal; a test proves\nit.\n\n## Notes\n\nEither terminate the session when the upgrade future fails, or claim inside the upgrade task and\nrefuse the upgrade when the claim fails.\n","summary":"The claim is consumed before the WebSocket upgrade; a dropped client leaves the process running until lease or timeout (sessions.rs attach).","tags":["review"],"title":"A session whose attachment claim never upgraded is terminated rather than left running"},"args":{"command":"create-entity","data":{"body":"# Story: A claimed but unattached session is contained\n\n## Context\n\n`pipe_session_attach` consumes the durable attachment claim before the WebSocket upgrade completes\n(`crates/substrate-daemon/src/app/sessions.rs`, `claim_pipe_session_attachment` then\n`ws.on_upgrade`). If the client drops between the `101` and the upgrade, the `on_upgrade` closure\nnever runs, the permit is dropped, the claim stays `attached`, and the process runs unattached\nuntil its lease or timeout ends it. No test covers this path.\n\n## Acceptance\n\nA client that drops after the attachment claim and before the upgrade leaves no running process\nwithin one maintenance tick, and the session reads as terminal with a named refusal; a test proves\nit.\n\n## Notes\n\nEither terminate the session when the upgrade future fails, or claim inside the upgrade task and\nrefuse the upgrade when the claim fails.\n","status":"draft","summary":"The claim is consumed before the WebSocket upgrade; a dropped client leaves the process running until lease or timeout (sessions.rs attach).","tags":["review"],"title":"A session whose attachment claim never upgraded is terminated rather than left running"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/unattached-claimed-session-is-contained"},"payload":{"actor":"human:timo","at":1788484040757,"causation":"cmd-new-story-unattached-claimed-session-is-contained","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:unattached-claimed-session-is-contained@1#0~952d6bad967f241c","executor":null,"recorded_at":"2026-09-04T01:07:20Z"}}
{"entity":"story","version":1,"id":"unattached-claimed-session-is-contained","revision":1,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:review-2026-09-03-findings"}]},"args":{"command":"create-relation","kind":"decomposes","source":"01MEM0000000000000442","target":"01MEM0000000000000025"},"payload":{"actor":"human:timo","at":1788484040765,"causation":"cmd-story-unattached-claimed-session-is-contained-decomposes-epic-review-2026-09-03-findings","change":{"change":"related","relation":"decomposes","target":"epic:review-2026-09-03-findings"},"correlation":"protocol-artifact-relate","event_id":"story:unattached-claimed-session-is-contained@1#0~5dadf7e0400905c6","executor":null,"recorded_at":"2026-09-04T01:07:20Z"}}
{"entity":"story","version":1,"id":"line-patch-edits-apply-in-line-order","revision":2,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"scope":[{"confidence":"cited","path":"crates/substrate-host/src/fs.rs"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/substrate-host/src/fs.rs"}]},"command":"update-entity","target":"01MEM0000000000000127"},"payload":{"actor":"human:timo","at":1788484040907,"causation":"cmd-protocol-artifact-scope-story-line-patch-edits-apply-in-line-order-1788484040907","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:line-patch-edits-apply-in-line-order@2#0~66bd87d0ba2ecf2c","executor":null,"recorded_at":"2026-09-04T01:07:20Z"}}
{"entity":"story","version":1,"id":"directory-listing-survives-special-entries","revision":2,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"scope":[{"confidence":"cited","path":"crates/substrate-host/src/fs.rs"},{"confidence":"cited","path":"crates/substrate-wire/src/lib.rs"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/substrate-host/src/fs.rs"},{"confidence":"cited","path":"crates/substrate-wire/src/lib.rs"}]},"command":"update-entity","target":"01MEM0000000000000073"},"payload":{"actor":"human:timo","at":1788484041051,"causation":"cmd-protocol-artifact-scope-story-directory-listing-survives-special-entries-1788484041051","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:directory-listing-survives-special-entries@2#0~b3522b3a9d22d2d2","executor":null,"recorded_at":"2026-09-04T01:07:21Z"}}
{"entity":"story","version":1,"id":"metrics-streams-are-bounded","revision":2,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"scope":[{"confidence":"cited","path":"crates/substrate-daemon/src/app/metrics.rs"},{"confidence":"cited","path":"crates/substrate-daemon/src/app/service.rs"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/substrate-daemon/src/app/metrics.rs"},{"confidence":"cited","path":"crates/substrate-daemon/src/app/service.rs"}]},"command":"update-entity","target":"01MEM0000000000000133"},"payload":{"actor":"human:timo","at":1788484041193,"causation":"cmd-protocol-artifact-scope-story-metrics-streams-are-bounded-1788484041193","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:metrics-streams-are-bounded@2#0~f08bfed28e0c91d8","executor":null,"recorded_at":"2026-09-04T01:07:21Z"}}
{"entity":"story","version":1,"id":"upgraded-connections-keep-their-permit","revision":2,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"scope":[{"confidence":"cited","path":"crates/substrate-daemon/src/runtime.rs"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/substrate-daemon/src/runtime.rs"}]},"command":"update-entity","target":"01MEM0000000000000211"},"payload":{"actor":"human:timo","at":1788484041336,"causation":"cmd-protocol-artifact-scope-story-upgraded-connections-keep-their-permit-1788484041336","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:upgraded-connections-keep-their-permit@2#0~0115ad684f7773a5","executor":null,"recorded_at":"2026-09-04T01:07:21Z"}}
{"entity":"story","version":1,"id":"lease-cleanup-reads-exec-state-only","revision":2,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"scope":[{"confidence":"cited","path":"crates/substrate-daemon/src/app/service.rs"},{"confidence":"cited","path":"crates/substrate-store/src/execs.rs"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/substrate-daemon/src/app/service.rs"},{"confidence":"cited","path":"crates/substrate-store/src/execs.rs"}]},"command":"update-entity","target":"01MEM0000000000000121"},"payload":{"actor":"human:timo","at":1788484041479,"causation":"cmd-protocol-artifact-scope-story-lease-cleanup-reads-exec-state-only-1788484041479","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:lease-cleanup-reads-exec-state-only@2#0~5317479594fa73c5","executor":null,"recorded_at":"2026-09-04T01:07:21Z"}}
{"entity":"story","version":1,"id":"confined-processes-cannot-nest-user-namespaces","revision":2,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"scope":[{"confidence":"cited","path":"crates/substrate-host/src/egress.rs"},{"confidence":"cited","path":"crates/substrate-host/src/probe.rs"},{"confidence":"cited","path":"crates/substrate-host/src/process.rs"},{"confidence":"cited","path":"crates/substrate-host/src/pty.rs"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/substrate-host/src/egress.rs"},{"confidence":"cited","path":"crates/substrate-host/src/probe.rs"},{"confidence":"cited","path":"crates/substrate-host/src/process.rs"},{"confidence":"cited","path":"crates/substrate-host/src/pty.rs"}]},"command":"update-entity","target":"01MEM0000000000000052"},"payload":{"actor":"human:timo","at":1788484041621,"causation":"cmd-protocol-artifact-scope-story-confined-processes-cannot-nest-user-namespaces-1788484041621","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:confined-processes-cannot-nest-user-namespaces@2#0~87201207af366fed","executor":null,"recorded_at":"2026-09-04T01:07:21Z"}}
{"entity":"story","version":1,"id":"seccomp-denies-af-vsock","revision":2,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"scope":[{"confidence":"cited","path":"crates/substrate-host/src/probe.rs"},{"confidence":"cited","path":"crates/substrate-host/src/seccomp.rs"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/substrate-host/src/probe.rs"},{"confidence":"cited","path":"crates/substrate-host/src/seccomp.rs"}]},"command":"update-entity","target":"01MEM0000000000000190"},"payload":{"actor":"human:timo","at":1788484041763,"causation":"cmd-protocol-artifact-scope-story-seccomp-denies-af-vsock-1788484041763","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:seccomp-denies-af-vsock@2#0~dc7a62a149321763","executor":null,"recorded_at":"2026-09-04T01:07:21Z"}}
{"entity":"story","version":1,"id":"exec-oom-kills-the-whole-tree","revision":2,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"scope":[{"confidence":"cited","path":"crates/substrate-host/src/probe.rs"},{"confidence":"cited","path":"crates/substrate-host/src/process.rs"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/substrate-host/src/probe.rs"},{"confidence":"cited","path":"crates/substrate-host/src/process.rs"}]},"command":"update-entity","target":"01MEM0000000000000091"},"payload":{"actor":"human:timo","at":1788484041907,"causation":"cmd-protocol-artifact-scope-story-exec-oom-kills-the-whole-tree-1788484041907","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:exec-oom-kills-the-whole-tree@2#0~51662614152b4c76","executor":null,"recorded_at":"2026-09-04T01:07:21Z"}}
{"entity":"story","version":1,"id":"session-attachment-lifetime-is-an-accepted-decision","revision":2,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"scope":[{"confidence":"cited","path":"adr/0008-pipe-sessions-have-distinct-durable-identity.md"},{"confidence":"cited","path":"docs/design/05-streams-sessions-and-endpoints.md"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"adr/0008-pipe-sessions-have-distinct-durable-identity.md"},{"confidence":"cited","path":"docs/design/05-streams-sessions-and-endpoints.md"}]},"command":"update-entity","target":"01MEM0000000000000196"},"payload":{"actor":"human:timo","at":1788484042048,"causation":"cmd-protocol-artifact-scope-story-session-attachment-lifetime-is-an-accepted-decision-1788484042048","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:session-attachment-lifetime-is-an-accepted-decision@2#0~4f1ae3d025cb56c8","executor":null,"recorded_at":"2026-09-04T01:07:22Z"}}
{"entity":"story","version":1,"id":"hosted-admission-reuses-identity-authority","revision":2,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"scope":[{"confidence":"cited","path":"adr/0026-hosted-admission-resolves-opaque-identity-authority.md"},{"confidence":"cited","path":"crates/substrate-daemon/src/hosted.rs"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"adr/0026-hosted-admission-resolves-opaque-identity-authority.md"},{"confidence":"cited","path":"crates/substrate-daemon/src/hosted.rs"}]},"command":"update-entity","target":"01MEM0000000000000109"},"payload":{"actor":"human:timo","at":1788484042190,"causation":"cmd-protocol-artifact-scope-story-hosted-admission-reuses-identity-authority-1788484042190","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:hosted-admission-reuses-identity-authority@2#0~d48aabacfcdac314","executor":null,"recorded_at":"2026-09-04T01:07:22Z"}}
{"entity":"story","version":1,"id":"daemon-image-serves-exec-or-says-it-cannot","revision":2,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"scope":[{"confidence":"cited","path":"Dockerfile"},{"confidence":"cited","path":"README.md"},{"confidence":"cited","path":"STATUS.md"},{"confidence":"cited","path":"crates/substrate-host/src/probe.rs"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"Dockerfile"},{"confidence":"cited","path":"README.md"},{"confidence":"cited","path":"STATUS.md"},{"confidence":"cited","path":"crates/substrate-host/src/probe.rs"}]},"command":"update-entity","target":"01MEM0000000000000067"},"payload":{"actor":"human:timo","at":1788484042332,"causation":"cmd-protocol-artifact-scope-story-daemon-image-serves-exec-or-says-it-cannot-1788484042332","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:daemon-image-serves-exec-or-says-it-cannot@2#0~0ed614dd49f8a03e","executor":null,"recorded_at":"2026-09-04T01:07:22Z"}}
{"entity":"story","version":1,"id":"backend-recheck-hashes-only-on-change","revision":2,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"scope":[{"confidence":"cited","path":"crates/substrate-host/src/probe.rs"},{"confidence":"cited","path":"crates/substrate-host/src/process.rs"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/substrate-host/src/probe.rs"},{"confidence":"cited","path":"crates/substrate-host/src/process.rs"}]},"command":"update-entity","target":"01MEM0000000000000043"},"payload":{"actor":"human:timo","at":1788484042477,"causation":"cmd-protocol-artifact-scope-story-backend-recheck-hashes-only-on-change-1788484042477","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:backend-recheck-hashes-only-on-change@2#0~4eb3303db0a390a9","executor":null,"recorded_at":"2026-09-04T01:07:22Z"}}
{"entity":"story","version":1,"id":"events-table-has-one-index","revision":2,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"scope":[{"confidence":"cited","path":"crates/substrate-store/src/schema.rs"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/substrate-store/src/schema.rs"}]},"command":"update-entity","target":"01MEM0000000000000088"},"payload":{"actor":"human:timo","at":1788484042619,"causation":"cmd-protocol-artifact-scope-story-events-table-has-one-index-1788484042619","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:events-table-has-one-index@2#0~dc35331611b7ace9","executor":null,"recorded_at":"2026-09-04T01:07:22Z"}}
{"entity":"story","version":1,"id":"unattached-claimed-session-is-contained","revision":2,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"scope":[{"confidence":"cited","path":"crates/substrate-daemon/src/app/sessions.rs"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/substrate-daemon/src/app/sessions.rs"}]},"command":"update-entity","target":"01MEM0000000000000208"},"payload":{"actor":"human:timo","at":1788484042762,"causation":"cmd-protocol-artifact-scope-story-unattached-claimed-session-is-contained-1788484042762","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:unattached-claimed-session-is-contained@2#0~f189c40468ed4c86","executor":null,"recorded_at":"2026-09-04T01:07:22Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-scope-round-1","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"active","changed":{"body":"approve\n\n**What I read:** the epic body (`aep artifact show epic:review-2026-09-03-findings`), the parent review register (`~/.cache/substrate-review/2026-09-03-substrate-in-depth-review.md`, all 14 findings), the full body of all 14 stories carrying `decomposes: epic:review-2026-09-03-findings` (`aep artifact show story:<id>` × 14, ids confirmed via `aep artifact graph --format json` filtered on that relation), and four unrelated existing \"implemented\" stories (`confinement-runtime-hardening`, `execution-resource-metrics`, `workspace-and-scratch-quotas`, `network-session-authority`, `hosted-trust-envelope-admission`, `production-tls-control-listener`) to rule out a finding already being claimed elsewhere in the graph. I extracted 18 promises from the epic — the 14 explicit finding→story mappings named in its Scope section, plus four cross-cutting constraints (closure-by-test-or-recorded-decision for every finding; the frozen-contract-bundle exclusion, which only story 2 can trigger; the named-refusal requirement for stories 6/7 under invariant 3; the observe-before-fix framing for story 4) — and traced all 18 to a story: 18/18.\n\nEach of the 14 stories reproduces its finding's specifics accurately (file:line citations match, severity groupings match: 2 correctness, 3 capacity, 3 hardening, 3 decisions, 3 minor), and the three risk-driven constraints are honored verbatim in the relevant story bodies — story 2 and story 9 both name the successor-bundle path for a possible contract change, story 4's acceptance is phrased as observe-first (\"the story records the observation ... and closes with no code change\"), and stories 6/7 route to the named-refusal path. No story claims work outside the review's 14 findings, and no existing artifact in the graph already claims any of them.\n\n**What I could not establish:** finding 8 in the review also notes `cpu.max` is clamped to one CPU as \"a documented choice worth stating on the capability fact,\" but the review's own suggested-fixes list only cites `memory.oom.group=1` for this finding, and story 8's acceptance covers only the oom-group and refusal-reporting parts — whether the cpu.max documentation point was consciously dropped or simply not separately tracked, I could not tell from the story body alone; this reads as ambiguous rather than a clear narrowing, so it is not raised as a finding.\n\n```findings\n[]\n```\n","tags":["critic","review"],"title":"Scope critic (aep-planning:plan-critic-scope), round 1"},"args":{"command":"create-entity","data":{"body":"approve\n\n**What I read:** the epic body (`aep artifact show epic:review-2026-09-03-findings`), the parent review register (`~/.cache/substrate-review/2026-09-03-substrate-in-depth-review.md`, all 14 findings), the full body of all 14 stories carrying `decomposes: epic:review-2026-09-03-findings` (`aep artifact show story:<id>` × 14, ids confirmed via `aep artifact graph --format json` filtered on that relation), and four unrelated existing \"implemented\" stories (`confinement-runtime-hardening`, `execution-resource-metrics`, `workspace-and-scratch-quotas`, `network-session-authority`, `hosted-trust-envelope-admission`, `production-tls-control-listener`) to rule out a finding already being claimed elsewhere in the graph. I extracted 18 promises from the epic — the 14 explicit finding→story mappings named in its Scope section, plus four cross-cutting constraints (closure-by-test-or-recorded-decision for every finding; the frozen-contract-bundle exclusion, which only story 2 can trigger; the named-refusal requirement for stories 6/7 under invariant 3; the observe-before-fix framing for story 4) — and traced all 18 to a story: 18/18.\n\nEach of the 14 stories reproduces its finding's specifics accurately (file:line citations match, severity groupings match: 2 correctness, 3 capacity, 3 hardening, 3 decisions, 3 minor), and the three risk-driven constraints are honored verbatim in the relevant story bodies — story 2 and story 9 both name the successor-bundle path for a possible contract change, story 4's acceptance is phrased as observe-first (\"the story records the observation ... and closes with no code change\"), and stories 6/7 route to the named-refusal path. No story claims work outside the review's 14 findings, and no existing artifact in the graph already claims any of them.\n\n**What I could not establish:** finding 8 in the review also notes `cpu.max` is clamped to one CPU as \"a documented choice worth stating on the capability fact,\" but the review's own suggested-fixes list only cites `memory.oom.group=1` for this finding, and story 8's acceptance covers only the oom-group and refusal-reporting parts — whether the cpu.max documentation point was consciously dropped or simply not separately tracked, I could not tell from the story body alone; this reads as ambiguous rather than a clear narrowing, so it is not raised as a finding.\n\n```findings\n[]\n```\n","status":"active","tags":["critic","review"],"title":"Scope critic (aep-planning:plan-critic-scope), round 1"},"entity_type":"aep.review-result/v1","locator":"ep://planning/store/review-result/review-2026-09-03-scope-round-1"},"payload":{"actor":"human:timo","at":1788484223630,"causation":"cmd-new-review-result-review-2026-09-03-scope-round-1","change":{"change":"created","status":"active"},"correlation":"protocol-artifact-new","event_id":"review-result:review-2026-09-03-scope-round-1@1#0~85dd641db06e0ee7","executor":null,"recorded_at":"2026-09-04T01:10:23Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-scope-round-1","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000447","target":"01MEM0000000000000025"},"payload":{"actor":"human:timo","at":1788484223641,"causation":"cmd-review-result-review-2026-09-03-scope-round-1-reviews-epic-review-2026-09-03-findings","change":{"change":"related","relation":"reviews","target":"epic:review-2026-09-03-findings"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-scope-round-1@1#0~c4906704723056ac","executor":null,"recorded_at":"2026-09-04T01:10:23Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-scope-round-1","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000447","target":"01MEM0000000000000127"},"payload":{"actor":"human:timo","at":1788484223650,"causation":"cmd-review-result-review-2026-09-03-scope-round-1-reviews-story-line-patch-edits-apply-in-line-order","change":{"change":"related","relation":"reviews","target":"story:line-patch-edits-apply-in-line-order"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-scope-round-1@1#0~742a391871ba544d","executor":null,"recorded_at":"2026-09-04T01:10:23Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-scope-round-1","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000447","target":"01MEM0000000000000073"},"payload":{"actor":"human:timo","at":1788484223666,"causation":"cmd-review-result-review-2026-09-03-scope-round-1-reviews-story-directory-listing-survives-special-entries","change":{"change":"related","relation":"reviews","target":"story:directory-listing-survives-special-entries"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-scope-round-1@1#0~070eb92e34d286cf","executor":null,"recorded_at":"2026-09-04T01:10:23Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-scope-round-1","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000447","target":"01MEM0000000000000133"},"payload":{"actor":"human:timo","at":1788484223676,"causation":"cmd-review-result-review-2026-09-03-scope-round-1-reviews-story-metrics-streams-are-bounded","change":{"change":"related","relation":"reviews","target":"story:metrics-streams-are-bounded"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-scope-round-1@1#0~cf79110f879afd5c","executor":null,"recorded_at":"2026-09-04T01:10:23Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-scope-round-1","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"},{"reviews":"story:upgraded-connections-keep-their-permit"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000447","target":"01MEM0000000000000211"},"payload":{"actor":"human:timo","at":1788484223685,"causation":"cmd-review-result-review-2026-09-03-scope-round-1-reviews-story-upgraded-connections-keep-their-permit","change":{"change":"related","relation":"reviews","target":"story:upgraded-connections-keep-their-permit"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-scope-round-1@1#0~7850ab67e7d8fced","executor":null,"recorded_at":"2026-09-04T01:10:23Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-scope-round-1","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"},{"reviews":"story:upgraded-connections-keep-their-permit"},{"reviews":"story:lease-cleanup-reads-exec-state-only"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000447","target":"01MEM0000000000000121"},"payload":{"actor":"human:timo","at":1788484223697,"causation":"cmd-review-result-review-2026-09-03-scope-round-1-reviews-story-lease-cleanup-reads-exec-state-only","change":{"change":"related","relation":"reviews","target":"story:lease-cleanup-reads-exec-state-only"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-scope-round-1@1#0~61efe318672585bb","executor":null,"recorded_at":"2026-09-04T01:10:23Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-scope-round-1","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"},{"reviews":"story:upgraded-connections-keep-their-permit"},{"reviews":"story:lease-cleanup-reads-exec-state-only"},{"reviews":"story:confined-processes-cannot-nest-user-namespaces"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000447","target":"01MEM0000000000000052"},"payload":{"actor":"human:timo","at":1788484223709,"causation":"cmd-review-result-review-2026-09-03-scope-round-1-reviews-story-confined-processes-cannot-nest-user-namespaces","change":{"change":"related","relation":"reviews","target":"story:confined-processes-cannot-nest-user-namespaces"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-scope-round-1@1#0~d854d61eb466edbe","executor":null,"recorded_at":"2026-09-04T01:10:23Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-scope-round-1","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"},{"reviews":"story:upgraded-connections-keep-their-permit"},{"reviews":"story:lease-cleanup-reads-exec-state-only"},{"reviews":"story:confined-processes-cannot-nest-user-namespaces"},{"reviews":"story:seccomp-denies-af-vsock"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000447","target":"01MEM0000000000000190"},"payload":{"actor":"human:timo","at":1788484223721,"causation":"cmd-review-result-review-2026-09-03-scope-round-1-reviews-story-seccomp-denies-af-vsock","change":{"change":"related","relation":"reviews","target":"story:seccomp-denies-af-vsock"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-scope-round-1@1#0~35f6f1df4b4ddf07","executor":null,"recorded_at":"2026-09-04T01:10:23Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-scope-round-1","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"},{"reviews":"story:upgraded-connections-keep-their-permit"},{"reviews":"story:lease-cleanup-reads-exec-state-only"},{"reviews":"story:confined-processes-cannot-nest-user-namespaces"},{"reviews":"story:seccomp-denies-af-vsock"},{"reviews":"story:exec-oom-kills-the-whole-tree"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000447","target":"01MEM0000000000000091"},"payload":{"actor":"human:timo","at":1788484223730,"causation":"cmd-review-result-review-2026-09-03-scope-round-1-reviews-story-exec-oom-kills-the-whole-tree","change":{"change":"related","relation":"reviews","target":"story:exec-oom-kills-the-whole-tree"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-scope-round-1@1#0~36dfb2b974cabfd3","executor":null,"recorded_at":"2026-09-04T01:10:23Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-scope-round-1","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"},{"reviews":"story:upgraded-connections-keep-their-permit"},{"reviews":"story:lease-cleanup-reads-exec-state-only"},{"reviews":"story:confined-processes-cannot-nest-user-namespaces"},{"reviews":"story:seccomp-denies-af-vsock"},{"reviews":"story:exec-oom-kills-the-whole-tree"},{"reviews":"story:session-attachment-lifetime-is-an-accepted-decision"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000447","target":"01MEM0000000000000196"},"payload":{"actor":"human:timo","at":1788484223741,"causation":"cmd-review-result-review-2026-09-03-scope-round-1-reviews-story-session-attachment-lifetime-is-an-accepted-decision","change":{"change":"related","relation":"reviews","target":"story:session-attachment-lifetime-is-an-accepted-decision"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-scope-round-1@1#0~020affdf2de85631","executor":null,"recorded_at":"2026-09-04T01:10:23Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-scope-round-1","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"},{"reviews":"story:upgraded-connections-keep-their-permit"},{"reviews":"story:lease-cleanup-reads-exec-state-only"},{"reviews":"story:confined-processes-cannot-nest-user-namespaces"},{"reviews":"story:seccomp-denies-af-vsock"},{"reviews":"story:exec-oom-kills-the-whole-tree"},{"reviews":"story:session-attachment-lifetime-is-an-accepted-decision"},{"reviews":"story:hosted-admission-reuses-identity-authority"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000447","target":"01MEM0000000000000109"},"payload":{"actor":"human:timo","at":1788484223758,"causation":"cmd-review-result-review-2026-09-03-scope-round-1-reviews-story-hosted-admission-reuses-identity-authority","change":{"change":"related","relation":"reviews","target":"story:hosted-admission-reuses-identity-authority"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-scope-round-1@1#0~fc6dfb29d7dc3b09","executor":null,"recorded_at":"2026-09-04T01:10:23Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-scope-round-1","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"},{"reviews":"story:upgraded-connections-keep-their-permit"},{"reviews":"story:lease-cleanup-reads-exec-state-only"},{"reviews":"story:confined-processes-cannot-nest-user-namespaces"},{"reviews":"story:seccomp-denies-af-vsock"},{"reviews":"story:exec-oom-kills-the-whole-tree"},{"reviews":"story:session-attachment-lifetime-is-an-accepted-decision"},{"reviews":"story:hosted-admission-reuses-identity-authority"},{"reviews":"story:daemon-image-serves-exec-or-says-it-cannot"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000447","target":"01MEM0000000000000067"},"payload":{"actor":"human:timo","at":1788484223770,"causation":"cmd-review-result-review-2026-09-03-scope-round-1-reviews-story-daemon-image-serves-exec-or-says-it-cannot","change":{"change":"related","relation":"reviews","target":"story:daemon-image-serves-exec-or-says-it-cannot"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-scope-round-1@1#0~641099254c24d1f6","executor":null,"recorded_at":"2026-09-04T01:10:23Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-scope-round-1","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"},{"reviews":"story:upgraded-connections-keep-their-permit"},{"reviews":"story:lease-cleanup-reads-exec-state-only"},{"reviews":"story:confined-processes-cannot-nest-user-namespaces"},{"reviews":"story:seccomp-denies-af-vsock"},{"reviews":"story:exec-oom-kills-the-whole-tree"},{"reviews":"story:session-attachment-lifetime-is-an-accepted-decision"},{"reviews":"story:hosted-admission-reuses-identity-authority"},{"reviews":"story:daemon-image-serves-exec-or-says-it-cannot"},{"reviews":"story:backend-recheck-hashes-only-on-change"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000447","target":"01MEM0000000000000043"},"payload":{"actor":"human:timo","at":1788484223782,"causation":"cmd-review-result-review-2026-09-03-scope-round-1-reviews-story-backend-recheck-hashes-only-on-change","change":{"change":"related","relation":"reviews","target":"story:backend-recheck-hashes-only-on-change"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-scope-round-1@1#0~4e882915d9c584e8","executor":null,"recorded_at":"2026-09-04T01:10:23Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-scope-round-1","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"},{"reviews":"story:upgraded-connections-keep-their-permit"},{"reviews":"story:lease-cleanup-reads-exec-state-only"},{"reviews":"story:confined-processes-cannot-nest-user-namespaces"},{"reviews":"story:seccomp-denies-af-vsock"},{"reviews":"story:exec-oom-kills-the-whole-tree"},{"reviews":"story:session-attachment-lifetime-is-an-accepted-decision"},{"reviews":"story:hosted-admission-reuses-identity-authority"},{"reviews":"story:daemon-image-serves-exec-or-says-it-cannot"},{"reviews":"story:backend-recheck-hashes-only-on-change"},{"reviews":"story:events-table-has-one-index"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000447","target":"01MEM0000000000000088"},"payload":{"actor":"human:timo","at":1788484223795,"causation":"cmd-review-result-review-2026-09-03-scope-round-1-reviews-story-events-table-has-one-index","change":{"change":"related","relation":"reviews","target":"story:events-table-has-one-index"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-scope-round-1@1#0~cfb8b1b0adc8e4e9","executor":null,"recorded_at":"2026-09-04T01:10:23Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-scope-round-1","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"},{"reviews":"story:upgraded-connections-keep-their-permit"},{"reviews":"story:lease-cleanup-reads-exec-state-only"},{"reviews":"story:confined-processes-cannot-nest-user-namespaces"},{"reviews":"story:seccomp-denies-af-vsock"},{"reviews":"story:exec-oom-kills-the-whole-tree"},{"reviews":"story:session-attachment-lifetime-is-an-accepted-decision"},{"reviews":"story:hosted-admission-reuses-identity-authority"},{"reviews":"story:daemon-image-serves-exec-or-says-it-cannot"},{"reviews":"story:backend-recheck-hashes-only-on-change"},{"reviews":"story:events-table-has-one-index"},{"reviews":"story:unattached-claimed-session-is-contained"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000447","target":"01MEM0000000000000208"},"payload":{"actor":"human:timo","at":1788484223805,"causation":"cmd-review-result-review-2026-09-03-scope-round-1-reviews-story-unattached-claimed-session-is-contained","change":{"change":"related","relation":"reviews","target":"story:unattached-claimed-session-is-contained"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-scope-round-1@1#0~8750125ed2f88923","executor":null,"recorded_at":"2026-09-04T01:10:23Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-parallel-safety-round-1","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"active","changed":{"body":"Now I have exact citations. Let me finalize the verdict.\n\n**needs-revision**\n\nstory:backend-recheck-hashes-only-on-change — this story and four siblings (story:confined-processes-cannot-nest-user-namespaces, story:daemon-image-serves-exec-or-says-it-cannot, story:exec-oom-kills-the-whole-tree, story:seccomp-denies-af-vsock) all declare `crates/substrate-host/src/probe.rs` as cited scope, three of them also share `crates/substrate-host/src/process.rs`, and none of the five bodies or relations names another — `aep artifact waves` (10 of its 15 reported collisions are this cluster); .engineering/planning/story/backend-recheck-hashes-only-on-change.md:14,16\n\nstory:line-patch-edits-apply-in-line-order — this story and story:directory-listing-survives-special-entries both cite `crates/substrate-host/src/fs.rs` as scope and neither body mentions the other — .engineering/planning/story/line-patch-edits-apply-in-line-order.md:14\n\nstory:metrics-streams-are-bounded — this story and story:lease-cleanup-reads-exec-state-only both cite `crates/substrate-daemon/src/app/service.rs` as scope and neither body mentions the other — .engineering/planning/story/metrics-streams-are-bounded.md:16\n\nWhat I read: all 14 stories under `decomposes: epic:review-2026-09-03-findings` (`aep artifact list --format json`), each read with `aep artifact show <id>`, cross-checked against raw frontmatter (`grep -n path:` on each story file), and against `aep artifact waves`, which independently derives the same 15 file-level collisions across 12 pairs and assigns 5 waves to keep them apart. Surface established: 14 cited, 0 inferred, 0 unplaceable — every one of the 14 has a declared `scope` block and no story appears in the tool's own `unassessed` list.\n\nWhat I could not establish: none within my lane. One item I checked and ruled out — story:session-attachment-lifetime-is-an-accepted-decision cites `sessions.rs` in its context prose but scopes only an ADR and a design doc; its acceptance is doc-only, so it does not actually collide with story:unattached-claimed-session-is-contained (which does scope `app/sessions.rs`) — this is correct as drafted, not a finding. Whether the scope frontmatter's \"cited\" label for `service.rs` on story:metrics-streams-are-bounded is itself accurate (the body never names that path in prose, only in the frontmatter) is a scope/acceptance-lane question, not mine — noted as out of my lane, does not affect my verdict. Sequencing the 5-way probe.rs/process.rs cluster and the two 2-way pairs is the operator's call, not mine to prescribe.\n\n```findings\n- file: .engineering/planning/story/backend-recheck-hashes-only-on-change.md\n  line: 14\n  category: parallel-safety\n  severity: blocker\n  verdict: needs-revision\n  origin: introduced\n  message: this story and four siblings (story:confined-processes-cannot-nest-user-namespaces, story:daemon-image-serves-exec-or-says-it-cannot, story:exec-oom-kills-the-whole-tree, story:seccomp-denies-af-vsock) all declare crates/substrate-host/src/probe.rs as cited scope, three of them also share crates/substrate-host/src/process.rs, and none of the five bodies or relations names another (cited, all five scope blocks; corroborated by `aep artifact waves`)\n- file: .engineering/planning/story/line-patch-edits-apply-in-line-order.md\n  line: 14\n  category: parallel-safety\n  severity: blocker\n  verdict: needs-revision\n  origin: introduced\n  message: this story and story:directory-listing-survives-special-entries both cite crates/substrate-host/src/fs.rs as scope and neither body mentions the other (cited, both scope blocks; corroborated by `aep artifact waves`)\n- file: .engineering/planning/story/metrics-streams-are-bounded.md\n  line: 16\n  category: parallel-safety\n  severity: blocker\n  verdict: needs-revision\n  origin: introduced\n  message: this story and story:lease-cleanup-reads-exec-state-only both cite crates/substrate-daemon/src/app/service.rs as scope and neither body mentions the other (cited, both scope blocks; corroborated by `aep artifact waves`)\n```\n","tags":["critic","review"],"title":"Parallel-safety critic (aep-planning:plan-critic-parallel-safety), round 1"},"args":{"command":"create-entity","data":{"body":"Now I have exact citations. Let me finalize the verdict.\n\n**needs-revision**\n\nstory:backend-recheck-hashes-only-on-change — this story and four siblings (story:confined-processes-cannot-nest-user-namespaces, story:daemon-image-serves-exec-or-says-it-cannot, story:exec-oom-kills-the-whole-tree, story:seccomp-denies-af-vsock) all declare `crates/substrate-host/src/probe.rs` as cited scope, three of them also share `crates/substrate-host/src/process.rs`, and none of the five bodies or relations names another — `aep artifact waves` (10 of its 15 reported collisions are this cluster); .engineering/planning/story/backend-recheck-hashes-only-on-change.md:14,16\n\nstory:line-patch-edits-apply-in-line-order — this story and story:directory-listing-survives-special-entries both cite `crates/substrate-host/src/fs.rs` as scope and neither body mentions the other — .engineering/planning/story/line-patch-edits-apply-in-line-order.md:14\n\nstory:metrics-streams-are-bounded — this story and story:lease-cleanup-reads-exec-state-only both cite `crates/substrate-daemon/src/app/service.rs` as scope and neither body mentions the other — .engineering/planning/story/metrics-streams-are-bounded.md:16\n\nWhat I read: all 14 stories under `decomposes: epic:review-2026-09-03-findings` (`aep artifact list --format json`), each read with `aep artifact show <id>`, cross-checked against raw frontmatter (`grep -n path:` on each story file), and against `aep artifact waves`, which independently derives the same 15 file-level collisions across 12 pairs and assigns 5 waves to keep them apart. Surface established: 14 cited, 0 inferred, 0 unplaceable — every one of the 14 has a declared `scope` block and no story appears in the tool's own `unassessed` list.\n\nWhat I could not establish: none within my lane. One item I checked and ruled out — story:session-attachment-lifetime-is-an-accepted-decision cites `sessions.rs` in its context prose but scopes only an ADR and a design doc; its acceptance is doc-only, so it does not actually collide with story:unattached-claimed-session-is-contained (which does scope `app/sessions.rs`) — this is correct as drafted, not a finding. Whether the scope frontmatter's \"cited\" label for `service.rs` on story:metrics-streams-are-bounded is itself accurate (the body never names that path in prose, only in the frontmatter) is a scope/acceptance-lane question, not mine — noted as out of my lane, does not affect my verdict. Sequencing the 5-way probe.rs/process.rs cluster and the two 2-way pairs is the operator's call, not mine to prescribe.\n\n```findings\n- file: .engineering/planning/story/backend-recheck-hashes-only-on-change.md\n  line: 14\n  category: parallel-safety\n  severity: blocker\n  verdict: needs-revision\n  origin: introduced\n  message: this story and four siblings (story:confined-processes-cannot-nest-user-namespaces, story:daemon-image-serves-exec-or-says-it-cannot, story:exec-oom-kills-the-whole-tree, story:seccomp-denies-af-vsock) all declare crates/substrate-host/src/probe.rs as cited scope, three of them also share crates/substrate-host/src/process.rs, and none of the five bodies or relations names another (cited, all five scope blocks; corroborated by `aep artifact waves`)\n- file: .engineering/planning/story/line-patch-edits-apply-in-line-order.md\n  line: 14\n  category: parallel-safety\n  severity: blocker\n  verdict: needs-revision\n  origin: introduced\n  message: this story and story:directory-listing-survives-special-entries both cite crates/substrate-host/src/fs.rs as scope and neither body mentions the other (cited, both scope blocks; corroborated by `aep artifact waves`)\n- file: .engineering/planning/story/metrics-streams-are-bounded.md\n  line: 16\n  category: parallel-safety\n  severity: blocker\n  verdict: needs-revision\n  origin: introduced\n  message: this story and story:lease-cleanup-reads-exec-state-only both cite crates/substrate-daemon/src/app/service.rs as scope and neither body mentions the other (cited, both scope blocks; corroborated by `aep artifact waves`)\n```\n","status":"active","tags":["critic","review"],"title":"Parallel-safety critic (aep-planning:plan-critic-parallel-safety), round 1"},"entity_type":"aep.review-result/v1","locator":"ep://planning/store/review-result/review-2026-09-03-parallel-safety-round-1"},"payload":{"actor":"human:timo","at":1788484299231,"causation":"cmd-new-review-result-review-2026-09-03-parallel-safety-round-1","change":{"change":"created","status":"active"},"correlation":"protocol-artifact-new","event_id":"review-result:review-2026-09-03-parallel-safety-round-1@1#0~8165acd772ee8f61","executor":null,"recorded_at":"2026-09-04T01:11:39Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-parallel-safety-round-1","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000480","target":"01MEM0000000000000025"},"payload":{"actor":"human:timo","at":1788484299248,"causation":"cmd-review-result-review-2026-09-03-parallel-safety-round-1-reviews-epic-review-2026-09-03-findings","change":{"change":"related","relation":"reviews","target":"epic:review-2026-09-03-findings"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-parallel-safety-round-1@1#0~760db219aecc0437","executor":null,"recorded_at":"2026-09-04T01:11:39Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-parallel-safety-round-1","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000480","target":"01MEM0000000000000130"},"payload":{"actor":"human:timo","at":1788484299256,"causation":"cmd-review-result-review-2026-09-03-parallel-safety-round-1-reviews-story-line-patch-edits-apply-in-line-order","change":{"change":"related","relation":"reviews","target":"story:line-patch-edits-apply-in-line-order"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-parallel-safety-round-1@1#0~8521870ea8197152","executor":null,"recorded_at":"2026-09-04T01:11:39Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-parallel-safety-round-1","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000480","target":"01MEM0000000000000076"},"payload":{"actor":"human:timo","at":1788484299263,"causation":"cmd-review-result-review-2026-09-03-parallel-safety-round-1-reviews-story-directory-listing-survives-special-entries","change":{"change":"related","relation":"reviews","target":"story:directory-listing-survives-special-entries"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-parallel-safety-round-1@1#0~14a4c731fb2b837b","executor":null,"recorded_at":"2026-09-04T01:11:39Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-parallel-safety-round-1","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000480","target":"01MEM0000000000000136"},"payload":{"actor":"human:timo","at":1788484299271,"causation":"cmd-review-result-review-2026-09-03-parallel-safety-round-1-reviews-story-metrics-streams-are-bounded","change":{"change":"related","relation":"reviews","target":"story:metrics-streams-are-bounded"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-parallel-safety-round-1@1#0~b927790ec5940ee0","executor":null,"recorded_at":"2026-09-04T01:11:39Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-parallel-safety-round-1","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"},{"reviews":"story:upgraded-connections-keep-their-permit"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000480","target":"01MEM0000000000000214"},"payload":{"actor":"human:timo","at":1788484299279,"causation":"cmd-review-result-review-2026-09-03-parallel-safety-round-1-reviews-story-upgraded-connections-keep-their-permit","change":{"change":"related","relation":"reviews","target":"story:upgraded-connections-keep-their-permit"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-parallel-safety-round-1@1#0~397235617724fc9d","executor":null,"recorded_at":"2026-09-04T01:11:39Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-parallel-safety-round-1","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"},{"reviews":"story:upgraded-connections-keep-their-permit"},{"reviews":"story:lease-cleanup-reads-exec-state-only"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000480","target":"01MEM0000000000000124"},"payload":{"actor":"human:timo","at":1788484299286,"causation":"cmd-review-result-review-2026-09-03-parallel-safety-round-1-reviews-story-lease-cleanup-reads-exec-state-only","change":{"change":"related","relation":"reviews","target":"story:lease-cleanup-reads-exec-state-only"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-parallel-safety-round-1@1#0~ad516905a143a423","executor":null,"recorded_at":"2026-09-04T01:11:39Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-parallel-safety-round-1","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"},{"reviews":"story:upgraded-connections-keep-their-permit"},{"reviews":"story:lease-cleanup-reads-exec-state-only"},{"reviews":"story:confined-processes-cannot-nest-user-namespaces"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000480","target":"01MEM0000000000000055"},"payload":{"actor":"human:timo","at":1788484299294,"causation":"cmd-review-result-review-2026-09-03-parallel-safety-round-1-reviews-story-confined-processes-cannot-nest-user-namespaces","change":{"change":"related","relation":"reviews","target":"story:confined-processes-cannot-nest-user-namespaces"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-parallel-safety-round-1@1#0~a753b24367b46268","executor":null,"recorded_at":"2026-09-04T01:11:39Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-parallel-safety-round-1","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"},{"reviews":"story:upgraded-connections-keep-their-permit"},{"reviews":"story:lease-cleanup-reads-exec-state-only"},{"reviews":"story:confined-processes-cannot-nest-user-namespaces"},{"reviews":"story:seccomp-denies-af-vsock"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000480","target":"01MEM0000000000000193"},"payload":{"actor":"human:timo","at":1788484299302,"causation":"cmd-review-result-review-2026-09-03-parallel-safety-round-1-reviews-story-seccomp-denies-af-vsock","change":{"change":"related","relation":"reviews","target":"story:seccomp-denies-af-vsock"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-parallel-safety-round-1@1#0~eb84b03ec7b01969","executor":null,"recorded_at":"2026-09-04T01:11:39Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-parallel-safety-round-1","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"},{"reviews":"story:upgraded-connections-keep-their-permit"},{"reviews":"story:lease-cleanup-reads-exec-state-only"},{"reviews":"story:confined-processes-cannot-nest-user-namespaces"},{"reviews":"story:seccomp-denies-af-vsock"},{"reviews":"story:exec-oom-kills-the-whole-tree"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000480","target":"01MEM0000000000000094"},"payload":{"actor":"human:timo","at":1788484299310,"causation":"cmd-review-result-review-2026-09-03-parallel-safety-round-1-reviews-story-exec-oom-kills-the-whole-tree","change":{"change":"related","relation":"reviews","target":"story:exec-oom-kills-the-whole-tree"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-parallel-safety-round-1@1#0~d93a996494565acb","executor":null,"recorded_at":"2026-09-04T01:11:39Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-parallel-safety-round-1","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"},{"reviews":"story:upgraded-connections-keep-their-permit"},{"reviews":"story:lease-cleanup-reads-exec-state-only"},{"reviews":"story:confined-processes-cannot-nest-user-namespaces"},{"reviews":"story:seccomp-denies-af-vsock"},{"reviews":"story:exec-oom-kills-the-whole-tree"},{"reviews":"story:session-attachment-lifetime-is-an-accepted-decision"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000480","target":"01MEM0000000000000199"},"payload":{"actor":"human:timo","at":1788484299319,"causation":"cmd-review-result-review-2026-09-03-parallel-safety-round-1-reviews-story-session-attachment-lifetime-is-an-accepted-decision","change":{"change":"related","relation":"reviews","target":"story:session-attachment-lifetime-is-an-accepted-decision"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-parallel-safety-round-1@1#0~b6a4b63ea97bca83","executor":null,"recorded_at":"2026-09-04T01:11:39Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-parallel-safety-round-1","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"},{"reviews":"story:upgraded-connections-keep-their-permit"},{"reviews":"story:lease-cleanup-reads-exec-state-only"},{"reviews":"story:confined-processes-cannot-nest-user-namespaces"},{"reviews":"story:seccomp-denies-af-vsock"},{"reviews":"story:exec-oom-kills-the-whole-tree"},{"reviews":"story:session-attachment-lifetime-is-an-accepted-decision"},{"reviews":"story:hosted-admission-reuses-identity-authority"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000480","target":"01MEM0000000000000112"},"payload":{"actor":"human:timo","at":1788484299335,"causation":"cmd-review-result-review-2026-09-03-parallel-safety-round-1-reviews-story-hosted-admission-reuses-identity-authority","change":{"change":"related","relation":"reviews","target":"story:hosted-admission-reuses-identity-authority"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-parallel-safety-round-1@1#0~08a048fcb7245aca","executor":null,"recorded_at":"2026-09-04T01:11:39Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-parallel-safety-round-1","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"},{"reviews":"story:upgraded-connections-keep-their-permit"},{"reviews":"story:lease-cleanup-reads-exec-state-only"},{"reviews":"story:confined-processes-cannot-nest-user-namespaces"},{"reviews":"story:seccomp-denies-af-vsock"},{"reviews":"story:exec-oom-kills-the-whole-tree"},{"reviews":"story:session-attachment-lifetime-is-an-accepted-decision"},{"reviews":"story:hosted-admission-reuses-identity-authority"},{"reviews":"story:daemon-image-serves-exec-or-says-it-cannot"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000480","target":"01MEM0000000000000070"},"payload":{"actor":"human:timo","at":1788484299355,"causation":"cmd-review-result-review-2026-09-03-parallel-safety-round-1-reviews-story-daemon-image-serves-exec-or-says-it-cannot","change":{"change":"related","relation":"reviews","target":"story:daemon-image-serves-exec-or-says-it-cannot"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-parallel-safety-round-1@1#0~26df1d3205c0520d","executor":null,"recorded_at":"2026-09-04T01:11:39Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-parallel-safety-round-1","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"},{"reviews":"story:upgraded-connections-keep-their-permit"},{"reviews":"story:lease-cleanup-reads-exec-state-only"},{"reviews":"story:confined-processes-cannot-nest-user-namespaces"},{"reviews":"story:seccomp-denies-af-vsock"},{"reviews":"story:exec-oom-kills-the-whole-tree"},{"reviews":"story:session-attachment-lifetime-is-an-accepted-decision"},{"reviews":"story:hosted-admission-reuses-identity-authority"},{"reviews":"story:daemon-image-serves-exec-or-says-it-cannot"},{"reviews":"story:backend-recheck-hashes-only-on-change"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000480","target":"01MEM0000000000000046"},"payload":{"actor":"human:timo","at":1788484299371,"causation":"cmd-review-result-review-2026-09-03-parallel-safety-round-1-reviews-story-backend-recheck-hashes-only-on-change","change":{"change":"related","relation":"reviews","target":"story:backend-recheck-hashes-only-on-change"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-parallel-safety-round-1@1#0~1f01094c37d7c464","executor":null,"recorded_at":"2026-09-04T01:11:39Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-parallel-safety-round-1","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"},{"reviews":"story:upgraded-connections-keep-their-permit"},{"reviews":"story:lease-cleanup-reads-exec-state-only"},{"reviews":"story:confined-processes-cannot-nest-user-namespaces"},{"reviews":"story:seccomp-denies-af-vsock"},{"reviews":"story:exec-oom-kills-the-whole-tree"},{"reviews":"story:session-attachment-lifetime-is-an-accepted-decision"},{"reviews":"story:hosted-admission-reuses-identity-authority"},{"reviews":"story:daemon-image-serves-exec-or-says-it-cannot"},{"reviews":"story:backend-recheck-hashes-only-on-change"},{"reviews":"story:events-table-has-one-index"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000480","target":"01MEM0000000000000091"},"payload":{"actor":"human:timo","at":1788484299383,"causation":"cmd-review-result-review-2026-09-03-parallel-safety-round-1-reviews-story-events-table-has-one-index","change":{"change":"related","relation":"reviews","target":"story:events-table-has-one-index"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-parallel-safety-round-1@1#0~04986664aceb3ed0","executor":null,"recorded_at":"2026-09-04T01:11:39Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-parallel-safety-round-1","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"},{"reviews":"story:upgraded-connections-keep-their-permit"},{"reviews":"story:lease-cleanup-reads-exec-state-only"},{"reviews":"story:confined-processes-cannot-nest-user-namespaces"},{"reviews":"story:seccomp-denies-af-vsock"},{"reviews":"story:exec-oom-kills-the-whole-tree"},{"reviews":"story:session-attachment-lifetime-is-an-accepted-decision"},{"reviews":"story:hosted-admission-reuses-identity-authority"},{"reviews":"story:daemon-image-serves-exec-or-says-it-cannot"},{"reviews":"story:backend-recheck-hashes-only-on-change"},{"reviews":"story:events-table-has-one-index"},{"reviews":"story:unattached-claimed-session-is-contained"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000480","target":"01MEM0000000000000211"},"payload":{"actor":"human:timo","at":1788484299391,"causation":"cmd-review-result-review-2026-09-03-parallel-safety-round-1-reviews-story-unattached-claimed-session-is-contained","change":{"change":"related","relation":"reviews","target":"story:unattached-claimed-session-is-contained"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-parallel-safety-round-1@1#0~1e8eba6167a8c182","executor":null,"recorded_at":"2026-09-04T01:11:39Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-design-round-1","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"active","changed":{"body":"approve\n\nWait — reconsidering, my analysis found a real finding, so the verdict is `needs-revision`.\n\n**Read:** the epic and all 14 stories bearing `decomposes: epic:review-2026-09-03-findings` (`aep artifact show` on each of the 15 ids), `aep artifact relations`, `aep artifact graph` (full dump, 154 lines), and `aep artifact validate`.\n\n**Verdict:** needs-revision\n\nstory:unattached-claimed-session-is-contained — its Context and Notes describe the same hyper connection/upgrade hand-off gap that story:upgraded-connections-keep-their-permit's Context names (\"hyper 1.11.1 … resolves an upgradeable connection future when it hands the socket to the upgrade, so the permit is released the moment a WebSocket upgrade succeeds\", story/upgraded-connections-keep-their-permit.md:22-24), and both stories independently propose the identical remedy at that seam (\"move it into the upgrade task\", story/upgraded-connections-keep-their-permit.md:37-38, vs. \"claim inside the upgrade task\", story/unattached-claimed-session-is-contained.md:35), yet no edge records that the two touch one seam — `aep artifact graph` — reasonstory:upgraded-connections-keep-their-permit states every listener, including the session-attach listener, is wrapped by the same `enforce_connection_lifetime` (story/upgraded-connections-keep-their-permit.md:19-20), and story:unattached-claimed-session-is-contained's own bug (\"If the client drops between the `101` and the upgrade, the `on_upgrade` closure never runs, the permit is dropped\", story/unattached-claimed-session-is-contained.md:21-22) is the identical timing window described from the other side.\n\n**What I read:** 1 epic + 14 stories, `aep artifact relations` (13 relation kinds), `aep artifact graph` (full store, 154 lines — walked all edges, not just ones touching the 14 ids, to check for cycles or inbound edges from outside the set), `aep artifact validate`. Within the 14-story set every item declares exactly one edge (`decomposes` → the epic); no `depends_on` among them, so there is no cycle and no serialising chain in the declared graph. The only inbound edges to the 14 beyond the epic are 14 `reviews` edges from `review-result:review-2026-09-03-scope-round-1`, which is a review record, not a work-order edge.\n\n**What I could not establish:** whether a fix to the shared upgrade-task seam (finding above) would actually require sequencing rather than just cross-reading — I read this as an ordering risk worth a `depends_on` or at minimum `informed_by` edge, but cannot rule out that the two remedies land in disjoint code paths once written. Also out of my lane: five stories (backend-recheck-hashes-only-on-change, confined-processes-cannot-nest-user-namespaces, daemon-image-serves-exec-or-says-it-cannot, exec-oom-kills-the-whole-tree, seccomp-denies-af-vsock) all cite `probe.rs` and at least two (confined-processes, seccomp-denies-af-vsock) both extend what reads like the same sentinel-case list — each addition is self-sufficient on its own, so I did not treat it as a split abstraction, but whether five stories may be worked on that file concurrently is plan-critic-parallel-safety's question, not mine.\n\n```findings\n- file: story/unattached-claimed-session-is-contained.md\n  line: 35\n  category: design\n  severity: blocker\n  verdict: needs-revision\n  origin: introduced\n  message: its Context and Notes describe the same hyper connection/upgrade hand-off gap that story:upgraded-connections-keep-their-permit's Context names, and both stories independently propose the identical remedy (\"move it into the upgrade task\") at that seam, yet no edge records that the two touch one seam\n```\n","tags":["critic","review"],"title":"Design critic (aep-planning:plan-critic-design), round 1"},"args":{"command":"create-entity","data":{"body":"approve\n\nWait — reconsidering, my analysis found a real finding, so the verdict is `needs-revision`.\n\n**Read:** the epic and all 14 stories bearing `decomposes: epic:review-2026-09-03-findings` (`aep artifact show` on each of the 15 ids), `aep artifact relations`, `aep artifact graph` (full dump, 154 lines), and `aep artifact validate`.\n\n**Verdict:** needs-revision\n\nstory:unattached-claimed-session-is-contained — its Context and Notes describe the same hyper connection/upgrade hand-off gap that story:upgraded-connections-keep-their-permit's Context names (\"hyper 1.11.1 … resolves an upgradeable connection future when it hands the socket to the upgrade, so the permit is released the moment a WebSocket upgrade succeeds\", story/upgraded-connections-keep-their-permit.md:22-24), and both stories independently propose the identical remedy at that seam (\"move it into the upgrade task\", story/upgraded-connections-keep-their-permit.md:37-38, vs. \"claim inside the upgrade task\", story/unattached-claimed-session-is-contained.md:35), yet no edge records that the two touch one seam — `aep artifact graph` — reasonstory:upgraded-connections-keep-their-permit states every listener, including the session-attach listener, is wrapped by the same `enforce_connection_lifetime` (story/upgraded-connections-keep-their-permit.md:19-20), and story:unattached-claimed-session-is-contained's own bug (\"If the client drops between the `101` and the upgrade, the `on_upgrade` closure never runs, the permit is dropped\", story/unattached-claimed-session-is-contained.md:21-22) is the identical timing window described from the other side.\n\n**What I read:** 1 epic + 14 stories, `aep artifact relations` (13 relation kinds), `aep artifact graph` (full store, 154 lines — walked all edges, not just ones touching the 14 ids, to check for cycles or inbound edges from outside the set), `aep artifact validate`. Within the 14-story set every item declares exactly one edge (`decomposes` → the epic); no `depends_on` among them, so there is no cycle and no serialising chain in the declared graph. The only inbound edges to the 14 beyond the epic are 14 `reviews` edges from `review-result:review-2026-09-03-scope-round-1`, which is a review record, not a work-order edge.\n\n**What I could not establish:** whether a fix to the shared upgrade-task seam (finding above) would actually require sequencing rather than just cross-reading — I read this as an ordering risk worth a `depends_on` or at minimum `informed_by` edge, but cannot rule out that the two remedies land in disjoint code paths once written. Also out of my lane: five stories (backend-recheck-hashes-only-on-change, confined-processes-cannot-nest-user-namespaces, daemon-image-serves-exec-or-says-it-cannot, exec-oom-kills-the-whole-tree, seccomp-denies-af-vsock) all cite `probe.rs` and at least two (confined-processes, seccomp-denies-af-vsock) both extend what reads like the same sentinel-case list — each addition is self-sufficient on its own, so I did not treat it as a split abstraction, but whether five stories may be worked on that file concurrently is plan-critic-parallel-safety's question, not mine.\n\n```findings\n- file: story/unattached-claimed-session-is-contained.md\n  line: 35\n  category: design\n  severity: blocker\n  verdict: needs-revision\n  origin: introduced\n  message: its Context and Notes describe the same hyper connection/upgrade hand-off gap that story:upgraded-connections-keep-their-permit's Context names, and both stories independently propose the identical remedy (\"move it into the upgrade task\") at that seam, yet no edge records that the two touch one seam\n```\n","status":"active","tags":["critic","review"],"title":"Design critic (aep-planning:plan-critic-design), round 1"},"entity_type":"aep.review-result/v1","locator":"ep://planning/store/review-result/review-2026-09-03-design-round-1"},"payload":{"actor":"human:timo","at":1788484324649,"causation":"cmd-new-review-result-review-2026-09-03-design-round-1","change":{"change":"created","status":"active"},"correlation":"protocol-artifact-new","event_id":"review-result:review-2026-09-03-design-round-1@1#0~46340218e5fe269a","executor":null,"recorded_at":"2026-09-04T01:12:04Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-design-round-1","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000513","target":"01MEM0000000000000025"},"payload":{"actor":"human:timo","at":1788484324658,"causation":"cmd-review-result-review-2026-09-03-design-round-1-reviews-epic-review-2026-09-03-findings","change":{"change":"related","relation":"reviews","target":"epic:review-2026-09-03-findings"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-design-round-1@1#0~1335448e9570983a","executor":null,"recorded_at":"2026-09-04T01:12:04Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-design-round-1","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000513","target":"01MEM0000000000000133"},"payload":{"actor":"human:timo","at":1788484324665,"causation":"cmd-review-result-review-2026-09-03-design-round-1-reviews-story-line-patch-edits-apply-in-line-order","change":{"change":"related","relation":"reviews","target":"story:line-patch-edits-apply-in-line-order"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-design-round-1@1#0~112759d21f3194ce","executor":null,"recorded_at":"2026-09-04T01:12:04Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-design-round-1","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000513","target":"01MEM0000000000000079"},"payload":{"actor":"human:timo","at":1788484324678,"causation":"cmd-review-result-review-2026-09-03-design-round-1-reviews-story-directory-listing-survives-special-entries","change":{"change":"related","relation":"reviews","target":"story:directory-listing-survives-special-entries"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-design-round-1@1#0~2a848876b017ba23","executor":null,"recorded_at":"2026-09-04T01:12:04Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-design-round-1","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000513","target":"01MEM0000000000000139"},"payload":{"actor":"human:timo","at":1788484324686,"causation":"cmd-review-result-review-2026-09-03-design-round-1-reviews-story-metrics-streams-are-bounded","change":{"change":"related","relation":"reviews","target":"story:metrics-streams-are-bounded"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-design-round-1@1#0~67d2efd2504bb2b0","executor":null,"recorded_at":"2026-09-04T01:12:04Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-design-round-1","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"},{"reviews":"story:upgraded-connections-keep-their-permit"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000513","target":"01MEM0000000000000217"},"payload":{"actor":"human:timo","at":1788484324693,"causation":"cmd-review-result-review-2026-09-03-design-round-1-reviews-story-upgraded-connections-keep-their-permit","change":{"change":"related","relation":"reviews","target":"story:upgraded-connections-keep-their-permit"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-design-round-1@1#0~fdcfe63d23af293d","executor":null,"recorded_at":"2026-09-04T01:12:04Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-design-round-1","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"},{"reviews":"story:upgraded-connections-keep-their-permit"},{"reviews":"story:lease-cleanup-reads-exec-state-only"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000513","target":"01MEM0000000000000127"},"payload":{"actor":"human:timo","at":1788484324701,"causation":"cmd-review-result-review-2026-09-03-design-round-1-reviews-story-lease-cleanup-reads-exec-state-only","change":{"change":"related","relation":"reviews","target":"story:lease-cleanup-reads-exec-state-only"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-design-round-1@1#0~fa49c1db2fd63b0f","executor":null,"recorded_at":"2026-09-04T01:12:04Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-design-round-1","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"},{"reviews":"story:upgraded-connections-keep-their-permit"},{"reviews":"story:lease-cleanup-reads-exec-state-only"},{"reviews":"story:confined-processes-cannot-nest-user-namespaces"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000513","target":"01MEM0000000000000058"},"payload":{"actor":"human:timo","at":1788484324712,"causation":"cmd-review-result-review-2026-09-03-design-round-1-reviews-story-confined-processes-cannot-nest-user-namespaces","change":{"change":"related","relation":"reviews","target":"story:confined-processes-cannot-nest-user-namespaces"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-design-round-1@1#0~12ea11884cea5912","executor":null,"recorded_at":"2026-09-04T01:12:04Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-design-round-1","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"},{"reviews":"story:upgraded-connections-keep-their-permit"},{"reviews":"story:lease-cleanup-reads-exec-state-only"},{"reviews":"story:confined-processes-cannot-nest-user-namespaces"},{"reviews":"story:seccomp-denies-af-vsock"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000513","target":"01MEM0000000000000196"},"payload":{"actor":"human:timo","at":1788484324720,"causation":"cmd-review-result-review-2026-09-03-design-round-1-reviews-story-seccomp-denies-af-vsock","change":{"change":"related","relation":"reviews","target":"story:seccomp-denies-af-vsock"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-design-round-1@1#0~22d89904e3cae88f","executor":null,"recorded_at":"2026-09-04T01:12:04Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-design-round-1","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"},{"reviews":"story:upgraded-connections-keep-their-permit"},{"reviews":"story:lease-cleanup-reads-exec-state-only"},{"reviews":"story:confined-processes-cannot-nest-user-namespaces"},{"reviews":"story:seccomp-denies-af-vsock"},{"reviews":"story:exec-oom-kills-the-whole-tree"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000513","target":"01MEM0000000000000097"},"payload":{"actor":"human:timo","at":1788484324728,"causation":"cmd-review-result-review-2026-09-03-design-round-1-reviews-story-exec-oom-kills-the-whole-tree","change":{"change":"related","relation":"reviews","target":"story:exec-oom-kills-the-whole-tree"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-design-round-1@1#0~f34f9264e6635743","executor":null,"recorded_at":"2026-09-04T01:12:04Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-design-round-1","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"},{"reviews":"story:upgraded-connections-keep-their-permit"},{"reviews":"story:lease-cleanup-reads-exec-state-only"},{"reviews":"story:confined-processes-cannot-nest-user-namespaces"},{"reviews":"story:seccomp-denies-af-vsock"},{"reviews":"story:exec-oom-kills-the-whole-tree"},{"reviews":"story:session-attachment-lifetime-is-an-accepted-decision"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000513","target":"01MEM0000000000000202"},"payload":{"actor":"human:timo","at":1788484324736,"causation":"cmd-review-result-review-2026-09-03-design-round-1-reviews-story-session-attachment-lifetime-is-an-accepted-decision","change":{"change":"related","relation":"reviews","target":"story:session-attachment-lifetime-is-an-accepted-decision"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-design-round-1@1#0~a6d91536aa0ebe9b","executor":null,"recorded_at":"2026-09-04T01:12:04Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-design-round-1","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"},{"reviews":"story:upgraded-connections-keep-their-permit"},{"reviews":"story:lease-cleanup-reads-exec-state-only"},{"reviews":"story:confined-processes-cannot-nest-user-namespaces"},{"reviews":"story:seccomp-denies-af-vsock"},{"reviews":"story:exec-oom-kills-the-whole-tree"},{"reviews":"story:session-attachment-lifetime-is-an-accepted-decision"},{"reviews":"story:hosted-admission-reuses-identity-authority"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000513","target":"01MEM0000000000000115"},"payload":{"actor":"human:timo","at":1788484324744,"causation":"cmd-review-result-review-2026-09-03-design-round-1-reviews-story-hosted-admission-reuses-identity-authority","change":{"change":"related","relation":"reviews","target":"story:hosted-admission-reuses-identity-authority"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-design-round-1@1#0~3062e3c2caee1bee","executor":null,"recorded_at":"2026-09-04T01:12:04Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-design-round-1","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"},{"reviews":"story:upgraded-connections-keep-their-permit"},{"reviews":"story:lease-cleanup-reads-exec-state-only"},{"reviews":"story:confined-processes-cannot-nest-user-namespaces"},{"reviews":"story:seccomp-denies-af-vsock"},{"reviews":"story:exec-oom-kills-the-whole-tree"},{"reviews":"story:session-attachment-lifetime-is-an-accepted-decision"},{"reviews":"story:hosted-admission-reuses-identity-authority"},{"reviews":"story:daemon-image-serves-exec-or-says-it-cannot"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000513","target":"01MEM0000000000000073"},"payload":{"actor":"human:timo","at":1788484324752,"causation":"cmd-review-result-review-2026-09-03-design-round-1-reviews-story-daemon-image-serves-exec-or-says-it-cannot","change":{"change":"related","relation":"reviews","target":"story:daemon-image-serves-exec-or-says-it-cannot"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-design-round-1@1#0~5db18276ccda7489","executor":null,"recorded_at":"2026-09-04T01:12:04Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-design-round-1","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"},{"reviews":"story:upgraded-connections-keep-their-permit"},{"reviews":"story:lease-cleanup-reads-exec-state-only"},{"reviews":"story:confined-processes-cannot-nest-user-namespaces"},{"reviews":"story:seccomp-denies-af-vsock"},{"reviews":"story:exec-oom-kills-the-whole-tree"},{"reviews":"story:session-attachment-lifetime-is-an-accepted-decision"},{"reviews":"story:hosted-admission-reuses-identity-authority"},{"reviews":"story:daemon-image-serves-exec-or-says-it-cannot"},{"reviews":"story:backend-recheck-hashes-only-on-change"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000513","target":"01MEM0000000000000049"},"payload":{"actor":"human:timo","at":1788484324759,"causation":"cmd-review-result-review-2026-09-03-design-round-1-reviews-story-backend-recheck-hashes-only-on-change","change":{"change":"related","relation":"reviews","target":"story:backend-recheck-hashes-only-on-change"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-design-round-1@1#0~f035507fac6f8078","executor":null,"recorded_at":"2026-09-04T01:12:04Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-design-round-1","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"},{"reviews":"story:upgraded-connections-keep-their-permit"},{"reviews":"story:lease-cleanup-reads-exec-state-only"},{"reviews":"story:confined-processes-cannot-nest-user-namespaces"},{"reviews":"story:seccomp-denies-af-vsock"},{"reviews":"story:exec-oom-kills-the-whole-tree"},{"reviews":"story:session-attachment-lifetime-is-an-accepted-decision"},{"reviews":"story:hosted-admission-reuses-identity-authority"},{"reviews":"story:daemon-image-serves-exec-or-says-it-cannot"},{"reviews":"story:backend-recheck-hashes-only-on-change"},{"reviews":"story:events-table-has-one-index"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000513","target":"01MEM0000000000000094"},"payload":{"actor":"human:timo","at":1788484324768,"causation":"cmd-review-result-review-2026-09-03-design-round-1-reviews-story-events-table-has-one-index","change":{"change":"related","relation":"reviews","target":"story:events-table-has-one-index"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-design-round-1@1#0~eaa66d64e17beaba","executor":null,"recorded_at":"2026-09-04T01:12:04Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-design-round-1","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"},{"reviews":"story:upgraded-connections-keep-their-permit"},{"reviews":"story:lease-cleanup-reads-exec-state-only"},{"reviews":"story:confined-processes-cannot-nest-user-namespaces"},{"reviews":"story:seccomp-denies-af-vsock"},{"reviews":"story:exec-oom-kills-the-whole-tree"},{"reviews":"story:session-attachment-lifetime-is-an-accepted-decision"},{"reviews":"story:hosted-admission-reuses-identity-authority"},{"reviews":"story:daemon-image-serves-exec-or-says-it-cannot"},{"reviews":"story:backend-recheck-hashes-only-on-change"},{"reviews":"story:events-table-has-one-index"},{"reviews":"story:unattached-claimed-session-is-contained"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000513","target":"01MEM0000000000000214"},"payload":{"actor":"human:timo","at":1788484324775,"causation":"cmd-review-result-review-2026-09-03-design-round-1-reviews-story-unattached-claimed-session-is-contained","change":{"change":"related","relation":"reviews","target":"story:unattached-claimed-session-is-contained"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-design-round-1@1#0~17cc5d3d32672f70","executor":null,"recorded_at":"2026-09-04T01:12:04Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-acceptance-round-1","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"active","changed":{"body":"approve\nneeds-revision\n\nepic:review-2026-09-03-findings — the epic carries no Acceptance or Done-When section (only Outcome, Why Now, Scope, Out of Scope, Risks), so there is no epic-level statement that closes independently of \"all 14 stories implemented\" — .engineering/planning/epic/review-2026-09-03-findings.md (headings at lines 16, 23, 32, 52, 58; no Acceptance heading)\n\nstory:backend-recheck-hashes-only-on-change — the acceptance joins two independent scenarios with \"while\" (unchanged-metadata skips the read; a replaced binary is still refused), so one can pass while the other fails — .engineering/planning/story/backend-recheck-hashes-only-on-change.md:30\n\nstory:confined-processes-cannot-nest-user-namespaces — the acceptance conjoins three separately-failable claims (unshare -U fails on a modern host, the probe passes the new flag, exec facts are withheld on a host that lacks it), and \"observes both\" names only two of the three it just listed — .engineering/planning/story/confined-processes-cannot-nest-user-namespaces.md:37\n\nstory:daemon-image-serves-exec-or-says-it-cannot — each side of the either/or bundles two independent claims (image contents plus the delegated lane running against it; three documents stating the limit plus naming what to add), so a revision can satisfy one half of a branch and not the other — .engineering/planning/story/daemon-image-serves-exec-or-says-it-cannot.md:36\n\nstory:exec-oom-kills-the-whole-tree — the acceptance joins the whole-cgroup kill and the refusal-code label with \"and\", and the story's own Context says the label is derived from a different code path (`record_resource_bound`, gated on `measurements`) than the kill mechanism (`memory.oom.group`), so the two are independently failable — .engineering/planning/story/exec-oom-kills-the-whole-tree.md:34\n\nstory:hosted-admission-reuses-identity-authority — each side of the either/or bundles multiple independent claims (at-most-one resolution, bounded revocation latency, and a capability-document statement on one side; an ADR and a deployment-guide cost statement on the other) — .engineering/planning/story/hosted-admission-reuses-identity-authority.md:33\n\nstory:metrics-streams-are-bounded — the acceptance names three independent mechanisms (per-subject cap/429, lifetime expiry, oversized-message close) and says \"a test proves all three\", so any one of the three can regress without the others noticing — .engineering/planning/story/metrics-streams-are-bounded.md:33\n\nstory:session-attachment-lifetime-is-an-accepted-decision — past the either/or (ADR vs. filed story), a third, unconditional clause (\"the design doc and the published capability document agree with the code\") is joined by a semicolon as an independent claim — .engineering/planning/story/session-attachment-lifetime-is-an-accepted-decision.md:32\n\nWhat I read: the epic and all 14 decomposing stories, 15 artifacts total, via `aep artifact list --format json`, `aep artifact show <id>` for each of the 15, `aep artifact kinds`, `aep artifact lifecycle epic`, `aep artifact lifecycle story`, plus `grep -n` on the store files for line citations. I also read two other draft/proposed epics (`epic:container-driver-entry`, `epic:mcp-test-surface`) and two implemented ones to check this store's epic-acceptance convention before flagging the epic.\n\nWhat I could not establish: whether this store treats an epic's Scope (a numbered list of its stories) as an intentional substitute for a Done-When/Acceptance section — other epics in this store are inconsistent (`epic:release-hardening` and `epic:container-driver-entry` carry \"Done When\"; `epic:mcp-test-surface` carries neither), so I flagged the gap rather than assuming it is accepted convention. Six stories (`directory-listing-survives-special-entries`, `events-table-has-one-index`, `lease-cleanup-reads-exec-state-only`, `line-patch-edits-apply-in-line-order`, `seccomp-denies-af-vsock`, `unattached-claimed-session-is-contained`, `upgraded-connections-keep-their-permit`) read as single, checkable statements and carry no finding from me. Coupling between stories, coverage of the epic's 14-finding register, and any shared-surface risk from stories 6/7's confinement tightening are outside my lane and not reflected in this verdict.\n\n```findings\n- file: .engineering/planning/epic/review-2026-09-03-findings.md\n  line: 16\n  category: acceptance\n  severity: blocker\n  verdict: needs-revision\n  origin: introduced\n  message: the epic carries no Acceptance or Done-When section, so there is no epic-level statement that closes independently of \"all 14 stories implemented\"\n- file: .engineering/planning/story/backend-recheck-hashes-only-on-change.md\n  line: 30\n  category: acceptance\n  severity: blocker\n  verdict: needs-revision\n  origin: introduced\n  message: the acceptance joins two independent scenarios with \"while\" (unchanged-metadata skips the read; a replaced binary is still refused), so one can pass while the other fails\n- file: .engineering/planning/story/confined-processes-cannot-nest-user-namespaces.md\n  line: 37\n  category: acceptance\n  severity: blocker\n  verdict: needs-revision\n  origin: introduced\n  message: the acceptance conjoins three separately-failable claims (unshare -U fails, the probe passes the new flag, exec facts are withheld on an unsupporting host), and \"observes both\" names only two of the three it just listed\n- file: .engineering/planning/story/daemon-image-serves-exec-or-says-it-cannot.md\n  line: 36\n  category: acceptance\n  severity: blocker\n  verdict: needs-revision\n  origin: introduced\n  message: each side of the either/or bundles two independent claims (image contents plus the delegated lane running against it; three documents stating the limit plus naming what to add), so a revision can satisfy one half of a branch and not the other\n- file: .engineering/planning/story/exec-oom-kills-the-whole-tree.md\n  line: 34\n  category: acceptance\n  severity: blocker\n  verdict: needs-revision\n  origin: introduced\n  message: the acceptance joins the whole-cgroup kill and the refusal-code label with \"and\", and the story's own Context says the label is derived from a different code path than the kill mechanism, so the two are independently failable\n- file: .engineering/planning/story/hosted-admission-reuses-identity-authority.md\n  line: 33\n  category: acceptance\n  severity: blocker\n  verdict: needs-revision\n  origin: introduced\n  message: each side of the either/or bundles multiple independent claims (at-most-one resolution, bounded revocation latency and a capability-document statement on one side; an ADR and a deployment-guide cost statement on the other)\n- file: .engineering/planning/story/metrics-streams-are-bounded.md\n  line: 33\n  category: acceptance\n  severity: blocker\n  verdict: needs-revision\n  origin: introduced\n  message: the acceptance names three independent mechanisms (per-subject cap/429, lifetime expiry, oversized-message close) and says \"a test proves all three\", so any one of the three can regress without the others noticing\n- file: .engineering/planning/story/session-attachment-lifetime-is-an-accepted-decision.md\n  line: 32\n  category: acceptance\n  severity: blocker\n  verdict: needs-revision\n  origin: introduced\n  message: past the either/or (ADR vs. filed story), a third, unconditional clause (\"the design doc and the published capability document agree with the code\") is joined by a semicolon as an independent claim\n```\n","tags":["critic","review"],"title":"Acceptance critic (aep-planning:plan-critic-acceptance), round 1"},"args":{"command":"create-entity","data":{"body":"approve\nneeds-revision\n\nepic:review-2026-09-03-findings — the epic carries no Acceptance or Done-When section (only Outcome, Why Now, Scope, Out of Scope, Risks), so there is no epic-level statement that closes independently of \"all 14 stories implemented\" — .engineering/planning/epic/review-2026-09-03-findings.md (headings at lines 16, 23, 32, 52, 58; no Acceptance heading)\n\nstory:backend-recheck-hashes-only-on-change — the acceptance joins two independent scenarios with \"while\" (unchanged-metadata skips the read; a replaced binary is still refused), so one can pass while the other fails — .engineering/planning/story/backend-recheck-hashes-only-on-change.md:30\n\nstory:confined-processes-cannot-nest-user-namespaces — the acceptance conjoins three separately-failable claims (unshare -U fails on a modern host, the probe passes the new flag, exec facts are withheld on a host that lacks it), and \"observes both\" names only two of the three it just listed — .engineering/planning/story/confined-processes-cannot-nest-user-namespaces.md:37\n\nstory:daemon-image-serves-exec-or-says-it-cannot — each side of the either/or bundles two independent claims (image contents plus the delegated lane running against it; three documents stating the limit plus naming what to add), so a revision can satisfy one half of a branch and not the other — .engineering/planning/story/daemon-image-serves-exec-or-says-it-cannot.md:36\n\nstory:exec-oom-kills-the-whole-tree — the acceptance joins the whole-cgroup kill and the refusal-code label with \"and\", and the story's own Context says the label is derived from a different code path (`record_resource_bound`, gated on `measurements`) than the kill mechanism (`memory.oom.group`), so the two are independently failable — .engineering/planning/story/exec-oom-kills-the-whole-tree.md:34\n\nstory:hosted-admission-reuses-identity-authority — each side of the either/or bundles multiple independent claims (at-most-one resolution, bounded revocation latency, and a capability-document statement on one side; an ADR and a deployment-guide cost statement on the other) — .engineering/planning/story/hosted-admission-reuses-identity-authority.md:33\n\nstory:metrics-streams-are-bounded — the acceptance names three independent mechanisms (per-subject cap/429, lifetime expiry, oversized-message close) and says \"a test proves all three\", so any one of the three can regress without the others noticing — .engineering/planning/story/metrics-streams-are-bounded.md:33\n\nstory:session-attachment-lifetime-is-an-accepted-decision — past the either/or (ADR vs. filed story), a third, unconditional clause (\"the design doc and the published capability document agree with the code\") is joined by a semicolon as an independent claim — .engineering/planning/story/session-attachment-lifetime-is-an-accepted-decision.md:32\n\nWhat I read: the epic and all 14 decomposing stories, 15 artifacts total, via `aep artifact list --format json`, `aep artifact show <id>` for each of the 15, `aep artifact kinds`, `aep artifact lifecycle epic`, `aep artifact lifecycle story`, plus `grep -n` on the store files for line citations. I also read two other draft/proposed epics (`epic:container-driver-entry`, `epic:mcp-test-surface`) and two implemented ones to check this store's epic-acceptance convention before flagging the epic.\n\nWhat I could not establish: whether this store treats an epic's Scope (a numbered list of its stories) as an intentional substitute for a Done-When/Acceptance section — other epics in this store are inconsistent (`epic:release-hardening` and `epic:container-driver-entry` carry \"Done When\"; `epic:mcp-test-surface` carries neither), so I flagged the gap rather than assuming it is accepted convention. Six stories (`directory-listing-survives-special-entries`, `events-table-has-one-index`, `lease-cleanup-reads-exec-state-only`, `line-patch-edits-apply-in-line-order`, `seccomp-denies-af-vsock`, `unattached-claimed-session-is-contained`, `upgraded-connections-keep-their-permit`) read as single, checkable statements and carry no finding from me. Coupling between stories, coverage of the epic's 14-finding register, and any shared-surface risk from stories 6/7's confinement tightening are outside my lane and not reflected in this verdict.\n\n```findings\n- file: .engineering/planning/epic/review-2026-09-03-findings.md\n  line: 16\n  category: acceptance\n  severity: blocker\n  verdict: needs-revision\n  origin: introduced\n  message: the epic carries no Acceptance or Done-When section, so there is no epic-level statement that closes independently of \"all 14 stories implemented\"\n- file: .engineering/planning/story/backend-recheck-hashes-only-on-change.md\n  line: 30\n  category: acceptance\n  severity: blocker\n  verdict: needs-revision\n  origin: introduced\n  message: the acceptance joins two independent scenarios with \"while\" (unchanged-metadata skips the read; a replaced binary is still refused), so one can pass while the other fails\n- file: .engineering/planning/story/confined-processes-cannot-nest-user-namespaces.md\n  line: 37\n  category: acceptance\n  severity: blocker\n  verdict: needs-revision\n  origin: introduced\n  message: the acceptance conjoins three separately-failable claims (unshare -U fails, the probe passes the new flag, exec facts are withheld on an unsupporting host), and \"observes both\" names only two of the three it just listed\n- file: .engineering/planning/story/daemon-image-serves-exec-or-says-it-cannot.md\n  line: 36\n  category: acceptance\n  severity: blocker\n  verdict: needs-revision\n  origin: introduced\n  message: each side of the either/or bundles two independent claims (image contents plus the delegated lane running against it; three documents stating the limit plus naming what to add), so a revision can satisfy one half of a branch and not the other\n- file: .engineering/planning/story/exec-oom-kills-the-whole-tree.md\n  line: 34\n  category: acceptance\n  severity: blocker\n  verdict: needs-revision\n  origin: introduced\n  message: the acceptance joins the whole-cgroup kill and the refusal-code label with \"and\", and the story's own Context says the label is derived from a different code path than the kill mechanism, so the two are independently failable\n- file: .engineering/planning/story/hosted-admission-reuses-identity-authority.md\n  line: 33\n  category: acceptance\n  severity: blocker\n  verdict: needs-revision\n  origin: introduced\n  message: each side of the either/or bundles multiple independent claims (at-most-one resolution, bounded revocation latency and a capability-document statement on one side; an ADR and a deployment-guide cost statement on the other)\n- file: .engineering/planning/story/metrics-streams-are-bounded.md\n  line: 33\n  category: acceptance\n  severity: blocker\n  verdict: needs-revision\n  origin: introduced\n  message: the acceptance names three independent mechanisms (per-subject cap/429, lifetime expiry, oversized-message close) and says \"a test proves all three\", so any one of the three can regress without the others noticing\n- file: .engineering/planning/story/session-attachment-lifetime-is-an-accepted-decision.md\n  line: 32\n  category: acceptance\n  severity: blocker\n  verdict: needs-revision\n  origin: introduced\n  message: past the either/or (ADR vs. filed story), a third, unconditional clause (\"the design doc and the published capability document agree with the code\") is joined by a semicolon as an independent claim\n```\n","status":"active","tags":["critic","review"],"title":"Acceptance critic (aep-planning:plan-critic-acceptance), round 1"},"entity_type":"aep.review-result/v1","locator":"ep://planning/store/review-result/review-2026-09-03-acceptance-round-1"},"payload":{"actor":"human:timo","at":1788484423424,"causation":"cmd-new-review-result-review-2026-09-03-acceptance-round-1","change":{"change":"created","status":"active"},"correlation":"protocol-artifact-new","event_id":"review-result:review-2026-09-03-acceptance-round-1@1#0~5bb898ee301cf78c","executor":null,"recorded_at":"2026-09-04T01:13:43Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-acceptance-round-1","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000546","target":"01MEM0000000000000025"},"payload":{"actor":"human:timo","at":1788484423436,"causation":"cmd-review-result-review-2026-09-03-acceptance-round-1-reviews-epic-review-2026-09-03-findings","change":{"change":"related","relation":"reviews","target":"epic:review-2026-09-03-findings"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-acceptance-round-1@1#0~5057f1c677c46304","executor":null,"recorded_at":"2026-09-04T01:13:43Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-acceptance-round-1","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000546","target":"01MEM0000000000000136"},"payload":{"actor":"human:timo","at":1788484423445,"causation":"cmd-review-result-review-2026-09-03-acceptance-round-1-reviews-story-line-patch-edits-apply-in-line-order","change":{"change":"related","relation":"reviews","target":"story:line-patch-edits-apply-in-line-order"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-acceptance-round-1@1#0~745516d19b213597","executor":null,"recorded_at":"2026-09-04T01:13:43Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-acceptance-round-1","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000546","target":"01MEM0000000000000082"},"payload":{"actor":"human:timo","at":1788484423453,"causation":"cmd-review-result-review-2026-09-03-acceptance-round-1-reviews-story-directory-listing-survives-special-entries","change":{"change":"related","relation":"reviews","target":"story:directory-listing-survives-special-entries"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-acceptance-round-1@1#0~50a8e2f93282998b","executor":null,"recorded_at":"2026-09-04T01:13:43Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-acceptance-round-1","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000546","target":"01MEM0000000000000142"},"payload":{"actor":"human:timo","at":1788484423465,"causation":"cmd-review-result-review-2026-09-03-acceptance-round-1-reviews-story-metrics-streams-are-bounded","change":{"change":"related","relation":"reviews","target":"story:metrics-streams-are-bounded"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-acceptance-round-1@1#0~dcd00d0d1dbdf6a8","executor":null,"recorded_at":"2026-09-04T01:13:43Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-acceptance-round-1","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"},{"reviews":"story:upgraded-connections-keep-their-permit"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000546","target":"01MEM0000000000000220"},"payload":{"actor":"human:timo","at":1788484423472,"causation":"cmd-review-result-review-2026-09-03-acceptance-round-1-reviews-story-upgraded-connections-keep-their-permit","change":{"change":"related","relation":"reviews","target":"story:upgraded-connections-keep-their-permit"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-acceptance-round-1@1#0~02721d0fb5b2c60d","executor":null,"recorded_at":"2026-09-04T01:13:43Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-acceptance-round-1","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"},{"reviews":"story:upgraded-connections-keep-their-permit"},{"reviews":"story:lease-cleanup-reads-exec-state-only"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000546","target":"01MEM0000000000000130"},"payload":{"actor":"human:timo","at":1788484423480,"causation":"cmd-review-result-review-2026-09-03-acceptance-round-1-reviews-story-lease-cleanup-reads-exec-state-only","change":{"change":"related","relation":"reviews","target":"story:lease-cleanup-reads-exec-state-only"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-acceptance-round-1@1#0~414424d17e770381","executor":null,"recorded_at":"2026-09-04T01:13:43Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-acceptance-round-1","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"},{"reviews":"story:upgraded-connections-keep-their-permit"},{"reviews":"story:lease-cleanup-reads-exec-state-only"},{"reviews":"story:confined-processes-cannot-nest-user-namespaces"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000546","target":"01MEM0000000000000061"},"payload":{"actor":"human:timo","at":1788484423488,"causation":"cmd-review-result-review-2026-09-03-acceptance-round-1-reviews-story-confined-processes-cannot-nest-user-namespaces","change":{"change":"related","relation":"reviews","target":"story:confined-processes-cannot-nest-user-namespaces"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-acceptance-round-1@1#0~ddb8cde7473ad774","executor":null,"recorded_at":"2026-09-04T01:13:43Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-acceptance-round-1","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"},{"reviews":"story:upgraded-connections-keep-their-permit"},{"reviews":"story:lease-cleanup-reads-exec-state-only"},{"reviews":"story:confined-processes-cannot-nest-user-namespaces"},{"reviews":"story:seccomp-denies-af-vsock"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000546","target":"01MEM0000000000000199"},"payload":{"actor":"human:timo","at":1788484423496,"causation":"cmd-review-result-review-2026-09-03-acceptance-round-1-reviews-story-seccomp-denies-af-vsock","change":{"change":"related","relation":"reviews","target":"story:seccomp-denies-af-vsock"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-acceptance-round-1@1#0~599b7e2557379958","executor":null,"recorded_at":"2026-09-04T01:13:43Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-acceptance-round-1","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"},{"reviews":"story:upgraded-connections-keep-their-permit"},{"reviews":"story:lease-cleanup-reads-exec-state-only"},{"reviews":"story:confined-processes-cannot-nest-user-namespaces"},{"reviews":"story:seccomp-denies-af-vsock"},{"reviews":"story:exec-oom-kills-the-whole-tree"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000546","target":"01MEM0000000000000100"},"payload":{"actor":"human:timo","at":1788484423505,"causation":"cmd-review-result-review-2026-09-03-acceptance-round-1-reviews-story-exec-oom-kills-the-whole-tree","change":{"change":"related","relation":"reviews","target":"story:exec-oom-kills-the-whole-tree"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-acceptance-round-1@1#0~60b486ec580be552","executor":null,"recorded_at":"2026-09-04T01:13:43Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-acceptance-round-1","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"},{"reviews":"story:upgraded-connections-keep-their-permit"},{"reviews":"story:lease-cleanup-reads-exec-state-only"},{"reviews":"story:confined-processes-cannot-nest-user-namespaces"},{"reviews":"story:seccomp-denies-af-vsock"},{"reviews":"story:exec-oom-kills-the-whole-tree"},{"reviews":"story:session-attachment-lifetime-is-an-accepted-decision"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000546","target":"01MEM0000000000000205"},"payload":{"actor":"human:timo","at":1788484423513,"causation":"cmd-review-result-review-2026-09-03-acceptance-round-1-reviews-story-session-attachment-lifetime-is-an-accepted-decision","change":{"change":"related","relation":"reviews","target":"story:session-attachment-lifetime-is-an-accepted-decision"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-acceptance-round-1@1#0~82e6b22117a6d668","executor":null,"recorded_at":"2026-09-04T01:13:43Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-acceptance-round-1","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"},{"reviews":"story:upgraded-connections-keep-their-permit"},{"reviews":"story:lease-cleanup-reads-exec-state-only"},{"reviews":"story:confined-processes-cannot-nest-user-namespaces"},{"reviews":"story:seccomp-denies-af-vsock"},{"reviews":"story:exec-oom-kills-the-whole-tree"},{"reviews":"story:session-attachment-lifetime-is-an-accepted-decision"},{"reviews":"story:hosted-admission-reuses-identity-authority"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000546","target":"01MEM0000000000000118"},"payload":{"actor":"human:timo","at":1788484423523,"causation":"cmd-review-result-review-2026-09-03-acceptance-round-1-reviews-story-hosted-admission-reuses-identity-authority","change":{"change":"related","relation":"reviews","target":"story:hosted-admission-reuses-identity-authority"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-acceptance-round-1@1#0~effc54e38b5ca85f","executor":null,"recorded_at":"2026-09-04T01:13:43Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-acceptance-round-1","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"},{"reviews":"story:upgraded-connections-keep-their-permit"},{"reviews":"story:lease-cleanup-reads-exec-state-only"},{"reviews":"story:confined-processes-cannot-nest-user-namespaces"},{"reviews":"story:seccomp-denies-af-vsock"},{"reviews":"story:exec-oom-kills-the-whole-tree"},{"reviews":"story:session-attachment-lifetime-is-an-accepted-decision"},{"reviews":"story:hosted-admission-reuses-identity-authority"},{"reviews":"story:daemon-image-serves-exec-or-says-it-cannot"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000546","target":"01MEM0000000000000076"},"payload":{"actor":"human:timo","at":1788484423531,"causation":"cmd-review-result-review-2026-09-03-acceptance-round-1-reviews-story-daemon-image-serves-exec-or-says-it-cannot","change":{"change":"related","relation":"reviews","target":"story:daemon-image-serves-exec-or-says-it-cannot"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-acceptance-round-1@1#0~78e6c8f02bb91084","executor":null,"recorded_at":"2026-09-04T01:13:43Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-acceptance-round-1","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"},{"reviews":"story:upgraded-connections-keep-their-permit"},{"reviews":"story:lease-cleanup-reads-exec-state-only"},{"reviews":"story:confined-processes-cannot-nest-user-namespaces"},{"reviews":"story:seccomp-denies-af-vsock"},{"reviews":"story:exec-oom-kills-the-whole-tree"},{"reviews":"story:session-attachment-lifetime-is-an-accepted-decision"},{"reviews":"story:hosted-admission-reuses-identity-authority"},{"reviews":"story:daemon-image-serves-exec-or-says-it-cannot"},{"reviews":"story:backend-recheck-hashes-only-on-change"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000546","target":"01MEM0000000000000052"},"payload":{"actor":"human:timo","at":1788484423540,"causation":"cmd-review-result-review-2026-09-03-acceptance-round-1-reviews-story-backend-recheck-hashes-only-on-change","change":{"change":"related","relation":"reviews","target":"story:backend-recheck-hashes-only-on-change"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-acceptance-round-1@1#0~23b400dee2416976","executor":null,"recorded_at":"2026-09-04T01:13:43Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-acceptance-round-1","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"},{"reviews":"story:upgraded-connections-keep-their-permit"},{"reviews":"story:lease-cleanup-reads-exec-state-only"},{"reviews":"story:confined-processes-cannot-nest-user-namespaces"},{"reviews":"story:seccomp-denies-af-vsock"},{"reviews":"story:exec-oom-kills-the-whole-tree"},{"reviews":"story:session-attachment-lifetime-is-an-accepted-decision"},{"reviews":"story:hosted-admission-reuses-identity-authority"},{"reviews":"story:daemon-image-serves-exec-or-says-it-cannot"},{"reviews":"story:backend-recheck-hashes-only-on-change"},{"reviews":"story:events-table-has-one-index"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000546","target":"01MEM0000000000000097"},"payload":{"actor":"human:timo","at":1788484423549,"causation":"cmd-review-result-review-2026-09-03-acceptance-round-1-reviews-story-events-table-has-one-index","change":{"change":"related","relation":"reviews","target":"story:events-table-has-one-index"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-acceptance-round-1@1#0~fc5d3402202259ad","executor":null,"recorded_at":"2026-09-04T01:13:43Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-acceptance-round-1","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"},{"reviews":"story:upgraded-connections-keep-their-permit"},{"reviews":"story:lease-cleanup-reads-exec-state-only"},{"reviews":"story:confined-processes-cannot-nest-user-namespaces"},{"reviews":"story:seccomp-denies-af-vsock"},{"reviews":"story:exec-oom-kills-the-whole-tree"},{"reviews":"story:session-attachment-lifetime-is-an-accepted-decision"},{"reviews":"story:hosted-admission-reuses-identity-authority"},{"reviews":"story:daemon-image-serves-exec-or-says-it-cannot"},{"reviews":"story:backend-recheck-hashes-only-on-change"},{"reviews":"story:events-table-has-one-index"},{"reviews":"story:unattached-claimed-session-is-contained"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000546","target":"01MEM0000000000000217"},"payload":{"actor":"human:timo","at":1788484423557,"causation":"cmd-review-result-review-2026-09-03-acceptance-round-1-reviews-story-unattached-claimed-session-is-contained","change":{"change":"related","relation":"reviews","target":"story:unattached-claimed-session-is-contained"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-acceptance-round-1@1#0~f1dce029e2d687b3","executor":null,"recorded_at":"2026-09-04T01:13:43Z"}}
{"entity":"epic","version":1,"id":"review-2026-09-03-findings","revision":2,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"# Epic: Close the 2026-09-03 in-depth review findings\n\n## Outcome\n\nEvery finding of the 2026-09-03 whole-repository review (`~/.cache/substrate-review/2026-09-03-substrate-in-depth-review.md`) is either closed by a change that a\ntest or probe proves, or recorded as an accepted decision. Two correctness defects, three\nauthenticated-client capacity gaps, three confinement hardening gaps, three design constraints\nand three minor costs; the review's own register carries the file and line for each.\n\n## Why Now\n\nThe review read every file under `crates/` at `cc5671c` and ran both gates green\n(`scripts/gate.sh`: 491 tests; `scripts/delegated-lane.sh`: 96 tests), so the findings are\ndefects the suite does not see. Finding 1 was reproduced at runtime: the same two line-patch edits\nproduce two different files depending on their order in the request. Findings 3 to 5 are reachable\nby any authenticated local uid. The 0.5.1 release on `origin/main` changes none of the cited code\nexcept `crates/substrate-host/src/fs.rs` executable-mode preservation.\n\n## Scope\n\nOne story per finding, in the register's order. Correctness first, then capacity, then hardening,\nthen the three decisions, then the minor costs:\n\n1. `story:line-patch-edits-apply-in-line-order`\n2. `story:directory-listing-survives-special-entries`\n3. `story:metrics-streams-are-bounded`\n4. `story:upgraded-connections-keep-their-permit`\n5. `story:lease-cleanup-reads-exec-state-only`\n6. `story:confined-processes-cannot-nest-user-namespaces`\n7. `story:seccomp-denies-af-vsock`\n8. `story:exec-oom-kills-the-whole-tree`\n9. `story:session-attachment-lifetime-is-an-accepted-decision`\n10. `story:hosted-admission-reuses-identity-authority`\n11. `story:daemon-image-serves-exec-or-says-it-cannot`\n12. `story:backend-recheck-hashes-only-on-change`\n13. `story:events-table-has-one-index`\n14. `story:unattached-claimed-session-is-contained`\n\n## Out of Scope\n\nAny change to a frozen contract bundle. Where a story needs a wire change (story 2 may), it cuts a\nsuccessor bundle under invariant 6 and says so in its own body. Product policy, fleet scheduling\nand the remote-serving track stay where they are.\n\n## Risks\n\n- Stories 6 and 7 tighten confinement; a host whose bubblewrap or kernel refuses the new flag must\n  answer a named refusal, never a quieter sandbox (invariant 3).\n- Story 4 rests on hyper 1.11.1 upgrade semantics inferred from the library, not observed; the\n  story's first step is a test that observes it.\n- Stories 9 to 11 may end as recorded decisions rather than code; that is a valid close.\n\n## Done When\n\nEvery one of the 14 stories in Scope is `implemented` or `rejected` with a body that names the test, probe or ADR that closed it, and `aep artifact validate` reports the set valid.\n"},"args":{"changes":{"body":"# Epic: Close the 2026-09-03 in-depth review findings\n\n## Outcome\n\nEvery finding of the 2026-09-03 whole-repository review (`~/.cache/substrate-review/2026-09-03-substrate-in-depth-review.md`) is either closed by a change that a\ntest or probe proves, or recorded as an accepted decision. Two correctness defects, three\nauthenticated-client capacity gaps, three confinement hardening gaps, three design constraints\nand three minor costs; the review's own register carries the file and line for each.\n\n## Why Now\n\nThe review read every file under `crates/` at `cc5671c` and ran both gates green\n(`scripts/gate.sh`: 491 tests; `scripts/delegated-lane.sh`: 96 tests), so the findings are\ndefects the suite does not see. Finding 1 was reproduced at runtime: the same two line-patch edits\nproduce two different files depending on their order in the request. Findings 3 to 5 are reachable\nby any authenticated local uid. The 0.5.1 release on `origin/main` changes none of the cited code\nexcept `crates/substrate-host/src/fs.rs` executable-mode preservation.\n\n## Scope\n\nOne story per finding, in the register's order. Correctness first, then capacity, then hardening,\nthen the three decisions, then the minor costs:\n\n1. `story:line-patch-edits-apply-in-line-order`\n2. `story:directory-listing-survives-special-entries`\n3. `story:metrics-streams-are-bounded`\n4. `story:upgraded-connections-keep-their-permit`\n5. `story:lease-cleanup-reads-exec-state-only`\n6. `story:confined-processes-cannot-nest-user-namespaces`\n7. `story:seccomp-denies-af-vsock`\n8. `story:exec-oom-kills-the-whole-tree`\n9. `story:session-attachment-lifetime-is-an-accepted-decision`\n10. `story:hosted-admission-reuses-identity-authority`\n11. `story:daemon-image-serves-exec-or-says-it-cannot`\n12. `story:backend-recheck-hashes-only-on-change`\n13. `story:events-table-has-one-index`\n14. `story:unattached-claimed-session-is-contained`\n\n## Out of Scope\n\nAny change to a frozen contract bundle. Where a story needs a wire change (story 2 may), it cuts a\nsuccessor bundle under invariant 6 and says so in its own body. Product policy, fleet scheduling\nand the remote-serving track stay where they are.\n\n## Risks\n\n- Stories 6 and 7 tighten confinement; a host whose bubblewrap or kernel refuses the new flag must\n  answer a named refusal, never a quieter sandbox (invariant 3).\n- Story 4 rests on hyper 1.11.1 upgrade semantics inferred from the library, not observed; the\n  story's first step is a test that observes it.\n- Stories 9 to 11 may end as recorded decisions rather than code; that is a valid close.\n\n## Done When\n\nEvery one of the 14 stories in Scope is `implemented` or `rejected` with a body that names the test, probe or ADR that closed it, and `aep artifact validate` reports the set valid.\n"},"command":"update-entity","target":"01MEM0000000000000025"},"payload":{"actor":"human:timo","at":1788484423742,"causation":"cmd-protocol-artifact-body-section-epic-review-2026-09-03-findings-1788484423742","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"epic:review-2026-09-03-findings@2#0~c93e3b93ee5d4402","executor":null,"recorded_at":"2026-09-04T01:13:43Z"}}
{"entity":"story","version":1,"id":"backend-recheck-hashes-only-on-change","revision":3,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"# Story: Backend recheck hashes only on change\n\n## Context\n\nEvery exec admission calls `recheck_backend`, which calls `backend_binding`, which reads the\nwhole bubblewrap binary and computes its SHA-256 (`crates/substrate-host/src/process.rs:1753`;\n`probe.rs:267`). The binding already records device, inode and size. On a busy daemon this is one\nfile read and hash per exec start on the admission path.\n\n## Acceptance\n\nAn exec admission against a bubblewrap whose device, inode, size and modification time match the probed binding completes without opening the binary, proven by a test that counts the opens.\n\n## Notes\n\nKeep the full hash in the startup probe; the recheck compares metadata first and hashes only when\nit differs.\n"},"args":{"changes":{"body":"# Story: Backend recheck hashes only on change\n\n## Context\n\nEvery exec admission calls `recheck_backend`, which calls `backend_binding`, which reads the\nwhole bubblewrap binary and computes its SHA-256 (`crates/substrate-host/src/process.rs:1753`;\n`probe.rs:267`). The binding already records device, inode and size. On a busy daemon this is one\nfile read and hash per exec start on the admission path.\n\n## Acceptance\n\nAn exec admission against a bubblewrap whose device, inode, size and modification time match the probed binding completes without opening the binary, proven by a test that counts the opens.\n\n## Notes\n\nKeep the full hash in the startup probe; the recheck compares metadata first and hashes only when\nit differs.\n"},"command":"update-entity","target":"01MEM0000000000000055"},"payload":{"actor":"human:timo","at":1788484423911,"causation":"cmd-protocol-artifact-body-section-story-backend-recheck-hashes-only-on-change-1788484423911","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:backend-recheck-hashes-only-on-change@3#0~1e864635a85526d2","executor":null,"recorded_at":"2026-09-04T01:13:43Z"}}
{"entity":"story","version":1,"id":"backend-recheck-hashes-only-on-change","revision":4,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"# Story: Backend recheck hashes only on change\n\n## Context\n\nEvery exec admission calls `recheck_backend`, which calls `backend_binding`, which reads the\nwhole bubblewrap binary and computes its SHA-256 (`crates/substrate-host/src/process.rs:1753`;\n`probe.rs:267`). The binding already records device, inode and size. On a busy daemon this is one\nfile read and hash per exec start on the admission path.\n\n## Acceptance\n\nAn exec admission against a bubblewrap whose device, inode, size and modification time match the probed binding completes without opening the binary, proven by a test that counts the opens.\n\n## Notes\n\nKeep the full hash in the startup probe; the recheck compares metadata first and hashes only when it differs. The existing `exec.capability-stale` refusal for a replaced binary must keep holding; it is already covered by `stale_snapshot_refuses_before_backend_access` and is not this story's acceptance.\n"},"args":{"changes":{"body":"# Story: Backend recheck hashes only on change\n\n## Context\n\nEvery exec admission calls `recheck_backend`, which calls `backend_binding`, which reads the\nwhole bubblewrap binary and computes its SHA-256 (`crates/substrate-host/src/process.rs:1753`;\n`probe.rs:267`). The binding already records device, inode and size. On a busy daemon this is one\nfile read and hash per exec start on the admission path.\n\n## Acceptance\n\nAn exec admission against a bubblewrap whose device, inode, size and modification time match the probed binding completes without opening the binary, proven by a test that counts the opens.\n\n## Notes\n\nKeep the full hash in the startup probe; the recheck compares metadata first and hashes only when it differs. The existing `exec.capability-stale` refusal for a replaced binary must keep holding; it is already covered by `stale_snapshot_refuses_before_backend_access` and is not this story's acceptance.\n"},"command":"update-entity","target":"01MEM0000000000000055"},"payload":{"actor":"human:timo","at":1788484424078,"causation":"cmd-protocol-artifact-body-section-story-backend-recheck-hashes-only-on-change-1788484424078","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:backend-recheck-hashes-only-on-change@4#0~a27cec3bc86cc944","executor":null,"recorded_at":"2026-09-04T01:13:44Z"}}
{"entity":"story","version":1,"id":"confined-processes-cannot-nest-user-namespaces","revision":3,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"# Story: Confined processes cannot nest user namespaces\n\n## Context\n\nThe exec argv passes `--unshare-user` and not `--disable-userns`\n(`crates/substrate-host/src/process.rs:1813-1838`); the three probe argv lists in `probe.rs`,\n`pty.rs` and `egress.rs` match it. The installed bubblewrap 0.11.2 lists `--disable-userns` and\n`--assert-userns-disabled` (`bwrap --help`). A confined process can therefore create nested user\nnamespaces, which is the entry point of most unprivileged kernel privilege escalations. The\ncurrent seccomp profile does not block `unshare` or `clone` with `CLONE_NEWUSER`\n(`crates/substrate-host/src/seccomp.rs:70-99`).\n\n## Acceptance\n\nInside an admitted exec on this host, `unshare -U` fails, observed by a delegated-lane case.\n\n## Notes\n\nInvariant 3: a bubblewrap too old for the flag is a named refusal, never a sandbox without it.\n"},"args":{"changes":{"body":"# Story: Confined processes cannot nest user namespaces\n\n## Context\n\nThe exec argv passes `--unshare-user` and not `--disable-userns`\n(`crates/substrate-host/src/process.rs:1813-1838`); the three probe argv lists in `probe.rs`,\n`pty.rs` and `egress.rs` match it. The installed bubblewrap 0.11.2 lists `--disable-userns` and\n`--assert-userns-disabled` (`bwrap --help`). A confined process can therefore create nested user\nnamespaces, which is the entry point of most unprivileged kernel privilege escalations. The\ncurrent seccomp profile does not block `unshare` or `clone` with `CLONE_NEWUSER`\n(`crates/substrate-host/src/seccomp.rs:70-99`).\n\n## Acceptance\n\nInside an admitted exec on this host, `unshare -U` fails, observed by a delegated-lane case.\n\n## Notes\n\nInvariant 3: a bubblewrap too old for the flag is a named refusal, never a sandbox without it.\n"},"command":"update-entity","target":"01MEM0000000000000064"},"payload":{"actor":"human:timo","at":1788484424243,"causation":"cmd-protocol-artifact-body-section-story-confined-processes-cannot-nest-user-namespaces-1788484424243","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:confined-processes-cannot-nest-user-namespaces@3#0~30b9d52b6c44bcf1","executor":null,"recorded_at":"2026-09-04T01:13:44Z"}}
{"entity":"story","version":1,"id":"confined-processes-cannot-nest-user-namespaces","revision":4,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"# Story: Confined processes cannot nest user namespaces\n\n## Context\n\nThe exec argv passes `--unshare-user` and not `--disable-userns`\n(`crates/substrate-host/src/process.rs:1813-1838`); the three probe argv lists in `probe.rs`,\n`pty.rs` and `egress.rs` match it. The installed bubblewrap 0.11.2 lists `--disable-userns` and\n`--assert-userns-disabled` (`bwrap --help`). A confined process can therefore create nested user\nnamespaces, which is the entry point of most unprivileged kernel privilege escalations. The\ncurrent seccomp profile does not block `unshare` or `clone` with `CLONE_NEWUSER`\n(`crates/substrate-host/src/seccomp.rs:70-99`).\n\n## Acceptance\n\nInside an admitted exec on this host, `unshare -U` fails, observed by a delegated-lane case.\n\n## Notes\n\nInvariant 3: a bubblewrap too old for `--disable-userns` is a named refusal, never a sandbox without it. The probe therefore passes `--assert-userns-disabled` and withholds the exec facts when it fails; that path is delivered in this story and checked by its own probe case, separate from the acceptance above.\n"},"args":{"changes":{"body":"# Story: Confined processes cannot nest user namespaces\n\n## Context\n\nThe exec argv passes `--unshare-user` and not `--disable-userns`\n(`crates/substrate-host/src/process.rs:1813-1838`); the three probe argv lists in `probe.rs`,\n`pty.rs` and `egress.rs` match it. The installed bubblewrap 0.11.2 lists `--disable-userns` and\n`--assert-userns-disabled` (`bwrap --help`). A confined process can therefore create nested user\nnamespaces, which is the entry point of most unprivileged kernel privilege escalations. The\ncurrent seccomp profile does not block `unshare` or `clone` with `CLONE_NEWUSER`\n(`crates/substrate-host/src/seccomp.rs:70-99`).\n\n## Acceptance\n\nInside an admitted exec on this host, `unshare -U` fails, observed by a delegated-lane case.\n\n## Notes\n\nInvariant 3: a bubblewrap too old for `--disable-userns` is a named refusal, never a sandbox without it. The probe therefore passes `--assert-userns-disabled` and withholds the exec facts when it fails; that path is delivered in this story and checked by its own probe case, separate from the acceptance above.\n"},"command":"update-entity","target":"01MEM0000000000000064"},"payload":{"actor":"human:timo","at":1788484424417,"causation":"cmd-protocol-artifact-body-section-story-confined-processes-cannot-nest-user-namespaces-1788484424417","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:confined-processes-cannot-nest-user-namespaces@4#0~a92415b7b772534f","executor":null,"recorded_at":"2026-09-04T01:13:44Z"}}
{"entity":"story","version":1,"id":"daemon-image-serves-exec-or-says-it-cannot","revision":3,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"# Story: The daemon image serves exec or says it cannot\n\n## Context\n\nExec capability requires the configured bubblewrap binary and `/usr/bin/socat` on the host\n(`crates/substrate-host/src/probe.rs:319-320`; documented at `README.md:280`). The published\ndaemon image is `gcr.io/distroless/cc-debian12:nonroot` (`Dockerfile:12`) and installs neither,\nnor `/usr/bin/env`, which the sandbox argv execs (`process.rs`, `command`). Inferred from the base\nimage, not pulled: that image can only answer `exec.sandbox-unavailable`, and a reader of the\nrelease notes is not told so.\n\n## Acceptance\n\nThe GHCR release notes and `README.md` state whether the published daemon image serves exec and, when it does not, name what a host must add.\n\n## Notes\n\nThe `socat` dependency exists only to prove the seccomp `AF_UNIX` refusal; a small Rust probe\nchild would remove it.\n"},"args":{"changes":{"body":"# Story: The daemon image serves exec or says it cannot\n\n## Context\n\nExec capability requires the configured bubblewrap binary and `/usr/bin/socat` on the host\n(`crates/substrate-host/src/probe.rs:319-320`; documented at `README.md:280`). The published\ndaemon image is `gcr.io/distroless/cc-debian12:nonroot` (`Dockerfile:12`) and installs neither,\nnor `/usr/bin/env`, which the sandbox argv execs (`process.rs`, `command`). Inferred from the base\nimage, not pulled: that image can only answer `exec.sandbox-unavailable`, and a reader of the\nrelease notes is not told so.\n\n## Acceptance\n\nThe GHCR release notes and `README.md` state whether the published daemon image serves exec and, when it does not, name what a host must add.\n\n## Notes\n\nThe `socat` dependency exists only to prove the seccomp `AF_UNIX` refusal; a small Rust probe\nchild would remove it.\n"},"command":"update-entity","target":"01MEM0000000000000079"},"payload":{"actor":"human:timo","at":1788484424587,"causation":"cmd-protocol-artifact-body-section-story-daemon-image-serves-exec-or-says-it-cannot-1788484424587","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:daemon-image-serves-exec-or-says-it-cannot@3#0~f5c844dbcf9287e4","executor":null,"recorded_at":"2026-09-04T01:13:44Z"}}
{"entity":"story","version":1,"id":"daemon-image-serves-exec-or-says-it-cannot","revision":4,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"# Story: The daemon image serves exec or says it cannot\n\n## Context\n\nExec capability requires the configured bubblewrap binary and `/usr/bin/socat` on the host\n(`crates/substrate-host/src/probe.rs:319-320`; documented at `README.md:280`). The published\ndaemon image is `gcr.io/distroless/cc-debian12:nonroot` (`Dockerfile:12`) and installs neither,\nnor `/usr/bin/env`, which the sandbox argv execs (`process.rs`, `command`). Inferred from the base\nimage, not pulled: that image can only answer `exec.sandbox-unavailable`, and a reader of the\nrelease notes is not told so.\n\n## Acceptance\n\nThe GHCR release notes and `README.md` state whether the published daemon image serves exec and, when it does not, name what a host must add.\n\n## Notes\n\nThe statement holds either way: if a later story ships bubblewrap, socat and coreutils in the image and runs the delegated lane against it, the documents say the image serves exec. The `socat` dependency exists only to prove the seccomp `AF_UNIX` refusal; a small Rust probe child would remove it.\n"},"args":{"changes":{"body":"# Story: The daemon image serves exec or says it cannot\n\n## Context\n\nExec capability requires the configured bubblewrap binary and `/usr/bin/socat` on the host\n(`crates/substrate-host/src/probe.rs:319-320`; documented at `README.md:280`). The published\ndaemon image is `gcr.io/distroless/cc-debian12:nonroot` (`Dockerfile:12`) and installs neither,\nnor `/usr/bin/env`, which the sandbox argv execs (`process.rs`, `command`). Inferred from the base\nimage, not pulled: that image can only answer `exec.sandbox-unavailable`, and a reader of the\nrelease notes is not told so.\n\n## Acceptance\n\nThe GHCR release notes and `README.md` state whether the published daemon image serves exec and, when it does not, name what a host must add.\n\n## Notes\n\nThe statement holds either way: if a later story ships bubblewrap, socat and coreutils in the image and runs the delegated lane against it, the documents say the image serves exec. The `socat` dependency exists only to prove the seccomp `AF_UNIX` refusal; a small Rust probe child would remove it.\n"},"command":"update-entity","target":"01MEM0000000000000079"},"payload":{"actor":"human:timo","at":1788484424765,"causation":"cmd-protocol-artifact-body-section-story-daemon-image-serves-exec-or-says-it-cannot-1788484424765","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:daemon-image-serves-exec-or-says-it-cannot@4#0~709378a4826a3296","executor":null,"recorded_at":"2026-09-04T01:13:44Z"}}
{"entity":"story","version":1,"id":"exec-oom-kills-the-whole-tree","revision":3,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"# Story: An exec OOM kills the whole tree\n\n## Context\n\n`Cgroup::create` writes `pids.max`, `memory.max`, `memory.swap.max=0` and `cpu.max`\n(`crates/substrate-host/src/process.rs:2848-2860`) and not `memory.oom.group=1`. The kernel then\nkills one process on OOM and the rest of the tree continues, which is not the whole-tree semantics\nthe safety envelope names (`AGENTS.md` § Safety envelope). The `exec.memory-limit` refusal is\nderived from `memory_oom_kills` in the terminal usage, which exists only when the client asked for\n`measurements` (`process.rs`, `run_child` and `record_resource_bound`), so an OOM on a run without\nmeasurements is reported as an ordinary exit. `cpu.max` is clamped to one CPU\n(`process.rs:2858`), which is a documented choice worth stating on the capability fact.\n\n## Acceptance\n\nAn exec whose child exceeds `memory.max` ends with no process left in its cgroup, observed in the delegated lane.\n\n## Notes\n\n`memory.oom.group` is a cgroup v2 file present on every kernel the probe already requires; probe\nit and withhold the exec facts where it is absent.\n"},"args":{"changes":{"body":"# Story: An exec OOM kills the whole tree\n\n## Context\n\n`Cgroup::create` writes `pids.max`, `memory.max`, `memory.swap.max=0` and `cpu.max`\n(`crates/substrate-host/src/process.rs:2848-2860`) and not `memory.oom.group=1`. The kernel then\nkills one process on OOM and the rest of the tree continues, which is not the whole-tree semantics\nthe safety envelope names (`AGENTS.md` § Safety envelope). The `exec.memory-limit` refusal is\nderived from `memory_oom_kills` in the terminal usage, which exists only when the client asked for\n`measurements` (`process.rs`, `run_child` and `record_resource_bound`), so an OOM on a run without\nmeasurements is reported as an ordinary exit. `cpu.max` is clamped to one CPU\n(`process.rs:2858`), which is a documented choice worth stating on the capability fact.\n\n## Acceptance\n\nAn exec whose child exceeds `memory.max` ends with no process left in its cgroup, observed in the delegated lane.\n\n## Notes\n\n`memory.oom.group` is a cgroup v2 file present on every kernel the probe already requires; probe\nit and withhold the exec facts where it is absent.\n"},"command":"update-entity","target":"01MEM0000000000000103"},"payload":{"actor":"human:timo","at":1788484424934,"causation":"cmd-protocol-artifact-body-section-story-exec-oom-kills-the-whole-tree-1788484424934","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:exec-oom-kills-the-whole-tree@3#0~e7181e92994dbecc","executor":null,"recorded_at":"2026-09-04T01:13:44Z"}}
{"entity":"story","version":1,"id":"exec-oom-kills-the-whole-tree","revision":4,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"# Story: An exec OOM kills the whole tree\n\n## Context\n\n`Cgroup::create` writes `pids.max`, `memory.max`, `memory.swap.max=0` and `cpu.max`\n(`crates/substrate-host/src/process.rs:2848-2860`) and not `memory.oom.group=1`. The kernel then\nkills one process on OOM and the rest of the tree continues, which is not the whole-tree semantics\nthe safety envelope names (`AGENTS.md` § Safety envelope). The `exec.memory-limit` refusal is\nderived from `memory_oom_kills` in the terminal usage, which exists only when the client asked for\n`measurements` (`process.rs`, `run_child` and `record_resource_bound`), so an OOM on a run without\nmeasurements is reported as an ordinary exit. `cpu.max` is clamped to one CPU\n(`process.rs:2858`), which is a documented choice worth stating on the capability fact.\n\n## Acceptance\n\nAn exec whose child exceeds `memory.max` ends with no process left in its cgroup, observed in the delegated lane.\n\n## Notes\n\n`memory.oom.group` is a cgroup v2 file present on every kernel the probe already requires; probe it and withhold the exec facts where it is absent. Naming `exec.memory-limit` on the observation when `measurements` was not requested is a second change on `record_resource_bound`, delivered in this story and checked by its own case rather than by the acceptance above.\n"},"args":{"changes":{"body":"# Story: An exec OOM kills the whole tree\n\n## Context\n\n`Cgroup::create` writes `pids.max`, `memory.max`, `memory.swap.max=0` and `cpu.max`\n(`crates/substrate-host/src/process.rs:2848-2860`) and not `memory.oom.group=1`. The kernel then\nkills one process on OOM and the rest of the tree continues, which is not the whole-tree semantics\nthe safety envelope names (`AGENTS.md` § Safety envelope). The `exec.memory-limit` refusal is\nderived from `memory_oom_kills` in the terminal usage, which exists only when the client asked for\n`measurements` (`process.rs`, `run_child` and `record_resource_bound`), so an OOM on a run without\nmeasurements is reported as an ordinary exit. `cpu.max` is clamped to one CPU\n(`process.rs:2858`), which is a documented choice worth stating on the capability fact.\n\n## Acceptance\n\nAn exec whose child exceeds `memory.max` ends with no process left in its cgroup, observed in the delegated lane.\n\n## Notes\n\n`memory.oom.group` is a cgroup v2 file present on every kernel the probe already requires; probe it and withhold the exec facts where it is absent. Naming `exec.memory-limit` on the observation when `measurements` was not requested is a second change on `record_resource_bound`, delivered in this story and checked by its own case rather than by the acceptance above.\n"},"command":"update-entity","target":"01MEM0000000000000103"},"payload":{"actor":"human:timo","at":1788484425106,"causation":"cmd-protocol-artifact-body-section-story-exec-oom-kills-the-whole-tree-1788484425106","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:exec-oom-kills-the-whole-tree@4#0~ed1d952cf514b880","executor":null,"recorded_at":"2026-09-04T01:13:45Z"}}
{"entity":"story","version":1,"id":"hosted-admission-reuses-identity-authority","revision":3,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"# Story: Hosted admission reuses a resolved Identity authority\n\n## Context\n\nEvery request on the production TLS listener resolves its credential against Identity with a\nfresh TCP connect, TLS 1.3 handshake and HTTP/1.1 request carrying `Connection: close`\n(`crates/substrate-daemon/src/hosted.rs:170-194`). Nothing is cached, although the authority\ncarries `jti`, `exp` and a lifetime bound of 5 min (`hosted.rs:42-43`, `validate_authority`).\nPer-request latency includes one Identity round trip, and Identity load equals Substrate request\nrate. ADR 0026 chose online resolution over introspection caching; it does not state that\nconnection reuse or a bounded per-credential cache is excluded.\n\n## Acceptance\n\nTwo hosted requests carrying the same credential within its remaining authority lifetime cause at most one Identity resolution, or an ADR records that every request must resolve online.\n\n## Notes\n\nConnection reuse alone (a small keep-alive pool to Identity) removes the handshake without any\nchange to the trust posture.\n"},"args":{"changes":{"body":"# Story: Hosted admission reuses a resolved Identity authority\n\n## Context\n\nEvery request on the production TLS listener resolves its credential against Identity with a\nfresh TCP connect, TLS 1.3 handshake and HTTP/1.1 request carrying `Connection: close`\n(`crates/substrate-daemon/src/hosted.rs:170-194`). Nothing is cached, although the authority\ncarries `jti`, `exp` and a lifetime bound of 5 min (`hosted.rs:42-43`, `validate_authority`).\nPer-request latency includes one Identity round trip, and Identity load equals Substrate request\nrate. ADR 0026 chose online resolution over introspection caching; it does not state that\nconnection reuse or a bounded per-credential cache is excluded.\n\n## Acceptance\n\nTwo hosted requests carrying the same credential within its remaining authority lifetime cause at most one Identity resolution, or an ADR records that every request must resolve online.\n\n## Notes\n\nConnection reuse alone (a small keep-alive pool to Identity) removes the handshake without any\nchange to the trust posture.\n"},"command":"update-entity","target":"01MEM0000000000000121"},"payload":{"actor":"human:timo","at":1788484425273,"causation":"cmd-protocol-artifact-body-section-story-hosted-admission-reuses-identity-authority-1788484425273","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:hosted-admission-reuses-identity-authority@3#0~5d5dcf5c29b646d7","executor":null,"recorded_at":"2026-09-04T01:13:45Z"}}
{"entity":"story","version":1,"id":"hosted-admission-reuses-identity-authority","revision":4,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"# Story: Hosted admission reuses a resolved Identity authority\n\n## Context\n\nEvery request on the production TLS listener resolves its credential against Identity with a\nfresh TCP connect, TLS 1.3 handshake and HTTP/1.1 request carrying `Connection: close`\n(`crates/substrate-daemon/src/hosted.rs:170-194`). Nothing is cached, although the authority\ncarries `jti`, `exp` and a lifetime bound of 5 min (`hosted.rs:42-43`, `validate_authority`).\nPer-request latency includes one Identity round trip, and Identity load equals Substrate request\nrate. ADR 0026 chose online resolution over introspection caching; it does not state that\nconnection reuse or a bounded per-credential cache is excluded.\n\n## Acceptance\n\nTwo hosted requests carrying the same credential within its remaining authority lifetime cause at most one Identity resolution, or an ADR records that every request must resolve online.\n\n## Notes\n\nConnection reuse alone (a small keep-alive pool to Identity) removes the handshake without any change to the trust posture. Whichever branch closes the story also states the revocation latency bound, or the per-request cost, on the capability document and in the deployment guide; those statements follow from the branch and are not a third claim.\n"},"args":{"changes":{"body":"# Story: Hosted admission reuses a resolved Identity authority\n\n## Context\n\nEvery request on the production TLS listener resolves its credential against Identity with a\nfresh TCP connect, TLS 1.3 handshake and HTTP/1.1 request carrying `Connection: close`\n(`crates/substrate-daemon/src/hosted.rs:170-194`). Nothing is cached, although the authority\ncarries `jti`, `exp` and a lifetime bound of 5 min (`hosted.rs:42-43`, `validate_authority`).\nPer-request latency includes one Identity round trip, and Identity load equals Substrate request\nrate. ADR 0026 chose online resolution over introspection caching; it does not state that\nconnection reuse or a bounded per-credential cache is excluded.\n\n## Acceptance\n\nTwo hosted requests carrying the same credential within its remaining authority lifetime cause at most one Identity resolution, or an ADR records that every request must resolve online.\n\n## Notes\n\nConnection reuse alone (a small keep-alive pool to Identity) removes the handshake without any change to the trust posture. Whichever branch closes the story also states the revocation latency bound, or the per-request cost, on the capability document and in the deployment guide; those statements follow from the branch and are not a third claim.\n"},"command":"update-entity","target":"01MEM0000000000000121"},"payload":{"actor":"human:timo","at":1788484425445,"causation":"cmd-protocol-artifact-body-section-story-hosted-admission-reuses-identity-authority-1788484425445","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:hosted-admission-reuses-identity-authority@4#0~de585b9de8c8bf90","executor":null,"recorded_at":"2026-09-04T01:13:45Z"}}
{"entity":"story","version":1,"id":"metrics-streams-are-bounded","revision":3,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"# Story: Metrics streams are bounded\n\n## Context\n\n`GET /v1/metrics/stream` upgrades to a WebSocket with no permit, no lifetime and the library's\ndefault frame and message bounds (`crates/substrate-daemon/src/app/metrics.rs:75-90`). Each open\nstream runs `load_exec_usage` once a second, which is an `observe_exec` plus a `put_exec` SQLite\nwrite (`metrics.rs:98-` and `:215-`), until the exec ends, up to the 24 h exec timeout. Event\nstreams are capped at 64 global and 4 per subject (`app/events.rs:47-48`) and pipe attachments at\n32 (`app/sessions.rs:63`); metrics streams at nothing. One authenticated uid can open as many as\nthe kernel allows.\n\n## Acceptance\n\nOpening one more metrics stream than a published per-subject cap answers `429` with a named `exhausted` refusal.\n\n## Notes\n\nReuse the `EventStreamLimits` shape and `EventStreamPolicy` bounds; the metrics stream is the one\nupgrade that has none of them.\n"},"args":{"changes":{"body":"# Story: Metrics streams are bounded\n\n## Context\n\n`GET /v1/metrics/stream` upgrades to a WebSocket with no permit, no lifetime and the library's\ndefault frame and message bounds (`crates/substrate-daemon/src/app/metrics.rs:75-90`). Each open\nstream runs `load_exec_usage` once a second, which is an `observe_exec` plus a `put_exec` SQLite\nwrite (`metrics.rs:98-` and `:215-`), until the exec ends, up to the 24 h exec timeout. Event\nstreams are capped at 64 global and 4 per subject (`app/events.rs:47-48`) and pipe attachments at\n32 (`app/sessions.rs:63`); metrics streams at nothing. One authenticated uid can open as many as\nthe kernel allows.\n\n## Acceptance\n\nOpening one more metrics stream than a published per-subject cap answers `429` with a named `exhausted` refusal.\n\n## Notes\n\nReuse the `EventStreamLimits` shape and `EventStreamPolicy` bounds; the metrics stream is the one\nupgrade that has none of them.\n"},"command":"update-entity","target":"01MEM0000000000000145"},"payload":{"actor":"human:timo","at":1788484425613,"causation":"cmd-protocol-artifact-body-section-story-metrics-streams-are-bounded-1788484425613","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:metrics-streams-are-bounded@3#0~7a762e1760d83f54","executor":null,"recorded_at":"2026-09-04T01:13:45Z"}}
{"entity":"story","version":1,"id":"metrics-streams-are-bounded","revision":4,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"# Story: Metrics streams are bounded\n\n## Context\n\n`GET /v1/metrics/stream` upgrades to a WebSocket with no permit, no lifetime and the library's\ndefault frame and message bounds (`crates/substrate-daemon/src/app/metrics.rs:75-90`). Each open\nstream runs `load_exec_usage` once a second, which is an `observe_exec` plus a `put_exec` SQLite\nwrite (`metrics.rs:98-` and `:215-`), until the exec ends, up to the 24 h exec timeout. Event\nstreams are capped at 64 global and 4 per subject (`app/events.rs:47-48`) and pipe attachments at\n32 (`app/sessions.rs:63`); metrics streams at nothing. One authenticated uid can open as many as\nthe kernel allows.\n\n## Acceptance\n\nOpening one more metrics stream than a published per-subject cap answers `429` with a named `exhausted` refusal.\n\n## Notes\n\nReuse the `EventStreamLimits` shape and `EventStreamPolicy` bounds; the metrics stream is the one upgrade that has none of them. The lifetime and the client-message bound arrive with the same policy struct and are checked by their own cases, not by the acceptance above.\n"},"args":{"changes":{"body":"# Story: Metrics streams are bounded\n\n## Context\n\n`GET /v1/metrics/stream` upgrades to a WebSocket with no permit, no lifetime and the library's\ndefault frame and message bounds (`crates/substrate-daemon/src/app/metrics.rs:75-90`). Each open\nstream runs `load_exec_usage` once a second, which is an `observe_exec` plus a `put_exec` SQLite\nwrite (`metrics.rs:98-` and `:215-`), until the exec ends, up to the 24 h exec timeout. Event\nstreams are capped at 64 global and 4 per subject (`app/events.rs:47-48`) and pipe attachments at\n32 (`app/sessions.rs:63`); metrics streams at nothing. One authenticated uid can open as many as\nthe kernel allows.\n\n## Acceptance\n\nOpening one more metrics stream than a published per-subject cap answers `429` with a named `exhausted` refusal.\n\n## Notes\n\nReuse the `EventStreamLimits` shape and `EventStreamPolicy` bounds; the metrics stream is the one upgrade that has none of them. The lifetime and the client-message bound arrive with the same policy struct and are checked by their own cases, not by the acceptance above.\n"},"command":"update-entity","target":"01MEM0000000000000145"},"payload":{"actor":"human:timo","at":1788484425783,"causation":"cmd-protocol-artifact-body-section-story-metrics-streams-are-bounded-1788484425783","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:metrics-streams-are-bounded@4#0~8b8be86761f55e77","executor":null,"recorded_at":"2026-09-04T01:13:45Z"}}
{"entity":"story","version":1,"id":"session-attachment-lifetime-is-an-accepted-decision","revision":3,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"# Story: The session attachment lifetime is an accepted decision\n\n## Context\n\nA session attachment lives at most one hour (`crates/substrate-daemon/src/app/sessions.rs:70`);\nwhen that expires, or on any disconnect or protocol error, the process tree is killed\n(`sessions.rs:1301`) and the one-shot attachment right is consumed\n(`crates/substrate-store/src/sessions.rs`, `claim_pipe_session_attachment_inner`). No re-attach\nexists. ADR 0008 states the kill-on-loss rule; nothing states the one-hour figure or that an\ninteractive agent session longer than an hour is out of scope.\n\n## Acceptance\n\nADR 0008, or a successor ADR that supersedes it, records the one-hour attachment lifetime and the no-re-attach rule together with the consumer they were chosen for.\n\n## Notes\n\nIf re-attach is wanted, it is a contract change (a new attachment state and refusal) and needs a\nsuccessor bundle.\n"},"args":{"changes":{"body":"# Story: The session attachment lifetime is an accepted decision\n\n## Context\n\nA session attachment lives at most one hour (`crates/substrate-daemon/src/app/sessions.rs:70`);\nwhen that expires, or on any disconnect or protocol error, the process tree is killed\n(`sessions.rs:1301`) and the one-shot attachment right is consumed\n(`crates/substrate-store/src/sessions.rs`, `claim_pipe_session_attachment_inner`). No re-attach\nexists. ADR 0008 states the kill-on-loss rule; nothing states the one-hour figure or that an\ninteractive agent session longer than an hour is out of scope.\n\n## Acceptance\n\nADR 0008, or a successor ADR that supersedes it, records the one-hour attachment lifetime and the no-re-attach rule together with the consumer they were chosen for.\n\n## Notes\n\nIf re-attach is wanted, it is a contract change (a new attachment state and refusal) and needs a\nsuccessor bundle.\n"},"command":"update-entity","target":"01MEM0000000000000208"},"payload":{"actor":"human:timo","at":1788484425956,"causation":"cmd-protocol-artifact-body-section-story-session-attachment-lifetime-is-an-accepted-decision-1788484425956","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:session-attachment-lifetime-is-an-accepted-decision@3#0~6663bd8354722363","executor":null,"recorded_at":"2026-09-04T01:13:45Z"}}
{"entity":"story","version":1,"id":"session-attachment-lifetime-is-an-accepted-decision","revision":4,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"# Story: The session attachment lifetime is an accepted decision\n\n## Context\n\nA session attachment lives at most one hour (`crates/substrate-daemon/src/app/sessions.rs:70`);\nwhen that expires, or on any disconnect or protocol error, the process tree is killed\n(`sessions.rs:1301`) and the one-shot attachment right is consumed\n(`crates/substrate-store/src/sessions.rs`, `claim_pipe_session_attachment_inner`). No re-attach\nexists. ADR 0008 states the kill-on-loss rule; nothing states the one-hour figure or that an\ninteractive agent session longer than an hour is out of scope.\n\n## Acceptance\n\nADR 0008, or a successor ADR that supersedes it, records the one-hour attachment lifetime and the no-re-attach rule together with the consumer they were chosen for.\n\n## Notes\n\nIf re-attach is wanted instead, the ADR says so and a story is filed; that is a contract change (a new attachment state and refusal) and needs a successor bundle. `docs/design/05-streams-sessions-and-endpoints.md` and the published session capability document are brought into agreement with whatever the ADR records.\n"},"args":{"changes":{"body":"# Story: The session attachment lifetime is an accepted decision\n\n## Context\n\nA session attachment lives at most one hour (`crates/substrate-daemon/src/app/sessions.rs:70`);\nwhen that expires, or on any disconnect or protocol error, the process tree is killed\n(`sessions.rs:1301`) and the one-shot attachment right is consumed\n(`crates/substrate-store/src/sessions.rs`, `claim_pipe_session_attachment_inner`). No re-attach\nexists. ADR 0008 states the kill-on-loss rule; nothing states the one-hour figure or that an\ninteractive agent session longer than an hour is out of scope.\n\n## Acceptance\n\nADR 0008, or a successor ADR that supersedes it, records the one-hour attachment lifetime and the no-re-attach rule together with the consumer they were chosen for.\n\n## Notes\n\nIf re-attach is wanted instead, the ADR says so and a story is filed; that is a contract change (a new attachment state and refusal) and needs a successor bundle. `docs/design/05-streams-sessions-and-endpoints.md` and the published session capability document are brought into agreement with whatever the ADR records.\n"},"command":"update-entity","target":"01MEM0000000000000208"},"payload":{"actor":"human:timo","at":1788484426133,"causation":"cmd-protocol-artifact-body-section-story-session-attachment-lifetime-is-an-accepted-decision-1788484426133","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:session-attachment-lifetime-is-an-accepted-decision@4#0~9cefea7ff9b3d295","executor":null,"recorded_at":"2026-09-04T01:13:46Z"}}
{"entity":"story","version":1,"id":"backend-recheck-hashes-only-on-change","revision":5,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"# Story: Backend recheck hashes only on change\n\n## Context\n\nEvery exec admission calls `recheck_backend`, which calls `backend_binding`, which reads the\nwhole bubblewrap binary and computes its SHA-256 (`crates/substrate-host/src/process.rs:1753`;\n`probe.rs:267`). The binding already records device, inode and size. On a busy daemon this is one\nfile read and hash per exec start on the admission path.\n\n## Acceptance\n\nAn exec admission against a bubblewrap whose device, inode, size and modification time match the probed binding completes without opening the binary, proven by a test that counts the opens.\n\n## Notes\n\nKeep the full hash in the startup probe; the recheck compares metadata first and hashes only when it differs. The existing `exec.capability-stale` refusal for a replaced binary must keep holding; it is already covered by `stale_snapshot_refuses_before_backend_access` and is not this story's acceptance.\n\n## Parallel work\n\nThis story shares `crates/substrate-host/src/probe.rs` with story:backend-recheck-hashes-only-on-change, story:confined-processes-cannot-nest-user-namespaces, story:daemon-image-serves-exec-or-says-it-cannot, story:exec-oom-kills-the-whole-tree and story:seccomp-denies-af-vsock; three of them also share `crates/substrate-host/src/process.rs`. Work them one at a time, or in one wave by one implementor; `aep artifact waves` sequences them.\n"},"args":{"changes":{"body":"# Story: Backend recheck hashes only on change\n\n## Context\n\nEvery exec admission calls `recheck_backend`, which calls `backend_binding`, which reads the\nwhole bubblewrap binary and computes its SHA-256 (`crates/substrate-host/src/process.rs:1753`;\n`probe.rs:267`). The binding already records device, inode and size. On a busy daemon this is one\nfile read and hash per exec start on the admission path.\n\n## Acceptance\n\nAn exec admission against a bubblewrap whose device, inode, size and modification time match the probed binding completes without opening the binary, proven by a test that counts the opens.\n\n## Notes\n\nKeep the full hash in the startup probe; the recheck compares metadata first and hashes only when it differs. The existing `exec.capability-stale` refusal for a replaced binary must keep holding; it is already covered by `stale_snapshot_refuses_before_backend_access` and is not this story's acceptance.\n\n## Parallel work\n\nThis story shares `crates/substrate-host/src/probe.rs` with story:backend-recheck-hashes-only-on-change, story:confined-processes-cannot-nest-user-namespaces, story:daemon-image-serves-exec-or-says-it-cannot, story:exec-oom-kills-the-whole-tree and story:seccomp-denies-af-vsock; three of them also share `crates/substrate-host/src/process.rs`. Work them one at a time, or in one wave by one implementor; `aep artifact waves` sequences them.\n"},"command":"update-entity","target":"01MEM0000000000000055"},"payload":{"actor":"human:timo","at":1788484426300,"causation":"cmd-protocol-artifact-body-section-story-backend-recheck-hashes-only-on-change-1788484426300","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:backend-recheck-hashes-only-on-change@5#0~123066e1e3101462","executor":null,"recorded_at":"2026-09-04T01:13:46Z"}}
{"entity":"story","version":1,"id":"confined-processes-cannot-nest-user-namespaces","revision":5,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"# Story: Confined processes cannot nest user namespaces\n\n## Context\n\nThe exec argv passes `--unshare-user` and not `--disable-userns`\n(`crates/substrate-host/src/process.rs:1813-1838`); the three probe argv lists in `probe.rs`,\n`pty.rs` and `egress.rs` match it. The installed bubblewrap 0.11.2 lists `--disable-userns` and\n`--assert-userns-disabled` (`bwrap --help`). A confined process can therefore create nested user\nnamespaces, which is the entry point of most unprivileged kernel privilege escalations. The\ncurrent seccomp profile does not block `unshare` or `clone` with `CLONE_NEWUSER`\n(`crates/substrate-host/src/seccomp.rs:70-99`).\n\n## Acceptance\n\nInside an admitted exec on this host, `unshare -U` fails, observed by a delegated-lane case.\n\n## Notes\n\nInvariant 3: a bubblewrap too old for `--disable-userns` is a named refusal, never a sandbox without it. The probe therefore passes `--assert-userns-disabled` and withholds the exec facts when it fails; that path is delivered in this story and checked by its own probe case, separate from the acceptance above.\n\n## Parallel work\n\nThis story shares `crates/substrate-host/src/probe.rs` with story:backend-recheck-hashes-only-on-change, story:confined-processes-cannot-nest-user-namespaces, story:daemon-image-serves-exec-or-says-it-cannot, story:exec-oom-kills-the-whole-tree and story:seccomp-denies-af-vsock; three of them also share `crates/substrate-host/src/process.rs`. Work them one at a time, or in one wave by one implementor; `aep artifact waves` sequences them.\n"},"args":{"changes":{"body":"# Story: Confined processes cannot nest user namespaces\n\n## Context\n\nThe exec argv passes `--unshare-user` and not `--disable-userns`\n(`crates/substrate-host/src/process.rs:1813-1838`); the three probe argv lists in `probe.rs`,\n`pty.rs` and `egress.rs` match it. The installed bubblewrap 0.11.2 lists `--disable-userns` and\n`--assert-userns-disabled` (`bwrap --help`). A confined process can therefore create nested user\nnamespaces, which is the entry point of most unprivileged kernel privilege escalations. The\ncurrent seccomp profile does not block `unshare` or `clone` with `CLONE_NEWUSER`\n(`crates/substrate-host/src/seccomp.rs:70-99`).\n\n## Acceptance\n\nInside an admitted exec on this host, `unshare -U` fails, observed by a delegated-lane case.\n\n## Notes\n\nInvariant 3: a bubblewrap too old for `--disable-userns` is a named refusal, never a sandbox without it. The probe therefore passes `--assert-userns-disabled` and withholds the exec facts when it fails; that path is delivered in this story and checked by its own probe case, separate from the acceptance above.\n\n## Parallel work\n\nThis story shares `crates/substrate-host/src/probe.rs` with story:backend-recheck-hashes-only-on-change, story:confined-processes-cannot-nest-user-namespaces, story:daemon-image-serves-exec-or-says-it-cannot, story:exec-oom-kills-the-whole-tree and story:seccomp-denies-af-vsock; three of them also share `crates/substrate-host/src/process.rs`. Work them one at a time, or in one wave by one implementor; `aep artifact waves` sequences them.\n"},"command":"update-entity","target":"01MEM0000000000000064"},"payload":{"actor":"human:timo","at":1788484426468,"causation":"cmd-protocol-artifact-body-section-story-confined-processes-cannot-nest-user-namespaces-1788484426468","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:confined-processes-cannot-nest-user-namespaces@5#0~3eeb2c6f3fe0128b","executor":null,"recorded_at":"2026-09-04T01:13:46Z"}}
{"entity":"story","version":1,"id":"daemon-image-serves-exec-or-says-it-cannot","revision":5,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"# Story: The daemon image serves exec or says it cannot\n\n## Context\n\nExec capability requires the configured bubblewrap binary and `/usr/bin/socat` on the host\n(`crates/substrate-host/src/probe.rs:319-320`; documented at `README.md:280`). The published\ndaemon image is `gcr.io/distroless/cc-debian12:nonroot` (`Dockerfile:12`) and installs neither,\nnor `/usr/bin/env`, which the sandbox argv execs (`process.rs`, `command`). Inferred from the base\nimage, not pulled: that image can only answer `exec.sandbox-unavailable`, and a reader of the\nrelease notes is not told so.\n\n## Acceptance\n\nThe GHCR release notes and `README.md` state whether the published daemon image serves exec and, when it does not, name what a host must add.\n\n## Notes\n\nThe statement holds either way: if a later story ships bubblewrap, socat and coreutils in the image and runs the delegated lane against it, the documents say the image serves exec. The `socat` dependency exists only to prove the seccomp `AF_UNIX` refusal; a small Rust probe child would remove it.\n\n## Parallel work\n\nThis story shares `crates/substrate-host/src/probe.rs` with story:backend-recheck-hashes-only-on-change, story:confined-processes-cannot-nest-user-namespaces, story:daemon-image-serves-exec-or-says-it-cannot, story:exec-oom-kills-the-whole-tree and story:seccomp-denies-af-vsock; three of them also share `crates/substrate-host/src/process.rs`. Work them one at a time, or in one wave by one implementor; `aep artifact waves` sequences them.\n"},"args":{"changes":{"body":"# Story: The daemon image serves exec or says it cannot\n\n## Context\n\nExec capability requires the configured bubblewrap binary and `/usr/bin/socat` on the host\n(`crates/substrate-host/src/probe.rs:319-320`; documented at `README.md:280`). The published\ndaemon image is `gcr.io/distroless/cc-debian12:nonroot` (`Dockerfile:12`) and installs neither,\nnor `/usr/bin/env`, which the sandbox argv execs (`process.rs`, `command`). Inferred from the base\nimage, not pulled: that image can only answer `exec.sandbox-unavailable`, and a reader of the\nrelease notes is not told so.\n\n## Acceptance\n\nThe GHCR release notes and `README.md` state whether the published daemon image serves exec and, when it does not, name what a host must add.\n\n## Notes\n\nThe statement holds either way: if a later story ships bubblewrap, socat and coreutils in the image and runs the delegated lane against it, the documents say the image serves exec. The `socat` dependency exists only to prove the seccomp `AF_UNIX` refusal; a small Rust probe child would remove it.\n\n## Parallel work\n\nThis story shares `crates/substrate-host/src/probe.rs` with story:backend-recheck-hashes-only-on-change, story:confined-processes-cannot-nest-user-namespaces, story:daemon-image-serves-exec-or-says-it-cannot, story:exec-oom-kills-the-whole-tree and story:seccomp-denies-af-vsock; three of them also share `crates/substrate-host/src/process.rs`. Work them one at a time, or in one wave by one implementor; `aep artifact waves` sequences them.\n"},"command":"update-entity","target":"01MEM0000000000000079"},"payload":{"actor":"human:timo","at":1788484426646,"causation":"cmd-protocol-artifact-body-section-story-daemon-image-serves-exec-or-says-it-cannot-1788484426646","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:daemon-image-serves-exec-or-says-it-cannot@5#0~7797dd874f54da90","executor":null,"recorded_at":"2026-09-04T01:13:46Z"}}
{"entity":"story","version":1,"id":"exec-oom-kills-the-whole-tree","revision":5,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"# Story: An exec OOM kills the whole tree\n\n## Context\n\n`Cgroup::create` writes `pids.max`, `memory.max`, `memory.swap.max=0` and `cpu.max`\n(`crates/substrate-host/src/process.rs:2848-2860`) and not `memory.oom.group=1`. The kernel then\nkills one process on OOM and the rest of the tree continues, which is not the whole-tree semantics\nthe safety envelope names (`AGENTS.md` § Safety envelope). The `exec.memory-limit` refusal is\nderived from `memory_oom_kills` in the terminal usage, which exists only when the client asked for\n`measurements` (`process.rs`, `run_child` and `record_resource_bound`), so an OOM on a run without\nmeasurements is reported as an ordinary exit. `cpu.max` is clamped to one CPU\n(`process.rs:2858`), which is a documented choice worth stating on the capability fact.\n\n## Acceptance\n\nAn exec whose child exceeds `memory.max` ends with no process left in its cgroup, observed in the delegated lane.\n\n## Notes\n\n`memory.oom.group` is a cgroup v2 file present on every kernel the probe already requires; probe it and withhold the exec facts where it is absent. Naming `exec.memory-limit` on the observation when `measurements` was not requested is a second change on `record_resource_bound`, delivered in this story and checked by its own case rather than by the acceptance above.\n\n## Parallel work\n\nThis story shares `crates/substrate-host/src/probe.rs` with story:backend-recheck-hashes-only-on-change, story:confined-processes-cannot-nest-user-namespaces, story:daemon-image-serves-exec-or-says-it-cannot, story:exec-oom-kills-the-whole-tree and story:seccomp-denies-af-vsock; three of them also share `crates/substrate-host/src/process.rs`. Work them one at a time, or in one wave by one implementor; `aep artifact waves` sequences them.\n"},"args":{"changes":{"body":"# Story: An exec OOM kills the whole tree\n\n## Context\n\n`Cgroup::create` writes `pids.max`, `memory.max`, `memory.swap.max=0` and `cpu.max`\n(`crates/substrate-host/src/process.rs:2848-2860`) and not `memory.oom.group=1`. The kernel then\nkills one process on OOM and the rest of the tree continues, which is not the whole-tree semantics\nthe safety envelope names (`AGENTS.md` § Safety envelope). The `exec.memory-limit` refusal is\nderived from `memory_oom_kills` in the terminal usage, which exists only when the client asked for\n`measurements` (`process.rs`, `run_child` and `record_resource_bound`), so an OOM on a run without\nmeasurements is reported as an ordinary exit. `cpu.max` is clamped to one CPU\n(`process.rs:2858`), which is a documented choice worth stating on the capability fact.\n\n## Acceptance\n\nAn exec whose child exceeds `memory.max` ends with no process left in its cgroup, observed in the delegated lane.\n\n## Notes\n\n`memory.oom.group` is a cgroup v2 file present on every kernel the probe already requires; probe it and withhold the exec facts where it is absent. Naming `exec.memory-limit` on the observation when `measurements` was not requested is a second change on `record_resource_bound`, delivered in this story and checked by its own case rather than by the acceptance above.\n\n## Parallel work\n\nThis story shares `crates/substrate-host/src/probe.rs` with story:backend-recheck-hashes-only-on-change, story:confined-processes-cannot-nest-user-namespaces, story:daemon-image-serves-exec-or-says-it-cannot, story:exec-oom-kills-the-whole-tree and story:seccomp-denies-af-vsock; three of them also share `crates/substrate-host/src/process.rs`. Work them one at a time, or in one wave by one implementor; `aep artifact waves` sequences them.\n"},"command":"update-entity","target":"01MEM0000000000000103"},"payload":{"actor":"human:timo","at":1788484426816,"causation":"cmd-protocol-artifact-body-section-story-exec-oom-kills-the-whole-tree-1788484426816","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:exec-oom-kills-the-whole-tree@5#0~b0ac53a3afc7cbb2","executor":null,"recorded_at":"2026-09-04T01:13:46Z"}}
{"entity":"story","version":1,"id":"seccomp-denies-af-vsock","revision":3,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"# Story: seccomp denies AF_VSOCK\n\n## Context\n\nThe seccomp profile denies `io_uring_setup`, `socket(AF_UNIX, …)` and\n`socketpair(AF_UNIX, SOCK_DGRAM)` and allows every other socket family\n(`crates/substrate-host/src/seccomp.rs:70-99`). `AF_VSOCK` is not confined by a network namespace,\nso on a virtual-machine host that exposes a vsock transport a confined process can open a socket to\nthe hypervisor side while `--unshare-net` holds. The observed development nodes are EKS virtual\nmachines (`STATUS.md` § Current state). Inferred from the kernel's vsock namespace model; not\ntested on such a host.\n\n## Acceptance\n\n`socket(AF_VSOCK, SOCK_STREAM, 0)` inside an admitted exec fails with `EACCES`, the probe's sentinel\ncase includes it, and `seccomp::tests` carries the same case beside the existing `AF_UNIX` ones.\n\n## Notes\n\nOne more `jump` against `libc::AF_VSOCK` in `filters`. Consider the same for `AF_NETLINK` and\n`AF_PACKET` and record in the story why each is or is not refused.\n\n## Parallel work\n\nThis story shares `crates/substrate-host/src/probe.rs` with story:backend-recheck-hashes-only-on-change, story:confined-processes-cannot-nest-user-namespaces, story:daemon-image-serves-exec-or-says-it-cannot, story:exec-oom-kills-the-whole-tree and story:seccomp-denies-af-vsock; three of them also share `crates/substrate-host/src/process.rs`. Work them one at a time, or in one wave by one implementor; `aep artifact waves` sequences them.\n"},"args":{"changes":{"body":"# Story: seccomp denies AF_VSOCK\n\n## Context\n\nThe seccomp profile denies `io_uring_setup`, `socket(AF_UNIX, …)` and\n`socketpair(AF_UNIX, SOCK_DGRAM)` and allows every other socket family\n(`crates/substrate-host/src/seccomp.rs:70-99`). `AF_VSOCK` is not confined by a network namespace,\nso on a virtual-machine host that exposes a vsock transport a confined process can open a socket to\nthe hypervisor side while `--unshare-net` holds. The observed development nodes are EKS virtual\nmachines (`STATUS.md` § Current state). Inferred from the kernel's vsock namespace model; not\ntested on such a host.\n\n## Acceptance\n\n`socket(AF_VSOCK, SOCK_STREAM, 0)` inside an admitted exec fails with `EACCES`, the probe's sentinel\ncase includes it, and `seccomp::tests` carries the same case beside the existing `AF_UNIX` ones.\n\n## Notes\n\nOne more `jump` against `libc::AF_VSOCK` in `filters`. Consider the same for `AF_NETLINK` and\n`AF_PACKET` and record in the story why each is or is not refused.\n\n## Parallel work\n\nThis story shares `crates/substrate-host/src/probe.rs` with story:backend-recheck-hashes-only-on-change, story:confined-processes-cannot-nest-user-namespaces, story:daemon-image-serves-exec-or-says-it-cannot, story:exec-oom-kills-the-whole-tree and story:seccomp-denies-af-vsock; three of them also share `crates/substrate-host/src/process.rs`. Work them one at a time, or in one wave by one implementor; `aep artifact waves` sequences them.\n"},"command":"update-entity","target":"01MEM0000000000000202"},"payload":{"actor":"human:timo","at":1788484426984,"causation":"cmd-protocol-artifact-body-section-story-seccomp-denies-af-vsock-1788484426984","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:seccomp-denies-af-vsock@3#0~e079d7234ce12ced","executor":null,"recorded_at":"2026-09-04T01:13:46Z"}}
{"entity":"story","version":1,"id":"line-patch-edits-apply-in-line-order","revision":3,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"# Story: Line patch edits apply in line order\n\n## Context\n\n`apply_line_patch` validates edit ranges on a sorted copy but applies the edits by iterating the\nrequest in reverse **input** order (`crates/substrate-host/src/fs.rs:1226`). An edit list that is\nnot ascending shifts later edits onto the wrong lines. Reproduced outside the crate with the\nfunction copied verbatim: on a six-line file, `[replace line 5, insert before line 2]` yields\n`L1 A B L2 X L4 L5 L6` while the ascending order of the same edits yields `L1 A B L2 L3 L4 X L6`.\nThe published schema `contracts/substrate-wire/0.15.0/schemas/inputs/workspace-file-patch-v2.json`\nstates no ordering rule for `edits`, and no released vector exercises two edits.\n\n## Acceptance\n\n`POST /v2/workspaces/{id}/file-patches/{path}` with the same set of non-overlapping edits produces\nbyte-identical files for every permutation of the list, proven by a `fs::tests` case that submits\ntwo permutations of a two-edit patch.\n\n## Notes\n\nSmallest fix: sort by `(start_line, kind)` descending before the apply loop. Alternative that\nneeds a successor bundle: refuse a non-ascending list with a new `workspace.patch-order` code.\nEither way the released bytes of `0.15.0` do not change.\n\n## Parallel work\n\nThis story shares `crates/substrate-host/src/fs.rs` with story:directory-listing-survives-special-entries and story:line-patch-edits-apply-in-line-order; the two touch different functions (`apply_line_patch` versus `list_directory`/`walk_tree`) but land on one file, so they are worked in sequence, not at once.\n"},"args":{"changes":{"body":"# Story: Line patch edits apply in line order\n\n## Context\n\n`apply_line_patch` validates edit ranges on a sorted copy but applies the edits by iterating the\nrequest in reverse **input** order (`crates/substrate-host/src/fs.rs:1226`). An edit list that is\nnot ascending shifts later edits onto the wrong lines. Reproduced outside the crate with the\nfunction copied verbatim: on a six-line file, `[replace line 5, insert before line 2]` yields\n`L1 A B L2 X L4 L5 L6` while the ascending order of the same edits yields `L1 A B L2 L3 L4 X L6`.\nThe published schema `contracts/substrate-wire/0.15.0/schemas/inputs/workspace-file-patch-v2.json`\nstates no ordering rule for `edits`, and no released vector exercises two edits.\n\n## Acceptance\n\n`POST /v2/workspaces/{id}/file-patches/{path}` with the same set of non-overlapping edits produces\nbyte-identical files for every permutation of the list, proven by a `fs::tests` case that submits\ntwo permutations of a two-edit patch.\n\n## Notes\n\nSmallest fix: sort by `(start_line, kind)` descending before the apply loop. Alternative that\nneeds a successor bundle: refuse a non-ascending list with a new `workspace.patch-order` code.\nEither way the released bytes of `0.15.0` do not change.\n\n## Parallel work\n\nThis story shares `crates/substrate-host/src/fs.rs` with story:directory-listing-survives-special-entries and story:line-patch-edits-apply-in-line-order; the two touch different functions (`apply_line_patch` versus `list_directory`/`walk_tree`) but land on one file, so they are worked in sequence, not at once.\n"},"command":"update-entity","target":"01MEM0000000000000139"},"payload":{"actor":"human:timo","at":1788484427153,"causation":"cmd-protocol-artifact-body-section-story-line-patch-edits-apply-in-line-order-1788484427153","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:line-patch-edits-apply-in-line-order@3#0~8bb1a23bc1d0907d","executor":null,"recorded_at":"2026-09-04T01:13:47Z"}}
{"entity":"story","version":1,"id":"directory-listing-survives-special-entries","revision":3,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"# Story: Directory listing survives special entries\n\n## Context\n\n`list_directory` and `walk_tree` return `workspace.path-escape` for any entry that is not a\nregular file, directory or symlink, and for any name that is not UTF-8\n(`crates/substrate-host/src/fs.rs:768`, `:773`, `:803`, `:813`). A confined process can create a\nFIFO (`mkfifo`) or a byte-named file in `/workspace`; afterwards every directory page of that\ndirectory and the whole `GET /v2/workspaces/{id}/tree` refuse. The refusal names an escape that did\nnot happen, and the client cannot list its own workspace. Code reading only; not reproduced at\nruntime.\n\n## Acceptance\n\nA directory holding a FIFO, a socket and a non-UTF-8 name lists its regular entries with `200` on\nboth the v1 directory page and the v2 tree, and a test creates those three entries and reads both.\n\n## Notes\n\nThe wire `DirectoryEntryKind` has three members (`crates/substrate-wire/src/lib.rs:1045`). Naming\na fourth is a contract change and needs a successor bundle under invariant 6; skipping such\nentries with the existing `truncated` flag on the tree, and omitting them from the page, needs\nnone. The story body of whichever path is taken records the choice before code (invariant 8).\n\n## Parallel work\n\nThis story shares `crates/substrate-host/src/fs.rs` with story:directory-listing-survives-special-entries and story:line-patch-edits-apply-in-line-order; the two touch different functions (`apply_line_patch` versus `list_directory`/`walk_tree`) but land on one file, so they are worked in sequence, not at once.\n"},"args":{"changes":{"body":"# Story: Directory listing survives special entries\n\n## Context\n\n`list_directory` and `walk_tree` return `workspace.path-escape` for any entry that is not a\nregular file, directory or symlink, and for any name that is not UTF-8\n(`crates/substrate-host/src/fs.rs:768`, `:773`, `:803`, `:813`). A confined process can create a\nFIFO (`mkfifo`) or a byte-named file in `/workspace`; afterwards every directory page of that\ndirectory and the whole `GET /v2/workspaces/{id}/tree` refuse. The refusal names an escape that did\nnot happen, and the client cannot list its own workspace. Code reading only; not reproduced at\nruntime.\n\n## Acceptance\n\nA directory holding a FIFO, a socket and a non-UTF-8 name lists its regular entries with `200` on\nboth the v1 directory page and the v2 tree, and a test creates those three entries and reads both.\n\n## Notes\n\nThe wire `DirectoryEntryKind` has three members (`crates/substrate-wire/src/lib.rs:1045`). Naming\na fourth is a contract change and needs a successor bundle under invariant 6; skipping such\nentries with the existing `truncated` flag on the tree, and omitting them from the page, needs\nnone. The story body of whichever path is taken records the choice before code (invariant 8).\n\n## Parallel work\n\nThis story shares `crates/substrate-host/src/fs.rs` with story:directory-listing-survives-special-entries and story:line-patch-edits-apply-in-line-order; the two touch different functions (`apply_line_patch` versus `list_directory`/`walk_tree`) but land on one file, so they are worked in sequence, not at once.\n"},"command":"update-entity","target":"01MEM0000000000000085"},"payload":{"actor":"human:timo","at":1788484427325,"causation":"cmd-protocol-artifact-body-section-story-directory-listing-survives-special-entries-1788484427325","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:directory-listing-survives-special-entries@3#0~80640eb260907d1a","executor":null,"recorded_at":"2026-09-04T01:13:47Z"}}
{"entity":"story","version":1,"id":"metrics-streams-are-bounded","revision":5,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"# Story: Metrics streams are bounded\n\n## Context\n\n`GET /v1/metrics/stream` upgrades to a WebSocket with no permit, no lifetime and the library's\ndefault frame and message bounds (`crates/substrate-daemon/src/app/metrics.rs:75-90`). Each open\nstream runs `load_exec_usage` once a second, which is an `observe_exec` plus a `put_exec` SQLite\nwrite (`metrics.rs:98-` and `:215-`), until the exec ends, up to the 24 h exec timeout. Event\nstreams are capped at 64 global and 4 per subject (`app/events.rs:47-48`) and pipe attachments at\n32 (`app/sessions.rs:63`); metrics streams at nothing. One authenticated uid can open as many as\nthe kernel allows.\n\n## Acceptance\n\nOpening one more metrics stream than a published per-subject cap answers `429` with a named `exhausted` refusal.\n\n## Notes\n\nReuse the `EventStreamLimits` shape and `EventStreamPolicy` bounds; the metrics stream is the one upgrade that has none of them. The lifetime and the client-message bound arrive with the same policy struct and are checked by their own cases, not by the acceptance above.\n\n## Parallel work\n\nThis story shares `crates/substrate-daemon/src/app/service.rs` with story:metrics-streams-are-bounded and story:lease-cleanup-reads-exec-state-only; the two touch different functions (stream limits on `App` versus `cleanup_expired`) but land on one file, so they are worked in sequence, not at once.\n"},"args":{"changes":{"body":"# Story: Metrics streams are bounded\n\n## Context\n\n`GET /v1/metrics/stream` upgrades to a WebSocket with no permit, no lifetime and the library's\ndefault frame and message bounds (`crates/substrate-daemon/src/app/metrics.rs:75-90`). Each open\nstream runs `load_exec_usage` once a second, which is an `observe_exec` plus a `put_exec` SQLite\nwrite (`metrics.rs:98-` and `:215-`), until the exec ends, up to the 24 h exec timeout. Event\nstreams are capped at 64 global and 4 per subject (`app/events.rs:47-48`) and pipe attachments at\n32 (`app/sessions.rs:63`); metrics streams at nothing. One authenticated uid can open as many as\nthe kernel allows.\n\n## Acceptance\n\nOpening one more metrics stream than a published per-subject cap answers `429` with a named `exhausted` refusal.\n\n## Notes\n\nReuse the `EventStreamLimits` shape and `EventStreamPolicy` bounds; the metrics stream is the one upgrade that has none of them. The lifetime and the client-message bound arrive with the same policy struct and are checked by their own cases, not by the acceptance above.\n\n## Parallel work\n\nThis story shares `crates/substrate-daemon/src/app/service.rs` with story:metrics-streams-are-bounded and story:lease-cleanup-reads-exec-state-only; the two touch different functions (stream limits on `App` versus `cleanup_expired`) but land on one file, so they are worked in sequence, not at once.\n"},"command":"update-entity","target":"01MEM0000000000000145"},"payload":{"actor":"human:timo","at":1788484427494,"causation":"cmd-protocol-artifact-body-section-story-metrics-streams-are-bounded-1788484427494","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:metrics-streams-are-bounded@5#0~146b67d3be447e82","executor":null,"recorded_at":"2026-09-04T01:13:47Z"}}
{"entity":"story","version":1,"id":"lease-cleanup-reads-exec-state-only","revision":3,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"# Story: Lease cleanup reads exec state only\n\n## Context\n\nWorkspace lease cleanup calls `execs_for_workspace`, which loads every exec row of the workspace\nthrough `load_exec`, including the `stdout` and `stderr` blobs\n(`crates/substrate-store/src/execs.rs:545`). The caller reads only `resource.state` and\n`resource.id` (`crates/substrate-daemon/src/app/service.rs:688-700`). The bounds allow 2048 execs\nper subject (`crates/substrate-wire/src/lib.rs:28`) with 1 MiB per stream (`:22`), so one expiring\nworkspace can pull 4 GiB into daemon memory in one sweep. Code reading only.\n\n## Acceptance\n\nA workspace lease expiry over the maximum exec count completes with the daemon's resident memory\ngrowth bounded by the exec metadata alone, proven by a store query that returns `(id, state)` and\na test asserting no output column is read on that path.\n\n## Notes\n\n`workspace_has_nonterminal_execs` in `store/workspaces.rs` already parses only `resource_json`;\nthe same shape serves here.\n\n## Parallel work\n\nThis story shares `crates/substrate-daemon/src/app/service.rs` with story:metrics-streams-are-bounded and story:lease-cleanup-reads-exec-state-only; the two touch different functions (stream limits on `App` versus `cleanup_expired`) but land on one file, so they are worked in sequence, not at once.\n"},"args":{"changes":{"body":"# Story: Lease cleanup reads exec state only\n\n## Context\n\nWorkspace lease cleanup calls `execs_for_workspace`, which loads every exec row of the workspace\nthrough `load_exec`, including the `stdout` and `stderr` blobs\n(`crates/substrate-store/src/execs.rs:545`). The caller reads only `resource.state` and\n`resource.id` (`crates/substrate-daemon/src/app/service.rs:688-700`). The bounds allow 2048 execs\nper subject (`crates/substrate-wire/src/lib.rs:28`) with 1 MiB per stream (`:22`), so one expiring\nworkspace can pull 4 GiB into daemon memory in one sweep. Code reading only.\n\n## Acceptance\n\nA workspace lease expiry over the maximum exec count completes with the daemon's resident memory\ngrowth bounded by the exec metadata alone, proven by a store query that returns `(id, state)` and\na test asserting no output column is read on that path.\n\n## Notes\n\n`workspace_has_nonterminal_execs` in `store/workspaces.rs` already parses only `resource_json`;\nthe same shape serves here.\n\n## Parallel work\n\nThis story shares `crates/substrate-daemon/src/app/service.rs` with story:metrics-streams-are-bounded and story:lease-cleanup-reads-exec-state-only; the two touch different functions (stream limits on `App` versus `cleanup_expired`) but land on one file, so they are worked in sequence, not at once.\n"},"command":"update-entity","target":"01MEM0000000000000133"},"payload":{"actor":"human:timo","at":1788484427695,"causation":"cmd-protocol-artifact-body-section-story-lease-cleanup-reads-exec-state-only-1788484427695","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:lease-cleanup-reads-exec-state-only@3#0~960ca317d98ddccd","executor":null,"recorded_at":"2026-09-04T01:13:47Z"}}
{"entity":"story","version":1,"id":"unattached-claimed-session-is-contained","revision":2,"type":"aep.relation.create/v1","from_state":"draft","to_state":"draft","changed":{"relations":[{"decomposes":"epic:review-2026-09-03-findings"},{"informed_by":"story:upgraded-connections-keep-their-permit"}]},"args":{"command":"create-relation","kind":"informed_by","source":"01MEM0000000000000220","target":"01MEM0000000000000223"},"payload":{"actor":"human:timo","at":1788484427872,"causation":"cmd-story-unattached-claimed-session-is-contained-informed_by-story-upgraded-connections-keep-their-permit","change":{"change":"related","relation":"informed_by","target":"story:upgraded-connections-keep-their-permit"},"correlation":"protocol-artifact-relate","event_id":"story:unattached-claimed-session-is-contained@2#0~7dc912c9aeecf236","executor":null,"recorded_at":"2026-09-04T01:13:47Z"}}
{"entity":"story","version":1,"id":"unattached-claimed-session-is-contained","revision":3,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"# Story: A claimed but unattached session is contained\n\n## Context\n\n`pipe_session_attach` consumes the durable attachment claim before the WebSocket upgrade completes\n(`crates/substrate-daemon/src/app/sessions.rs`, `claim_pipe_session_attachment` then\n`ws.on_upgrade`). If the client drops between the `101` and the upgrade, the `on_upgrade` closure\nnever runs, the permit is dropped, the claim stays `attached`, and the process runs unattached\nuntil its lease or timeout ends it. No test covers this path.\n\n## Acceptance\n\nA client that drops after the attachment claim and before the upgrade leaves no running process\nwithin one maintenance tick, and the session reads as terminal with a named refusal; a test proves\nit.\n\n## Notes\n\nEither terminate the session when the upgrade future fails, or claim inside the upgrade task and refuse the upgrade when the claim fails. This is the same hyper upgrade hand-off seam story:upgraded-connections-keep-their-permit observes first (`informed_by`); read its observation before choosing, and if that story moves the permit into the upgrade task, put the claim there too so the two changes are one edit to `on_upgrade`.\n"},"args":{"changes":{"body":"# Story: A claimed but unattached session is contained\n\n## Context\n\n`pipe_session_attach` consumes the durable attachment claim before the WebSocket upgrade completes\n(`crates/substrate-daemon/src/app/sessions.rs`, `claim_pipe_session_attachment` then\n`ws.on_upgrade`). If the client drops between the `101` and the upgrade, the `on_upgrade` closure\nnever runs, the permit is dropped, the claim stays `attached`, and the process runs unattached\nuntil its lease or timeout ends it. No test covers this path.\n\n## Acceptance\n\nA client that drops after the attachment claim and before the upgrade leaves no running process\nwithin one maintenance tick, and the session reads as terminal with a named refusal; a test proves\nit.\n\n## Notes\n\nEither terminate the session when the upgrade future fails, or claim inside the upgrade task and refuse the upgrade when the claim fails. This is the same hyper upgrade hand-off seam story:upgraded-connections-keep-their-permit observes first (`informed_by`); read its observation before choosing, and if that story moves the permit into the upgrade task, put the claim there too so the two changes are one edit to `on_upgrade`.\n"},"command":"update-entity","target":"01MEM0000000000000220"},"payload":{"actor":"human:timo","at":1788484428042,"causation":"cmd-protocol-artifact-body-section-story-unattached-claimed-session-is-contained-1788484428042","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:unattached-claimed-session-is-contained@3#0~a9aaf8d7563a32cf","executor":null,"recorded_at":"2026-09-04T01:13:48Z"}}
{"entity":"story","version":1,"id":"upgraded-connections-keep-their-permit","revision":3,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"# Story: Upgraded connections keep their permit\n\n## Context\n\nEvery listener wraps the hyper connection future in `enforce_connection_lifetime`, which owns the\nper-uid and global `ConnectionPermit` (`crates/substrate-daemon/src/runtime.rs:596`, `:919`,\n`:1084`; limits at `:53-56`). hyper 1.11.1 (`Cargo.lock:1141`) resolves an upgradeable connection\nfuture when it hands the socket to the upgrade, so the permit is released the moment a WebSocket\nupgrade succeeds and the upgraded socket runs in a task the transport budget no longer counts.\nInferred from the library's semantics and consistent with the separate 1 h attachment lifetime the\nsession code needs (`app/sessions.rs:70`); not observed at runtime.\n\n## Acceptance\n\nA test holds the per-uid connection budget with upgraded WebSockets and shows the next plain HTTP\nconnection from that uid is refused at accept, or the story records the observation that the\npermit already survives the upgrade and closes with no code change.\n\n## Notes\n\nFirst step is the observation, not the fix. If the permit is released, either move it into the upgrade task or give every upgrade its own bounded permit as event streams and pipe attachments already have. story:unattached-claimed-session-is-contained lands on the same `on_upgrade` seam for the session attach route and is `informed_by` this story; whichever remedy is chosen here decides where that story puts its claim.\n"},"args":{"changes":{"body":"# Story: Upgraded connections keep their permit\n\n## Context\n\nEvery listener wraps the hyper connection future in `enforce_connection_lifetime`, which owns the\nper-uid and global `ConnectionPermit` (`crates/substrate-daemon/src/runtime.rs:596`, `:919`,\n`:1084`; limits at `:53-56`). hyper 1.11.1 (`Cargo.lock:1141`) resolves an upgradeable connection\nfuture when it hands the socket to the upgrade, so the permit is released the moment a WebSocket\nupgrade succeeds and the upgraded socket runs in a task the transport budget no longer counts.\nInferred from the library's semantics and consistent with the separate 1 h attachment lifetime the\nsession code needs (`app/sessions.rs:70`); not observed at runtime.\n\n## Acceptance\n\nA test holds the per-uid connection budget with upgraded WebSockets and shows the next plain HTTP\nconnection from that uid is refused at accept, or the story records the observation that the\npermit already survives the upgrade and closes with no code change.\n\n## Notes\n\nFirst step is the observation, not the fix. If the permit is released, either move it into the upgrade task or give every upgrade its own bounded permit as event streams and pipe attachments already have. story:unattached-claimed-session-is-contained lands on the same `on_upgrade` seam for the session attach route and is `informed_by` this story; whichever remedy is chosen here decides where that story puts its claim.\n"},"command":"update-entity","target":"01MEM0000000000000223"},"payload":{"actor":"human:timo","at":1788484428211,"causation":"cmd-protocol-artifact-body-section-story-upgraded-connections-keep-their-permit-1788484428211","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:upgraded-connections-keep-their-permit@3#0~22730c10d7f7aa15","executor":null,"recorded_at":"2026-09-04T01:13:48Z"}}
{"entity":"epic","version":1,"id":"review-2026-09-03-findings","revision":2,"type":"aep.evidence.record/v1","from_state":"draft","to_state":"draft","changed":{},"args":{"command":"record-evidence","kind":"review_outcome","outcome":"fixed","review":"review-result:review-2026-09-03-acceptance-round-1","source":"review-result:review-2026-09-03-acceptance-round-1","target":"01MEM0000000000000025"},"payload":{"actor":"human:timo","at":1788484446000,"causation":"cmd-evidence-epic-review-2026-09-03-findings-review_outcome-1788484446517","change":{"change":"evidence","kind":"review_outcome","outcome":"fixed","review":"review-result:review-2026-09-03-acceptance-round-1","source":"review-result:review-2026-09-03-acceptance-round-1"},"correlation":"protocol-artifact-evidence","event_id":"epic:review-2026-09-03-findings@2#0~bee134e94c20197d","executor":null,"recorded_at":"2026-09-04T01:14:06Z"}}
{"entity":"story","version":1,"id":"backend-recheck-hashes-only-on-change","revision":5,"type":"aep.evidence.record/v1","from_state":"draft","to_state":"draft","changed":{},"args":{"command":"record-evidence","kind":"review_outcome","outcome":"fixed","review":"review-result:review-2026-09-03-acceptance-round-1","source":"review-result:review-2026-09-03-acceptance-round-1","target":"01MEM0000000000000055"},"payload":{"actor":"human:timo","at":1788484446000,"causation":"cmd-evidence-story-backend-recheck-hashes-only-on-change-review_outcome-1788484446703","change":{"change":"evidence","kind":"review_outcome","outcome":"fixed","review":"review-result:review-2026-09-03-acceptance-round-1","source":"review-result:review-2026-09-03-acceptance-round-1"},"correlation":"protocol-artifact-evidence","event_id":"story:backend-recheck-hashes-only-on-change@5#0~4004b4f221da0832","executor":null,"recorded_at":"2026-09-04T01:14:06Z"}}
{"entity":"story","version":1,"id":"confined-processes-cannot-nest-user-namespaces","revision":5,"type":"aep.evidence.record/v1","from_state":"draft","to_state":"draft","changed":{},"args":{"command":"record-evidence","kind":"review_outcome","outcome":"fixed","review":"review-result:review-2026-09-03-acceptance-round-1","source":"review-result:review-2026-09-03-acceptance-round-1","target":"01MEM0000000000000064"},"payload":{"actor":"human:timo","at":1788484446000,"causation":"cmd-evidence-story-confined-processes-cannot-nest-user-namespaces-review_outcome-1788484446879","change":{"change":"evidence","kind":"review_outcome","outcome":"fixed","review":"review-result:review-2026-09-03-acceptance-round-1","source":"review-result:review-2026-09-03-acceptance-round-1"},"correlation":"protocol-artifact-evidence","event_id":"story:confined-processes-cannot-nest-user-namespaces@5#0~4283ce0c5a4c3c2a","executor":null,"recorded_at":"2026-09-04T01:14:06Z"}}
{"entity":"story","version":1,"id":"daemon-image-serves-exec-or-says-it-cannot","revision":5,"type":"aep.evidence.record/v1","from_state":"draft","to_state":"draft","changed":{},"args":{"command":"record-evidence","kind":"review_outcome","outcome":"fixed","review":"review-result:review-2026-09-03-acceptance-round-1","source":"review-result:review-2026-09-03-acceptance-round-1","target":"01MEM0000000000000079"},"payload":{"actor":"human:timo","at":1788484447000,"causation":"cmd-evidence-story-daemon-image-serves-exec-or-says-it-cannot-review_outcome-1788484447062","change":{"change":"evidence","kind":"review_outcome","outcome":"fixed","review":"review-result:review-2026-09-03-acceptance-round-1","source":"review-result:review-2026-09-03-acceptance-round-1"},"correlation":"protocol-artifact-evidence","event_id":"story:daemon-image-serves-exec-or-says-it-cannot@5#0~fe026903a6e9d3fc","executor":null,"recorded_at":"2026-09-04T01:14:07Z"}}
{"entity":"story","version":1,"id":"exec-oom-kills-the-whole-tree","revision":5,"type":"aep.evidence.record/v1","from_state":"draft","to_state":"draft","changed":{},"args":{"command":"record-evidence","kind":"review_outcome","outcome":"fixed","review":"review-result:review-2026-09-03-acceptance-round-1","source":"review-result:review-2026-09-03-acceptance-round-1","target":"01MEM0000000000000103"},"payload":{"actor":"human:timo","at":1788484447000,"causation":"cmd-evidence-story-exec-oom-kills-the-whole-tree-review_outcome-1788484447237","change":{"change":"evidence","kind":"review_outcome","outcome":"fixed","review":"review-result:review-2026-09-03-acceptance-round-1","source":"review-result:review-2026-09-03-acceptance-round-1"},"correlation":"protocol-artifact-evidence","event_id":"story:exec-oom-kills-the-whole-tree@5#0~64b943aeabba3ef2","executor":null,"recorded_at":"2026-09-04T01:14:07Z"}}
{"entity":"story","version":1,"id":"hosted-admission-reuses-identity-authority","revision":4,"type":"aep.evidence.record/v1","from_state":"draft","to_state":"draft","changed":{},"args":{"command":"record-evidence","kind":"review_outcome","outcome":"fixed","review":"review-result:review-2026-09-03-acceptance-round-1","source":"review-result:review-2026-09-03-acceptance-round-1","target":"01MEM0000000000000121"},"payload":{"actor":"human:timo","at":1788484447000,"causation":"cmd-evidence-story-hosted-admission-reuses-identity-authority-review_outcome-1788484447412","change":{"change":"evidence","kind":"review_outcome","outcome":"fixed","review":"review-result:review-2026-09-03-acceptance-round-1","source":"review-result:review-2026-09-03-acceptance-round-1"},"correlation":"protocol-artifact-evidence","event_id":"story:hosted-admission-reuses-identity-authority@4#0~614f299f43d745be","executor":null,"recorded_at":"2026-09-04T01:14:07Z"}}
{"entity":"story","version":1,"id":"metrics-streams-are-bounded","revision":5,"type":"aep.evidence.record/v1","from_state":"draft","to_state":"draft","changed":{},"args":{"command":"record-evidence","kind":"review_outcome","outcome":"fixed","review":"review-result:review-2026-09-03-acceptance-round-1","source":"review-result:review-2026-09-03-acceptance-round-1","target":"01MEM0000000000000145"},"payload":{"actor":"human:timo","at":1788484447000,"causation":"cmd-evidence-story-metrics-streams-are-bounded-review_outcome-1788484447587","change":{"change":"evidence","kind":"review_outcome","outcome":"fixed","review":"review-result:review-2026-09-03-acceptance-round-1","source":"review-result:review-2026-09-03-acceptance-round-1"},"correlation":"protocol-artifact-evidence","event_id":"story:metrics-streams-are-bounded@5#0~e991b58de6d6d87c","executor":null,"recorded_at":"2026-09-04T01:14:07Z"}}
{"entity":"story","version":1,"id":"session-attachment-lifetime-is-an-accepted-decision","revision":4,"type":"aep.evidence.record/v1","from_state":"draft","to_state":"draft","changed":{},"args":{"command":"record-evidence","kind":"review_outcome","outcome":"fixed","review":"review-result:review-2026-09-03-acceptance-round-1","source":"review-result:review-2026-09-03-acceptance-round-1","target":"01MEM0000000000000208"},"payload":{"actor":"human:timo","at":1788484447000,"causation":"cmd-evidence-story-session-attachment-lifetime-is-an-accepted-decision-review_outcome-1788484447761","change":{"change":"evidence","kind":"review_outcome","outcome":"fixed","review":"review-result:review-2026-09-03-acceptance-round-1","source":"review-result:review-2026-09-03-acceptance-round-1"},"correlation":"protocol-artifact-evidence","event_id":"story:session-attachment-lifetime-is-an-accepted-decision@4#0~a6aa327993db3672","executor":null,"recorded_at":"2026-09-04T01:14:07Z"}}
{"entity":"story","version":1,"id":"backend-recheck-hashes-only-on-change","revision":5,"type":"aep.evidence.record/v1","from_state":"draft","to_state":"draft","changed":{},"args":{"command":"record-evidence","kind":"review_outcome","outcome":"fixed","review":"review-result:review-2026-09-03-parallel-safety-round-1","source":"review-result:review-2026-09-03-parallel-safety-round-1","target":"01MEM0000000000000055"},"payload":{"actor":"human:timo","at":1788484447000,"causation":"cmd-evidence-story-backend-recheck-hashes-only-on-change-review_outcome-1788484447934","change":{"change":"evidence","kind":"review_outcome","outcome":"fixed","review":"review-result:review-2026-09-03-parallel-safety-round-1","source":"review-result:review-2026-09-03-parallel-safety-round-1"},"correlation":"protocol-artifact-evidence","event_id":"story:backend-recheck-hashes-only-on-change@5#0~d0e51e0b36199b0a","executor":null,"recorded_at":"2026-09-04T01:14:07Z"}}
{"entity":"story","version":1,"id":"line-patch-edits-apply-in-line-order","revision":3,"type":"aep.evidence.record/v1","from_state":"draft","to_state":"draft","changed":{},"args":{"command":"record-evidence","kind":"review_outcome","outcome":"fixed","review":"review-result:review-2026-09-03-parallel-safety-round-1","source":"review-result:review-2026-09-03-parallel-safety-round-1","target":"01MEM0000000000000139"},"payload":{"actor":"human:timo","at":1788484448000,"causation":"cmd-evidence-story-line-patch-edits-apply-in-line-order-review_outcome-1788484448109","change":{"change":"evidence","kind":"review_outcome","outcome":"fixed","review":"review-result:review-2026-09-03-parallel-safety-round-1","source":"review-result:review-2026-09-03-parallel-safety-round-1"},"correlation":"protocol-artifact-evidence","event_id":"story:line-patch-edits-apply-in-line-order@3#0~398b8de0d162ab87","executor":null,"recorded_at":"2026-09-04T01:14:08Z"}}
{"entity":"story","version":1,"id":"metrics-streams-are-bounded","revision":5,"type":"aep.evidence.record/v1","from_state":"draft","to_state":"draft","changed":{},"args":{"command":"record-evidence","kind":"review_outcome","outcome":"fixed","review":"review-result:review-2026-09-03-parallel-safety-round-1","source":"review-result:review-2026-09-03-parallel-safety-round-1","target":"01MEM0000000000000145"},"payload":{"actor":"human:timo","at":1788484448000,"causation":"cmd-evidence-story-metrics-streams-are-bounded-review_outcome-1788484448284","change":{"change":"evidence","kind":"review_outcome","outcome":"fixed","review":"review-result:review-2026-09-03-parallel-safety-round-1","source":"review-result:review-2026-09-03-parallel-safety-round-1"},"correlation":"protocol-artifact-evidence","event_id":"story:metrics-streams-are-bounded@5#0~4e1421c6153de3b4","executor":null,"recorded_at":"2026-09-04T01:14:08Z"}}
{"entity":"story","version":1,"id":"unattached-claimed-session-is-contained","revision":3,"type":"aep.evidence.record/v1","from_state":"draft","to_state":"draft","changed":{},"args":{"command":"record-evidence","kind":"review_outcome","outcome":"fixed","review":"review-result:review-2026-09-03-design-round-1","source":"review-result:review-2026-09-03-design-round-1","target":"01MEM0000000000000220"},"payload":{"actor":"human:timo","at":1788484448000,"causation":"cmd-evidence-story-unattached-claimed-session-is-contained-review_outcome-1788484448456","change":{"change":"evidence","kind":"review_outcome","outcome":"fixed","review":"review-result:review-2026-09-03-design-round-1","source":"review-result:review-2026-09-03-design-round-1"},"correlation":"protocol-artifact-evidence","event_id":"story:unattached-claimed-session-is-contained@3#0~9fc6986ebebfbd60","executor":null,"recorded_at":"2026-09-04T01:14:08Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-design-round-2","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"active","changed":{"body":"approve\n\n**What I read:** the epic and all 14 stories bearing `decomposes: epic:review-2026-09-03-findings` (`aep artifact show` on all 15 ids, full body), `aep artifact relations` (13 relation kinds), the full `aep artifact graph` dump (183 lines, all 78 artifacts in the store — walked every edge, not only the ones touching the 14 ids), and `aep artifact validate`. Within the 14-story set the only edges are 14 `decomposes → epic:review-2026-09-03-findings` and one `informed_by`: `story:unattached-claimed-session-is-contained → story:upgraded-connections-keep-their-permit`. No `depends_on` among the 14, so no cycle and no serialising chain in the declared graph.\n\nThe round-1 finding is resolved. `story:unattached-claimed-session-is-contained` now carries the `informed_by` edge and its Notes name the shared seam explicitly (\"This is the same hyper upgrade hand-off seam story:upgraded-connections-keep-their-permit observes first (`informed_by`); read its observation before choosing, and if that story moves the permit into the upgrade task, put the claim there too so the two changes are one edit to `on_upgrade`\"). `story:upgraded-connections-keep-their-permit`'s Notes reciprocally name the same story and seam (\"story:unattached-claimed-session-is-contained lands on the same `on_upgrade` seam for the session attach route and is `informed_by` this story; whichever remedy is chosen here decides where that story puts its claim\"). Both halves now disclose the coupling, its direction, and the possible single-edit outcome, and an edge records it — the defect round 1 named (no edge, no cross-reference) no longer holds.\n\nI checked the remaining 12 stories for the same three defects (split abstraction, hidden dependency, serialising chain) and found none: each declares exactly one outcome, one acceptance, and one file-set; the five stories sharing `probe.rs` (`story:confined-processes-cannot-nest-user-namespaces`, `story:seccomp-denies-af-vsock`, `story:exec-oom-kills-the-whole-tree`, `story:daemon-image-serves-exec-or-says-it-cannot`, `story:backend-recheck-hashes-only-on-change`) each add an independently-testable, self-contained check rather than two halves of one check, and the pairs sharing `fs.rs` and `service.rs` touch disjoint functions with independent acceptances.\n\n**What I could not establish:** whether `informed_by` (versus `depends_on`) is the exactly-right relation kind for the upgraded-connections/unattached-claimed-session pair — the body text (\"read its observation before choosing\") maps closely to the CLI's stated meaning of `informed_by`, so I did not treat the choice as a defect, but I cannot rule out a stronger ordering constraint once the remedy is actually picked. Also out of my lane, unchanged from round 1: whether the five `probe.rs` stories and the `fs.rs`/`service.rs` pairs may be worked concurrently is plan-critic-parallel-safety's question, not mine — each story's own \"Parallel work\" section already asserts they are sequenced, which is a claim that critic should check, not one I re-judge here.\n\n```findings\n[]\n```\n","tags":["critic","review"],"title":"Design critic (aep-planning:plan-critic-design), round 2"},"args":{"command":"create-entity","data":{"body":"approve\n\n**What I read:** the epic and all 14 stories bearing `decomposes: epic:review-2026-09-03-findings` (`aep artifact show` on all 15 ids, full body), `aep artifact relations` (13 relation kinds), the full `aep artifact graph` dump (183 lines, all 78 artifacts in the store — walked every edge, not only the ones touching the 14 ids), and `aep artifact validate`. Within the 14-story set the only edges are 14 `decomposes → epic:review-2026-09-03-findings` and one `informed_by`: `story:unattached-claimed-session-is-contained → story:upgraded-connections-keep-their-permit`. No `depends_on` among the 14, so no cycle and no serialising chain in the declared graph.\n\nThe round-1 finding is resolved. `story:unattached-claimed-session-is-contained` now carries the `informed_by` edge and its Notes name the shared seam explicitly (\"This is the same hyper upgrade hand-off seam story:upgraded-connections-keep-their-permit observes first (`informed_by`); read its observation before choosing, and if that story moves the permit into the upgrade task, put the claim there too so the two changes are one edit to `on_upgrade`\"). `story:upgraded-connections-keep-their-permit`'s Notes reciprocally name the same story and seam (\"story:unattached-claimed-session-is-contained lands on the same `on_upgrade` seam for the session attach route and is `informed_by` this story; whichever remedy is chosen here decides where that story puts its claim\"). Both halves now disclose the coupling, its direction, and the possible single-edit outcome, and an edge records it — the defect round 1 named (no edge, no cross-reference) no longer holds.\n\nI checked the remaining 12 stories for the same three defects (split abstraction, hidden dependency, serialising chain) and found none: each declares exactly one outcome, one acceptance, and one file-set; the five stories sharing `probe.rs` (`story:confined-processes-cannot-nest-user-namespaces`, `story:seccomp-denies-af-vsock`, `story:exec-oom-kills-the-whole-tree`, `story:daemon-image-serves-exec-or-says-it-cannot`, `story:backend-recheck-hashes-only-on-change`) each add an independently-testable, self-contained check rather than two halves of one check, and the pairs sharing `fs.rs` and `service.rs` touch disjoint functions with independent acceptances.\n\n**What I could not establish:** whether `informed_by` (versus `depends_on`) is the exactly-right relation kind for the upgraded-connections/unattached-claimed-session pair — the body text (\"read its observation before choosing\") maps closely to the CLI's stated meaning of `informed_by`, so I did not treat the choice as a defect, but I cannot rule out a stronger ordering constraint once the remedy is actually picked. Also out of my lane, unchanged from round 1: whether the five `probe.rs` stories and the `fs.rs`/`service.rs` pairs may be worked concurrently is plan-critic-parallel-safety's question, not mine — each story's own \"Parallel work\" section already asserts they are sequenced, which is a claim that critic should check, not one I re-judge here.\n\n```findings\n[]\n```\n","status":"active","tags":["critic","review"],"title":"Design critic (aep-planning:plan-critic-design), round 2"},"entity_type":"aep.review-result/v1","locator":"ep://planning/store/review-result/review-2026-09-03-design-round-2"},"payload":{"actor":"human:timo","at":1788484585043,"causation":"cmd-new-review-result-review-2026-09-03-design-round-2","change":{"change":"created","status":"active"},"correlation":"protocol-artifact-new","event_id":"review-result:review-2026-09-03-design-round-2@1#0~7da21b605d05d8c1","executor":null,"recorded_at":"2026-09-04T01:16:25Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-design-round-2","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000581","target":"01MEM0000000000000025"},"payload":{"actor":"human:timo","at":1788484585065,"causation":"cmd-review-result-review-2026-09-03-design-round-2-reviews-epic-review-2026-09-03-findings","change":{"change":"related","relation":"reviews","target":"epic:review-2026-09-03-findings"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-design-round-2@1#0~429bc0870f13f8cf","executor":null,"recorded_at":"2026-09-04T01:16:25Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-design-round-2","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000581","target":"01MEM0000000000000139"},"payload":{"actor":"human:timo","at":1788484585074,"causation":"cmd-review-result-review-2026-09-03-design-round-2-reviews-story-line-patch-edits-apply-in-line-order","change":{"change":"related","relation":"reviews","target":"story:line-patch-edits-apply-in-line-order"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-design-round-2@1#0~7a09947b8e7db8b9","executor":null,"recorded_at":"2026-09-04T01:16:25Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-design-round-2","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000581","target":"01MEM0000000000000085"},"payload":{"actor":"human:timo","at":1788484585085,"causation":"cmd-review-result-review-2026-09-03-design-round-2-reviews-story-directory-listing-survives-special-entries","change":{"change":"related","relation":"reviews","target":"story:directory-listing-survives-special-entries"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-design-round-2@1#0~6d99a0546cdcbcbd","executor":null,"recorded_at":"2026-09-04T01:16:25Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-design-round-2","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000581","target":"01MEM0000000000000145"},"payload":{"actor":"human:timo","at":1788484585093,"causation":"cmd-review-result-review-2026-09-03-design-round-2-reviews-story-metrics-streams-are-bounded","change":{"change":"related","relation":"reviews","target":"story:metrics-streams-are-bounded"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-design-round-2@1#0~d88770845098271a","executor":null,"recorded_at":"2026-09-04T01:16:25Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-design-round-2","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"},{"reviews":"story:upgraded-connections-keep-their-permit"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000581","target":"01MEM0000000000000223"},"payload":{"actor":"human:timo","at":1788484585101,"causation":"cmd-review-result-review-2026-09-03-design-round-2-reviews-story-upgraded-connections-keep-their-permit","change":{"change":"related","relation":"reviews","target":"story:upgraded-connections-keep-their-permit"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-design-round-2@1#0~c9e60a081f701bfb","executor":null,"recorded_at":"2026-09-04T01:16:25Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-design-round-2","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"},{"reviews":"story:upgraded-connections-keep-their-permit"},{"reviews":"story:lease-cleanup-reads-exec-state-only"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000581","target":"01MEM0000000000000133"},"payload":{"actor":"human:timo","at":1788484585110,"causation":"cmd-review-result-review-2026-09-03-design-round-2-reviews-story-lease-cleanup-reads-exec-state-only","change":{"change":"related","relation":"reviews","target":"story:lease-cleanup-reads-exec-state-only"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-design-round-2@1#0~46029a7b71015a93","executor":null,"recorded_at":"2026-09-04T01:16:25Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-design-round-2","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"},{"reviews":"story:upgraded-connections-keep-their-permit"},{"reviews":"story:lease-cleanup-reads-exec-state-only"},{"reviews":"story:confined-processes-cannot-nest-user-namespaces"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000581","target":"01MEM0000000000000064"},"payload":{"actor":"human:timo","at":1788484585118,"causation":"cmd-review-result-review-2026-09-03-design-round-2-reviews-story-confined-processes-cannot-nest-user-namespaces","change":{"change":"related","relation":"reviews","target":"story:confined-processes-cannot-nest-user-namespaces"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-design-round-2@1#0~719bd9a7a9578db4","executor":null,"recorded_at":"2026-09-04T01:16:25Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-design-round-2","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"},{"reviews":"story:upgraded-connections-keep-their-permit"},{"reviews":"story:lease-cleanup-reads-exec-state-only"},{"reviews":"story:confined-processes-cannot-nest-user-namespaces"},{"reviews":"story:seccomp-denies-af-vsock"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000581","target":"01MEM0000000000000202"},"payload":{"actor":"human:timo","at":1788484585127,"causation":"cmd-review-result-review-2026-09-03-design-round-2-reviews-story-seccomp-denies-af-vsock","change":{"change":"related","relation":"reviews","target":"story:seccomp-denies-af-vsock"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-design-round-2@1#0~9b2d26f89f0635ea","executor":null,"recorded_at":"2026-09-04T01:16:25Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-design-round-2","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"},{"reviews":"story:upgraded-connections-keep-their-permit"},{"reviews":"story:lease-cleanup-reads-exec-state-only"},{"reviews":"story:confined-processes-cannot-nest-user-namespaces"},{"reviews":"story:seccomp-denies-af-vsock"},{"reviews":"story:exec-oom-kills-the-whole-tree"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000581","target":"01MEM0000000000000103"},"payload":{"actor":"human:timo","at":1788484585136,"causation":"cmd-review-result-review-2026-09-03-design-round-2-reviews-story-exec-oom-kills-the-whole-tree","change":{"change":"related","relation":"reviews","target":"story:exec-oom-kills-the-whole-tree"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-design-round-2@1#0~a1d22a63b23bd08c","executor":null,"recorded_at":"2026-09-04T01:16:25Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-design-round-2","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"},{"reviews":"story:upgraded-connections-keep-their-permit"},{"reviews":"story:lease-cleanup-reads-exec-state-only"},{"reviews":"story:confined-processes-cannot-nest-user-namespaces"},{"reviews":"story:seccomp-denies-af-vsock"},{"reviews":"story:exec-oom-kills-the-whole-tree"},{"reviews":"story:session-attachment-lifetime-is-an-accepted-decision"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000581","target":"01MEM0000000000000208"},"payload":{"actor":"human:timo","at":1788484585145,"causation":"cmd-review-result-review-2026-09-03-design-round-2-reviews-story-session-attachment-lifetime-is-an-accepted-decision","change":{"change":"related","relation":"reviews","target":"story:session-attachment-lifetime-is-an-accepted-decision"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-design-round-2@1#0~f1d8bcf8d02053cc","executor":null,"recorded_at":"2026-09-04T01:16:25Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-design-round-2","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"},{"reviews":"story:upgraded-connections-keep-their-permit"},{"reviews":"story:lease-cleanup-reads-exec-state-only"},{"reviews":"story:confined-processes-cannot-nest-user-namespaces"},{"reviews":"story:seccomp-denies-af-vsock"},{"reviews":"story:exec-oom-kills-the-whole-tree"},{"reviews":"story:session-attachment-lifetime-is-an-accepted-decision"},{"reviews":"story:hosted-admission-reuses-identity-authority"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000581","target":"01MEM0000000000000121"},"payload":{"actor":"human:timo","at":1788484585153,"causation":"cmd-review-result-review-2026-09-03-design-round-2-reviews-story-hosted-admission-reuses-identity-authority","change":{"change":"related","relation":"reviews","target":"story:hosted-admission-reuses-identity-authority"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-design-round-2@1#0~a53c44731a1ec9c0","executor":null,"recorded_at":"2026-09-04T01:16:25Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-design-round-2","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"},{"reviews":"story:upgraded-connections-keep-their-permit"},{"reviews":"story:lease-cleanup-reads-exec-state-only"},{"reviews":"story:confined-processes-cannot-nest-user-namespaces"},{"reviews":"story:seccomp-denies-af-vsock"},{"reviews":"story:exec-oom-kills-the-whole-tree"},{"reviews":"story:session-attachment-lifetime-is-an-accepted-decision"},{"reviews":"story:hosted-admission-reuses-identity-authority"},{"reviews":"story:daemon-image-serves-exec-or-says-it-cannot"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000581","target":"01MEM0000000000000079"},"payload":{"actor":"human:timo","at":1788484585163,"causation":"cmd-review-result-review-2026-09-03-design-round-2-reviews-story-daemon-image-serves-exec-or-says-it-cannot","change":{"change":"related","relation":"reviews","target":"story:daemon-image-serves-exec-or-says-it-cannot"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-design-round-2@1#0~80e2fca169241d26","executor":null,"recorded_at":"2026-09-04T01:16:25Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-design-round-2","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"},{"reviews":"story:upgraded-connections-keep-their-permit"},{"reviews":"story:lease-cleanup-reads-exec-state-only"},{"reviews":"story:confined-processes-cannot-nest-user-namespaces"},{"reviews":"story:seccomp-denies-af-vsock"},{"reviews":"story:exec-oom-kills-the-whole-tree"},{"reviews":"story:session-attachment-lifetime-is-an-accepted-decision"},{"reviews":"story:hosted-admission-reuses-identity-authority"},{"reviews":"story:daemon-image-serves-exec-or-says-it-cannot"},{"reviews":"story:backend-recheck-hashes-only-on-change"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000581","target":"01MEM0000000000000055"},"payload":{"actor":"human:timo","at":1788484585173,"causation":"cmd-review-result-review-2026-09-03-design-round-2-reviews-story-backend-recheck-hashes-only-on-change","change":{"change":"related","relation":"reviews","target":"story:backend-recheck-hashes-only-on-change"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-design-round-2@1#0~73e0c0b0c7faa400","executor":null,"recorded_at":"2026-09-04T01:16:25Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-design-round-2","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"},{"reviews":"story:upgraded-connections-keep-their-permit"},{"reviews":"story:lease-cleanup-reads-exec-state-only"},{"reviews":"story:confined-processes-cannot-nest-user-namespaces"},{"reviews":"story:seccomp-denies-af-vsock"},{"reviews":"story:exec-oom-kills-the-whole-tree"},{"reviews":"story:session-attachment-lifetime-is-an-accepted-decision"},{"reviews":"story:hosted-admission-reuses-identity-authority"},{"reviews":"story:daemon-image-serves-exec-or-says-it-cannot"},{"reviews":"story:backend-recheck-hashes-only-on-change"},{"reviews":"story:events-table-has-one-index"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000581","target":"01MEM0000000000000100"},"payload":{"actor":"human:timo","at":1788484585182,"causation":"cmd-review-result-review-2026-09-03-design-round-2-reviews-story-events-table-has-one-index","change":{"change":"related","relation":"reviews","target":"story:events-table-has-one-index"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-design-round-2@1#0~86fbb363a2ca2699","executor":null,"recorded_at":"2026-09-04T01:16:25Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-design-round-2","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"},{"reviews":"story:upgraded-connections-keep-their-permit"},{"reviews":"story:lease-cleanup-reads-exec-state-only"},{"reviews":"story:confined-processes-cannot-nest-user-namespaces"},{"reviews":"story:seccomp-denies-af-vsock"},{"reviews":"story:exec-oom-kills-the-whole-tree"},{"reviews":"story:session-attachment-lifetime-is-an-accepted-decision"},{"reviews":"story:hosted-admission-reuses-identity-authority"},{"reviews":"story:daemon-image-serves-exec-or-says-it-cannot"},{"reviews":"story:backend-recheck-hashes-only-on-change"},{"reviews":"story:events-table-has-one-index"},{"reviews":"story:unattached-claimed-session-is-contained"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000581","target":"01MEM0000000000000220"},"payload":{"actor":"human:timo","at":1788484585190,"causation":"cmd-review-result-review-2026-09-03-design-round-2-reviews-story-unattached-claimed-session-is-contained","change":{"change":"related","relation":"reviews","target":"story:unattached-claimed-session-is-contained"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-design-round-2@1#0~c13ce5081a88af72","executor":null,"recorded_at":"2026-09-04T01:16:25Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-parallel-safety-round-2","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"active","changed":{"body":"approve\n\nWhat I read: all 14 stories under `decomposes: epic:review-2026-09-03-findings` (`aep artifact list --format json`), each via `aep artifact show <id>`, plus `aep artifact waves --format json` (computes scope-based collisions directly) and the round-1 record `review-result:review-2026-09-03-parallel-safety-round-1`. Verified the 7 shared file paths exist in the tree (`crates/substrate-host/src/{probe,process,fs}.rs`, `crates/substrate-daemon/src/app/service.rs`, `crates/substrate-daemon/src/app/metrics.rs`, `crates/substrate-store/src/execs.rs`, `crates/substrate-wire/src/lib.rs`), and confirmed with `grep -n on_upgrade` that the two session/upgrade stories do not in fact share a file. Surface established: 14 cited, 0 inferred, 0 unplaceable — all 14 appear in `waves`' scoped set, none in its `unassessed` list.\n\nFindings: none. `aep artifact waves` reports exactly 15 collisions across 3 groups (the 5-way `probe.rs`/`process.rs` cluster: story:backend-recheck-hashes-only-on-change, story:confined-processes-cannot-nest-user-namespaces, story:daemon-image-serves-exec-or-says-it-cannot, story:exec-oom-kills-the-whole-tree, story:seccomp-denies-af-vsock; the `fs.rs` pair story:line-patch-edits-apply-in-line-order / story:directory-listing-survives-special-entries; the `service.rs` pair story:metrics-streams-are-bounded / story:lease-cleanup-reads-exec-state-only). Each of these 9 stories now carries a `## Parallel work` section naming the exact shared path(s) and every colliding sibling, matching the tool's collision set pair-for-pair (including the \"three of them also share `process.rs`\" detail, which is accurate), and directing sequential work. This closes round 1's three findings (all recorded `fixed` in the round-1 outcomes). The remaining 5 stories have no collisions in `waves` and none introduce a new one.\n\nWhat I could not establish: none within my lane. Out of my lane, not affecting my verdict: story:unattached-claimed-session-is-contained and story:upgraded-connections-keep-their-permit describe themselves in prose as landing on \"the same `on_upgrade` seam\" and carry an `informed_by` relation — but their declared scopes are different files (`app/sessions.rs` vs `runtime.rs`), and `grep -n on_upgrade crates/substrate-daemon/src/runtime.rs` returns nothing, so there is no file-level collision; whether that conceptual coupling is handled correctly is a design-split question, not a parallel-safety one.\n\n```findings\n[]\n```\n","tags":["critic","review"],"title":"Parallel-safety critic (aep-planning:plan-critic-parallel-safety), round 2"},"args":{"command":"create-entity","data":{"body":"approve\n\nWhat I read: all 14 stories under `decomposes: epic:review-2026-09-03-findings` (`aep artifact list --format json`), each via `aep artifact show <id>`, plus `aep artifact waves --format json` (computes scope-based collisions directly) and the round-1 record `review-result:review-2026-09-03-parallel-safety-round-1`. Verified the 7 shared file paths exist in the tree (`crates/substrate-host/src/{probe,process,fs}.rs`, `crates/substrate-daemon/src/app/service.rs`, `crates/substrate-daemon/src/app/metrics.rs`, `crates/substrate-store/src/execs.rs`, `crates/substrate-wire/src/lib.rs`), and confirmed with `grep -n on_upgrade` that the two session/upgrade stories do not in fact share a file. Surface established: 14 cited, 0 inferred, 0 unplaceable — all 14 appear in `waves`' scoped set, none in its `unassessed` list.\n\nFindings: none. `aep artifact waves` reports exactly 15 collisions across 3 groups (the 5-way `probe.rs`/`process.rs` cluster: story:backend-recheck-hashes-only-on-change, story:confined-processes-cannot-nest-user-namespaces, story:daemon-image-serves-exec-or-says-it-cannot, story:exec-oom-kills-the-whole-tree, story:seccomp-denies-af-vsock; the `fs.rs` pair story:line-patch-edits-apply-in-line-order / story:directory-listing-survives-special-entries; the `service.rs` pair story:metrics-streams-are-bounded / story:lease-cleanup-reads-exec-state-only). Each of these 9 stories now carries a `## Parallel work` section naming the exact shared path(s) and every colliding sibling, matching the tool's collision set pair-for-pair (including the \"three of them also share `process.rs`\" detail, which is accurate), and directing sequential work. This closes round 1's three findings (all recorded `fixed` in the round-1 outcomes). The remaining 5 stories have no collisions in `waves` and none introduce a new one.\n\nWhat I could not establish: none within my lane. Out of my lane, not affecting my verdict: story:unattached-claimed-session-is-contained and story:upgraded-connections-keep-their-permit describe themselves in prose as landing on \"the same `on_upgrade` seam\" and carry an `informed_by` relation — but their declared scopes are different files (`app/sessions.rs` vs `runtime.rs`), and `grep -n on_upgrade crates/substrate-daemon/src/runtime.rs` returns nothing, so there is no file-level collision; whether that conceptual coupling is handled correctly is a design-split question, not a parallel-safety one.\n\n```findings\n[]\n```\n","status":"active","tags":["critic","review"],"title":"Parallel-safety critic (aep-planning:plan-critic-parallel-safety), round 2"},"entity_type":"aep.review-result/v1","locator":"ep://planning/store/review-result/review-2026-09-03-parallel-safety-round-2"},"payload":{"actor":"human:timo","at":1788484609375,"causation":"cmd-new-review-result-review-2026-09-03-parallel-safety-round-2","change":{"change":"created","status":"active"},"correlation":"protocol-artifact-new","event_id":"review-result:review-2026-09-03-parallel-safety-round-2@1#0~a4b2e5ad04b03e7a","executor":null,"recorded_at":"2026-09-04T01:16:49Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-parallel-safety-round-2","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000614","target":"01MEM0000000000000025"},"payload":{"actor":"human:timo","at":1788484609567,"causation":"cmd-review-result-review-2026-09-03-parallel-safety-round-2-reviews-epic-review-2026-09-03-findings","change":{"change":"related","relation":"reviews","target":"epic:review-2026-09-03-findings"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-parallel-safety-round-2@1#0~b33035ef81eb450e","executor":null,"recorded_at":"2026-09-04T01:16:49Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-parallel-safety-round-2","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000614","target":"01MEM0000000000000142"},"payload":{"actor":"human:timo","at":1788484609750,"causation":"cmd-review-result-review-2026-09-03-parallel-safety-round-2-reviews-story-line-patch-edits-apply-in-line-order","change":{"change":"related","relation":"reviews","target":"story:line-patch-edits-apply-in-line-order"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-parallel-safety-round-2@1#0~6dd52d1531e7545a","executor":null,"recorded_at":"2026-09-04T01:16:49Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-parallel-safety-round-2","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000614","target":"01MEM0000000000000088"},"payload":{"actor":"human:timo","at":1788484609930,"causation":"cmd-review-result-review-2026-09-03-parallel-safety-round-2-reviews-story-directory-listing-survives-special-entries","change":{"change":"related","relation":"reviews","target":"story:directory-listing-survives-special-entries"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-parallel-safety-round-2@1#0~978f99433d3d73e7","executor":null,"recorded_at":"2026-09-04T01:16:49Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-parallel-safety-round-2","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000614","target":"01MEM0000000000000148"},"payload":{"actor":"human:timo","at":1788484610098,"causation":"cmd-review-result-review-2026-09-03-parallel-safety-round-2-reviews-story-metrics-streams-are-bounded","change":{"change":"related","relation":"reviews","target":"story:metrics-streams-are-bounded"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-parallel-safety-round-2@1#0~c481c3156303253c","executor":null,"recorded_at":"2026-09-04T01:16:50Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-parallel-safety-round-2","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"},{"reviews":"story:upgraded-connections-keep-their-permit"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000614","target":"01MEM0000000000000226"},"payload":{"actor":"human:timo","at":1788484610330,"causation":"cmd-review-result-review-2026-09-03-parallel-safety-round-2-reviews-story-upgraded-connections-keep-their-permit","change":{"change":"related","relation":"reviews","target":"story:upgraded-connections-keep-their-permit"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-parallel-safety-round-2@1#0~40072fb2e1bf5eed","executor":null,"recorded_at":"2026-09-04T01:16:50Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-parallel-safety-round-2","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"},{"reviews":"story:upgraded-connections-keep-their-permit"},{"reviews":"story:lease-cleanup-reads-exec-state-only"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000614","target":"01MEM0000000000000136"},"payload":{"actor":"human:timo","at":1788484610374,"causation":"cmd-review-result-review-2026-09-03-parallel-safety-round-2-reviews-story-lease-cleanup-reads-exec-state-only","change":{"change":"related","relation":"reviews","target":"story:lease-cleanup-reads-exec-state-only"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-parallel-safety-round-2@1#0~af947f3ef3e6a875","executor":null,"recorded_at":"2026-09-04T01:16:50Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-parallel-safety-round-2","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"},{"reviews":"story:upgraded-connections-keep-their-permit"},{"reviews":"story:lease-cleanup-reads-exec-state-only"},{"reviews":"story:confined-processes-cannot-nest-user-namespaces"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000614","target":"01MEM0000000000000067"},"payload":{"actor":"human:timo","at":1788484610393,"causation":"cmd-review-result-review-2026-09-03-parallel-safety-round-2-reviews-story-confined-processes-cannot-nest-user-namespaces","change":{"change":"related","relation":"reviews","target":"story:confined-processes-cannot-nest-user-namespaces"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-parallel-safety-round-2@1#0~ff2f9410bd5dbe5c","executor":null,"recorded_at":"2026-09-04T01:16:50Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-parallel-safety-round-2","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"},{"reviews":"story:upgraded-connections-keep-their-permit"},{"reviews":"story:lease-cleanup-reads-exec-state-only"},{"reviews":"story:confined-processes-cannot-nest-user-namespaces"},{"reviews":"story:seccomp-denies-af-vsock"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000614","target":"01MEM0000000000000205"},"payload":{"actor":"human:timo","at":1788484610412,"causation":"cmd-review-result-review-2026-09-03-parallel-safety-round-2-reviews-story-seccomp-denies-af-vsock","change":{"change":"related","relation":"reviews","target":"story:seccomp-denies-af-vsock"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-parallel-safety-round-2@1#0~aa5576a0e2bfa1ca","executor":null,"recorded_at":"2026-09-04T01:16:50Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-parallel-safety-round-2","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"},{"reviews":"story:upgraded-connections-keep-their-permit"},{"reviews":"story:lease-cleanup-reads-exec-state-only"},{"reviews":"story:confined-processes-cannot-nest-user-namespaces"},{"reviews":"story:seccomp-denies-af-vsock"},{"reviews":"story:exec-oom-kills-the-whole-tree"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000614","target":"01MEM0000000000000106"},"payload":{"actor":"human:timo","at":1788484610429,"causation":"cmd-review-result-review-2026-09-03-parallel-safety-round-2-reviews-story-exec-oom-kills-the-whole-tree","change":{"change":"related","relation":"reviews","target":"story:exec-oom-kills-the-whole-tree"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-parallel-safety-round-2@1#0~ca40e13676664002","executor":null,"recorded_at":"2026-09-04T01:16:50Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-parallel-safety-round-2","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"},{"reviews":"story:upgraded-connections-keep-their-permit"},{"reviews":"story:lease-cleanup-reads-exec-state-only"},{"reviews":"story:confined-processes-cannot-nest-user-namespaces"},{"reviews":"story:seccomp-denies-af-vsock"},{"reviews":"story:exec-oom-kills-the-whole-tree"},{"reviews":"story:session-attachment-lifetime-is-an-accepted-decision"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000614","target":"01MEM0000000000000211"},"payload":{"actor":"human:timo","at":1788484610445,"causation":"cmd-review-result-review-2026-09-03-parallel-safety-round-2-reviews-story-session-attachment-lifetime-is-an-accepted-decision","change":{"change":"related","relation":"reviews","target":"story:session-attachment-lifetime-is-an-accepted-decision"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-parallel-safety-round-2@1#0~71ac3aa9e07e6b77","executor":null,"recorded_at":"2026-09-04T01:16:50Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-parallel-safety-round-2","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"},{"reviews":"story:upgraded-connections-keep-their-permit"},{"reviews":"story:lease-cleanup-reads-exec-state-only"},{"reviews":"story:confined-processes-cannot-nest-user-namespaces"},{"reviews":"story:seccomp-denies-af-vsock"},{"reviews":"story:exec-oom-kills-the-whole-tree"},{"reviews":"story:session-attachment-lifetime-is-an-accepted-decision"},{"reviews":"story:hosted-admission-reuses-identity-authority"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000614","target":"01MEM0000000000000124"},"payload":{"actor":"human:timo","at":1788484610461,"causation":"cmd-review-result-review-2026-09-03-parallel-safety-round-2-reviews-story-hosted-admission-reuses-identity-authority","change":{"change":"related","relation":"reviews","target":"story:hosted-admission-reuses-identity-authority"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-parallel-safety-round-2@1#0~3191ff47ca5a3aea","executor":null,"recorded_at":"2026-09-04T01:16:50Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-parallel-safety-round-2","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"},{"reviews":"story:upgraded-connections-keep-their-permit"},{"reviews":"story:lease-cleanup-reads-exec-state-only"},{"reviews":"story:confined-processes-cannot-nest-user-namespaces"},{"reviews":"story:seccomp-denies-af-vsock"},{"reviews":"story:exec-oom-kills-the-whole-tree"},{"reviews":"story:session-attachment-lifetime-is-an-accepted-decision"},{"reviews":"story:hosted-admission-reuses-identity-authority"},{"reviews":"story:daemon-image-serves-exec-or-says-it-cannot"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000614","target":"01MEM0000000000000082"},"payload":{"actor":"human:timo","at":1788484610483,"causation":"cmd-review-result-review-2026-09-03-parallel-safety-round-2-reviews-story-daemon-image-serves-exec-or-says-it-cannot","change":{"change":"related","relation":"reviews","target":"story:daemon-image-serves-exec-or-says-it-cannot"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-parallel-safety-round-2@1#0~cc6f93435b71c2cd","executor":null,"recorded_at":"2026-09-04T01:16:50Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-parallel-safety-round-2","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"},{"reviews":"story:upgraded-connections-keep-their-permit"},{"reviews":"story:lease-cleanup-reads-exec-state-only"},{"reviews":"story:confined-processes-cannot-nest-user-namespaces"},{"reviews":"story:seccomp-denies-af-vsock"},{"reviews":"story:exec-oom-kills-the-whole-tree"},{"reviews":"story:session-attachment-lifetime-is-an-accepted-decision"},{"reviews":"story:hosted-admission-reuses-identity-authority"},{"reviews":"story:daemon-image-serves-exec-or-says-it-cannot"},{"reviews":"story:backend-recheck-hashes-only-on-change"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000614","target":"01MEM0000000000000058"},"payload":{"actor":"human:timo","at":1788484610506,"causation":"cmd-review-result-review-2026-09-03-parallel-safety-round-2-reviews-story-backend-recheck-hashes-only-on-change","change":{"change":"related","relation":"reviews","target":"story:backend-recheck-hashes-only-on-change"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-parallel-safety-round-2@1#0~57664b2b261b6556","executor":null,"recorded_at":"2026-09-04T01:16:50Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-parallel-safety-round-2","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"},{"reviews":"story:upgraded-connections-keep-their-permit"},{"reviews":"story:lease-cleanup-reads-exec-state-only"},{"reviews":"story:confined-processes-cannot-nest-user-namespaces"},{"reviews":"story:seccomp-denies-af-vsock"},{"reviews":"story:exec-oom-kills-the-whole-tree"},{"reviews":"story:session-attachment-lifetime-is-an-accepted-decision"},{"reviews":"story:hosted-admission-reuses-identity-authority"},{"reviews":"story:daemon-image-serves-exec-or-says-it-cannot"},{"reviews":"story:backend-recheck-hashes-only-on-change"},{"reviews":"story:events-table-has-one-index"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000614","target":"01MEM0000000000000103"},"payload":{"actor":"human:timo","at":1788484610532,"causation":"cmd-review-result-review-2026-09-03-parallel-safety-round-2-reviews-story-events-table-has-one-index","change":{"change":"related","relation":"reviews","target":"story:events-table-has-one-index"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-parallel-safety-round-2@1#0~e5245c3685e27b1d","executor":null,"recorded_at":"2026-09-04T01:16:50Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-parallel-safety-round-2","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"},{"reviews":"story:upgraded-connections-keep-their-permit"},{"reviews":"story:lease-cleanup-reads-exec-state-only"},{"reviews":"story:confined-processes-cannot-nest-user-namespaces"},{"reviews":"story:seccomp-denies-af-vsock"},{"reviews":"story:exec-oom-kills-the-whole-tree"},{"reviews":"story:session-attachment-lifetime-is-an-accepted-decision"},{"reviews":"story:hosted-admission-reuses-identity-authority"},{"reviews":"story:daemon-image-serves-exec-or-says-it-cannot"},{"reviews":"story:backend-recheck-hashes-only-on-change"},{"reviews":"story:events-table-has-one-index"},{"reviews":"story:unattached-claimed-session-is-contained"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000614","target":"01MEM0000000000000223"},"payload":{"actor":"human:timo","at":1788484610556,"causation":"cmd-review-result-review-2026-09-03-parallel-safety-round-2-reviews-story-unattached-claimed-session-is-contained","change":{"change":"related","relation":"reviews","target":"story:unattached-claimed-session-is-contained"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-parallel-safety-round-2@1#0~26d6b4b2d3b4b852","executor":null,"recorded_at":"2026-09-04T01:16:50Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-scope-round-2","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"active","changed":{"body":"approve\n\n**What I read:** the epic body (`aep artifact show epic:review-2026-09-03-findings`, now carrying `## Done When`), the parent review register (`~/.cache/substrate-review/2026-09-03-substrate-in-depth-review.md`, all 14 findings and the \"smallest fixes\" list), the full body of all 14 stories carrying `decomposes: epic:review-2026-09-03-findings` (`aep artifact show story:<id>` × 14), the round-1 scope verdict (`review-result:review-2026-09-03-scope-round-1`, approved, 0 findings), `aep artifact graph` (confirms exactly 14 `decomposes` edges into the epic, no other artifact claims any of these findings), and `aep artifact validate` (reports `valid`; the two items it flags — 10 unscoped unrelated stories and round-1's prose-only findings block — are pre-existing and outside this set). I extracted 18 promises from the epic (the 14 explicit finding→story mappings plus 4 cross-cutting constraints: closure-by-test-or-recorded-decision now stated explicitly as `## Done When`; the frozen-contract-bundle exclusion story 2 may trigger; the named-refusal requirement for stories 6/7 under invariant 3; the observe-before-fix framing for story 4) and traced all 18 to a story: 18/18.\n\nChecked each of the 7 narrowed stories (`backend-recheck-hashes-only-on-change`, `confined-processes-cannot-nest-user-namespaces`, `daemon-image-serves-exec-or-says-it-cannot`, `exec-oom-kills-the-whole-tree`, `hosted-admission-reuses-identity-authority`, `metrics-streams-are-bounded`, `session-attachment-lifetime-is-an-accepted-decision`) against the finding-specific content moved into `## Notes`: in every case the moved clause is stated as still delivered (\"delivered in this story and checked by its own probe/case\", \"not this story's acceptance\" because it is \"already covered by\" an existing test, or \"the statement holds either way\") rather than dropped — none reads as a silent narrowing. Invariant 3's named-refusal requirement for stories 6/7 survives in Notes for `confined-processes-cannot-nest-user-namespaces` and was already in `seccomp-denies-af-vsock`'s single-statement acceptance (unchanged). No story's `## Notes` claims work the epic's Scope or Out of Scope did not ask for.\n\n**What I could not establish:** `exec-oom-kills-the-whole-tree`'s Context still carries finding 8's third clause — \"`cpu.max` is clamped to one CPU … which is a documented choice worth stating on the capability fact\" — and neither its Acceptance nor its Notes commits to that documentation step; this is the same ambiguity round 1 flagged and declined to raise as a finding, and it is unchanged by this round's edits, so I am carrying it forward as unresolved rather than as a new finding (`.engineering/planning/story/exec-oom-kills-the-whole-tree.md`, `## Context`).\n\n```findings\n[]\n```\n","tags":["critic","review"],"title":"Scope critic (aep-planning:plan-critic-scope), round 2"},"args":{"command":"create-entity","data":{"body":"approve\n\n**What I read:** the epic body (`aep artifact show epic:review-2026-09-03-findings`, now carrying `## Done When`), the parent review register (`~/.cache/substrate-review/2026-09-03-substrate-in-depth-review.md`, all 14 findings and the \"smallest fixes\" list), the full body of all 14 stories carrying `decomposes: epic:review-2026-09-03-findings` (`aep artifact show story:<id>` × 14), the round-1 scope verdict (`review-result:review-2026-09-03-scope-round-1`, approved, 0 findings), `aep artifact graph` (confirms exactly 14 `decomposes` edges into the epic, no other artifact claims any of these findings), and `aep artifact validate` (reports `valid`; the two items it flags — 10 unscoped unrelated stories and round-1's prose-only findings block — are pre-existing and outside this set). I extracted 18 promises from the epic (the 14 explicit finding→story mappings plus 4 cross-cutting constraints: closure-by-test-or-recorded-decision now stated explicitly as `## Done When`; the frozen-contract-bundle exclusion story 2 may trigger; the named-refusal requirement for stories 6/7 under invariant 3; the observe-before-fix framing for story 4) and traced all 18 to a story: 18/18.\n\nChecked each of the 7 narrowed stories (`backend-recheck-hashes-only-on-change`, `confined-processes-cannot-nest-user-namespaces`, `daemon-image-serves-exec-or-says-it-cannot`, `exec-oom-kills-the-whole-tree`, `hosted-admission-reuses-identity-authority`, `metrics-streams-are-bounded`, `session-attachment-lifetime-is-an-accepted-decision`) against the finding-specific content moved into `## Notes`: in every case the moved clause is stated as still delivered (\"delivered in this story and checked by its own probe/case\", \"not this story's acceptance\" because it is \"already covered by\" an existing test, or \"the statement holds either way\") rather than dropped — none reads as a silent narrowing. Invariant 3's named-refusal requirement for stories 6/7 survives in Notes for `confined-processes-cannot-nest-user-namespaces` and was already in `seccomp-denies-af-vsock`'s single-statement acceptance (unchanged). No story's `## Notes` claims work the epic's Scope or Out of Scope did not ask for.\n\n**What I could not establish:** `exec-oom-kills-the-whole-tree`'s Context still carries finding 8's third clause — \"`cpu.max` is clamped to one CPU … which is a documented choice worth stating on the capability fact\" — and neither its Acceptance nor its Notes commits to that documentation step; this is the same ambiguity round 1 flagged and declined to raise as a finding, and it is unchanged by this round's edits, so I am carrying it forward as unresolved rather than as a new finding (`.engineering/planning/story/exec-oom-kills-the-whole-tree.md`, `## Context`).\n\n```findings\n[]\n```\n","status":"active","tags":["critic","review"],"title":"Scope critic (aep-planning:plan-critic-scope), round 2"},"entity_type":"aep.review-result/v1","locator":"ep://planning/store/review-result/review-2026-09-03-scope-round-2"},"payload":{"actor":"human:timo","at":1788484635236,"causation":"cmd-new-review-result-review-2026-09-03-scope-round-2","change":{"change":"created","status":"active"},"correlation":"protocol-artifact-new","event_id":"review-result:review-2026-09-03-scope-round-2@1#0~199186ec236fc5c0","executor":null,"recorded_at":"2026-09-04T01:17:15Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-scope-round-2","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000647","target":"01MEM0000000000000025"},"payload":{"actor":"human:timo","at":1788484635402,"causation":"cmd-review-result-review-2026-09-03-scope-round-2-reviews-epic-review-2026-09-03-findings","change":{"change":"related","relation":"reviews","target":"epic:review-2026-09-03-findings"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-scope-round-2@1#0~e480dbf04d85ebf6","executor":null,"recorded_at":"2026-09-04T01:17:15Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-scope-round-2","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000647","target":"01MEM0000000000000145"},"payload":{"actor":"human:timo","at":1788484635427,"causation":"cmd-review-result-review-2026-09-03-scope-round-2-reviews-story-line-patch-edits-apply-in-line-order","change":{"change":"related","relation":"reviews","target":"story:line-patch-edits-apply-in-line-order"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-scope-round-2@1#0~c8c29c161499069f","executor":null,"recorded_at":"2026-09-04T01:17:15Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-scope-round-2","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000647","target":"01MEM0000000000000091"},"payload":{"actor":"human:timo","at":1788484635448,"causation":"cmd-review-result-review-2026-09-03-scope-round-2-reviews-story-directory-listing-survives-special-entries","change":{"change":"related","relation":"reviews","target":"story:directory-listing-survives-special-entries"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-scope-round-2@1#0~90b037c68114d529","executor":null,"recorded_at":"2026-09-04T01:17:15Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-scope-round-2","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000647","target":"01MEM0000000000000151"},"payload":{"actor":"human:timo","at":1788484635470,"causation":"cmd-review-result-review-2026-09-03-scope-round-2-reviews-story-metrics-streams-are-bounded","change":{"change":"related","relation":"reviews","target":"story:metrics-streams-are-bounded"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-scope-round-2@1#0~0244d80d17922db2","executor":null,"recorded_at":"2026-09-04T01:17:15Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-scope-round-2","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"},{"reviews":"story:upgraded-connections-keep-their-permit"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000647","target":"01MEM0000000000000229"},"payload":{"actor":"human:timo","at":1788484635488,"causation":"cmd-review-result-review-2026-09-03-scope-round-2-reviews-story-upgraded-connections-keep-their-permit","change":{"change":"related","relation":"reviews","target":"story:upgraded-connections-keep-their-permit"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-scope-round-2@1#0~6e3ccfa73141afdc","executor":null,"recorded_at":"2026-09-04T01:17:15Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-scope-round-2","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"},{"reviews":"story:upgraded-connections-keep-their-permit"},{"reviews":"story:lease-cleanup-reads-exec-state-only"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000647","target":"01MEM0000000000000139"},"payload":{"actor":"human:timo","at":1788484635504,"causation":"cmd-review-result-review-2026-09-03-scope-round-2-reviews-story-lease-cleanup-reads-exec-state-only","change":{"change":"related","relation":"reviews","target":"story:lease-cleanup-reads-exec-state-only"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-scope-round-2@1#0~9216b03f92a130d4","executor":null,"recorded_at":"2026-09-04T01:17:15Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-scope-round-2","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"},{"reviews":"story:upgraded-connections-keep-their-permit"},{"reviews":"story:lease-cleanup-reads-exec-state-only"},{"reviews":"story:confined-processes-cannot-nest-user-namespaces"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000647","target":"01MEM0000000000000070"},"payload":{"actor":"human:timo","at":1788484635526,"causation":"cmd-review-result-review-2026-09-03-scope-round-2-reviews-story-confined-processes-cannot-nest-user-namespaces","change":{"change":"related","relation":"reviews","target":"story:confined-processes-cannot-nest-user-namespaces"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-scope-round-2@1#0~d84ab919e35d1248","executor":null,"recorded_at":"2026-09-04T01:17:15Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-scope-round-2","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"},{"reviews":"story:upgraded-connections-keep-their-permit"},{"reviews":"story:lease-cleanup-reads-exec-state-only"},{"reviews":"story:confined-processes-cannot-nest-user-namespaces"},{"reviews":"story:seccomp-denies-af-vsock"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000647","target":"01MEM0000000000000208"},"payload":{"actor":"human:timo","at":1788484635544,"causation":"cmd-review-result-review-2026-09-03-scope-round-2-reviews-story-seccomp-denies-af-vsock","change":{"change":"related","relation":"reviews","target":"story:seccomp-denies-af-vsock"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-scope-round-2@1#0~db6dce98092664c5","executor":null,"recorded_at":"2026-09-04T01:17:15Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-scope-round-2","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"},{"reviews":"story:upgraded-connections-keep-their-permit"},{"reviews":"story:lease-cleanup-reads-exec-state-only"},{"reviews":"story:confined-processes-cannot-nest-user-namespaces"},{"reviews":"story:seccomp-denies-af-vsock"},{"reviews":"story:exec-oom-kills-the-whole-tree"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000647","target":"01MEM0000000000000109"},"payload":{"actor":"human:timo","at":1788484635563,"causation":"cmd-review-result-review-2026-09-03-scope-round-2-reviews-story-exec-oom-kills-the-whole-tree","change":{"change":"related","relation":"reviews","target":"story:exec-oom-kills-the-whole-tree"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-scope-round-2@1#0~56237836e40f7b0f","executor":null,"recorded_at":"2026-09-04T01:17:15Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-scope-round-2","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"},{"reviews":"story:upgraded-connections-keep-their-permit"},{"reviews":"story:lease-cleanup-reads-exec-state-only"},{"reviews":"story:confined-processes-cannot-nest-user-namespaces"},{"reviews":"story:seccomp-denies-af-vsock"},{"reviews":"story:exec-oom-kills-the-whole-tree"},{"reviews":"story:session-attachment-lifetime-is-an-accepted-decision"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000647","target":"01MEM0000000000000214"},"payload":{"actor":"human:timo","at":1788484635583,"causation":"cmd-review-result-review-2026-09-03-scope-round-2-reviews-story-session-attachment-lifetime-is-an-accepted-decision","change":{"change":"related","relation":"reviews","target":"story:session-attachment-lifetime-is-an-accepted-decision"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-scope-round-2@1#0~5da7649e9b44de1c","executor":null,"recorded_at":"2026-09-04T01:17:15Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-scope-round-2","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"},{"reviews":"story:upgraded-connections-keep-their-permit"},{"reviews":"story:lease-cleanup-reads-exec-state-only"},{"reviews":"story:confined-processes-cannot-nest-user-namespaces"},{"reviews":"story:seccomp-denies-af-vsock"},{"reviews":"story:exec-oom-kills-the-whole-tree"},{"reviews":"story:session-attachment-lifetime-is-an-accepted-decision"},{"reviews":"story:hosted-admission-reuses-identity-authority"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000647","target":"01MEM0000000000000127"},"payload":{"actor":"human:timo","at":1788484635608,"causation":"cmd-review-result-review-2026-09-03-scope-round-2-reviews-story-hosted-admission-reuses-identity-authority","change":{"change":"related","relation":"reviews","target":"story:hosted-admission-reuses-identity-authority"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-scope-round-2@1#0~6ab2ca489a245d9b","executor":null,"recorded_at":"2026-09-04T01:17:15Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-scope-round-2","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"},{"reviews":"story:upgraded-connections-keep-their-permit"},{"reviews":"story:lease-cleanup-reads-exec-state-only"},{"reviews":"story:confined-processes-cannot-nest-user-namespaces"},{"reviews":"story:seccomp-denies-af-vsock"},{"reviews":"story:exec-oom-kills-the-whole-tree"},{"reviews":"story:session-attachment-lifetime-is-an-accepted-decision"},{"reviews":"story:hosted-admission-reuses-identity-authority"},{"reviews":"story:daemon-image-serves-exec-or-says-it-cannot"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000647","target":"01MEM0000000000000085"},"payload":{"actor":"human:timo","at":1788484635645,"causation":"cmd-review-result-review-2026-09-03-scope-round-2-reviews-story-daemon-image-serves-exec-or-says-it-cannot","change":{"change":"related","relation":"reviews","target":"story:daemon-image-serves-exec-or-says-it-cannot"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-scope-round-2@1#0~1e16ebbff7555364","executor":null,"recorded_at":"2026-09-04T01:17:15Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-scope-round-2","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"},{"reviews":"story:upgraded-connections-keep-their-permit"},{"reviews":"story:lease-cleanup-reads-exec-state-only"},{"reviews":"story:confined-processes-cannot-nest-user-namespaces"},{"reviews":"story:seccomp-denies-af-vsock"},{"reviews":"story:exec-oom-kills-the-whole-tree"},{"reviews":"story:session-attachment-lifetime-is-an-accepted-decision"},{"reviews":"story:hosted-admission-reuses-identity-authority"},{"reviews":"story:daemon-image-serves-exec-or-says-it-cannot"},{"reviews":"story:backend-recheck-hashes-only-on-change"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000647","target":"01MEM0000000000000061"},"payload":{"actor":"human:timo","at":1788484635674,"causation":"cmd-review-result-review-2026-09-03-scope-round-2-reviews-story-backend-recheck-hashes-only-on-change","change":{"change":"related","relation":"reviews","target":"story:backend-recheck-hashes-only-on-change"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-scope-round-2@1#0~8153f01369bcb53e","executor":null,"recorded_at":"2026-09-04T01:17:15Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-scope-round-2","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"},{"reviews":"story:upgraded-connections-keep-their-permit"},{"reviews":"story:lease-cleanup-reads-exec-state-only"},{"reviews":"story:confined-processes-cannot-nest-user-namespaces"},{"reviews":"story:seccomp-denies-af-vsock"},{"reviews":"story:exec-oom-kills-the-whole-tree"},{"reviews":"story:session-attachment-lifetime-is-an-accepted-decision"},{"reviews":"story:hosted-admission-reuses-identity-authority"},{"reviews":"story:daemon-image-serves-exec-or-says-it-cannot"},{"reviews":"story:backend-recheck-hashes-only-on-change"},{"reviews":"story:events-table-has-one-index"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000647","target":"01MEM0000000000000106"},"payload":{"actor":"human:timo","at":1788484635699,"causation":"cmd-review-result-review-2026-09-03-scope-round-2-reviews-story-events-table-has-one-index","change":{"change":"related","relation":"reviews","target":"story:events-table-has-one-index"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-scope-round-2@1#0~e5886736a447da8a","executor":null,"recorded_at":"2026-09-04T01:17:15Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-scope-round-2","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"},{"reviews":"story:upgraded-connections-keep-their-permit"},{"reviews":"story:lease-cleanup-reads-exec-state-only"},{"reviews":"story:confined-processes-cannot-nest-user-namespaces"},{"reviews":"story:seccomp-denies-af-vsock"},{"reviews":"story:exec-oom-kills-the-whole-tree"},{"reviews":"story:session-attachment-lifetime-is-an-accepted-decision"},{"reviews":"story:hosted-admission-reuses-identity-authority"},{"reviews":"story:daemon-image-serves-exec-or-says-it-cannot"},{"reviews":"story:backend-recheck-hashes-only-on-change"},{"reviews":"story:events-table-has-one-index"},{"reviews":"story:unattached-claimed-session-is-contained"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000647","target":"01MEM0000000000000226"},"payload":{"actor":"human:timo","at":1788484635732,"causation":"cmd-review-result-review-2026-09-03-scope-round-2-reviews-story-unattached-claimed-session-is-contained","change":{"change":"related","relation":"reviews","target":"story:unattached-claimed-session-is-contained"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-scope-round-2@1#0~3273b4a70fa21a15","executor":null,"recorded_at":"2026-09-04T01:17:15Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-acceptance-round-2","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"active","changed":{"body":"needs-revision\n\nstory:daemon-image-serves-exec-or-says-it-cannot — the revision fixed the either/or's image-contents-plus-delegated-lane branch (moved to Notes, now conditional framing only), but the acceptance still joins two independently-updatable artifacts with \"and\" (\"The GHCR release notes and `README.md` state whether...\"), so a revision can update one and not the other and neither party can tell from the sentence alone which failed — .engineering/planning/story/daemon-image-serves-exec-or-says-it-cannot.md:36\n\nWhat I read: the epic and all 14 decomposing stories (15 artifacts total) via `aep artifact list --format json` filtered on the `decomposes` relation, `aep artifact show <id>` for each of the 15, `aep artifact show review-result:review-2026-09-03-acceptance-round-1`, `aep artifact lifecycle epic`, and `grep -n` on the store files for line citations. I also read the three other epics with a \"Done When\" section (`epic:byte-plane-completion`, `epic:container-driver-entry`, `epic:release-hardening`) to check this store's convention for epic-level acceptance before judging the new \"## Done When\" section.\n\nWhat I could not establish: whether this store's epic \"Done When\" convention (a compound, multi-clause statement — seen in all three precedent epics, including `epic:release-hardening` which reached `implemented` with five semicolon-joined clauses) is meant to be exempt from the one-statement rule this critic applies to stories, or whether all four epics share the same latent defect. I treated the new epic's two-clause Done When (\"every story implemented/rejected with a named closure\" and \"`aep artifact validate` reports the set valid\") as consistent with established convention and did not flag it, but flag the uncertainty here rather than silently deciding it. Six of the seven round-1-fixed stories (`backend-recheck-hashes-only-on-change`, `confined-processes-cannot-nest-user-namespaces`, `exec-oom-kills-the-whole-tree`, `hosted-admission-reuses-identity-authority`, `metrics-streams-are-bounded`, `session-attachment-lifetime-is-an-accepted-decision`) now read as single, observable statements with the removed clauses correctly relocated to Notes, and carry no finding from me. Coupling, coverage of the epic's 14-finding register, and shared-surface risk are outside my lane and not reflected in this verdict.\n\n```findings\n- file: .engineering/planning/story/daemon-image-serves-exec-or-says-it-cannot.md\n  line: 36\n  category: acceptance\n  severity: blocker\n  verdict: needs-revision\n  origin: introduced\n  message: the revision fixed the either/or's image-contents-plus-delegated-lane branch (moved to Notes, now conditional framing only), but the acceptance still joins two independently-updatable artifacts with \"and\" (\"The GHCR release notes and README.md state whether...\"), so a revision can update one and not the other and neither party can tell from the sentence alone which failed\n```\n","tags":["critic","review"],"title":"Acceptance critic (aep-planning:plan-critic-acceptance), round 2"},"args":{"command":"create-entity","data":{"body":"needs-revision\n\nstory:daemon-image-serves-exec-or-says-it-cannot — the revision fixed the either/or's image-contents-plus-delegated-lane branch (moved to Notes, now conditional framing only), but the acceptance still joins two independently-updatable artifacts with \"and\" (\"The GHCR release notes and `README.md` state whether...\"), so a revision can update one and not the other and neither party can tell from the sentence alone which failed — .engineering/planning/story/daemon-image-serves-exec-or-says-it-cannot.md:36\n\nWhat I read: the epic and all 14 decomposing stories (15 artifacts total) via `aep artifact list --format json` filtered on the `decomposes` relation, `aep artifact show <id>` for each of the 15, `aep artifact show review-result:review-2026-09-03-acceptance-round-1`, `aep artifact lifecycle epic`, and `grep -n` on the store files for line citations. I also read the three other epics with a \"Done When\" section (`epic:byte-plane-completion`, `epic:container-driver-entry`, `epic:release-hardening`) to check this store's convention for epic-level acceptance before judging the new \"## Done When\" section.\n\nWhat I could not establish: whether this store's epic \"Done When\" convention (a compound, multi-clause statement — seen in all three precedent epics, including `epic:release-hardening` which reached `implemented` with five semicolon-joined clauses) is meant to be exempt from the one-statement rule this critic applies to stories, or whether all four epics share the same latent defect. I treated the new epic's two-clause Done When (\"every story implemented/rejected with a named closure\" and \"`aep artifact validate` reports the set valid\") as consistent with established convention and did not flag it, but flag the uncertainty here rather than silently deciding it. Six of the seven round-1-fixed stories (`backend-recheck-hashes-only-on-change`, `confined-processes-cannot-nest-user-namespaces`, `exec-oom-kills-the-whole-tree`, `hosted-admission-reuses-identity-authority`, `metrics-streams-are-bounded`, `session-attachment-lifetime-is-an-accepted-decision`) now read as single, observable statements with the removed clauses correctly relocated to Notes, and carry no finding from me. Coupling, coverage of the epic's 14-finding register, and shared-surface risk are outside my lane and not reflected in this verdict.\n\n```findings\n- file: .engineering/planning/story/daemon-image-serves-exec-or-says-it-cannot.md\n  line: 36\n  category: acceptance\n  severity: blocker\n  verdict: needs-revision\n  origin: introduced\n  message: the revision fixed the either/or's image-contents-plus-delegated-lane branch (moved to Notes, now conditional framing only), but the acceptance still joins two independently-updatable artifacts with \"and\" (\"The GHCR release notes and README.md state whether...\"), so a revision can update one and not the other and neither party can tell from the sentence alone which failed\n```\n","status":"active","tags":["critic","review"],"title":"Acceptance critic (aep-planning:plan-critic-acceptance), round 2"},"entity_type":"aep.review-result/v1","locator":"ep://planning/store/review-result/review-2026-09-03-acceptance-round-2"},"payload":{"actor":"human:timo","at":1788484732106,"causation":"cmd-new-review-result-review-2026-09-03-acceptance-round-2","change":{"change":"created","status":"active"},"correlation":"protocol-artifact-new","event_id":"review-result:review-2026-09-03-acceptance-round-2@1#0~03bc49b3113bc013","executor":null,"recorded_at":"2026-09-04T01:18:52Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-acceptance-round-2","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000680","target":"01MEM0000000000000025"},"payload":{"actor":"human:timo","at":1788484732116,"causation":"cmd-review-result-review-2026-09-03-acceptance-round-2-reviews-epic-review-2026-09-03-findings","change":{"change":"related","relation":"reviews","target":"epic:review-2026-09-03-findings"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-acceptance-round-2@1#0~142941bc22e6d275","executor":null,"recorded_at":"2026-09-04T01:18:52Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-acceptance-round-2","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000680","target":"01MEM0000000000000148"},"payload":{"actor":"human:timo","at":1788484732125,"causation":"cmd-review-result-review-2026-09-03-acceptance-round-2-reviews-story-line-patch-edits-apply-in-line-order","change":{"change":"related","relation":"reviews","target":"story:line-patch-edits-apply-in-line-order"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-acceptance-round-2@1#0~73868454f219a0df","executor":null,"recorded_at":"2026-09-04T01:18:52Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-acceptance-round-2","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000680","target":"01MEM0000000000000094"},"payload":{"actor":"human:timo","at":1788484732136,"causation":"cmd-review-result-review-2026-09-03-acceptance-round-2-reviews-story-directory-listing-survives-special-entries","change":{"change":"related","relation":"reviews","target":"story:directory-listing-survives-special-entries"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-acceptance-round-2@1#0~27dd985fd67f3e65","executor":null,"recorded_at":"2026-09-04T01:18:52Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-acceptance-round-2","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000680","target":"01MEM0000000000000154"},"payload":{"actor":"human:timo","at":1788484732147,"causation":"cmd-review-result-review-2026-09-03-acceptance-round-2-reviews-story-metrics-streams-are-bounded","change":{"change":"related","relation":"reviews","target":"story:metrics-streams-are-bounded"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-acceptance-round-2@1#0~060a5a5dd1aeba66","executor":null,"recorded_at":"2026-09-04T01:18:52Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-acceptance-round-2","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"},{"reviews":"story:upgraded-connections-keep-their-permit"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000680","target":"01MEM0000000000000232"},"payload":{"actor":"human:timo","at":1788484732155,"causation":"cmd-review-result-review-2026-09-03-acceptance-round-2-reviews-story-upgraded-connections-keep-their-permit","change":{"change":"related","relation":"reviews","target":"story:upgraded-connections-keep-their-permit"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-acceptance-round-2@1#0~e8b3822883d89d73","executor":null,"recorded_at":"2026-09-04T01:18:52Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-acceptance-round-2","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"},{"reviews":"story:upgraded-connections-keep-their-permit"},{"reviews":"story:lease-cleanup-reads-exec-state-only"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000680","target":"01MEM0000000000000142"},"payload":{"actor":"human:timo","at":1788484732164,"causation":"cmd-review-result-review-2026-09-03-acceptance-round-2-reviews-story-lease-cleanup-reads-exec-state-only","change":{"change":"related","relation":"reviews","target":"story:lease-cleanup-reads-exec-state-only"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-acceptance-round-2@1#0~1cf5ee54c1164ab5","executor":null,"recorded_at":"2026-09-04T01:18:52Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-acceptance-round-2","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"},{"reviews":"story:upgraded-connections-keep-their-permit"},{"reviews":"story:lease-cleanup-reads-exec-state-only"},{"reviews":"story:confined-processes-cannot-nest-user-namespaces"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000680","target":"01MEM0000000000000073"},"payload":{"actor":"human:timo","at":1788484732173,"causation":"cmd-review-result-review-2026-09-03-acceptance-round-2-reviews-story-confined-processes-cannot-nest-user-namespaces","change":{"change":"related","relation":"reviews","target":"story:confined-processes-cannot-nest-user-namespaces"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-acceptance-round-2@1#0~bc367fd474dcde72","executor":null,"recorded_at":"2026-09-04T01:18:52Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-acceptance-round-2","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"},{"reviews":"story:upgraded-connections-keep-their-permit"},{"reviews":"story:lease-cleanup-reads-exec-state-only"},{"reviews":"story:confined-processes-cannot-nest-user-namespaces"},{"reviews":"story:seccomp-denies-af-vsock"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000680","target":"01MEM0000000000000211"},"payload":{"actor":"human:timo","at":1788484732182,"causation":"cmd-review-result-review-2026-09-03-acceptance-round-2-reviews-story-seccomp-denies-af-vsock","change":{"change":"related","relation":"reviews","target":"story:seccomp-denies-af-vsock"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-acceptance-round-2@1#0~f687c537f220dbd8","executor":null,"recorded_at":"2026-09-04T01:18:52Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-acceptance-round-2","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"},{"reviews":"story:upgraded-connections-keep-their-permit"},{"reviews":"story:lease-cleanup-reads-exec-state-only"},{"reviews":"story:confined-processes-cannot-nest-user-namespaces"},{"reviews":"story:seccomp-denies-af-vsock"},{"reviews":"story:exec-oom-kills-the-whole-tree"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000680","target":"01MEM0000000000000112"},"payload":{"actor":"human:timo","at":1788484732193,"causation":"cmd-review-result-review-2026-09-03-acceptance-round-2-reviews-story-exec-oom-kills-the-whole-tree","change":{"change":"related","relation":"reviews","target":"story:exec-oom-kills-the-whole-tree"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-acceptance-round-2@1#0~605a3c7e847aef28","executor":null,"recorded_at":"2026-09-04T01:18:52Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-acceptance-round-2","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"},{"reviews":"story:upgraded-connections-keep-their-permit"},{"reviews":"story:lease-cleanup-reads-exec-state-only"},{"reviews":"story:confined-processes-cannot-nest-user-namespaces"},{"reviews":"story:seccomp-denies-af-vsock"},{"reviews":"story:exec-oom-kills-the-whole-tree"},{"reviews":"story:session-attachment-lifetime-is-an-accepted-decision"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000680","target":"01MEM0000000000000217"},"payload":{"actor":"human:timo","at":1788484732203,"causation":"cmd-review-result-review-2026-09-03-acceptance-round-2-reviews-story-session-attachment-lifetime-is-an-accepted-decision","change":{"change":"related","relation":"reviews","target":"story:session-attachment-lifetime-is-an-accepted-decision"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-acceptance-round-2@1#0~c1a7d337d3ec9a8a","executor":null,"recorded_at":"2026-09-04T01:18:52Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-acceptance-round-2","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"},{"reviews":"story:upgraded-connections-keep-their-permit"},{"reviews":"story:lease-cleanup-reads-exec-state-only"},{"reviews":"story:confined-processes-cannot-nest-user-namespaces"},{"reviews":"story:seccomp-denies-af-vsock"},{"reviews":"story:exec-oom-kills-the-whole-tree"},{"reviews":"story:session-attachment-lifetime-is-an-accepted-decision"},{"reviews":"story:hosted-admission-reuses-identity-authority"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000680","target":"01MEM0000000000000130"},"payload":{"actor":"human:timo","at":1788484732212,"causation":"cmd-review-result-review-2026-09-03-acceptance-round-2-reviews-story-hosted-admission-reuses-identity-authority","change":{"change":"related","relation":"reviews","target":"story:hosted-admission-reuses-identity-authority"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-acceptance-round-2@1#0~f9091e6d3086f440","executor":null,"recorded_at":"2026-09-04T01:18:52Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-acceptance-round-2","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"},{"reviews":"story:upgraded-connections-keep-their-permit"},{"reviews":"story:lease-cleanup-reads-exec-state-only"},{"reviews":"story:confined-processes-cannot-nest-user-namespaces"},{"reviews":"story:seccomp-denies-af-vsock"},{"reviews":"story:exec-oom-kills-the-whole-tree"},{"reviews":"story:session-attachment-lifetime-is-an-accepted-decision"},{"reviews":"story:hosted-admission-reuses-identity-authority"},{"reviews":"story:daemon-image-serves-exec-or-says-it-cannot"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000680","target":"01MEM0000000000000088"},"payload":{"actor":"human:timo","at":1788484732221,"causation":"cmd-review-result-review-2026-09-03-acceptance-round-2-reviews-story-daemon-image-serves-exec-or-says-it-cannot","change":{"change":"related","relation":"reviews","target":"story:daemon-image-serves-exec-or-says-it-cannot"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-acceptance-round-2@1#0~0087ae68de0ea1bc","executor":null,"recorded_at":"2026-09-04T01:18:52Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-acceptance-round-2","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"},{"reviews":"story:upgraded-connections-keep-their-permit"},{"reviews":"story:lease-cleanup-reads-exec-state-only"},{"reviews":"story:confined-processes-cannot-nest-user-namespaces"},{"reviews":"story:seccomp-denies-af-vsock"},{"reviews":"story:exec-oom-kills-the-whole-tree"},{"reviews":"story:session-attachment-lifetime-is-an-accepted-decision"},{"reviews":"story:hosted-admission-reuses-identity-authority"},{"reviews":"story:daemon-image-serves-exec-or-says-it-cannot"},{"reviews":"story:backend-recheck-hashes-only-on-change"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000680","target":"01MEM0000000000000064"},"payload":{"actor":"human:timo","at":1788484732231,"causation":"cmd-review-result-review-2026-09-03-acceptance-round-2-reviews-story-backend-recheck-hashes-only-on-change","change":{"change":"related","relation":"reviews","target":"story:backend-recheck-hashes-only-on-change"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-acceptance-round-2@1#0~57647add595b1782","executor":null,"recorded_at":"2026-09-04T01:18:52Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-acceptance-round-2","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"},{"reviews":"story:upgraded-connections-keep-their-permit"},{"reviews":"story:lease-cleanup-reads-exec-state-only"},{"reviews":"story:confined-processes-cannot-nest-user-namespaces"},{"reviews":"story:seccomp-denies-af-vsock"},{"reviews":"story:exec-oom-kills-the-whole-tree"},{"reviews":"story:session-attachment-lifetime-is-an-accepted-decision"},{"reviews":"story:hosted-admission-reuses-identity-authority"},{"reviews":"story:daemon-image-serves-exec-or-says-it-cannot"},{"reviews":"story:backend-recheck-hashes-only-on-change"},{"reviews":"story:events-table-has-one-index"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000680","target":"01MEM0000000000000109"},"payload":{"actor":"human:timo","at":1788484732239,"causation":"cmd-review-result-review-2026-09-03-acceptance-round-2-reviews-story-events-table-has-one-index","change":{"change":"related","relation":"reviews","target":"story:events-table-has-one-index"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-acceptance-round-2@1#0~e6f8e575b2e445ac","executor":null,"recorded_at":"2026-09-04T01:18:52Z"}}
{"entity":"review-result","version":1,"id":"review-2026-09-03-acceptance-round-2","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"epic:review-2026-09-03-findings"},{"reviews":"story:line-patch-edits-apply-in-line-order"},{"reviews":"story:directory-listing-survives-special-entries"},{"reviews":"story:metrics-streams-are-bounded"},{"reviews":"story:upgraded-connections-keep-their-permit"},{"reviews":"story:lease-cleanup-reads-exec-state-only"},{"reviews":"story:confined-processes-cannot-nest-user-namespaces"},{"reviews":"story:seccomp-denies-af-vsock"},{"reviews":"story:exec-oom-kills-the-whole-tree"},{"reviews":"story:session-attachment-lifetime-is-an-accepted-decision"},{"reviews":"story:hosted-admission-reuses-identity-authority"},{"reviews":"story:daemon-image-serves-exec-or-says-it-cannot"},{"reviews":"story:backend-recheck-hashes-only-on-change"},{"reviews":"story:events-table-has-one-index"},{"reviews":"story:unattached-claimed-session-is-contained"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000680","target":"01MEM0000000000000229"},"payload":{"actor":"human:timo","at":1788484732246,"causation":"cmd-review-result-review-2026-09-03-acceptance-round-2-reviews-story-unattached-claimed-session-is-contained","change":{"change":"related","relation":"reviews","target":"story:unattached-claimed-session-is-contained"},"correlation":"protocol-artifact-relate","event_id":"review-result:review-2026-09-03-acceptance-round-2@1#0~746f452f1b9681ab","executor":null,"recorded_at":"2026-09-04T01:18:52Z"}}
{"entity":"story","version":1,"id":"daemon-image-serves-exec-or-says-it-cannot","revision":6,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"# Story: The daemon image serves exec or says it cannot\n\n## Context\n\nExec capability requires the configured bubblewrap binary and `/usr/bin/socat` on the host\n(`crates/substrate-host/src/probe.rs:319-320`; documented at `README.md:280`). The published\ndaemon image is `gcr.io/distroless/cc-debian12:nonroot` (`Dockerfile:12`) and installs neither,\nnor `/usr/bin/env`, which the sandbox argv execs (`process.rs`, `command`). Inferred from the base\nimage, not pulled: that image can only answer `exec.sandbox-unavailable`, and a reader of the\nrelease notes is not told so.\n\n## Acceptance\n\n`README.md` § Serving exec states whether the published daemon image serves exec and, when it does not, names what a host must add.\n\n## Notes\n\nThe statement holds either way: if a later story ships bubblewrap, socat and coreutils in the image and runs the delegated lane against it, the documents say the image serves exec. The `socat` dependency exists only to prove the seccomp `AF_UNIX` refusal; a small Rust probe child would remove it.\n\n## Parallel work\n\nThis story shares `crates/substrate-host/src/probe.rs` with story:backend-recheck-hashes-only-on-change, story:confined-processes-cannot-nest-user-namespaces, story:daemon-image-serves-exec-or-says-it-cannot, story:exec-oom-kills-the-whole-tree and story:seccomp-denies-af-vsock; three of them also share `crates/substrate-host/src/process.rs`. Work them one at a time, or in one wave by one implementor; `aep artifact waves` sequences them.\n"},"args":{"changes":{"body":"# Story: The daemon image serves exec or says it cannot\n\n## Context\n\nExec capability requires the configured bubblewrap binary and `/usr/bin/socat` on the host\n(`crates/substrate-host/src/probe.rs:319-320`; documented at `README.md:280`). The published\ndaemon image is `gcr.io/distroless/cc-debian12:nonroot` (`Dockerfile:12`) and installs neither,\nnor `/usr/bin/env`, which the sandbox argv execs (`process.rs`, `command`). Inferred from the base\nimage, not pulled: that image can only answer `exec.sandbox-unavailable`, and a reader of the\nrelease notes is not told so.\n\n## Acceptance\n\n`README.md` § Serving exec states whether the published daemon image serves exec and, when it does not, names what a host must add.\n\n## Notes\n\nThe statement holds either way: if a later story ships bubblewrap, socat and coreutils in the image and runs the delegated lane against it, the documents say the image serves exec. The `socat` dependency exists only to prove the seccomp `AF_UNIX` refusal; a small Rust probe child would remove it.\n\n## Parallel work\n\nThis story shares `crates/substrate-host/src/probe.rs` with story:backend-recheck-hashes-only-on-change, story:confined-processes-cannot-nest-user-namespaces, story:daemon-image-serves-exec-or-says-it-cannot, story:exec-oom-kills-the-whole-tree and story:seccomp-denies-af-vsock; three of them also share `crates/substrate-host/src/process.rs`. Work them one at a time, or in one wave by one implementor; `aep artifact waves` sequences them.\n"},"command":"update-entity","target":"01MEM0000000000000091"},"payload":{"actor":"human:timo","at":1788484732461,"causation":"cmd-protocol-artifact-body-section-story-daemon-image-serves-exec-or-says-it-cannot-1788484732461","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:daemon-image-serves-exec-or-says-it-cannot@6#0~c650adf389dd4d34","executor":null,"recorded_at":"2026-09-04T01:18:52Z"}}
{"entity":"story","version":1,"id":"daemon-image-serves-exec-or-says-it-cannot","revision":7,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"# Story: The daemon image serves exec or says it cannot\n\n## Context\n\nExec capability requires the configured bubblewrap binary and `/usr/bin/socat` on the host\n(`crates/substrate-host/src/probe.rs:319-320`; documented at `README.md:280`). The published\ndaemon image is `gcr.io/distroless/cc-debian12:nonroot` (`Dockerfile:12`) and installs neither,\nnor `/usr/bin/env`, which the sandbox argv execs (`process.rs`, `command`). Inferred from the base\nimage, not pulled: that image can only answer `exec.sandbox-unavailable`, and a reader of the\nrelease notes is not told so.\n\n## Acceptance\n\n`README.md` § Serving exec states whether the published daemon image serves exec and, when it does not, names what a host must add.\n\n## Notes\n\nThe GHCR release notes are generated from the release workflow and repeat the README statement; they are not a second claim. The statement holds either way: if a later story ships bubblewrap, socat and coreutils in the image and runs the delegated lane against it, the README says the image serves exec. The `socat` dependency exists only to prove the seccomp `AF_UNIX` refusal; a small Rust probe child would remove it.\n\n## Parallel work\n\nThis story shares `crates/substrate-host/src/probe.rs` with story:backend-recheck-hashes-only-on-change, story:confined-processes-cannot-nest-user-namespaces, story:daemon-image-serves-exec-or-says-it-cannot, story:exec-oom-kills-the-whole-tree and story:seccomp-denies-af-vsock; three of them also share `crates/substrate-host/src/process.rs`. Work them one at a time, or in one wave by one implementor; `aep artifact waves` sequences them.\n"},"args":{"changes":{"body":"# Story: The daemon image serves exec or says it cannot\n\n## Context\n\nExec capability requires the configured bubblewrap binary and `/usr/bin/socat` on the host\n(`crates/substrate-host/src/probe.rs:319-320`; documented at `README.md:280`). The published\ndaemon image is `gcr.io/distroless/cc-debian12:nonroot` (`Dockerfile:12`) and installs neither,\nnor `/usr/bin/env`, which the sandbox argv execs (`process.rs`, `command`). Inferred from the base\nimage, not pulled: that image can only answer `exec.sandbox-unavailable`, and a reader of the\nrelease notes is not told so.\n\n## Acceptance\n\n`README.md` § Serving exec states whether the published daemon image serves exec and, when it does not, names what a host must add.\n\n## Notes\n\nThe GHCR release notes are generated from the release workflow and repeat the README statement; they are not a second claim. The statement holds either way: if a later story ships bubblewrap, socat and coreutils in the image and runs the delegated lane against it, the README says the image serves exec. The `socat` dependency exists only to prove the seccomp `AF_UNIX` refusal; a small Rust probe child would remove it.\n\n## Parallel work\n\nThis story shares `crates/substrate-host/src/probe.rs` with story:backend-recheck-hashes-only-on-change, story:confined-processes-cannot-nest-user-namespaces, story:daemon-image-serves-exec-or-says-it-cannot, story:exec-oom-kills-the-whole-tree and story:seccomp-denies-af-vsock; three of them also share `crates/substrate-host/src/process.rs`. Work them one at a time, or in one wave by one implementor; `aep artifact waves` sequences them.\n"},"command":"update-entity","target":"01MEM0000000000000091"},"payload":{"actor":"human:timo","at":1788484732664,"causation":"cmd-protocol-artifact-body-section-story-daemon-image-serves-exec-or-says-it-cannot-1788484732664","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:daemon-image-serves-exec-or-says-it-cannot@7#0~70ad3007737064a3","executor":null,"recorded_at":"2026-09-04T01:18:52Z"}}
{"entity":"story","version":1,"id":"exec-oom-kills-the-whole-tree","revision":6,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"body":"# Story: An exec OOM kills the whole tree\n\n## Context\n\n`Cgroup::create` writes `pids.max`, `memory.max`, `memory.swap.max=0` and `cpu.max`\n(`crates/substrate-host/src/process.rs:2848-2860`) and not `memory.oom.group=1`. The kernel then\nkills one process on OOM and the rest of the tree continues, which is not the whole-tree semantics\nthe safety envelope names (`AGENTS.md` § Safety envelope). The `exec.memory-limit` refusal is\nderived from `memory_oom_kills` in the terminal usage, which exists only when the client asked for\n`measurements` (`process.rs`, `run_child` and `record_resource_bound`), so an OOM on a run without\nmeasurements is reported as an ordinary exit. `cpu.max` is clamped to one CPU\n(`process.rs:2858`), which is a documented choice worth stating on the capability fact.\n\n## Acceptance\n\nAn exec whose child exceeds `memory.max` ends with no process left in its cgroup, observed in the delegated lane.\n\n## Notes\n\n`memory.oom.group` is a cgroup v2 file present on every kernel the probe already requires; probe it and withhold the exec facts where it is absent. Naming `exec.memory-limit` on the observation when `measurements` was not requested is a second change on `record_resource_bound`, delivered in this story and checked by its own case rather than by the acceptance above. The one-CPU `cpu.max` clamp (`process.rs:2858`) is not changed here; stating it on the capability fact is a contract addition and, if wanted, is its own story under a successor bundle.\n\n## Parallel work\n\nThis story shares `crates/substrate-host/src/probe.rs` with story:backend-recheck-hashes-only-on-change, story:confined-processes-cannot-nest-user-namespaces, story:daemon-image-serves-exec-or-says-it-cannot, story:exec-oom-kills-the-whole-tree and story:seccomp-denies-af-vsock; three of them also share `crates/substrate-host/src/process.rs`. Work them one at a time, or in one wave by one implementor; `aep artifact waves` sequences them.\n"},"args":{"changes":{"body":"# Story: An exec OOM kills the whole tree\n\n## Context\n\n`Cgroup::create` writes `pids.max`, `memory.max`, `memory.swap.max=0` and `cpu.max`\n(`crates/substrate-host/src/process.rs:2848-2860`) and not `memory.oom.group=1`. The kernel then\nkills one process on OOM and the rest of the tree continues, which is not the whole-tree semantics\nthe safety envelope names (`AGENTS.md` § Safety envelope). The `exec.memory-limit` refusal is\nderived from `memory_oom_kills` in the terminal usage, which exists only when the client asked for\n`measurements` (`process.rs`, `run_child` and `record_resource_bound`), so an OOM on a run without\nmeasurements is reported as an ordinary exit. `cpu.max` is clamped to one CPU\n(`process.rs:2858`), which is a documented choice worth stating on the capability fact.\n\n## Acceptance\n\nAn exec whose child exceeds `memory.max` ends with no process left in its cgroup, observed in the delegated lane.\n\n## Notes\n\n`memory.oom.group` is a cgroup v2 file present on every kernel the probe already requires; probe it and withhold the exec facts where it is absent. Naming `exec.memory-limit` on the observation when `measurements` was not requested is a second change on `record_resource_bound`, delivered in this story and checked by its own case rather than by the acceptance above. The one-CPU `cpu.max` clamp (`process.rs:2858`) is not changed here; stating it on the capability fact is a contract addition and, if wanted, is its own story under a successor bundle.\n\n## Parallel work\n\nThis story shares `crates/substrate-host/src/probe.rs` with story:backend-recheck-hashes-only-on-change, story:confined-processes-cannot-nest-user-namespaces, story:daemon-image-serves-exec-or-says-it-cannot, story:exec-oom-kills-the-whole-tree and story:seccomp-denies-af-vsock; three of them also share `crates/substrate-host/src/process.rs`. Work them one at a time, or in one wave by one implementor; `aep artifact waves` sequences them.\n"},"command":"update-entity","target":"01MEM0000000000000115"},"payload":{"actor":"human:timo","at":1788484732866,"causation":"cmd-protocol-artifact-body-section-story-exec-oom-kills-the-whole-tree-1788484732866","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:exec-oom-kills-the-whole-tree@6#0~7cf7cf057e4a2f8d","executor":null,"recorded_at":"2026-09-04T01:18:52Z"}}
{"entity":"story","version":1,"id":"daemon-image-serves-exec-or-says-it-cannot","revision":7,"type":"aep.evidence.record/v1","from_state":"draft","to_state":"draft","changed":{},"args":{"command":"record-evidence","kind":"review_outcome","outcome":"fixed","review":"review-result:review-2026-09-03-acceptance-round-2","source":"review-result:review-2026-09-03-acceptance-round-2","target":"01MEM0000000000000091"},"payload":{"actor":"human:timo","at":1788484733000,"causation":"cmd-evidence-story-daemon-image-serves-exec-or-says-it-cannot-review_outcome-1788484733069","change":{"change":"evidence","kind":"review_outcome","outcome":"fixed","review":"review-result:review-2026-09-03-acceptance-round-2","source":"review-result:review-2026-09-03-acceptance-round-2"},"correlation":"protocol-artifact-evidence","event_id":"story:daemon-image-serves-exec-or-says-it-cannot@7#0~79f2a6dc3ef2e086","executor":null,"recorded_at":"2026-09-04T01:18:53Z"}}
{"entity":"story","version":1,"id":"metrics-streams-are-bounded","revision":6,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"review_outcome\":2}}","target":"01MEM0000000000000157","to":"proposed"},"payload":{"actor":"human:timo","at":1788490485259,"causation":"cmd-move-story-metrics-streams-are-bounded-1788490485259","change":{"change":"moved","decided_on":{"recorded":{"review_outcome":2}},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:metrics-streams-are-bounded@6#0~24d36b11327a531c","executor":null,"recorded_at":"2026-09-04T02:54:45Z"}}
{"entity":"story","version":1,"id":"metrics-streams-are-bounded","revision":7,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"review_outcome\":2}}","target":"01MEM0000000000000157","to":"active"},"payload":{"actor":"human:timo","at":1788490485843,"causation":"cmd-move-story-metrics-streams-are-bounded-1788490485843","change":{"change":"moved","decided_on":{"recorded":{"review_outcome":2}},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:metrics-streams-are-bounded@7#0~0d7c08cfcf623df0","executor":null,"recorded_at":"2026-09-04T02:54:45Z"}}
{"entity":"story","version":1,"id":"upgraded-connections-keep-their-permit","revision":4,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000235","to":"proposed"},"payload":{"actor":"human:timo","at":1788490486428,"causation":"cmd-move-story-upgraded-connections-keep-their-permit-1788490486428","change":{"change":"moved","decided_on":{},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:upgraded-connections-keep-their-permit@4#0~489b2834d26fa10a","executor":null,"recorded_at":"2026-09-04T02:54:46Z"}}
{"entity":"story","version":1,"id":"upgraded-connections-keep-their-permit","revision":5,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{}","target":"01MEM0000000000000235","to":"active"},"payload":{"actor":"human:timo","at":1788490486864,"causation":"cmd-move-story-upgraded-connections-keep-their-permit-1788490486864","change":{"change":"moved","decided_on":{},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:upgraded-connections-keep-their-permit@5#0~fdc0aa3ca5015afe","executor":null,"recorded_at":"2026-09-04T02:54:46Z"}}
{"entity":"story","version":1,"id":"unattached-claimed-session-is-contained","revision":4,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"review_outcome\":1}}","target":"01MEM0000000000000232","to":"proposed"},"payload":{"actor":"human:timo","at":1788490487208,"causation":"cmd-move-story-unattached-claimed-session-is-contained-1788490487208","change":{"change":"moved","decided_on":{"recorded":{"review_outcome":1}},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:unattached-claimed-session-is-contained@4#0~7303775f488edc5b","executor":null,"recorded_at":"2026-09-04T02:54:47Z"}}
{"entity":"story","version":1,"id":"unattached-claimed-session-is-contained","revision":5,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"review_outcome\":1}}","target":"01MEM0000000000000232","to":"active"},"payload":{"actor":"human:timo","at":1788490487544,"causation":"cmd-move-story-unattached-claimed-session-is-contained-1788490487544","change":{"change":"moved","decided_on":{"recorded":{"review_outcome":1}},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:unattached-claimed-session-is-contained@5#0~2d82b4853af84973","executor":null,"recorded_at":"2026-09-04T02:54:47Z"}}
{"entity":"story","version":1,"id":"confined-processes-cannot-nest-user-namespaces","revision":6,"type":"aep.status.move/v1","from_state":"draft","to_state":"proposed","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"review_outcome\":1}}","target":"01MEM0000000000000076","to":"proposed"},"payload":{"actor":"human:timo","at":1788490487845,"causation":"cmd-move-story-confined-processes-cannot-nest-user-namespaces-1788490487845","change":{"change":"moved","decided_on":{"recorded":{"review_outcome":1}},"from":"draft","to":"proposed"},"correlation":"protocol-artifact-move","event_id":"story:confined-processes-cannot-nest-user-namespaces@6#0~90eb9720bbe18bd5","executor":null,"recorded_at":"2026-09-04T02:54:47Z"}}
{"entity":"story","version":1,"id":"confined-processes-cannot-nest-user-namespaces","revision":7,"type":"aep.status.move/v1","from_state":"proposed","to_state":"active","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"review_outcome\":1}}","target":"01MEM0000000000000076","to":"active"},"payload":{"actor":"human:timo","at":1788490488163,"causation":"cmd-move-story-confined-processes-cannot-nest-user-namespaces-1788490488163","change":{"change":"moved","decided_on":{"recorded":{"review_outcome":1}},"from":"proposed","to":"active"},"correlation":"protocol-artifact-move","event_id":"story:confined-processes-cannot-nest-user-namespaces@7#0~10a3576235b4d875","executor":null,"recorded_at":"2026-09-04T02:54:48Z"}}
{"entity":"story","version":1,"id":"upgraded-connections-keep-their-permit","revision":5,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"47dce6f","source":"cargo test -p b10x-substrate-daemon --release --locked (unit u2, worktree impl/upgraded-connections-permit)","target":"01MEM0000000000000235"},"payload":{"actor":"human:timo","at":1788491993000,"causation":"cmd-evidence-story-upgraded-connections-keep-their-permit-test_result-1788491993817","change":{"change":"evidence","kind":"test_result","reference":"47dce6f","source":"cargo test -p b10x-substrate-daemon --release --locked (unit u2, worktree impl/upgraded-connections-permit)"},"correlation":"protocol-artifact-evidence","event_id":"story:upgraded-connections-keep-their-permit@5#0~1e925093d2eecce3","executor":null,"recorded_at":"2026-09-04T03:19:53Z"}}
{"entity":"review-result","version":1,"id":"adversary-u4-pass-1","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"active","changed":{"body":"# Adversary pass 1 — unit u4, `story:confined-processes-cannot-nest-user-namespaces`\n\nAgent type `adp:adversary`. Worktree `wt-5698af5e5dcf`, branch `impl/disable-userns`,\nHEAD `a941dcd`, base `617bbed`. Report as returned.\n\n```\nunit: u4 — story:confined-processes-cannot-nest-user-namespaces\nverdict: red\ncases: executed 84→88, red 3\norigin: introduced 4, pre-existing 0, undecided 0\nwrote-outside-worktree: 5 paths\nneeds-coordinator: no\n```\n\n## Cases added\n\n`crates/substrate-host/tests/user_namespace_posture_adversarial.rs` (untracked; no implementation\nfile touched, `seccomp.rs` untouched).\n\n| case | asserts | result |\n|---|---|---|\n| `the_re_expressed_posture_rule_agrees_with_the_shipped_case_on_the_unmodified_tree` | control: the re-expressed rule is green on the untouched tree | green |\n| `the_posture_check_catches_an_exec_argv_that_stopped_splicing_the_constant` | with `command.args(USER_NAMESPACE_ARGV);` deleted, the check reports a violation | **red** |\n| `the_posture_check_catches_an_eighth_sandbox_that_spells_the_user_namespace_differently` | with an `--unshare-all` sandbox added, the check reports a violation | **red** |\n| `a_backend_that_cannot_disable_nested_user_namespaces_withholds_every_exec_fact` | with a bwrap that refuses both options, no `exec.` key is published | **red** |\n\nRun of these cases alone, `cargo test -p b10x-substrate-host --release --locked --test user_namespace_posture_adversarial`, exit 101:\n\n```\nrunning 4 tests\ntest the_posture_check_catches_an_eighth_sandbox_that_spells_the_user_namespace_differently ... FAILED\ntest the_posture_check_catches_an_exec_argv_that_stopped_splicing_the_constant ... FAILED\ntest the_re_expressed_posture_rule_agrees_with_the_shipped_case_on_the_unmodified_tree ... ok\ntest a_backend_that_cannot_disable_nested_user_namespaces_withholds_every_exec_fact ... FAILED\n\n---- the_posture_check_catches_an_eighth_sandbox_that_spells_the_user_namespace_differently stdout ----\npanicked at crates/substrate-host/tests/user_namespace_posture_adversarial.rs:179:5:\na sandbox spelled --unshare-all creates a user namespace with nesting left open and the class check\nis green, so `an eighth sandbox gets the posture or gets a red case` (process.rs:45-46) does not\nhold for --unshare-all, --unshare-user-try or --userns\n\n---- the_posture_check_catches_an_exec_argv_that_stopped_splicing_the_constant stdout ----\npanicked at crates/substrate-host/tests/user_namespace_posture_adversarial.rs:132:5:\nthe exec argv no longer carries --unshare-user or --disable-userns and the class check is still\ngreen: it proves the constant is well-formed, not that any sandbox uses it, so review finding 6's\nexact shape — six argv lists right and the exec one wrong — passes it\n\n---- a_backend_that_cannot_disable_nested_user_namespaces_withholds_every_exec_fact stdout ----\npanicked at crates/substrate-host/tests/user_namespace_posture_adversarial.rs:239:5:\nassertion `left == right` failed: probe.rs:369 says such a backend leaves `every exec fact\n**absent**`; these are still published, so a client reading the capability document sees exec facts\nserved by a host whose confinement floor was never proved\n  left: [\"exec.max-current\", \"exec.output-limit-bytes\"]\n right: []\n\ntest result: FAILED. 1 passed; 3 failed; 0 ignored; 0 measured; 0 filtered out\n```\n\nPackage gate after the cases existed: **exit 101**, 84 → 88 executed. Delegated host lane: exit 101,\nsame result; the unit's own acceptance passed there\n(`process::tests::a_confined_process_cannot_nest_a_user_namespace ... ok`).\n\n## Findings\n\n| # | file:line | verdict | origin | measured | reaches it |\n|---|---|---|---|---|---|\n| F1 | `crates/substrate-host/src/process.rs:45` | needs-revision | introduced | test:132, exit 101 — the class check stays green with the exec argv's only splice deleted | nothing today; the claim is about future edits. Constructed corpus |\n| F2 | `crates/substrate-host/src/process.rs:46` | needs-revision | introduced | test:179, exit 101 — the check stays green with an `--unshare-all` sandbox added | nothing today; no in-tree site uses that spelling. Constructed corpus |\n| F3 | `crates/substrate-host/src/probe.rs:369` | needs-revision | introduced | test:239, exit 101 — `[\"exec.max-current\",\"exec.output-limit-bytes\"]` still published | any host whose bwrap predates `--disable-userns`. Real, but no exec is admitted: `operations.rs:502` gates on `exec_namespaces` |\n| F4 | `docs/design/15-docker-driver-entry-gate.md:152` | needs-revision | introduced | read, not run — the floor table has no row for nested user namespaces | a second driver admitted through that gate. Judgement |\n| F5 | `crates/substrate-host/src/probe.rs:371` | needs-revision | introduced | read, not run — no case pins `--assert-userns-disabled` | every run of the shipped probe case. Judgement |\n\n**F1 — the class check does not check the class.** `process.rs:45-46` claims the check makes an\neighth sandbox get the posture or get a red case. The check reads the crate's sources for\n`\"--unshare-user\"` and proves the *constant* is written once with both halves. It never reads\nwhether any argv **uses** it. Delete `process.rs:1834` `command.args(USER_NAMESPACE_ARGV);` and the\ntree is back in review finding 6's exact shape — six argv lists carrying the posture, the exec one\nnot — and the check is green. On the portable lane nothing else covers it: the acceptance case is\nabsent without `SUBSTRATE_VECTORS_CGROUP_ROOT`, and the probe case only exercises `probe.rs`. The\nsame blindness covers `pty.rs`'s new comment about splicing at every use site. Fix not applied:\nassert on the built argv, not the source text — a recording `bwrap` stub in `config.bubblewrap`\nplus one exec through the public `HostDriver`, asserting the captured argv carries both options.\nThat runs on the portable lane and pins all seven sites.\n\n**F2 — three other spellings evade the grep.** `bwrap --help` on 0.11.2 lists `--unshare-all`\n(includes the user namespace), `--unshare-user-try` and `--userns FD`. None contains the needle —\n`--unshare-user-try` does not, because the needle carries the closing quote. Also\n`std::fs::read_dir` is non-recursive, so a future `src/sandbox/mod.rs` is invisible regardless of\nspelling.\n\n**F3 — \"every exec fact absent\" is not what the snapshot does.** `probe.rs:366-369`, `probe.rs:876`\nand the commit message all state such a backend leaves *every* exec fact absent. Measured through\n`HostDriver::open` → `machine()`, `exec.max-current` and `exec.output-limit-bytes` are still\npublished. The behaviour reproduces at the base (`617bbed:probe.rs:126-127`), so the *code* half is\npre-existing; the **contradiction** is new, because this diff wrote the sentence. Cheapest correct\nfix is the sentence.\n\n**F4 — a new floor clause three floor documents do not name.** AGENTS.md's enforced isolation set\n(`:78-84`), the docker entry gate's clause-by-clause table, and\n`docs/design/10-destination-bound-egress.md:34` (which still describes the posture as\n`--unshare-net` with `--unshare-user/ipc/pid/uts`). The design-10 citation was already stale at\nbase, so that half is pre-existing.\n\n**F5 — the one line the story's Notes name is a free mutant.** The shipped probe case's stub refuses\n`--disable-userns` *and* `--assert-userns-disabled`; since the former arrives via\n`USER_NAMESPACE_ARGV`, deleting `.arg(\"--assert-userns-disabled\")` at `probe.rs:371` leaves the\ncase green. Not reproduced as a red case: `probe_bubblewrap` is private and the mutation is\nbehavioural, which would need an implementation edit.\n\n## Attacked and could not break\n\n- **The flag itself works.** Inside the exact exec posture, `unshare -U` fails\n  (`unshare failed: No space left on device`, exit 1), `unshare(CLONE_NEWUSER)` returns ENOSPC, and\n  a direct `syscall(56, CLONE_NEWUSER|SIGCHLD)` returns ENOSPC. There is no open route left for\n  `seccomp.rs` to close, so this is not a pre-existing seccomp finding.\n- **Re-raising the limit from inside fails.** Empty capability set;\n  `/proc/sys/user/max_user_namespaces` write is EACCES and nesting stays refused after.\n- **All seven argv sites are the seven.** Repo-wide grep for `--unshare-*` finds bubblewrap argv\n  construction only in this crate, at the seven named sites.\n- **No conditional strips the posture from the exec argv.** `command.args(USER_NAMESPACE_ARGV)` is\n  unconditional in `ProcessRuntime::command`; no profile, network mode, capsule, aperture, scratch\n  or terminal branch removes it.\n- **The exec facts do not admit anything they should not.** `operations.rs:502` and\n  `pipe_confinement_available` both require `exec_namespaces`, so F3 leaks no admission.\n- **`--disable-userns` does not break the egress forwarder's `setns` handback or the pty probe** —\n  73 lib cases and all 6 pty acceptance cases pass on the delegated lane.\n- **Not run, deliberately:** the daemon / SDK / MCP packages and the full `scripts/delegated-lane.sh`\n  — disk at 99%, package gate only.\n\n```findings\n- file: crates/substrate-host/src/process.rs\n  line: 45\n  category: mutant\n  severity: blocker\n  verdict: needs-revision\n  origin: introduced\n  message: >-\n    The class check proves the constant is well-formed, not that any argv splices it, so review\n    finding 6's exact shape passes it. Delete the single line process.rs:1834\n    `command.args(USER_NAMESPACE_ARGV);` and the exec argv carries neither --unshare-user nor\n    --disable-userns, a confined process can nest a user namespace again, and\n    the_user_namespace_posture_is_written_in_exactly_one_place is still green. The portable lane\n    covers nothing else: the acceptance case is absent without SUBSTRATE_VECTORS_CGROUP_ROOT and\n    the probe case only exercises probe.rs. Measured at tests/user_namespace_posture_adversarial.rs:132, exit 101.\n- file: crates/substrate-host/src/process.rs\n  line: 46\n  category: contract-drift\n  severity: warning\n  verdict: needs-revision\n  origin: introduced\n  message: >-\n    \"An eighth sandbox gets the posture or gets a red case\" is false for --unshare-all,\n    --unshare-user-try and --userns, and for any source file in a subdirectory of src/. Add a\n    sandbox spelled .args([\"--unshare-all\", ...]) with no --disable-userns and the child gets a\n    user namespace with nesting open while the check reports exactly one site, green. The needle\n    carries a closing quote so --unshare-user-try does not match, and read_dir is non-recursive so\n    a future src/sandbox/mod.rs is not read at all. Measured at\n    tests/user_namespace_posture_adversarial.rs:179, exit 101.\n- file: crates/substrate-host/src/probe.rs\n  line: 369\n  category: contract-drift\n  severity: warning\n  verdict: needs-revision\n  origin: introduced\n  message: >-\n    The new comment, the new test doc at :876 and the commit message all say such a backend leaves\n    every exec fact absent; two are still published. HostDriver::open with a bwrap that exits\n    non-zero on --disable-userns and --assert-userns-disabled still serializes exec.max-current and\n    exec.output-limit-bytes. The ungated lines reproduce at base 617bbed probe.rs:126-127, so only\n    the claim is new. No admission leaks: operations.rs:502 gates on exec.namespaces. Measured at\n    tests/user_namespace_posture_adversarial.rs:239, exit 101.\n- file: docs/design/15-docker-driver-entry-gate.md\n  line: 152\n  category: judgement\n  severity: note\n  verdict: needs-revision\n  origin: introduced\n  message: >-\n    A new floor clause was added without a row in the clause-by-clause floor table, without an entry\n    in AGENTS.md's enforced isolation set (:78-84), and docs/design/10-destination-bound-egress.md:34\n    still describes the posture as --unshare-user/ipc/pid/uts. A container driver walked through\n    this table's floor clauses can be admitted while leaving nested user namespaces open, because\n    no row requires them closed. The design-10 citation was already stale at base, so that half is\n    pre-existing. Read, not run.\n- file: crates/substrate-host/src/probe.rs\n  line: 371\n  category: mutant\n  severity: warning\n  verdict: needs-revision\n  origin: introduced\n  message: >-\n    The shipped probe case's stub refuses both options, so the --assert-userns-disabled line the\n    story's Notes name specifically is pinned by nothing. Deleting .arg(\"--assert-userns-disabled\")\n    leaves a_backend_that_cannot_disable_nested_user_namespaces_withholds_the_exec_floor green,\n    because the stub still refuses --disable-userns, which arrives via USER_NAMESPACE_ARGV. Not\n    reproduced as a red case: probe_bubblewrap is private and the mutation is behavioural, which\n    would require an implementation edit. Read, not run.\n```\n\n## Coordinator note on this record\n\nThe adversary returned its findings block keyed `summary` / `failure_scenario`, because the brief\ntemplate this wave dispatched with named those fields. The store's schema is\n`{file, line, category, severity, verdict, origin, message}`, and its severity vocabulary is\n`blocker | warning | note`, not `blocker | major | minor`. The block above is the same five findings\nwith `summary` and `failure_scenario` merged into `message`, and severity re-mapped: the agent's\n`major` (F1) to `blocker`, its `minor` to `warning` for F2, F3 and F5, and to `note` for F4, which\nis a documentation judgement it did not run. No finding was added, dropped or reworded beyond\nthat. The brief template was corrected before the next adversary was\ndispatched. The prose above the block is the agent's own text unedited.\n","title":"Adversary pass 1, unit u4 (disable-userns)"},"args":{"command":"create-entity","data":{"body":"# Adversary pass 1 — unit u4, `story:confined-processes-cannot-nest-user-namespaces`\n\nAgent type `adp:adversary`. Worktree `wt-5698af5e5dcf`, branch `impl/disable-userns`,\nHEAD `a941dcd`, base `617bbed`. Report as returned.\n\n```\nunit: u4 — story:confined-processes-cannot-nest-user-namespaces\nverdict: red\ncases: executed 84→88, red 3\norigin: introduced 4, pre-existing 0, undecided 0\nwrote-outside-worktree: 5 paths\nneeds-coordinator: no\n```\n\n## Cases added\n\n`crates/substrate-host/tests/user_namespace_posture_adversarial.rs` (untracked; no implementation\nfile touched, `seccomp.rs` untouched).\n\n| case | asserts | result |\n|---|---|---|\n| `the_re_expressed_posture_rule_agrees_with_the_shipped_case_on_the_unmodified_tree` | control: the re-expressed rule is green on the untouched tree | green |\n| `the_posture_check_catches_an_exec_argv_that_stopped_splicing_the_constant` | with `command.args(USER_NAMESPACE_ARGV);` deleted, the check reports a violation | **red** |\n| `the_posture_check_catches_an_eighth_sandbox_that_spells_the_user_namespace_differently` | with an `--unshare-all` sandbox added, the check reports a violation | **red** |\n| `a_backend_that_cannot_disable_nested_user_namespaces_withholds_every_exec_fact` | with a bwrap that refuses both options, no `exec.` key is published | **red** |\n\nRun of these cases alone, `cargo test -p b10x-substrate-host --release --locked --test user_namespace_posture_adversarial`, exit 101:\n\n```\nrunning 4 tests\ntest the_posture_check_catches_an_eighth_sandbox_that_spells_the_user_namespace_differently ... FAILED\ntest the_posture_check_catches_an_exec_argv_that_stopped_splicing_the_constant ... FAILED\ntest the_re_expressed_posture_rule_agrees_with_the_shipped_case_on_the_unmodified_tree ... ok\ntest a_backend_that_cannot_disable_nested_user_namespaces_withholds_every_exec_fact ... FAILED\n\n---- the_posture_check_catches_an_eighth_sandbox_that_spells_the_user_namespace_differently stdout ----\npanicked at crates/substrate-host/tests/user_namespace_posture_adversarial.rs:179:5:\na sandbox spelled --unshare-all creates a user namespace with nesting left open and the class check\nis green, so `an eighth sandbox gets the posture or gets a red case` (process.rs:45-46) does not\nhold for --unshare-all, --unshare-user-try or --userns\n\n---- the_posture_check_catches_an_exec_argv_that_stopped_splicing_the_constant stdout ----\npanicked at crates/substrate-host/tests/user_namespace_posture_adversarial.rs:132:5:\nthe exec argv no longer carries --unshare-user or --disable-userns and the class check is still\ngreen: it proves the constant is well-formed, not that any sandbox uses it, so review finding 6's\nexact shape — six argv lists right and the exec one wrong — passes it\n\n---- a_backend_that_cannot_disable_nested_user_namespaces_withholds_every_exec_fact stdout ----\npanicked at crates/substrate-host/tests/user_namespace_posture_adversarial.rs:239:5:\nassertion `left == right` failed: probe.rs:369 says such a backend leaves `every exec fact\n**absent**`; these are still published, so a client reading the capability document sees exec facts\nserved by a host whose confinement floor was never proved\n  left: [\"exec.max-current\", \"exec.output-limit-bytes\"]\n right: []\n\ntest result: FAILED. 1 passed; 3 failed; 0 ignored; 0 measured; 0 filtered out\n```\n\nPackage gate after the cases existed: **exit 101**, 84 → 88 executed. Delegated host lane: exit 101,\nsame result; the unit's own acceptance passed there\n(`process::tests::a_confined_process_cannot_nest_a_user_namespace ... ok`).\n\n## Findings\n\n| # | file:line | verdict | origin | measured | reaches it |\n|---|---|---|---|---|---|\n| F1 | `crates/substrate-host/src/process.rs:45` | needs-revision | introduced | test:132, exit 101 — the class check stays green with the exec argv's only splice deleted | nothing today; the claim is about future edits. Constructed corpus |\n| F2 | `crates/substrate-host/src/process.rs:46` | needs-revision | introduced | test:179, exit 101 — the check stays green with an `--unshare-all` sandbox added | nothing today; no in-tree site uses that spelling. Constructed corpus |\n| F3 | `crates/substrate-host/src/probe.rs:369` | needs-revision | introduced | test:239, exit 101 — `[\"exec.max-current\",\"exec.output-limit-bytes\"]` still published | any host whose bwrap predates `--disable-userns`. Real, but no exec is admitted: `operations.rs:502` gates on `exec_namespaces` |\n| F4 | `docs/design/15-docker-driver-entry-gate.md:152` | needs-revision | introduced | read, not run — the floor table has no row for nested user namespaces | a second driver admitted through that gate. Judgement |\n| F5 | `crates/substrate-host/src/probe.rs:371` | needs-revision | introduced | read, not run — no case pins `--assert-userns-disabled` | every run of the shipped probe case. Judgement |\n\n**F1 — the class check does not check the class.** `process.rs:45-46` claims the check makes an\neighth sandbox get the posture or get a red case. The check reads the crate's sources for\n`\"--unshare-user\"` and proves the *constant* is written once with both halves. It never reads\nwhether any argv **uses** it. Delete `process.rs:1834` `command.args(USER_NAMESPACE_ARGV);` and the\ntree is back in review finding 6's exact shape — six argv lists carrying the posture, the exec one\nnot — and the check is green. On the portable lane nothing else covers it: the acceptance case is\nabsent without `SUBSTRATE_VECTORS_CGROUP_ROOT`, and the probe case only exercises `probe.rs`. The\nsame blindness covers `pty.rs`'s new comment about splicing at every use site. Fix not applied:\nassert on the built argv, not the source text — a recording `bwrap` stub in `config.bubblewrap`\nplus one exec through the public `HostDriver`, asserting the captured argv carries both options.\nThat runs on the portable lane and pins all seven sites.\n\n**F2 — three other spellings evade the grep.** `bwrap --help` on 0.11.2 lists `--unshare-all`\n(includes the user namespace), `--unshare-user-try` and `--userns FD`. None contains the needle —\n`--unshare-user-try` does not, because the needle carries the closing quote. Also\n`std::fs::read_dir` is non-recursive, so a future `src/sandbox/mod.rs` is invisible regardless of\nspelling.\n\n**F3 — \"every exec fact absent\" is not what the snapshot does.** `probe.rs:366-369`, `probe.rs:876`\nand the commit message all state such a backend leaves *every* exec fact absent. Measured through\n`HostDriver::open` → `machine()`, `exec.max-current` and `exec.output-limit-bytes` are still\npublished. The behaviour reproduces at the base (`617bbed:probe.rs:126-127`), so the *code* half is\npre-existing; the **contradiction** is new, because this diff wrote the sentence. Cheapest correct\nfix is the sentence.\n\n**F4 — a new floor clause three floor documents do not name.** AGENTS.md's enforced isolation set\n(`:78-84`), the docker entry gate's clause-by-clause table, and\n`docs/design/10-destination-bound-egress.md:34` (which still describes the posture as\n`--unshare-net` with `--unshare-user/ipc/pid/uts`). The design-10 citation was already stale at\nbase, so that half is pre-existing.\n\n**F5 — the one line the story's Notes name is a free mutant.** The shipped probe case's stub refuses\n`--disable-userns` *and* `--assert-userns-disabled`; since the former arrives via\n`USER_NAMESPACE_ARGV`, deleting `.arg(\"--assert-userns-disabled\")` at `probe.rs:371` leaves the\ncase green. Not reproduced as a red case: `probe_bubblewrap` is private and the mutation is\nbehavioural, which would need an implementation edit.\n\n## Attacked and could not break\n\n- **The flag itself works.** Inside the exact exec posture, `unshare -U` fails\n  (`unshare failed: No space left on device`, exit 1), `unshare(CLONE_NEWUSER)` returns ENOSPC, and\n  a direct `syscall(56, CLONE_NEWUSER|SIGCHLD)` returns ENOSPC. There is no open route left for\n  `seccomp.rs` to close, so this is not a pre-existing seccomp finding.\n- **Re-raising the limit from inside fails.** Empty capability set;\n  `/proc/sys/user/max_user_namespaces` write is EACCES and nesting stays refused after.\n- **All seven argv sites are the seven.** Repo-wide grep for `--unshare-*` finds bubblewrap argv\n  construction only in this crate, at the seven named sites.\n- **No conditional strips the posture from the exec argv.** `command.args(USER_NAMESPACE_ARGV)` is\n  unconditional in `ProcessRuntime::command`; no profile, network mode, capsule, aperture, scratch\n  or terminal branch removes it.\n- **The exec facts do not admit anything they should not.** `operations.rs:502` and\n  `pipe_confinement_available` both require `exec_namespaces`, so F3 leaks no admission.\n- **`--disable-userns` does not break the egress forwarder's `setns` handback or the pty probe** —\n  73 lib cases and all 6 pty acceptance cases pass on the delegated lane.\n- **Not run, deliberately:** the daemon / SDK / MCP packages and the full `scripts/delegated-lane.sh`\n  — disk at 99%, package gate only.\n\n```findings\n- file: crates/substrate-host/src/process.rs\n  line: 45\n  category: mutant\n  severity: blocker\n  verdict: needs-revision\n  origin: introduced\n  message: >-\n    The class check proves the constant is well-formed, not that any argv splices it, so review\n    finding 6's exact shape passes it. Delete the single line process.rs:1834\n    `command.args(USER_NAMESPACE_ARGV);` and the exec argv carries neither --unshare-user nor\n    --disable-userns, a confined process can nest a user namespace again, and\n    the_user_namespace_posture_is_written_in_exactly_one_place is still green. The portable lane\n    covers nothing else: the acceptance case is absent without SUBSTRATE_VECTORS_CGROUP_ROOT and\n    the probe case only exercises probe.rs. Measured at tests/user_namespace_posture_adversarial.rs:132, exit 101.\n- file: crates/substrate-host/src/process.rs\n  line: 46\n  category: contract-drift\n  severity: warning\n  verdict: needs-revision\n  origin: introduced\n  message: >-\n    \"An eighth sandbox gets the posture or gets a red case\" is false for --unshare-all,\n    --unshare-user-try and --userns, and for any source file in a subdirectory of src/. Add a\n    sandbox spelled .args([\"--unshare-all\", ...]) with no --disable-userns and the child gets a\n    user namespace with nesting open while the check reports exactly one site, green. The needle\n    carries a closing quote so --unshare-user-try does not match, and read_dir is non-recursive so\n    a future src/sandbox/mod.rs is not read at all. Measured at\n    tests/user_namespace_posture_adversarial.rs:179, exit 101.\n- file: crates/substrate-host/src/probe.rs\n  line: 369\n  category: contract-drift\n  severity: warning\n  verdict: needs-revision\n  origin: introduced\n  message: >-\n    The new comment, the new test doc at :876 and the commit message all say such a backend leaves\n    every exec fact absent; two are still published. HostDriver::open with a bwrap that exits\n    non-zero on --disable-userns and --assert-userns-disabled still serializes exec.max-current and\n    exec.output-limit-bytes. The ungated lines reproduce at base 617bbed probe.rs:126-127, so only\n    the claim is new. No admission leaks: operations.rs:502 gates on exec.namespaces. Measured at\n    tests/user_namespace_posture_adversarial.rs:239, exit 101.\n- file: docs/design/15-docker-driver-entry-gate.md\n  line: 152\n  category: judgement\n  severity: note\n  verdict: needs-revision\n  origin: introduced\n  message: >-\n    A new floor clause was added without a row in the clause-by-clause floor table, without an entry\n    in AGENTS.md's enforced isolation set (:78-84), and docs/design/10-destination-bound-egress.md:34\n    still describes the posture as --unshare-user/ipc/pid/uts. A container driver walked through\n    this table's floor clauses can be admitted while leaving nested user namespaces open, because\n    no row requires them closed. The design-10 citation was already stale at base, so that half is\n    pre-existing. Read, not run.\n- file: crates/substrate-host/src/probe.rs\n  line: 371\n  category: mutant\n  severity: warning\n  verdict: needs-revision\n  origin: introduced\n  message: >-\n    The shipped probe case's stub refuses both options, so the --assert-userns-disabled line the\n    story's Notes name specifically is pinned by nothing. Deleting .arg(\"--assert-userns-disabled\")\n    leaves a_backend_that_cannot_disable_nested_user_namespaces_withholds_the_exec_floor green,\n    because the stub still refuses --disable-userns, which arrives via USER_NAMESPACE_ARGV. Not\n    reproduced as a red case: probe_bubblewrap is private and the mutation is behavioural, which\n    would require an implementation edit. Read, not run.\n```\n\n## Coordinator note on this record\n\nThe adversary returned its findings block keyed `summary` / `failure_scenario`, because the brief\ntemplate this wave dispatched with named those fields. The store's schema is\n`{file, line, category, severity, verdict, origin, message}`, and its severity vocabulary is\n`blocker | warning | note`, not `blocker | major | minor`. The block above is the same five findings\nwith `summary` and `failure_scenario` merged into `message`, and severity re-mapped: the agent's\n`major` (F1) to `blocker`, its `minor` to `warning` for F2, F3 and F5, and to `note` for F4, which\nis a documentation judgement it did not run. No finding was added, dropped or reworded beyond\nthat. The brief template was corrected before the next adversary was\ndispatched. The prose above the block is the agent's own text unedited.\n","status":"active","title":"Adversary pass 1, unit u4 (disable-userns)"},"entity_type":"aep.review-result/v1","locator":"ep://planning/store/review-result/adversary-u4-pass-1"},"payload":{"actor":"human:timo","at":1788493588485,"causation":"cmd-new-review-result-adversary-u4-pass-1","change":{"change":"created","status":"active"},"correlation":"protocol-artifact-new","event_id":"review-result:adversary-u4-pass-1@1#0~ac9f9ade7d54e88b","executor":null,"recorded_at":"2026-09-04T03:46:28Z"}}
{"entity":"review-result","version":1,"id":"adversary-u4-pass-1","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"story:confined-processes-cannot-nest-user-namespaces"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000713","target":"01MEM0000000000000076"},"payload":{"actor":"human:timo","at":1788493588498,"causation":"cmd-review-result-adversary-u4-pass-1-reviews-story-confined-processes-cannot-nest-user-namespaces","change":{"change":"related","relation":"reviews","target":"story:confined-processes-cannot-nest-user-namespaces"},"correlation":"protocol-artifact-relate","event_id":"review-result:adversary-u4-pass-1@1#0~13abac0d12a2745c","executor":null,"recorded_at":"2026-09-04T03:46:28Z"}}
{"entity":"review-result","version":1,"id":"adversary-u3-pass-1","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"active","changed":{"body":"# Adversary pass 1 — unit u3, `story:unattached-claimed-session-is-contained`\n\nAgent type `adp:adversary`. Worktree `wt-b180f053180d`, branch `impl/unattached-session-contained`,\nHEAD `4cbc953` plus 3 uncommitted test cases, base `617bbed`. Report as returned.\n\n```\nunit: u3 story:unattached-claimed-session-is-contained\nverdict: red\ncases: executed 128→131, red 2\norigin: introduced 3, pre-existing 2, undecided 0\nwrote-outside-worktree: u3-target (assigned build dir, cargo artifacts only)\nneeds-coordinator: no\n```\n\n## Cases added\n\n`crates/substrate-daemon/tests/pipe_session.rs`, +236, one path, no implementation file touched.\nThe fixture edit is additive — `PipeFixtureDriver.refuse_signal: AtomicBool`, default `false`,\nguarded at the top of `signal`; all 25 pre-existing cases in that binary still pass.\n\n| line | case | result |\n|---|---|---|\n| 2365 | `adversary_a_client_that_drops_after_the_switch_leaves_no_running_process` — the acceptance sentence on the *production* server shape (`with_upgrades()`), plus the refusal's class/status/retriable against the published `session.not-attachable` row | green |\n| 2444 | `adversary_a_stranded_attach_with_an_unproven_kill_keeps_the_bounded_capacity` | **red** |\n| 2523 | `adversary_a_stranded_attach_whose_kill_is_unproven_is_not_contained_by_maintenance` | **red** |\n\nRed output, each run alone, before any suite run:\n\n```\nthread 'adversary_a_stranded_attach_with_an_unproven_kill_keeps_the_bounded_capacity' panicked at\ncrates/substrate-daemon/tests/pipe_session.rs:2488:13:\nattachment 31 of the bounded 32 is still answered 429 twenty seconds after one stranded attach\nwhose kill could not be proven: the failed-upgrade containment retained the tombstone, and\nPipeAttachmentPermit::drop then ran global.forget(), so one of this daemon's 32 attachment slots is\ngone until restart. On 617bbed the same disconnect always returned the slot.\ntest result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 25 filtered out; finished in 21.26s\n```\n\n```\nthread 'adversary_a_stranded_attach_whose_kill_is_unproven_is_not_contained_by_maintenance' panicked\nat crates/substrate-daemon/tests/pipe_session.rs:2564:30:\na claimed session whose upgrade never completed is still running after ten seconds and many\nmaintenance ticks, because the single containment attempt was refused and nothing retries it; the\nacceptance asks for no running process within one maintenance tick: Elapsed(())\ntest result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 26 filtered out; finished in 10.67s\n```\n\nSuite after the cases existed: `cargo test -p b10x-substrate-daemon --release --locked\n--no-fail-fast`, exit **101**. 131 executed. `pipe_session` 25 passed / 2 failed; every other lane\ngreen, including `runtime_vectors` 8/8 — the known flake did not fire.\n\n## Findings\n\n**F1 — one stranded attach whose kill is unproven permanently destroys a global attachment slot.**\n`sessions.rs:1300`. Measured at `tests/pipe_session.rs:2488`, exit 101: after **one** stranded\nattach with a refusing driver, only 31 of the daemon's 32 attachment slots are ever servable again;\nthe 32nd is `429 session.attachment-capacity` for the life of the process.\n`retain_attachment_tombstone()` → `Drop` takes the `else` arm at `:151` and runs `global.forget()`.\nReachability: `on_failed_upgrade` is production-reachable — all three listeners call\n`.with_upgrades()` (`runtime.rs:595`, `:918`, `:1083`), so a connection error before hyper fulfils\nthe upgrade resolves the future with `Canceled` and enters this closure. The\n`terminate_pipe_session == false` half was **constructed** with a fixture driver that refuses\n`signal`; the code's own doc comment and `MAINTENANCE_DRIVER_TIMEOUT` name that state as expected,\nbut no production driver was observed producing it. Origin `introduced`: at `617bbed` the file has\nno `on_failed_upgrade`, so a failed upgrade always dropped the permit with\n`remove_key_on_drop == true` and returned the slot. Fix not applied: do not retain the tombstone on\nthe failed-upgrade path — the durable claim is already `Consumed`, so the state gate at\n`sessions.rs:1149` refuses every further attach *before* `acquire` at `:1167` is called. The\nprocess-local tombstone cannot be consulted for that session again; it buys nothing and costs a slot.\n\n**F2 — the containment is one attempt with no retry and no backstop.** `sessions.rs:1299`. Measured\nat `tests/pipe_session.rs:2564`, exit 101: with a 500 ms transient driver refusal the stranded exec\nis still `\"running\"` after 10 s and ~200 explicit `App::sweep_expired()` ticks. The acceptance asks\nfor \"no running process within one maintenance tick\". The only backstop is `cleanup_expired`\n(`app/service.rs:645`), which acts on an **expired lease** — the very \"until its lease or timeout\nended it\" the story's Context calls the defect. Origin `pre-existing`: the residual reproduces at\n`617bbed`, where this path has no containment at all, so HEAD is strictly better. It is an unclosed\npart of the unit's own acceptance statement, not a regression.\n\n**F3 — the unit's shipped case builds a state no daemon listener can be in.**\n`tests/pipe_session.rs:989`. `spawn_without_upgrades` reaches `on_failed_upgrade` through hyper's\n`Pending::manual()` — the branch taken only when `with_upgrades()` was **not** called. All three\ndaemon listeners call it. The adversary's green case at `:2365` shows a real client drop after the\n`101` on a `with_upgrades()` listener is contained by the *other* hand-off (`on_upgrade` →\n`run_pipe_attachment` sees EOF → `terminate_pipe_session`), on a code path byte-identical at\n`617bbed`. Nothing production-side reaches `manual()`. The production trigger for the new closure is\n`Canceled` after a connection error in the flush window, and no case in the tree builds it. So the\nordinary client drop the story describes was already contained on base, and the change's residual\nvalue is unproven by its own suite. Origin `introduced` — it is about the case this unit added.\n\n**F4 — the containment discards its own outcome and says nothing.** `sessions.rs:1298`. Read, not\nrun. The closure body is a bare `tokio::spawn`; no `warn!`, no event, no metric on either branch,\nwhile `runtime.rs` and `service.rs` log every comparable maintenance failure. A containment that\nfails is invisible — invariant 3's silent degradation. It is also the only detached `tokio::spawn`\nin the daemon's non-test `app/` code, untracked by the connection `JoinSet` that `runtime.rs:621`\naborts at shutdown. Origin `introduced`; the sibling `on_upgrade` path at `:1319` is equally silent\nand pre-existing.\n\n**F5 — two doc comments about the same branch contradict each other, and F1 measures which is\nfalse.** `sessions.rs:134` says \"Capacity is still recovered\"; `:149` says the branch \"consumes one\nof the fixed global attachment slots until daemon restart\". F1's red case shows `:149` is the true\none. Origin `pre-existing` text; the unit edited exactly those lines (`&mut self` → `&self`) and\nleft the false sentence standing.\n\n## Attacked, could not break\n\n- **Memory ordering.** `Relaxed` store in `retain_attachment_tombstone` vs `get_mut()` in `Drop`:\n  `Arc`'s `Release` decrement plus `Acquire` fence orders the store before the drop on any thread.\n  Sufficient.\n- **\"Exactly one hand-off runs.\"** axum 0.8.9 `on_upgrade` (`ws.rs:359`) calls\n  `on_failed_upgrade.call()` only in the `Err` arm and `callback` only in the `Ok` arm of one\n  spawned task, and owns both closures — neither twice, never both, and the permit's `Drop` always\n  runs after whichever ran.\n- **\"No fallible code between the claim and `.on_upgrade`.\"** Holds. `store_io` →\n  `bounded_blocking` → `Semaphore::acquire_owned().await` then `block_in_place`; the claim and the\n  whole tail of the handler complete inside one poll, so the handler future cannot be dropped\n  between them.\n- **Builder ordering.** `WebSocketUpgrade::on_failed_upgrade` copies every `WebSocketConfig` field,\n  so placing it after the five buffer/frame builders loses none of them.\n- **Refusal register conformance.** The contained session's refusal matches\n  `contracts/substrate-wire/0.10.0/refusals.json`'s `session.not-attachable` row on class, status\n  and retriable (asserted green at `:2365`).\n- **The happy containment path.** When the kill succeeds, the slot and the `attached` key both come\n  back — indices 0..30 of the capacity case take them.\n\n```findings\n- file: crates/substrate-daemon/src/app/sessions.rs\n  line: 1300\n  category: boundary\n  severity: blocker\n  verdict: needs-revision\n  origin: introduced\n  message: >-\n    The failed-upgrade containment retains the attachment tombstone when it cannot prove the kill,\n    and Drop then runs global.forget(), permanently destroying one of the 32 global attachment slots\n    on a path that always returned the slot at 617bbed. One attach whose upgrade fails while the\n    driver refuses the signal -> terminate_pipe_session returns false -> retain_attachment_tombstone\n    -> Drop takes the else arm at sessions.rs:151 -> the daemon serves only 31 concurrent\n    attachments for the rest of the process's life and answers the 32nd 429\n    session.attachment-capacity forever. Measured at tests/pipe_session.rs:2488, exit 101.\n- file: crates/substrate-daemon/src/app/sessions.rs\n  line: 1299\n  category: acceptance\n  severity: blocker\n  verdict: needs-revision\n  origin: pre-existing\n  message: >-\n    The containment is a single unretried kill with no maintenance backstop, so a transient driver\n    refusal returns the daemon to the pre-fix behaviour the story exists to remove and the\n    acceptance sentence \"no running process within one maintenance tick\" is not met. Stranded attach\n    plus a 500 ms driver refusal, driver healthy again immediately -> the exec is still \"running\"\n    after 10 s and ~200 explicit App::sweep_expired() ticks; the only backstop is cleanup_expired at\n    app/service.rs:645, which fires on lease expiry, which is exactly what the story's Context calls\n    the defect. Measured at tests/pipe_session.rs:2564, exit 101. Reproduces at 617bbed, so HEAD is\n    strictly better and this is an unclosed part of the acceptance rather than a regression.\n- file: crates/substrate-daemon/tests/pipe_session.rs\n  line: 989\n  category: judgement\n  severity: warning\n  verdict: needs-revision\n  origin: introduced\n  message: >-\n    The case shipped as proof reaches on_failed_upgrade through hyper's Pending::manual(), a branch\n    taken only when with_upgrades() was not called, while all three daemon listeners call it, so the\n    new code's production trigger is exercised by nothing in the tree. A real client dropping\n    straight after the 101 on a with_upgrades() listener is contained by the other hand-off\n    (on_upgrade -> run_pipe_attachment sees EOF -> terminate_pipe_session), a path byte-identical at\n    617bbed; the adversary's green case at tests/pipe_session.rs:2365 shows it, so the story's\n    stated scenario was already contained on base and the residual window the change covers is\n    untested. Read plus one green case, not a red one.\n- file: crates/substrate-daemon/src/app/sessions.rs\n  line: 1298\n  category: judgement\n  severity: warning\n  verdict: needs-revision\n  origin: introduced\n  message: >-\n    The containment runs in a bare detached tokio::spawn that discards its own outcome and logs\n    nothing on either branch, so a containment that fails is invisible - invariant 3's silent\n    degradation - and it is the only untracked task in the daemon's non-test app/ code, outside the\n    connection JoinSet that runtime.rs:621 aborts at shutdown. terminate_pipe_session returns false\n    -> no warn!, no event, no metric; the operator sees a session whose process is still running and\n    no record that the daemon tried and failed to end it, while runtime.rs and app/service.rs log\n    every comparable maintenance failure. Read, not run.\n- file: crates/substrate-daemon/src/app/sessions.rs\n  line: 134\n  category: contract-drift\n  severity: note\n  verdict: needs-revision\n  origin: pre-existing\n  message: >-\n    retain_attachment_tombstone's doc comment says \"Capacity is still recovered\" while Drop's own\n    comment fifteen lines below says the branch \"consumes one of the fixed global attachment slots\n    until daemon restart\"; the unit edited these exact lines and left the false sentence standing. A\n    reader choosing where to call retain_attachment_tombstone is told capacity is safe;\n    tests/pipe_session.rs:2488 measures that global.forget() runs and the slot never returns, so\n    :149 is the true sentence and :134 is not.\n```\n\n## Coordinator note on this record\n\nThis agent was dispatched before the brief template's findings schema was corrected, so it returned\nthe block keyed `summary` / `failure_scenario` with severities `major` / `minor`. The block above is\nthe same five findings with those two fields merged into `message`, and severity mapped to the\nstore's vocabulary: `major` to `blocker` (F1, F2), `minor` to `warning` (F3, F4) and to `note` (F5,\na documentation contradiction). No finding was added, dropped or reworded beyond that merge. The\nprose above the block is the agent's own text unedited.\n","title":"Adversary pass 1, unit u3 (unattached session containment)"},"args":{"command":"create-entity","data":{"body":"# Adversary pass 1 — unit u3, `story:unattached-claimed-session-is-contained`\n\nAgent type `adp:adversary`. Worktree `wt-b180f053180d`, branch `impl/unattached-session-contained`,\nHEAD `4cbc953` plus 3 uncommitted test cases, base `617bbed`. Report as returned.\n\n```\nunit: u3 story:unattached-claimed-session-is-contained\nverdict: red\ncases: executed 128→131, red 2\norigin: introduced 3, pre-existing 2, undecided 0\nwrote-outside-worktree: u3-target (assigned build dir, cargo artifacts only)\nneeds-coordinator: no\n```\n\n## Cases added\n\n`crates/substrate-daemon/tests/pipe_session.rs`, +236, one path, no implementation file touched.\nThe fixture edit is additive — `PipeFixtureDriver.refuse_signal: AtomicBool`, default `false`,\nguarded at the top of `signal`; all 25 pre-existing cases in that binary still pass.\n\n| line | case | result |\n|---|---|---|\n| 2365 | `adversary_a_client_that_drops_after_the_switch_leaves_no_running_process` — the acceptance sentence on the *production* server shape (`with_upgrades()`), plus the refusal's class/status/retriable against the published `session.not-attachable` row | green |\n| 2444 | `adversary_a_stranded_attach_with_an_unproven_kill_keeps_the_bounded_capacity` | **red** |\n| 2523 | `adversary_a_stranded_attach_whose_kill_is_unproven_is_not_contained_by_maintenance` | **red** |\n\nRed output, each run alone, before any suite run:\n\n```\nthread 'adversary_a_stranded_attach_with_an_unproven_kill_keeps_the_bounded_capacity' panicked at\ncrates/substrate-daemon/tests/pipe_session.rs:2488:13:\nattachment 31 of the bounded 32 is still answered 429 twenty seconds after one stranded attach\nwhose kill could not be proven: the failed-upgrade containment retained the tombstone, and\nPipeAttachmentPermit::drop then ran global.forget(), so one of this daemon's 32 attachment slots is\ngone until restart. On 617bbed the same disconnect always returned the slot.\ntest result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 25 filtered out; finished in 21.26s\n```\n\n```\nthread 'adversary_a_stranded_attach_whose_kill_is_unproven_is_not_contained_by_maintenance' panicked\nat crates/substrate-daemon/tests/pipe_session.rs:2564:30:\na claimed session whose upgrade never completed is still running after ten seconds and many\nmaintenance ticks, because the single containment attempt was refused and nothing retries it; the\nacceptance asks for no running process within one maintenance tick: Elapsed(())\ntest result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 26 filtered out; finished in 10.67s\n```\n\nSuite after the cases existed: `cargo test -p b10x-substrate-daemon --release --locked\n--no-fail-fast`, exit **101**. 131 executed. `pipe_session` 25 passed / 2 failed; every other lane\ngreen, including `runtime_vectors` 8/8 — the known flake did not fire.\n\n## Findings\n\n**F1 — one stranded attach whose kill is unproven permanently destroys a global attachment slot.**\n`sessions.rs:1300`. Measured at `tests/pipe_session.rs:2488`, exit 101: after **one** stranded\nattach with a refusing driver, only 31 of the daemon's 32 attachment slots are ever servable again;\nthe 32nd is `429 session.attachment-capacity` for the life of the process.\n`retain_attachment_tombstone()` → `Drop` takes the `else` arm at `:151` and runs `global.forget()`.\nReachability: `on_failed_upgrade` is production-reachable — all three listeners call\n`.with_upgrades()` (`runtime.rs:595`, `:918`, `:1083`), so a connection error before hyper fulfils\nthe upgrade resolves the future with `Canceled` and enters this closure. The\n`terminate_pipe_session == false` half was **constructed** with a fixture driver that refuses\n`signal`; the code's own doc comment and `MAINTENANCE_DRIVER_TIMEOUT` name that state as expected,\nbut no production driver was observed producing it. Origin `introduced`: at `617bbed` the file has\nno `on_failed_upgrade`, so a failed upgrade always dropped the permit with\n`remove_key_on_drop == true` and returned the slot. Fix not applied: do not retain the tombstone on\nthe failed-upgrade path — the durable claim is already `Consumed`, so the state gate at\n`sessions.rs:1149` refuses every further attach *before* `acquire` at `:1167` is called. The\nprocess-local tombstone cannot be consulted for that session again; it buys nothing and costs a slot.\n\n**F2 — the containment is one attempt with no retry and no backstop.** `sessions.rs:1299`. Measured\nat `tests/pipe_session.rs:2564`, exit 101: with a 500 ms transient driver refusal the stranded exec\nis still `\"running\"` after 10 s and ~200 explicit `App::sweep_expired()` ticks. The acceptance asks\nfor \"no running process within one maintenance tick\". The only backstop is `cleanup_expired`\n(`app/service.rs:645`), which acts on an **expired lease** — the very \"until its lease or timeout\nended it\" the story's Context calls the defect. Origin `pre-existing`: the residual reproduces at\n`617bbed`, where this path has no containment at all, so HEAD is strictly better. It is an unclosed\npart of the unit's own acceptance statement, not a regression.\n\n**F3 — the unit's shipped case builds a state no daemon listener can be in.**\n`tests/pipe_session.rs:989`. `spawn_without_upgrades` reaches `on_failed_upgrade` through hyper's\n`Pending::manual()` — the branch taken only when `with_upgrades()` was **not** called. All three\ndaemon listeners call it. The adversary's green case at `:2365` shows a real client drop after the\n`101` on a `with_upgrades()` listener is contained by the *other* hand-off (`on_upgrade` →\n`run_pipe_attachment` sees EOF → `terminate_pipe_session`), on a code path byte-identical at\n`617bbed`. Nothing production-side reaches `manual()`. The production trigger for the new closure is\n`Canceled` after a connection error in the flush window, and no case in the tree builds it. So the\nordinary client drop the story describes was already contained on base, and the change's residual\nvalue is unproven by its own suite. Origin `introduced` — it is about the case this unit added.\n\n**F4 — the containment discards its own outcome and says nothing.** `sessions.rs:1298`. Read, not\nrun. The closure body is a bare `tokio::spawn`; no `warn!`, no event, no metric on either branch,\nwhile `runtime.rs` and `service.rs` log every comparable maintenance failure. A containment that\nfails is invisible — invariant 3's silent degradation. It is also the only detached `tokio::spawn`\nin the daemon's non-test `app/` code, untracked by the connection `JoinSet` that `runtime.rs:621`\naborts at shutdown. Origin `introduced`; the sibling `on_upgrade` path at `:1319` is equally silent\nand pre-existing.\n\n**F5 — two doc comments about the same branch contradict each other, and F1 measures which is\nfalse.** `sessions.rs:134` says \"Capacity is still recovered\"; `:149` says the branch \"consumes one\nof the fixed global attachment slots until daemon restart\". F1's red case shows `:149` is the true\none. Origin `pre-existing` text; the unit edited exactly those lines (`&mut self` → `&self`) and\nleft the false sentence standing.\n\n## Attacked, could not break\n\n- **Memory ordering.** `Relaxed` store in `retain_attachment_tombstone` vs `get_mut()` in `Drop`:\n  `Arc`'s `Release` decrement plus `Acquire` fence orders the store before the drop on any thread.\n  Sufficient.\n- **\"Exactly one hand-off runs.\"** axum 0.8.9 `on_upgrade` (`ws.rs:359`) calls\n  `on_failed_upgrade.call()` only in the `Err` arm and `callback` only in the `Ok` arm of one\n  spawned task, and owns both closures — neither twice, never both, and the permit's `Drop` always\n  runs after whichever ran.\n- **\"No fallible code between the claim and `.on_upgrade`.\"** Holds. `store_io` →\n  `bounded_blocking` → `Semaphore::acquire_owned().await` then `block_in_place`; the claim and the\n  whole tail of the handler complete inside one poll, so the handler future cannot be dropped\n  between them.\n- **Builder ordering.** `WebSocketUpgrade::on_failed_upgrade` copies every `WebSocketConfig` field,\n  so placing it after the five buffer/frame builders loses none of them.\n- **Refusal register conformance.** The contained session's refusal matches\n  `contracts/substrate-wire/0.10.0/refusals.json`'s `session.not-attachable` row on class, status\n  and retriable (asserted green at `:2365`).\n- **The happy containment path.** When the kill succeeds, the slot and the `attached` key both come\n  back — indices 0..30 of the capacity case take them.\n\n```findings\n- file: crates/substrate-daemon/src/app/sessions.rs\n  line: 1300\n  category: boundary\n  severity: blocker\n  verdict: needs-revision\n  origin: introduced\n  message: >-\n    The failed-upgrade containment retains the attachment tombstone when it cannot prove the kill,\n    and Drop then runs global.forget(), permanently destroying one of the 32 global attachment slots\n    on a path that always returned the slot at 617bbed. One attach whose upgrade fails while the\n    driver refuses the signal -> terminate_pipe_session returns false -> retain_attachment_tombstone\n    -> Drop takes the else arm at sessions.rs:151 -> the daemon serves only 31 concurrent\n    attachments for the rest of the process's life and answers the 32nd 429\n    session.attachment-capacity forever. Measured at tests/pipe_session.rs:2488, exit 101.\n- file: crates/substrate-daemon/src/app/sessions.rs\n  line: 1299\n  category: acceptance\n  severity: blocker\n  verdict: needs-revision\n  origin: pre-existing\n  message: >-\n    The containment is a single unretried kill with no maintenance backstop, so a transient driver\n    refusal returns the daemon to the pre-fix behaviour the story exists to remove and the\n    acceptance sentence \"no running process within one maintenance tick\" is not met. Stranded attach\n    plus a 500 ms driver refusal, driver healthy again immediately -> the exec is still \"running\"\n    after 10 s and ~200 explicit App::sweep_expired() ticks; the only backstop is cleanup_expired at\n    app/service.rs:645, which fires on lease expiry, which is exactly what the story's Context calls\n    the defect. Measured at tests/pipe_session.rs:2564, exit 101. Reproduces at 617bbed, so HEAD is\n    strictly better and this is an unclosed part of the acceptance rather than a regression.\n- file: crates/substrate-daemon/tests/pipe_session.rs\n  line: 989\n  category: judgement\n  severity: warning\n  verdict: needs-revision\n  origin: introduced\n  message: >-\n    The case shipped as proof reaches on_failed_upgrade through hyper's Pending::manual(), a branch\n    taken only when with_upgrades() was not called, while all three daemon listeners call it, so the\n    new code's production trigger is exercised by nothing in the tree. A real client dropping\n    straight after the 101 on a with_upgrades() listener is contained by the other hand-off\n    (on_upgrade -> run_pipe_attachment sees EOF -> terminate_pipe_session), a path byte-identical at\n    617bbed; the adversary's green case at tests/pipe_session.rs:2365 shows it, so the story's\n    stated scenario was already contained on base and the residual window the change covers is\n    untested. Read plus one green case, not a red one.\n- file: crates/substrate-daemon/src/app/sessions.rs\n  line: 1298\n  category: judgement\n  severity: warning\n  verdict: needs-revision\n  origin: introduced\n  message: >-\n    The containment runs in a bare detached tokio::spawn that discards its own outcome and logs\n    nothing on either branch, so a containment that fails is invisible - invariant 3's silent\n    degradation - and it is the only untracked task in the daemon's non-test app/ code, outside the\n    connection JoinSet that runtime.rs:621 aborts at shutdown. terminate_pipe_session returns false\n    -> no warn!, no event, no metric; the operator sees a session whose process is still running and\n    no record that the daemon tried and failed to end it, while runtime.rs and app/service.rs log\n    every comparable maintenance failure. Read, not run.\n- file: crates/substrate-daemon/src/app/sessions.rs\n  line: 134\n  category: contract-drift\n  severity: note\n  verdict: needs-revision\n  origin: pre-existing\n  message: >-\n    retain_attachment_tombstone's doc comment says \"Capacity is still recovered\" while Drop's own\n    comment fifteen lines below says the branch \"consumes one of the fixed global attachment slots\n    until daemon restart\"; the unit edited these exact lines and left the false sentence standing. A\n    reader choosing where to call retain_attachment_tombstone is told capacity is safe;\n    tests/pipe_session.rs:2488 measures that global.forget() runs and the slot never returns, so\n    :149 is the true sentence and :134 is not.\n```\n\n## Coordinator note on this record\n\nThis agent was dispatched before the brief template's findings schema was corrected, so it returned\nthe block keyed `summary` / `failure_scenario` with severities `major` / `minor`. The block above is\nthe same five findings with those two fields merged into `message`, and severity mapped to the\nstore's vocabulary: `major` to `blocker` (F1, F2), `minor` to `warning` (F3, F4) and to `note` (F5,\na documentation contradiction). No finding was added, dropped or reworded beyond that merge. The\nprose above the block is the agent's own text unedited.\n","status":"active","title":"Adversary pass 1, unit u3 (unattached session containment)"},"entity_type":"aep.review-result/v1","locator":"ep://planning/store/review-result/adversary-u3-pass-1"},"payload":{"actor":"human:timo","at":1788493733018,"causation":"cmd-new-review-result-adversary-u3-pass-1","change":{"change":"created","status":"active"},"correlation":"protocol-artifact-new","event_id":"review-result:adversary-u3-pass-1@1#0~9b8599b9970f8951","executor":null,"recorded_at":"2026-09-04T03:48:53Z"}}
{"entity":"review-result","version":1,"id":"adversary-u3-pass-1","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"story:unattached-claimed-session-is-contained"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000718","target":"01MEM0000000000000235"},"payload":{"actor":"human:timo","at":1788493733034,"causation":"cmd-review-result-adversary-u3-pass-1-reviews-story-unattached-claimed-session-is-contained","change":{"change":"related","relation":"reviews","target":"story:unattached-claimed-session-is-contained"},"correlation":"protocol-artifact-relate","event_id":"review-result:adversary-u3-pass-1@1#0~eeb07cf1d133a204","executor":null,"recorded_at":"2026-09-04T03:48:53Z"}}
{"entity":"story","version":1,"id":"confined-processes-cannot-nest-user-namespaces","revision":7,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"review_outcome","outcome":"fixed","review":"review-result:adversary-u4-pass-1","source":"review-result:adversary-u4-pass-1","target":"01MEM0000000000000082"},"payload":{"actor":"human:timo","at":1788508405000,"causation":"cmd-evidence-story-confined-processes-cannot-nest-user-namespaces-review_outcome-1788508405076","change":{"change":"evidence","kind":"review_outcome","outcome":"fixed","review":"review-result:adversary-u4-pass-1","source":"review-result:adversary-u4-pass-1"},"correlation":"protocol-artifact-evidence","event_id":"story:confined-processes-cannot-nest-user-namespaces@7#0~95695f268cdf74a6","executor":null,"recorded_at":"2026-09-04T07:53:25Z"}}
{"entity":"story","version":1,"id":"unattached-claimed-session-is-contained","revision":5,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"review_outcome","outcome":"fixed","review":"review-result:adversary-u3-pass-1","source":"review-result:adversary-u3-pass-1","target":"01MEM0000000000000238"},"payload":{"actor":"human:timo","at":1788509550000,"causation":"cmd-evidence-story-unattached-claimed-session-is-contained-review_outcome-1788509550407","change":{"change":"evidence","kind":"review_outcome","outcome":"fixed","review":"review-result:adversary-u3-pass-1","source":"review-result:adversary-u3-pass-1"},"correlation":"protocol-artifact-evidence","event_id":"story:unattached-claimed-session-is-contained@5#0~1ed4624d14b25e1f","executor":null,"recorded_at":"2026-09-04T08:12:30Z"}}
{"entity":"review-result","version":1,"id":"adversary-u4-pass-2","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"active","changed":{"body":"# Adversary pass 2 — unit u4, `story:confined-processes-cannot-nest-user-namespaces`\n\nAgent type `adp:adversary`. Worktree `wt-5698af5e5dcf`, branch `impl/disable-userns`, HEAD `8ebfc74`,\nbase `617bbed`. Report as returned; the findings block is the agent's own, in the store's schema.\n\n```\nunit: u4 story:confined-processes-cannot-nest-user-namespaces\nverdict: red\ncases: executed 87→89, red 2\norigin: introduced 7, pre-existing 1, undecided 0\nwrote-outside-worktree: 9 paths\nneeds-coordinator: no\n```\n\n## Cases added\n\n`crates/substrate-host/tests/user_namespace_floor_documents.rs`, 166 lines, untracked. No\nimplementation file touched. Both cases assert documentation states, both red.\n\n```\nrunning 2 tests\ntest the_deployment_list_names_the_backend_options_exec_now_requires ... FAILED\ntest every_document_the_floor_is_named_in_states_the_no_nested_user_namespace_clause ... FAILED\n\n---- the_deployment_list_names_the_backend_options_exec_now_requires stdout ----\npanicked at crates/substrate-host/tests/user_namespace_floor_documents.rs:152:5:\nthe list says only \"provide the configured bubblewrap binary\", which is no longer sufficient:\n`probe_bubblewrap` puts `--disable-userns` and `--assert-userns-disabled` in its argv\n(probe.rs:336, :381) and a backend that refuses either leaves every `exec` fact absent.\n\n---- every_document_the_floor_is_named_in_states_the_no_nested_user_namespace_clause stdout ----\npanicked at crates/substrate-host/tests/user_namespace_floor_documents.rs:119:5:\nassertion `left == right` failed: design 15:147 names these documents as the floor, and the host now\nwithholds `exec.namespaces` outright when a backend cannot prove a non-nestable user namespace\n  left: [\"docs/design/04-security-and-isolation.md § 7, Minimum host guarantee\"]\n right: []\n\ntest result: FAILED. 0 passed; 2 failed; 0 ignored; 0 measured; 0 filtered out\n```\n\nPackage gate with the file present: exit 101, 87 → 89 executed, lib 76 passed, all seven of the\nunit's own targets green.\n\n## What the adversary attacked and could not break\n\nThis is the load-bearing half of pass 2, because it is what says the security fix itself holds.\n\n- **The acceptance statement is real, not a silent skip.** Straced under the delegated lane:\n  `a_confined_process_cannot_nest_a_user_namespace` performs two real\n  `execve(\"/usr/bin/bwrap\", [\"--unshare-user\", \"--disable-userns\", …])` followed by\n  `execve(\"/usr/bin/unshare\", [\"-U\", \"/bin/true\"])`, and `host-pty-nested-userns` exists in the\n  delegation root afterwards.\n- **Nesting routes pass 1 did not try**, all inside the exact exec posture: nested\n  `bwrap --unshare-user`, nested `bwrap` with implicit userns, `unshare -r`,\n  `unshare --map-root-user`, `unshare -Um` — **all ENOSPC**. `unshare -m` alone EPERM. Writing\n  `/proc/sys/user/max_user_namespaces` from inside: EACCES. Child capability sets all zero.\n- **`--assert-userns-disabled` is a real observation, not a no-op** — fails with\n  `creation of new user namespaces was not disabled as requested` when `--disable-userns` is absent,\n  passes when present.\n- **All eight bubblewrap sites** (by `Command::new` enumeration) splice the constant; the doc's\n  count of eight is correct.\n- **`--unshare-all` / `--unshare-user-try` / `--userns` and raw-string spellings** are caught by the\n  scan; `[\"--unshare-user-try\", \"--disable-userns\"]` would go red on `strays`.\n- **design 15's row 1 citations** `process.rs:1905` and `probe.rs:381` are accurate at HEAD, its\n  column 4 matches `sandbox_unavailable()` at `process.rs:2912-2917`, and `AGENTS.md:91-99` is the\n  correct range.\n\n## Findings, and the named fixes the adversary did not apply\n\n1. add the clause to design 04 § 7's bullet list; 2. add the option requirement to README item 3;\n3. add `concat!(\"\\\"--\",\"userns2\\\"\")` to `OTHER_SPELLINGS`, bump `[&str; 3]` → `[&str; 4]`, correct\n\"the three other spellings\" at `:4380`; 4. either observe `egress.rs`'s test sandbox or say plainly\nit is unobserved; 5. make the `--assert-userns-disabled` pin unconditional; 6. add\n`assert!(expected > 0)` or delete the sentence; 7. correct \"scratch\" at `:890`.\n\n`crates/substrate-host/src/seccomp.rs` was not attacked, per the brief. No finding there.\n\n```findings\n- file: docs/design/04-security-and-isolation.md\n  line: 83\n  category: contract-drift\n  severity: warning\n  verdict: needs-revision\n  origin: introduced\n  message: >-\n    design 15:147 declares design 04 section 7 to be part of the floor, but section 7's \"requires all\n    of the following before it advertises exec\" list gained no no-nested-user-namespace clause while\n    AGENTS.md:91-99 and design 15's table did; the code makes it a real precondition (probe.rs:381,\n    :49), so the two floor documents now state different floors. Measured red at\n    crates/substrate-host/tests/user_namespace_floor_documents.rs:119.\n- file: README.md\n  line: 271\n  category: contract-drift\n  severity: warning\n  verdict: needs-revision\n  origin: introduced\n  message: >-\n    README section \"Serving exec\" item 3 still says only \"provide the configured bubblewrap binary\n    and /usr/bin/socat\", but exec admission now requires a backend accepting --disable-userns and\n    --assert-userns-disabled, so an operator on an older bubblewrap loses exec entirely with no\n    documented cause. Measured red at\n    crates/substrate-host/tests/user_namespace_floor_documents.rs:152.\n- file: crates/substrate-host/src/process.rs\n  line: 4396\n  category: mutant\n  severity: warning\n  verdict: needs-revision\n  origin: introduced\n  message: >-\n    OTHER_SPELLINGS enumerates three other user-namespace spellings and omits --userns2 FD, which\n    bwrap 0.11.2 lists and design 10:98-99 already records, and which bwrap accepts alongside\n    --unshare-user --disable-userns (measured: \"bwrap: Setting userns2 failed: Invalid argument\",\n    i.e. parsed and attempted, not rejected); the needle \"--userns\" does not match \"--userns2\", and\n    --args FD defeats the scan entirely. Nothing in the crate reaches either today.\n- file: crates/substrate-host/src/process.rs\n  line: 4349\n  category: contract-drift\n  severity: note\n  verdict: needs-revision\n  origin: introduced\n  message: >-\n    The claim that \"the source scan below is what covers\" the eighth argv list is false. The scan\n    asserts exactly one line carrying the literal \"--unshare-user\" and egress.rs:1467 carries the\n    constant instead, so deleting that splice changes neither the site count nor the stray list. The\n    scan can detect an added spelling, never a removed splice, at any site.\n- file: crates/substrate-host/src/probe.rs\n  line: 918\n  category: mutant\n  severity: warning\n  verdict: needs-revision\n  origin: introduced\n  message: >-\n    Both guards over probe_bubblewrap's argv are conditional on /usr/bin/socat existing (the stub\n    case returns at :918, the recorder pins 2 + usize::from(socat) at :986) and the source scan never\n    looks for --assert-userns-disabled, so on a socat-less host deleting\n    .arg(\"--assert-userns-disabled\") at :381 leaves the whole package gate green. Security impact nil\n    there because exec is unavailable anyway (probe.rs:320); what evaporates is the regression pin.\n- file: crates/substrate-host/src/process.rs\n  line: 104\n  category: judgement\n  severity: note\n  verdict: needs-revision\n  origin: introduced\n  message: >-\n    assert_recorded_posture's doc promises an \"and unless one was recorded\" empty check that the body\n    does not contain — it is assert_eq!(len, expected), so assert_recorded_posture(\"x\", &[], 0)\n    passes on an empty log. Both current callers pass at least 2, so nothing reaches it today.\n- file: crates/substrate-host/src/probe.rs\n  line: 889\n  category: contract-drift\n  severity: note\n  verdict: needs-revision\n  origin: introduced\n  message: >-\n    The case doc lists \"the capsule and scratch facts\" among the facts gated on exec, but\n    exec_scratch_quota is gated only on quota (probe.rs:135, :87), so with project_quota_ids\n    configured a third exec.* fact survives a withheld floor where the source comment at :370-376\n    says exactly two do. Not constructible on this host, so the reachable consequence is unmeasured.\n- file: docs/design/10-destination-bound-egress.md\n  line: 34\n  category: contract-drift\n  severity: note\n  verdict: needs-revision\n  origin: pre-existing\n  message: >-\n    The evidence row cites process.rs:901-905 for the sandbox argv, which lives at :1902-1928 and was\n    already elsewhere at 617bbed (the row is byte-identical at base), and it still states the posture\n    as \"--unshare-net with --unshare-user/ipc/pid/uts\", which is no longer the whole argv.\n```\n","title":"Adversary pass 2, unit u4 (disable-userns)"},"args":{"command":"create-entity","data":{"body":"# Adversary pass 2 — unit u4, `story:confined-processes-cannot-nest-user-namespaces`\n\nAgent type `adp:adversary`. Worktree `wt-5698af5e5dcf`, branch `impl/disable-userns`, HEAD `8ebfc74`,\nbase `617bbed`. Report as returned; the findings block is the agent's own, in the store's schema.\n\n```\nunit: u4 story:confined-processes-cannot-nest-user-namespaces\nverdict: red\ncases: executed 87→89, red 2\norigin: introduced 7, pre-existing 1, undecided 0\nwrote-outside-worktree: 9 paths\nneeds-coordinator: no\n```\n\n## Cases added\n\n`crates/substrate-host/tests/user_namespace_floor_documents.rs`, 166 lines, untracked. No\nimplementation file touched. Both cases assert documentation states, both red.\n\n```\nrunning 2 tests\ntest the_deployment_list_names_the_backend_options_exec_now_requires ... FAILED\ntest every_document_the_floor_is_named_in_states_the_no_nested_user_namespace_clause ... FAILED\n\n---- the_deployment_list_names_the_backend_options_exec_now_requires stdout ----\npanicked at crates/substrate-host/tests/user_namespace_floor_documents.rs:152:5:\nthe list says only \"provide the configured bubblewrap binary\", which is no longer sufficient:\n`probe_bubblewrap` puts `--disable-userns` and `--assert-userns-disabled` in its argv\n(probe.rs:336, :381) and a backend that refuses either leaves every `exec` fact absent.\n\n---- every_document_the_floor_is_named_in_states_the_no_nested_user_namespace_clause stdout ----\npanicked at crates/substrate-host/tests/user_namespace_floor_documents.rs:119:5:\nassertion `left == right` failed: design 15:147 names these documents as the floor, and the host now\nwithholds `exec.namespaces` outright when a backend cannot prove a non-nestable user namespace\n  left: [\"docs/design/04-security-and-isolation.md § 7, Minimum host guarantee\"]\n right: []\n\ntest result: FAILED. 0 passed; 2 failed; 0 ignored; 0 measured; 0 filtered out\n```\n\nPackage gate with the file present: exit 101, 87 → 89 executed, lib 76 passed, all seven of the\nunit's own targets green.\n\n## What the adversary attacked and could not break\n\nThis is the load-bearing half of pass 2, because it is what says the security fix itself holds.\n\n- **The acceptance statement is real, not a silent skip.** Straced under the delegated lane:\n  `a_confined_process_cannot_nest_a_user_namespace` performs two real\n  `execve(\"/usr/bin/bwrap\", [\"--unshare-user\", \"--disable-userns\", …])` followed by\n  `execve(\"/usr/bin/unshare\", [\"-U\", \"/bin/true\"])`, and `host-pty-nested-userns` exists in the\n  delegation root afterwards.\n- **Nesting routes pass 1 did not try**, all inside the exact exec posture: nested\n  `bwrap --unshare-user`, nested `bwrap` with implicit userns, `unshare -r`,\n  `unshare --map-root-user`, `unshare -Um` — **all ENOSPC**. `unshare -m` alone EPERM. Writing\n  `/proc/sys/user/max_user_namespaces` from inside: EACCES. Child capability sets all zero.\n- **`--assert-userns-disabled` is a real observation, not a no-op** — fails with\n  `creation of new user namespaces was not disabled as requested` when `--disable-userns` is absent,\n  passes when present.\n- **All eight bubblewrap sites** (by `Command::new` enumeration) splice the constant; the doc's\n  count of eight is correct.\n- **`--unshare-all` / `--unshare-user-try` / `--userns` and raw-string spellings** are caught by the\n  scan; `[\"--unshare-user-try\", \"--disable-userns\"]` would go red on `strays`.\n- **design 15's row 1 citations** `process.rs:1905` and `probe.rs:381` are accurate at HEAD, its\n  column 4 matches `sandbox_unavailable()` at `process.rs:2912-2917`, and `AGENTS.md:91-99` is the\n  correct range.\n\n## Findings, and the named fixes the adversary did not apply\n\n1. add the clause to design 04 § 7's bullet list; 2. add the option requirement to README item 3;\n3. add `concat!(\"\\\"--\",\"userns2\\\"\")` to `OTHER_SPELLINGS`, bump `[&str; 3]` → `[&str; 4]`, correct\n\"the three other spellings\" at `:4380`; 4. either observe `egress.rs`'s test sandbox or say plainly\nit is unobserved; 5. make the `--assert-userns-disabled` pin unconditional; 6. add\n`assert!(expected > 0)` or delete the sentence; 7. correct \"scratch\" at `:890`.\n\n`crates/substrate-host/src/seccomp.rs` was not attacked, per the brief. No finding there.\n\n```findings\n- file: docs/design/04-security-and-isolation.md\n  line: 83\n  category: contract-drift\n  severity: warning\n  verdict: needs-revision\n  origin: introduced\n  message: >-\n    design 15:147 declares design 04 section 7 to be part of the floor, but section 7's \"requires all\n    of the following before it advertises exec\" list gained no no-nested-user-namespace clause while\n    AGENTS.md:91-99 and design 15's table did; the code makes it a real precondition (probe.rs:381,\n    :49), so the two floor documents now state different floors. Measured red at\n    crates/substrate-host/tests/user_namespace_floor_documents.rs:119.\n- file: README.md\n  line: 271\n  category: contract-drift\n  severity: warning\n  verdict: needs-revision\n  origin: introduced\n  message: >-\n    README section \"Serving exec\" item 3 still says only \"provide the configured bubblewrap binary\n    and /usr/bin/socat\", but exec admission now requires a backend accepting --disable-userns and\n    --assert-userns-disabled, so an operator on an older bubblewrap loses exec entirely with no\n    documented cause. Measured red at\n    crates/substrate-host/tests/user_namespace_floor_documents.rs:152.\n- file: crates/substrate-host/src/process.rs\n  line: 4396\n  category: mutant\n  severity: warning\n  verdict: needs-revision\n  origin: introduced\n  message: >-\n    OTHER_SPELLINGS enumerates three other user-namespace spellings and omits --userns2 FD, which\n    bwrap 0.11.2 lists and design 10:98-99 already records, and which bwrap accepts alongside\n    --unshare-user --disable-userns (measured: \"bwrap: Setting userns2 failed: Invalid argument\",\n    i.e. parsed and attempted, not rejected); the needle \"--userns\" does not match \"--userns2\", and\n    --args FD defeats the scan entirely. Nothing in the crate reaches either today.\n- file: crates/substrate-host/src/process.rs\n  line: 4349\n  category: contract-drift\n  severity: note\n  verdict: needs-revision\n  origin: introduced\n  message: >-\n    The claim that \"the source scan below is what covers\" the eighth argv list is false. The scan\n    asserts exactly one line carrying the literal \"--unshare-user\" and egress.rs:1467 carries the\n    constant instead, so deleting that splice changes neither the site count nor the stray list. The\n    scan can detect an added spelling, never a removed splice, at any site.\n- file: crates/substrate-host/src/probe.rs\n  line: 918\n  category: mutant\n  severity: warning\n  verdict: needs-revision\n  origin: introduced\n  message: >-\n    Both guards over probe_bubblewrap's argv are conditional on /usr/bin/socat existing (the stub\n    case returns at :918, the recorder pins 2 + usize::from(socat) at :986) and the source scan never\n    looks for --assert-userns-disabled, so on a socat-less host deleting\n    .arg(\"--assert-userns-disabled\") at :381 leaves the whole package gate green. Security impact nil\n    there because exec is unavailable anyway (probe.rs:320); what evaporates is the regression pin.\n- file: crates/substrate-host/src/process.rs\n  line: 104\n  category: judgement\n  severity: note\n  verdict: needs-revision\n  origin: introduced\n  message: >-\n    assert_recorded_posture's doc promises an \"and unless one was recorded\" empty check that the body\n    does not contain — it is assert_eq!(len, expected), so assert_recorded_posture(\"x\", &[], 0)\n    passes on an empty log. Both current callers pass at least 2, so nothing reaches it today.\n- file: crates/substrate-host/src/probe.rs\n  line: 889\n  category: contract-drift\n  severity: note\n  verdict: needs-revision\n  origin: introduced\n  message: >-\n    The case doc lists \"the capsule and scratch facts\" among the facts gated on exec, but\n    exec_scratch_quota is gated only on quota (probe.rs:135, :87), so with project_quota_ids\n    configured a third exec.* fact survives a withheld floor where the source comment at :370-376\n    says exactly two do. Not constructible on this host, so the reachable consequence is unmeasured.\n- file: docs/design/10-destination-bound-egress.md\n  line: 34\n  category: contract-drift\n  severity: note\n  verdict: needs-revision\n  origin: pre-existing\n  message: >-\n    The evidence row cites process.rs:901-905 for the sandbox argv, which lives at :1902-1928 and was\n    already elsewhere at 617bbed (the row is byte-identical at base), and it still states the posture\n    as \"--unshare-net with --unshare-user/ipc/pid/uts\", which is no longer the whole argv.\n```\n","status":"active","title":"Adversary pass 2, unit u4 (disable-userns)"},"entity_type":"aep.review-result/v1","locator":"ep://planning/store/review-result/adversary-u4-pass-2"},"payload":{"actor":"human:timo","at":1788510140136,"causation":"cmd-new-review-result-adversary-u4-pass-2","change":{"change":"created","status":"active"},"correlation":"protocol-artifact-new","event_id":"review-result:adversary-u4-pass-2@1#0~99b9e4773d4b035d","executor":null,"recorded_at":"2026-09-04T08:22:20Z"}}
{"entity":"review-result","version":1,"id":"adversary-u4-pass-2","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"story:confined-processes-cannot-nest-user-namespaces"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000723","target":"01MEM0000000000000082"},"payload":{"actor":"human:timo","at":1788510140153,"causation":"cmd-review-result-adversary-u4-pass-2-reviews-story-confined-processes-cannot-nest-user-namespaces","change":{"change":"related","relation":"reviews","target":"story:confined-processes-cannot-nest-user-namespaces"},"correlation":"protocol-artifact-relate","event_id":"review-result:adversary-u4-pass-2@1#0~5bd6d7c52116d282","executor":null,"recorded_at":"2026-09-04T08:22:20Z"}}
{"entity":"story","version":1,"id":"confined-processes-cannot-nest-user-namespaces","revision":7,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"review_outcome","outcome":"fixed","review":"review-result:adversary-u4-pass-2","source":"review-result:adversary-u4-pass-2","target":"01MEM0000000000000085"},"payload":{"actor":"human:timo","at":1788510694000,"causation":"cmd-evidence-story-confined-processes-cannot-nest-user-namespaces-review_outcome-1788510694653","change":{"change":"evidence","kind":"review_outcome","outcome":"fixed","review":"review-result:adversary-u4-pass-2","source":"review-result:adversary-u4-pass-2"},"correlation":"protocol-artifact-evidence","event_id":"story:confined-processes-cannot-nest-user-namespaces@7#0~3de32d8901f7c5ad","executor":null,"recorded_at":"2026-09-04T08:31:34Z"}}
{"entity":"review-result","version":1,"id":"adversary-u1-pass-1","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"active","changed":{"body":"# Adversary pass 1 — unit u1, `story:metrics-streams-are-bounded`\n\nAgent type `adp:adversary`. Worktree `wt-d5a923d7143d`, branch `impl/metrics-streams-bounded`,\nHEAD `377b115`, base `617bbed`. Report as returned.\n\n```\nunit: u1 — story:metrics-streams-are-bounded\nverdict: red\ncases: executed 130→135, red 2\norigin: introduced 4 / pre-existing 1 / undecided 0\nwrote-outside-worktree: 5 paths\nneeds-coordinator: no\n```\n\n## Cases added\n\n`crates/substrate-daemon/tests/metrics_stream_adversary.rs`, 627 lines, untracked. No implementation\nfile touched.\n\n| line | case | result |\n|---|---|---|\n| 453 | `a_metrics_stream_samples_at_the_interval_its_contract_advertises` | **red** |\n| 602 | `the_metrics_sampling_cadence_does_not_depend_on_client_traffic` | **red** |\n| 528 | `an_oversized_client_frame_ends_the_metrics_stream_and_returns_its_permit` | green |\n| 547 | `the_metrics_stream_cap_is_not_shared_with_the_event_stream_cap` | green |\n| 572 | `a_handshake_abandoned_before_its_first_sample_returns_its_permit` | green |\n\n```\nthread 'a_metrics_stream_samples_at_the_interval_its_contract_advertises' panicked at\ncrates/substrate-daemon/tests/metrics_stream_adversary.rs:411:5:\nthe metrics stream advertises a 1s sample interval and delivered its samples\n[1.998987499s, 2.000531688s] apart (ceiling 1.5s)\ntest result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 0 filtered out; finished in 4.16s\n\nthread 'the_metrics_sampling_cadence_does_not_depend_on_client_traffic' panicked at\ncrates/substrate-daemon/tests/metrics_stream_adversary.rs:621:5:\na silent client is sampled every 2.000488208s and a client that sends one ping per sample every\n999.768283ms (ratio 2.00); the cadence must not depend on what the client sends\n```\n\nSuite after the cases existed: exit 101, 135 executed, 133 passed, 2 failed. Every other lane green,\n`runtime_vectors` 8/8.\n\n## Findings\n\n**F1 — the metrics stream samples at half its advertised rate.** `app/metrics.rs:176` with `:207`.\n`run_stream` awaits `interval.tick()` **twice per frame**: once at the head of the loop body\n(`:176`) and once as an arm of the `tokio::select!` at its foot (`:207`). Both consume a tick, so\nevery frame costs two periods. Measured at `tests/metrics_stream_adversary.rs:468` (gaps\n`[1.9998 s, 2.0000 s]` against a 1.5 s ceiling) and `:621` (silent 1.9997 s vs one-ping-per-sample\n999.75 ms, **ratio exactly 2.00**), `EXIT_STATUS=101`.\n\nReachability: every metrics stream. `router_for` (`app/routes.rs:54`) → `metrics_stream` →\n`run_stream`; consumer `Client::metrics_stream` (`crates/b10x-substrate-sdk/src/transport.rs:150`).\n`contracts/substrate-wire/0.15.0/operations.json` gates the route on the machine fact\n`{\"latest_wins\":true,\"replay\":false,\"sample_interval_ms\":1000}`;\n`website/docs/guides/storage-and-metrics.md:135` says \"approximately once per second\";\n`website/docs/guides/run-a-command.md:156` tells a UI to pace on \"the advertised interval\".\n\nOrigin **pre-existing**: `git show 617bbed:crates/substrate-daemon/src/app/metrics.rs` has the same\ntwo `interval.tick()` awaits; the unit's only change inside `run_stream` is\n`Duration::from_secs(5)` → `policy.send_timeout`.\n\nThe second case is what makes the diagnosis unarguable rather than inferred: a control frame cancels\nthe `select!`'s tick arm, so a noisy client gets exactly the advertised 1 s and a silent one gets 2 s\nfrom the same server over the same exec.\n\n**F2 — `MetricsStreamPolicy` restates five `EventStreamPolicy` values and two of them are pinned.**\n`app/metrics.rs:32`. `max_input_bytes`, `max_output_bytes`, `write_buffer_bytes`, `send_timeout` and\n`lifetime` are duplicated and equal today; the only pin is\n`EventStreamPolicy::production().streams_per_subject` at `metrics.rs:760` and `:788` — 2 fields of 7.\nNo case is possible: the four have no visibility modifier, so they are private to `app::events` and\nno module outside it can read them. Nothing reaches it today; the seam opens on the next edit to\n`events.rs`.\n\n**F3 — the 1 h lifetime is wired and unobservable.** `app/metrics.rs:152`.\n`tokio::time::timeout(policy.lifetime, session)` is reached by no test, and cannot be:\n`lifetime` is private to `app::metrics` and `MetricsStreamPolicy` has no test constructor. A\n`start_paused` probe is not honest here — with the 5 s send deadline live, an auto-advancing clock\nfires the deadline instead of the lifetime. Any stream open past 1 h reaches it; the exec timeout is\n24 h, so this is the ordinary long-run case, and it ends by dropping the future — no close frame, no\ncode. Fix named: call the already-`pub(super)` `enforce_event_stream_lifetime`\n(`app/events.rs:629`) and add the metrics twin of\n`app/tests.rs:event_stream_lifetime_cancels_session_and_recovers_permits`.\n\n**F4 — the class check is weaker than its docstring.** `app/metrics.rs:824`. One non-recursive\n`read_dir` over `src/app/`, so `src/app.rs`, `src/hosted.rs`, `src/tls.rs` and `src/runtime.rs` are\noutside it; it matches the literal `.on_upgrade(`, so `hyper::upgrade::on` is invisible; and it\naccepts `.max_frame_size(` anywhere in the raw 800 bytes before the call, which a comment satisfies.\nNot constructed — the demonstration is a fourth `on_upgrade` under `src/app/`, an implementation\nfile, outside the adversary's charter.\n\n**F5 — a new client-visible refusal on a documented route, undocumented.**\n`website/docs/guides/storage-and-metrics.md:133`. The acceptance says \"one more than a *published*\nper-subject cap\". `metrics.stream-capacity` and the number 4 appear only in daemon source\n(`metrics.rs:25`, `:45`). Repo-wide grep: `metrics.stream-capacity` at `metrics.rs:25` and `:778`\nonly; `event.stream-capacity` at `events.rs:256` only. `contracts/substrate-wire/0.15.0/refusals.json`\nis `b10x.substrate-session-refusals.v1`, 36 rows, every prefix `session` — **so the implementor is\nright that no bundle byte was owed**. The SDK surfaces it as `SdkError::Refusal { code }`, so a\ncaller can act on it once it knows it exists.\n\n## Attacked, could not break\n\n- The acceptance itself, re-driven from an independent fixture: 4 upgrade, the 5th is `429` /\n  `metrics.stream-capacity` / `exhausted` / `retriable` / address `stream`.\n- Permit return after an abnormal end — a 1 025-byte client frame kills the stream and all 4 permits\n  come back. The frame ceiling fires; the structural claim is now behavioural too.\n- **The u3 class**, carried across from another unit in this wave: 12 handshakes written then dropped\n  unread, full capacity returns.\n- **Metrics and event caps are independent — this is the mutant the unit's own three cases miss.**\n  `metrics_stream` wired to `app.event_stream_limits` keeps all three of them green.\n- Outgoing frame truncation by the new 1 KiB ceiling: `max_frame_size`/`max_message_size` are\n  read-side only in tungstenite 0.29, so a server frame cannot be cut by them.\n- Ping-flood amplification, measured not argued: the loop consumes one control frame per sample\n  period, so a flooding client gets 1 pong/s, not more.\n- `EventStreamLimits` accounting cannot overcount across the remove-then-reacquire race.\n- The `NoDriver` fake's `unreachable!()` is not vacuous: a driver call panics the request task and\n  the unit's own cases go red.\n\n```findings\n- file: crates/substrate-daemon/src/app/metrics.rs\n  line: 176\n  category: contract-drift\n  severity: warning\n  verdict: needs-revision\n  origin: pre-existing\n  message: >-\n    run_stream awaits interval.tick() twice per frame (:176 and the select! arm at :207), so the\n    metrics stream delivers one sample every 2.000s against the 1000ms its released capability\n    predicate and both guides advertise; measured at tests/metrics_stream_adversary.rs:468 (gaps\n    1.9998s/2.0000s) and :621 (silent 1.9997s vs one-ping-per-sample 999.75ms, ratio 2.00), and the\n    same two ticks are present at base 617bbed.\n- file: crates/substrate-daemon/src/app/metrics.rs\n  line: 32\n  category: contract-drift\n  severity: warning\n  verdict: needs-revision\n  origin: introduced\n  message: >-\n    MetricsStreamPolicy duplicates five EventStreamPolicy values and only streams_per_subject and\n    global_streams are pinned by a case, and the other four are private to app::events so no test\n    outside that module can compare them; the values agree today, so the drift is latent and cannot\n    be made red without widening their visibility or reusing EventStreamPolicy.\n- file: crates/substrate-daemon/src/app/metrics.rs\n  line: 152\n  category: acceptance\n  severity: warning\n  verdict: needs-revision\n  origin: introduced\n  message: >-\n    the 1h lifetime added at :152 is reached by no case and cannot be reached by one, because\n    lifetime is private to app::metrics and MetricsStreamPolicy has no test constructor; routing it\n    through the pub(super) enforce_event_stream_lifetime at app/events.rs:629 would make it\n    observable the way the event stream's already is.\n- file: crates/substrate-daemon/src/app/metrics.rs\n  line: 824\n  category: mutant\n  severity: note\n  verdict: needs-revision\n  origin: introduced\n  message: >-\n    the class check scans src/app/ with one non-recursive read_dir and matches the substring\n    \".max_frame_size(\" in the raw 800 bytes before \".on_upgrade(\", so an upgrade in src/app.rs,\n    src/hosted.rs, src/tls.rs or src/runtime.rs, one written as hyper::upgrade::on, or one whose\n    bound appears only in a comment all pass it; not constructed, because the demonstration is a\n    fourth upgrade under src/app/ and that is an implementation file.\n- file: website/docs/guides/storage-and-metrics.md\n  line: 133\n  category: contract-drift\n  severity: note\n  verdict: needs-revision\n  origin: introduced\n  message: >-\n    the unit adds a client-visible 429 metrics.stream-capacity to a route this guide documents, and\n    neither the cap of 4 nor the refusal code appears anywhere outside daemon source; 0.15.0\n    refusals.json is confirmed session-only (36 rows, all session.*) so no bundle byte was owed, but\n    the acceptance's word \"published\" is satisfied by nothing a client can read.\n```\n","title":"Adversary pass 1, unit u1 (metrics stream caps)"},"args":{"command":"create-entity","data":{"body":"# Adversary pass 1 — unit u1, `story:metrics-streams-are-bounded`\n\nAgent type `adp:adversary`. Worktree `wt-d5a923d7143d`, branch `impl/metrics-streams-bounded`,\nHEAD `377b115`, base `617bbed`. Report as returned.\n\n```\nunit: u1 — story:metrics-streams-are-bounded\nverdict: red\ncases: executed 130→135, red 2\norigin: introduced 4 / pre-existing 1 / undecided 0\nwrote-outside-worktree: 5 paths\nneeds-coordinator: no\n```\n\n## Cases added\n\n`crates/substrate-daemon/tests/metrics_stream_adversary.rs`, 627 lines, untracked. No implementation\nfile touched.\n\n| line | case | result |\n|---|---|---|\n| 453 | `a_metrics_stream_samples_at_the_interval_its_contract_advertises` | **red** |\n| 602 | `the_metrics_sampling_cadence_does_not_depend_on_client_traffic` | **red** |\n| 528 | `an_oversized_client_frame_ends_the_metrics_stream_and_returns_its_permit` | green |\n| 547 | `the_metrics_stream_cap_is_not_shared_with_the_event_stream_cap` | green |\n| 572 | `a_handshake_abandoned_before_its_first_sample_returns_its_permit` | green |\n\n```\nthread 'a_metrics_stream_samples_at_the_interval_its_contract_advertises' panicked at\ncrates/substrate-daemon/tests/metrics_stream_adversary.rs:411:5:\nthe metrics stream advertises a 1s sample interval and delivered its samples\n[1.998987499s, 2.000531688s] apart (ceiling 1.5s)\ntest result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 0 filtered out; finished in 4.16s\n\nthread 'the_metrics_sampling_cadence_does_not_depend_on_client_traffic' panicked at\ncrates/substrate-daemon/tests/metrics_stream_adversary.rs:621:5:\na silent client is sampled every 2.000488208s and a client that sends one ping per sample every\n999.768283ms (ratio 2.00); the cadence must not depend on what the client sends\n```\n\nSuite after the cases existed: exit 101, 135 executed, 133 passed, 2 failed. Every other lane green,\n`runtime_vectors` 8/8.\n\n## Findings\n\n**F1 — the metrics stream samples at half its advertised rate.** `app/metrics.rs:176` with `:207`.\n`run_stream` awaits `interval.tick()` **twice per frame**: once at the head of the loop body\n(`:176`) and once as an arm of the `tokio::select!` at its foot (`:207`). Both consume a tick, so\nevery frame costs two periods. Measured at `tests/metrics_stream_adversary.rs:468` (gaps\n`[1.9998 s, 2.0000 s]` against a 1.5 s ceiling) and `:621` (silent 1.9997 s vs one-ping-per-sample\n999.75 ms, **ratio exactly 2.00**), `EXIT_STATUS=101`.\n\nReachability: every metrics stream. `router_for` (`app/routes.rs:54`) → `metrics_stream` →\n`run_stream`; consumer `Client::metrics_stream` (`crates/b10x-substrate-sdk/src/transport.rs:150`).\n`contracts/substrate-wire/0.15.0/operations.json` gates the route on the machine fact\n`{\"latest_wins\":true,\"replay\":false,\"sample_interval_ms\":1000}`;\n`website/docs/guides/storage-and-metrics.md:135` says \"approximately once per second\";\n`website/docs/guides/run-a-command.md:156` tells a UI to pace on \"the advertised interval\".\n\nOrigin **pre-existing**: `git show 617bbed:crates/substrate-daemon/src/app/metrics.rs` has the same\ntwo `interval.tick()` awaits; the unit's only change inside `run_stream` is\n`Duration::from_secs(5)` → `policy.send_timeout`.\n\nThe second case is what makes the diagnosis unarguable rather than inferred: a control frame cancels\nthe `select!`'s tick arm, so a noisy client gets exactly the advertised 1 s and a silent one gets 2 s\nfrom the same server over the same exec.\n\n**F2 — `MetricsStreamPolicy` restates five `EventStreamPolicy` values and two of them are pinned.**\n`app/metrics.rs:32`. `max_input_bytes`, `max_output_bytes`, `write_buffer_bytes`, `send_timeout` and\n`lifetime` are duplicated and equal today; the only pin is\n`EventStreamPolicy::production().streams_per_subject` at `metrics.rs:760` and `:788` — 2 fields of 7.\nNo case is possible: the four have no visibility modifier, so they are private to `app::events` and\nno module outside it can read them. Nothing reaches it today; the seam opens on the next edit to\n`events.rs`.\n\n**F3 — the 1 h lifetime is wired and unobservable.** `app/metrics.rs:152`.\n`tokio::time::timeout(policy.lifetime, session)` is reached by no test, and cannot be:\n`lifetime` is private to `app::metrics` and `MetricsStreamPolicy` has no test constructor. A\n`start_paused` probe is not honest here — with the 5 s send deadline live, an auto-advancing clock\nfires the deadline instead of the lifetime. Any stream open past 1 h reaches it; the exec timeout is\n24 h, so this is the ordinary long-run case, and it ends by dropping the future — no close frame, no\ncode. Fix named: call the already-`pub(super)` `enforce_event_stream_lifetime`\n(`app/events.rs:629`) and add the metrics twin of\n`app/tests.rs:event_stream_lifetime_cancels_session_and_recovers_permits`.\n\n**F4 — the class check is weaker than its docstring.** `app/metrics.rs:824`. One non-recursive\n`read_dir` over `src/app/`, so `src/app.rs`, `src/hosted.rs`, `src/tls.rs` and `src/runtime.rs` are\noutside it; it matches the literal `.on_upgrade(`, so `hyper::upgrade::on` is invisible; and it\naccepts `.max_frame_size(` anywhere in the raw 800 bytes before the call, which a comment satisfies.\nNot constructed — the demonstration is a fourth `on_upgrade` under `src/app/`, an implementation\nfile, outside the adversary's charter.\n\n**F5 — a new client-visible refusal on a documented route, undocumented.**\n`website/docs/guides/storage-and-metrics.md:133`. The acceptance says \"one more than a *published*\nper-subject cap\". `metrics.stream-capacity` and the number 4 appear only in daemon source\n(`metrics.rs:25`, `:45`). Repo-wide grep: `metrics.stream-capacity` at `metrics.rs:25` and `:778`\nonly; `event.stream-capacity` at `events.rs:256` only. `contracts/substrate-wire/0.15.0/refusals.json`\nis `b10x.substrate-session-refusals.v1`, 36 rows, every prefix `session` — **so the implementor is\nright that no bundle byte was owed**. The SDK surfaces it as `SdkError::Refusal { code }`, so a\ncaller can act on it once it knows it exists.\n\n## Attacked, could not break\n\n- The acceptance itself, re-driven from an independent fixture: 4 upgrade, the 5th is `429` /\n  `metrics.stream-capacity` / `exhausted` / `retriable` / address `stream`.\n- Permit return after an abnormal end — a 1 025-byte client frame kills the stream and all 4 permits\n  come back. The frame ceiling fires; the structural claim is now behavioural too.\n- **The u3 class**, carried across from another unit in this wave: 12 handshakes written then dropped\n  unread, full capacity returns.\n- **Metrics and event caps are independent — this is the mutant the unit's own three cases miss.**\n  `metrics_stream` wired to `app.event_stream_limits` keeps all three of them green.\n- Outgoing frame truncation by the new 1 KiB ceiling: `max_frame_size`/`max_message_size` are\n  read-side only in tungstenite 0.29, so a server frame cannot be cut by them.\n- Ping-flood amplification, measured not argued: the loop consumes one control frame per sample\n  period, so a flooding client gets 1 pong/s, not more.\n- `EventStreamLimits` accounting cannot overcount across the remove-then-reacquire race.\n- The `NoDriver` fake's `unreachable!()` is not vacuous: a driver call panics the request task and\n  the unit's own cases go red.\n\n```findings\n- file: crates/substrate-daemon/src/app/metrics.rs\n  line: 176\n  category: contract-drift\n  severity: warning\n  verdict: needs-revision\n  origin: pre-existing\n  message: >-\n    run_stream awaits interval.tick() twice per frame (:176 and the select! arm at :207), so the\n    metrics stream delivers one sample every 2.000s against the 1000ms its released capability\n    predicate and both guides advertise; measured at tests/metrics_stream_adversary.rs:468 (gaps\n    1.9998s/2.0000s) and :621 (silent 1.9997s vs one-ping-per-sample 999.75ms, ratio 2.00), and the\n    same two ticks are present at base 617bbed.\n- file: crates/substrate-daemon/src/app/metrics.rs\n  line: 32\n  category: contract-drift\n  severity: warning\n  verdict: needs-revision\n  origin: introduced\n  message: >-\n    MetricsStreamPolicy duplicates five EventStreamPolicy values and only streams_per_subject and\n    global_streams are pinned by a case, and the other four are private to app::events so no test\n    outside that module can compare them; the values agree today, so the drift is latent and cannot\n    be made red without widening their visibility or reusing EventStreamPolicy.\n- file: crates/substrate-daemon/src/app/metrics.rs\n  line: 152\n  category: acceptance\n  severity: warning\n  verdict: needs-revision\n  origin: introduced\n  message: >-\n    the 1h lifetime added at :152 is reached by no case and cannot be reached by one, because\n    lifetime is private to app::metrics and MetricsStreamPolicy has no test constructor; routing it\n    through the pub(super) enforce_event_stream_lifetime at app/events.rs:629 would make it\n    observable the way the event stream's already is.\n- file: crates/substrate-daemon/src/app/metrics.rs\n  line: 824\n  category: mutant\n  severity: note\n  verdict: needs-revision\n  origin: introduced\n  message: >-\n    the class check scans src/app/ with one non-recursive read_dir and matches the substring\n    \".max_frame_size(\" in the raw 800 bytes before \".on_upgrade(\", so an upgrade in src/app.rs,\n    src/hosted.rs, src/tls.rs or src/runtime.rs, one written as hyper::upgrade::on, or one whose\n    bound appears only in a comment all pass it; not constructed, because the demonstration is a\n    fourth upgrade under src/app/ and that is an implementation file.\n- file: website/docs/guides/storage-and-metrics.md\n  line: 133\n  category: contract-drift\n  severity: note\n  verdict: needs-revision\n  origin: introduced\n  message: >-\n    the unit adds a client-visible 429 metrics.stream-capacity to a route this guide documents, and\n    neither the cap of 4 nor the refusal code appears anywhere outside daemon source; 0.15.0\n    refusals.json is confirmed session-only (36 rows, all session.*) so no bundle byte was owed, but\n    the acceptance's word \"published\" is satisfied by nothing a client can read.\n```\n","status":"active","title":"Adversary pass 1, unit u1 (metrics stream caps)"},"entity_type":"aep.review-result/v1","locator":"ep://planning/store/review-result/adversary-u1-pass-1"},"payload":{"actor":"human:timo","at":1788510904847,"causation":"cmd-new-review-result-adversary-u1-pass-1","change":{"change":"created","status":"active"},"correlation":"protocol-artifact-new","event_id":"review-result:adversary-u1-pass-1@1#0~a513714b3b70b58a","executor":null,"recorded_at":"2026-09-04T08:35:04Z"}}
{"entity":"review-result","version":1,"id":"adversary-u1-pass-1","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"story:metrics-streams-are-bounded"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000728","target":"01MEM0000000000000166"},"payload":{"actor":"human:timo","at":1788510904858,"causation":"cmd-review-result-adversary-u1-pass-1-reviews-story-metrics-streams-are-bounded","change":{"change":"related","relation":"reviews","target":"story:metrics-streams-are-bounded"},"correlation":"protocol-artifact-relate","event_id":"review-result:adversary-u1-pass-1@1#0~c6674acf373f9320","executor":null,"recorded_at":"2026-09-04T08:35:04Z"}}
{"entity":"review-result","version":1,"id":"adversary-u3-pass-2","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"active","changed":{"body":"# Adversary pass 2 — unit u3, `story:unattached-claimed-session-is-contained`\n\nAgent type `adp:adversary`. Worktree `wt-b180f053180d`, branch `impl/unattached-session-contained`,\nHEAD `f0c387e`, base `617bbed`. Report as returned.\n\n```\nunit: u3 story:unattached-claimed-session-is-contained\nverdict: red\ncases: executed 132→133, red 1\norigin: introduced 2 / pre-existing 2 / undecided 0\nwrote-outside-worktree: 4 paths\nneeds-coordinator: no\n```\n\n## Case added\n\n`crates/substrate-daemon/tests/pipe_session.rs:2716-2804`, +89, one path, no implementation file\ntouched. `adversary_a_served_attachment_with_an_unproven_kill_keeps_the_bounded_capacity` — the\nround-1 capacity case **with the upgrade allowed to succeed**, so `on_upgrade` runs.\n\n```\nthread 'adversary_a_served_attachment_with_an_unproven_kill_keeps_the_bounded_capacity' panicked at\ncrates/substrate-daemon/tests/pipe_session.rs:2787:13:\nattachment 31 of the bounded 32 is still answered 429 ten seconds after one served attachment whose\ncontainment kill could not be proven: sessions.rs:1338 retained the tombstone and\nPipeAttachmentPermit::drop then ran global.forget(), so one of this daemon's 32 attachment slots is\ngone until restart. The durable claim already barred that session, so the tombstone bought nothing\n-- which is the rule this unit's own sessions.rs:131-135 states.\ntest result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 28 filtered out; finished in 11.32s\n```\n\nSuite: exit 101, 133 executed across 16 targets, `pipe_session` 28 passed / 1 failed, every other\ntarget green including `runtime_vectors` (the known flake did not fire).\n\n## Findings\n\n**1 — `sessions.rs:1338`, blocker, `pre-existing`.** The slot leak round 1 closed on the\nfailed-upgrade path is still open on the **ordinary served-attachment path**.\n`terminate_pipe_session` (`:1871-1887`) returns false whenever `driver.signal` errors, exceeds\n`MAINTENANCE_DRIVER_TIMEOUT`, or the observation write fails — no flag, no fixture-only state; it is\nwhat a wedged driver does on any client disconnect. `Drop` then runs `global.forget()` (`:150`),\nremoving a permit from the fixed `Semaphore` for the process's life. **32 such events and the daemon\nserves zero attachments until restart**, answering `session.attachment-capacity` — a row published\nas *exhausted* and *retriable*, for a capacity that is destroyed and that no retry recovers.\nThat is `AGENTS.md` invariant 3.\n\n**2 — `sessions.rs:131`, blocker, `introduced`.** The doc comment **this diff added** states the\ntombstone must go \"never on a path the durable claim already bars, which would pay a permanent slot\nfor a bar that is already in place\" — and `:1338`, the one call site it left standing, is exactly\nsuch a path. The claim is `Attached` before `on_upgrade` runs and never returns to `Available`:\n`claim_pipe_session_attachment` answers `AlreadyClaimed` for `attached|consumed|uncertain`\n(`substrate-store/src/sessions.rs:735-739`), the handler gate refuses anything but `Available`\nbefore `acquire` (`sessions.rs:1150-1153`), and only `finish_pipe_session_start` sets `Available`\n(`app/operations.rs:1053`). **The unit ships a rule and a violation of it in one commit, green at\nevery gate step.**\n\n**3 — `tests/pipe_session.rs:2428`, warning, `introduced`.** The doc of\n`adversary_a_stranded_attach_with_an_unproven_kill_keeps_the_bounded_capacity` still says the\n`on_failed_upgrade` closure \"calls `retain_attachment_tombstone()` whenever `terminate_pipe_session`\nreturns `false`\". The correction removed that call; the closure's own comment now reads \"No tombstone\nhere, deliberately\". The case is green **because** the described call is gone, and its doc tells the\nnext reader it is present.\n\n**4 — `tests/pipe_session.rs:2661`, note, `INFEASIBLE`.** The RST case's `!claimed.is_empty()` guard\nis satisfied by any round, including one caught by the pre-existing `on_upgrade`/EOF hand-off, and\nnothing observes *which* hand-off ran; its filter GET also races the handler's claim. **Nothing\nfound:** 24 rounds green across 16 isolated runs, 8 parallel, 5 pinned to one CPU and 3 to two —\n32/32 passes, no skip observed. The implementor's own `red-f3-base.log` shows the case red with\n`app/sessions.rs` reverted, so it does gate the containment as a whole.\n\n**5 — `sessions.rs:91`, note, `pre-existing`.** `PipeAttachmentPermit.exec_id` and\n`PipeAttachmentLimits::acquire(scope, exec_id)` are passed a **session** id (`:1170` passes\n`&session_id`), while `terminate_pipe_session(app, scope, exec_id)` in the same file takes a real\nexec id. Nothing breaks — both the insert and the `Drop` remove use the same key.\n\n**Origin method.** `pre-existing` on 1 and 5 was established with\n`git show 617bbed:crates/substrate-daemon/src/app/sessions.rs`: `acquire`, `Drop`,\n`retain_attachment_tombstone`'s body and the entire `on_upgrade` closure are byte-identical to HEAD,\nand the diff's only two hunks are the doc comment and the `on_failed_upgrade` block — neither of\nwhich the new case executes. The base checkout was not moved; it is another agent's tree.\n\n**Named fix for 1 and 2, not applied.** Delete `retain_attachment_tombstone`, `remove_key_on_drop`\nand the `else` arm of `Drop`, and reduce `sessions.rs:1337-1339` to\n`if !completed { let _ = terminate_pipe_session(&app, &scope, &exec_id).await; }` — the warn-log the\n`on_failed_upgrade` branch already has belongs on this branch too. That restores the invariant the\ndiff's own new doc states and costs nothing, because the durable claim is the bar.\n\n## Attacked and could not break\n\n- **The F3 measurement's soundness** — the mechanism is library-level, not harness-level:\n  `vendor/axum-0.8.9/src/extract/ws.rs:359-366` calls `on_failed_upgrade` whenever\n  `hyper::upgrade::on(...)` resolves `Err`, which is what a dropped or errored connection future\n  with a pending upgrade produces, **independent of `with_upgrades()`**. All three production\n  listeners call `with_upgrades()` and additionally wrap the connection in\n  `enforce_connection_lifetime` (`runtime.rs:200-210`), which *drops* the future at 5 minutes — a\n  second, non-client trigger. Production reaches the closure at least as readily as the harness. Not\n  observed on a real listener; sound in mechanism, not measured.\n- **RST case stability** — 32 consecutive passes: 8 isolated (0.73–1.03 s), 8 parallel on a 20-core\n  box (1.53–1.55 s), 5 pinned to `cpu 0`, 3 pinned to `cpu 0,1`. No flake.\n- **The F1 fix's load-bearing claim** — that no route reaches `acquire` for a session whose claim was\n  consumed. Every write to `SessionAttachmentState` enumerated; `Available` is set only at session\n  start. Even a request whose gate read a stale `Available` is refused at the store. Holds, including\n  across a daemon restart.\n- **Caller enumeration for `retain_attachment_tombstone`** — complete; `grep` finds exactly one call\n  site. The enumeration was right; the decision to keep it was not (finding 1).\n- **F2's pinned case** — `sweep_expired` is the production tick (250 ms, `runtime.rs:534`) and the\n  case drives it explicitly 20 times, so a backstop landing in `sweep_expired`/`cleanup_expired`\n  trips it. It pins what it says.\n- **`warn!` on the unproven branch fires**, reached via `spawn_without_upgrades` + `refuse_signal`.\n- **`acquire`'s early return on `AlreadyAttached`** does not leak the global permit.\n\n```findings\n- file: crates/substrate-daemon/src/app/sessions.rs\n  line: 1338\n  category: mutant\n  severity: blocker\n  verdict: needs-revision\n  origin: pre-existing\n  message: >-\n    on the ordinary served-attachment path one client disconnect whose kill the driver cannot prove\n    permanently forgets a global attachment permit, so the daemon serves 31 of its 32 slots until\n    restart and reports the loss as a retriable \"exhausted\" capacity; measured red at\n    crates/substrate-daemon/tests/pipe_session.rs:2787, gate exit 101.\n- file: crates/substrate-daemon/src/app/sessions.rs\n  line: 131\n  category: contract-drift\n  severity: blocker\n  verdict: needs-revision\n  origin: introduced\n  message: >-\n    the doc comment this diff adds forbids the tombstone \"on a path the durable claim already bars\",\n    and the one call site the diff leaves standing at :1338 is exactly such a path, so the unit ships\n    a rule and a violation of it in one commit, green at every step.\n- file: crates/substrate-daemon/tests/pipe_session.rs\n  line: 2428\n  category: contract-drift\n  severity: warning\n  verdict: needs-revision\n  origin: introduced\n  message: >-\n    the doc of adversary_a_stranded_attach_with_an_unproven_kill_keeps_the_bounded_capacity still\n    says the on_failed_upgrade closure calls retain_attachment_tombstone, which the F1 correction\n    removed, so the case now passes because the behaviour it describes is gone.\n- file: crates/substrate-daemon/tests/pipe_session.rs\n  line: 2661\n  category: property\n  severity: note\n  verdict: needs-revision\n  origin: introduced\n  message: >-\n    the RST case's !claimed.is_empty() guard is satisfied by a round caught by the pre-existing\n    on_upgrade EOF hand-off and nothing observes which hand-off ran, and its filter GET races the\n    handler's claim, but no skip or flake could be exhibited in 32 runs across four scheduling\n    regimes.\n- file: crates/substrate-daemon/src/app/sessions.rs\n  line: 91\n  category: judgement\n  severity: note\n  verdict: needs-revision\n  origin: pre-existing\n  message: >-\n    PipeAttachmentPermit.exec_id and PipeAttachmentLimits::acquire's exec_id parameter both hold a\n    session id (:1170 passes &session_id) while terminate_pipe_session in the same file takes a real\n    exec id, three lines from the machinery this unit changed.\n```\n","title":"Adversary pass 2, unit u3 (unattached session containment)"},"args":{"command":"create-entity","data":{"body":"# Adversary pass 2 — unit u3, `story:unattached-claimed-session-is-contained`\n\nAgent type `adp:adversary`. Worktree `wt-b180f053180d`, branch `impl/unattached-session-contained`,\nHEAD `f0c387e`, base `617bbed`. Report as returned.\n\n```\nunit: u3 story:unattached-claimed-session-is-contained\nverdict: red\ncases: executed 132→133, red 1\norigin: introduced 2 / pre-existing 2 / undecided 0\nwrote-outside-worktree: 4 paths\nneeds-coordinator: no\n```\n\n## Case added\n\n`crates/substrate-daemon/tests/pipe_session.rs:2716-2804`, +89, one path, no implementation file\ntouched. `adversary_a_served_attachment_with_an_unproven_kill_keeps_the_bounded_capacity` — the\nround-1 capacity case **with the upgrade allowed to succeed**, so `on_upgrade` runs.\n\n```\nthread 'adversary_a_served_attachment_with_an_unproven_kill_keeps_the_bounded_capacity' panicked at\ncrates/substrate-daemon/tests/pipe_session.rs:2787:13:\nattachment 31 of the bounded 32 is still answered 429 ten seconds after one served attachment whose\ncontainment kill could not be proven: sessions.rs:1338 retained the tombstone and\nPipeAttachmentPermit::drop then ran global.forget(), so one of this daemon's 32 attachment slots is\ngone until restart. The durable claim already barred that session, so the tombstone bought nothing\n-- which is the rule this unit's own sessions.rs:131-135 states.\ntest result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 28 filtered out; finished in 11.32s\n```\n\nSuite: exit 101, 133 executed across 16 targets, `pipe_session` 28 passed / 1 failed, every other\ntarget green including `runtime_vectors` (the known flake did not fire).\n\n## Findings\n\n**1 — `sessions.rs:1338`, blocker, `pre-existing`.** The slot leak round 1 closed on the\nfailed-upgrade path is still open on the **ordinary served-attachment path**.\n`terminate_pipe_session` (`:1871-1887`) returns false whenever `driver.signal` errors, exceeds\n`MAINTENANCE_DRIVER_TIMEOUT`, or the observation write fails — no flag, no fixture-only state; it is\nwhat a wedged driver does on any client disconnect. `Drop` then runs `global.forget()` (`:150`),\nremoving a permit from the fixed `Semaphore` for the process's life. **32 such events and the daemon\nserves zero attachments until restart**, answering `session.attachment-capacity` — a row published\nas *exhausted* and *retriable*, for a capacity that is destroyed and that no retry recovers.\nThat is `AGENTS.md` invariant 3.\n\n**2 — `sessions.rs:131`, blocker, `introduced`.** The doc comment **this diff added** states the\ntombstone must go \"never on a path the durable claim already bars, which would pay a permanent slot\nfor a bar that is already in place\" — and `:1338`, the one call site it left standing, is exactly\nsuch a path. The claim is `Attached` before `on_upgrade` runs and never returns to `Available`:\n`claim_pipe_session_attachment` answers `AlreadyClaimed` for `attached|consumed|uncertain`\n(`substrate-store/src/sessions.rs:735-739`), the handler gate refuses anything but `Available`\nbefore `acquire` (`sessions.rs:1150-1153`), and only `finish_pipe_session_start` sets `Available`\n(`app/operations.rs:1053`). **The unit ships a rule and a violation of it in one commit, green at\nevery gate step.**\n\n**3 — `tests/pipe_session.rs:2428`, warning, `introduced`.** The doc of\n`adversary_a_stranded_attach_with_an_unproven_kill_keeps_the_bounded_capacity` still says the\n`on_failed_upgrade` closure \"calls `retain_attachment_tombstone()` whenever `terminate_pipe_session`\nreturns `false`\". The correction removed that call; the closure's own comment now reads \"No tombstone\nhere, deliberately\". The case is green **because** the described call is gone, and its doc tells the\nnext reader it is present.\n\n**4 — `tests/pipe_session.rs:2661`, note, `INFEASIBLE`.** The RST case's `!claimed.is_empty()` guard\nis satisfied by any round, including one caught by the pre-existing `on_upgrade`/EOF hand-off, and\nnothing observes *which* hand-off ran; its filter GET also races the handler's claim. **Nothing\nfound:** 24 rounds green across 16 isolated runs, 8 parallel, 5 pinned to one CPU and 3 to two —\n32/32 passes, no skip observed. The implementor's own `red-f3-base.log` shows the case red with\n`app/sessions.rs` reverted, so it does gate the containment as a whole.\n\n**5 — `sessions.rs:91`, note, `pre-existing`.** `PipeAttachmentPermit.exec_id` and\n`PipeAttachmentLimits::acquire(scope, exec_id)` are passed a **session** id (`:1170` passes\n`&session_id`), while `terminate_pipe_session(app, scope, exec_id)` in the same file takes a real\nexec id. Nothing breaks — both the insert and the `Drop` remove use the same key.\n\n**Origin method.** `pre-existing` on 1 and 5 was established with\n`git show 617bbed:crates/substrate-daemon/src/app/sessions.rs`: `acquire`, `Drop`,\n`retain_attachment_tombstone`'s body and the entire `on_upgrade` closure are byte-identical to HEAD,\nand the diff's only two hunks are the doc comment and the `on_failed_upgrade` block — neither of\nwhich the new case executes. The base checkout was not moved; it is another agent's tree.\n\n**Named fix for 1 and 2, not applied.** Delete `retain_attachment_tombstone`, `remove_key_on_drop`\nand the `else` arm of `Drop`, and reduce `sessions.rs:1337-1339` to\n`if !completed { let _ = terminate_pipe_session(&app, &scope, &exec_id).await; }` — the warn-log the\n`on_failed_upgrade` branch already has belongs on this branch too. That restores the invariant the\ndiff's own new doc states and costs nothing, because the durable claim is the bar.\n\n## Attacked and could not break\n\n- **The F3 measurement's soundness** — the mechanism is library-level, not harness-level:\n  `vendor/axum-0.8.9/src/extract/ws.rs:359-366` calls `on_failed_upgrade` whenever\n  `hyper::upgrade::on(...)` resolves `Err`, which is what a dropped or errored connection future\n  with a pending upgrade produces, **independent of `with_upgrades()`**. All three production\n  listeners call `with_upgrades()` and additionally wrap the connection in\n  `enforce_connection_lifetime` (`runtime.rs:200-210`), which *drops* the future at 5 minutes — a\n  second, non-client trigger. Production reaches the closure at least as readily as the harness. Not\n  observed on a real listener; sound in mechanism, not measured.\n- **RST case stability** — 32 consecutive passes: 8 isolated (0.73–1.03 s), 8 parallel on a 20-core\n  box (1.53–1.55 s), 5 pinned to `cpu 0`, 3 pinned to `cpu 0,1`. No flake.\n- **The F1 fix's load-bearing claim** — that no route reaches `acquire` for a session whose claim was\n  consumed. Every write to `SessionAttachmentState` enumerated; `Available` is set only at session\n  start. Even a request whose gate read a stale `Available` is refused at the store. Holds, including\n  across a daemon restart.\n- **Caller enumeration for `retain_attachment_tombstone`** — complete; `grep` finds exactly one call\n  site. The enumeration was right; the decision to keep it was not (finding 1).\n- **F2's pinned case** — `sweep_expired` is the production tick (250 ms, `runtime.rs:534`) and the\n  case drives it explicitly 20 times, so a backstop landing in `sweep_expired`/`cleanup_expired`\n  trips it. It pins what it says.\n- **`warn!` on the unproven branch fires**, reached via `spawn_without_upgrades` + `refuse_signal`.\n- **`acquire`'s early return on `AlreadyAttached`** does not leak the global permit.\n\n```findings\n- file: crates/substrate-daemon/src/app/sessions.rs\n  line: 1338\n  category: mutant\n  severity: blocker\n  verdict: needs-revision\n  origin: pre-existing\n  message: >-\n    on the ordinary served-attachment path one client disconnect whose kill the driver cannot prove\n    permanently forgets a global attachment permit, so the daemon serves 31 of its 32 slots until\n    restart and reports the loss as a retriable \"exhausted\" capacity; measured red at\n    crates/substrate-daemon/tests/pipe_session.rs:2787, gate exit 101.\n- file: crates/substrate-daemon/src/app/sessions.rs\n  line: 131\n  category: contract-drift\n  severity: blocker\n  verdict: needs-revision\n  origin: introduced\n  message: >-\n    the doc comment this diff adds forbids the tombstone \"on a path the durable claim already bars\",\n    and the one call site the diff leaves standing at :1338 is exactly such a path, so the unit ships\n    a rule and a violation of it in one commit, green at every step.\n- file: crates/substrate-daemon/tests/pipe_session.rs\n  line: 2428\n  category: contract-drift\n  severity: warning\n  verdict: needs-revision\n  origin: introduced\n  message: >-\n    the doc of adversary_a_stranded_attach_with_an_unproven_kill_keeps_the_bounded_capacity still\n    says the on_failed_upgrade closure calls retain_attachment_tombstone, which the F1 correction\n    removed, so the case now passes because the behaviour it describes is gone.\n- file: crates/substrate-daemon/tests/pipe_session.rs\n  line: 2661\n  category: property\n  severity: note\n  verdict: needs-revision\n  origin: introduced\n  message: >-\n    the RST case's !claimed.is_empty() guard is satisfied by a round caught by the pre-existing\n    on_upgrade EOF hand-off and nothing observes which hand-off ran, and its filter GET races the\n    handler's claim, but no skip or flake could be exhibited in 32 runs across four scheduling\n    regimes.\n- file: crates/substrate-daemon/src/app/sessions.rs\n  line: 91\n  category: judgement\n  severity: note\n  verdict: needs-revision\n  origin: pre-existing\n  message: >-\n    PipeAttachmentPermit.exec_id and PipeAttachmentLimits::acquire's exec_id parameter both hold a\n    session id (:1170 passes &session_id) while terminate_pipe_session in the same file takes a real\n    exec id, three lines from the machinery this unit changed.\n```\n","status":"active","title":"Adversary pass 2, unit u3 (unattached session containment)"},"entity_type":"aep.review-result/v1","locator":"ep://planning/store/review-result/adversary-u3-pass-2"},"payload":{"actor":"human:timo","at":1788511161864,"causation":"cmd-new-review-result-adversary-u3-pass-2","change":{"change":"created","status":"active"},"correlation":"protocol-artifact-new","event_id":"review-result:adversary-u3-pass-2@1#0~9308b4436886b6c3","executor":null,"recorded_at":"2026-09-04T08:39:21Z"}}
{"entity":"review-result","version":1,"id":"adversary-u3-pass-2","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"story:unattached-claimed-session-is-contained"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000733","target":"01MEM0000000000000244"},"payload":{"actor":"human:timo","at":1788511161881,"causation":"cmd-review-result-adversary-u3-pass-2-reviews-story-unattached-claimed-session-is-contained","change":{"change":"related","relation":"reviews","target":"story:unattached-claimed-session-is-contained"},"correlation":"protocol-artifact-relate","event_id":"review-result:adversary-u3-pass-2@1#0~7c19763f32f54469","executor":null,"recorded_at":"2026-09-04T08:39:21Z"}}
{"entity":"story","version":1,"id":"unattached-claimed-session-is-contained","revision":5,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"review_outcome","outcome":"fixed","review":"review-result:adversary-u3-pass-2","source":"review-result:adversary-u3-pass-2","target":"01MEM0000000000000247"},"payload":{"actor":"human:timo","at":1788511677000,"causation":"cmd-evidence-story-unattached-claimed-session-is-contained-review_outcome-1788511677981","change":{"change":"evidence","kind":"review_outcome","outcome":"fixed","review":"review-result:adversary-u3-pass-2","source":"review-result:adversary-u3-pass-2"},"correlation":"protocol-artifact-evidence","event_id":"story:unattached-claimed-session-is-contained@5#0~035fa666d73426f3","executor":null,"recorded_at":"2026-09-04T08:47:57Z"}}
{"entity":"story","version":1,"id":"metrics-streams-are-bounded","revision":7,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"review_outcome","outcome":"fixed","review":"review-result:adversary-u1-pass-1","source":"review-result:adversary-u1-pass-1","target":"01MEM0000000000000172"},"payload":{"actor":"human:timo","at":1788512041000,"causation":"cmd-evidence-story-metrics-streams-are-bounded-review_outcome-1788512041853","change":{"change":"evidence","kind":"review_outcome","outcome":"fixed","review":"review-result:adversary-u1-pass-1","source":"review-result:adversary-u1-pass-1"},"correlation":"protocol-artifact-evidence","event_id":"story:metrics-streams-are-bounded@7#0~aabf962fc7b68f70","executor":null,"recorded_at":"2026-09-04T08:54:01Z"}}
{"entity":"review-result","version":1,"id":"adversary-u1-pass-2","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"active","changed":{"body":"# Adversary pass 2 — unit u1, `story:metrics-streams-are-bounded`\n\nAgent type `adp:adversary`. Worktree `wt-d5a923d7143d`, branch `impl/metrics-streams-bounded`,\nHEAD `41aa112`, base `617bbed`. Report as returned.\n\n```\nunit: u1 — story:metrics-streams-are-bounded\nverdict: red\ncases: executed 139→142, red 2\norigin: introduced 2 / pre-existing 2 / undecided 0\nwrote-outside-worktree: 4 paths\nneeds-coordinator: no\n```\n\n## Cases added\n\n182 insertions, **0 deletions**, appended below a marker line; no existing line in the pass-1 file\ntouched.\n\n| line | case | result |\n|---|---|---|\n| 718 | `a_metrics_control_frame_flood_earns_the_close_the_event_stream_gives` | **red** |\n| 762 | `a_metrics_data_frame_earns_the_named_close_the_event_stream_gives` | **red** |\n| 793 | `the_first_metrics_sample_is_immediate_and_the_second_is_one_interval_later` | green |\n\n```\nthread 'a_metrics_control_frame_flood_earns_the_close_the_event_stream_gives' panicked at\ncrates/substrate-daemon/tests/metrics_stream_adversary.rs:716:5:\nassertion `left == right` failed: the event stream spends a client's 120 control frames per window\nand then closes 1008 (tests/websocket.rs:354); the metrics stream answered 121 pongs to 121 pings in\n6.00158252s and ended as StillOpen\n  left: StillOpen\n right: Close(1008)\n\nthread 'a_metrics_data_frame_earns_the_named_close_the_event_stream_gives' panicked at\ncrates/substrate-daemon/tests/metrics_stream_adversary.rs:760:5:\nassertion `left == right` failed: a 512 byte data frame is half the declared 1024 byte ceiling, and\nthe metrics stream ended as Eof — the same ending the 1 025 byte frame at line 527 gets, so that\ncase does not observe the ceiling; the event stream answers this frame with close 1003\n(tests/websocket.rs:314)\n  left: Eof\n right: Close(1003)\n```\n\nSuite: **exit 101**, 142 executed, 140 passed, 2 failed. Every other target green. `139` corroborated\ntwo ways: the implementing state's number, and a re-run with the three new cases deselected\n(`5 passed; 3 filtered out`, exit 0).\n\n## Findings\n\n**F1 — the metrics stream has no control-frame budget.** `metrics.rs:214`, blocker,\n**pre-existing**. 121 pings answered with 121 pongs, no close frame, stream still open at the 6 s\ndeadline. The identical flood on the event stream closes 1008\n(`tests/websocket.rs:354`, green). `EventStreamPolicy` bounds this with\n`max_controls_per_window: 120` / `control_window: 1 min` (`events.rs:52-53`) enforced at\n`events.rs:515-527`; `MetricsStreamPolicy` (`metrics.rs:31-39`) restates neither, while its own\ndocstring (`metrics.rs:26`) claims it holds \"the shape `EventStreamPolicy` publishes\".\n`617bbed` has no `ControlRate` either. Fix named: carry the two fields and run `events::ControlRate`\nin `run_stream`'s inner loop, closing 1008 the way `events.rs:521` does.\n\n**F2 — the cadence fix removed the incidental throttle that stood in for F1's missing guard.**\n`metrics.rs:214`, blocker, **introduced**. At `617bbed` and `377b115` the `select!` was the last\nstatement of the outer loop, whose head awaited `interval.tick()`, so **at most one** control frame\nwas answered per 1 s sample period — pass 1 measured exactly this and filed it under *could not\nbreak*: \"a flooding client gets 1 pong/s, not more\". `402cb55` wrapped the `select!` in an inner\n`loop`, so the route now answers control frames at line rate: a read, a match and a `socket.send`\nper frame, on a permit held up to the 1 h lifetime. Same code, ~20× the answered frames in the same\nwindow, and unbounded above that.\n\n**F3 — the oversized-frame case does not observe the frame ceiling, and `metrics.rs:944` says it\ndoes.** `metrics.rs:222`, warning, **pre-existing**. A 512-byte frame — half the declared ceiling,\nrejected by no bound — ends the stream as `Eof` through the same `_ => return` arm an oversized\nframe takes. Neither sends a close frame; both end by dropping the socket. Pass 1's case accepts\n`None | Some(1009)`, so it cannot tell them apart. Consequence: **the mutant\n`max_frame_size(usize::MAX)` survives the whole suite**, and the unit's stated limit (\"proves the\nbounds are declared, never that they fire\") has no partner closing it. The guide paragraph this unit\nadded promises \"a client is told when it hits a bound\"\n(`website/docs/guides/storage-and-metrics.md:139`); the client is told nothing. The sibling route\nanswers the same frame with close 1003 and a reason (`events.rs:505-511`).\n\n**F4 — one of `41aa112`'s five new assertions is vacuous.** `app/tests.rs:534`, warning,\n**introduced**. `assert_eq!(metrics.max_output_bytes, BODY_LIMIT)` compares `BODY_LIMIT` with\n`BODY_LIMIT` — `MetricsStreamPolicy::production()` sets `max_output_bytes: BODY_LIMIT`\n(`metrics.rs:48`). It cannot fail. `tests.rs:526`,\n`assert_eq!(metrics.max_output_bytes, events.max_output_bytes)`, is blind to the same move:\n`events.rs:50` is also `BODY_LIMIT`. The other four new comparisons read independent literals in two\nindependent `const fn`s and **do** bite. The docstring `41aa112` installed at `tests.rs:514-517`\nsays \"every published value is also pinned against its literal\" — for `max_output_bytes` there is no\nliteral and no pin, so moving `app.rs:6` (`const BODY_LIMIT: usize = 2_097_152`, one edit away,\nalready feeding `routes.rs:36`) moves the metrics write-buffer ceiling with nothing red.\n\n**F5 — the pong reply has no send deadline.** `metrics.rs:219`, note, `INFEASIBLE`.\n`socket.send(Message::Pong(bytes)).await` is bare; the sample send at `metrics.rs:196-201` is wrapped\nin `tokio::time::timeout(policy.send_timeout, …)`. Not constructible: the sample send fires its 5 s\ndeadline first in every ordering the adversary could build. The asymmetry is one line and\n`policy.send_timeout` is already in scope.\n\n## Attacked, could not break\n\n- **The pass-1 file's integrity — verified clean.** All five case bodies present, all five line\n  numbers shifted by exactly −1, and both assertion messages the pass-1 report quoted are\n  byte-identical in the committed file. **No assertion was changed, relaxed, renamed or dropped.**\n- **`41aa112` leaks nothing.** `mod app;` is private in `lib.rs:3`, so `pub(super)` on an\n  `app::events` field is visible in `app` and nowhere else; no re-export.\n- **Four of `41aa112`'s five new assertions bite** — two independent `const fn`s with independent\n  literals. Only `max_output_bytes` is vacuous (F4).\n- **`max_catch_up_pages` / `max_page_items` left private is right** — no metrics analogue.\n  `max_controls_per_window` / `control_window` are the seam that commit's reasoning missed (F1);\n  they were **already** `pub(super)`, so no widening was needed to close it.\n- **The post-fix cadence shape is right at the head of the stream** — first sample < 500 ms after\n  the upgrade, second ≥ 500 ms after the first. Neither dropped nor doubled.\n- **F1's `tokio::time::interval` enumeration is complete** — repo-wide grep finds exactly the four\n  sites named.\n- **`the_advertised_sample_interval…` genuinely cannot be red-first**, and `probe.rs:153` uses\n  `substrate_wire::RESOURCE_USAGE_SAMPLE_INTERVAL_MS` rather than a literal, so the advertised fact\n  cannot desync from what the daemon paces on. Small gap: the case counts *versions*, not\n  operations.\n- **The masker could not be desynced from outside.** The one candidate found by reading — `'\\''` —\n  occurs nowhere under `src/`, so it is latent, not live.\n- **The guide edit does not reach an Atlas-owned field** — frontmatter is `title`/`description` only\n  and was not touched. Agreeing with the unit that reconciliation has nothing to drift.\n- **Agreeing that the machine-readable cap is a story, not a line.**\n\n```findings\n- file: crates/substrate-daemon/src/app/metrics.rs\n  line: 214\n  category: contract-drift\n  severity: blocker\n  verdict: needs-revision\n  origin: pre-existing\n  message: >-\n    the metrics stream answers client control frames with no budget and no close — 121 pings earned\n    121 pongs and no close frame at metrics_stream_adversary.rs:718 (exit 101) — while\n    EventStreamPolicy bounds exactly this at events.rs:52-53 and closes 1008 (tests/websocket.rs:354),\n    and MetricsStreamPolicy at metrics.rs:31-39 restates neither of those two fields despite its\n    docstring claiming the shape EventStreamPolicy publishes; reproduces at 617bbed, which has no\n    ControlRate either.\n- file: crates/substrate-daemon/src/app/metrics.rs\n  line: 214\n  category: concurrency\n  severity: blocker\n  verdict: needs-revision\n  origin: introduced\n  message: >-\n    402cb55 wrapped the client-frame select! in an inner loop, removing the outer interval.tick()\n    that admitted at most one control frame per sample period, so the unguarded control path now runs\n    at line rate — 121 frames answered inside one 6 s window against at most 6 in the pre-fix shape,\n    on a permit held up to the 1 h lifetime.\n- file: crates/substrate-daemon/src/app/metrics.rs\n  line: 222\n  category: mutant\n  severity: warning\n  verdict: needs-revision\n  origin: pre-existing\n  message: >-\n    a 512-byte data frame ends the metrics stream as Eof (metrics_stream_adversary.rs:762, exit 101)\n    through the same `_ => return` arm an oversized frame takes, so\n    an_oversized_client_frame_ends_the_metrics_stream_and_returns_its_permit cannot observe the frame\n    ceiling and max_frame_size(usize::MAX) survives the suite — yet metrics.rs:944 names that case as\n    the one that observes the bound firing and the guide at storage-and-metrics.md:139 promises the\n    client is told which bound it hit.\n- file: crates/substrate-daemon/src/app/tests.rs\n  line: 534\n  category: mutant\n  severity: warning\n  verdict: needs-revision\n  origin: introduced\n  message: >-\n    assert_eq!(metrics.max_output_bytes, BODY_LIMIT) compares BODY_LIMIT with itself (metrics.rs:48)\n    and the cross-policy comparison at tests.rs:526 reads BODY_LIMIT on both sides (events.rs:50), so\n    one of the seven bounds is pinned by two tautologies and moving app.rs:6 moves the metrics\n    write-buffer ceiling with nothing red, against the docstring at tests.rs:514-517 claiming every\n    published value is pinned against its literal.\n- file: crates/substrate-daemon/src/app/metrics.rs\n  line: 219\n  category: judgement\n  severity: note\n  verdict: needs-revision\n  origin: pre-existing\n  message: >-\n    the pong reply is a bare socket.send().await with no policy.send_timeout while the sample send at\n    metrics.rs:196-201 has one, leaving only the 1 h lifetime above a blocked write; not constructible\n    because the 5 s sample deadline fires first in every ordering that could be built.\n```\n","title":"Adversary pass 2, unit u1 (metrics stream caps)"},"args":{"command":"create-entity","data":{"body":"# Adversary pass 2 — unit u1, `story:metrics-streams-are-bounded`\n\nAgent type `adp:adversary`. Worktree `wt-d5a923d7143d`, branch `impl/metrics-streams-bounded`,\nHEAD `41aa112`, base `617bbed`. Report as returned.\n\n```\nunit: u1 — story:metrics-streams-are-bounded\nverdict: red\ncases: executed 139→142, red 2\norigin: introduced 2 / pre-existing 2 / undecided 0\nwrote-outside-worktree: 4 paths\nneeds-coordinator: no\n```\n\n## Cases added\n\n182 insertions, **0 deletions**, appended below a marker line; no existing line in the pass-1 file\ntouched.\n\n| line | case | result |\n|---|---|---|\n| 718 | `a_metrics_control_frame_flood_earns_the_close_the_event_stream_gives` | **red** |\n| 762 | `a_metrics_data_frame_earns_the_named_close_the_event_stream_gives` | **red** |\n| 793 | `the_first_metrics_sample_is_immediate_and_the_second_is_one_interval_later` | green |\n\n```\nthread 'a_metrics_control_frame_flood_earns_the_close_the_event_stream_gives' panicked at\ncrates/substrate-daemon/tests/metrics_stream_adversary.rs:716:5:\nassertion `left == right` failed: the event stream spends a client's 120 control frames per window\nand then closes 1008 (tests/websocket.rs:354); the metrics stream answered 121 pongs to 121 pings in\n6.00158252s and ended as StillOpen\n  left: StillOpen\n right: Close(1008)\n\nthread 'a_metrics_data_frame_earns_the_named_close_the_event_stream_gives' panicked at\ncrates/substrate-daemon/tests/metrics_stream_adversary.rs:760:5:\nassertion `left == right` failed: a 512 byte data frame is half the declared 1024 byte ceiling, and\nthe metrics stream ended as Eof — the same ending the 1 025 byte frame at line 527 gets, so that\ncase does not observe the ceiling; the event stream answers this frame with close 1003\n(tests/websocket.rs:314)\n  left: Eof\n right: Close(1003)\n```\n\nSuite: **exit 101**, 142 executed, 140 passed, 2 failed. Every other target green. `139` corroborated\ntwo ways: the implementing state's number, and a re-run with the three new cases deselected\n(`5 passed; 3 filtered out`, exit 0).\n\n## Findings\n\n**F1 — the metrics stream has no control-frame budget.** `metrics.rs:214`, blocker,\n**pre-existing**. 121 pings answered with 121 pongs, no close frame, stream still open at the 6 s\ndeadline. The identical flood on the event stream closes 1008\n(`tests/websocket.rs:354`, green). `EventStreamPolicy` bounds this with\n`max_controls_per_window: 120` / `control_window: 1 min` (`events.rs:52-53`) enforced at\n`events.rs:515-527`; `MetricsStreamPolicy` (`metrics.rs:31-39`) restates neither, while its own\ndocstring (`metrics.rs:26`) claims it holds \"the shape `EventStreamPolicy` publishes\".\n`617bbed` has no `ControlRate` either. Fix named: carry the two fields and run `events::ControlRate`\nin `run_stream`'s inner loop, closing 1008 the way `events.rs:521` does.\n\n**F2 — the cadence fix removed the incidental throttle that stood in for F1's missing guard.**\n`metrics.rs:214`, blocker, **introduced**. At `617bbed` and `377b115` the `select!` was the last\nstatement of the outer loop, whose head awaited `interval.tick()`, so **at most one** control frame\nwas answered per 1 s sample period — pass 1 measured exactly this and filed it under *could not\nbreak*: \"a flooding client gets 1 pong/s, not more\". `402cb55` wrapped the `select!` in an inner\n`loop`, so the route now answers control frames at line rate: a read, a match and a `socket.send`\nper frame, on a permit held up to the 1 h lifetime. Same code, ~20× the answered frames in the same\nwindow, and unbounded above that.\n\n**F3 — the oversized-frame case does not observe the frame ceiling, and `metrics.rs:944` says it\ndoes.** `metrics.rs:222`, warning, **pre-existing**. A 512-byte frame — half the declared ceiling,\nrejected by no bound — ends the stream as `Eof` through the same `_ => return` arm an oversized\nframe takes. Neither sends a close frame; both end by dropping the socket. Pass 1's case accepts\n`None | Some(1009)`, so it cannot tell them apart. Consequence: **the mutant\n`max_frame_size(usize::MAX)` survives the whole suite**, and the unit's stated limit (\"proves the\nbounds are declared, never that they fire\") has no partner closing it. The guide paragraph this unit\nadded promises \"a client is told when it hits a bound\"\n(`website/docs/guides/storage-and-metrics.md:139`); the client is told nothing. The sibling route\nanswers the same frame with close 1003 and a reason (`events.rs:505-511`).\n\n**F4 — one of `41aa112`'s five new assertions is vacuous.** `app/tests.rs:534`, warning,\n**introduced**. `assert_eq!(metrics.max_output_bytes, BODY_LIMIT)` compares `BODY_LIMIT` with\n`BODY_LIMIT` — `MetricsStreamPolicy::production()` sets `max_output_bytes: BODY_LIMIT`\n(`metrics.rs:48`). It cannot fail. `tests.rs:526`,\n`assert_eq!(metrics.max_output_bytes, events.max_output_bytes)`, is blind to the same move:\n`events.rs:50` is also `BODY_LIMIT`. The other four new comparisons read independent literals in two\nindependent `const fn`s and **do** bite. The docstring `41aa112` installed at `tests.rs:514-517`\nsays \"every published value is also pinned against its literal\" — for `max_output_bytes` there is no\nliteral and no pin, so moving `app.rs:6` (`const BODY_LIMIT: usize = 2_097_152`, one edit away,\nalready feeding `routes.rs:36`) moves the metrics write-buffer ceiling with nothing red.\n\n**F5 — the pong reply has no send deadline.** `metrics.rs:219`, note, `INFEASIBLE`.\n`socket.send(Message::Pong(bytes)).await` is bare; the sample send at `metrics.rs:196-201` is wrapped\nin `tokio::time::timeout(policy.send_timeout, …)`. Not constructible: the sample send fires its 5 s\ndeadline first in every ordering the adversary could build. The asymmetry is one line and\n`policy.send_timeout` is already in scope.\n\n## Attacked, could not break\n\n- **The pass-1 file's integrity — verified clean.** All five case bodies present, all five line\n  numbers shifted by exactly −1, and both assertion messages the pass-1 report quoted are\n  byte-identical in the committed file. **No assertion was changed, relaxed, renamed or dropped.**\n- **`41aa112` leaks nothing.** `mod app;` is private in `lib.rs:3`, so `pub(super)` on an\n  `app::events` field is visible in `app` and nowhere else; no re-export.\n- **Four of `41aa112`'s five new assertions bite** — two independent `const fn`s with independent\n  literals. Only `max_output_bytes` is vacuous (F4).\n- **`max_catch_up_pages` / `max_page_items` left private is right** — no metrics analogue.\n  `max_controls_per_window` / `control_window` are the seam that commit's reasoning missed (F1);\n  they were **already** `pub(super)`, so no widening was needed to close it.\n- **The post-fix cadence shape is right at the head of the stream** — first sample < 500 ms after\n  the upgrade, second ≥ 500 ms after the first. Neither dropped nor doubled.\n- **F1's `tokio::time::interval` enumeration is complete** — repo-wide grep finds exactly the four\n  sites named.\n- **`the_advertised_sample_interval…` genuinely cannot be red-first**, and `probe.rs:153` uses\n  `substrate_wire::RESOURCE_USAGE_SAMPLE_INTERVAL_MS` rather than a literal, so the advertised fact\n  cannot desync from what the daemon paces on. Small gap: the case counts *versions*, not\n  operations.\n- **The masker could not be desynced from outside.** The one candidate found by reading — `'\\''` —\n  occurs nowhere under `src/`, so it is latent, not live.\n- **The guide edit does not reach an Atlas-owned field** — frontmatter is `title`/`description` only\n  and was not touched. Agreeing with the unit that reconciliation has nothing to drift.\n- **Agreeing that the machine-readable cap is a story, not a line.**\n\n```findings\n- file: crates/substrate-daemon/src/app/metrics.rs\n  line: 214\n  category: contract-drift\n  severity: blocker\n  verdict: needs-revision\n  origin: pre-existing\n  message: >-\n    the metrics stream answers client control frames with no budget and no close — 121 pings earned\n    121 pongs and no close frame at metrics_stream_adversary.rs:718 (exit 101) — while\n    EventStreamPolicy bounds exactly this at events.rs:52-53 and closes 1008 (tests/websocket.rs:354),\n    and MetricsStreamPolicy at metrics.rs:31-39 restates neither of those two fields despite its\n    docstring claiming the shape EventStreamPolicy publishes; reproduces at 617bbed, which has no\n    ControlRate either.\n- file: crates/substrate-daemon/src/app/metrics.rs\n  line: 214\n  category: concurrency\n  severity: blocker\n  verdict: needs-revision\n  origin: introduced\n  message: >-\n    402cb55 wrapped the client-frame select! in an inner loop, removing the outer interval.tick()\n    that admitted at most one control frame per sample period, so the unguarded control path now runs\n    at line rate — 121 frames answered inside one 6 s window against at most 6 in the pre-fix shape,\n    on a permit held up to the 1 h lifetime.\n- file: crates/substrate-daemon/src/app/metrics.rs\n  line: 222\n  category: mutant\n  severity: warning\n  verdict: needs-revision\n  origin: pre-existing\n  message: >-\n    a 512-byte data frame ends the metrics stream as Eof (metrics_stream_adversary.rs:762, exit 101)\n    through the same `_ => return` arm an oversized frame takes, so\n    an_oversized_client_frame_ends_the_metrics_stream_and_returns_its_permit cannot observe the frame\n    ceiling and max_frame_size(usize::MAX) survives the suite — yet metrics.rs:944 names that case as\n    the one that observes the bound firing and the guide at storage-and-metrics.md:139 promises the\n    client is told which bound it hit.\n- file: crates/substrate-daemon/src/app/tests.rs\n  line: 534\n  category: mutant\n  severity: warning\n  verdict: needs-revision\n  origin: introduced\n  message: >-\n    assert_eq!(metrics.max_output_bytes, BODY_LIMIT) compares BODY_LIMIT with itself (metrics.rs:48)\n    and the cross-policy comparison at tests.rs:526 reads BODY_LIMIT on both sides (events.rs:50), so\n    one of the seven bounds is pinned by two tautologies and moving app.rs:6 moves the metrics\n    write-buffer ceiling with nothing red, against the docstring at tests.rs:514-517 claiming every\n    published value is pinned against its literal.\n- file: crates/substrate-daemon/src/app/metrics.rs\n  line: 219\n  category: judgement\n  severity: note\n  verdict: needs-revision\n  origin: pre-existing\n  message: >-\n    the pong reply is a bare socket.send().await with no policy.send_timeout while the sample send at\n    metrics.rs:196-201 has one, leaving only the 1 h lifetime above a blocked write; not constructible\n    because the 5 s sample deadline fires first in every ordering that could be built.\n```\n","status":"active","title":"Adversary pass 2, unit u1 (metrics stream caps)"},"entity_type":"aep.review-result/v1","locator":"ep://planning/store/review-result/adversary-u1-pass-2"},"payload":{"actor":"human:timo","at":1788513070667,"causation":"cmd-new-review-result-adversary-u1-pass-2","change":{"change":"created","status":"active"},"correlation":"protocol-artifact-new","event_id":"review-result:adversary-u1-pass-2@1#0~7c5df98f8c88d238","executor":null,"recorded_at":"2026-09-04T09:11:10Z"}}
{"entity":"review-result","version":1,"id":"adversary-u1-pass-2","revision":1,"type":"aep.relation.create/v1","from_state":"active","to_state":"active","changed":{"relations":[{"reviews":"story:metrics-streams-are-bounded"}]},"args":{"command":"create-relation","kind":"reviews","source":"01MEM0000000000000738","target":"01MEM0000000000000172"},"payload":{"actor":"human:timo","at":1788513070677,"causation":"cmd-review-result-adversary-u1-pass-2-reviews-story-metrics-streams-are-bounded","change":{"change":"related","relation":"reviews","target":"story:metrics-streams-are-bounded"},"correlation":"protocol-artifact-relate","event_id":"review-result:adversary-u1-pass-2@1#0~9a1d63879f53ee16","executor":null,"recorded_at":"2026-09-04T09:11:10Z"}}
{"entity":"story","version":1,"id":"metrics-streams-are-bounded","revision":7,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"review_outcome","outcome":"fixed","review":"review-result:adversary-u1-pass-2","source":"review-result:adversary-u1-pass-2","target":"01MEM0000000000000175"},"payload":{"actor":"human:timo","at":1788513742000,"causation":"cmd-evidence-story-metrics-streams-are-bounded-review_outcome-1788513742741","change":{"change":"evidence","kind":"review_outcome","outcome":"fixed","review":"review-result:adversary-u1-pass-2","source":"review-result:adversary-u1-pass-2"},"correlation":"protocol-artifact-evidence","event_id":"story:metrics-streams-are-bounded@7#0~76192a6af78ad81f","executor":null,"recorded_at":"2026-09-04T09:22:22Z"}}
{"entity":"story","version":1,"id":"metrics-streams-are-bounded","revision":8,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"scope":[{"confidence":"cited","path":"crates/substrate-daemon/src/app/events.rs"},{"confidence":"cited","path":"crates/substrate-daemon/src/app/metrics.rs"},{"confidence":"cited","path":"crates/substrate-daemon/src/app/service.rs"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/substrate-daemon/src/app/events.rs"},{"confidence":"cited","path":"crates/substrate-daemon/src/app/metrics.rs"},{"confidence":"cited","path":"crates/substrate-daemon/src/app/service.rs"}]},"command":"update-entity","target":"01MEM0000000000000175"},"payload":{"actor":"human:timo","at":1788514955127,"causation":"cmd-protocol-artifact-scope-story-metrics-streams-are-bounded-1788514955127","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:metrics-streams-are-bounded@8#0~184c5b3a2c32829b","executor":null,"recorded_at":"2026-09-04T09:42:35Z"}}
{"entity":"story","version":1,"id":"metrics-streams-are-bounded","revision":9,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"scope":[{"confidence":"cited","path":"crates/substrate-daemon/src/app/events.rs"},{"confidence":"cited","path":"crates/substrate-daemon/src/app/metrics.rs"},{"confidence":"cited","path":"crates/substrate-daemon/src/app/service.rs"},{"confidence":"cited","path":"crates/substrate-daemon/src/app/tests.rs"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/substrate-daemon/src/app/events.rs"},{"confidence":"cited","path":"crates/substrate-daemon/src/app/metrics.rs"},{"confidence":"cited","path":"crates/substrate-daemon/src/app/service.rs"},{"confidence":"cited","path":"crates/substrate-daemon/src/app/tests.rs"}]},"command":"update-entity","target":"01MEM0000000000000175"},"payload":{"actor":"human:timo","at":1788514955334,"causation":"cmd-protocol-artifact-scope-story-metrics-streams-are-bounded-1788514955334","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:metrics-streams-are-bounded@9#0~d0418a02b41c001e","executor":null,"recorded_at":"2026-09-04T09:42:35Z"}}
{"entity":"story","version":1,"id":"metrics-streams-are-bounded","revision":10,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"scope":[{"confidence":"cited","path":"crates/substrate-daemon/src/app/events.rs"},{"confidence":"cited","path":"crates/substrate-daemon/src/app/metrics.rs"},{"confidence":"cited","path":"crates/substrate-daemon/src/app/service.rs"},{"confidence":"cited","path":"crates/substrate-daemon/src/app/tests.rs"},{"confidence":"cited","path":"crates/substrate-daemon/tests/metrics_stream_adversary.rs"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/substrate-daemon/src/app/events.rs"},{"confidence":"cited","path":"crates/substrate-daemon/src/app/metrics.rs"},{"confidence":"cited","path":"crates/substrate-daemon/src/app/service.rs"},{"confidence":"cited","path":"crates/substrate-daemon/src/app/tests.rs"},{"confidence":"cited","path":"crates/substrate-daemon/tests/metrics_stream_adversary.rs"}]},"command":"update-entity","target":"01MEM0000000000000175"},"payload":{"actor":"human:timo","at":1788514955542,"causation":"cmd-protocol-artifact-scope-story-metrics-streams-are-bounded-1788514955542","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:metrics-streams-are-bounded@10#0~dd8a6f459d594195","executor":null,"recorded_at":"2026-09-04T09:42:35Z"}}
{"entity":"story","version":1,"id":"metrics-streams-are-bounded","revision":11,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"scope":[{"confidence":"cited","path":"crates/substrate-daemon/src/app/events.rs"},{"confidence":"cited","path":"crates/substrate-daemon/src/app/metrics.rs"},{"confidence":"cited","path":"crates/substrate-daemon/src/app/service.rs"},{"confidence":"cited","path":"crates/substrate-daemon/src/app/tests.rs"},{"confidence":"cited","path":"crates/substrate-daemon/tests/metrics_stream_adversary.rs"},{"confidence":"cited","path":"website/docs/guides/storage-and-metrics.md"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/substrate-daemon/src/app/events.rs"},{"confidence":"cited","path":"crates/substrate-daemon/src/app/metrics.rs"},{"confidence":"cited","path":"crates/substrate-daemon/src/app/service.rs"},{"confidence":"cited","path":"crates/substrate-daemon/src/app/tests.rs"},{"confidence":"cited","path":"crates/substrate-daemon/tests/metrics_stream_adversary.rs"},{"confidence":"cited","path":"website/docs/guides/storage-and-metrics.md"}]},"command":"update-entity","target":"01MEM0000000000000175"},"payload":{"actor":"human:timo","at":1788514955754,"causation":"cmd-protocol-artifact-scope-story-metrics-streams-are-bounded-1788514955754","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:metrics-streams-are-bounded@11#0~5151c6c8300cc86d","executor":null,"recorded_at":"2026-09-04T09:42:35Z"}}
{"entity":"story","version":1,"id":"unattached-claimed-session-is-contained","revision":6,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"scope":[{"confidence":"cited","path":"crates/substrate-daemon/src/app/sessions.rs"},{"confidence":"cited","path":"crates/substrate-daemon/tests/pipe_session.rs"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/substrate-daemon/src/app/sessions.rs"},{"confidence":"cited","path":"crates/substrate-daemon/tests/pipe_session.rs"}]},"command":"update-entity","target":"01MEM0000000000000250"},"payload":{"actor":"human:timo","at":1788514955963,"causation":"cmd-protocol-artifact-scope-story-unattached-claimed-session-is-contained-1788514955963","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:unattached-claimed-session-is-contained@6#0~7f6a398d2fec074f","executor":null,"recorded_at":"2026-09-04T09:42:35Z"}}
{"entity":"story","version":1,"id":"confined-processes-cannot-nest-user-namespaces","revision":8,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"scope":[{"confidence":"cited","path":"AGENTS.md"},{"confidence":"cited","path":"crates/substrate-host/src/egress.rs"},{"confidence":"cited","path":"crates/substrate-host/src/probe.rs"},{"confidence":"cited","path":"crates/substrate-host/src/process.rs"},{"confidence":"cited","path":"crates/substrate-host/src/pty.rs"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"AGENTS.md"},{"confidence":"cited","path":"crates/substrate-host/src/egress.rs"},{"confidence":"cited","path":"crates/substrate-host/src/probe.rs"},{"confidence":"cited","path":"crates/substrate-host/src/process.rs"},{"confidence":"cited","path":"crates/substrate-host/src/pty.rs"}]},"command":"update-entity","target":"01MEM0000000000000094"},"payload":{"actor":"human:timo","at":1788514956171,"causation":"cmd-protocol-artifact-scope-story-confined-processes-cannot-nest-user-namespaces-1788514956171","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:confined-processes-cannot-nest-user-namespaces@8#0~08ef46442e38cd96","executor":null,"recorded_at":"2026-09-04T09:42:36Z"}}
{"entity":"story","version":1,"id":"confined-processes-cannot-nest-user-namespaces","revision":9,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"scope":[{"confidence":"cited","path":"AGENTS.md"},{"confidence":"cited","path":"README.md"},{"confidence":"cited","path":"crates/substrate-host/src/egress.rs"},{"confidence":"cited","path":"crates/substrate-host/src/probe.rs"},{"confidence":"cited","path":"crates/substrate-host/src/process.rs"},{"confidence":"cited","path":"crates/substrate-host/src/pty.rs"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"AGENTS.md"},{"confidence":"cited","path":"README.md"},{"confidence":"cited","path":"crates/substrate-host/src/egress.rs"},{"confidence":"cited","path":"crates/substrate-host/src/probe.rs"},{"confidence":"cited","path":"crates/substrate-host/src/process.rs"},{"confidence":"cited","path":"crates/substrate-host/src/pty.rs"}]},"command":"update-entity","target":"01MEM0000000000000094"},"payload":{"actor":"human:timo","at":1788514956381,"causation":"cmd-protocol-artifact-scope-story-confined-processes-cannot-nest-user-namespaces-1788514956381","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:confined-processes-cannot-nest-user-namespaces@9#0~813403b4b11fc85f","executor":null,"recorded_at":"2026-09-04T09:42:36Z"}}
{"entity":"story","version":1,"id":"confined-processes-cannot-nest-user-namespaces","revision":10,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"scope":[{"confidence":"cited","path":"AGENTS.md"},{"confidence":"cited","path":"README.md"},{"confidence":"cited","path":"crates/substrate-host/src/egress.rs"},{"confidence":"cited","path":"crates/substrate-host/src/probe.rs"},{"confidence":"cited","path":"crates/substrate-host/src/process.rs"},{"confidence":"cited","path":"crates/substrate-host/src/pty.rs"},{"confidence":"cited","path":"crates/substrate-host/tests/user_namespace_floor_documents.rs"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"AGENTS.md"},{"confidence":"cited","path":"README.md"},{"confidence":"cited","path":"crates/substrate-host/src/egress.rs"},{"confidence":"cited","path":"crates/substrate-host/src/probe.rs"},{"confidence":"cited","path":"crates/substrate-host/src/process.rs"},{"confidence":"cited","path":"crates/substrate-host/src/pty.rs"},{"confidence":"cited","path":"crates/substrate-host/tests/user_namespace_floor_documents.rs"}]},"command":"update-entity","target":"01MEM0000000000000094"},"payload":{"actor":"human:timo","at":1788514956589,"causation":"cmd-protocol-artifact-scope-story-confined-processes-cannot-nest-user-namespaces-1788514956589","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:confined-processes-cannot-nest-user-namespaces@10#0~1c01e8171c0f39d5","executor":null,"recorded_at":"2026-09-04T09:42:36Z"}}
{"entity":"story","version":1,"id":"confined-processes-cannot-nest-user-namespaces","revision":11,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"scope":[{"confidence":"cited","path":"AGENTS.md"},{"confidence":"cited","path":"README.md"},{"confidence":"cited","path":"crates/substrate-host/src/egress.rs"},{"confidence":"cited","path":"crates/substrate-host/src/probe.rs"},{"confidence":"cited","path":"crates/substrate-host/src/process.rs"},{"confidence":"cited","path":"crates/substrate-host/src/pty.rs"},{"confidence":"cited","path":"crates/substrate-host/tests/user_namespace_floor_documents.rs"},{"confidence":"cited","path":"docs/design/04-security-and-isolation.md"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"AGENTS.md"},{"confidence":"cited","path":"README.md"},{"confidence":"cited","path":"crates/substrate-host/src/egress.rs"},{"confidence":"cited","path":"crates/substrate-host/src/probe.rs"},{"confidence":"cited","path":"crates/substrate-host/src/process.rs"},{"confidence":"cited","path":"crates/substrate-host/src/pty.rs"},{"confidence":"cited","path":"crates/substrate-host/tests/user_namespace_floor_documents.rs"},{"confidence":"cited","path":"docs/design/04-security-and-isolation.md"}]},"command":"update-entity","target":"01MEM0000000000000094"},"payload":{"actor":"human:timo","at":1788514956796,"causation":"cmd-protocol-artifact-scope-story-confined-processes-cannot-nest-user-namespaces-1788514956796","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:confined-processes-cannot-nest-user-namespaces@11#0~af0d3ee7ea2f957d","executor":null,"recorded_at":"2026-09-04T09:42:36Z"}}
{"entity":"story","version":1,"id":"confined-processes-cannot-nest-user-namespaces","revision":12,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"scope":[{"confidence":"cited","path":"AGENTS.md"},{"confidence":"cited","path":"README.md"},{"confidence":"cited","path":"crates/substrate-host/src/egress.rs"},{"confidence":"cited","path":"crates/substrate-host/src/probe.rs"},{"confidence":"cited","path":"crates/substrate-host/src/process.rs"},{"confidence":"cited","path":"crates/substrate-host/src/pty.rs"},{"confidence":"cited","path":"crates/substrate-host/tests/user_namespace_floor_documents.rs"},{"confidence":"cited","path":"docs/design/04-security-and-isolation.md"},{"confidence":"cited","path":"docs/design/15-docker-driver-entry-gate.md"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"AGENTS.md"},{"confidence":"cited","path":"README.md"},{"confidence":"cited","path":"crates/substrate-host/src/egress.rs"},{"confidence":"cited","path":"crates/substrate-host/src/probe.rs"},{"confidence":"cited","path":"crates/substrate-host/src/process.rs"},{"confidence":"cited","path":"crates/substrate-host/src/pty.rs"},{"confidence":"cited","path":"crates/substrate-host/tests/user_namespace_floor_documents.rs"},{"confidence":"cited","path":"docs/design/04-security-and-isolation.md"},{"confidence":"cited","path":"docs/design/15-docker-driver-entry-gate.md"}]},"command":"update-entity","target":"01MEM0000000000000094"},"payload":{"actor":"human:timo","at":1788514957005,"causation":"cmd-protocol-artifact-scope-story-confined-processes-cannot-nest-user-namespaces-1788514957005","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:confined-processes-cannot-nest-user-namespaces@12#0~b700a60b004ba832","executor":null,"recorded_at":"2026-09-04T09:42:37Z"}}
{"entity":"story","version":1,"id":"upgraded-connections-keep-their-permit","revision":6,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"scope":[{"confidence":"cited","path":"crates/substrate-daemon/src/app/events.rs"},{"confidence":"cited","path":"crates/substrate-daemon/src/runtime.rs"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/substrate-daemon/src/app/events.rs"},{"confidence":"cited","path":"crates/substrate-daemon/src/runtime.rs"}]},"command":"update-entity","target":"01MEM0000000000000253"},"payload":{"actor":"human:timo","at":1788514957214,"causation":"cmd-protocol-artifact-scope-story-upgraded-connections-keep-their-permit-1788514957214","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:upgraded-connections-keep-their-permit@6#0~82bc0cb849a39978","executor":null,"recorded_at":"2026-09-04T09:42:37Z"}}
{"entity":"story","version":1,"id":"upgraded-connections-keep-their-permit","revision":7,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"scope":[{"confidence":"cited","path":"crates/substrate-daemon/src/app/events.rs"},{"confidence":"cited","path":"crates/substrate-daemon/src/app/sessions.rs"},{"confidence":"cited","path":"crates/substrate-daemon/src/runtime.rs"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/substrate-daemon/src/app/events.rs"},{"confidence":"cited","path":"crates/substrate-daemon/src/app/sessions.rs"},{"confidence":"cited","path":"crates/substrate-daemon/src/runtime.rs"}]},"command":"update-entity","target":"01MEM0000000000000253"},"payload":{"actor":"human:timo","at":1788514957424,"causation":"cmd-protocol-artifact-scope-story-upgraded-connections-keep-their-permit-1788514957424","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:upgraded-connections-keep-their-permit@7#0~9c61bd164996a4b7","executor":null,"recorded_at":"2026-09-04T09:42:37Z"}}
{"entity":"story","version":1,"id":"upgraded-connections-keep-their-permit","revision":8,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"scope":[{"confidence":"cited","path":"crates/substrate-daemon/src/app/events.rs"},{"confidence":"cited","path":"crates/substrate-daemon/src/app/metrics.rs"},{"confidence":"cited","path":"crates/substrate-daemon/src/app/sessions.rs"},{"confidence":"cited","path":"crates/substrate-daemon/src/runtime.rs"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/substrate-daemon/src/app/events.rs"},{"confidence":"cited","path":"crates/substrate-daemon/src/app/metrics.rs"},{"confidence":"cited","path":"crates/substrate-daemon/src/app/sessions.rs"},{"confidence":"cited","path":"crates/substrate-daemon/src/runtime.rs"}]},"command":"update-entity","target":"01MEM0000000000000253"},"payload":{"actor":"human:timo","at":1788514957635,"causation":"cmd-protocol-artifact-scope-story-upgraded-connections-keep-their-permit-1788514957635","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:upgraded-connections-keep-their-permit@8#0~0f404c5433de78e8","executor":null,"recorded_at":"2026-09-04T09:42:37Z"}}
{"entity":"story","version":1,"id":"session-containment-retries-an-unproven-kill","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Story: Session containment retries an unproven kill\n\n## Context\n\nWhen a claimed pipe session must be contained — the client dropped, or the upgrade failed — the\ndaemon makes **one** attempt to end the process tree and does not try again.\n`terminate_pipe_session` (`crates/substrate-daemon/src/app/sessions.rs`) returns `false` whenever\n`driver.signal` errors, exceeds `MAINTENANCE_DRIVER_TIMEOUT`, or the observation write fails. On\nthat branch nothing retries, and no maintenance sweep picks the session up.\n\nMeasured during the 2026-09-04 security wave (`review-result:adversary-u3-pass-1`): with a 500 ms\ntransient driver refusal, a stranded exec is still `\"running\"` after 10 s and roughly 200 explicit\n`App::sweep_expired()` ticks. The only backstop is `cleanup_expired`\n(`crates/substrate-daemon/src/app/service.rs:645`), which acts on an **expired lease** — the very\n\"until its lease or timeout ended it\" that `story:unattached-claimed-session-is-contained` calls the\ndefect it exists to remove.\n\nThe defect is pre-existing: it reproduces at `617bbed`, where the failed-upgrade path had no\ncontainment at all. `story:unattached-claimed-session-is-contained` made the situation strictly\nbetter and deliberately did not close this half.\n\n## Acceptance\n\nA claimed session whose first containment attempt is refused by the driver leaves no running process\nwithin one maintenance tick once the driver recovers, proven by a case that refuses the first signal\nand then allows the second.\n\n## Notes\n\n`crates/substrate-daemon/tests/pipe_session.rs` already carries\n`a_stranded_attach_whose_kill_is_unproven_is_only_ended_by_lease_expiry`, which pins today's\nbehaviour: the exec is still `running` across 20 driven maintenance ticks, and renewing to\n`MIN_LEASE_TTL_MS` then expiring yields `state == \"expired\"`. **That case goes red when this story\nlands. Invert it, never relax it** — its own doc says so and names this story.\n\n`sweep_expired` is the production tick (250 ms, `crates/substrate-daemon/src/runtime.rs:534`), so a\nbackstop placed in `sweep_expired`/`cleanup_expired` trips that case. A backstop on its own timer\nslower than the ~500 ms window would leave it green; the adversary judged that unlikely and did not\nfile it separately.\n","title":"Session containment retries an unproven kill"},"args":{"command":"create-entity","data":{"body":"# Story: Session containment retries an unproven kill\n\n## Context\n\nWhen a claimed pipe session must be contained — the client dropped, or the upgrade failed — the\ndaemon makes **one** attempt to end the process tree and does not try again.\n`terminate_pipe_session` (`crates/substrate-daemon/src/app/sessions.rs`) returns `false` whenever\n`driver.signal` errors, exceeds `MAINTENANCE_DRIVER_TIMEOUT`, or the observation write fails. On\nthat branch nothing retries, and no maintenance sweep picks the session up.\n\nMeasured during the 2026-09-04 security wave (`review-result:adversary-u3-pass-1`): with a 500 ms\ntransient driver refusal, a stranded exec is still `\"running\"` after 10 s and roughly 200 explicit\n`App::sweep_expired()` ticks. The only backstop is `cleanup_expired`\n(`crates/substrate-daemon/src/app/service.rs:645`), which acts on an **expired lease** — the very\n\"until its lease or timeout ended it\" that `story:unattached-claimed-session-is-contained` calls the\ndefect it exists to remove.\n\nThe defect is pre-existing: it reproduces at `617bbed`, where the failed-upgrade path had no\ncontainment at all. `story:unattached-claimed-session-is-contained` made the situation strictly\nbetter and deliberately did not close this half.\n\n## Acceptance\n\nA claimed session whose first containment attempt is refused by the driver leaves no running process\nwithin one maintenance tick once the driver recovers, proven by a case that refuses the first signal\nand then allows the second.\n\n## Notes\n\n`crates/substrate-daemon/tests/pipe_session.rs` already carries\n`a_stranded_attach_whose_kill_is_unproven_is_only_ended_by_lease_expiry`, which pins today's\nbehaviour: the exec is still `running` across 20 driven maintenance ticks, and renewing to\n`MIN_LEASE_TTL_MS` then expiring yields `state == \"expired\"`. **That case goes red when this story\nlands. Invert it, never relax it** — its own doc says so and names this story.\n\n`sweep_expired` is the production tick (250 ms, `crates/substrate-daemon/src/runtime.rs:534`), so a\nbackstop placed in `sweep_expired`/`cleanup_expired` trips that case. A backstop on its own timer\nslower than the ~500 ms window would leave it green; the adversary judged that unlikely and did not\nfile it separately.\n","status":"draft","title":"Session containment retries an unproven kill"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/session-containment-retries-an-unproven-kill"},"payload":{"actor":"human:timo","at":1788514967603,"causation":"cmd-new-story-session-containment-retries-an-unproven-kill","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:session-containment-retries-an-unproven-kill@1#0~769e15954e5cd209","executor":null,"recorded_at":"2026-09-04T09:42:47Z"}}
{"entity":"story","version":1,"id":"the-daemons-detached-containment-task-is-tracked","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Story: The daemon's detached containment task is tracked\n\n## Context\n\n`crates/substrate-daemon/src/app/sessions.rs` contains the only detached `tokio::spawn` in the\ndaemon's non-test `app/` code. It runs the session containment that follows a failed WebSocket\nupgrade. Verified by grep during the 2026-09-04 security wave: `tokio::spawn|spawn_blocking|JoinSet`\nacross non-test `src/app/` returns exactly that one site.\n\nIt is outside the connection `JoinSet` that `crates/substrate-daemon/src/runtime.rs:621` aborts at\nshutdown, so a containment in flight when the daemon stops is neither awaited nor cancelled — it is\ndropped with the runtime.\n\n`story:unattached-claimed-session-is-contained` added the logging half of this\n(`review-result:adversary-u3-pass-1`, finding F4) and could not add the tracking half: `runtime.rs`\nbelonged to another unit in that wave.\n\n## Acceptance\n\nA containment task still running when the daemon shuts down is either awaited or cancelled through\nthe same `JoinSet` that owns the connection tasks, proven by a case that starts one and shuts down\nunder it.\n\n## Notes\n\nThe task already emits `tracing::info!` on containment and `tracing::warn!` on an unproven one, each\ncarrying `exec` and the `axum::Error`. What is missing is ownership, not observability.\n\nConsider whether the task belongs in `runtime.rs:621`'s existing `JoinSet` or in a maintenance-owned\none; the first keeps one shutdown path, the second does not tie a session's containment to the\nlifetime of the connection that triggered it.\n","title":"The daemon's detached containment task is tracked"},"args":{"command":"create-entity","data":{"body":"# Story: The daemon's detached containment task is tracked\n\n## Context\n\n`crates/substrate-daemon/src/app/sessions.rs` contains the only detached `tokio::spawn` in the\ndaemon's non-test `app/` code. It runs the session containment that follows a failed WebSocket\nupgrade. Verified by grep during the 2026-09-04 security wave: `tokio::spawn|spawn_blocking|JoinSet`\nacross non-test `src/app/` returns exactly that one site.\n\nIt is outside the connection `JoinSet` that `crates/substrate-daemon/src/runtime.rs:621` aborts at\nshutdown, so a containment in flight when the daemon stops is neither awaited nor cancelled — it is\ndropped with the runtime.\n\n`story:unattached-claimed-session-is-contained` added the logging half of this\n(`review-result:adversary-u3-pass-1`, finding F4) and could not add the tracking half: `runtime.rs`\nbelonged to another unit in that wave.\n\n## Acceptance\n\nA containment task still running when the daemon shuts down is either awaited or cancelled through\nthe same `JoinSet` that owns the connection tasks, proven by a case that starts one and shuts down\nunder it.\n\n## Notes\n\nThe task already emits `tracing::info!` on containment and `tracing::warn!` on an unproven one, each\ncarrying `exec` and the `axum::Error`. What is missing is ownership, not observability.\n\nConsider whether the task belongs in `runtime.rs:621`'s existing `JoinSet` or in a maintenance-owned\none; the first keeps one shutdown path, the second does not tie a session's containment to the\nlifetime of the connection that triggered it.\n","status":"draft","title":"The daemon's detached containment task is tracked"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/the-daemons-detached-containment-task-is-tracked"},"payload":{"actor":"human:timo","at":1788514968010,"causation":"cmd-new-story-the-daemons-detached-containment-task-is-tracked","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:the-daemons-detached-containment-task-is-tracked@1#0~4e52209c664025b0","executor":null,"recorded_at":"2026-09-04T09:42:48Z"}}
{"entity":"story","version":1,"id":"runtime-vector-cgroup-teardown-is-flaky","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Story: The runtime-vector delegated lane tears its cgroups down deterministically\n\n## Context\n\n`crates/substrate-daemon/tests/runtime_vectors.rs` fails intermittently on its delegated lane with\n\n```\nruntime_vectors.rs:78: per-test daemon cgroup … was not empty at teardown: Device or resource busy\n```\n\nMeasured during the 2026-09-04 security wave, on `617bbed` itself: **1 red of 2** on the base\ncommit, and 2 red of 4 on a branch whose diff touches neither `/v1/execs` nor cgroup teardown\n(`review-result:adversary-u3-pass-1`). One red additionally showed `runtime_vectors.rs:1224`\nreturning `500 operation.outcome-unknown` on exec 53 of 129.\n\nThe lane is the only proof that the shipped binary answers the wire as its vectors say. A lane that\nfails one run in two cannot be read as evidence either way, and it teaches every agent and every CI\nconsumer to re-run rather than to look.\n\n## Acceptance\n\n`bash scripts/delegated-lane.sh` run ten times consecutively on an unchanged tree exits 0 ten times.\n\n## Notes\n\n`Device or resource busy` on an rmdir of a cgroup directory means a process is still in it, or a\ncontroller is still attached. The teardown is racing something it does not wait for — most likely\nthe exec's own children, or the daemon's cleanup of the per-test group.\n\nThis is a test-harness defect, not a daemon defect, unless the investigation shows the daemon leaves\nthe group populated after it reports the exec terminal — in which case it is the more interesting\nfinding and this story should say so.\n","title":"The runtime-vector delegated lane tears its cgroups down deterministically"},"args":{"command":"create-entity","data":{"body":"# Story: The runtime-vector delegated lane tears its cgroups down deterministically\n\n## Context\n\n`crates/substrate-daemon/tests/runtime_vectors.rs` fails intermittently on its delegated lane with\n\n```\nruntime_vectors.rs:78: per-test daemon cgroup … was not empty at teardown: Device or resource busy\n```\n\nMeasured during the 2026-09-04 security wave, on `617bbed` itself: **1 red of 2** on the base\ncommit, and 2 red of 4 on a branch whose diff touches neither `/v1/execs` nor cgroup teardown\n(`review-result:adversary-u3-pass-1`). One red additionally showed `runtime_vectors.rs:1224`\nreturning `500 operation.outcome-unknown` on exec 53 of 129.\n\nThe lane is the only proof that the shipped binary answers the wire as its vectors say. A lane that\nfails one run in two cannot be read as evidence either way, and it teaches every agent and every CI\nconsumer to re-run rather than to look.\n\n## Acceptance\n\n`bash scripts/delegated-lane.sh` run ten times consecutively on an unchanged tree exits 0 ten times.\n\n## Notes\n\n`Device or resource busy` on an rmdir of a cgroup directory means a process is still in it, or a\ncontroller is still attached. The teardown is racing something it does not wait for — most likely\nthe exec's own children, or the daemon's cleanup of the per-test group.\n\nThis is a test-harness defect, not a daemon defect, unless the investigation shows the daemon leaves\nthe group populated after it reports the exec terminal — in which case it is the more interesting\nfinding and this story should say so.\n","status":"draft","title":"The runtime-vector delegated lane tears its cgroups down deterministically"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/runtime-vector-cgroup-teardown-is-flaky"},"payload":{"actor":"human:timo","at":1788514968334,"causation":"cmd-new-story-runtime-vector-cgroup-teardown-is-flaky","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:runtime-vector-cgroup-teardown-is-flaky@1#0~0bddf657e2aeaf16","executor":null,"recorded_at":"2026-09-04T09:42:48Z"}}
{"entity":"story","version":1,"id":"the-delegated-lane-script-honours-cargo-target-dir","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Story: The delegated lane script honours CARGO_TARGET_DIR\n\n## Context\n\n`scripts/delegated-lane.sh` builds and then invokes `${PWD}/target/debug/substrate-daemon` by a\nhardcoded path. Under any external `CARGO_TARGET_DIR` — which every worktree-per-unit workflow sets,\nbecause two trees must never share one build directory — the binary is not there and the step fails\nto find what it just built.\n\nFound during the 2026-09-04 security wave: unit u4 could not run the full lane for this reason and\nran a hand-copied host half instead, in the wave's own scratch directory: a runner that\nreproduces the script's systemd-scope, controller check and process-free-root setup verbatim and\nthen runs only the first `cargo test`. That is a correct workaround and a bad standing arrangement:\nthe copy drifts from the script it was copied from, and it lives outside the repository, so nothing\nhere can see it rot.\n\n## Acceptance\n\n`bash scripts/delegated-lane.sh` completes its daemon, host, SDK and MCP steps with\n`CARGO_TARGET_DIR` set to a directory outside the repository.\n\n## Notes\n\n`cargo build --message-format=json` reports the executable path it produced, which removes the\nguess entirely. Failing that, `${CARGO_TARGET_DIR:-$PWD/target}` is the one-line form.\n\nThe same script also builds three packages beyond the daemon, which is why a package-scoped unit\ncannot run it whole. Worth deciding whether the lane should take a package filter.\n","title":"The delegated lane script honours CARGO_TARGET_DIR"},"args":{"command":"create-entity","data":{"body":"# Story: The delegated lane script honours CARGO_TARGET_DIR\n\n## Context\n\n`scripts/delegated-lane.sh` builds and then invokes `${PWD}/target/debug/substrate-daemon` by a\nhardcoded path. Under any external `CARGO_TARGET_DIR` — which every worktree-per-unit workflow sets,\nbecause two trees must never share one build directory — the binary is not there and the step fails\nto find what it just built.\n\nFound during the 2026-09-04 security wave: unit u4 could not run the full lane for this reason and\nran a hand-copied host half instead, in the wave's own scratch directory: a runner that\nreproduces the script's systemd-scope, controller check and process-free-root setup verbatim and\nthen runs only the first `cargo test`. That is a correct workaround and a bad standing arrangement:\nthe copy drifts from the script it was copied from, and it lives outside the repository, so nothing\nhere can see it rot.\n\n## Acceptance\n\n`bash scripts/delegated-lane.sh` completes its daemon, host, SDK and MCP steps with\n`CARGO_TARGET_DIR` set to a directory outside the repository.\n\n## Notes\n\n`cargo build --message-format=json` reports the executable path it produced, which removes the\nguess entirely. Failing that, `${CARGO_TARGET_DIR:-$PWD/target}` is the one-line form.\n\nThe same script also builds three packages beyond the daemon, which is why a package-scoped unit\ncannot run it whole. Worth deciding whether the lane should take a package filter.\n","status":"draft","title":"The delegated lane script honours CARGO_TARGET_DIR"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/the-delegated-lane-script-honours-cargo-target-dir"},"payload":{"actor":"human:timo","at":1788514968646,"causation":"cmd-new-story-the-delegated-lane-script-honours-cargo-target-dir","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:the-delegated-lane-script-honours-cargo-target-dir@1#0~c07117947c7c39ae","executor":null,"recorded_at":"2026-09-04T09:42:48Z"}}
{"entity":"story","version":1,"id":"design-10-cites-a-stale-process-line","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Story: Design 10's egress evidence row cites the argv where it is\n\n## Context\n\n`docs/design/10-destination-bound-egress.md:34` cites\n`crates/substrate-host/src/process.rs:901-905` as the evidence for the sandbox argv. The argv is at\n`process.rs:1902-1928`. The row is byte-identical at `617bbed`, so the citation was already wrong\nbefore the 2026-09-04 security wave; it was found by that wave's adversarial passes over\n`story:confined-processes-cannot-nest-user-namespaces`\n(`review-result:adversary-u4-pass-1`, `-pass-2`).\n\nThe same row states the posture as \"`--unshare-net` with `--unshare-user/ipc/pid/uts`\". That is no\nlonger the whole argv: the exec and all seven other bubblewrap argv lists now also carry\n`--disable-userns`, spliced from `crates/substrate-host/src/process.rs`'s `USER_NAMESPACE_ARGV`.\n\n## Acceptance\n\n`docs/design/10-destination-bound-egress.md`'s evidence row cites the line range where the sandbox\nargv actually is and states the posture the code builds, checked by `cargo xtask check-links` and by\nreading.\n\n## Notes\n\n`cargo xtask check-links` verifies that a repository-relative target exists; it does not verify that\na cited line number says what the citing document claims. A citation that drifts is invisible to the\ngate, which is why this needed an adversary to find and why a line-range citation in a design\ndocument is a liability worth reviewing more widely than this one row.\n","title":"Design 10's egress evidence row cites the argv where it is"},"args":{"command":"create-entity","data":{"body":"# Story: Design 10's egress evidence row cites the argv where it is\n\n## Context\n\n`docs/design/10-destination-bound-egress.md:34` cites\n`crates/substrate-host/src/process.rs:901-905` as the evidence for the sandbox argv. The argv is at\n`process.rs:1902-1928`. The row is byte-identical at `617bbed`, so the citation was already wrong\nbefore the 2026-09-04 security wave; it was found by that wave's adversarial passes over\n`story:confined-processes-cannot-nest-user-namespaces`\n(`review-result:adversary-u4-pass-1`, `-pass-2`).\n\nThe same row states the posture as \"`--unshare-net` with `--unshare-user/ipc/pid/uts`\". That is no\nlonger the whole argv: the exec and all seven other bubblewrap argv lists now also carry\n`--disable-userns`, spliced from `crates/substrate-host/src/process.rs`'s `USER_NAMESPACE_ARGV`.\n\n## Acceptance\n\n`docs/design/10-destination-bound-egress.md`'s evidence row cites the line range where the sandbox\nargv actually is and states the posture the code builds, checked by `cargo xtask check-links` and by\nreading.\n\n## Notes\n\n`cargo xtask check-links` verifies that a repository-relative target exists; it does not verify that\na cited line number says what the citing document claims. A citation that drifts is invisible to the\ngate, which is why this needed an adversary to find and why a line-range citation in a design\ndocument is a liability worth reviewing more widely than this one row.\n","status":"draft","title":"Design 10's egress evidence row cites the argv where it is"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/design-10-cites-a-stale-process-line"},"payload":{"actor":"human:timo","at":1788514968981,"causation":"cmd-new-story-design-10-cites-a-stale-process-line","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:design-10-cites-a-stale-process-line@1#0~422bccce7d7c4f2c","executor":null,"recorded_at":"2026-09-04T09:42:48Z"}}
{"entity":"story","version":1,"id":"the-attachment-permit-names-the-id-it-holds","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Story: The attachment permit names the id it holds\n\n## Context\n\n`crates/substrate-daemon/src/app/sessions.rs:91` — `PipeAttachmentPermit.exec_id` — and\n`PipeAttachmentLimits::acquire(scope, exec_id)` are both passed a **session** id: `sessions.rs:1170`\npasses `&session_id`. In the same file, `terminate_pipe_session(app, scope, exec_id)` takes a real\nexec id.\n\nNothing breaks: the insert and the `Drop` remove use the same key, whatever it is called. Found\nduring the 2026-09-04 security wave (`review-result:adversary-u3-pass-2`), three lines from the\npermit machinery that wave changed, which is exactly where a reader will be when the name misleads\nthem.\n\n## Acceptance\n\nEvery parameter and field in the attachment-permit machinery is named for the kind of id it holds,\nand no session id is passed to a parameter named `exec_id`.\n\n## Notes\n\nRename only; no behaviour changes and no case should need to move. If the two kinds of id are the\nsame type today, consider whether they should be — a newtype makes this class of mistake a compile\nerror rather than a reading exercise.\n","title":"The attachment permit names the id it holds"},"args":{"command":"create-entity","data":{"body":"# Story: The attachment permit names the id it holds\n\n## Context\n\n`crates/substrate-daemon/src/app/sessions.rs:91` — `PipeAttachmentPermit.exec_id` — and\n`PipeAttachmentLimits::acquire(scope, exec_id)` are both passed a **session** id: `sessions.rs:1170`\npasses `&session_id`. In the same file, `terminate_pipe_session(app, scope, exec_id)` takes a real\nexec id.\n\nNothing breaks: the insert and the `Drop` remove use the same key, whatever it is called. Found\nduring the 2026-09-04 security wave (`review-result:adversary-u3-pass-2`), three lines from the\npermit machinery that wave changed, which is exactly where a reader will be when the name misleads\nthem.\n\n## Acceptance\n\nEvery parameter and field in the attachment-permit machinery is named for the kind of id it holds,\nand no session id is passed to a parameter named `exec_id`.\n\n## Notes\n\nRename only; no behaviour changes and no case should need to move. If the two kinds of id are the\nsame type today, consider whether they should be — a newtype makes this class of mistake a compile\nerror rather than a reading exercise.\n","status":"draft","title":"The attachment permit names the id it holds"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/the-attachment-permit-names-the-id-it-holds"},"payload":{"actor":"human:timo","at":1788514969407,"causation":"cmd-new-story-the-attachment-permit-names-the-id-it-holds","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:the-attachment-permit-names-the-id-it-holds@1#0~afc3b3d9b0db1f5b","executor":null,"recorded_at":"2026-09-04T09:42:49Z"}}
{"entity":"story","version":1,"id":"the-metrics-stream-cap-is-machine-readable","revision":1,"type":"aep.entity.create/v1","from_state":null,"to_state":"draft","changed":{"body":"# Story: The metrics stream cap is machine-readable\n\n## Context\n\n`story:metrics-streams-are-bounded` added a client-visible `429` with the code\n`metrics.stream-capacity` to `GET /v1/metrics/stream`. The cap of 4 per subject and 64 per\ndeployment, and the code itself, exist in daemon source and — since that story — in\n`website/docs/guides/storage-and-metrics.md`. **A client cannot read either from anything the wire\npublishes.**\n\nNo bundle byte was owed and none was written: `contracts/substrate-wire/0.15.0/refusals.json` is\n`b10x.substrate-session-refusals.v1`, 36 rows, every prefix `session`. The sibling cap\n`event.stream-capacity` is in the same position, so this is the second instance of one gap rather\nthan a new one. Confirmed twice during the 2026-09-04 security wave\n(`review-result:adversary-u1-pass-1`, `-pass-2`), both times agreeing that prose is the only\npublication available today.\n\n## Acceptance\n\nA client reading only what the wire publishes can discover the metrics stream's per-subject and\nglobal caps and the refusal code that names them.\n\n## Notes\n\nThis is a **successor bundle plus an ADR** under invariant 8, not a line: it needs a decision about\nwhere non-session refusals are published, which reaches `event.stream-capacity` too. Invariant 6\nforbids editing a released bundle, so `0.16.0` is the earliest home.\n\nWorth deciding at the same time whether stream capacity belongs in `refusals.json` or in the\ncapability facts a client already reads before it opens a stream.\n","title":"The metrics stream cap is machine-readable"},"args":{"command":"create-entity","data":{"body":"# Story: The metrics stream cap is machine-readable\n\n## Context\n\n`story:metrics-streams-are-bounded` added a client-visible `429` with the code\n`metrics.stream-capacity` to `GET /v1/metrics/stream`. The cap of 4 per subject and 64 per\ndeployment, and the code itself, exist in daemon source and — since that story — in\n`website/docs/guides/storage-and-metrics.md`. **A client cannot read either from anything the wire\npublishes.**\n\nNo bundle byte was owed and none was written: `contracts/substrate-wire/0.15.0/refusals.json` is\n`b10x.substrate-session-refusals.v1`, 36 rows, every prefix `session`. The sibling cap\n`event.stream-capacity` is in the same position, so this is the second instance of one gap rather\nthan a new one. Confirmed twice during the 2026-09-04 security wave\n(`review-result:adversary-u1-pass-1`, `-pass-2`), both times agreeing that prose is the only\npublication available today.\n\n## Acceptance\n\nA client reading only what the wire publishes can discover the metrics stream's per-subject and\nglobal caps and the refusal code that names them.\n\n## Notes\n\nThis is a **successor bundle plus an ADR** under invariant 8, not a line: it needs a decision about\nwhere non-session refusals are published, which reaches `event.stream-capacity` too. Invariant 6\nforbids editing a released bundle, so `0.16.0` is the earliest home.\n\nWorth deciding at the same time whether stream capacity belongs in `refusals.json` or in the\ncapability facts a client already reads before it opens a stream.\n","status":"draft","title":"The metrics stream cap is machine-readable"},"entity_type":"aep.story/v1","locator":"ep://planning/store/story/the-metrics-stream-cap-is-machine-readable"},"payload":{"actor":"human:timo","at":1788514969724,"causation":"cmd-new-story-the-metrics-stream-cap-is-machine-readable","change":{"change":"created","status":"draft"},"correlation":"protocol-artifact-new","event_id":"story:the-metrics-stream-cap-is-machine-readable@1#0~692449b72e35e2be","executor":null,"recorded_at":"2026-09-04T09:42:49Z"}}
{"entity":"story","version":1,"id":"session-containment-retries-an-unproven-kill","revision":2,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"scope":[{"confidence":"cited","path":"crates/substrate-daemon/src/app/sessions.rs"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/substrate-daemon/src/app/sessions.rs"}]},"command":"update-entity","target":"01MEM0000000000000247"},"payload":{"actor":"human:timo","at":1788514977190,"causation":"cmd-protocol-artifact-scope-story-session-containment-retries-an-unproven-kill-1788514977190","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:session-containment-retries-an-unproven-kill@2#0~6bf80ce39da4a0df","executor":null,"recorded_at":"2026-09-04T09:42:57Z"}}
{"entity":"story","version":1,"id":"session-containment-retries-an-unproven-kill","revision":3,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"scope":[{"confidence":"cited","path":"crates/substrate-daemon/src/app/service.rs"},{"confidence":"cited","path":"crates/substrate-daemon/src/app/sessions.rs"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/substrate-daemon/src/app/service.rs"},{"confidence":"cited","path":"crates/substrate-daemon/src/app/sessions.rs"}]},"command":"update-entity","target":"01MEM0000000000000247"},"payload":{"actor":"human:timo","at":1788514977554,"causation":"cmd-protocol-artifact-scope-story-session-containment-retries-an-unproven-kill-1788514977554","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:session-containment-retries-an-unproven-kill@3#0~709ab0c130e42302","executor":null,"recorded_at":"2026-09-04T09:42:57Z"}}
{"entity":"story","version":1,"id":"session-containment-retries-an-unproven-kill","revision":4,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"scope":[{"confidence":"cited","path":"crates/substrate-daemon/src/app/service.rs"},{"confidence":"cited","path":"crates/substrate-daemon/src/app/sessions.rs"},{"confidence":"cited","path":"crates/substrate-daemon/tests/pipe_session.rs"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/substrate-daemon/src/app/service.rs"},{"confidence":"cited","path":"crates/substrate-daemon/src/app/sessions.rs"},{"confidence":"cited","path":"crates/substrate-daemon/tests/pipe_session.rs"}]},"command":"update-entity","target":"01MEM0000000000000247"},"payload":{"actor":"human:timo","at":1788514977935,"causation":"cmd-protocol-artifact-scope-story-session-containment-retries-an-unproven-kill-1788514977935","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:session-containment-retries-an-unproven-kill@4#0~8e14c03d342696fa","executor":null,"recorded_at":"2026-09-04T09:42:57Z"}}
{"entity":"story","version":1,"id":"the-daemons-detached-containment-task-is-tracked","revision":2,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"scope":[{"confidence":"cited","path":"crates/substrate-daemon/src/app/sessions.rs"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/substrate-daemon/src/app/sessions.rs"}]},"command":"update-entity","target":"01MEM0000000000000259"},"payload":{"actor":"human:timo","at":1788514978418,"causation":"cmd-protocol-artifact-scope-story-the-daemons-detached-containment-task-is-tracked-1788514978418","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:the-daemons-detached-containment-task-is-tracked@2#0~d9813adabe1a1880","executor":null,"recorded_at":"2026-09-04T09:42:58Z"}}
{"entity":"story","version":1,"id":"the-daemons-detached-containment-task-is-tracked","revision":3,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"scope":[{"confidence":"cited","path":"crates/substrate-daemon/src/app/sessions.rs"},{"confidence":"cited","path":"crates/substrate-daemon/src/runtime.rs"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/substrate-daemon/src/app/sessions.rs"},{"confidence":"cited","path":"crates/substrate-daemon/src/runtime.rs"}]},"command":"update-entity","target":"01MEM0000000000000259"},"payload":{"actor":"human:timo","at":1788514978940,"causation":"cmd-protocol-artifact-scope-story-the-daemons-detached-containment-task-is-tracked-1788514978940","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:the-daemons-detached-containment-task-is-tracked@3#0~edde0fa068fa320a","executor":null,"recorded_at":"2026-09-04T09:42:58Z"}}
{"entity":"story","version":1,"id":"runtime-vector-cgroup-teardown-is-flaky","revision":2,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"scope":[{"confidence":"cited","path":"crates/substrate-daemon/tests/runtime_vectors.rs"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/substrate-daemon/tests/runtime_vectors.rs"}]},"command":"update-entity","target":"01MEM0000000000000217"},"payload":{"actor":"human:timo","at":1788514979661,"causation":"cmd-protocol-artifact-scope-story-runtime-vector-cgroup-teardown-is-flaky-1788514979661","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:runtime-vector-cgroup-teardown-is-flaky@2#0~0428dcf94f557c17","executor":null,"recorded_at":"2026-09-04T09:42:59Z"}}
{"entity":"story","version":1,"id":"the-delegated-lane-script-honours-cargo-target-dir","revision":2,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"scope":[{"confidence":"cited","path":"scripts/delegated-lane.sh"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"scripts/delegated-lane.sh"}]},"command":"update-entity","target":"01MEM0000000000000262"},"payload":{"actor":"human:timo","at":1788514980069,"causation":"cmd-protocol-artifact-scope-story-the-delegated-lane-script-honours-cargo-target-dir-1788514980069","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:the-delegated-lane-script-honours-cargo-target-dir@2#0~b9fb3e12eaa590e5","executor":null,"recorded_at":"2026-09-04T09:43:00Z"}}
{"entity":"story","version":1,"id":"design-10-cites-a-stale-process-line","revision":2,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"scope":[{"confidence":"cited","path":"docs/design/10-destination-bound-egress.md"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"docs/design/10-destination-bound-egress.md"}]},"command":"update-entity","target":"01MEM0000000000000112"},"payload":{"actor":"human:timo","at":1788514980513,"causation":"cmd-protocol-artifact-scope-story-design-10-cites-a-stale-process-line-1788514980513","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:design-10-cites-a-stale-process-line@2#0~6b11cd37241a835e","executor":null,"recorded_at":"2026-09-04T09:43:00Z"}}
{"entity":"story","version":1,"id":"the-attachment-permit-names-the-id-it-holds","revision":2,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"scope":[{"confidence":"cited","path":"crates/substrate-daemon/src/app/sessions.rs"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/substrate-daemon/src/app/sessions.rs"}]},"command":"update-entity","target":"01MEM0000000000000256"},"payload":{"actor":"human:timo","at":1788514980983,"causation":"cmd-protocol-artifact-scope-story-the-attachment-permit-names-the-id-it-holds-1788514980983","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:the-attachment-permit-names-the-id-it-holds@2#0~5793d7da74839151","executor":null,"recorded_at":"2026-09-04T09:43:00Z"}}
{"entity":"story","version":1,"id":"the-metrics-stream-cap-is-machine-readable","revision":2,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"scope":[{"confidence":"cited","path":"crates/substrate-daemon/src/app/metrics.rs"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/substrate-daemon/src/app/metrics.rs"}]},"command":"update-entity","target":"01MEM0000000000000265"},"payload":{"actor":"human:timo","at":1788514981344,"causation":"cmd-protocol-artifact-scope-story-the-metrics-stream-cap-is-machine-readable-1788514981344","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:the-metrics-stream-cap-is-machine-readable@2#0~863671d52ebb31dd","executor":null,"recorded_at":"2026-09-04T09:43:01Z"}}
{"entity":"story","version":1,"id":"the-metrics-stream-cap-is-machine-readable","revision":3,"type":"aep.entity.update/v1","from_state":"draft","to_state":"draft","changed":{"scope":[{"confidence":"cited","path":"crates/substrate-daemon/src/app/events.rs"},{"confidence":"cited","path":"crates/substrate-daemon/src/app/metrics.rs"}]},"args":{"changes":{"scope":[{"confidence":"cited","path":"crates/substrate-daemon/src/app/events.rs"},{"confidence":"cited","path":"crates/substrate-daemon/src/app/metrics.rs"}]},"command":"update-entity","target":"01MEM0000000000000265"},"payload":{"actor":"human:timo","at":1788514981960,"causation":"cmd-protocol-artifact-scope-story-the-metrics-stream-cap-is-machine-readable-1788514981960","change":{"change":"body_replaced"},"correlation":"protocol-artifact-scope","event_id":"story:the-metrics-stream-cap-is-machine-readable@3#0~e8300aa8739ad34e","executor":null,"recorded_at":"2026-09-04T09:43:01Z"}}
{"entity":"story","version":1,"id":"metrics-streams-are-bounded","revision":12,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"body":"# Story: Metrics streams are bounded\n\n## Context\n\n`GET /v1/metrics/stream` upgrades to a WebSocket with no permit, no lifetime and the library's\ndefault frame and message bounds (`crates/substrate-daemon/src/app/metrics.rs:75-90`). Each open\nstream runs `load_exec_usage` once a second, which is an `observe_exec` plus a `put_exec` SQLite\nwrite (`metrics.rs:98-` and `:215-`), until the exec ends, up to the 24 h exec timeout. Event\nstreams are capped at 64 global and 4 per subject (`app/events.rs:47-48`) and pipe attachments at\n32 (`app/sessions.rs:63`); metrics streams at nothing. One authenticated uid can open as many as\nthe kernel allows.\n\n## Acceptance\n\nOpening one more metrics stream than a published per-subject cap answers `429` with a named `exhausted` refusal.\n\n## Notes\n\nReuse the `EventStreamLimits` shape and `EventStreamPolicy` bounds; the metrics stream is the one upgrade that has none of them. The lifetime and the client-message bound arrive with the same policy struct and are checked by their own cases, not by the acceptance above.\n\n## Parallel work\n\nThis story shares `crates/substrate-daemon/src/app/service.rs` with\nstory:lease-cleanup-reads-exec-state-only; the two touch different functions (stream limits on\n`App` versus `cleanup_expired`) but land on one file, so they are worked in sequence, not at once.\n\n**What the 2026-09-04 wave learned, which no scope entry expressed.** This story's own class check\nreads every `.rs` file under `crates/substrate-daemon/src/app/`, so it is coupled to\n`app/sessions.rs` and `app/events.rs` even though it edits neither. Working this story beside one\nthat restructures an upgrade builder chain in those files turns the package gate green on both\nbranches and red on the merge. That happened: story:unattached-claimed-session-is-contained\ninserted a closure between `sessions.rs`'s frame bounds and its `.on_upgrade(`, and the check read\nan empty chain. The check now skips balanced brackets, but the coupling is a property of a check\nthat reads its siblings, not of that one defect.\n"},"args":{"changes":{"body":"# Story: Metrics streams are bounded\n\n## Context\n\n`GET /v1/metrics/stream` upgrades to a WebSocket with no permit, no lifetime and the library's\ndefault frame and message bounds (`crates/substrate-daemon/src/app/metrics.rs:75-90`). Each open\nstream runs `load_exec_usage` once a second, which is an `observe_exec` plus a `put_exec` SQLite\nwrite (`metrics.rs:98-` and `:215-`), until the exec ends, up to the 24 h exec timeout. Event\nstreams are capped at 64 global and 4 per subject (`app/events.rs:47-48`) and pipe attachments at\n32 (`app/sessions.rs:63`); metrics streams at nothing. One authenticated uid can open as many as\nthe kernel allows.\n\n## Acceptance\n\nOpening one more metrics stream than a published per-subject cap answers `429` with a named `exhausted` refusal.\n\n## Notes\n\nReuse the `EventStreamLimits` shape and `EventStreamPolicy` bounds; the metrics stream is the one upgrade that has none of them. The lifetime and the client-message bound arrive with the same policy struct and are checked by their own cases, not by the acceptance above.\n\n## Parallel work\n\nThis story shares `crates/substrate-daemon/src/app/service.rs` with\nstory:lease-cleanup-reads-exec-state-only; the two touch different functions (stream limits on\n`App` versus `cleanup_expired`) but land on one file, so they are worked in sequence, not at once.\n\n**What the 2026-09-04 wave learned, which no scope entry expressed.** This story's own class check\nreads every `.rs` file under `crates/substrate-daemon/src/app/`, so it is coupled to\n`app/sessions.rs` and `app/events.rs` even though it edits neither. Working this story beside one\nthat restructures an upgrade builder chain in those files turns the package gate green on both\nbranches and red on the merge. That happened: story:unattached-claimed-session-is-contained\ninserted a closure between `sessions.rs`'s frame bounds and its `.on_upgrade(`, and the check read\nan empty chain. The check now skips balanced brackets, but the coupling is a property of a check\nthat reads its siblings, not of that one defect.\n"},"command":"update-entity","target":"01MEM0000000000000178"},"payload":{"actor":"human:timo","at":1788515004471,"causation":"cmd-protocol-artifact-body-section-story-metrics-streams-are-bounded-1788515004471","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:metrics-streams-are-bounded@12#0~c1d5d5c03fb8f8e6","executor":null,"recorded_at":"2026-09-04T09:43:24Z"}}
{"entity":"story","version":1,"id":"confined-processes-cannot-nest-user-namespaces","revision":13,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"body":"# Story: Confined processes cannot nest user namespaces\n\n## Context\n\nThe exec argv passes `--unshare-user` and not `--disable-userns`\n(`crates/substrate-host/src/process.rs:1813-1838`); the three probe argv lists in `probe.rs`,\n`pty.rs` and `egress.rs` match it. The installed bubblewrap 0.11.2 lists `--disable-userns` and\n`--assert-userns-disabled` (`bwrap --help`). A confined process can therefore create nested user\nnamespaces, which is the entry point of most unprivileged kernel privilege escalations. The\ncurrent seccomp profile does not block `unshare` or `clone` with `CLONE_NEWUSER`\n(`crates/substrate-host/src/seccomp.rs:70-99`).\n\n## Acceptance\n\nInside an admitted exec on this host, `unshare -U` fails, observed by a delegated-lane case.\n\n## Notes\n\nInvariant 3: a bubblewrap too old for `--disable-userns` is a named refusal, never a sandbox without it. The probe therefore passes `--assert-userns-disabled` and withholds the exec facts when it fails; that path is delivered in this story and checked by its own probe case, separate from the acceptance above.\n\n## Parallel work\n\nThis story shares `crates/substrate-host/src/probe.rs` with\nstory:backend-recheck-hashes-only-on-change, story:daemon-image-serves-exec-or-says-it-cannot,\nstory:exec-oom-kills-the-whole-tree and story:seccomp-denies-af-vsock; three of them also share\n`crates/substrate-host/src/process.rs`. Work them one at a time, or in one wave by one implementor;\n`aep artifact waves` sequences them.\n"},"args":{"changes":{"body":"# Story: Confined processes cannot nest user namespaces\n\n## Context\n\nThe exec argv passes `--unshare-user` and not `--disable-userns`\n(`crates/substrate-host/src/process.rs:1813-1838`); the three probe argv lists in `probe.rs`,\n`pty.rs` and `egress.rs` match it. The installed bubblewrap 0.11.2 lists `--disable-userns` and\n`--assert-userns-disabled` (`bwrap --help`). A confined process can therefore create nested user\nnamespaces, which is the entry point of most unprivileged kernel privilege escalations. The\ncurrent seccomp profile does not block `unshare` or `clone` with `CLONE_NEWUSER`\n(`crates/substrate-host/src/seccomp.rs:70-99`).\n\n## Acceptance\n\nInside an admitted exec on this host, `unshare -U` fails, observed by a delegated-lane case.\n\n## Notes\n\nInvariant 3: a bubblewrap too old for `--disable-userns` is a named refusal, never a sandbox without it. The probe therefore passes `--assert-userns-disabled` and withholds the exec facts when it fails; that path is delivered in this story and checked by its own probe case, separate from the acceptance above.\n\n## Parallel work\n\nThis story shares `crates/substrate-host/src/probe.rs` with\nstory:backend-recheck-hashes-only-on-change, story:daemon-image-serves-exec-or-says-it-cannot,\nstory:exec-oom-kills-the-whole-tree and story:seccomp-denies-af-vsock; three of them also share\n`crates/substrate-host/src/process.rs`. Work them one at a time, or in one wave by one implementor;\n`aep artifact waves` sequences them.\n"},"command":"update-entity","target":"01MEM0000000000000094"},"payload":{"actor":"human:timo","at":1788515004700,"causation":"cmd-protocol-artifact-body-section-story-confined-processes-cannot-nest-user-namespaces-1788515004700","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:confined-processes-cannot-nest-user-namespaces@13#0~bed889c00dd5f3f9","executor":null,"recorded_at":"2026-09-04T09:43:24Z"}}
{"entity":"story","version":1,"id":"confined-processes-cannot-nest-user-namespaces","revision":14,"type":"aep.entity.update/v1","from_state":"active","to_state":"active","changed":{"body":"# Story: Confined processes cannot nest user namespaces\n\n## Context\n\nThe exec argv passes `--unshare-user` and not `--disable-userns`\n(`crates/substrate-host/src/process.rs`); the probe argv lists in `probe.rs`, `pty.rs` and\n`egress.rs` match it. The installed bubblewrap 0.11.2 lists `--disable-userns` and\n`--assert-userns-disabled` (`bwrap --help`). A confined process can therefore create nested user\nnamespaces, which is the entry point of most unprivileged kernel privilege escalations. The\ncurrent seccomp profile does not block `unshare` or `clone` with `CLONE_NEWUSER`\n(`crates/substrate-host/src/seccomp.rs`).\n\n**Correction, 2026-09-04:** this section previously said \"the three probe argv lists\". There are\n**seven** bubblewrap argv sites in the crate, not three — `probe.rs` builds three, `pty.rs` one,\n`egress.rs` two, and `process.rs` the exec one. An eighth exists in `egress.rs`'s own test module.\nThe implementor found this while taking the story and the count is now spliced from a single\nconstant, so the number cannot drift again without a red case.\n\n## Acceptance\n\nInside an admitted exec on this host, `unshare -U` fails, observed by a delegated-lane case.\n\n## Notes\n\nInvariant 3: a bubblewrap too old for `--disable-userns` is a named refusal, never a sandbox without it. The probe therefore passes `--assert-userns-disabled` and withholds the exec facts when it fails; that path is delivered in this story and checked by its own probe case, separate from the acceptance above.\n\n## Parallel work\n\nThis story shares `crates/substrate-host/src/probe.rs` with\nstory:backend-recheck-hashes-only-on-change, story:daemon-image-serves-exec-or-says-it-cannot,\nstory:exec-oom-kills-the-whole-tree and story:seccomp-denies-af-vsock; three of them also share\n`crates/substrate-host/src/process.rs`. Work them one at a time, or in one wave by one implementor;\n`aep artifact waves` sequences them.\n"},"args":{"changes":{"body":"# Story: Confined processes cannot nest user namespaces\n\n## Context\n\nThe exec argv passes `--unshare-user` and not `--disable-userns`\n(`crates/substrate-host/src/process.rs`); the probe argv lists in `probe.rs`, `pty.rs` and\n`egress.rs` match it. The installed bubblewrap 0.11.2 lists `--disable-userns` and\n`--assert-userns-disabled` (`bwrap --help`). A confined process can therefore create nested user\nnamespaces, which is the entry point of most unprivileged kernel privilege escalations. The\ncurrent seccomp profile does not block `unshare` or `clone` with `CLONE_NEWUSER`\n(`crates/substrate-host/src/seccomp.rs`).\n\n**Correction, 2026-09-04:** this section previously said \"the three probe argv lists\". There are\n**seven** bubblewrap argv sites in the crate, not three — `probe.rs` builds three, `pty.rs` one,\n`egress.rs` two, and `process.rs` the exec one. An eighth exists in `egress.rs`'s own test module.\nThe implementor found this while taking the story and the count is now spliced from a single\nconstant, so the number cannot drift again without a red case.\n\n## Acceptance\n\nInside an admitted exec on this host, `unshare -U` fails, observed by a delegated-lane case.\n\n## Notes\n\nInvariant 3: a bubblewrap too old for `--disable-userns` is a named refusal, never a sandbox without it. The probe therefore passes `--assert-userns-disabled` and withholds the exec facts when it fails; that path is delivered in this story and checked by its own probe case, separate from the acceptance above.\n\n## Parallel work\n\nThis story shares `crates/substrate-host/src/probe.rs` with\nstory:backend-recheck-hashes-only-on-change, story:daemon-image-serves-exec-or-says-it-cannot,\nstory:exec-oom-kills-the-whole-tree and story:seccomp-denies-af-vsock; three of them also share\n`crates/substrate-host/src/process.rs`. Work them one at a time, or in one wave by one implementor;\n`aep artifact waves` sequences them.\n"},"command":"update-entity","target":"01MEM0000000000000094"},"payload":{"actor":"human:timo","at":1788515004917,"causation":"cmd-protocol-artifact-body-section-story-confined-processes-cannot-nest-user-namespaces-1788515004917","change":{"change":"body_replaced"},"correlation":"protocol-artifact-body-section","event_id":"story:confined-processes-cannot-nest-user-namespaces@14#0~651ceaa2dd38ec11","executor":null,"recorded_at":"2026-09-04T09:43:24Z"}}
{"entity":"story","version":1,"id":"confined-processes-cannot-nest-user-namespaces","revision":14,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"0e40524","source":"bash scripts/gate.sh, per step, on wave/security-2026-09-04","target":"01MEM0000000000000094"},"payload":{"actor":"human:timo","at":1788515012000,"causation":"cmd-evidence-story-confined-processes-cannot-nest-user-namespaces-test_result-1788515012479","change":{"change":"evidence","kind":"test_result","reference":"0e40524","source":"bash scripts/gate.sh, per step, on wave/security-2026-09-04"},"correlation":"protocol-artifact-evidence","event_id":"story:confined-processes-cannot-nest-user-namespaces@14#0~a23558cde4929512","executor":null,"recorded_at":"2026-09-04T09:43:32Z"}}
{"entity":"story","version":1,"id":"unattached-claimed-session-is-contained","revision":6,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"160b1df","source":"bash scripts/gate.sh, per step, on wave/security-2026-09-04","target":"01MEM0000000000000271"},"payload":{"actor":"human:timo","at":1788515012000,"causation":"cmd-evidence-story-unattached-claimed-session-is-contained-test_result-1788515012728","change":{"change":"evidence","kind":"test_result","reference":"160b1df","source":"bash scripts/gate.sh, per step, on wave/security-2026-09-04"},"correlation":"protocol-artifact-evidence","event_id":"story:unattached-claimed-session-is-contained@6#0~12b7285b5f708e6b","executor":null,"recorded_at":"2026-09-04T09:43:32Z"}}
{"entity":"story","version":1,"id":"metrics-streams-are-bounded","revision":12,"type":"aep.evidence.record/v1","from_state":"active","to_state":"active","changed":{},"args":{"command":"record-evidence","kind":"test_result","reference":"9b919c0","source":"bash scripts/gate.sh, per step, on wave/security-2026-09-04","target":"01MEM0000000000000178"},"payload":{"actor":"human:timo","at":1788515012000,"causation":"cmd-evidence-story-metrics-streams-are-bounded-test_result-1788515012949","change":{"change":"evidence","kind":"test_result","reference":"9b919c0","source":"bash scripts/gate.sh, per step, on wave/security-2026-09-04"},"correlation":"protocol-artifact-evidence","event_id":"story:metrics-streams-are-bounded@12#0~58c1f62e19ea63c4","executor":null,"recorded_at":"2026-09-04T09:43:32Z"}}
{"entity":"story","version":1,"id":"confined-processes-cannot-nest-user-namespaces","revision":15,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1,\"review_outcome\":3}}","target":"01MEM0000000000000094","to":"implemented"},"payload":{"actor":"human:timo","at":1788515013260,"causation":"cmd-move-story-confined-processes-cannot-nest-user-namespaces-1788515013260","change":{"change":"moved","decided_on":{"recorded":{"review_outcome":3,"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:confined-processes-cannot-nest-user-namespaces@15#0~8aded67c5bf2dd5a","executor":null,"recorded_at":"2026-09-04T09:43:33Z"}}
{"entity":"story","version":1,"id":"unattached-claimed-session-is-contained","revision":7,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1,\"review_outcome\":3}}","target":"01MEM0000000000000271","to":"implemented"},"payload":{"actor":"human:timo","at":1788515013563,"causation":"cmd-move-story-unattached-claimed-session-is-contained-1788515013563","change":{"change":"moved","decided_on":{"recorded":{"review_outcome":3,"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:unattached-claimed-session-is-contained@7#0~c964cef42dc55a7f","executor":null,"recorded_at":"2026-09-04T09:43:33Z"}}
{"entity":"story","version":1,"id":"metrics-streams-are-bounded","revision":13,"type":"aep.status.move/v1","from_state":"active","to_state":"implemented","changed":{},"args":{"command":"move-status","decided_on":"{\"recorded\":{\"test_result\":1,\"review_outcome\":4}}","target":"01MEM0000000000000178","to":"implemented"},"payload":{"actor":"human:timo","at":1788515013867,"causation":"cmd-move-story-metrics-streams-are-bounded-1788515013867","change":{"change":"moved","decided_on":{"recorded":{"review_outcome":4,"test_result":1}},"from":"active","to":"implemented"},"correlation":"protocol-artifact-move","event_id":"story:metrics-streams-are-bounded@13#0~e1edb08c238ae720","executor":null,"recorded_at":"2026-09-04T09:43:33Z"}}
