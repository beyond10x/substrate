[workspace]
members = [
    "crates/substrate-wire",
    "crates/substrate-store",
    "crates/substrate-host",
    "crates/substrate-daemon",
    "crates/substrate-contract-check",
    "crates/b10x-substrate-sdk",
    "crates/substrate-mcp",
    "xtask",
]
resolver = "2"

[workspace.package]
edition = "2024"
license = "Apache-2.0"
repository = "https://github.com/beyond10x/substrate"
rust-version = "1.97"
version = "0.7.0"

[workspace.dependencies]
anyhow = "1.0"
async-trait = "0.1"
axum = { version = "0.8", default-features = false, features = ["http1", "json", "query", "tokio", "ws"] }
base64 = "0.23"
bytes = "1.10"
chrono = { version = "0.4", default-features = false, features = ["clock", "serde", "std"] }
clap = { version = "4.5", features = ["derive", "env"] }
ed25519-dalek = "3.0"
fancy-regex = "0.19"
hex = "0.4"
futures-util = { version = "0.3", features = ["sink"] }
getrandom = "0.4"
http = "1.3"
http-body-util = "0.1"
jsonschema = { version = "=0.52.1", default-features = false }
hyper = { version = "1.7", features = ["client", "http1", "server"] }
hyper-util = { version = "0.1", features = ["service", "tokio"] }
libc = "0.2"
nix = { version = "0.31", features = ["fs", "ioctl", "term", "user"] }
parking_lot = "0.12"
pulldown-cmark = "0.13"
rusqlite = { version = "0.40", features = ["bundled"] }
rmcp = { version = "=3.2.0", default-features = false }
rustls = { version = "0.23", default-features = false, features = ["aws_lc_rs", "std"] }
rustls-pemfile = "2.2"
rustsec = { version = "0.33", default-features = false }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
serde_urlencoded = "0.7"
sha2 = "0.11"
zeroize = "1"
subtle = "2.6"
tempfile = "3.20"
thiserror = "2.0"
tokio = { version = "1.47", features = ["fs", "io-std", "io-util", "macros", "net", "process", "rt-multi-thread", "signal", "sync", "test-util", "time"] }
tokio-rustls = { version = "0.26", default-features = false, features = ["aws_lc_rs"] }
tokio-tungstenite = { version = "0.30", default-features = false, features = ["handshake"] }
toml = "1.0"
tower = { version = "0.5", features = ["util"] }
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter", "fmt"] }
ulid = { version = "3.0", features = ["serde"] }
x509-parser = { version = "0.18", default-features = false }

[workspace.lints.rust]
unsafe_op_in_unsafe_fn = "deny"

[workspace.lints.clippy]
all = { level = "deny", priority = -1 }
pedantic = { level = "deny", priority = -1 }
module_name_repetitions = "allow"
must_use_candidate = "allow"

[patch.crates-io]
# Axum 0.8.9 leaves Hyper's default features on even when the application selects only Axum's
# `http1` feature. The local source is the published crate with only those two dependency-default
# switches disabled plus one source-spelling-only secret-scan fixture split;
# `vendor/axum-0.8.9/README.substrate.md` records the fence.
axum = { path = "vendor/axum-0.8.9" }
